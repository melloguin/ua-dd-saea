# handoff/R1-e103.md — Rodada 1, fan-out FINAL: e103 IBEA-MS (standalone do autor, **OFFLINE** — L.15/N.3/D90/D93; 2º worker de path dedicado)

**Data:** 2026-07-18 · **Env:** MATLAB **R2025a** (Mac arm64) + PONTE `pyenv` InProcess
(`~/ponte_teste/bin/python`, 3.11 — round-trip → 6 verificado ANTES, D81; **ponte usada SÓ para
metadados** D/M/bounds — zero avaliação real; Statistics Toolbox — kmeans/pdist/pdist2; SEM Deep
Learning). Leitor Python independente no **env-main** (`.../mestrado_experimentos_dissertacao/bin/python`).

**Gate objetivo (encanamento, D97):** `accept.py R1-e103 --alg e103 --problema {MMF1,DTLZ2,ZDT1}
--semente 0` → **exit 0 (VERDE)** nos três: 4 saídas, **① = 31D−1 linhas = o DATASET completo**,
CP-init offline bit-a-bit (x_hash **E** f_hash vs sidecar do ds). ✅ **FECHA A RODADA 1 MATLAB.**
**Fidelidade:** instrumentada e entregue para a validação MANUAL do autor (D97) — **não é gate meu**.
**PROVA DE NÃO-CONTAMINAÇÃO DE PATH: ✅** — no MESMO processo MATLAB (4.15 pré-carregada),
`c217 → e103 → nsga2 → b3` todos VERDES; após CADA estágio, zero resíduos e103 no `which` e
`dacefit` volta ao K-RVEA da 4.15 (o call-site do b3 — a vítima natural da sombra DACE) (§4).
**Regressão:** R1-00 stub (accept_r1_00 0 falhas) + c217, c141, b1, b3, b4, e7, c238, e74 + os 4
pisos (todos ×MMF1) VERDES via accept.py (12 gates exit 0); F0-01/02/03/04 exit 0;
`preflight.py --write` exit 0 (3 âncoras e103 = APLICADO; `e103_ibeams` re-hashado
`7ab83a7e…`); suíte **90 testes OK** (1 skip). Gates do c154/c262 NÃO rodados (faixa paralela).

**Escopo:** 10º e ÚLTIMO cartão MATLAB da R1, e o ÚNICO **OFFLINE** dela: o IBEA-MS treina UMA
vez no dataset injetado (D90, 31D−1) e busca SÓ no surrogate — **ZERO FE real na busca** (o
FEBudget é a rede: orçamento esgotado na carga; qualquer avaliação nova = `PlatEMO:Termination`
tratada como VIOLAÇÃO do regime, pára-e-loga — nunca engolida como término). Infra transversal
INTOCADA. Eu NÃO julguei fidelidade (D97).

---

## 1. Resultado do gate (por problema, semente 0)

| Problema | D | M | n_ds=31D−1 | ①=n_ds | CP x/f | ger | KFlag=1 | membros ds na pop (g1→g99) | fit (krig+rbfn) | busca | wall total | accept.py |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **MMF1** | 2 | 2 | 61 | ✅ | ✅/✅ | 99 | **0/99** | 50 → 29 | 0,2 s | 4,4 s | **8,9 s** | **VERDE** |
| **DTLZ2** | 12 | 3 | 371 | ✅ | ✅/✅ | 99 | **0/99** | 75 → 15 | 4,8 s | 6,7 s | **15,3 s** | **VERDE** |
| **ZDT1** | 30 | 2 | 929 | ✅ | ✅/✅ | 99 | **99/99** | 82 → **0 (na g5)** | 24,0 s | 11,7 s | **36,9 s** | **VERDE** |

`cache_hits=0` nos três (não há probe no e103 — standalone sem UserProblem; a carga do dataset é
livre de duplicata, checada ANTES — hazard dacefit). **99 gerações** = o off-by-one do código
(`while CurGen<100` com `CurGen=1` — CÓDIGO, registrado). **Wall p/ M7: o e103 é o run MAIS LEVE
da R1** — ZDT1 em 37 s (vs 3h55 do c238, 8 min do e74); extrapolação 30 sementes ZDT1 ≈ **19
min/core**. O gargalo é o fit único do DACE (24 s em n=929, D=30) — nunca retreina.

## 2. O regime OFFLINE na prática (o desenho do runner)

1. **O orçamento É o dataset:** `FEBudget(D, maxfe=31D−1, n_init=31D−1)`; as 31D−1 linhas do
   `ds_{p}_{s}.parquet` (D90, tier small/lhs — nome sem sufixo) entram uma a uma na carga
   (fase `init`), com evalFcn devolvendo o **F do artefato** (a ponte NUNCA avalia — F canônico
   do `problems.py` já congelado no ds). Após a carga o saldo está ESGOTADO.
2. **ZERO FE na busca, garantido por construção:** nada no caminho do e103 chama `bud.evaluate`;
   se algo chamar com X inédita, o hard-stop levanta `PlatEMO:Termination`, que o `run_e103`
   NÃO engole — loga `violacao D90` no footer e rethrow (pára-e-loga D81).
3. **① = o dataset** (bit-exato; auditado por pyarrow: X e F == artefato após o cast float32
   D53). **CP-init offline** = sha256 float64 do X carregado == `x_hash` **E** do F == `f_hash`
   do sidecar (mais forte que o CP online, que só cobre X).
4. **Ponte metadata-only:** `py_problem` fornece D/M/bounds p/ o CP-bounds (§5.5) contra o
   sidecar do ds; higiene D86 mantida por uniformidade.
5. **Pré-checagens pára-e-loga:** n_ds==31D−1 · bounds==sidecar · `unique(X,'rows')==n_ds`
   (duplicata → dacefit fatal — hazard L.15) · tier/dist do sidecar == small/lhs.

## 3. Patches aplicados (por arquivo:linha, numeração STOCK)

**Em `algorithms/e103_IBEA-MS/` (repo do autor; content-hash re-hashado `7ab83a7e…`):**
- `IBEAMS.m:1` — **L.15(d)**: `function IBEAMS(Global)` → `[FinalDec,FinalObj] = IBEAMS(Global)`
  (o `OutPopulation` stock era descartado); no fim, `FinalDec/FinalObj = decs/objs(Population)`.
- `IBEAMS.m:23-31` — **L.15(a)**: hardcodes `Global.D=20/M=3/N=100/problem='DTLZ1'/bounds[0,1]`
  REMOVIDOS — D/M/N/lower/upper vêm do chamador (N=100 mantido — Balde B); `Global.problem` não
  existe mais (único consumidor era o `Fitness`, eliminado pela injeção). kappa/Generations ficam.
- `IBEAMS.m:32-33` — **L.15(b)**: `cd(fileparts(...)); addpath(genpath(cd))` →
  `addpath(genpath(fileparts(mfilename('fullpath'))))` — **o CWD do processo NÃO muda** (o
  hazard nº 1 do N.3); o path é montado/restaurado por fora pelo `ensure_paths_e103`.
- `IBEAMS.m:35-36` — **L.15(c)**: `LHS_sam`+`Fitness` → `Population = PopStruct(Global.data.X0,
  Global.data.F0)` (dataset D90 injetado). `MSE=zeros` (:39 stock) marca os membros como "reais"
  — `AmendKriCal.m:6`/`AmendRBFCal.m:5` preservam o Obj de quem tem SumMSE==0 (✓ por construção).
- `IBEAMS.m:43` — **âncora `e103-pm-D93a` (🔴 D30 — ARTIGO)**: `{1,20,1/Global.D,20}` →
  **`{1,20,1,20}`** (proM=1). O `GA.m:69` divide proM por D DE NOVO (convenção PlatEMO) → o
  stock efetivava pm=1/D²; com proM=1, pm efetiva = **1/D** (paper). `GA.m` INTACTO.
- `JudgeModel.m:32` — **âncora `e103-judgemodel` (🔴 D30 — ARTIGO)**: `Msite =
  (sum(site,3)>=(M-1));` → **`Msite = (sum(site,3)>=(M-1)) | logical(eye(N));`** — exclui os
  pares i==j do teste (na diagonal I=0 e MSEI=6√MSE_i → qualquer MSE_i>0 reprovava o par i==i e
  zerava o `all(all(Msite))` → KFlag=1 raríssimo, o viés estrutural pró-RBFN do S.2-e103).
  ⚠ **CONCRETIZAÇÃO SINALIZADA P/ SEU VETO** (§8.2).
- `construct_Rnets.m:4` — **âncora `e103-centros-D93` (a que mata o NO-OP)**:
  `ceil(sqrt(Global.D*11-1))` → **`ceil(sqrt(Global.n_dataset))`** — capacidade ∝ √(tamanho REAL
  do dataset), com o **n INJETADO** pelo wrapper (31D−1 no principal; 2k/50k no sweep).
- `construct_Rnets.m:10` — **§22-e103**: `inv(Z'*Z)*Z'*PopObj` → **`(Z'*Z)\(Z'*PopObj)`**
  (mldivide; a quase-singularidade continua ESPERADA — spread 5·max(pdist) quase-plano — e é
  CONTADA no `e103_setup`, nunca silenciada).
- `IBEAMS.m` (3 pontos) — hooks `e103_instrument` **read-only** guardados por
  `isfield(Global,'inst')` (setup / fim-de-ciclo / pós-laço) + tic/tocs de fit; captura
  `MSE_julgada` ANTES do ramo KFlag=0 sobrescrever OffMSE. ZERO decisão alterada.

**O que FICA stock (registrado no manifesto):** off-by-one 99 gerações; relaxação UF≥M−1 (custa
acurácia em M=2 — M.6/D73, limitação declarada); spread 5·max(pdist) (kernel quase-plano);
kernel da RBFN despachado por `eval()` de string; `AmendKriCal` duplica ponto se N==1
(workaround stock) e REORDENA a pop (membros do dataset p/ o fim); Kriging B7.7 (regpoly1+
corrgauss, θ0=10, [1e-3,1e3]); `CalFitness` com objetivo constante na pop → NaN (hazard stock,
não consertado — falha honesta se disparar); `OffMSE=ones` do ramo RBFN = MARCADOR, não σ.

**Integração [IMPL] (repo raiz):**
- `src/experiment.m` — `case 'e103' → run_e103` (§2) + `ensure_paths_e103` (§4) +
  `load_dataset` + `nm_dataset_path/nm_dataset_manifest_path` (espelho do naming.py D90).
- `src/e103_instrument.m` (**novo**) — ②③/jsonl/timing (§5).
- `anchors.json` — as 3 âncoras e103 **concretizadas** (preflight = APLICADO ×3).
- `repos.lock` — `e103_ibeams.sha256_tree` re-hashado via `preflight.py --write`.

## 4. O worker de path dedicado (molde e74, 2ª instância)

**Varredura de sombra (find por basename, 2026-07-18):** `CalFitness.m` ×62 e
`EnvironmentalSelection.m` ×155 na `_PlatEMO` 4.15; o DACE inteiro (`dacefit`/`predictor` ×29,
`regpoly*`/`corr*` ×2 — os call-sites do b1/b3!); `GA.m` (single-objective/GA) e
`TournamentSelection.m` (Utility functions da 4.15 **e** CLMEA_Code 4.1); `dsmerge.m` ×2
(c141/AB-SAEA). **Zero colisão com `src/*.m`.** `ensure_paths_e103`:
1. `prev = path` → 2. `rmpath(genpath(_PlatEMO))` **E** `rmpath(genpath(CLMEA_Code))` (as duas
   árvores-gorila; no-op se ausentes) → 3. `addpath(genpath(e103_IBEA-MS))` POR ÚLTIMO
   (precedência sobre pastas remanescentes de c141/c238) → 4. asserts `which -all` sobre **25
   símbolos** (1º hit DENTRO do e103 E zero resoluções remanescentes em _PlatEMO/CLMEA_Code —
   sombra ABORTA, não corrompe; smoke no header do `.jsonl`) → 5. `onCleanup(path(prev))`.
**Sem `cd` em lugar NENHUM** (o L.15(b) removeu o do IBEAMS; CWD intocado).

**Prova (mesmo processo, 4.15 pré-carregada = pior caso):**
```
[1] c217  ok  9,4s fe=61 cp=1   → which limpo (0 resíduos e103)
[2] e103  ok  5,6s fe=61 cp=1   → which limpo
[3] nsga2 ok  0,6s fe=61 cp=1   → which limpo
[4] b3    ok  4,6s fe=61 cp=1   → which('dacefit') = K-RVEA da 4.15 ✓
```
Asserts do script (não inspeção): pós-estágio, `which` de IBEAMS/JudgeModel/construct_Rnets/
RBFN_cal/kriging_cal VAZIOS e dacefit/predictor/CalFitness/EnvironmentalSelection/GA/
TournamentSelection/UserProblem/UniformPoint NUNCA resolvem p/ `e103_IBEA-MS`.

## 5. Instrumentação produzida (para a SUA validação de fidelidade — D97)

Por run: **① real** = o DATASET (31D−1, fase `init`, bit-exato) · **② pop** = os MEMBROS DO
DATASET presentes na população selecionada, por geração (`solutionIdOf` bit-exato; geração sem
nenhum membro real não tem linha — no ZDT1 os membros somem na g5; cross-check ↔ `n_ds_membros`
do jsonl auditado 99/99 gerações) · **③ surrogate** (DEF-C2 offline: pop SELECIONADA, TODAS as
gerações): **2 linhas por membro×geração — o μ dos DOIS modelos** (L.15/C4): `Kriging-DACE`
(μ=predictor por objetivo, σ=`sqrt(max(MSE,0))`, guard `mse_neg` conta MSE<0 do erro float) e
`RBFN` (μ=RBFN_cal **INCONDICIONAL** — mesmo quando o Kriging liderou; σ NULL — RBF sem
incerteza). Re-predição read-only sobre os decs selecionados; membros do dataset inclusos
(DACE interpola exato — max|Δ| = 0,0 vs F real, auditado). Volume ③: 19.800 linhas/run (2×100×99).
**Dicionário completo no `sigma_dict` do manifesto** (inclui: o MSE no MECANISMO é gate booleano
de ORDEM + marcador real/surrogate — não confundir com o σ da ③; KFlag por geração no jsonl).

**`.jsonl` §17.5/S.7:** `e103_setup` (n_dataset, center_num, spread, θ min/med/max e σ² do DACE
por objetivo, tempos de fit, último warning do treino) · linha **`e103_gen` por geração**
(CurGen, **KFlag**/modelo_lider, n_julgados, n_sel, n_ds_membros, **√MSE** — max do conjunto
julgado + min/med/max do subconjunto surrogate + max da seleção, n_mse_neg, fe) · `e103_busca`
(tempo do laço) · guards: `quase_singular_setup` (warning esperado da RBFN — CONTADO),
`mse_neg`, `cache_hit`/`hard_stop` (FEBudget — rede de violação offline). **Timing §17.6:** 1
linha (treino único): `n_acumulado=n_ds`, `tempo_fit_s`=kriging+RBFN, `tempo_busca_s`=laço.

**Números por problema (semente 0) — GUIA, não gate (D97):**

| Problema | centros | spread | θ_med por obj | σ²_DACE | KFlag=1 | √MSE_surr med (g1→g99) | warn setup |
|---|---|---|---|---|---|---|---|
| MMF1 (d=2) | 8 | 9,18 | [1,83 · 50] | [4,7e-2 · 2,7] | **0/99** | 0,017→0,077 | nearlySingularMatrix (1×) |
| DTLZ2 (d=12, M=3) | 20 | 7,82 | [9,0 · 1,9 · 3,2] | [5,2e-2 · 4,4e-2 · 3,7e-2] | **0/99** | 0,196→0,199 | nenhum |
| ZDT1 (d=30) | 31 | 7,32 | [10 · 10] | [**3,8e-32** · 1,2e-2] | **99/99** | 0,056→0,058 | nenhum |

> ⚠ **CAVEATS para a sua leitura (D97 — eu não julgo):**
> 1. **O fix do JudgeModel funciona como mecanismo, e a seleção agora DISCRIMINA:** no stock, a
>    diagonal zerava o teste → RBFN ~sempre. Com o fix, o ZDT1 escolhe **Kriging 99/99** (f₀=x₁
>    → o DACE interpola exato, σ²=3,8e-32; diferenças ≫ 3σ) e MMF1/DTLZ2 escolhem **RBFN 0/99**
>    (√MSE grande vs as diferenças par-a-par — o gate 3σ reprova a ordem). A distribuição
>    KFlag por problema é o material central da sua leitura §17.5.1#1.
> 2. **Membros do dataset na população:** MMF1 50→29, DTLZ2 75→15, ZDT1 82→**0 na g5** (os
>    candidatos ótimos-no-modelo dominam os reais rapidamente no ZDT1 — o "erro de fantasia"
>    em câmera lenta; cruzável ③×① pelos real_solution_id).
> 3. **A âncora J (ZDT1 IGD 4,596e-3) NÃO é comparável a este run**: o setup do paper usa
>    dataset 11D−1 e computa IGD sobre o ND final AVALIADO — nosso dataset é 31D−1 (D93 muda a
>    capacidade da RBFN de propósito) e a avaliação real do ND final não acontece neste run
>    (§8.1). A comparação justa é do MECANISMO (KFlag/3σ/relaxação — item 1).
> 4. **FinalObj é do MODELO** (última seleção; membros do dataset preservam F real). Os decs
>    finais estão na ③ (geração 99) e em FinalDec — nada se perde p/ a avaliação pós-hoc.
> 5. **Wall-clock semente 0:** 8,9 / 15,3 / 36,9 s — o e103 é o run mais BARATO da R1 (M7);
>    o custo é o fit único do DACE (O(n³): 0,06→4,7→24,0 s em n=61→371→929 — a curva §17.6
>    do sweep vai enxergar isso).
> 6. Divergências código×paper mantidas (CÓDIGO): off-by-one 99 ger; relaxação M−1 (M.6 —
>    custa acurácia em M=2, metade dos nossos problemas; limitação p/ a §15); spread
>    quase-plano; eval() no dispatch do kernel — registradas no manifesto.

## 6. Como reproduzir

```
# MATLAB (raiz do repo) — o run_e103 troca o path sozinho e RESTAURA ao fim:
addpath('src'); addpath(genpath('algorithms/_PlatEMO/PlatEMO'));   % 4.15 pode estar no path
experiment('e103','MMF1',0,'main','data')     % idem DTLZ2, ZDT1

# Python (env-main), gate objetivo:
python3 scripts/accept.py R1-e103 --alg e103 --problema MMF1 --semente 0   # idem DTLZ2/ZDT1
```
Outputs em `data/experiments/main/e103/` (gitignored — regeneráveis). Na bateria, o despacho
roda o e103 com `exp='off'` (runs_matrix) e em **processo MATLAB dedicado** (N.3; o run_e103
torna até um processo compartilhado seguro path-wise — provado na §4 — mas o processo dedicado
segue sendo a regra do despacho).

## 7. O que este cartão acrescentou ao padrão (fecha a R1)

1. **O molde OFFLINE está provado ponta-a-ponta** (o 1º dos 5 configs offline): orçamento = o
   dataset via FEBudget esgotado-na-carga; ① = o dataset; CP-init x_hash+f_hash; violação de
   regime como exceção não-engolida. **b5r/b5m/c311/piso offline (R3) herdam o desenho** — o
   análogo Python é direto (budget.py).
2. **2ª instância do worker de path dedicado** (após o e74): o molde captura-integral → rmpath
   das árvores-gorila → addpath próprio → asserts which-all → onCleanup generaliza — agora com
   DUAS árvores removidas e a lição "addpath próprio POR ÚLTIMO" (precedência sobre pastas
   residuais de adapters que não removem as suas, c141/c238).
3. **③ com o μ de DOIS modelos por re-predição read-only** (2 linhas/candidato com modelo_flag
   distinto) — precedente p/ qualquer algoritmo com modelos alternativos onde a SPEC pedir
   "μ dos dois" sem o mecanismo consultar ambos na decisão.

## 8. Pendências / DEFINIÇÕES EM ABERTO para a torre levantar com o autor

1. **[DEFINIÇÃO EM ABERTO — afeta os 5 configs offline, decidir ANTES do R3] Onde persiste a
   avaliação REAL do ND final (§11/B7.5).** O §11 manda avaliar o não-dominado final 1× na
   verdade ("a única chamada real do offline") e computar as métricas sobre ele; o cartão/L.15
   diz "ND final avaliado 1× FORA" e fixa ① = o dataset (o gate exige 31D−1 exatas — as ~100
   avaliações finais NÃO cabem na ① sem quebrá-lo). Este run salva TUDO que a avaliação
   pós-hoc precisa (FinalDec ≡ decs da ③ g99). **Recomendação:** avaliação pós-hoc em Python
   canônico (`problems.py`), uniforme p/ os 5 configs offline (elimina a ponte da equação),
   persistida numa camada própria (ex.: `__final.parquet`) — exige decisão de schema/naming
   (território da torre: naming.py é faixa Python + §17 é documental).
2. **(a) Concretização da âncora `e103-judgemodel`** — a âncora descritiva ("excluir pares
   i==j") virou **`Msite = (sum(site,3)>=(M-1)) | logical(eye(N));`** — equivalente em 1 linha
   ao `Msite(logical(eye(N)))=true;` sugerido como "ex.:" no S.2-e103 (o OR com a identidade
   marca o par i==i como confiável em todos os objetivos; comparação de um indivíduo consigo
   mesmo não informa ordem). Nota no anchors.json; **p/ seu veto** (precedente e74-ndsort-obj).
3. **(b) `expect_after` da `e103-pm-D93a` concretizado** de `"1"` (fraco — casaria com qualquer
   arquivo) p/ `{1,20,1,20}` (a cell de parâmetros pós-patch); idem `e103-centros-D93` p/ o
   literal MATLAB `ceil(sqrt(Global.n_dataset))`. Ratificação sua.
4. **(c) ③ com 2 linhas/candidato (Kriging + RBFN)** — o "μ dos DOIS modelos" da L.15 foi
   materializado por RE-PREDIÇÃO read-only dos dois modelos sobre a pop selecionada (o
   RBFN_cal incondicional que a própria L.15 sugere); a linha Kriging traz o μ CRU do modelo
   (nos membros do dataset ≡ F real — DACE interpola), não o valor amendado. Leitura p/ veto.
5. **(d) ② offline = membros do DATASET na pop selecionada** — geração sem nenhum membro real
   fica sem linha (ZDT1 g5+); a série completa (inclusive zeros) está no `n_ds_membros` do
   jsonl. Leitura p/ veto (o ② é "população REAL" — no offline só o dataset é real).
6. **Commits `[R1-e103]`** (3): patches e103_IBEA-MS · adapter+instrument+âncoras+repos.lock ·
   handoff+relatório. Nenhum blob. SEM INDEX (a torre marca). A sessão paralela c154 tem
   arquivos não-commitados na árvore (`src/experiment.py`, `src/c154_jes.py`,
   `tests/test_c154.py`) — intocados, fora dos meus commits.
7. **A validação de fidelidade é SUA (D97)**: números da §5 são guia; o veredito (a
   distribuição KFlag por problema é fiel ao mecanismo 3σ? a relaxação M−1 e o spread
   quase-plano se comportam como o paper prevê? o sumiço dos membros reais no ZDT1 é o
   erro-de-fantasia esperado?) é seu, lendo `e103_setup`/`e103_gen` + ①③ com o `sigma_dict`.
