# R3-b5 — Prob-RVEA (b5r, mode 7) e Prob-MOEA/D (b5m, mode 72), OFFLINE

> **Cartão único** desta sessão (1 sessão = 1 cartão): cobre os DOIS configs, b5r e
> b5m. Nenhum outro algoritmo tocado (em especial NÃO o mode 12 = moead_media, cartão
> próprio). Data: 2026-07-22/23. Runner: `src/b5_prob.py`. Env: `env_b5`.

## 0. Veredito
`src/b5_prob.py` implementa b5r (ProbRVEA_v3) e b5m (ProbMOEAD) no regime OFFLINE, sobre
o `standalone_harness` (molde `run_stubr3` + e103). **Todos os gates objetivos VERDES**
(ver §6). A fidelidade é validação MANUAL do autor a posteriori (D97) — não entra aqui.

## 1. Fase 0 — gate de ambiente
- `env_b5` = py3.7.12 x86_64/Rosetta: sklearn 0.21.3, desdeo-problem 0.14.0, desdeo-tools
  0.2.6, statsmodels 0.13.5, pyDOE 0.9.1, matplotlib 3.5.3. Harness offline proof
  (`load_sonda`) OK.
- Artefatos: `data/datasets/{MMF1,DTLZ2,ZDT1}/ds_*_0.parquet` (61/371/929 = 31D−1) +
  `data/sonda/*.parquet`. Vendored `algorithms/b5_Prob-RVEA/desdeo_{emo,problem,tools}`.
- Suíte env-main: **295 OK** (skipped=7); preflight **exit 0** (baseline).
- ⚠ **O env NÃO estava pronto para RODAR o desdeo** — o proof do R3-00 só exercitou
  `import + export + load_sonda`, nunca o caminho evolver/DataProblem/dataset-read. Ver
  §3 (completamento do env, sinalizado p/ veto).

## 2. Decisões cravadas COM o autor (o autor delegou à recomendação em cada uma)
1. **Pin desdeo-emo (gate R3.2):** VENDORED root-first, **SEM pacote pip**. Fonte única =
   `algorithms/b5_Prob-RVEA/desdeo_emo` (a `SurrogateKriging` de kernel fixo vive no
   `desdeo_problem` vendorizado). Travado por content-hash (`b5_desdeo` no repos.lock).
   Placeholders resolvidos em `envs.json` e `requirements/env_b5.txt`.
2. **Sonda geracao=NULL (offline):** `emit_sonda_block` faz `int(geracao)` e não aceita
   None; `auditar` HARD-FAILA sonda offline com geracao não-nula (b5r/b5m ∈ OFFLINE set).
   Mecanismo: chamar `emit_sonda_block(geracao=1)` (mantém RNG-guard + chunk + evento
   jsonl) e **carimbar geracao=None nas S linhas de `buf.surr_rows[-S:]`** — precedente
   `c149._stamp_c3_sonda` (mesma mutação do buffer do runner). Local, zero mudança no
   harness compartilhado. (b5 é o 1º config offline-Python; c311/moead_media herdam.)
3. **Patches vendorizados** (ancorados; §5). Ambos aplicados como apresentados.
4. **pandas 0.25.3 → 1.3.5** (env_b5): o 0.25.3 QUEBRA o `DataProblem` do desdeo; ver §3.

## 3. Completamento do env_b5 (⚠ SINALIZADO PARA VETO DA TORRE — D80)
O env "validado" (DI-22) não rodava o desdeo de fato. Gaps encontrados e fechados:

| Gap | Fix | Fonte da verdade |
|---|---|---|
| `pymoo` ausente (finais ⑦ + filtro ND via `src/problems.py` no runner offline) | **instalado pymoo==0.6.1.2** + shim py3.7 `typing.Literal` no runner | R3-00 REPASSE:276 já assumia "pymoo antigo" em env_b5; desdeo/finais exigem |
| `plotly`/`graphviz` ausentes (import-time de `desdeo_emo.othertools.plotlyanimate`/`BioGP`) | **instalados plotly==4.14.3 + graphviz** (iguais ao env_c311) | env_c311 (irmão desdeo) já os tem |
| `pygmo` ausente (force-import de `EAs.__init__`→NSGAIII/PPGA e `selection.__init__`→NSGAIII_select) | **STUB no runner** (só `fast_non_dominated_sorting`, nunca chamado) — b5 NUNCA usa NSGAIII/PPGA; pygmo é dep Boost pesada, ausente até no env_c311 | inspeção do import-chain |
| `doe.py:219` `ChunkedArray.to_numpy(zero_copy_only=)` estoura no pyarrow 12 (env_b5) — o kwarg é do `Array.to_numpy` | **SHIM LOCAL no runner** (`combine_chunks()`, saída bit-idêntica) — NÃO editei o `doe.py` COMPARTILHADO (sessão c311 concorrente) | funciona no pyarrow 25 do env_main (onde o stub R3-00 rodou); nunca exercitado no offline-Python |
| `pandas==0.25.3` QUEBRA `DataProblem` (`pd.DataFrame(columns=,index=[0])` é bug do pandas 0.25) | **subido p/ 1.3.5** (autor cravou); desdeo NÃO pina pandas; harness=pyarrow ⇒ saída inalterada | metadata do desdeo-problem/tools |

`requirements/env_b5.txt`, `locks/env_b5.lock.txt`, `PROVISIONAMENTO.md §2` e
`envs.json` atualizados. **BUG DE TORRE**: o `doe.py:219` deve ganhar fix central
(vale p/ b5 + c311 + moead_media); o meu shim é local para não conflitar com c311.

## 4. O runner — `src/b5_prob.py`
Molde offline (`run_stubr3`) com o motor como CAIXA-PRETA. Fluxo:
1. Shim `typing.Literal`; `import standalone_harness as H` (pina threads D79) ANTES de numpy.
2. Seeds D62/D91: `base=H.seed_base(alg,s)` (b5 sem offset ⇒ base=s); `random.seed`/
   `np.random.seed` via `iteration_seed(base, ALG_ID, 0, uso)` — **ALG_ID: b5r=17, b5m=18**
   (seeds.json). ⚠ O cartão L.16 escrevia `np.random.seed(s)`; segui a CONVENÇÃO do
   pipeline (D62/c122/c154, anti-descompasso) — ver REPASSE (definição p/ o autor ratificar).
3. `_import_vendored()`: stub pygmo → root vendorizado A FRENTE do sys.path → importa
   `DataProblem, SurrogateKriging, {7:ProbRVEA_v3, 72:ProbMOEAD}` (confere root-first).
4. `load_offline_budget` (① = dataset, orçamento esgotado) + `load_sonda(regime=offline)`.
5. `DataProblem(df 1-based, bounds REAIS via H._bounds)` → `problem.train(SurrogateKriging)`
   (kernel/alpha/n_restarts fixos; treino ÚNICO), cronometrado → `buf.add_timing` (④ 1 linha).
6. `_predict(X)=problem.evaluate(X, use_surrogate=True)→(.objectives μ, .uncertainity σ)`.
7. `emit_sonda_block` (1 bloco, S=20000) FORA do laço → geracao=NULL pós-hoc.
8. `evolver={7:ProbRVEA_v3,72:ProbMOEAD}[mode](problem, use_surrogates=True,
   n_gen_per_iter=10, total_function_evaluations=40000)` (SEMPRE 40000: a rampa θ divide
   por ele) → `with offline_guard: while continue_evolution(): iterate()`.
9. Replay dos arquivos por geração (`individuals/objectives/uncertainty_archive`) → ③ busca
   (`regime='offline'`, μ/σ, espaco_modelo='cru', real_solution_id=NULL) + ⑥ (DI-10 por gen).
10. ⑦ __final: `problems.evaluate_problem` (1× na verdade, fora do orçamento) + ND filtrado
    DEPOIS (`_nds_filter`) → `write_final`. pop_final = última geração gravada na ③.
11. `write_run_outputs(status=ok, motivo_parada=orcamento, q=1, sigma_dict, regime=offline)`.

**Notas por-modo:**
- **mode 7 (b5r):** `Prob_APD_select_v3` = APD probabilístico por **aproximação MÉDIA-MC**
  — DECLARADA como aproximação (não o Prob-APD publicado exato). Ver `sigma_dict.fidelidade_por_modo`.
- **mode 72 (b5m):** `ProbMOEAD_select` = comparação MC pareada
  (`compute_probability_wrong_MC`, P_wrong>0.5) — **quase-fiel** ao paper (o KDE morto foi removido).

## 5. Patches vendorizados (ancorados; re-lacrados com `preflight --write`)
- **b5m — `desdeo_emo/selection/ProbMOEAD_select.py:66-75`** COMENTADO. Bloco KDE morto
  (66-73, `compute_pdf` cujo resultado a decisão nunca usa) + `plt_density` VIVO e crashy
  (75: salva PDF por vizinho sob `usetex`, `reshape(20,…)` → ValueError se n_neighbors≠20).
  A decisão (linha 79) usa `compute_probability_wrong_MC`. Âncora `b5-mode72-kde`.
- **b5r — `desdeo_emo/EAs/BaseEA.py` `_next_gen` (pós `keep()` na L.205)**: re-carimba
  `individuals/objectives/uncertainty_archive[str(gen_count-1)]` com a população
  POS-seleção (DI-16.16). Sem isto o `Population.add()` deixa a PROLE PRÉ-seleção no
  arquivo e a ⑦ do mode 7 nasce irreconstituível da ③ (defeito 🔴 do R3-00). Afeta só
  ProbRVEA (ProbMOEAD sobrescreve `_next_gen`). Âncora NOVA `b5-mode7-archive`.

## 6. Gates de aceite (re-executados AO VIVO — ver RELATORIO)
Todos os runs OFFLINE, semente 0, `PYTHONHASHSEED=0`, FE_final = 31D−1, `cp_init_ok=True`.

| config | D | FE | n_ger | n_final | accept | auditar | final_eval |
|---|---|---|---|---|---|---|---|
| b5r/MMF1  | 2  | 61  | 950 | 44  | 🟢 | 🟢 | 🟢 |
| b5r/DTLZ2 | 12 | 371 | 684 | 61  | 🟢 | 🟢 | 🟢 |
| b5r/ZDT1  | 30 | 929 | 859 | 48  | 🟢 | 🟢 | 🟢 |
| b5m/MMF1  | 2  | 61  | 801 | 50  | 🟢 | 🟢 | 🟢 |
| b5m/DTLZ2 | 12 | 371 | 381 | 105 | 🟢 | 🟢 | 🟢 |
| b5m/ZDT1  | 30 | 929 | 801 | 50  | 🟢 | 🟢 | 🟢 (re-run após fix nd_pos_real) |

- **Determinismo (gate 5):** 🟢 — 2 runs b5r/MMF1 mesma semente ⇒ ③-busca + ⑦ bit-a-bit.
- **Não-perturbação (gate 4):** 🟢 — b5r/MMF1 sonda-ON vs sonda-OFF ⇒ ③-busca + ⑦ bit-a-bit.
- **Suíte (295+) + preflight:** re-executados no fechamento; anchors `b5-mode72-kde` /
  `b5-mode7-archive` = APLICADO; sem placeholders; preflight exit 0.
- **check_r3_b5** (aditivo, antes do catch-all F0-01) afere runs gravados; NÃO exige ②
  não-vazia (DI-16.17). Dispatch b5r/b5m registrado (`experiment.py`).

⚠ **Bug de nd_pos_real corrigido durante a validação:** eu passava `nd_pos_real` calculado
no float64 cru ao `write_final`; a docstring dele manda OMITIR (ele calcula na vista
float32 que a ⑦ persiste). Em empates próximos (medido só em b5m/ZDT1: 20 vs 19) o float64
diverge do float32 e o `--check` reprova. Fix: omitir `nd_pos_real` (write_final computa no
float32). Os outros 5 runs já batiam (sem empate) — ⑦ idêntica; só b5m/ZDT1 re-rodado.

## 7. Rituais
NUNCA `git push` · NUNCA `git add -A` · não escrevi em `_baseline_pre_retrofit/**` · a
sessão c311 concorrente (src/c311_tgprmo.py, tests/test_c311.py, off/c311) foi IGNORADA
no meu fechamento. Commits `[R3-b5]`.
