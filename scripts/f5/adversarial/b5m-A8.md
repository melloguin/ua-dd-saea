## b5m-A8 — VEREDITO: CONFIRMADO(b) — *e a etiqueta "classe (3)" cai*

**Resumo em 3 linhas**
A cadeia `adapt zera values → pbi 0/0 = NaN → P_wrong ≡ 0,0 → probabilities>0.5 nunca dispara` **não caiu**: reproduzi o INÍCIO de 4 células reais **bit-a-bit** (31/31, 21/21, 21/21, 41/41 gerações idênticas à ③) com o código oficial instrumentado em float64 e medi cada elo — inclusive o gatilho que a ③ float32 **é incapaz de mostrar**. Mas o achado **não é classe (3)**: é comportamento do **código oficial** (`ReferenceVectors.adapt` de Cheng-2016 + `ProbMOEAD_select.pbi`) sob surrogate degenerado (causa-raiz sancionada em DI-30) ⇒ **(2)/(b) — RESULTADO da dissertação**, com um sub-item (d) de instrumentação. Duas correções de precisão no relatório e um **achado novo meu**: existe um congelamento **PARCIAL** (slots isolados) em 8 células do piso que ninguém tinha visto — e ele confirma a cadeia por geometria, com previsão exata de *quais* slots morrem.

---

**O que tentei para derrubar (os 4 passos, com números)**

**1 · Por DECISÃO — falhou em derrubar, mas reclassifica.** Nada em DI-01..DI-40, CONTRATO §6.1, bundle `alg_b5_prob.md` ou `sigma_dict` (25 chaves, lidas do header real) menciona vetor nulo/NaN/congelamento. O que existe: **DI-30** ratifica a degeneração do GP como "dado HONESTO e determinístico… achado científico legítimo + caveat na dissertação"; o bundle declara "**modes 12/72 adaptam de verdade a cada iterate/10 gers**" (linha 26). A **bússola D29** (`claude_code_context/CLAUDE.md:16`) manda: 🟠 impl → **segue o CÓDIGO**. Não há prescrição no paper para amplitude nula ⇒ não há 🔴 "voltar ao artigo". Logo: causa sancionada + código oficial = **(2)**, não (3).

**2 · Por QUERY — a evidência do analista é reprodutível, mas a dele NÃO PODIA provar o gatilho.** Reimplementei tudo do zero (lattice Das-Dennis próprio, sem o repo vendorizado). Reproduzi os agregados exatos: **1.543 transições congeladas** em 5 células (DTLZ3 380 · DTLZ1 370 · `swap_medium-mvns_ZDT4` 771 · `swap_small-mvns_ZDT1` 21 · MMF11_L 1), denominador correto **30.120 transições** (o relatório escreve "de 30.165 gerações"). **Duas falhas reais da evidência dele**: (i) a ③ é **float32** e não resolve o gatilho — em `off/DTLZ1` a amplitude de μ é **0,0 no export em TODAS as 381 gerações**, mas a busca só congela na 11ª; se a leitura float32 bastasse, DTLZ1 teria de congelar na geração 1 (como DTLZ3). (ii) **22 das 1.543 transições não são mecânicas**: MMF11_L (amp mín 0,86) e `swap_small-mvns_ZDT1` (0,25) nunca têm amplitude zero ⇒ vetores nunca zerados ⇒ é estagnação terminal comum (θ=500 + população convergida). Mecânicas mesmo: **1.521**. Controle negativo achado por acaso e aprovado: `b5m/BBOB_F17` tem amplitude float32 zero na geração **24** — que **não é fronteira de iterate** (24 mod 10 ≠ 1) ⇒ nenhum `adapt` roda lá ⇒ previsão "não congela"; observado 36 e 44 slots mudando nas transições vizinhas. ✔

**3 · Por CÓDIGO — bancada no `env_b5` real (py3.7.12/numpy 1.21.6/sklearn 0.21.3).** `ReferenceVectors.adapt(fitness)` com `max==min`: `values` fica **exatamente ZERO** (não NaN — `normalize()` tem o guard `norm_2[norm_2==0]=eps`, e 0/eps=0), **50/50** linhas (M=2) e **105/105** (M=3) com norma nula. `ProbMOEAD_select.pbi:109-110` (`norm_weights=np.linalg.norm(weights); weights=weights/norm_weights`) devolve **1000/1000 amostras NaN**. `compute_probability_wrong_MC(NaN,NaN) = 0.0` exato (e `(NaN,ok)`/`(ok,NaN)` também 0,0). `do()` real: **0/400** substituições no degenerado × **188/400 (47,0%)** com amplitude 1e-12 × **201/400 (50,2%)** com 1e-3. **Contrafactual decisivo**: amplitude ≠ 0 **cancela na normalização** (‖values−values_iniciais‖ = 1,1e-16 com amplitude 1e-12) ⇒ só o **zero EXATO em float64** congela. `MOEAD_select` (o piso, média) sob a mesma condição: **0/400** — a cadeia **não é do mecanismo probabilístico**, é do `adapt`+`pbi`.

**Re-execução instrumentada (o experimento que o §8 pediu, feito):**

| célula | ③ reproduzida | adapt @ ger 1 (float64) | adapt @ ger 11/21/31 | substituições | pbi NaN | P_wrong |
|---|---|---|---|---|---|---|
| `off/DTLZ1` | **31/31 bit-a-bit** | amp = **6,67e-76 / 5,79e-75 / 3,40e-74** → 0/105 linhas nulas | ger 11: **0,0/0,0/0,0** → **105/105 nulas** | it1 **10.479/21.000 (49,9%)**; it2 e it3 **0** | 84.000/126.000 (= exatamente it2+it3) | it1 mediana **0,499977**; it2/it3 **21.000/21.000 = 0,0** |
| `off/DTLZ3` | **21/21** | amp **0,0** já na LHS inicial → 105/105 nulas | idem | **0** desde o 1º iterate | **84.000/84.000** | **42.000/42.000 = 0,0** |
| `off/DTLZ2` (controle −) | **21/21** | amp 2,19/1,97/2,31 → 0 nulas | ger 11: 1,67/1,74/1,40 → 0 nulas | 2.218 e 577 | **0/84.000** | mediana 0,0068 → 6e-06 |
| `swap_medium-mvns_ZDT4` | **41/41** | amp 0,975/173,1 | ger 31: **0,0/0,0** → **50/50 nulas** | it1-3: 1.960/1.720/1.691; **it4: 0** | 20.000/80.000 (= it4) | it4 **10.000/10.000 = 0,0** |

Isso explica **mecanicamente** o timing que o analista só pôde inferir: DTLZ1 mantém amplitude **6,7e-76** (não-zero, normalização recupera os vetores) durante o 1º iterate — e as 10 gerações de "busca" são **puro cara-ou-coroa** (P̂ mediana 0,499977, aceitação 49,9%) — e só na geração 11 a amplitude vira **0,0 exato**. E prova que 0 substituições em 798.000 comparações é impossível sem o NaN (sob P̂≈0,5 a chance é ~2⁻⁷⁹⁸⁰⁰⁰).

**4 · Controles positivo/negativo — a previsão mais dura, e ela acertou 8/8.** Se o congelamento vem do `adapt` zerando *linhas*, então quando **só um subconjunto J de objetivos** tem amplitude nula devem morrer **exatamente** os slots cujo vetor do lattice tem suporte ⊆ J (uma **face** do lattice). Testei `S == {i : supp(v_i) ⊆ J*}` com `J*` = união dos suportes dos mortos (sem parâmetro livre; 7 faces possíveis entre 105 vetores): **8/8 células batem**, todas no **piso** (fora da amostra do analista): `moead_media` DTLZ2/DTLZ3/`swap_small-lhs_DTLZ2` → slot **104** = vetor (1,0,0); ZDT6 → slot **49** = (1,0); `swap_medium-lhs_ZDT4` e `swap_medium-mvns_ZDT4` → slot **0** = (0,1); DTLZ4 e `swap_small-mvns_DTLZ2` → os **14** vetores com componente 1 nula (a aresta inteira). O piso congela **4.201 transições em 16 células** (5 mecânicas) contra 1.543/5 (3 mecânicas) do b5m.

---

**Por que não caiu**
Todos os quatro elos são reproduzíveis no código oficial e **medidos em float64 dentro de execuções bit-idênticas às arquivadas**; o gatilho (amplitude exatamente 0), o efeito (100% das linhas de `values` com norma 0), o sintoma (100% de PBI NaN) e a consequência (P ≡ 0,0, zero substituições) foram observados juntos, com controle negativo (DTLZ2 saudável: 0 NaN, substitui), controle temporal (BBOB_F17 amp-zero fora de fronteira ⇒ não congela) e controle geométrico fora da amostra (8/8 faces no piso). O único ponto em que a *evidência do analista* não sustentava a *conclusão do analista* era o float32 — e a re-execução fecha esse buraco a favor dele.

**Enquadramento e bússola D29**
**(b) COMPORTAMENTO LEGÍTIMO** do código oficial sob o nosso orçamento/desenho → **RESULTADO da dissertação**: *"com `normalize_y=False`+`alpha=0`, quando o treino único cai na bacia prior (DI-30), a população fica com μ idêntico; a adaptação de vetores de Cheng-2016 multiplica os RVs pela amplitude nula, o PBI vira NaN e o MOEA/D probabilístico (e o genérico) param de substituir — o algoritmo morre em silêncio, sem exceção, sem log e com HV-surrogate no máximo teórico 1,331"*. Bússola **D29 = 🟠 (detalhe de implementação → segue o CÓDIGO)**: não há prescrição no paper para amplitude nula (§IV-A8 só manda adaptar a cada 10 gerações "as in [14]"), e pôr um guard seria **🟢 extensão nossa** — que mudaria o mecanismo medido e **não deve** ser aplicada em silêncio. **Não é (a)**: nenhuma linha do nosso runner participa da cadeia (`src/b5_prob.py` só chama `evolver.iterate()`). **Não é (c)**: dado honesto e determinístico, nenhuma célula é corrompida. **Sub-item (d) para a torre**: o mecanismo está certo, a **instrumentação é que engana** — a ③ float32 (D53) não distingue 6,7e-76 de 0,0, e não há log de `norm(values)` nem de `p_wrong_stats` (é o mesmo buraco do A25).

**Impacto**
- **Nenhuma célula sai da análise**; nenhum número publicado do relatório muda por erro. Correções de precisão: (1) das 1.543 transições congeladas, **1.521 são mecânicas**; as 22 restantes (MMF11_L 1, `swap_small-mvns_ZDT1` 21) são estagnação comum — não citar como evidência da cadeia; (2) o denominador é **30.120 transições**, não 30.165 gerações; (3) A7 (b5m.md:62) diz que DTLZ1 tem "1 único valor distinto … nas 20.000 de sonda" — **falso**: a sonda de DTLZ1 tem **23/23/25** valores distintos, até 1,17e-08 (é por isso que a corr é definida, −0,0055 etc.); só DTLZ3 tem 1 valor; (4) "a ⑦ das duas células congeladas é a amostra LHS inicial" vale **exatamente para DTLZ3** (105 X distintos); a de **DTLZ1 é LHS + 10 gerações de deriva aleatória** (51 X distintos de 105, IGD+ 109,9).
- **Armadilha nova para o D97**: a ⑦ de `swap_medium-mvns_ZDT4` tem 50 linhas mas **1 único X distinto** — e `nd_pos_real = 50/50`; a "fantasia 1,000" e o "|ND| = 50" citados em A21/§6 são **50 duplicatas de um ponto**.
- **Confusor da ablação D77 medido**: 14 células estão contaminadas (3 do b5m, 5 mecânicas + 8 parciais do piso). A âncora do mecanismo **sobrevive**: σ da ⑦ b5m<piso **32/45, razão 0,724, p=0,0012** → excluindo as 14: **23/31, razão 0,724, p=0,0020**. Já a âncora **direcional J1 em IGD+ enfraquece**: 27/45 (0,3596 vs 0,5603; p=0,281) → **17/31** (0,3232 vs 0,4275; p=0,624) — parte da margem vinha de células onde **o piso** estava mecanicamente morto. Reportar as duas leituras.
- Incidental (torre, 1 linha): o pinning de threads D79 é **load-bearing** — minha 1ª re-execução de `swap_medium-mvns_ZDT4` **sem** `OMP/OPENBLAS/MKL=1` divergiu da ③ já na geração 2 (Δ 1,7); com o pin, 41/41 bit-a-bit. Reprodutibilidade do b5m em N=2.000 depende do env limpo do `run_in_venv`.

**Ação p/ F5.7 + custo**
1. **Instrumentar o gatilho (read-only, DI-12.1)** em `src/b5_prob.py` e `src/piso_offline.py`: um wrapper de `ReferenceVectors.adapt` que loga por iterate `amp_fitness` (float64, M valores), `n_vetores_norma0` e `vetores_degenerados=bool` — **captura também o caso PARCIAL** (8 células que hoje ninguém vê). Junto, o pedido do A25: `p_wrong_stats` (min/med/máx de `probabilities`) e `n_substituicoes = len(selection)` por geração. **~8 linhas, zero FE, custo de wall < 0,1%** (uma redução numpy por iterate); torna A8 auditável do log e tira A12 do balde T.
2. **Não mexer no D53** (export float32) — caro e de raio largo; o log da amplitude float64 resolve o mesmo problema por ~0 custo.
3. **Não re-rodar as 45 células** e **não** inserir guard no vendorizado (seria 🟢 nossa, mudaria o mecanismo). Se o autor quiser o contrafactual "com guard" como braço declarado: 45 células ≈ **37,4 h-core por semente** (b5m wall mediano 2.982 s).
4. **D97/dissertação**: declarar as 3 células mecanicamente mortas do b5m (DTLZ1, DTLZ3, `swap_medium-mvns_ZDT4`) e as 5+8 do piso; publicar a ablação σ nas duas leituras (45 e 31). Custo: redação.

**Evidências** (todas em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/baterias/f54/b5m-A8/`): `A8_01_bancada_codigo.py` + `A8_01_bancada_saida.txt` (bancada no env_b5) · `A8_02_dados_predicao.py` + `A8_02_saida.txt` + `A8_dados_predicao.csv` (90 células; a tentativa de refutação por query) · `A8_03_reexecucao_instrumentada.py` + `A8_03_{DTLZ1,DTLZ3,DTLZ2,swap_medium-mvns_ZDT4}.json` (re-execução bit-a-bit instrumentada) · `A8_04_slots_face.py` + `A8_04_saida.txt` + `A8_slots_face.csv` (teste de face 8/8) · `A8_05_impacto_ablacao.py` + `.txt`. Nada foi escrito fora dessa pasta; `algorithms/` e `resultados_experimentos/` só foram lidos (instrumentação por monkeypatch em memória).