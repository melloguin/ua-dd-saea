## c149-A26 — VEREDITO: **CONFIRMADO (c) DADO DESCASADO** (causa-raiz provada; o enquadramento (d) instrumentação é o item real para a torre central)

**Resumo em 3 linhas.** Ataquei o achado por decisão, por query independente, por código e por controle positivo/negativo — **não caiu; ao contrário, subiu de "hipótese H1/H2" para prova byte-a-byte**: a ③ consolidada de `c149/q10_ZDT4/42` é **bit-idêntica (md5 `1269b87f7612`) ao arquivo de uma SEGUNDA execução da mesma célula feita no **Mac (arm64) em 2026-07-24T13:01:52Z**, que existe em disco em `ua-dd-saea/data/experiments/batch/c149/`; casada com a ① *dessa* execução ela fecha **2000/2000 bit-exato**. As camadas ①②④⑤⑥ consolidadas são bit-idênticas ao disco local da **v6 (2026-07-26T19:05:44Z)**. Não é ruído, não é query, não é corrupção: é **colisão de blob** — `naming.blob_path()` não carrega identidade de execução e `src/gcs.py:155` apaga a única cópia local da ③.

---

### O que tentei para derrubar (os 4 passos, com os números)

**1) Por DECISÃO — falhou (nenhuma decisão sanciona).** D58/§17.7 sanciona a ③ *bucket-only* para `{c154, c122, e81, c149, c262}` (`src/gcs.py:40`) e a poda local (`src/gcs.py:154-156`); DI-21/D-03 sanciona o *resume bucket-aware* (`src/manifest.py:183`). Nenhuma delas autoriza camadas de **execuções diferentes** na mesma célula. O CONTRATO §17.2 exige `real_solution_id = s` ⇒ X da ③ ≡ X da linha `s` da ①. O `sigma_dict` do próprio c149 promete `res.F` reconstituível da ③. Nada cobre o observado. Precedente inverso encontrado (F5.2a, `main/b1/WFG1`, "dois writers na mesma célula") é MATLAB/`'parallel',false` — não se aplica a config Python.

**2) Por QUERY — falhou; minha reformulação independente dá o MESMO resultado, mais forte.**
- Refiz o teste com código próprio (`02_zdt4_selfconsist.py`): match **por geração** e **por conjunto** (lexsort, ignorando ordem de linha — a armadilha nº 5 do próprio relatório): `q10_ZDT4` **0/200 gerações** em ambos, `max|ΔX| = 9,99983`; controle `main/ZDT4` **200/200 em ambos, max|ΔX| = 0,0**.
- Critério totalmente diferente, sem tolerância e sem X: avaliei **ZDT4 analiticamente** nos X marcados da ③ e reconstruí o arquivo. A ③ reproduz os **seus próprios** `transf_params` em **200/200 gerações, erro máx 3,73e-6 / mediana 1,55e-6**; contra o arquivo da ① consolidada, **0/200, erro máx 7,77 / mediana 5,24**. Ou seja: a ③ **não está corrompida — é um run inteiro, íntegro e auto-consistente**.
- Achei e evitei a armadilha de string apontada no roteiro: `sonda_f52e.csv` tem `bloco` como **object** — ordenar sem `to_numeric` pega o bloco "98" como último (lexicográfico) e move o ΔWAPE mediano de −41,24% para −19,59%. Reportei os dois.

**3) Por CÓDIGO — falhou; o comportamento é impossível para um processo só.** `src/c149_lbnmobo.py` acumula a ③ num buffer único em memória (`buf.surr_rows`, l. 1017) e grava uma vez; `src/atomic_io.py` grava `.tmp` por PID + `os.replace`. Um processo não emite ③ alheia. O que o código **permite** é a colisão: `plan_targets` monta `blob = naming.blob_path(exp, alg, fname)` (`src/gcs.py:79`, `src/naming.py:344`) — **sem máquina, sem uuid, sem geração**; `mirror_run` faz `os.remove(local)` da ③ após o upload (`src/gcs.py:155`); e `is_run_done` dá a célula por pronta se o **blob existir**, seja de quem for (`src/manifest.py:183`). Três peças que, juntas, tornam este defeito invisível por construção.

**4) Controle positivo/negativo — o defeito NÃO é convenção; é único na campanha.**
- **Varredura do gate proposto em toda a campanha** (`08_sweep_gate.py`, **666 arquivos ③, ~6 s**): **360 células testáveis → 359 OK com `frac_exact = 1,000` e `max_dX = 0,0` (bit-exato, sem zona cinzenta)**, 105 ③ vazias por desenho (pisos), 200 sem `real_solution_id` (b5m 45, b5r 45, c311 55, moead_media 45, treed_media 10), **1 MISMATCH: `c149/q10_ZDT4/42` (`frac_exact = 0,000`, `max_dX = 9,9994`)**.
- **Controle de determinismo (o teste que matou a H2 do analista)**: nos 5 pares `(main/P, q10_P)` do c149 a geração 1 é idêntica por construção (mesmo DoE de 109 pts, mesmas sementes). O ⑥ de g=1 é **idêntico nos 5 pares** (10 `val_mse`, `acq_resF_*`, `mu_sel_nat`, `seed_nsga2`, `solution_id`). O bloco de **sonda g=1 da ③** é **100,00% bit-idêntico** em ZDT1, DTLZ2, WFG9 e MMF16_20 (2.000 pts × M, v5 × v6) e **19,93% (mu) / 1,38% (sigma)** em ZDT4, com `max|Δmu| = 1,53e-4` e rel. mediana `3,57e-7`. **O runner É bit-reprodutível entre execuções — logo "torch não é bit-reprodutível" (H2) está REFUTADA, e "escrita concorrente na v6" (H3) também** (um segundo processo na v6 produziria bytes idênticos e seria indetectável).
- **Coerência do lado bom, no nível do X** (não só do Y): `dist_min_arquivo` do ⑥ é reproduzido a partir do X da ① em espaço **nativo** com erro máx **9,54e-7** nas 200 gerações; ④≡⑥ (`|Δt_fit| ≤ 7,6e-6 s`, `Δn = 0`); ② = 222.800 = Σ(109+10g) exato; ① auto-consistente (f ≡ ZDT4(x), erro rel. máx **3,88e-7**).

---

### Por que não caiu: a prova byte-a-byte (o que o relatório original não tinha)

Encontrei as três árvores de proveniência (`_old/_bucket_raw`, `_old/_maquinas/v6`, `ua-dd-saea/data/experiments`) e comparei inodes + md5 dos 6 artefatos:

| artefato | consolidada | `_bucket_raw` | `_maquinas/v6` (disco local) | `data/experiments` (Mac) |
|---|---|---|---|---|
| `__surrogate` | `1269b87f…` | `1269b87f…` (mesmo inode) | **AUSENTE** (podada, `gcs.py:155`) | **`1269b87f…` ← IGUAL** |
| `__real` | `f2cff81d…` | `f2cff81d…` | `f2cff81d…` | `0f2fa365…` (difere) |
| `.jsonl` | `6ee03787…` | `6ee03787…` | `6ee03787…` | `5b22586f…` (difere) |
| `.manifest` | v6, `created_at 2026-07-26T19:05:44Z` | idem | idem | **Mac, `2026-07-24T13:01:52Z`, `executable=/Users/gmello/…`, `upload_status: null`** |

E o fecho (`12_prova_final.py`), casando a ③ consolidada com a ① do **Mac**:
- X marcado: **2000/2000 bit-exato**, `max|ΔX| = 0,000000` (contra 0/2000 e 9,99983 na ① da v6);
- `transf_params`(③) ≡ `z_mean/z_std`(⑥ do Mac): **máx 5,0e-7** nas 200 gerações;
- `acq_resF_*`(⑥ do Mac) reconstituído da ③: **200/200 com erro ≤ 8,6e-7**;
- `lote_solution_ids`(⑥ do Mac) ≡ `real_solution_id`(③): **200/200**.

Os **cinco** testes que reprovaram no relatório passam **a 1e-7** quando a ③ é casada com a irmã certa. A divergência de ~1e-6 em g=1 é exatamente a assinatura O-16 (arm64 × AMD Rome muda o kernel BLAS), amplificada caoticamente pelo NSGA-II a partir de g=2 (`z_mean` f₁: 165,034 × 167,893).

**Escopo do "duplo disparo"** (`13_escopo_e_endpoint.py`, 1.061 manifestos locais): exatamente **2 células** têm 2ª execução divergente — `batch/c149/ZDT4` e `batch/sobol_batch/ZDT4`, ambas do lote-piloto do Mac de 2026-07-24. A do `sobol_batch` é **inócua**: ①, ②, ③ são byte-idênticas entre Mac e VM (Sobol é determinístico e sem rede). **O c149 é o único config que reúne as três condições necessárias**: ③ bucket-only + sensibilidade numérica à microarquitetura + duplo disparo.

**O único elo que não consegui fechar** (fora do alcance dos dados locais): *como* o blob do Mac (que declara `upload_status: null`) ficou sendo o objeto corrente. Duas hipóteses restantes: (i) sync out-of-band do Mac depois de 19:05Z; (ii) o consolidador caiu para a cópia local quando o blob faltou — o `_FONTES.csv` tem cabeçalho e **zero linhas**, isto é, o log de proveniência nunca disparou. Resolvível com um comando (abaixo).

---

### Enquadramento e bússola D29

**Classe (c) para a disposição do dado** — a ③ sai da célula (mas é **relabel**, não descarte: o run existe inteiro em disco). **A causa-raiz é (d)/(a) de infraestrutura**, com arquivo:linha:
- `src/naming.py:344` + `src/gcs.py:79` — `blob_path(exp, alg, filename)` **sem identidade de execução**: duas execuções da mesma célula, em máquinas diferentes, escrevem o **mesmo objeto**.
- `src/gcs.py:154-156` — `os.remove(local)` elimina a única cópia local da ③ ⇒ perda irrecuperável em caso de colisão.
- `src/manifest.py:183` — `is_run_done` aceita como pronta uma camada cujo **blob existe**, sem verificar de qual execução veio.
- Nenhuma camada carrega carimbo de execução (`run_id = "batch_c149_ZDT4_42"` é o id da **célula**, determinístico).

**Bússola D29: não dispara.** O defeito não toca nem o artigo (Ansari 2023), nem o código do autor, nem o surrogate do método. Sob D29 é **verde — infraestrutura nossa** — e o mecanismo LBN-MOBO auditado no §2 permanece **integralmente validado** (inclusive por esta célula: o run do Mac é uma execução fiel).

---

### Impacto

1. **Métricas oficiais: NÃO mudam.** `metricas_finais_f52c.csv` `batch,c149,ZDT4` (IGD+ `91,92166808266371`, HV `0,0`, n_nd 16) vem da ①, que é **byte-idêntica ao disco local da v6**. Confirmado independentemente. As duas execuções, aliás, **concordam no endpoint**: `min f0 = 0,00000` e `min f1 = 92,2548` iguais, |ND| 15 (v6) × 16 (Mac) — nem a leitura científica do §6 se move.
2. **Um número publicado precisa ser CORRIGIDO — para melhor.** A22 conta `pick-1 argmax 624/624` e `lote completo 622/624 (99,7%)`; os **200** itens da célula contaminada estão dentro, e **as duas únicas falhas do lote são dela** (`queryjoia_batch_c149.csv`: ZDT4 `S1_lote = 198`, as outras 4 células 200/200/12/12). Excluindo-a: **424/424 = 100%** nas duas contagens. A reconstrução da regra D42 é **perfeita** no dado limpo.
3. **`sonda_f52e.csv`: 202 linhas mal rotuladas** (`batch,c149,ZDT4`, 101 blocos × 2 objetivos) = 16,7% das linhas do c149-batch e 0,45% das 44.928 do estudo. Elas descrevem a curva de aprendizado do run do **Mac**. **Nenhum agregado se move**: mediana de ΔWAPE do c149-batch = **−41,24% com e sem ZDT4** (0,00 p.p.; as duas linhas, −78,3% e +155,3%, ficam em lados opostos da mediana); cobertura95 final 0,8800 e correlação 0,9364 idênticas com/sem.
4. **Nenhuma célula sai da análise de métricas.** Sai **1 camada de 1 célula** (402.000 linhas = 2,50% das 16.096.960 linhas da ③ do c149) das análises Classe A que usam sonda ou front de aquisição.
5. **O desconto de 1,0 ponto no SCORE do c149 (9,0) merece revisão pelo autor**: o A26 deixa de ser "classe (3) inexplicado" e passa a defeito de persistência **com causa provada, escopo de 1/666 e gêmeo íntegro em disco** — enquanto A22 sobe para 100%.

---

### Ação recomendada para a F5.7 + custo

| # | Ação | Custo |
|---|---|---|
| **A0** | `gsutil ls -a -l gs://mestrado_experiments/experiments/batch/c149/exp_batch_c149_ZDT4_42__surrogate.parquet` — **o bucket tem versionamento ON** (`src/gcs.py:22`). Se a geração da v6 (≈19:05Z de 26/07) sobreviveu como *noncurrent*, baixá-la **repara a célula com zero compute**. **Fazer isto ANTES de qualquer re-run.** | ~2 min, 17,6 MB |
| **A1** | Se A0 falhar: **relabel, não descarte**. Manter ①②④⑤⑥ da v6 (métricas válidas) e registrar a ③ como pertencente ao gêmeo do Mac (`ua-dd-saea/data/experiments/batch/c149/exp_batch_c149_ZDT4_42.*`, execução completa e coerente). Marcar as 202 linhas de `sonda_f52e.csv` com `maquina=macA` ou removê-las dos agregados do c149-batch. Corrigir A22 para 424/424. | ~0 |
| **A2** | Só se for exigida ③ nativa da v6: re-rodar `batch/c149/ZDT4/42` numa VM classe v5/v6 via **`lote_python.sh` (OMP=1, D79/O-15)** — obrigatório pela regra "um config, uma máquina" (O-16). **Verificação grátis**: a nova ① tem de ser **byte-idêntica** à colhida (determinismo v5≡v6 provado aqui em 4/5 pares a 100%). | **4,21 h-core** (wall medido 15.164,6 s, 1 core) |
| **A3** | Instalar o gate `08_sweep_gate.py` (X da ③ marcada ≡ X da ① da linha `s`) na torre central. Separação binária perfeita, zero falso-positivo em 359 células. **Ressalva de cobertura**: vê 360/666; 105 têm ③ vazia por desenho; **200 (b5m, b5r, c311, moead_media, treed_media) não populam `real_solution_id` e ficam CEGAS** — para elas use o invariante complementar `transf_params(③) ≡ z_mean/z_std(⑥.fit)`. | **~6 s** para a campanha inteira |
| **A4** | **A correção de raiz**: carimbo de execução (`uuid4 + host + created_at`) no key-value metadata de **cada parquet** e no ⑤, comparado por gate. Sem ele, `blob_path` sem identidade (`naming.py:344`) + poda da única cópia local (`gcs.py:155`) + resume que aceita blob de qualquer execução (`manifest.py:183`) tornam esta classe de defeito **indetectável por construção** — e o M8/M9 vai rodar 30 sementes × 3 máquinas. | ~5 linhas + 1 gate |
| **A5** | Consertar a proveniência do consolidador: `resultados_experimentos/_FONTES.csv` tem cabeçalho e **0 linhas** — deve registrar fonte e *generation* por camada. | ~1 h |

**Artefatos (único diretório de escrita)**: `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/baterias/f54/c149-A26/` — `00_schema.py` · `01_forense.py` · `02_zdt4_selfconsist.py` · `03_zstats.py` · `04_manifest.py` · `05_pqmeta.py` · `06_g1_control.py` · `07_L6_L1_link.py` · **`08_sweep_gate.py`** (o gate proposto, campanha inteira) · **`09_pairs_g1.py`** (controle de determinismo v5×v6) · `10_impacto_sonda.py` · `11_resumo.py` · **`12_prova_final.py`** (a prova byte-a-byte) · `13_escopo_e_endpoint.py`; evidência em `sweep_gate_L3xL1.csv` (666 linhas), `escopo_dupla_execucao.csv`, `evidencia_selfconsistencia.json`, `sweep.log`. Nada foi escrito fora desta pasta.