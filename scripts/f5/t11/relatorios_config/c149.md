# RELATÓRIO DE FIDELIDADE T11 — c149 LBN-MOBO (revalidação pós-correções · protocolo v1.1)

**Config**: c149 LBN-MOBO (Ansari et al. 2023, arXiv:2306.01095 · Python torch+pymoo, reconstrução in-memory D78) · **Corpus de mecanismo**: 30 células da s42 (25 main + 5 batch q=10) · **Corpus de instrumentação**: 1 célula de smoke T11 · **Prior F5**: 9,0 (aceitar+caveat) · **Veredito T11**: **MELHOROU → 9,5**

> ### ⚠ ERRATA 17 CONTRA O HANDOFF T11 E CONTRA O MEU PRÓPRIO BRIEFING
> O briefing e o `T11-RODADA-FINAL.md` §15.2 (e o `evidencia_T11/LEIA-ME.md`) afirmam: *"Os 11 smokes PYTHON não estão preservados — rodaram em tempdirs removidos"*. **É FALSO.** O smoke T11 do c149 **existe e foi auditado nesta análise**:
> `/Users/gmello/Documents/python_repos/mestrado/evidencia_T11/smoke_python/experiments/main/c149/exp_main_c149_MMF1_0.*` (6 artefatos, pasta com mtime `2026-07-31 10:45:39`, i.e. **posterior** ao handoff de 29/30-07).
> Autenticidade provada: `campanha_id = 9ad0138aa9a4_2026-07-31`, `repo_hash = 9ad0138a…` ≡ **HEAD (`9ad0138`)**, `schema_version = 2`. Os 10 outros configs Python também estão lá (`b5r, c122, c154, c262, c311, e81, moead_media, sobol_batch, treed_media`).
> **Consequência**: as correções da T11 neste config foram verificadas em **dado MEDIDO**, não só em código lido. As demais torres de fidelidade Python devem ser avisadas.

---

## 1. Ficha do mecanismo (condensada)

Loop de BO puro, sem população persistente. Por iteração: **retreino DO ZERO** de um **deep ensemble K=10** de MLPs `D→100→50→100→M`, init `N(0,1e-3²)`/bias 0, **1ª camada com ativação distinta por rede** na convenção da RAIZ `[tanh, ReLU, CELU, LeakyReLU, ELU, Hardswish, tanh, ReLU, CELU, LeakyReLU]`, Adam `lr₀=5e-4` ×0,95/época, loss `sum_mse`, batch 10, **60 épocas**, split 90/10 sem shuffle, treino sobre o **arquivo INTEIRO** (sem cap); **z-score de Y por objetivo refeito por iteração**; predição = média das K=10; **incerteza = variância populacional (ddof=0) ENTRE as redes** (Eq. 2b, epistêmica pura); **aquisição 2MD** (Eq. 3) por NSGA-II pymoo 0.6.2 (pop 1000 × 100 ger., SBX 0,9/15, PM 0,9/20 com `prob_var = min(0.5,1/D)`) sobre `F = [μ ⊕ −σ²]`; `res.X` = rank-0 da pop final; **sub-seleção q=1 por HVI-greedy D96**; q=10 = o mesmo laço sem reposição (D42). Orçamento `31D−1` / `11D−1+200q` via `FEBudget` (D89). Espaço interno `[0,1]`; `torch.set_default_dtype(float64)`, export float32 (D53).

**Divergências sancionadas nomeadas**: 🔴 `[:, :2]`→`[:, :M]` → **ARTIGO** (D30, `SPEC:434`) · 🟢 z-score de Y → **EXTENSÃO nossa** (B13.4/D30, `SPEC:434`) · 🟠 Adam/lr/decay/`sum_mse` → **CÓDIGO** (paper omisso) · regras q=1 (D41/D96) e q=10 (D42) → **NOSSAS** (o paper não sub-seleciona) · DoE LHS-maximin do artefato (D63/D87/D88) · orçamento `31D−1` (D21/D61/D66) · offset de semente `+1000·s` (D22) · `prob_var` default do pymoo (**DI-23** — corrige a nota "1/30 hard-coded" da SPEC).

**Volume auditado nesta rodada**: 8.020 iterações · 80.200 treinos de rede · 3.936.030 linhas da ② · 4.040 blocos de sonda (8,08 M linhas) · 17.020 infills · 21.885 avaliações reais · + 40 iterações e 82.000 linhas da ③ do smoke T11.

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.1 Tabela-resumo — 35 aspectos (31 classificáveis + 4 no teto T)

| # | Aspecto | Classe | Verificab. | Resultado-síntese (fonte) |
|---|---|---|---|---|
| A01 | Orçamento FE + hard-stop | **(2)** D21/D61/D66 | direta | 30/30 exatos + smoke 61/61; `fe_index` denso 30/30; 0 overshoot |
| A02 | DoE `11D−1` do artefato | **(2)** D63/D87/D88 | direta | 30/30 (`n_init`, `doe_hash` ⑤≡⑥) + smoke ✅ |
| A03 | 1 fit/iteração; treino = arquivo INTEIRO | (1) | direta | `n_treino = 11D−1+q(g−1)` em **8.020/8.020** + 40/40 no smoke |
| A04 | Sonda k=2 + artefato fixo 2.000 pts | **(2)** DI-12.5/DI-21 | direta | cadência exata 30/30; 4.040 blocos × 2.000; **0 NaN**; smoke 21/21 |
| A05 | U7 `fit+busca ≤ tempo_geracao` | (1) | direta | **0 violações em 8.020**; espelho +sonda 100% em ger. com sonda; smoke 0/40 |
| A06 | U8 `fe_treino_max` monotônico | (1) | direta | **8.020/8.020** = `11D−1+q(g−1)−1`; monotônico 30/30 |
| A07 | U9 guards ⑥≡⑤≡footer | (1) | direta | `cache_hits`=0, `n_clamp_sigma2`=0, `n_geracoes` — 30/30 |
| A08 | U10 aritmética entre camadas | (1) | direta | ② = Σ(11D−1+qg) exato 30/30 (3.936.030); ③online = Σ\|front\| 30/30 |
| A09 | U11 erro de fantasia dos infills | (1) | direta | WAPE mediano 0,26; 0,04 (BBOB_F55) a 5,16 (DTLZ3 f0) |
| A10 | Deep ensemble K=10 (Eq. 1-2) | (1) | direta | 10 `val_mse` em **8.020/8.020** fits + 40/40 no smoke |
| A11 | Rede + Adam/loss/batch/épocas | (1) | direta-**declarativa** + indireta | echo 30/30; custo/rede/época 5,1–131 ms coerente |
| A12 | **Ativações diversas da RAIZ** (§D.1) | (1) | **indireta FORTE** | ranks pareiam por ativação: **max\|Δ\| entre os 4 pares = 0,088**; σ(ranks)=1,315 |
| A13 | Split 90/10 s/ shuffle + semente por rede | **(2)** D22/A6 | declarativa + indireta | `n_train/n_val` fecham 90/10 em 8.020/8.020 |
| A14 | σ² epistêmica = var. populacional entre K | (1) | **direta** | ⑥≡③ recomputado: **≤4,91e-7 em 40/40 no smoke**; 29/30 células s42 |
| A15 | Aquisição 2MD `F=[μ ⊕ −σ²]` (Eq. 3) | (1) | **direta** | `acq_resF_*` reconstituído: **≤5,76e-7 em 40/40 (smoke)**; 7.820/8.020 (s42) |
| A16 | NSGA-II interno (pop 1000, 100 gen) | (1) | direta-**declarativa** | 7 valores de `prob_var`, um por D — **DI-23 confirmada** |
| A17 | `res.X` = rank-0 da pop final | (1) | **direta** | ⚠ **ERRATA F5**: `<1000` em **5/8.020**, não 33 (2 vias concordam) |
| A18 | z-score de Y refit/iteração | **(2)** 🟢 B13.4/D30 | **direta** | `transf_params`(③) ≡ `z_mean/z_std`(⑥): **≤4,96e-7 em 40/40 (smoke)** |
| A19 | fix `[:, :M]` | (1) via D30 (🔴→ARTIGO) | direta | 8 células M=3 com `mu_2`/`sigma_2` e front 6D não-dominado |
| A20 | **Regra q=1 = HVI-greedy D96 (QUERY-JOIA)** | **(2)** D41/D96 | **direta** | **5.368/5.368** a ≤5,04e-7; argmax 99,98%; hipótese-B refutada (61,6%) |
| A21 | Desempate σ²-em-z + fallback | **(2)** DI-25.2/D91 | **direta** | s42 **1.965/1.965**; smoke **8/8**; geometria de bounds reproduzida |
| A22 | Regra q=10 = laço sequencial (D42) | **(2)** D42/B13.1 | **direta** | ⬆ **424/424 (100%)** no dado limpo (era 622/624); S2 refutada 95/424 |
| A23 | Retreino do zero K×60 épocas | (1) | direta | `epocas=60` em 8.020/8.020; `tempo_fit` sobe com n, sem warm-start |
| A24 | Sementes internas por iteração | **(2)** D22/D62/D91 | **direta bit-a-bit** | **8.020/8.020** (s42, base 42000) + **40/40** (smoke, base 0) |
| A25 | Guards do harness + integração in-memory | **(2)** DI-21/D78/D89 | direta | guardas instaladas, nunca dispararam, 30/30 |
| A27 | Escalabilidade M.11 | (1) | direta | **α = 0,956** [0,83–1,03]; R² linear 0,9885; busca 9,2–11,2 s constante |
| 🆕 A28 | **Checkpoint atômico (DI-43)** | **(2)** DI-43/DI-44 | **direta (smoke)** | 1 evento em `it=25`, **0,1747 s = 0,069% do wall**; projeção 0,109% |
| 🆕 A29 | **Não-perturbação da sonda (flag `sonda_on`)** | **(2)** G-6 | direta-declarativa + código | 2/2 sítios de emissão gateados; 2 testes AST/plumbing PASSAM; veredito `38b5584b` |
| 🆕 A30 | **⑤ schema v2 + `campanha_id` + `repo_hash`** | **(2)** G-3/G-7/B-03 | **direta** | s42: v1, `repo_hash=''` → smoke: **v2, `9ad0138a`, campanha preenchida** |
| 🆕 A31 | **Trava estrutural anti-colisão de blob (B-10)** | **(2)** B-10/D58 | código lido + testado | `if_generation_match=0` → `ColisaoDeBlob` → sem poda; 2 testes |
| 🆕 A32 | **O oráculo é GRATUITO no nosso banco** | **(2)** §4/D21 | **direta** | avaliação real = **7,401 s de 337.368 s = 0,00219% do wall** |
| A26 | ③ de `batch/ZDT4` alheia | **(3)→RESOLVIDO** | direta | causa-raiz PROVADA (infra); D29 não dispara; sai do denominador (§4.2) |
| T1 | Init de pesos `N(0,1e-3²)`/bias 0 | **T** | declarativa-apenas | echo 30/30; runtime invisível |
| T3 | Operadores SBX/PM nas 100 gerações internas | **T** | não-verificável | só `res.X` é gravado |
| T4 | Ordem do DataLoader / passos de Adam | **T** | declarativa-apenas | sem curva de loss por época |
| T5 | `preserve_all_rng()`/`guarded_pymoo_minimize()` | **T** | não-verificável | efeito nulo por desenho no pymoo 0.6.2 (E-02/DI-16.10) |

*(A02–A25 e A27 mantêm a numeração da F5; ~~T2~~ foi **descarregado** e virou A29.)*

### 2.2 Blocos narrativos — os aspectos que MUDARAM ou são NOVOS

*(Os 16 aspectos cujo veredito é idêntico ao da F5 — A01–A11, A13, A16, A19, A23, A25 — estão dissecados no relatório F5 `f5/relatorios_config/c149.md` §2.2 e foram **re-medidos** aqui em `04_s42_estrutural.py` com reprodução exata; não os reescrevo. Abaixo, apenas o que mudou.)*

---

**A17 — `res.X` = rank-0 da pop final ⚠ ERRATA CONTRA A MINHA PRÓPRIA F5.**
**(a)** O paper diz que a aquisição *"returns the set of S Pareto-efficient solutions"* — o tamanho é o que o front der (§4.2). **(b)** O bundle v2.2 explicita: `res.X` = rank-0 da pop final (≤pop, não fixo). **(c)** A F5 publicou *"`|front| == 1000` em 7.987 (99,6%) e **<1.000 em 33 gerações**"*. **Re-medi por duas vias independentes** (`05_a17_recheck.py`): (i) `n_front_acq` do ⑥ e (ii) contagem de linhas da ③ por geração. **As duas dão 5, não 33** — e concordam entre si em 8.020/8.020 gerações. As 4 células e os mínimos da F5 estavam certos; só a CONTAGEM estava errada:

| célula | G | `<1000` (⑥) | `<1000` (③) | mín |
|---|---|---|---|---|
| main/BBOB_F55 | 200 | 2 | 2 | **111** |
| main/BBOB_F22 | 200 | 1 | 1 | 497 |
| main/BBOB_F37 | 200 | 1 | 1 | 764 |
| main/MMF1 | 40 | 1 | 1 | 338 |

**(d)** A F5 já era internamente inconsistente: os percentuais que ela mesma citou ("2% das gerações" em BBOB_F55 de 200 = 4) somavam ~7, não 33. O correto é `|front| == 1000` em **8.015/8.020 = 99,94%**. A leitura causal não muda: front rank-0 menor que a pop só ocorre quando o NSGA-II converge e a diversidade em 2M colapsa — as 4 células são exatamente as de μ quase-constante. **(e)** **Classe (1)**, direta. *(A armadilha 7 da F5 — "um gate que exija 1.000 linhas/geração reprova gerações fiéis" — continua válida, agora com o número certo: 5.)*

---

**A22 — Regra q=10 (D42): de 622/624 para 424/424.**
**(a)** O paper avalia **todo** o front rank-0 (S=1000 sintéticos, 15.000 airfoil, 20.000 gamut) — a mistura deliberada de explotação e alta-incerteza é o coração do método (§4.2, §5.4). **(b)** D42 fixa `q=10` obtido pelo **mesmo** HVI-greedy iterado 10× sem reposição: o "off-design declarado" do config. **(c)** A F5 mediu `pick-1 argmax 624/624` e `lote completo 622/624 (99,7%)` sob a hipótese **S1** (arquivo de referência CRESCE com o μ do pick anterior). A verificação adversarial (`f5/adversarial/c149-A26.md` §Impacto-2) apontou que **as duas únicas falhas do lote pertencem à célula contaminada** — e eu confirmei relendo `queryjoia_batch_c149.csv`: ZDT4 `S1_lote = 198`; ZDT1 200/200; WFG9 200/200; DTLZ2 12/12; MMF16_20 12/12. **Excluindo a célula com ③ alheia: 424/424 = 100,0%** nas duas contagens. A hipótese S2 (arquivo congelado) fecha **95/424 = 22,4%** no dado limpo — continua REFUTADA. **(d)** As 2 falhas não eram do algoritmo: eram do fato de a ③ pertencer a outra execução (A26). Removida a contaminação, a reconstrução da regra D42 é **perfeita**. **(e)** **Classe (2)** D42/B13.1, **direta** — e o número **melhora**.

---

**A12 — Ativações da RAIZ: re-medição confirma a estrutura, corrige os valores.**
**(a)** §D.1 é explícito: *"Tanh ×2, ReLU ×2, CELU ×2, LeakyReLU ×2, ELU, Hardswish"* — modificação dos autores sobre Lakshminarayanan, feita para melhorar a qualidade da incerteza. **(b)** O bundle v2.2 fixa a convenção da RAIZ (net_n 0..9) contra a variante do cluster (1..11). **(c)** Re-agregando o rank de `val_mse` por índice sobre os **8.020 fits / 80.200 treinos** (`09_ativacoes.py`): net0 **7,27** ↔ net6 **7,27** (tanh, \|Δ\|=0,006) · net1 6,86 ↔ net7 6,78 (ReLU, 0,088) · net2 4,70 ↔ net8 4,66 (CELU, 0,046) · net3 3,86 ↔ net9 3,89 (LeakyReLU, 0,029); singletons ELU 4,66 e Hardswish 5,05 caem no meio. **σ(ranks) = 1,315 ≫ 0** ⇒ as redes **não são permutáveis**. Os valores diferem da F5 (6,95/6,77/4,85/4,02) por tratamento de empates no rank — a **estrutura de pares, que é o achado, é idêntica e mais nítida** (max\|Δ\| entre pares = 0,088). **(d)** O par (i, i+6) só pode ter o mesmo perfil de erro se compartilhar a ativação da 1ª camada, já que só difere pela semente `1000·s+net_n+1`. Isso reconstrói a convenção da raiz **a partir do dado**. ⚠ **O smoke NÃO reproduz o padrão** (40 fits, σ(ranks)=0,495, max\|Δ\|=1,775): 400 treinos são poucos para o sinal estatístico, e MMF1/D=2 é célula degenerada. A evidência de A12 **pertence à s42** e não é transferível ao smoke. **(e)** **Classe (1)**, indireta forte.

---

**A20/A21 — A query-joia no código PÓS-T11: fecha, mas a célula do smoke não discrimina.**
**(a)/(b)** Sem mudança: o paper não fornece regra de sub-seleção; a nossa é D41/D96 (HVI sobre μ normalizado pelo min/max do arquivo **observado**, `ref = nadir_obs × 1,1`, desempate por σ² agregada **em z**, fallback aleatório-do-front). **(c)** Rodei a reconstrução completa no **smoke T11** (`02_smoke_mecanismo.py`): `hvi_escolhido` reproduzido em **40/40**, argmax global em **40/40**, desempate σ²-em-z em **8/8**. **Mas a checagem é vazia para o `ref`**: `03_seeds_hvi_smoke.py` mostra que a célula MMF1/D=2/s0 tem **HVI ≡ 0 em 40/40** gerações (`n_hvi_pos = 0` em todas, `n_empatados = 1000` em todas, `hvi_escolhido ∈ {0.0}`), e os caminhos são 32× `fallback_aleatorio` + 8× `desempate_sigma`, **zero** pelo caminho `hvi`. Com HVI ≡ 0, as hipóteses A (`ref=1,1` literal) e B (`ref=1,1×nadir do front-ND`) são **indistinguíveis** — ambas fecham 40/40 trivialmente. **A refutação da hipótese-B continua sendo da s42** (5.368 iterações, 25 células, 61,6% × 100%). **(d)** A degenerescência do smoke é ela própria um resultado: MMF1 colapsou 40/40 na semente 0 exatamente como na semente 42 (F5 §6.2: MMF1 40/40) — **o modo (I) reproduz entre sementes**. A geometria também: fração de coordenadas do X escolhido num bound do box = **0,953 no fallback** e **0,688 no desempate** (s42: 1,000 e 0,403). **(e)** **Classe (2)** D41/D96/DI-25.2, **direta na s42**; no smoke, a prova é do desempate e da reprodução do valor, não do `ref`. Verificabilidade honesta declarada.

---

**🆕 A28 — Checkpoint atômico periódico (DI-43/DI-44): a instrumentação nova, medida.**
**(a)** Nada disso está no paper. **(b)** A T11-G5 (commit `936a687`) instalou `src/checkpoint.py` e ligou-o em 8 runners, incluindo o c149 — `src/c149_lbnmobo.py:604` (`_Checkpointer(...)`) e `:886` (`ckpt.talvez_gravar(bud, buf, iteracao=g)`). Racional: as 4 camadas + um ⑤ coerente a cada **25 iterações OU 30 min**, atomicamente, nos MESMOS caminhos das camadas finais; o ⑤ do checkpoint é `failed`/`checkpoint_em_andamento` para que `is_run_done` diga False. Isto importa **desproporcionalmente** ao c149: é o config mais caro da campanha (71,0 h-core só no main da s42) e um kill no meio de uma célula de 8,8 h entregava **zero dado**. **(c)** **MEDIDO no smoke**: 1 evento `checkpoint` (`rec` novo, ausente em 100% da s42), `iteracao=25`, `fe=46`, `n_linhas_terceira=51000`, `tempo_checkpoint_s=0,1747` — **0,069% do wall de 253,6 s**. Verifiquei por leitura de código (`src/checkpoint.py:97-131`) que `gravar()` **só lê** `bud.records`, `buf.pop_rows`, `buf.surr_rows`, `buf.timing_rows`: **não consome RNG, não muta o orçamento**, e falha de escrita é engolida (`except` → `log.guard`, nunca mata o run). E a ordem no laço preserva o invariante U7: `buf.update_timing(...)` é chamado **antes** de `talvez_gravar`, então o custo do checkpoint **não contamina `tempo_geracao_s`** — confirmado nos dados (A05 do smoke: 0 violações em 40/40). **(d)** Projetei o custo na escala REAL do c149 (`08`, tabela abaixo): na pior célula (main/ZDT1, 1.202.000 linhas de ③, 107 MB, 23 checkpoints) o custo é **47,4 s de 31.579 s = 0,150%**; nas 30 células, **367 s de 337.368 s = 0,109%**; em 30 sementes, **3,1 h-core de 2.811 h-core**. ⚠ **Ressalva de escala honesta**: o smoke validou o instrumento a **51.000 linhas e 1 evento**; a realidade do c149 é **1.202.000 linhas (23,6×) e 23 eventos (23×)**. O custo projetado é desprezível, mas a extrapolação não foi exercitada. **(e)** **Classe (2)** DI-43/DI-44, **direta** (medida no smoke, projetada na s42).

---

**🆕 A29 — Não-perturbação da sonda: o teto T2 da F5, DESCARREGADO.**
**(a)** Invariante 🔴 do `CONTRATO_DE_DADOS` §3.1: *"a sonda NÃO PODE alterar a busca"*. **(b)** Na F5 isto era **T2 — não-verificável** ("exige par com/sem sonda; com 1 run é indecidível"). A T11-G6 (commit `398d253`) instalou a flag `sonda_on` nos 5 runners Python que não a tinham, incluindo o c149. **(c)** Verifiquei **por código, sítio a sítio** (`grep` em `src/c149_lbnmobo.py`): a flag entra em `run_c149` (`:547`), atravessa para `_run_c149_inner` (`:578` `sonda_on=sonda_on`, `:582` na assinatura) e gateia **os DOIS únicos sítios de emissão** — a cadência (`:771` `if sonda_on and H.sonda_due(...)`) **e a emissão FINAL fora do laço** (`:965` `if sonda_on and g and ultima_sonda_g != g ...`). Este segundo sítio é decisivo: uma flag que pegasse só a cadência deixaria a sonda rodando no fim e **a prova seria falsa**. Rodei os dois testes que travam isto: `tests.test_gates_g6::test_a_flag_esta_NO_ESCOPO_de_cada_sitio_de_emissao` (prova por **AST**, não por grep) e `::test_a_flag_atravessa_o_wrapper_ate_o_inner` — **ambos PASSAM** (56 testes, OK). O teste AST tem controle documentado: substituiu um teste por grep que **passava verde enquanto c154/c262 estouravam `NameError`** no run real. **(d)** Veredito do par registrado: `T11_STATUS.md:119` dá **c149 `38b5584b`** (① bit-idêntica com e sem sonda, MMF1/s0), entre 9 de 11 pares provados. ⚠ **O artefato do par NÃO está preservado** — `evidencia_T11/g6_com|g6_sem` contêm **apenas os 9 configs MATLAB** (b1, b3, b4, c141, c217, c238, e7, e74, e103); nenhum c149. E na tabela §5 do handoff o c149 é **a única linha Python com ✅ e sem hash**, embora o hash exista no `T11_STATUS.md` — inconsistência entre os dois documentos. **(e)** **Classe (2)** G-6, verificabilidade **direta-declarativa** (código lido, arquivo:linha; testes executados por mim) **+ veredito registrado pela T11** (não re-medido por mim). Sai do balde T; **não** vira "direta" plena enquanto o par não for preservado.

---

**🆕 A30 — O ⑤ ganhou identidade de campanha.**
**(a)** Não existe no paper. **(b)** G-3/G-7/B-03: `campanha_id` + `repo_hash` preenchidos nos dois writers, `schema_version=2`, e `is_run_done` (`src/manifest.py:219-236`) passa a **recusar** manifesto v1 ou de campanha anterior — *"é o que impede uma célula de smoke ou pré-retrofit de entrar como resultado oficial"*. **(c)** Diff medido (`06_t11_campos.py`), s42 → smoke T11: `schema_version` **1 → 2**; `repo_hash` **'' (vazio) → `9ad0138a…`**; `campanha_id` **ausente → `9ad0138aa9a4_2026-07-31`**. E o diff completo dos manifestos mostra que, **fora esses campos**, o ⑤ do c149 é idêntico: `sigma_dict` **bit-igual** (`True`) e `params` idênticos **exceto `seed_base`** (42000 vs 0, i.e. a semente). **(d)** Isto é a prova mais forte de que **o mecanismo do c149 não mudou entre a s42 e o HEAD**: o dicionário semântico e todos os hiperparâmetros declarados são os mesmos. Corrobora a premissa do briefing ("a s42 é a busca que o código de hoje faria") **por medição**, não por asserção. **(e)** **Classe (2)** B-03/G-3/G-7, **direta**.

---

**🆕 A31 — A causa-raiz da A26 recebeu trava ESTRUTURAL (B-10) — melhor do que eu recomendei.**
**(a)** Nada no paper. **(b)** Minha F5 §8 recomendou um gate de detecção (A3) e a verificação adversarial acrescentou A4 (carimbo de execução `uuid+host` nos parquets). **O que foi feito é mais forte**: `src/gcs.py:151-236` passou a subir **cada** artefato com `if_generation_match=0` — *"só se o objeto NÃO existir"*: o GCS responde **412** e o upload **falha** em vez de passar por cima do objeto de outra execução, virando `ColisaoDeBlob` → status `colidiu_412`. Três consequências encadeadas, todas verificadas por leitura: (i) o blob de outra execução **nunca é sobrescrito**; (ii) na colisão **a cópia local NÃO é podada** (`mirror_run` faz `continue` antes do `os.remove` da linha 233) — *"Era a poda cega (`os.remove` em `gcs.py:155`) que tornava a quimera do c149 IRRECUPERÁVEL"*, diz o docstring, nomeando o meu achado; (iii) `verificar=podar` torna a **confirmação por md5** pré-condição da poda. Somado ao B-03 (A30), os **três** sítios de causa-raiz que o adversarial isolou (`naming.py:344`, `gcs.py:155`, `manifest.py:183`) estão cobertos. **(c)** Testes: `tests/test_espelho.py:107` (`assertEqual(st["surrogate"], "colidiu_412")`) e `:167` (`assertRaises(gcs.ColisaoDeBlob)`) — existem e são específicos. **(d)** ⚠ **Limite da evidência**: isto é **código lido + testado com fake de GCS**; nenhum dado real exercitou o caminho (o smoke roda com `enable_bucket=False`, `paths.bucket = None`). A trava está instalada; sua eficácia no M8/M9 é **projetada, não medida**. **(e)** **Classe (2)** B-10/D58, **código lido + teste unitário** — não direta.

---

**🆕 A32 — O oráculo é gratuito: o desencaixe de regime, agora com número.**
**(a)** LBN-MOBO é um método para o regime em que **a avaliação real domina o custo** — o paper roda airfoil (CFD) e gamut (impressora física), e a tese dele é *iteration efficiency*: *"the sole limiting factor for executing LBN-MOBO is our parallel processing or experimentation capability"* (§4.2). **(b)** A SPEC roda os 25 problemas **sintéticos analíticos** (§4), onde uma avaliação custa microssegundos. **(c)** Medido nos 30 manifestos (`timing.tempo_aval_real_s`, o cronômetro do portão único `budget.py:241-251`): **7,401 s de avaliação real em 337.368 s de wall = 0,00219%**. Em contraste, o **fit** consome 70,4% do wall no main e **87,0% no batch**, e a busca 28,9%/12,7%. Na célula MMF1: 0,0039 s de oráculo contra 507,7 s de wall. **(d)** O surrogate custa **45.600×** o que ele existe para poupar. Esta é a forma mais nua de enunciar o desencaixe que o agente-4 classificou E3=3 ("desencaixe severo de regime") e que o §6 mede pelo endpoint: não é que o c149 seja mal implementado — é que a bateria o roda num regime onde o mecanismo **não pode** pagar por si. Vale para todo SA-MOEA do estudo, mas é **maximal** aqui porque o c149 tem o surrogate mais caro da grade. **(e)** **Classe (2)** — consequência sancionada da escolha de banco (§4) + orçamento (D21/D61), **direta**. Confirma também **I-3**: `tempo_aval_real_s` é não-nulo nos dois corpora (a T11 declarou explicitamente **não** ter tocado as 5 medições manuais de c122/c149/c154/c262/e81, `T11_STATUS.md:140`).

---

**A26 — A ③ de `batch/ZDT4`: de classe (3) a defeito de infraestrutura com causa PROVADA.**
**(a)/(b)** O contrato exige que a linha da ③ marcada com `real_solution_id = s` seja o candidato que virou o FE `s`. **(c)** A verificação adversarial (`f5/adversarial/c149-A26.md`) **atacou o achado para derrubá-lo e ele subiu de hipótese a prova byte-a-byte**: a ③ consolidada é **bit-idêntica (md5 `1269b87f…`) ao arquivo de uma SEGUNDA execução da mesma célula feita no Mac (arm64) em 2026-07-24T13:01:52Z**, que existe em disco em `ua-dd-saea/data/experiments/batch/c149/`; casada com a ① *dessa* execução ela fecha **2000/2000 bit-exato** e os cinco testes que reprovaram passam **a 1e-7**. As minhas hipóteses **H2** (torch não bit-reprodutível) e **H3** (escrita concorrente na v6) foram **REFUTADAS** por um controle de determinismo que eu não tinha feito: nos 5 pares `(main/P, q10_P)` a geração 1 é idêntica por construção, e o bloco de sonda g=1 é **100,00% bit-idêntico** em ZDT1/DTLZ2/WFG9/MMF16_20 entre v5 e v6. Minha **H1** estava certa em substância. **Eu re-executei o gate G-1 nas 30 células** (`01_g1_regate.py`) e reproduzi o placar **ao dígito**: **29 OK a `frac=1,000000` e `max|ΔX|=0`; 1 RUIM — `q10_ZDT4`, `0/2000`, `max|ΔX| = 9,99983`**. **(d)** Causa-raiz: colisão de blob — `naming.blob_path()` sem identidade de execução + poda da única cópia local + `is_run_done` aceitando blob de qualquer dono. **Bússola D29 não dispara**: o defeito não toca o artigo, nem o código do autor, nem o surrogate. **(e)** Deixa de ser **(3) desvio inexplicado**: é **defeito de persistência de infraestrutura**, com causa provada, escopo 1/666, gêmeo íntegro em disco e **causa-raiz corrigida** (A31) + **gate instalado** (§4.1). Sai do denominador de fidelidade e vai para o veredito de contrato. **A célula, porém, ainda não foi reparada** (`T11_STATUS.md:397` lista "quimera c149 (rota A0→Mac)" como pendência V2).

---

## 3. Percentual por classe

**35 aspectos. T = 4 (fora do denominador). A26 sai do denominador de FIDELIDADE (§2.2, §4.2). DENOMINADOR = 31.**

| Classe | Aspectos | % (n/31) |
|---|---|---|
| **(1) conforme o artigo** | A03, A05, A06, A07, A08, A09, A10, A11, A12, A14, A15, A16, A17, A19, A23, A27 | **16/31 = 51,6%** |
| **(2) desvio sancionado** | A01 (D21/D61/D66) · A02 (D63/D87/D88) · A04 (DI-12.5/DI-21) · A13 (D22/A6) · A18 (B13.4/D30) · A20 (D41/D96) · A21 (DI-25.2/D91) · A22 (D42/B13.1) · A24 (D62/D91/D22) · A25 (DI-21/D78/D89) · **A28 (DI-43/44)** · **A29 (G-6)** · **A30 (B-03/G-3/G-7)** · **A31 (B-10/D58)** · **A32 (§4/D21)** | **15/31 = 48,4%** |
| **(3) desvio inexplicado 🎯** | — nenhum | **0/31 = 0,0%** |
| **T (teto declarado)** | T1 init de pesos · T3 operadores internos · T4 DataLoader/Adam · T5 preserve_all_rng | 4, à parte |
| **Fora do denominador** | A26 — defeito de persistência, causa provada, D29 não dispara (§4.2) | 1 |

**Denominador comparável com a F5** (mantendo A26 como (3), como a F5 fez): **(1) 16/32 = 50,0% · (2) 15/32 = 46,9% · (3) 1/32 = 3,1%**. Publico os dois porque a mudança de denominador é minha decisão e o autor tem de poder auditá-la.

**Marca declarativa visível** (Botão 3): A11, A13, A16, **A29**, **A31** — todos com corroboração anexada (custo de treino, `n_train/n_val`, `prob_var`×D, testes AST executados, testes unitários de colisão).

**Comparação F5 → T11**: (1) 59,3% → 51,6% e (2) 37,0% → 48,4% **não** indicam perda de conformidade — são efeito de 5 aspectos NOVOS, **todos (2)**, que a F5 não tinha porque a instrumentação não existia. Nenhum aspecto **mudou de classe para pior**; um (A26) saiu de (3).

---

## 4. 🆕 AS CORREÇÕES DA T11 — implementadas? funcionam de fato?

### 4.1 O gate G-1 (3×1) — a minha recomendação da F5 §8, INSTALADA e FUNCIONANDO ✅

**Recomendei** (F5 §8): *"cruzar, por célula com surrogate, o `X` da linha da ③ marcada com `real_solution_id` contra a ① (bit-a-bit) — 1 query, custo desprezível"*. **Está em produção** como `gate_3x1` em `scripts/gates_proveniencia.py:89-149`, integrado à bateria `gates_de_proveniencia()` (`:603`).

**Funciona de fato — três provas independentes:**

1. **Reproduz o achado**: re-executei o gate eu mesmo nas 30 células (`01_g1_regate.py`) → **29 OK · 1 RUIM**, `q10_ZDT4` com `frac=0,000000` e **`max|ΔX| = 9,99983`** — o mesmo dígito da F5 e do re-gate da T11 (`T11_STATUS.md:146`). Nas 29 boas: `frac = 1,000000`, `max|ΔX| = 0`, **zero zona cinzenta** — separação binária perfeita.
2. **Sabe dizer NÃO e sabe dizer SIM**: `tests/test_g1_controle_positivo.py` tem os **três** casos e todos passam — `test_quimera_sintetica_e_REPROVADA` ("o lado que faltava: prova que o G-1 sabe dizer NÃO"), `test_par_coerente_e_APROVADO` ("controle simétrico: sem ele, um gate que reprova TUDO passaria acima") e `test_parquet_ilegivel_e_INCONCLUSIVO_e_nao_derruba`. É exatamente a lição §4.2-1 da T11 ("um gate que nunca reprova é decorativo") aplicada.
3. **Foi endurecido depois de instalado**: o comentário `[conserto 2026-07-30 · achado #48]` (`:132-140`) documenta que o denominador era `len(achados)` e virou `len(marcadas)` — sem isso, uma ③ com `real_solution_id` **inexistente** na ① sairia do denominador e fecharia `frac=1,000000` verde sobre uma linha só. O gate foi auditado contra si mesmo.

**O que ainda falta**: cobertura. O gate vê **360/666** células; **200** (b5m, b5r, c311, moead_media, treed_media) não populam `real_solution_id` e ficam **CEGAS**. Para elas o adversarial propôs o invariante complementar `transf_params(③) ≡ z_mean/z_std(⑥.fit)` — **não implementado**.

### 4.2 A causa-raiz da quimera — corrigida com trava ESTRUTURAL ✅ (código)

| sítio da causa-raiz (adversarial) | correção T11 | evidência |
|---|---|---|
| `naming.py:344` — `blob_path` sem identidade de execução | **não alterado** (a função continua `f"{prefix}/{filename}"`) | código lido |
| `gcs.py:155` — poda cega da única ③ local | **`if_generation_match=0` → 412 → `ColisaoDeBlob` → `continue` ANTES do `os.remove`**; e `verificar=podar` exige md5 confirmado | `src/gcs.py:151-236`; `tests/test_espelho.py:107,167` |
| `manifest.py:183` — `is_run_done` aceita blob de qualquer execução | **B-03: `campanha_id` obrigatório**; manifesto v1 ou de campanha anterior ⇒ `False` | `src/manifest.py:219-236`; medido em A30 |

**Avaliação**: a trava é **melhor** que a minha A4 (carimbo de execução) porque **impede** a colisão em vez de detectá-la a posteriori. O `blob_path` continua sem identidade, mas isso deixou de importar: com `if_generation_match=0`, dois runs para o mesmo blob resultam em `colidiu_412` e a cópia local sobrevive. ⚠ **Verificação é por CÓDIGO LIDO + teste com fake de GCS**; nenhum dado real exercitou o caminho (o smoke roda `enable_bucket=False`, `paths.bucket = None`).

### 4.3 Checkpoint atômico (DI-43) — implementado e MEDIDO ✅

Ver A28. Ligado em `src/c149_lbnmobo.py:604,886`. **Disparou no smoke**: `rec="checkpoint"`, `iteracao=25`, `n_linhas_terceira=51000`, `tempo_checkpoint_s=0,1747`. Não consome RNG nem muta o orçamento (código lido, `src/checkpoint.py:97-131`). Não contamina `tempo_geracao_s` (ordem no laço + A05 do smoke: 0 violações em 40/40).

| célula (pior caso) | G | linhas ③ | MB | nº ckpt | custo proj. | wall | % |
|---|---|---|---|---|---|---|---|
| main/ZDT1 | 600 | 1.202.000 | 107,0 | 23 | 47,4 s | 31.579 s | 0,150% |
| main/ZDT3 | 600 | 1.202.000 | 106,5 | 23 | 47,4 s | 31.628 s | 0,150% |
| **30 células** | — | 16,1 M | — | — | **367 s** | **337.368 s** | **0,109%** |

⚠ **Lacuna de escala**: validado a **51.000 linhas / 1 evento**; o c149 real chega a **1.202.000 linhas / 23 eventos** (**23,6× / 23×**).

### 4.4 Flag `sonda_on` (G-6) — implementada, testada; o ARTEFATO do par falta ⚠

Ver A29. Os 2 sítios de emissão estão gateados (`:771` cadência, `:965` emissão final fora do laço); os 2 testes de plumbing **passam** (executados por mim). **O que falta**: o par com/sem sonda do c149 **não foi preservado** (`g6_com`/`g6_sem` só têm os 9 MATLAB). Só existe o veredito registrado `38b5584b` (`T11_STATUS.md:119`), e o handoff §5 lista o c149 como **a única linha Python com ✅ sem hash** — inconsistente com o `T11_STATUS`. **Recomendo preservar o par** (custo: ~9 min, o smoke leva 251 s; é o mesmo `naoperturbacao.py --par c149 MMF1 0`).

### 4.5 A caça ao padrão do c217 (campo presente, dado sentinela) — NEGATIVA ✅

Varri **todas as 21 colunas** da ③ nos dois corpora procurando constantes e nulos (`06_t11_campos.py`). Achados: `pred_classe`, `pred_score`, `pred_confianca` são **100% NULOS** (82.000/82.000 no smoke; 1.202.000/1.202.000 em main/ZDT1) — **mas isto é DECLARADO**: `pred_tipo = 'valor'` está gravado como constante na própria ③ e o `sigma_dict` diz `"pred_tipo": "valor — mu_*/sigma_* por objetivo"`. Não é sentinela: é um config não-classificador cumprindo o contrato. As demais constantes (`algoritmo`, `problema`, `semente`, `modelo_flag='BNN-ensemble(K=10)'`, `espaco_modelo='cru'`, `transf_tipo='zscore'`) são metadados por desenho. **Nenhum `-1` sentinela, nenhum campo prometido e vazio.** O c149 **não** repete o padrão do c217 (`pmid_ids = [-1,-1,-1,-1,-1]` em 42/42).

### 4.6 O que NÃO se aplica ao c149 (e por quê)

`REGRA_DO_ROTULO` (I-2), `sonda_estratificada` (I-6), `y_treino_dist` (I-5), `pmid_ids`/`ref_ids` (I-1) — **todos ausentes nos dois corpora, corretamente**: são instrumentos de **classificador par-a-par** (b4, c122, c217, e74). O c149 é regressor (`pred_tipo='valor'`); a régua Sobol de 2.000 pontos é a sua instrumentação de qualidade, e ela existe e está limpa (§6). **Regra 12 do CONTRATO não se aplica** — não há bloco estratificado a segregar.

### 4.7 O que ficou PENDENTE

| # | pendência | fonte |
|---|---|---|
| P1 | **A célula `batch/ZDT4` não foi reparada.** A rota A0 (baixar a geração *noncurrent* do bucket, versionamento ON, ~2 min / 17,6 MB) não foi executada | `T11_STATUS.md:397` |
| P2 | `_FONTES.csv` do consolidador **continua com cabeçalho e 0 linhas** (verifiquei: `wc -l` = 1) — a proveniência por camada nunca foi registrada (A5 do adversarial) | medido |
| P3 | Par G-6 do c149 **não preservado** (só o hash registrado) | §4.4 |
| P4 | Invariante complementar `transf_params(③) ≡ z_mean/z_std(⑥)` para as 200 células cegas ao G-1 — não implementado | §4.1 |
| P5 | Checkpoint validado a 1/24 da escala real do c149 | §4.3 |

---

## 5. Comparação canônica (Entrega 2)

**Setup do paper** (Anexo J + §C.1/C.2/§5): ZDT1/ZDT2 30D · ZDT3 6D e 30D · DTLZ1 6D M=3 · DTLZ4 6D M=3 · DTLZ5 20D M=3 · airfoil 5D · gamut 44D. `init = S` **aleatório uniforme**, `S = 1000`–20.000, `Q ≈ 8–10` ⇒ **11.000 FE** nos ZDT/DTLZ. **1 execução, sem repetições, sem testes estatísticos.** Métricas: **HV aproximado por amostragem** com ref não publicado + regret próprio — **o paper não reporta IGD nem IGD+**. Números só em figuras; o único par citável é o de tempo (§C.1.4).

| Grupo (nosso) | Células | Veredito | Sentido executado (re-medido) |
|---|---|---|---|
| **ZDT1 (D=30, M=2)** | 1 main + 1 batch | **âncora direcional** — família, D **e** M idênticos; orçamento 929 vs 11.000 (**11,8×**) | q=1: IGD+ **0,2209 vs nsga2 0,4441 (+50,3%)** → **PASSA**. q=10: IGD+ **0,0737 vs e81 0,7498 (10,2×)** e **vs sobol_batch 1,8086 (24,5×)** → **PASSA com folga** |
| **ZDT3 (D=30, M=2)** | 1 main | **âncora direcional** | IGD+ 0,3555 vs nsga2 0,3443 = **−3,3% — dentro do piso de ruído (≤58,98%, O-18) ⇒ inconclusivo**; HV −42,6% → **NÃO PASSA**. Registrado como está |
| **DTLZ1 (nosso 7D M=3 × paper 6D M=3)** | 1 main | **incomensurável (eixo orçamento)** — D quase igual, **216 vs 11.000 FE = 51×** | Faixa-guia J inaplicável: o paper não publica número |
| **DTLZ4 (nosso 12D × paper 6D)** | 1 main | **incomensurável (eixo D + orçamento)** — D 2×, orçamento 30× | — |
| **Escalabilidade (§C.1.4 / M.11)** | 30 | **comparável em TEMPO** (mesma config de NSGA-II) | Aquisição: paper **13,5 s** (cluster GPU) × nosso **9,2–11,2 s** (1 core CPU), constante em n e D → **PASSA**. Treino: paper **167 s @ n≈10.000** (A100) × nosso **132,4 s @ n=2.319** (CPU), **α=0,956** ⇒ ~600 s @ n=10.000 → mesma ordem → **PASSA** |
| ZDT2, DTLZ5, airfoil, gamut | 0 | **sem interseção** | — |
| ZDT4/6, DTLZ2/3/7, WFG1/2/4/5/9, MMF1/4/11_L/16_20 | 15 | **sem interseção** (famílias ausentes do paper) | — |
| BBOB_F1/5/17/22/37/49/55 | 7 | **sem comparação com literatura por regra** (front empírico) | — |

**Veredito canônico (inalterado vs F5): 3 âncoras direcionais no sentido certo (ZDT1 q=1, ZDT1 q=10, escalabilidade); 1 âncora inconclusiva/negativa honestamente registrada (ZDT3); 26 células incomensuráveis ou sem interseção; ZERO células estritamente comparáveis. NUNCA "artigo melhor"** (Botão 4) — o paper não publica um único indicador que a nossa régua saiba ler, e o orçamento difere por 11,8–51×.

---

## 6. Saúde (s42 = corpus principal) — com 3 correções aos meus próprios números

### 6.1 ⚠ A armadilha do `bloco` como STRING — corrijo dois números que publiquei

O adversarial apontou que `sonda_f52e.csv` tem `bloco` como **object**; ordenar sem `to_numeric` põe o bloco "98" por último (lexicográfico). **A F5 publicou os números da ordenação errada.** Re-medi as duas (`07_saude.py`):

| ΔWAPE 1º→último bloco (mediana) | ordem lexicográfica (**o que a F5 publicou**) | **ordem numérica (correta)** |
|---|---|---|
| main (q=1), 56 séries | +11,15% | **+15,61%** |
| batch (q=10), 12 séries | −19,59% | **−41,24%** |

O número oficial da F5.2e para o main era **+16%** — praticamente idêntico ao correto (+15,61%). **A fase F5.2e estava certa e eu estava errado.** Excluindo a célula contaminada, o batch fica **−41,24%** (idêntico: as duas linhas de ZDT4, −78,3% e +155,3%, caem em lados opostos da mediana). **A conclusão qualitativa não muda e fica mais forte**: em q=1 o surrogate **piora** com o tempo; em q=10 ele **aprende**, e o ganho é 2× maior do que a F5 disse.

### 6.2 Régua Sobol — 9.138 medições, 0 NaN, 2.000 válidas/bloco em 100%

*(Regra 12 do CONTRATO não se aplica: o c149 não tem bloco `sonda_estratificada`.)*

| Regime | cobertura95 (mediana, último bloco) | correlação (mediana) | ΔWAPE |
|---|---|---|---|
| **main (q=1)** | **0,346** (F5 dizia 0,360) | **0,653** (F5: 0,627) | **+15,61%** |
| **batch (q=10)** | **0,880** | **0,936** | **−41,24%** |

Cobertura main: mín **0,026 (MMF1)**, máx **1,000 (ZDT3)**. A cobertura de 0,35 **não é reprovação** — é a família (protocolo §3: *"σ estocástico: cobertura baixa É da família — medir, não reprovar"*); o e7 (MC-dropout) dá 0,81. O init `N(0,1e-3²)` faz as K=10 redes partirem de quase a mesma função constante e 60 épocas de Adam a 5e-4 não as separam. O próprio paper reconhece: *"a fundamental limitation of Bayesian neural networks"* (§4.1).

### 6.3 Ganho sobre o DoE — re-verifiquei 11 das 25 células main, todas batendo

| célula | IGD+ só-DoE | IGD+ final | ganho |
|---|---|---|---|
| BBOB_F1/5/17/22/37/49/55 (7) | — | **idêntico ao 15º dígito** | **+0,00%** em 7/7 |
| DTLZ1 | 115,243659 | 103,443050 | **+10,24%** (F5: +10,2%) |
| DTLZ3 | 446,915135 | 336,892952 | **+24,62%** (F5: +24,6%) |
| DTLZ2, DTLZ4 | 0,448419 / 0,819604 | idênticos | **+0,00%** |

Reproduz a F5 exatamente. Os agregados da F5 (main mediana **0,00%**, **13/25** com ganho literalmente zero; batch mediana **+40,50%**) permanecem válidos e não foram contestados pelo adversarial — **cito, não refiz por inteiro** (o recompute do `reference_set` de DTLZ7/WFG estourou o orçamento de tempo).

**As duas causas do ganho-zero seguem separadas e medidas** (F5 §6.2), e o smoke as **confirma numa segunda semente**: em MMF1/s0 o HVI colapsa em **40/40** gerações (`n_hvi_pos=0`, `n_empatados=1000`), com **0** decisões pelo caminho `hvi`, 32 por `fallback_aleatorio` e 8 por `desempate_sigma`, e **95,3%** das coordenadas do X escolhido no fallback caem exatamente num bound do box. É o **modo (I)** — colapso do HVI → σ²-máx → **vértices do hipercubo** — reproduzido em semente independente.

### 6.4 Posição vs pisos e tempo (re-medidos)

- **Rank médio de IGD+ nos 25 problemas main: 14,34 de 17 — o ÚLTIMO da grade** (c262 3,71 · c122 4,72 · c141 4,84 · … · moead 13,44 · **c149 14,34**). Reproduzido exatamente.
- **Bate o melhor piso em 4/25**: ZDT6 **+72,2%**, ZDT1 **+50,3%**, DTLZ7 **+23,6%**, WFG5 **+13,7%**.
- **No batch a foto inverte**: bate `sobol_batch` em 2/5 e `e81` em 2/5, e **ZDT1 q=10 é a vitória mais larga da análise** (10,2× e 24,5×).
- **Wall s42: 93,7 h-core** (71,0 main + 22,7 batch) — o config mais caro da campanha. ⚠ **ERRATA F5**: publiquei *"projeção 2.131 h-core p/ 30 sementes"* — esse é o **main sozinho** (71,0 × 30 = 2.130). **Com o batch: 2.811 h-core.**
- Fração do wall no fit: **0,704 main / 0,870 batch**. Sonda: 0,037%. Checkpoint (novo): 0,109%.

**Comportamento de 1 semente ≠ infidelidade** (caveat 8). O rank 14,34 é o que o agente-4 previu antes de qualquer run (E2=5, E3=3) e o que A32 explica em uma linha: **o oráculo custa 0,00219% do wall**. O mecanismo está provado fiel (§2); a nota baixa é de comportamento no regime errado.

---

## 7. SCORE, recomendação e VEREDITO

# SCORE: 9,5 / 10 — recomendação: **ACEITAR + CAVEAT** (caveat reduzido)

### VEREDITO: **MELHOROU** (9,0 → 9,5)

**A causa, nomeada — e é CORREÇÃO REAL, não amostra nova:**

1. **O único aspecto classe (3) da F5 deixou de existir (−1,0 → 0 de desconto por (3)).** A verificação adversarial provou a causa-raiz byte-a-byte (colisão de blob, `md5 1269b87f…` idêntico ao gêmeo do Mac de 2026-07-24), **refutou** as minhas H2 e H3, e estabeleceu que **D29 não dispara**: o defeito não toca o artigo, nem o código do autor, nem o surrogate. É persistência, não fidelidade.
2. **A minha recomendação da F5 §8 virou código, com controle positivo E negativo, e eu a re-executei**: `gate_3x1` em `scripts/gates_proveniencia.py:89` reproduz **29 OK / 1 RUIM** com `max|ΔX| = 9,99983` ao dígito.
3. **A causa-raiz recebeu trava estrutural mais forte que a recomendada**: `if_generation_match=0` → `ColisaoDeBlob` → sem sobrescrita e **sem poda da cópia local**, + md5 como pré-condição da poda, + `campanha_id` obrigatório no `is_run_done`. Os 3 sítios isolados pelo adversarial estão cobertos.
4. **Um número do mecanismo MELHOROU**: A22 sobe de **622/624 (99,7%) para 424/424 (100,0%)** — as 2 únicas falhas eram da célula contaminada. A reconstrução da regra D42 é perfeita no dado limpo.
5. **Um teto foi descarregado**: T2 (não-perturbação da sonda) virou o aspecto A29, com a flag `sonda_on` gateando **os dois** sítios de emissão e 2 testes de plumbing (AST + wrapper→inner) que **executei e passam**.
6. **Instrumentação nova, medida e barata**: checkpoint DI-43 disparou no smoke (0,069% do wall) e projeta 0,109% na escala real — relevante porque o c149 é o config mais caro (2.811 h-core em 30 sementes).
7. **A premissa do corpus foi PROVADA, não assumida**: `sigma_dict` **bit-igual** e `params` idênticos (exceto `seed_base`) entre a s42 e o HEAD ⇒ o mecanismo do c149 não mudou; a s42 é literalmente a busca que o código de hoje faria.

**Por que 9,5 e não 10** — três descontos de 0,5 no total:
- A **célula não foi reparada** (P1): a ③ de `batch/ZDT4` segue inutilizável para análises Classe A, e a rota A0 (2 min, 17,6 MB, versionamento do bucket ON) não foi executada. O contrato não está perfeito.
- A trava B-10 (A31) e o par G-6 (A29) são **código lido / veredito registrado**, não dado medido — nenhum run real exercitou a colisão, e o par do c149 não foi preservado.
- **Eu próprio errei 5 números na F5** (§8) — nenhum muda classificação, mas reduz a confiança de reporte e obriga o autor a preferir os números deste relatório.

**Por que não subiu mais**: o **mecanismo não mudou** — 16 dos 31 aspectos têm veredito idêntico ao da F5, re-medidos e reproduzidos. O que subiu foi o entorno (defeito explicado + corrigido + gateado, teto descarregado, instrumentação nova). "Manteve" seria o veredito se apenas a instrumentação tivesse mudado; aqui houve **correção de causa-raiz e melhora de um número do próprio mecanismo**.

**Caveat (reduzido)**: excluir a camada ③ — front de aquisição e 101 blocos de sonda — da célula `batch/ZDT4` de qualquer análise da Classe A. **As ①/②/④/⑥ e todas as métricas dessa célula seguem válidas** (a ① é byte-idêntica ao disco local da v6, e as duas execuções concordam no endpoint: `min f0 = 0,00000`, `min f1 = 92,2548`, |ND| 15×16). As 202 linhas de `sonda_f52e.csv` dessa célula descrevem o run do Mac — nenhum agregado se move.

---

## 8. Aspectos classe (3) 🎯

# NENHUM.

O único (3) da F5 — **A26** — foi investigado adversarialmente, teve **causa-raiz provada byte-a-byte**, **D29 não dispara**, a causa foi **corrigida no código** (§4.2) e um **gate que a detecta foi instalado e re-executado por mim** (§4.1). Ele sai do denominador de fidelidade e vive agora no veredito de contrato (§9), com a ação P1 pendente.

**Nada novo entrou como (3).** As 31 checagens estruturais das 30 células (`04_s42_estrutural.py`) fecharam **30/30** em 17 invariantes e **8.020/8.020** em 4 invariantes por-geração, incluindo as **8.020 sementes NSGA-II bit-a-bit** — e o smoke T11 repetiu 25 das 27 checagens em código pós-T11 (as 2 exceções são declaradas: A20-refutação e A12, que exigem escala que 40 fits não dão).

---

## 9. Veredito de contrato, teto T e armadilhas

### 9.1 Contrato

- `integridade_f52a.csv` (alg=c149): **0 linhas** (de 111 no estudo). `contrato_f52b.csv` (alg=c149): **0 linhas** (de 197). Re-verificado.
- F5.1: **30/30 exit=0 VERDE**. **G-1 (gate NOVO, não existia na F5.1): 29 OK / 1 RUIM** — é o único vermelho do config, e é o P1.
- **Quase-⚠ por desenho, com regra citada**: `pred_classe`/`pred_score`/`pred_confianca` NULOS em 100% (`pred_tipo='valor'`, gravado na própria ③) · `real_solution_id` só nas linhas do escolhido, só no regime `online` · a ② é o **arquivo acumulado**, não população (`|②(g)| = 11D−1+q·g`, fecha exato 30/30) · sem camada ⑦ (regime online) · `schema_version=1` e `repo_hash=''` na s42 são **pré-T11**, corrigidos no HEAD (A30).

### 9.2 Teto T — 4 itens (T2 saiu)

- **T1** — Init `N(0,1e-3²)`/bias 0: só echo; o estado inicial dos pesos não sobrevive ao run.
- **T3** — Operadores SBX/PM ao longo das 100 gerações internas: só `res.X` é gravado; 100 ger. × 1.000 indivíduos não deixam rastro.
- **T4** — Ordem do DataLoader (sem shuffle) e nº real de passos de Adam: `epocas`/`batch`/`n_train`/`n_val` são echo; não há curva de loss por época.
- **T5** — `preserve_all_rng()`/`guarded_pymoo_minimize()`: efeito **nulo por desenho** no pymoo 0.6.2 (E-02/DI-16.10).
- ~~**T2**~~ — **DESCARREGADO** → A29.

### 9.3 O que este corpus NÃO permite verificar

1. **A trava anti-colisão (B-10) na prática** — o smoke roda `enable_bucket=False`; o caminho 412 nunca foi exercitado com GCS real. Só o M8/M9 dirá.
2. **O checkpoint na escala do c149** — validado a 51.000 linhas / 1 evento contra 1.202.000 / 23 reais (23,6×/23×).
3. **A não-perturbação da sonda no c149 por artefato** — o par existe como veredito (`38b5584b`), não como dado auditável.
4. **A concorrência do M8/M9** — 1 célula por config no smoke; a campanha são 695 × 30 sementes noutro regime (disco, memória, teto).
5. **A hipótese-B do `ref` do HVI no código pós-T11** — o smoke (MMF1) é degenerado (HVI≡0 em 40/40) e não discrimina; a refutação continua ancorada na s42.
6. **O regime do paper** — 11.000 FE, batch de 1.000, avaliação cara. Nenhuma célula nossa chega perto; A32 quantifica: o oráculo custa **0,00219%** do wall.

### 9.4 ⚠ As 5 erratas contra a MINHA PRÓPRIA F5 (todas medidas, nenhuma muda classificação)

| # | a F5 dizia | é | como sei |
|---|---|---|---|
| 1 | A17: `\|front\|<1000` em **33** gerações | **5** (99,94% em 1.000) | 2 vias independentes (⑥ `n_front_acq` e contagem da ③) concordam |
| 2 | ΔWAPE main **+11,2%** | **+15,61%** | ordenação lexicográfica de `bloco`; a F5.2e (+16%) estava certa |
| 3 | ΔWAPE batch **−19,6%** | **−41,24%** | idem |
| 4 | Projeção 30 sementes **2.131 h-core** | **2.811** (2.130 é só o main) | 93,7 h × 30 |
| 5 | A22 lote **622/624** | **424/424** no dado limpo | as 2 falhas eram da célula A26 |
| *(menor)* | A12 ranks 6,95/6,77/4,85/4,02 · A27 α=0,98 | 7,27/6,86/4,70/3,86 · α=0,956 | tratamento de empates / detalhe de ajuste; conclusões idênticas |

### 9.5 Armadilhas confirmadas (atualizadas)

1. Chave de evento é **`rec`**; o evento de geração é **`decision`** (não `c149_gen`) — o `caminho` carrega `c149_gen:{hvi|desempate_sigma|fallback_aleatorio}`. Término em `motivo_parada` no ⑤ **e** no footer.
2. **A ③ grava μ e σ DES-PADRONIZADOS na escala nativa**, e `sigma_*` é **σ, nunca σ²** (A-02/DI-16.9). Comparar com `sigma2_agg_sel_z` sem elevar ao quadrado e dividir por `std` acusa falso desvio de 2 ordens.
3. **O `ref` do HVI D96 é `1,1` literal** no espaço normalizado pelo min/max do **arquivo**. "1,1 × nadir do FRONT-ND" fecha 61,6% e acusa falso desvio. **⚠ NOVO: a célula MMF1 não discrimina** (HVI≡0) — não valide o `ref` nela.
4. **No q=10 o arquivo de referência CRESCE dentro do lote** com o μ do pick anterior. "Arquivo congelado" fecha 22,4% — falso desvio garantido.
5. `mu_sel_nat`/`hvi_escolhido`/`n_empatados` do ⑥ referem-se ao **PICK 1**; os 10 estão em `lote_solution_ids`; a ordem das linhas da ③ **não** é a ordem dos picks — casar por `real_solution_id`.
6. `hvi_escolhido == 0` **não** implica colapso: o log arredonda a 6 casas (2.037 colapsos reais × 115 arredondamentos, s42).
7. **`|front| < 1000` não é defeito** — e são **5 gerações**, não 33 (errata 1). Um gate que exija 1.000 linhas/geração reprova 5 gerações fiéis.
8. `pred_classe`/`pred_score`/`pred_confianca` **NULOS em 100%** — `pred_tipo='valor'`. **Não é o padrão-sentinela do c217**: é declarado no `sigma_dict` e na própria ③.
9. A ② é o **ARQUIVO acumulado**, não uma população.
10. **Cobertura95 de 0,35 é da família**, não infidelidade (e7/MC-dropout: 0,81). Medir, não reprovar.
11. **`prob_var` do PM varia com D** (0,5 → 0,0333): default do pymoo 0.6.2, **não** "1/30 hard-coded" (DI-23).
12. Validar `S=1000`/`Q=10`/init uniforme/"batch inteiro avaliado" do paper contra o run **reprova um run FIEL**.
13. **Ganho de IGD+ zero em 13/25 é RESULTADO, não bug**, com **duas** causas distintas (colapso do HVI → vértices; fantasia do μ → WAPE 3,08 em DTLZ2).
14. **A ③ de `batch/ZDT4` está contaminada** — excluir de transversais que usem sonda ou front de aquisição do c149-batch; as métricas são válidas.
15. **⚠ NOVO — `bloco` de `sonda_f52e.csv` é STRING**: ordenar sem `to_numeric` move o ΔWAPE do batch de −41,24% para −19,59%. Foi assim que eu errei.
16. **⚠ NOVO — o smoke Python do c149 EXISTE** (`evidencia_T11/smoke_python/`), contra o que o handoff §15.2 e o `LEIA-ME.md` afirmam.
17. **⚠ NOVO — não confundir corpora**: `campanha_id`/`schema_version=2`/`repo_hash` **não existem** na s42 (é pré-T11) e o `rec="checkpoint"` também não. Acusar a s42 de campos faltantes é a ERRATA 11 da própria T11 repetida.

---

**Artefatos desta análise** (único diretório de escrita): `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/c149/`

*Scripts*: `01_g1_regate.py` (re-execução do G-1 nas 30) · `02_smoke_mecanismo.py` (27 checagens no smoke T11) · `03_seeds_hvi_smoke.py` (A24 + diagnóstico de degenerescência) · `04_s42_estrutural.py` (re-medição das 30 células) · `05_a17_recheck.py` (a errata do A17, 2 vias) · `06_t11_campos.py` (campos T11 + caça ao sentinela) · `07_saude.py` (sonda com a ordenação corrigida) · `08_ganho_doe.py` (ganho sobre o DoE) · `09_ativacoes.py` (convenção da RAIZ).
*Evidência*: `g1_regate_c149.csv` (30 linhas) · `s42_estrutural_c149.csv` (30×40) · `smoke_mecanismo_c149.json` · `ganho_doe_c149.csv` · `ganho_doe.log` · `timing_c149_t11.csv`.
*Dados lidos (READ-ONLY)*: `resultados_experimentos/c149/{label}/42/` (30 células) · `evidencia_T11/smoke_python/experiments/main/c149/` (6 artefatos) · `ua-dd-saea/src/{c149_lbnmobo,checkpoint,gcs,manifest,naming,budget,standalone_harness}.py` · `scripts/{gates_proveniencia,naoperturbacao}.py` · `tests/{test_gates_g6,test_g1_controle_positivo,test_espelho}.py` · `f5/{metricas_finais_f52c,sonda_f52e,integridade_f52a,contrato_f52b}.csv` · `f5/adversarial/c149-A26.md` · `f5/baterias/c149/queryjoia_batch_c149.csv`.
*Testes executados*: `tests.test_gates_g6` (56, OK) · `tests.test_g1_controle_positivo` (3, OK).
*Interpretador*: `/Users/gmello/Documents/python_venvs/mestrado_experimentos_dissertacao/bin/python`. **Nenhuma escrita fora do diretório acima; nenhuma célula executada; `data/experiments` intocada.**