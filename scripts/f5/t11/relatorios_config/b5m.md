# RELATÓRIO DE FIDELIDADE T11 — `b5m` (Prob-MOEA/D, mode 72) · validação da rodada final · comparação contra a F5

**Corpora usados.** ⓐ **s42 = 45/45 células** (`resultados_experimentos/b5m/{label}/42/`, 25 off + 20 sweep small/medium × lhs/mvns) — **o corpus do MECANISMO**, re-medido do zero nesta análise (2.701.620 linhas de ③, 30.165 eventos `decision`, 45 ⑦, 45 manifestos). ⓑ **O smoke T11 EXISTE e está preservado** — o briefing diz "NÃO PRESERVADO (tempdir removido)"; **é falso para este config**: `evidencia_T11/smoke_python/experiments/off/b5m/exp_off_b5m_MMF1_0.*`, 7 camadas completas, `repo_hash=9ad0138a…` ≡ HEAD, 801 eventos, 2.725,3 s de wall. **Todas as correções da T11 que tocam o b5m puderam ser medidas em dado, não só lidas em código.** ⓒ **Bancada isolada** (funções puras do desdeo vendorizado no `env_b5`, sem rodar célula) para a cadeia A8 e para o caminho de atributo do `flag_vetores_degenerados`.

> ⚠ **Correção ao briefing #2:** o handoff §15.5 promete a cadeia A8 "observável nas DUAS pontas". **Medido: só UMA ponta funciona.** `p_wrong_stats` e `n_substituicoes` são reais e reconciliam com a ③; `flag_vetores_degenerados` é **código morto — `None` em 801/801 eventos, e em 100% dos runs futuros**, por caminho de atributo inexistente (`src/b5_prob.py:118`). É exatamente o padrão do c217 que a torre mandou procurar, em versão mais silenciosa (AttributeError engolido por `except Exception`).

---

## 1. Ficha do mecanismo (condensada)

Mazumdar, Chugh, Hakanen & Miettinen (2022), *IEEE TEVC* 26(5):1182-1191 — **offline puro**: um Kriging por objetivo treinado **1×** no dataset (N_D=109 no paper), nenhuma f verdadeira na busca. Motor MOEA/D com PBI; a contribuição é a **seleção**: S=1000 amostras MC da posterior (Eq. 10) → PBI → PDF por KDE → substitui o vizinho se `P_wrong(g'≤g_j) < 0,5` (§III-B, l. 263 do .md), comparação **um-para-muitos** na vizinhança de 20. Paper §IV-A (re-lido, l. 306/310/314/320): FE_max = **40.000** aval-surrogate; S=1000; SBX η=30/p=1,0; PM η=20/p=1/n; α=2; vizinhança 20; **θ do PBI de 0 a 500 com as FE**; simplex-lattice; **adaptação dos vetores a cada 10ª geração**; pop. inicial = o dataset (Alg. 1 L2); N_D=109.

**Divergências sancionadas nomeadas** (bússola D29, precedência dados→sigma_dict→bundle→DI→agente-4→paper): (i) **KDE→CDF empírica MC** — bloco `ProbMOEAD_select.py:66-75` comentado, âncora `b5-mode72-kde` (🟠 CÓDIGO, SPEC:815 "quase-fiel"); (ii) MC via `truncnorm(−3,3)` (🟠 código); (iii) **pop. inicial = LHS NOVO** — `init_pop` comentado em todos os modes (**B18.9**, SPEC:815/1529) ⇒ ② vazia e `real_solution_id` NULL (DI-16.17); (iv) GPR `C(1,(1e-3,1e3))·RBF(10,(1e-2,1e2))`, `alpha=0`, `n_restarts=9`, `normalize_y=False`, sklearn **0.21.3** (D80), desdeo VENDORIZADO root-first; (v) orçamento = o **dataset** (**D90**/D51/D67), não 109; (vi) ⑦ = a última população avaliada 1× na verdade, ND filtrado **depois** (DI-08/DI-13.9); (vii) sonda de 1 bloco × 20.000 com `geracao=NULL` (DI-13.5); (viii) **Hyb fora do escopo** (D56). Rampa θ recalculada **por offspring** com `FE_total=40000` obrigatório (**B18.8**). Piso `moead_media` (mode 12 = Gen-MOEA/D do próprio paper) = **ablação exata** (D77/DEF-E3). **Novo na T11:** âncora `b5-pwrong-stats` (acumulador READ-ONLY em `ProbMOEAD_select.py:31-43`, registrada em `anchors.json`).

---

## 2. DISSECAÇÃO DOS ASPECTOS (o CORE) — 25 aspectos

Numeração preservada da F5 para rastreabilidade. **Fusões:** A13→A12 (S/truncnorm é parte do estimador), A20→A17 (② vazia é consequência do LHS). **Novos da T11:** A25 (flag morto), A27 (pesos no header). A22 mudou de classe.

### 2.1 Tabela-resumo

| # | aspecto | classe F5 | **classe T11** | verif. | resultado-síntese (re-medido) |
|---|---|---|---|---|---|
| A1 | zero FE real na busca (offline puro) | (1) | **(1)** | direta | `tempo_aval_real_s=0` 45/45 s42; ① 100% `init`; `fe_final==maxfe==N` 45/45 |
| A2 | orçamento = dataset (31D−1 / 2.000) | (2) | **(2)** | direta | N ∈ {61,216,309,371,619,681,929} = 31D−1 em 35/35; 2.000 em 10/10 |
| A3 | binding por hash + determinismo bit-a-bit | (1) | **(1)** | direta | Δ\|X\| vs artefato = **0,0 em 45/45**; off ≡ swap_small-lhs bit-a-bit em ③ e ⑦ nos **5/5** pares |
| A4 | Kriging 1/obj, treino ÚNICO, kernel/α/restarts | (1) | **(1)** | direta-declarativa | ④=1 linha 45/45; `fit_series`=1; `fe_treino_max` único em 2.701.620/2.701.620 linhas |
| A5 | σ = desvio-padrão a posteriori, sempre presente | (1) | **(1)** | direta | **0 NaN** de σ em 45/45 (s42) e 0 no smoke; contraste: piso `moead_media` = 120.100/120.100 NaN |
| A6 | sonda: 1 bloco × 20.000, ger NULL, join posicional | (2) | **(2)** | direta | 45/45 `n_blocos=1`; ger NULL 900.000/900.000; **max\|ΔX\| = 0,0 em 45/45** (+ 0,0 no smoke) |
| A7 | **GP DEGENERA** (DI-30) | (2) | **(2)** | direta | **9/104 pares** com WAPE=1,000000 ou corr NaN; μ≡0 e σ≡31,622776=√1000 |
| A8 | **CONGELAMENTO da busca** | (2) 🎯 inferida | **(2) CONFIRMADA em bancada** | direta+bancada | **2.237/30.120 transições (7,43%)** congeladas; cadeia provada elo a elo |
| A9 | população = lattice Das-Dennis (50/105) | (1) | **(1)** | direta | pop constante 45/45; 50 em 31 células, 105 em 14; lattice reconstruído bate |
| A10 | substituição restrita a UMA vizinhança de 20 | (1) | **(1)** | direta | re-teste em 6 células: **40.024/40.028 = 99,990%**; 4 exceções cobertas por 2 |
| A11 | substituição **um-para-muitos** (Fig. 5b) | (1) | **(1)** | direta | grupos multi-slot; **tamanho máximo = 20 = `n_neighbors`** (teto teórico) |
| A12 | P_wrong via MC pareado + S=1000/truncnorm | **T** | **T (reduzido)** | não-verif.→**parcial** | o VALOR agora é logado (min/med/max/n_nan); o **estimador** e o truncnorm seguem T |
| A14 | sentido "rejeita σ alto" — ablação D77 | (1) | **(1)** | indireta c/ signif. | σ(⑦) < piso em **32/45** (32/39 sem empate); **Wilcoxon p=0,0012047**; razão mediana **0,7239** |
| A15 | rampa θ 0→500·(FE/40.000) por offspring | (1) indireta | **(1) DIRETA** | **direta** (smoke) | **Spearman(P_wrong mediano, geração) = −0,8688**; 0,4611 → 0,0211 |
| A16 | adaptação dos vetores a cada 10 gerações | (1) | **(1)** | indireta | pulso de turnover na 1ª geração de cada `iterate` (41/44 na F5) |
| A17 | pop. inicial = LHS NOVO ⇒ ② vazia, `real_solution_id` NULL | (2) | **(2)** | direta | estratificação **D/D em 45/45** (bounds reais); interseção com ① = **0**; ② 0 linhas 45/45 |
| A18 | SBX 30/1,0 + PM 20/1/n + repair | **T** | **T** | não-verif. | consequência observável: **0 violações de bounds em 1.801.620 linhas** |
| A19 | 40.000 aval-surrogate + overshoot ≤1 geração | (2) | **(2) agora CONTÁVEL** | direta | `n_ger=⌊40000/pop⌋+1` **45/45**; **Σ`n_chamadas` = 40.000 exato** no smoke |
| A21 | ⑦ = pop final na verdade, ND pós-real, reconstituível | (2) | **(2)** | direta | ND recomputado ≡ `nd_pos_real` **45/45**; link posicional bit-a-bit **45/45** |
| A22 | ④ = 1 linha e os **três cronômetros** | (2) | **(3) 🎯 menor** | direta | ④ 45/45 e `fit+busca==tempo_geracao_s` 45/45 ✅ — mas `tempo_aval_real_s` **mudou de 0,0 para float não-nulo** contra o comentário do próprio código |
| A23 | ⑥ DI-10: mínimo comum **+ o específico do b5** | (2)+(3) | **(2) RESOLVIDO** | direta | `f_best` **30.165/30.165** (resíduo 5,94e-08 = ULP fp32); `p_wrong_stats` **800/801** e `n_substituicoes` **800/801** no smoke, reconciliados |
| A24 | env, pins, patches **+ proveniência nova** | **T** | **T** | declarativa | `campanha_id`/`repo_hash`/`params`/schema v2 presentes no ⑤ do smoke (**ERRATA 11 confirmada**) |
| A25 | **`flag_vetores_degenerados` — campo presente, dado ausente** | — | **(3) 🎯** | direta | **0/801 não-nulo**; causa provada em runtime: `DataProblem` não tem `.reference_vectors` |
| A26 | Hyb-MOEA/D fora do escopo | (2) | **(2)** | direta | D56; nenhuma célula Hyb no grid |
| A27 | **pesos de decomposição no HEADER — ainda ausentes** | (parte de A25) | **(3) 🎯** | direta | header **idêntico** s42×smoke (22 chaves); requisito **retirado do `contrato_61.json` sem errata** |

### 2.2 Blocos narrativos

**A1 — Zero FE real na busca.** *(a)* O paper é categórico (Abstract; §II): nenhum dado novo durante a otimização; Alg. 1 constrói os Kriging na linha 1 e o laço só usa previsões. *(b)* A SPEC não mudou o princípio; instalou o `offline_guard` (`standalone_harness.py:663`), que converte qualquer FE na busca em `OfflineBudgetViolation` **logada**. *(c)* Re-medido nas 45 células da s42: `timing.tempo_aval_real_s = 0,0` **45/45**, `fe_final == maxfe == n_dataset` **45/45**, ① 100% `fase='init'`, `motivo_parada='orcamento'` 45/45, **zero eventos `guard`**. *(d)* O orçamento se esgota na CARGA do dataset (`load_offline_budget` roda `bud.evaluate` nas n linhas e exige `bud.exhausted`); o laço de 40k só toca o surrogate. *(e)* **(1) · direta.** ⚠ Ver A22: o *observável* mudou pós-T11.

**A2 — O orçamento é o dataset, não os 109 do paper.** *(a)* §IV-A11: N_D=109, n=10 fixo. *(b)* **D90** substitui por **31D−1** (o mesmo orçamento dos configs online, para a régua SA×piso ser comparável); D51/D67 acrescentam os tiers. É decisão de comparabilidade interna, não de fidelidade. *(c)* Medido: n_dataset ∈ {61, 216, 309, 371, 619, 681, 929} = 31D−1 **exato em 35/35** small/off; 2.000 em **10/10** medium. *(d)* Para D=10 vemos 309 pontos contra 109 (≈2,8× mais dado): estamos **fora do envelope do paper, para cima**; para D=2 (MMF), 61 — abaixo. Isso desloca o regime de degeneração do GP (A7) de forma dependente de D. *(e)* **(2) · direta** (D90/D51/D67).

**A3 — Proveniência e determinismo.** *(a)* O paper só declara LHS/MVNS (§IV-A12). *(b)* D63/D87 exigem binding por hash; D62 exige determinismo; o `_patch_lhs_seeding` injeta o `RandomState` global semeado no `lhs()` porque o pyDOE 0.9.1 passou a criar um `default_rng()` fresco (declarado no `sigma_dict`). *(c)* Duas provas re-executadas: (i) reli o parquet apontado pelo `dataset_path` do **header** de cada célula — **Δ\|X\| = 0,0 em 45/45**; (ii) `off/{DTLZ2, MMF16_20, WFG9, ZDT1, ZDT4}` × `swap_small-lhs_{idem}` são execuções independentes sobre o mesmo dataset e semente e saem **bit-a-bit idênticas em ③ e ⑦ nos 5/5 pares** (60.005/60.050 linhas cada). *(d)* Fecha o único elo que um re-run poderia contestar: LHS semeado + SBX/PM + 9 restarts do GPR + `truncnorm` compõem um fluxo RNG determinístico ponta a ponta. *(e)* **(1) · direta.**

**A4 — Kriging: um por objetivo, treinado uma única vez.** *(a)* §IV-A5 (sklearn, kernel gaussiano, BFGS); Alg. 1 L1. *(b)* A `SurrogateKriging` vendorizada crava kernel/α/restarts que o paper **não publica** — 🟠 impl→código pela D29, declarados no `sigma_dict`. *(c)* 45/45: ④ com **exatamente 1 linha**, `fit_series` com 1 entrada, `fe_treino_max` **constante = N−1 nas 2.701.620 linhas** (busca+sonda, `nunique()==1`). Fit mediano 14,29 s = **0,501% do wall**. *(d)* Onde o c311 prova o congelamento do modelo com 2 blocos de sonda bit-idênticos, aqui não há 2º bloco (o modelo nunca retreina); a prova estrutural equivalente é a tripla `④=1 ∧ fit_series=1 ∧ fe_treino_max constante`, que fecha 45/45. *(e)* **(1) · direta-declarativa.**

**A5 — σ sempre definido.** *(a)* Eq. (10) depende de σ existir. *(b)* O `sigma_dict` declara "DESVIO-PADRÃO a posteriori (NUNCA variância)". *(c)* **Zero NaN** em `sigma_*` nas 45 células (1.801.620 de busca + 900.000 de sonda) e **0 no smoke**. O contraste que a T11 permitiu medir: o piso `moead_media`, na MESMA célula MMF1, tem σ **NaN em 120.100/120.100** valores (tanto no smoke quanto na s42) — é o "b5 sem σ" do DI-30.B3 aparecendo no dado. *(d)* GP exato (`alpha=0`): σ→0 nos pontos de treino e cresce com a distância; nenhum caminho produz NaN. *(e)* **(1) · direta** — a armadilha (15) do c311 (`cobertura95` inflada por NaN) **não se aplica** aqui.

**A6 — Sonda: 1 bloco, join posicional exato.** *(a)* Instrumentação nossa (§17.2.2). *(b)* DI-13.5 obriga `geracao=NULL` no bloco offline; `n_blocos=1` porque só há um modelo. *(c)* 45/45 com `n_blocos=1`, S=20.000, `hash_check="ok"`, ger NULL em 900.000/900.000; **join posicional contra `data/sonda/sonda_{P}.parquet` dá max\|ΔX\| = 0,0 em 45/45** (re-medido) — e 0,0 também no smoke. *(d)* Sem `sonda_id`, a única prova de alinhamento é posicional (regra 5 do §10); ela fecha exatamente, validando todo WAPE/corr/cobertura derivado. **Não-perturbação:** no b5m a sonda é emitida **fora e antes** do laço (`b5_prob.py:465-473`) e o `GPR.predict` é determinístico — a não-perturbação é trivial por construção, além do veredito G-6 registrado `bcda99b31dfb2c35`. *(e)* **(2) · direta.**

**A7 — O GP degenera (DI-30) e reproduz na s42.** *(a)* O paper não discute falha de ajuste. *(b)* Nada mudou; o autor ratificou em **DI-30** que a degeneração ocorre e é sensibilidade de semente do treino único. *(c)* `sonda_f52e.csv` (104 pares célula×objetivo, re-lido): **9 pares degenerados** — `off/DTLZ1` (3 obj, corr −0,005548/−0,004871/+0,014551), `off/DTLZ3` (3 obj, corr NaN), `off/DTLZ4` obj1 (NaN), `swap_medium-lhs_MMF16_20` obj0 (0,023726) e `swap_medium-mvns_MMF16_20` obj0 (0,011123) — todos com **WAPE = 1,000000 exato**. Nos casos totais: **μ ≡ 0,0 e σ ≡ 31,622776 = √1000 = o teto do `ConstantKernel(1e-3,1e3)`**, 1 único valor distinto em 40.005 linhas de busca e 20.000 de sonda. Fora dos degenerados: WAPE mediano **0,12683** (p25 0,043592 / p75 0,27426), corr mediana **0,9465** (>0,9 em **59/104**), cobertura95 mediana **0,8954**, σ-NaN **0/104**. *(d)* A MLE com 9 reinícios cai numa bacia em que o RBF vai ao piso do lengthscale (1e-2) e o ConstantKernel ao teto (1e3): a correlação entre pontos distintos vai a zero e o GP reverte ao **prior de média zero** (`normalize_y=False`) com amplitude saturada. É ótimo local legítimo; a bacia depende do RNG dos restarts, independente por config (DI-28) — daí b5m, b5r e o piso degenerarem em problemas **diferentes** com a mesma spec. *(e)* **(2) · direta** (DI-30).

**A8 — O congelamento: a cadeia inferida na F5 está agora CONFIRMADA EM BANCADA.** *(a)* Nenhuma prescrição do paper cobre um surrogate constante. *(b)* Nenhuma decisão nossa; o `adapt` é código oficial (Cheng 2016). *(c)* **Dado (s42, re-medido):** 45 células, 30.120 transições de geração, **2.237 congeladas (turnover exatamente 0) = 7,43%**. Distribuição: `swap_medium-mvns_ZDT4` 778, `swap_small-mvns_ZDT1` **565**, `DTLZ3` 380/380 (X idêntico à geração 1 em **todas** as 381 gerações ⇒ **zero substituições em ~798.000 comparações P_wrong**), `DTLZ1` 370 (congela na geração **11** = a fronteira do 1º `iterate()`, `n_gen_per_iter=10`), `MMF11_L` 63, `swap_medium-lhs_ZDT1` 29, `MMF4` 23, mais 8 células com 1–9. *(d)* **A cadeia, agora provada elo a elo em bancada isolada** (`t11_b5m_01_cadeia_A8.py`, `env_b5`, desdeo vendorizado, sem rodar célula):
1. `ReferenceVectors(H=49, M=2)` → 50 vetores, norma 1,000000;
2. `adapt(fitness constante)` → `values = initial_values·(max−min) = 0`; `normalize()` protege com `norm_2[norm_2==0]=eps` ⇒ **norma 0 em 50/50 vetores** (zero exato, não NaN);
3. `ProbMOEAD_select.pbi(weights = vetor nulo)` → `weights/norm(weights) = 0/0` ⇒ **1000/1000 amostras de PBI = NaN**;
4. `compute_probability_wrong_MC(NaN, NaN)` = `sum(NaN>NaN)/S²` = **0.0 exato** ⇒ `probabilities>0.5` nunca dispara ⇒ `selection` vazia, para sempre;
5. **Contrafactual medido:** com os MESMOS μ e σ mas um vetor SADIO, `P_wrong = 0,4995` — ou seja, ~50% de aceitação. **Jamais 0% em 798.000 comparações.** É a única cadeia compatível com o dado.
6. **Consequência para a instrumentação:** no estado congelado o `p_wrong_stats` registraria `{min:0.0, med:0.0, max:0.0, n_chamadas:50, n_nan:0}` — note `n_nan = 0`, porque o NaN mora **a montante** (nas amostras de PBI), não em `probabilities`. **O campo `n_nan` NÃO detecta a cadeia A8.** Quem detectaria era o `flag_vetores_degenerados` — e ele está morto (A25).

*(e)* **(2) · direta + bancada.** A F5 pediu essa confirmação à F5.4 "com 5 linhas"; ela foi feita e **o veredito da F5 se sustenta integralmente**. Caveat central para o D97, inalterado: **DTLZ1 e DTLZ3 são cientificamente vazios** (a ⑦ é a amostra LHS inicial; HV-surrogate = 1,331 = 1,1³ = o máximo teórico, com HV real 0,000).
⚠ **Errata contra a minha própria F5:** a F5 reportou "1.543 gerações congeladas" (o CSV dizia 1.588). Esse número é a **cauda permanente** (do onset até o fim). O total de transições congeladas **em qualquer ponto** é **2.237**. A diferença (649) é **congelamento INTERMITENTE** — e ele é dominante em `swap_small-mvns_ZDT1` (565 intermitentes contra 22 de cauda). Os dois números medem coisas diferentes; ambos valem, e o intermitente é o que o comentário do código chama "congelamento PARCIAL".

**A9 — População = o lattice, sem override.** *(a)* §IV-A8: vetores por simplex-lattice, número variando com M. *(b)* Nada mudou: `lattice_resolution=None` deixa o default `[49,13,…]`, e `population_size = number_of_vectors`. *(c)* Reconstruí o lattice Das-Dennis independentemente (`comb(H+M−1,M−1)` + normalização) e obtive **50 (M=2) / 105 (M=3)**; a ③ tem **pop constante e igual em 45/45** (`pop.nunique()==1`), 50 em 31 células e 105 em 14. *(d)* MOEA/D mantém uma solução por subproblema; a população nunca encolhe (substituição in-place), ao contrário do RVEA/c311. *(e)* **(1) · direta.**

**A10 — QUERY-JOIA: a substituição respeita a vizinhança de 20.** *(a)* §III-B (l. 244) + §IV-A8 fixando a vizinhança em 20. *(b)* Nada mudou: `self.neighborhoods = argsort(distance_matrix(V,V))[:, :20]`, calculado **1×** no `__init__` sobre o lattice não-adaptado. *(c)* Re-teste independente em 6 células (DTLZ2, ZDT1, WFG9, MMF16_20, BBOB_F1, MMF4), com o lattice e as vizinhanças reconstruídos **fora do repo vendorizado**: **40.024 de 40.028 grupos de substituição (99,990%) cabem dentro de UMA vizinhança de 20**; as 4 exceções são cobertas por exatamente 2. (F5, nas 45 células: 355.592/356.137 = 99,847%, exceções por 2 ou 3.) *(d)* As exceções são **aliasing de X sob o export float32 (D53)** — dois offspring distintos convergindo ao mesmo X em fp32, cada um inserindo na SUA vizinhança; o agrupamento por X os funde. A **regra 11 do CONTRATO §10** (dominância sobre ①/⑦ é lossy em fp32), nova nesta campanha, é a formalização exata dessa armadilha. *(e)* **(1) · direta** — o lattice, a vizinhança de 20 e a semântica "archive = população pós-substituição" ficam provados por dado.

**A11 — Substituição um-para-muitos (Fig. 5b).** *(a)* §III-B: o offspring compete com **todos** os vizinhos, ao contrário do APD pareado do RVEA. *(b)* Sem mudança. *(c)* No re-teste, **tamanho máximo de grupo = 20 = `n_neighbors`**, o teto teórico. **E agora há a medida direta**: no smoke, `Σ n_substituicoes = 16.477` contra `Σ slots efetivamente trocados = 10.397` — **razão mediana 1,2727**: um mesmo slot é reescrito mais de uma vez dentro da geração, que é a assinatura do `population.replace(indices, individual)` operando sobre vizinhanças sobrepostas. Antes da T11 essa razão era invisível (só se via o resultado líquido na ③). *(d)* O mecanismo do paper deixando impressão digital em duas camadas independentes. *(e)* **(1) · direta.**

**A12 — P_wrong via MC pareado: o teto ENCOLHEU, mas não fechou.** *(a)* Eqs. (7)-(8): `P_wrong = ∫PDF_A(y)·CDF_B(y)dy`, PDFs por KDE gaussiano com banda de Silverman — e a própria §III-A2 propõe, como redução de custo, "a coarse approximation of the CDF by computing the empirical CDF from the samples". §III-B (l. 263): atualizar se `P_wrong(g'≤g_j) < 0,5`. §IV-A6: S=1000; Eq. (10) é gaussiana **sem** truncagem. *(b)* O bloco KDE não alimentava a decisão (código morto) e o `plt_density` salvava um PDF por vizinho sob `usetex` — inviável em 40k FE; a âncora `b5-mode72-kde` o comenta (🟠 D29/D30). A decisão sai de `compute_probability_wrong_MC` = `Σ1[a>b]/S²`, substituindo se `>0,5`. As amostras vêm de `truncnorm(−3,3)` (a linha com ±1,96 está comentada). *(c)* **O que a T11 destravou:** o valor agora é logado (min/med/máx/`n_chamadas`/`n_nan` por geração — 800/801 eventos no smoke), e o domínio observado é `[0,0 ; 1,0]` com `n_nan = 0` em 40.000 chamadas. **Teste falsificável que executei:** `p_max > 0,5` em 800/800 gerações **e** `n_sub ≥ 1` em 800/800 — consistente com `selection = where(probabilities>0.5)`; a implicação contrária (`p_max ≤ 0,5 ⇒ n_sub = 0`) não pôde ser testada porque nenhuma geração desta célula teve `p_max ≤ 0,5`. **O que segue T:** (i) a identidade `Σ1[a>b]/S² ≡ ∫pdf_A·CDF_B` (só os agregados são logados, não os pares); (ii) S=1000 e a truncagem — as ~4e6 amostras/geração são descartadas por desenho. *(d)* Argumento de fidelidade para o D97 (inalterado): `Σ1[a>b]/S²` é a U-statistic de Mann–Whitney, estimador consistente e não-enviesado de `P(A>B)` = exatamente o integrando da Eq. (8); a troca remove o intermediário KDE (que o próprio paper sanciona pular) e a truncagem em 0 (motivada para o APD, não para o PBI, que pode ser negativo). A regra `P(SF_current > SF_offspring) > 0,5` é a forma empírica de `P_wrong(g'≤g_j) < 0,5` — **`P_wrong` no paper é a probabilidade de a afirmação estar ERRADA**, logo os dois critérios são o mesmo. A truncagem em ±3σ remove 0,27% das caudas e renormaliza; o viés em `P(A>B)` é de 2ª ordem e simétrico quando σ_A≈σ_B — mas **assimétrico quando os σ diferem muito**, que é justamente o caso que o mecanismo quer discriminar. *(e)* **T · não-verificável (reduzido de 2 itens para 2 sub-itens de 1 aspecto).**

**A14 — QUERY-JOIA COMPORTAMENTAL: o mecanismo REJEITA incerteza (ablação D77).** *(a)* É a tese do paper (§IV, l. 367): "the probabilistic approaches **reject solutions with better objective values if they have high uncertainties**". *(b)* A SPEC criou o instrumento exato: `moead_media` (mode 12) é o **Gen-MOEA/D do próprio paper** com o MESMO GP, o MESMO lattice (D65 crava N = o lattice do b5m, 50/105, justamente para não medir duas variáveis), a MESMA rampa θ, os MESMOS operadores e o MESMO dataset — só `MOEAD_select` (média) vs `ProbMOEAD_select` (P_wrong). *(c)* Régua independente do config: σ(x) em 20.000 pontos fixos do bloco de sonda do b5m, avaliado por 1-NN na população final (⑦) de cada config. Re-computado: **b5m termina em σ menor que o piso em 32/45 células (32/39 sem empate exato)**, razão mediana **0,7239** (sobre as 39 sem empate, 0,6166), **Wilcoxon p = 0,0012047**, binomial p = 7,03e-05. Ordenação por config: **b5m 0,5169 · b5r 0,5362 · moead_media 0,6973 · e103 0,7126 · c311 0,7584** — os dois probabilísticos do b5 são os dois de menor σ final, e o genérico com o MESMO motor fica 35% acima. **Confirmação nova no smoke** (MMF1/s0, mesma célula, os dois configs): o σ médio da população cai de 0,68592 (ger. 1) para 0,11903 (ger. 801) no b5m — razão **0,174**. *(d)* Cada aceitação exige vencer o vizinho na maioria dos 1e6 pares MC; média ligeiramente melhor com σ grande produz distribuição de PBI larga e perde a maioria. Iterado 40.000 vezes, arrasta a população para o suporte do dataset. *(e)* **(1) · indireta com significância** — σ é *load-bearing* na NOSSA execução, não só no paper.

**A15 — A rampa θ, agora medida DIRETAMENTE.** *(a)* §IV-A8 (l. 314): "θ in PBI varied from 0 to 500 with function evaluations". *(b)* B18.8: recalculada **por offspring** (`theta = 0 + 500·FE/40000`), com `total_function_evaluations=40000` obrigatório mesmo com FE real = 0 (senão ZeroDivision). *(c)* Na F5 isso era **indireto** (turnover decaindo, Spearman mediana −0,581 em 43/45; re-medido agora: **−0,5449, negativa em 43/45**). **Com a T11 é direto**: no smoke, `Spearman(mediana de P_wrong, geração) = −0,8688`, com a mediana caindo de **0,4611 (ger. 2) para 0,0211 (ger. 801)** — 22×. Por quartil de geração: p_med Q1 0,1056 → Q2 0,0597 → Q3 0,0286 → Q4 0,0256; `n_substituicoes` mediano 29 → 17 → 10 → 9. *(d)* Com θ≈0 o PBI é quase só a projeção d1 (quase tudo passa); com θ→500 a penalidade angular domina. Nenhum outro parâmetro varia com FE. A T11 transformou uma inferência sobre um proxy (turnover) numa leitura da própria quantidade. *(e)* **(1) · DIRETA** (era indireta na F5 — upgrade real de verificabilidade).

**A16 — Adaptação dos vetores a cada 10 gerações.** *(a)* §IV-A8 (l. 314): adaptação "after every 10th generation". *(b)* Sem mudança — e aqui b5m é **mais fiel que b5r** (no mode 7 o `adapt()` está comentado; no 72 ele roda, via `manage_preferences` no topo de cada `iterate()` com `n_gen_per_iter=10`). *(c)* Teste desenhado na F5: a 1ª geração de cada `iterate` deve ter turnover elevado. Medido: razão >1 em **41/44** células (mediana 1,129); no piso, 41/44 com mediana 1,468. Corroboração independente e forte: o **onset do congelamento do DTLZ1 é a geração 11** — exatamente a fronteira do 1º `iterate`, o único ponto do laço onde `adapt` roda. *(e)* **(1) · indireta.**

**A17 — Pop. inicial = LHS NOVO (a divergência que muda a forma da curva) ⇒ ② vazia.** *(a)* Alg. 1 L2 (l. 152) é inequívoco: usar o dataset como população inicial. *(b)* **B18.9** (SPEC:815/1529): `init_pop` comentado em todos os modes; a população nasce de `create_new_individuals(design="LHSDesign")`. Pela D29 (🟠 impl→código) seguimos o código, **declarando**. **DI-16.17** formaliza a consequência de export. *(c)* Verificação re-executada contra os **bounds REAIS** de `src/problems.py` (a F5 usara os mesmos): a geração 1 tem **exatamente um ponto por estrato de largura 1/pop em CADA dimensão — D/D em 45/45 células** (D=2 a D=30), e a **interseção com o dataset é 0 pontos**. Consequências de export: ② com **0 linhas em 45/45**, `real_solution_id` NULL em **1.801.620/1.801.620** linhas, `n_ds_membros=0` em 30.165/30.165 eventos, `origem_solution_id` NULL em 45/45 ⑦ — e no smoke idem (③ 60.050 linhas, 0 marcadas; ② 0 linhas). *(d)* Impacto científico, não de fidelidade: no paper a busca **começa do dataset** (um conjunto já bom no espaço real); aqui começa de uma amostra aleatória. Isso (i) explica por que o HV-surrogate precisa primeiro SUBIR antes de cair (âncora J2) e (ii) torna a comparação "⑦ vs Init" uma comparação entre conjuntos **disjuntos**. **É também a razão pela qual o gate G-1 (identidade ③↔① das linhas marcadas) é `n/a` para este config** — não há linha marcada, hoje nem nunca. *(e)* **(2) · direta.**

**A18 — Operadores de variação e reparo.** *(a)* §IV-A8: SBX índice 30 e p=1,0; PM índice 20 e p=1/n — **paper ✓**. *(b)* O código usa `SBX_xover()` e `BP_mutation(lb,ub)` com os defaults (as linhas com parâmetros explícitos estão comentadas); `population.repair` clipa nos bounds. *(c)* Os índices não são observáveis ⇒ T. O que é observável, re-medido: **0 violações de `[xl,xu]` em 1.801.620 linhas de busca** nas 45 células. *(e)* **T · não-verificável** (a única consequência mensurável fecha 45/45).

**A19 — 40.000 avaliações-surrogate: de identidade derivada a CONTAGEM.** *(a)* §IV-A4 (l. 306): "40 000 function evaluations with surrogates". *(b)* O bundle declara "contador 40k: init+pop; MOEA/D +1/offspring; overshoot ≤1 geração". *(c)* Na F5 a prova era a identidade derivada do código: `n_ger = ⌊40000/pop⌋ + 1`, que fecha **45/45** (re-medido: 801 gerações nas 31 células com pop=50; 381 nas 14 com pop=105; FE interno 40.050 e 40.005). **A T11 fecha isso por contagem direta:** `Σ n_chamadas` do `p_wrong_stats` = **40.000 exato** no smoke (`n_chamadas == pop == 50` em **800/800** gerações), porque há **uma chamada de seleção por offspring**. O overshoot fica explícito: 40.050 linhas de busca = 50 da população inicial + 40.000 offspring. *(d)* Que os dois caminhos independentes (aritmética da ③ e contagem no ⑥) deem 40.000 prova simultaneamente a contagem init+pop, o +1 por offspring, o corte **por geração** (não por `iterate` de 10) e que a geração 1 do arquivo é a população LHS, não uma geração de busca. *(e)* **(2) · direta.**

**A21 — A ⑦: o endpoint real do offline.** *(a)* §IV-A: os finais são avaliados nas funções verdadeiras "only for experimentation". *(b)* DI-08/DI-13.9: avaliar **todos** os finais na verdade e filtrar o ND **depois**; DI-16.16 exige reconstituibilidade da ③. *(c)* Re-medido: `|⑦| == pop` 45/45; **ND recomputado sobre a vista float32 ≡ `nd_pos_real` em 45/45**; **link posicional (origem_geracao, origem_linha) → ③ casa X bit-a-bit em 45/45**; `origem_geracao` = última geração 45/45. Razão de fantasia `nd_pos_real/n_final`: mediana **0,380**. *(d)* Que 62% da população final seja dominada quando avaliada na verdade é a medida direta da "fantasia" do surrogate — o fenômeno da Fig. 1. **Leitura nova obrigatória (regra 11 do §10, desta campanha):** a dominância sobre ⑦ é **lossy** (f0/f1 em fp32, a busca decidiu em fp64); o `write_final` já calcula `nd_pos_real` na vista fp32 por isso mesmo (a docstring cita "b5m/ZDT1: 20 vs 19"), de modo que a **consistência interna** fecha 45/45 — mas a razão de fantasia carrega o caveat fp32. *(e)* **(2) · direta.**

**A22 — ④ de uma linha, a invariante de tempo… e o cronômetro que mudou de semântica.** 🎯 *(a)* Sem análogo no paper. *(b)* O cartão declara ④ = 1 linha (o `iterate()` roda 10 gerações sem gancho); `tempo_geracao_s` **exclui** a sonda. **Novo na T11 — I-02**: `tempo_aval_real_s` passou a ser medido **no portão único** (`budget.py:241-250`), cuja docstring diz: "**None** se nenhuma avaliação passou por aqui… no offline o orçamento nasce ESGOTADO e nenhuma avaliação real acontece no run — gravar 0.0 ali afirmaria que avaliar custou zero" (repetida em `b5_prob.py:571-575`). *(c)* **O que fecha, re-medido:** ④ com 1 linha 45/45; `tempo_fit_s + tempo_busca_s == tempo_geracao_s` (|Δ|<1e-2 s) em **45/45**; `tempo_geracao_s < fit+busca+sonda` em **45/45** (a sonda ficou de fora). **O que NÃO fecha:** na s42 `tempo_aval_real_s` é **0,0 em 45/45**; no smoke T11 é **0,0001** — e o mesmo padrão aparece nos 4 irmãos offline Python (`b5r` 0,0003 · `c311` 0,0003 · `moead_media` 0,0001 · `treed_media` 0,0572), todos exatamente **0,0 em 100% das células da s42** (45+55+45+10). *(d)* **Causa provada por leitura:** `load_offline_budget` (`standalone_harness.py:648-653`) faz `bud.evaluate(X[i], lambda _x,_f=F[i]: _f)` para as **n linhas do dataset**. O cronômetro do I-02 está DENTRO do `evaluate`, logo `_n_avals_cronometradas = n ≠ 0` e a property **nunca devolve None no offline** — ela devolve o wall de ler n linhas de um artefato em memória. A premissa escrita em `b5_prob.py:571-575` ("nenhuma avaliação real acontece DENTRO do run") é falsa para o caminho que o próprio arquivo usa. **Impacto:** nenhum sobre a busca (0,0001 s = 0,000004% dos 2.725 s de wall) e nenhum sobre o orçamento; mas o **teste da F5 para A1 (`tempo_aval_real_s == 0` em 45/45) deixa de valer no corpus pós-T11**, e o campo perde a função declarada de distinguir "não houve avaliação real". *(e)* **(3) 🎯 menor · direta** — desvio documentação×comportamento, periférico, causa-raiz provada, benigno para o mecanismo. Recomendação para o D97: ou o `load_offline_budget` marca a carga como não-cronometrada, ou o comentário some e o campo passa a ser lido como "wall da ingestão do dataset" — mas as duas afirmações não podem coexistir.

**A23 — O ⑥ DI-10: o mínimo comum fecha por identidade e o específico do b5 FOI IMPLEMENTADO.** *(a)* Sem análogo no paper. *(b)* CONTRATO §6 exige `fe`, `f_best[]`, `n_front1` por evento; **§6.1, linha `b5`** exige "**substituições P_wrong (b5m)**" e "`p_wrong_stats` (min/med/max por geração)". A F5 mediu **0/30.165** e classificou como o único (3) do config, pedindo à F5.7 que se logasse `p_wrong_stats` **e** `n_substituicoes` (`len(selection)`), ambos já computados. *(c)* **Mínimo comum, re-medido na s42:** `f_best` fecha **30.165/30.165 eventos (100%)** com desvio relativo máximo **5,94e-08** = o ULP do float32 (prova de que o ⑥ foi calculado em fp64 antes do export, como manda o D53); `n_front1` fecha **29.310/30.165 (97,17%)**, 100% em 26/45 células — as 855 divergências são o mesmo arredondamento (em fp32 pares quase-idênticos viram empates), e a **regra 11 do §10** agora cobre isso normativamente. **Específico do b5, medido no smoke:** `p_wrong_stats` em **800/801** eventos e `n_substituicoes` em **800/801** (o único ausente é a geração 1 = a população LHS, onde não há seleção — `None` correto, não falha). **Reconciliação independente contra a ③** (o teste que separa "campo existe" de "campo funciona"): `n_sub ≥ slots efetivamente trocados` em **800/800**; `n_sub == 0 ⟺ slots trocados == 0` em **800/800**; Spearman(n_sub, slots) = **0,963024**; Σ 16.477 substituições em Σ 800.000 comparações P_wrong (40.000 chamadas × 20 vizinhos) = **taxa de aceitação 2,0596%**. *(d)* O acumulador (`ProbMOEAD_select.py:31-43` + `b5_prob.py:88-110`) só lê `probabilities` e `len(selection)` **depois** de a decisão estar tomada, não consome RNG e não altera o retorno — não-perturbação por construção, além do veredito G-6 `bcda99b31dfb2c35` e da âncora `b5-pwrong-stats` em `anchors.json`. *(e)* **(2) · direta — o (3) da F5 está RESOLVIDO na sua parte central.**

**A24 — Ambiente, pins, patches e a proveniência nova.** *(a)* §IV-A5 cita scikit-learn; o rodapé da p. 1188 dá o repositório oficial. *(b)* **D80** crava sklearn **0.21.3**; `desdeo_*` VENDORIZADO root-first (gate R3.2, content-hash `b5_desdeo` no `repos.lock`); `pygmo` é stub; âncoras `b5-mode72-kde`, `b5-mode7-archive` e agora `b5-pwrong-stats`. *(c)* Os 45 manifestos da s42 declaram python 3.7.12, numpy 1.21.6, scipy 1.7.3, **sklearn 0.21.3**, pymoo 0.6.1.2, pyarrow 12.0.1, executável `env_b5`, pinning OMP/OPENBLAS/MKL/NUMEXPR=1 (D79) — idênticos no smoke. **Novo e verificado:** o ⑤ do smoke tem `campanha_id='9ad0138aa9a4_2026-07-31'`, `repo_hash='9ad0138a…'` ≡ HEAD e `schema_version=2`; e **`params` está PRESENTE** (com `mode:72`, a spec do GP, `n_dataset` e a lista de patches vendorizados incluindo `b5-pwrong-stats`) — contra **0/45 na s42**. Isso **fecha o achado de contrato da F5** ("⑤ CHAVES faltam ['params']" em 45/45) e confirma a **ERRATA 11** do handoff. O `sigma_dict` cresceu de 18 para 19 chaves; a nova é `granularidade_③` — que documenta explicitamente "ger 1 = pop INICIAL (LHS, PRÉ-seleção); 2..n = PÓS-seleção; passos de seleção = n_geracoes−1", **fechando a armadilha nº 1 da minha F5**. *(e)* **T · não-verificável por dado** (config-echo coberto por `anchors.json` + `repos.lock`), com a camada de proveniência agora **direta**.

**A25 — 🎯 `flag_vetores_degenerados`: o campo existe, o dado não existe, e nunca vai existir.** *(a)* Sem análogo no paper. *(b)* Foi um pedido **explícito da minha F5** (§8): "logar um flag `vetores_degenerados` quando `norm(reference_vectors.values)==0`" — a **única** instrumentação capaz de observar a CAUSA a montante da cadeia A8. A T11 o implementou (`b5_prob.py:114-136`), com `n_vetores`, `n_norma_zero`, `norma_min`, `norma_max` e `amplitude`; a **ERRATA 13** do handoff registra que a "amplitude em float64" já estava lá. *(c)* **Medido: `None` em 801/801 eventos do smoke** (e `flag_vetores_degenerados` ausente em 0/30.165 da s42, como esperado — o campo é pós-T11). **Causa provada em runtime**, no `env_b5` com o desdeo vendorizado (`t11_b5m_00_flag_dead.py`): a linha é
> `V = evolver.population.problem.reference_vectors.values` — `src/b5_prob.py:118`

e `hasattr(DataProblem, 'reference_vectors') = **False**`, `hasattr(Population, 'reference_vectors') = **False**`, `'reference_vectors' in Population.__init__ = **False**`, 0 ocorrências no fonte do `DataProblem`. O atributo vive **no evolver**: `BaseDecompositionEA` faz `self.reference_vectors = ReferenceVectors(...)` (`BaseEA.py:182`) e o `ProbMOEAD` o usa como `self.reference_vectors` (`ProbMOEAD.py:128`). Portanto `evolver.population.problem.reference_vectors` **sempre** levanta `AttributeError`, capturado por `except Exception: return None`. Confirmação end-to-end: `_vetores_degenerados(evolver_simulado)` importado do próprio `src.b5_prob` devolve `None`; o caminho correto, sobre os mesmos vetores, devolveria `n_norma_zero=50, norma_min=0, norma_max=0, amplitude=0`. *(d)* Duas consequências, e a segunda é pior que a primeira:
1. **A ponta a montante da cadeia A8 não foi instrumentada.** O que resta é a assinatura a jusante (`p_wrong_stats` com min=med=max=0,0 **e** `n_substituicoes`=0) — forte, mas **não distingue** "vetores colapsados → PBI NaN" de "o offspring foi genuinamente pior em todos os 20 vizinhos". E `n_nan` **não ajuda**: provei em bancada que ele vale 0 no estado congelado, porque o NaN mora nas amostras de PBI, não em `probabilities`.
2. **Mesmo consertado, o campo não faz o que o comentário promete.** `_vetores_degenerados(evolver)` é chamado **dentro do laço de replay pós-busca** (`b5_prob.py:536`), sobre o estado **final** dos vetores — logo devolveria o **mesmo valor nas 801 gerações**. O comentário do código diz que a amplitude "mostra os vetores encolhendo ANTES de zerar, que é onde a cadeia A8 começa"; **isso é inalcançável com o sítio de coleta atual** (o motor é caixa-preta por `iterate()`; a série por geração exigiria acumular a norma no mesmo registro módulo-nível do `P_WRONG_STATS`).
*(e)* **(3) 🎯 · direta.** Correção mínima: 1 token (`evolver.population.problem.reference_vectors` → `evolver.reference_vectors`) para o campo deixar de ser nulo; +3 linhas no acumulador de `ProbMOEAD_select` para virar série por geração e datar o onset.

**A26 — 🎯 Os PESOS de decomposição no HEADER: requisito não cumprido e retirado do contrato sem errata.** *(a)* Sem análogo no paper. *(b)* `CONTRATO_DE_DADOS.md:332`, coluna "+ DI-10 (novo)", linha `b5`, é vinculante: "**os PESOS de decomposição do b5m no HEADER (determinísticos — 1×)**; `p_wrong_stats` (min/med/max por geração)". *(c)* **Medido:** o header do ⑥ tem **22 chaves na s42 (45/45) e as MESMAS 22 no smoke T11** — conjunto idêntico, verificado chave a chave: `D, M, alg, ambiente, dataset_hash, dataset_path, dist, doe_hash, f_hash, maxfe, n_dataset, pinning, problema, rec, regime, semente, sigma_dict, sonda_S, sonda_x_hash, tier, ts, versao`. **Nenhuma delas é os pesos/vetores de decomposição.** E o artefato normativo machine-readable da campanha, `claude_code_context/artifacts/contrato_61.json`, tem para `b5m` apenas `{"di10": ["p_wrong_stats"], "nao_se_aplica": {"tempo_busca_s": "idem b5r"}}` — **a exigência dos pesos desapareceu do contrato executável**, sem errata que a cubra (as ERRATAs 12 e 14 tratam de b5r/moead_media e do e103, não disto). *(d)* O custo analítico é limitado — os 50/105 vetores são **determinísticos** dado (M, H) e eu os reconstruí independentemente em A9/A10 —, mas o item exemplifica o modo de falha que a própria T11 nomeia (§4.2.1: "um gate que nunca reprova é decorativo"): o gate G-7 dá **verde** para o b5m porque confere o `contrato_61.json`, e o `contrato_61.json` já não pede o campo. **O gate concorda com o contrato executável e discorda do contrato escrito.** *(e)* **(3) 🎯 · direta.** Ação: ou implementar (1 linha no `log.header`), ou registrar a retirada como decisão explícita no §6.1. Hoje não é nem uma coisa nem outra.

**A27 — Hyb-MOEA/D fora do escopo.** *(a)* §III-C propõe o híbrido e §IV conclui que ele não supera o Prob-MOEA/D. *(b)* **D56** mantém b5r (headline) + b5m (quase-fiel) e deixa Hyb fora — a variante mais fraca do par. *(c)* Nenhuma célula Hyb no grid nem referência nos 45 manifestos. *(d)* A âncora J4 do Anexo J fica **não-testável**. *(e)* **(2) · direta** (escopo declarado).

---

## 3. Percentuais por classe

**Denominador = 25 aspectos − 3 T = 22.** (T listado à parte, com a razão.)

| classe | contagem | % | F5 (denom. 22) |
|---|---|---|---|
| **(1)** conforme o artigo | **10/22** | **45,5%** | 10/22 = 45,5% |
| **(2)** desvio sancionado (decisão citada em cada bloco) | **9/22** | **40,9%** | 11/22 = 50,0% |
| **(3)** desvio inexplicado 🎯 | **3/22** | **13,6%** | 1/22 = 4,5% |
| **T** teto declarado (fora do denominador) | **3** | — | 4 |

**(1)** = A1, A3, A4, A5, A9, A10, A11, A14, A15, A16 · **(2)** = A2, A6, A7, A8, A17, A19, A21, A23, A27 · **(3)** = A22 (cronômetro I-02), A25 (flag morto), A26 (pesos no header) · **T** = A12 (identidade do estimador + S/truncnorm), A18 (índices SBX/PM), A24 (env/pins/patches).

**Leitura honesta do movimento**: o denominador é o mesmo, mas a composição mudou por **dois** motivos opostos e ambos reais — o (3) da F5 (A25/DI-10) foi **resolvido** e migrou para (2) dentro de A23; e **três novos (3) apareceram, todos criados ou revelados pela própria T11**, todos de INSTRUMENTAÇÃO e nenhum de MECANISMO. O balde T encolheu de 4 para 3 itens (A12 e A13 fundidos, e o valor de P_wrong saiu do teto).

---

## 4. 🆕 AS CORREÇÕES DA T11 — implementada? funciona de fato? o que falta?

| # | melhoria | implementada? | **funciona de fato?** | evidência | o que falta |
|---|---|---|---|---|---|
| 1 | **`p_wrong_stats`** (min/med/máx + `n_chamadas` + `n_nan` por geração) | ✅ `ProbMOEAD_select.py:31-43` + `b5_prob.py:88-104,534` | ✅ **SIM** | **MEDIDA (smoke)**: 800/801 eventos com dict real; domínio [0,0 ; 1,0]; `n_chamadas==pop` 800/800; `n_nan`=0 em 40.000 chamadas | nada de estrutural. `n_nan` **não** detecta a cadeia A8 (bancada: 0 mesmo congelado) — o nome sugere o contrário |
| 2 | **`n_substituicoes`** (`len(selection)` por geração) | ✅ `ProbMOEAD_select.py:131` + `b5_prob.py:107-110,535` | ✅ **SIM, e RECONCILIA** | **MEDIDA (smoke)**: `n_sub ≥ slots trocados` 800/800 · `n_sub==0 ⟺ slots==0` 800/800 · Spearman 0,963 · razão mediana 1,2727 (o "one-to-many" agora quantificado) · Σ 16.477/800.000 = **2,0596%** de aceitação | nada. **Refuta definitivamente** a linha "Rejeitados no DI-10" que dava a contagem de substituições como inacessível sem patch invasivo |
| 3 | **`flag_vetores_degenerados`** (norma_min/max/amplitude/n_norma_zero) | ✅ escrito (`b5_prob.py:114-136`) | ❌ **NÃO — CÓDIGO MORTO** | **MEDIDA (smoke)**: `None` em **801/801**. **LIDA + PROVADA EM RUNTIME**: `evolver.population.problem.reference_vectors` (`b5_prob.py:118`) — `DataProblem` não tem o atributo (`hasattr=False`); ele vive em `evolver.reference_vectors` (`BaseEA.py:182`) | **1 token** para deixar de ser nulo; **+3 linhas** (acumular a norma junto do `P_WRONG_STATS`) para virar série por geração — sem isso, mesmo consertado ele é constante por run e **não data o onset**, ao contrário do que o próprio comentário promete |
| 4 | **PESOS de decomposição no HEADER** (CONTRATO §6.1) | ❌ | ❌ | **MEDIDA**: header 22 chaves idêntico s42×smoke; **e o requisito sumiu do `contrato_61.json`** | implementar (1 linha) **ou** registrar a retirada como decisão. Hoje o G-7 dá verde porque o contrato executável não pede |
| 5 | **`params` no ⑤** (achado de contrato da F5: 45/45 sem) | ✅ | ✅ **SIM** | **MEDIDA (smoke)**: `params` presente, com mode 72, spec do GP, `n_dataset` e a lista de âncoras. **ERRATA 11 confirmada** | nada |
| 6 | **`campanha_id` + `repo_hash` + schema v2** | ✅ | ✅ **SIM** | **MEDIDA**: `9ad0138aa9a4_2026-07-31` / `9ad0138a…` ≡ HEAD / `schema_version=2`; **0/45 na s42** | nada |
| 7 | **`sigma_dict.granularidade_③`** | ✅ | ✅ **SIM** | **MEDIDA**: 19 chaves no smoke vs 18 na s42; a nova documenta "ger 1 = pop INICIAL (LHS, PRÉ-seleção)" — **fecha a armadilha nº 1 da minha F5** | nada |
| 8 | **I-02 — cronômetro no portão único** | ✅ | ⚠ **SIM, com efeito colateral** | **MEDIDA**: `tempo_aval_real_s` = 0,0001 no smoke vs **0,0 em 45/45** na s42; mesmo padrão nos 5 offline Python. **LIDA**: `standalone_harness.py:648-653` roteia as n linhas do dataset pelo `bud.evaluate` ⇒ nunca devolve `None` no offline, ao contrário do que `b5_prob.py:571-575` e `budget.py:192-200` afirmam | reconciliar código×comentário; **o teste "==0" da F5 para o regime offline não vale mais** |
| 9 | **ERRATA 12** (`p_wrong_stats` exigido de b5r/moead_media era gate INVERTIDO) | ✅ | ✅ **CONFIRMADA POR DADO** | **MEDIDA**: `b5r` tem a chave presente mas **0/1144 não-nula**; `moead_media` **não tem a chave** (0/801); `c311`/`treed_media` idem. **A ablação D77 está limpa** — se `p_wrong_stats` aparecesse no piso, o experimento estaria inválido | nada |
| 10 | **Rito de teto** (`teto_wall`) | ✅ `b5_prob.py:485-493` | ⚪ não exercitado no b5m | **LIDA**: checado a cada `iterate` = 10 gerações; aborto limpo com camadas parciais e manifesto `failed` | granularidade ≈ 37 s no b5m (wall mediano 2.982 s / 801 gerações × 10). Folga enorme contra o teto de 12 h (wall **máximo medido: 3.589,6 s**) |
| 11 | **`NO_RETRY`** (I-4) | ✅ `experiments.py` | ⚪ N/A | **LIDA**: os 3 padrões são de b1/DTLZ4, c154 e c262/WFG1 | nenhum modo de falha do b5m está na lista — retry normal, correto |

**Erratas minhas contra o handoff T11** (a torre pediu que eu re-medisse):
- **§5, tabela dos 24:** `b5m` e `moead_media` aparecem como "✅ **6/6**" enquanto `b5r`/`c311` aparecem como "⚪ G-1 n/a". **Medido com `gates_proveniencia` sobre os smokes preservados: os quatro dão exatamente 5 OK + 1 n/a (G-1).** O b5m tem `real_solution_id` **0/60.050 não-nulo** e ② com **0 linhas** — o G-1 é não-aplicável por desenho (DI-16.17/A17), hoje nem nunca. Rótulos inconsistentes entre linhas com resultado idêntico; **não há defeito no config**, só na tabela.
- **§15.2 e o LEIA-ME:** "os 11 smokes PYTHON não estão preservados". **Falso** — `evidencia_T11/smoke_python/` existe com 11 células (b5m entre elas). Sem isso, metade desta análise teria sido só leitura de código.
- **§15.5:** a cadeia A8 "observável nas duas pontas" — **só uma ponta funciona** (item 3 acima).

---

## 5. Comparação canônica (Entrega 2) — inalterada em substância, re-verificada nas fontes

Setup do paper (§IV-A, l. 306-320 do .md + agente-4 §3.1/6.B): DBMOPP P1/P2, **n = 10 fixo**, K = 2…10, **N_D = 109**, FE_max = 40.000 surrogate, **31 execuções**, métricas **HV e RMSE** (referência no material suplementar), Wilcoxon + Bonferroni; DTLZ só no suplemento, sem números no texto.

| interseção | veredito | evidência |
|---|---|---|
| DBMOPP P1/P2 (o corpo inteiro do paper) | **incomensurável (eixo X)** — interseção **vazia** | nenhum DBMOPP no nosso grid de 25 |
| DTLZ1/2/3/4/7 | **incomensurável** | n=10 ≠ D=7/12/22 · K 2–10 ≠ M=3 · N_D 109 ≠ 216/371/681 · HV(ref suplementar)+RMSE ≠ IGD+/HV-norm(ref 1,1) · o paper não publica número de DTLZ no texto |
| ZDT / WFG / MMF / BBOB (20 problemas) | **incomensuráveis por inexistência no paper** | — |
| **J1 — "Prob-MOEA/D ≫ Gen-MOEA/D"** (§IV: "extremely poor performance of Gen-MOEA/D") | **SENTIDO CONFIRMADO** (forte no eixo do mecanismo, direcional no do desempenho) | ablação D77 exata, mesma máquina: σ(⑦) menor em **32/45**, **Wilcoxon p = 0,0012047**, razão mediana 0,7239; IGD+(⑦) melhor em 27/45, mediana **0,3596 vs 0,5603** (−36%), p = 0,281 — direcional, não significativo com 1 semente |
| **J2 — Fig. 10: "surrogate HV cai enquanto o HV real sobe"** | **PARCIALMENTE CONFIRMADO, com causa identificada** | 28 de 32 células com HV-surrogate não-nulo terminam **abaixo do próprio pico**; a versão estrita ("cai desde o início") só vale em 22/45 porque **B18.9 (A17) inverte a condição inicial**. Ilustração extrema: DTLZ1/DTLZ3 com HV-surrogate **1,331 = 1,1³ = o máximo teórico** e HV real **0,000** |
| **J3 — "o genérico pode ser pior que não fazer nada (Init)"** | **SENTIDO CONFIRMADO** | vs a métrica da ① (= o dataset = o "Init" do paper): b5m supera Init em **20/45**; o piso genérico só em **12/45** |
| J4 — "Hyb-MOEA/D não supera Prob-MOEA/D" | **não-testável** | Hyb fora do escopo (D56 / A27) |
| escalabilidade (nossa) | contexto para o M8/M9 | wall mediano **2.981,9 s** (máx **3.589,6 s**) vs b5r 211 s (14,1×) vs piso 67 s (44,5×); 45 células = **37,4 h-core** em 1 semente ⇒ **≈1.123 h-core** em 30 sementes |

**Nunca "artigo melhor"** (Botão 4): o paper não publica IGD+, nosso HV usa outra referência e outro normalizador, e a interseção de problemas é **vazia**. Os três vereditos acima comparam **sentido**, nunca valor.

---

## 6. Saúde em escala

**s42 = o corpus principal.** `integridade_f52a.csv` filtrado a b5m: **ZERO linhas** (45/45 com parquets legíveis, manifesto parseável, `.jsonl` sem linha órfã). `contrato_f52b.csv`: 45 linhas idênticas, `⑤ CHAVES faltam ['params']` — **achado agora FECHADO pela T11** (§4, item 5), sem perda analítica retroativa (os parâmetros eram recuperáveis do `sigma_dict` de 18 chaves + `artifacts/params.json`). 3 células com 1 só footer (`swap_medium-lhs_MMF16_20`, `swap_medium-lhs_ZDT4`, `swap_medium-mvns_ZDT4`) — falta o registro do **despachante**, não o do runner; a triagem usa `motivo_parada='orcamento'` (45/45), nunca a contagem de footers. `sweep-medium-mvns/b5m/WFG9` é 1 dos 4 falsos-vermelhos aceitos pelo autor (desvio relativo 1,530084e-05 = ULP fp32 × nº de condição) — nada a re-litigar, e a **regra 11 do §10** agora o cobre normativamente.

**Sonda — a régua Sobol (104 pares célula×objetivo, 1 bloco por célula, espaço CRU, `transf_tipo` NULL 45/45).** WAPE mediano **0,12683** (p25 0,043592 · p75 0,27426 · mín 4e-06), corr mediana **0,9465** (>0,9 em **59/104**), cobertura95 mediana **0,8954**, **σ-NaN = 0/104** — a `cobertura95` é lida ao pé da letra neste config. Padrão por família: ZDT f0 quase-exato (corr 1,000000), MMF11_L f0 idem, DTLZ2 bom nos 3 objetivos (0,050–0,099), MMF16_20 bom; **WFG é o pior bloco não-degenerado** (WFG4 f0 corr 0,005; WFG9 0,10–0,21); BBOB heterogêneo (F55 corr 0,28/0,19 com WAPE 0,077/0,073 — o GP acerta a escala e erra a forma). **Não há "tendência 1º→último bloco"**: com treino único há um bloco só (a prova de congelamento do modelo é estrutural, A4).
**Régua no smoke T11** (MMF1/s0, join posicional Δ=0,0): obj0 WAPE 0,043720 / corr 0,991918 / cob95 0,96510; obj1 WAPE 0,727434 / corr 0,242547 / cob95 0,82685 — coerente com a mesma célula na s42 (0,071199/0,982771 e 0,796866/0,279454). **σ-NaN = 0 nos dois.**
⚠ **Regra 12 do §10 não se aplica ao b5m**: não há `regime='sonda_estratificada'` (medido: os regimes da ③ são exatamente `{offline, sonda}` na s42 e no smoke). O bloco comparável entre configs é o único que existe aqui.

**Métricas oficiais — indiscriminantes por desenho.** IGD+, HV e |ND| são **exatamente iguais** entre os 5 configs offline em **45/45** células (D69: a métrica lê a ① = o dataset compartilhado). b5m "bate o piso" em 0/45 — e qualquer offline também. **O endpoint discriminante é a ⑦.**

**Endpoint ⑦.** IGD+ mediano: b5m **0,3596** · b5r 0,3149 · c311 0,3881 · moead_media 0,5603 · e103 0,2298 (⚠ e103 rodou na vm3 — o piso de ruído IGD+ ≤58,98% se aplica a essa coluna; b5m/b5r/c311/piso são todos **mac**, então as comparações internas deste relatório **não** carregam o piso). Fantasia mediana: b5m 0,380. σ(⑦) pela régua da sonda: b5m **0,5169** = o menor dos cinco. Melhores: MMF11_L 0,029 · MMF4 0,033 · DTLZ2 0,057 · MMF16_20 0,070 · BBOB_F1 0,087. Piores: **DTLZ3 542,3** e **DTLZ1 109,9** (as duas congeladas), ZDT4 46,7, ZDT6 6,6.

**Trajetórias.** 0 violações de monotonicidade do IGD+ em 855 transições (45×19) — no offline a trajetória percorre a **acumulação do dataset**, logo é monotônica por construção do arquivo ND: sinal de sanidade da métrica, **não** curva de aprendizado.

**Sweep.** "Mais dado → melhor?" **Não-monotônico e específico de problema**: WFG9 melhora (0,323→0,217 lhs; 0,216→0,147 mvns), DTLZ2/ZDT1 pioram levemente, **MMF16_20-lhs piora 13× (0,070→0,955)** — e é exatamente a célula cujo GP obj0 **degenerou no medium**. ZDT4 piora em ambas as distribuições. **Leitura para o D97: no b5m aumentar N não melhora de forma confiável porque o modo de falha dominante não é falta de dado — é o ajuste do GP.**

---

## 7. SCORE e VEREDITO

### **SCORE: 8,5/10** (F5: 8,0) · **RECOMENDAÇÃO: ACEITAR + CAVEAT** · **VEREDITO: MELHOROU**

**A causa nomeada do movimento, item a item — e o que NÃO o causou:**

| fator | move o score? | por quê |
|---|---|---|
| **CORREÇÃO REAL, verificada em dado** | **+** | o único (3) da F5 (A25/DI-10) foi implementado e **funciona**: `p_wrong_stats` 800/801 e `n_substituicoes` 800/801, reconciliados contra a ③ em 800/800 gerações. Era literalmente "o item que segura o score abaixo de 8,5" (F5 §7.3). A **quantidade central do artigo deixou de ser inauditável ex-post** |
| **A8 CONFIRMADA em bancada** | **+** | a cadeia que a F5 inferiu e mandou à F5.4 está provada elo a elo (`adapt`→norma 0 em 50/50 → PBI NaN 1000/1000 → `P_wrong = 0.0` exato → 0 substituições), com contrafactual (vetor sadio, mesmo μ/σ → **0,4995**). O caveat científico central do config passou de suspeita a fato |
| **verificabilidade elevada** | **+** | A15 (rampa θ) migrou de **indireta → direta** (Spearman −0,8688 sobre o próprio P_wrong); A19 (40.000 FE) ganhou **contagem exata** (Σ n_chamadas = 40.000); o balde T caiu de 4 para 3 |
| **achado de contrato da F5 fechado** | **+** | `params` no ⑤ (era 0/45) + `campanha_id`/`repo_hash`/schema v2 + `granularidade_③` no `sigma_dict` (fecha a armadilha nº 1 da F5) |
| **3 novos (3) — todos de instrumentação** | **−** | `flag_vetores_degenerados` morto (**e é justamente o que fecharia o laço da A8**), pesos no header ausentes + requisito retirado do contrato executável sem errata, e o I-02 quebrando a semântica offline de `tempo_aval_real_s` |
| **MECANISMO** | **0** | **não mudou nada, e é isso que se esperava.** As 45 células da s42 seguem válidas: re-medi 20 invariantes estruturais e **todos reproduzem exatamente** os números da F5 (n_ger 45/45, ⑦ ND 45/45, link bit-a-bit 45/45, sonda Δ=0,0 45/45, dataset Δ=0,0 45/45, LHS D/D 45/45, f_best 30.165/30.165 com resíduo 5,94e-08, bounds 0/1.801.620, ablação Wilcoxon 0,0012047, σ-NaN 0) |
| **amostra** | **0** | mesmo corpus (45 células, 1 semente); o smoke acrescenta 1 célula com instrumentação nova, não poder estatístico |

**Por que 8,5 e não 9,0:** a régua dá 9 para "(3) menores já investigados e **provados benignos**". Os três são de instrumentação, têm causa-raiz provada e **nenhum toca a busca** — mas um deles (A25) é o campo que a própria F5 pediu para fechar o laço da A8, e ele está **morto no HEAD** (`9ad0138`, arquivo limpo no git). Entregar um campo que nunca produz dado é pior do que não entregá-lo: o G-7 fica verde e um leitor futuro concluirá "os vetores nunca degeneraram" a partir de um `None` que é `AttributeError`. **Por que não 8,0:** seria ignorar que a correção principal foi feita, funciona, e reconcilia contra dado independente.

**Ação recomendada antes do disparo M8 (custo: minutos, risco: zero):**
1. `src/b5_prob.py:118` — `evolver.population.problem.reference_vectors` → **`evolver.reference_vectors`**. 1 token. Sem isso, as ~4.050 células offline da rodada final nascem com o campo nulo.
2. Se o onset importar (e importa: é o que separa congelamento total de parcial, e o parcial é 649 das 2.237 transições), mover a coleta da norma para o acumulador módulo-nível junto do `P_WRONG_STATS`, com a chave `gen_count` — 3 linhas, mesmo padrão já validado.
3. Decidir sobre os pesos no HEADER: implementar (1 linha) **ou** registrar a retirada no §6.1. Hoje o contrato escrito e o executável discordam.
4. Reconciliar o comentário do I-02 no caminho offline (ou o campo, ou o comentário — não os dois).

---

## 8. Aspectos classe (3) 🎯 — evidência completa

**🎯 A25 — `flag_vetores_degenerados` é código morto.**
- **Requisito:** pedido da F5 §8 ("logar um flag `vetores_degenerados` quando `norm(reference_vectors.values)==0`"), implementado pela T11 em `src/b5_prob.py:114-136` e chamado em `:536`.
- **Medição (dado):** `None` em **801/801** eventos `decision` do smoke `off/b5m/MMF1/s0` (`repo_hash 9ad0138a…` ≡ HEAD).
- **Causa (runtime, `env_b5`, desdeo vendorizado — `t11_b5m_00_flag_dead.py`):** `hasattr(DataProblem,'reference_vectors') = False`; `hasattr(Population,'reference_vectors') = False`; 0 ocorrências no fonte do `DataProblem`; `'reference_vectors' in Population.py.__init__ = False`. O atributo existe **só no evolver** (`BaseEA.py:182` `self.reference_vectors = ReferenceVectors(...)`; usado como `self.reference_vectors` em `ProbMOEAD.py:128`). Logo `evolver.population.problem.reference_vectors` levanta `AttributeError`, capturado por `except Exception: return None`. Confirmado end-to-end: `_vetores_degenerados` importado do próprio `src.b5_prob` devolve `None`; pelo caminho correto devolveria `n_norma_zero=50, norma_min=0, norma_max=0, amplitude=0`.
- **Defeito secundário (mesmo após o conserto):** a chamada está no **replay pós-busca** (`b5_prob.py:536`), sobre o estado final dos vetores ⇒ valor **constante nas 801 gerações**. O comentário do código promete o oposto ("mostra os vetores encolhendo ANTES de zerar, que é onde a cadeia A8 começa") — inalcançável neste sítio de coleta.
- **Consequência provada:** a única assinatura restante da A8 é a jusante (`p_wrong_stats` min=med=max=0,0 + `n_substituicoes`=0), que **não distingue** colapso-de-vetores de "offspring genuinamente pior"; e **`n_nan` não ajuda** — bancada [6]: no estado congelado o registro é `{min:0.0, med:0.0, max:0.0, n_chamadas:50, n_nan:0}`, porque o NaN está nas amostras de PBI, não em `probabilities`.
- **Nenhum artefato preservado exercita a A8:** o smoke é MMF1, célula sadia (0 gerações congeladas em 800). O comportamento congelado só existe na s42, que é **pré-T11**. Portanto a instrumentação da A8 **nunca foi testada contra o caso que ela existe para observar**.

**🎯 A26 — Os pesos de decomposição no HEADER: requisito não cumprido e desaparecido do contrato executável.**
- **Requisito:** `CONTRATO_DE_DADOS.md:332`, linha `b5`, coluna "+ DI-10 (novo)" — vinculante, e a leitura do CONTRATO é obrigatória por sessão.
- **Medição:** header do ⑥ com **22 chaves, conjunto IDÊNTICO** na s42 (45/45) e no smoke T11 — conferido chave a chave; nenhuma é pesos/vetores.
- **Agravante medido:** `claude_code_context/artifacts/contrato_61.json` → `configs.b5m = {"di10": ["p_wrong_stats"], …}`. A exigência dos pesos **não está** no artefato normativo que o gate G-7 consulta; nenhuma das 16 erratas a cobre. **O G-7 dá verde ("⑥ 5/5 campos") porque o contrato executável não pede o que o contrato escrito exige.**
- **Impacto analítico:** limitado — os 50/105 vetores são determinísticos dado (M,H) e eu os reconstruí independentemente (A9/A10). O problema é de governança do contrato, não de perda de dado.

**🎯 A22 — O cronômetro I-02 mudou a semântica offline de `tempo_aval_real_s`.**
- **Medição:** s42 = **0,0 em 45/45** (e em 100% das 155 células dos 5 offline Python); smoke T11 = **0,0001** (b5m), com o mesmo padrão em b5r 0,0003 · c311 0,0003 · moead_media 0,0001 · treed_media 0,0572.
- **Causa (código):** `standalone_harness.py:648-653` roteia as **n linhas do dataset** por `bud.evaluate`; o cronômetro do I-02 vive dentro do `evaluate` (`budget.py:249-251`) ⇒ `_n_avals_cronometradas = n ≠ 0` ⇒ a property **nunca devolve `None` no offline**. Contradiz a docstring de `budget.py:192-200` e o comentário de `b5_prob.py:571-575` ("nenhuma avaliação real acontece DENTRO do run").
- **Severidade:** baixa para o mecanismo (0,0001 s = 0,000004% do wall; FE e orçamento intocados) — **alta para a leitura**: o teste que a F5 usou para A1 (`tempo_aval_real_s == 0`) reprovaria todo o corpus offline pós-T11. Efeito colateral positivo: o Python offline passa a se comportar como o e103 (que já tinha o cronômetro no portão pela DI-12.4), o que **uniformiza** — mas o texto do código precisa acompanhar.

---

## 9. Teto T + armadilhas + o que este corpus NÃO permite verificar

**T (3 itens, com a razão):**
1. **A12 — a identidade do estimador** (`Σ1[a>b]/S² ≡ ∫pdf_A·CDF_B` da Eq. 8) e **S=1000 / `truncnorm(−3,3)`**: os agregados de P_wrong agora são logados, mas os pares e as ~4e6 amostras/geração são descartados por desenho. Sustentado por código + pela consequência estatística (A14) + pelo domínio observado.
2. **A18 — índices de distribuição do SBX (30) e da PM (20)**: defaults do código; só bounds/`repair` são observáveis (0 violações em 1.801.620 linhas).
3. **A24 — env/pins/patches** (sklearn 0.21.3, desdeo VENDORED root-first, stub pygmo, âncoras `b5-mode72-kde`, `b5-mode7-archive`, `b5-pwrong-stats`): declarativo, coberto por `anchors.json` + `repos.lock`. A **proveniência** (campanha_id/repo_hash/params) saiu do teto na T11.

**Saíram do teto nesta análise:** o **valor** de P_wrong (A12, parcialmente — min/med/máx por geração); a **rampa θ** (A15, agora direta); a **contagem de 40.000 FE** (A19, exata).

**Armadilhas confirmadas (as 15 da F5 seguem válidas; destaco as que mudaram ou são novas):**
1. **`n_geracoes` inclui a população inicial LHS** — a geração 1 da ③ não é geração de busca; há ⌊40000/pop⌋ passos de seleção. **A T11 fechou esta armadilha no `sigma_dict.granularidade_③`.**
2. **A ⑦ tem `pop` linhas, não `|ND|`** — contar `len(⑦)` como tamanho do Pareto superestima ~2,6×.
3. **As métricas oficiais empatam EXATAMENTE entre os 5 offline em 45/45** (D69) — nunca ranquear offline por elas; o endpoint é a ⑦.
4. **`WAPE == 1,000000` exato é assinatura de μ≡0**, e `corr = NaN` de μ exatamente constante — GP degenerado, não bug da régua. **`σ = 31,622776 = √1000` é o TETO do `ConstantKernel`.** **`HV-surrogate = 1,331 = 1,1^M` é o MÁXIMO teórico**, não excelência (coexiste com HV real 0,000).
5. **b5m tem 1 bloco de sonda, não 2** — a prova de congelamento do modelo da família treed não existe aqui; a equivalente é `④=1 ∧ fit_series=1 ∧ fe_treino_max constante`.
6. **`swap_small-lhs_{P}` é réplica bit-a-bit de `off/{P}`** (5 das 45) — não contar como evidência independente (as medianas as incluem, por consistência de denominador).
7. **Aliasing fp32 (D53)** ao agrupar substituições por X: 545/356.137 grupos são a união de 2–3 offspring distintos. **A regra 11 do §10 agora formaliza isso** — diferenças abaixo da resolução do fp32 não são conclusivas em nenhuma análise de dominância sobre ①/⑦.
8. **`n_front1` é calculado em fp64 antes do export**; recomputar da ③ diverge em ~2,8% dos eventos, sempre para menos. `f_best`, por ser mínimo, fecha 100%.
9. 🆕 **`tempo_aval_real_s == 0` NÃO é mais o teste do regime offline** (A22). Use `fase='init'` em 100% da ① + `fe_final==maxfe==n_dataset` + ausência de eventos `guard`.
10. 🆕 **`flag_vetores_degenerados: null` NÃO significa "os vetores não degeneraram"** — significa que o campo não foi coletado (A25). Até o conserto, ler `null` como **ausência de medida**, jamais como ausência de colapso.
11. 🆕 **`n_nan` do `p_wrong_stats` é 0 mesmo na busca congelada** (provado em bancada) — não é detector da cadeia A8.
12. 🆕 **O G-1 é `n/a` por desenho no b5m** (③ com 0 linhas marcadas, ② vazia — DI-16.17), apesar de a tabela §5 do handoff registrar "6/6". Não é pendência.
13. **`normalize_y=False` + `alpha=0` fazem o GP reverter a μ=0 fora do suporte** — em f estritamente positiva e grande (ZDT4, DTLZ1/3) isso cria um **ótimo falso** que nem P_wrong nem a média rejeitam (não é "boa média + σ alto", é "média imbatível **e** σ alto"). É a explicação dos runs com turnover 100% permanente e σ **crescente**.

**O que este corpus NÃO permite verificar:**
- **A instrumentação da A8 contra o caso da A8.** Nenhum artefato preservado combina a T11 com um GP degenerado: o único smoke b5m é MMF1 (sadio, 0/800 gerações congeladas), e as 5 células congeladas vivem na s42, que é pré-T11. Um smoke de **`off/b5m/DTLZ3/s42`** (~10 min, célula pequena e já conhecida) fecharia isso e é o teste que eu pediria antes do M8.
- **A não-perturbação do patch `b5-pwrong-stats` por dado próprio.** O par G-6 do b5m não está em `evidencia_T11/g6_com|g6_sem` (só os 9 MATLAB estão). Fica o **veredito registrado** (`bcda99b31dfb2c35`) + a prova por leitura (o acumulador só lê `probabilities` e `len(selection)` **depois** da decisão, não consome RNG, não altera o retorno). Nota do handoff §4.2.4: o par G-6 do b5m foi um dos que o `staleness.py` pegou **stale** e mandou refazer — mais uma razão para não aceitá-lo sem o artefato.
- **A identidade numérica do estimador MC** (T-1) e os **índices SBX/PM** (T-2) — exigem re-run instrumentado.
- **Qualquer comparação numérica com o paper** — interseção de problemas vazia (§5).
- **Poder estatístico**: 1 semente. A ablação D77 é significativa em σ (p=0,0012) mas **não** em IGD+ (p=0,281); as 30 sementes do M8/M9 custam **≈1.123 h-core** só neste config.

---

**Artefatos desta análise** — todos em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/b5m/` (único local de escrita; nada foi editado fora dele, nenhuma célula foi executada, `data/experiments` intocado):
`t11_b5m_00_flag_dead.py` (prova de runtime do caminho de atributo morto) · `t11_b5m_01_cadeia_A8.py` (bancada da cadeia A8, 9 asserções) · `t11_b5m_02_s42_remedicao.py` → `t11_b5m_s42.csv` (45×50: re-medição estrutural + ausência da T11 no dado velho + congelamento) · `t11_b5m_03_smoke.py` → `t11_b5m_smoke.csv` (800 gerações do smoke: `p_wrong_stats` × `n_substituicoes` × substituições reconstruídas da ③) · `t11_b5m_04_errata12.py` (cross-check da ERRATA 12 nos 5 smokes offline + par de ablação MMF1/s0) · `t11_b5m_05_identidades.py` → `t11_b5m_identidades.csv` (f_best/n_front1/⑦/sonda/dataset nas 45) · `t11_b5m_06_lhs_binding.py` → `t11_b5m_lhs_binding.csv` (LHS contra bounds reais + binding pelo `dataset_path` do header) · `t11_b5m_07_ablacao_sonda.py` (ablação D77 recomputada + régua Sobol) · `t11_b5m_08_vizinhanca_det.py` (A10/A11 em 6 células com lattice reconstruído + 5 pares de determinismo) · `t11_b5m_09_sonda_smoke.py` (régua do smoke vs s42).
**Insumos pré-computados consumidos sem recomputar:** `f5/sonda_f52e.csv`, `f5/metricas_finais_f52c.csv`, `f5/contrato_f52b.csv`, `f5/integridade_f52a.csv`, `f5/tempo_f52d.csv`, `f5/trajetorias/`, e as tabelas da minha própria F5 em `f5/baterias/b5m/` (`b5m_endpoint7.csv`, `b5m_congelamento.csv`) — todas re-conferidas contra medição fresca antes de serem citadas.