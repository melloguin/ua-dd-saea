# handoff/R1-c141.md — Rodada 1, 1º FAN-OUT: c141 MMRAEA (repo do autor, porte 4.15)

**Data:** 2026-07-16 · **Env:** MATLAB **R2025a** (Mac arm64) + PONTE `pyenv` InProcess
(`~/ponte_teste/bin/python`; numpy 2.4.6 · pymoo 0.6.2 — verificada ANTES, D80/D81).
Leitor Python independente no **env-main** (`.../mestrado_experimentos_dissertacao/bin/python`).

**Gate objetivo (encanamento, D97):** `accept.py R1-c141 --alg c141 --problema {MMF1,ZDT1}
--semente 0` → **exit 0 (VERDE)** nos dois: 4 saídas, **FE=31D−1 EXATO**, CP-init bit-a-bit. ✅
**Fidelidade:** instrumentada e entregue para a validação MANUAL do autor (D97) — **não é gate meu**.
**Regressão:** F0-01/02/03/04, R1-00 (stub) e R1-c217 (MMF1+ZDT1) seguem VERDES; `preflight.py` exit 0.

**Escopo:** 1º fan-out do padrão do caso-modelo c217 na infra do R1-00. **O padrão do c217
GENERALIZOU sem nenhuma mudança na infra transversal** (zero edição em FEBudget/RunBuffer/
hook_output/write_*/despachante): a receita N.0 do handoff R1-c217 §4 foi copiada literalmente
(`run_c141` = `run_c217` com os deltas do c141) e o embrulho de lote `c217_batch_eval` (D61) foi
**reusado como está** (genérico, como previsto). Eu NÃO julguei fidelidade (D97).

---

## 1. Resultado do gate (por problema, semente 0)

| Problema | D | M | maxFE=31D−1 | FE_final | EXATO | CP-init | ciclos | lote min/med/max | cache_hits | accept.py |
|---|---|---|---|---|---|---|---|---|---|---|
| **MMF1** (gate) | 2 | 2 | 61 | **61** | ✅ | ✅ | 10 | 2/3/6 | 1 | **VERDE** |
| **ZDT1** (gate) | 30 | 2 | 929 | **929** | ✅ | ✅ | 153 | 2/4/9 | 1 | **VERDE** |
| DTLZ2 (fidelidade) | 12 | 3 | 371 | **371** | ✅ | ✅ | 64 | 2/3/8 | 1 | VERDE |

`cache_hits = 1` = o **probe** do construtor absorvido (receita N.0 — `initFcn=DoE[0]`, `bud`
antes do Problem). O c141 não propôs duplicata bit-a-bit de infill nestes 3 runs (CSO/SBX em
real raramente reproduzem bits), mas o **sync D89 está fiado** (obrigatório — ver §2). O
**batch é VARIÁVEL** (2–9 aqui; pode ser 0 — guard armado, não disparou em nenhum run).

## 2. Patches aplicados (por arquivo:linha)

**Porte 3 linhas (L.7) + injeção + guardas — em `algorithms/c141_MMRAEA/extracted/MMRAEA/`:**
- `MMRAEA.m:24` — **injeção do DoE** (D63/D87/D88): `A2 = Problem.Evaluation(Problem.data.X0)`
  substitui o par UniformPoint-Latin([0,1])+re-escala **JUNTO** (mesma classe da D94 — injetar só
  no RHS re-escalaria de novo) E o `SOLUTION(dec)` da API 3.x (porte linha 1/3). X0 nativo, direto.
- `MMRAEA.m:59-61` — **guard batch-vazio [IMPL]** (`if ~isempty(PopNew)`) antes do
  `New = Problem.Evaluation(PopNew)` (porte linha 2/3): o batch pode ser 0 (iteração 0-FE, fiel
  ao paper); `Evaluation([])` quebraria o CallFcn/SOLUTION do 4.15. Disparo → evento no `.jsonl`.
- `MMRAEA.m:69` — **sync D89 herdado do c217** (PCS:61): `Problem.FE = Problem.data.bud.fe` após
  o infill — o `obj.FE` nativo NÃO governa (infla com cache-hit; o c141 PODE propor duplicatas:
  membros do arquivo sobrevivem nas subpops e chegam ao infill). Hard-stop real = throw do FEBudget.
- `MMRAEA.m:32/:75` — `tic/toc` no fit dos M+2 RBFs (§17.6) + chamada `c141_instrument(...)` no
  fim do ciclo (instrumentação PÓS-decisão — D97; nenhuma decisão alterada).
- `EAOptimization.m:39` — **porte linha 3/3**: `OperatorGA(PopDec2)` (3.x) →
  `OperatorGA(Problem,PopDec2)`; defaults 4.15 `{1,20,1,20}` = **SBX proC=1/disC=20 + PM proM=1
  (=1/d por variável)** — exatamente o Balde B do c141 (código oficial; paper ✓). `:55` — 3º
  output `nsub` (|subpops| pós-ES_PDR, só leitura).
- `calFitness.m:12-13` — **guard NaN do SDE (D76/L4, 🟠 obrigatório)**: denominador min-max
  degenerado (`fmax==fmin`) recebe `+eps`; caso não-degenerado **BIT-IDÊNTICO ao stock**; 2º
  output = flag → logado por sítio (`fmodel_train`/`infill_fit1`/`instrument_pool`). *Não
  disparou em nenhum dos 3 runs (armado).* ⚠ A âncora `c141-sde-eps-L4` apontava
  `InfillStrategy.m` com placeholders; o SDE VIVE em `calFitness.m` (chamado de MMRAEA:44 e
  InfillStrategy:22) → **path/literais corrigidos no `anchors.json`** (precedente Sessão 1);
  `preflight.py` = **APLICADO**, exit 0. `repos.lock` re-pinado (content-hash pós-patch, D80).
- `InfillStrategy.m` — 2º output `info` (nível 1/2/3 da cascata, Fit1/2/3, Q/U, |fronts|, flag
  L4): **só captura o que a função já computa** — decisões intactas (verificado linha a linha).

**Integração [IMPL] (repo raiz):**
- `src/experiment.m:43` — `case 'c141' → run_c141` (`:377`; corpo = cópia do `run_c217`):
  ponte → `load_doe` → **FEBudget ANTES do Problem** → UserProblem com `initFcn=DoE[0]` (probe
  absorvido) → **reuso do `c217_batch_eval`** (D61) → `rng(semente)` depois → Solve com
  `hook_output` (②) → export 4 camadas → CP-init → manifesto → falha honesta (D23/D81).
  **`'N', min(100, 11D−1)`** = N POR SUBPOPULAÇÃO (DEF-A5/B12.6 — ES_PDR.m:20 estoura se pool
  ger.1 < N, D≤9; pool de infill = 2N). MMRAEA **sem `'parameter'`** (wmax=20 hardcoded — paper ✓).
- `src/experiment.m:506` — `ensure_paths_c141`: PlatEMO 4.15 + a pasta do autor (FORA da árvore).
  Colisões de nome com o 4.15 (UpdataArchive [K-RVEA], CSO [single-obj], CalFitness [IBEA…],
  dsmerge [AB-SAEA], rbf_build/rbf_predict [SAMSO/SACC-EAM-II]) — **todas same-folder nos dois
  lados → auto-resolvem (S.8; renames defensivos dispensados)**; asserts garantem a precedência
  da cópia do c141 para as chamadas VIA PATH do `c141_instrument` (que vive em `src/`).
- `src/c141_instrument.m` (**novo**) — a instrumentação ②③/§17.5 específica (ver §4).

## 3. O que o fan-out CONFIRMOU do padrão c217 (para b1, b3, b4, e7, c238)

1. **`run_<alg>` por cópia funciona**: os únicos deltas reais foram N (DEF-A5), o path do repo do
   autor, a ausência de `'parameter'` e o instrument próprio. Infra transversal intocada.
2. **`c217_batch_eval` é de fato genérico** (D61) — reusado sem tocar; hard-stop no meio do lote
   disparou em MMF1/DTLZ2 (guard `hard_stop` no jsonl) e o FE fechou exato nos 3.
3. **A absorção do probe** (initFcn=DoE[0] + bud antes do Problem) reproduziu-se: 1 cache-hit por
   run, ① com exatamente 31D−1 linhas, CP-init hash = sidecar nos 3 problemas.
4. **O sync D89 é para TODOS** mesmo sem duplicata observada — barato e à prova de estagnação.
5. **`real_solution_id` nullable**: o c141 é o 1º a usar o fallback `double+NaN` do R1-00 (pool
   com candidatos NÃO avaliados). Funcionou como projetado; a consolidação re-casta p/ int32
   nullable. *(Nota: o tipo da coluna é double em TODO run c141 na prática — pool 2N sempre tem
   candidato não-avaliado; sem tipo variável observável.)*

## 4. Instrumentação produzida (para a SUA validação de fidelidade — D97)

Por run: **① real** (31D−1, init+opt) · **② pop** (membership do A2/ND-front por ciclo, todas as
gerações) · **③ surrogate** = **o pool 2N pós-ES_PDR por ciclo** (DEF-C2: a população que
sobrevive à seleção), `pred_tipo=valor`, `modelo=RBF-MQ3`, `mu_0..M-1` = objetivos preditos pelos
ARBFs, e **σ D45 em 2 colunas** (dicionário no manifesto, DEF-C4):
- `sigma_0` = **U de ranks NATIVA** `|Q1−Q2|+|Q1−Q3|+|Q2−Q3|`, calculada na **população
  completa** (a "extensão leve" da D45 — o algoritmo só a computa p/ o subconjunto do nível 3;
  mesmas convenções de ordenação de InfillStrategy:25-35).
- `sigma_1` = **desvio bruto entre os 3 modos** (std de [Fit1,Fit2,Fit3] por candidato) — proxy
  de ensemble comparável ao c149; escalas incomensuráveis (registrado — I.7/D45).
- `sigma_2..` = NULL (M=3 verificado no DTLZ2).

**`.jsonl` §17.5/S.7** (linha `c141_gen` por ciclo): `nivel` (1/2/3) + `motivo` + **`ramo_QU`**
(telemetria pedida), `n_front1/n_front2`, `lote`, `n_pool`, `n_sub1/n_sub2`, resumos
min/med/max de `fit1/2/3` e `Q/U` (do subconjunto da cascata), `U_pool_max`, `fe`, `|arquivo|`,
`tempo_fit_s`; + eventos `guard` (`batch_vazio`, `sde_eps` por sítio, `cache_hit`, `hard_stop`).
**Timing §17.6**: 1 retreino/ciclo, `n_acumulado`=|A1| no fit (ZDT1: 0,48 s no último fit com
n=926 — a curva O(n³) do mldivide denso está capturada).

**Números por problema (semente 0) — GUIA, não gate (D97):**

| Problema | FE | IGD_raw | \|ND\| | níveis (cascata) | âncora do paper (Anexo J) |
|---|---|---|---|---|---|
| MMF1 (d=2) | 61 | — (MMF: IGDX é pós-hoc D99) | — | **nível 3 em 10/10** | — |
| ZDT1 (d=30) | 929 | **1,7354e-2** (front analítico, 10k) | 43 | nível 3 em 153/153 | 3,3486e-2 @ **d=20**, 11d+119=339 FE |
| DTLZ2 (d=12, M=3) | 371 | **6,7651e-2** (das-dennis→esfera, 5050) | 179 | nível 3 em 64/64 | 1,2010e-1 @ **d=20**, 339 FE |

> ⚠ **CAVEATS para a sua leitura (D97 — eu não julgo):**
> 1. **Orçamento e d divergem do paper**: as âncoras J são a d=20 com FEmax nativo 11d+119=339;
>    nosso protocolo roda 31D−1 nos d canônicos (§5.1/D21). IGDs menores que a âncora são
>    esperados (mais FEs); a comparação justa é do **mecanismo** (cascata/níveis no `.jsonl`).
>    Se quiser a réplica d=20 do paper, é 1 entrada em `FIDELITY_PROBLEMS` (`src/experiment.py`,
>    padrão do DTLZ2_d15 do c217) — não fiz porque o cartão não pede.
> 2. **Ramo (Q,U) ativou em 100% dos ciclos nos 3 problemas** — I.7 esperava ativação RARA em
>    D baixo/M=2 ("|front|≤5"), mas essa expectativa era da leitura pré-v2.2 (gatilho u=5); com o
>    gatilho REAL do código (">1 candidato", v2.2 ✓), o front de [−Fit1,Fit2,−Fit3] (3 ordenações
>    quase-independentes) raramente colapsa a 1 ponto → nível 3 é o caminho comum. **Mecanismo
>    conforme o código oficial; julgue com o `.jsonl`.**
> 3. **IGD é o raw** (calculei fora do `metrics.py`, refs acima) — compute do SEU jeito da ① crua.
> 4. **Warning esperado em runtime**: `rbf_build:145` mldivide quase-singular (RCOND~1e-16, visto
>    no ZDT1) — é o "solve denso SEM regularização + dsmerge ds=1e-14 deixa quase-duplicatas
>    passarem → warning de mau condicionamento" do bundle (código herdado do paper, MANTIDO).
> 5. **Guardas armadas que NÃO dispararam**: `batch_vazio` (lote mínimo observado = 2) e
>    `sde_eps` (nenhum min-max degenerado) — ambas logam quando dispararem na bateria.

## 5. Como reproduzir

```
# MATLAB (raiz do repo):
addpath('src'); addpath(genpath('algorithms/_PlatEMO/PlatEMO'));
experiment('c141','MMF1',0,'main','data')     % idem ZDT1, DTLZ2
% ou pelo despachante (skip idempotente; c141 já está no roster default):
experiments('algorithms',{'c141'},'problems',{'MMF1','ZDT1'},'seeds',0,'parallel',false)

# Python (env-main), gate objetivo:
python3 scripts/accept.py R1-c141 --alg c141 --problema MMF1 --semente 0   # idem ZDT1/DTLZ2
```
Outputs em `data/experiments/main/c141/` (gitignored — regeneráveis). Wall-clock semente 0:
MMF1 ~10 s · DTLZ2 ~45 s · ZDT1 ~4 min (dentro do esperado p/ o piloto de timing §22.5).

## 6. Pendências / notas

1. **Commits `[R1-c141]`** (3): porte+injeção+guard+sync (2 arquivos do repo do autor) ·
   guard L4+captura da cascata (+`anchors.json`/`repos.lock`) · adapter+instrument (repo raiz).
   Nenhum blob (parquets gitignored). `cards/INDEX.md` → ✅ neste fechamento.
2. **Zero toolbox confirmado em runtime** (N.2.6): nenhum uso de Statistics/DL — o único MATLAB
   parfor-limpo do set segue limpo (instrument também não consome RNG: verificado, só
   sort/NDSort/rbf_predict determinísticos).
3. **A validação de fidelidade é SUA (D97):** os números da §4 são guia; o veredito (cascata fiel?
   níveis coerentes? σ D45 com cara certa?) é seu, lendo os `.jsonl` (campo `nivel`/`ramo_QU`) + a ①.
4. **Próximos fan-outs** (b1/b3/b4/e7/c238): o §3 acima é a lista do que copiar; b1/e7 têm o
   patch D94 próprio (par gera+re-escala), b4 tem cap-109/cpu (âncoras prontas), c238 precisa do
   embrulho classdef N.5 (o único que NÃO é cópia direta).
