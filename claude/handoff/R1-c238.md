# handoff/R1-c238.md — Rodada 1, fan-out: c238 EIM (repo STANDALONE do autor → embrulho classdef N.5)

**Data:** 2026-07-17 · **Env:** MATLAB **R2025a** (Mac arm64) + PONTE `pyenv` InProcess
(`~/ponte_teste/bin/python`; numpy 2.4.6 · pymoo 0.6.2 — round-trip verificado ANTES, D80/D81;
Optimization Toolbox — fmincon sqp do GP_Train; Statistics — normcdf/normpdf/lhsdesign).
Leitor Python independente no **env-main** (`.../mestrado_experimentos_dissertacao/bin/python`).

**Gate objetivo (encanamento, D97):** `accept.py R1-c238 --alg c238 --problema {MMF1,ZDT1,DTLZ2}
--semente 0` → **exit 0 (VERDE)** nos três: 4 saídas, **FE=31D−1 EXATO**, CP-init bit-a-bit. ✅
**Fidelidade:** instrumentada e entregue para a validação MANUAL do autor (D97) — **não é gate meu**.
**Regressão:** F0-01/02/03/04, R1-00 (stub MATLAB), R1-c217 (×3), R1-c141 (×3), R1-b3 (×3),
R1-b4 (×3), R1-b1 (×3) e R1-e7 (×3) seguem VERDES; `preflight.py` exit 0
(c238-hypervolume-rm = APLICADO; repos.lock c238_eim re-hashado — precedente c141).
**Verificação adversarial extra desta sessão:** 3 revisores independentes (lentes
fidelidade-stock / numérica-edge / fiação-schema) sobre os diffs, cada achado refutado por
2 verificadores adversariais (19 agentes) — **3 achados MINOR sobreviventes, 0 bloqueantes**:
1 fix de instrumentação aplicado (guard_range emitido NA DETECÇÃO — §5) e 2 hazards
documentados sem conserto (§8.5: crash latente STOCK do front-de-1-ponto; nota de infra do
fid pós-Solve, padrão herdado dos 6 runners). Fidelidade-stock: **0 achados** (o embrulho
não altera decisão nenhuma).

**Escopo:** 6º fan-out — o ÚNICO da R1 que NÃO é cópia direta do padrão: o EIM é um repo
STANDALONE (script `MultiObjective_EIM.m`, roda `clearvars;close all`, avalia por `feval`
próprio) e precisou do **EMBRULHO classdef do contrato N.5** (novo `algorithms/c238_EIM/EIM.m`).
Infra transversal INTOCADA (FEBudget/RunBuffer/hook_output/writers/despachante). Eu NÃO
julguei fidelidade (D97).

---

## 1. Resultado do gate (por problema, semente 0)

| Problema | D | M | maxFE | FE_final | EXATO | CP-init | iter. BO | GA pop (10D) | n_treino ini→fim | cache_hits | wall | accept.py |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **MMF1** | 2 | 2 | 61 | **61** | ✅ | ✅ | 40 | 20 | 21→60 | 1 | 8 s | **VERDE** |
| **DTLZ2** | 12 | 3 | 371 | **371** | ✅ | ✅ | 240 | 120 | 131→370 | 1 | 686 s | **VERDE** |
| **ZDT1** | 30 | 2 | 929 | **929** | ✅ | ✅ | 600 | 300 | 329→928 | 1 | **14.128 s** | **VERDE** |

`cache_hits=1` = o probe absorvido (receita N.0). **Overshoot zero por construção** (1
infill/iteração; o término é o `NotTerminated` com o `Problem.FE` SINCRONIZADO ao saldo
distinto do wrapper — sync D89); o throw D61 do FEBudget fica de rede.

## 2. O desenho do embrulho N.5 (o que NÃO existia nos outros fan-outs)

O script oficial virou o classdef **`algorithms/c238_EIM/EIM.m`** (novo, ~120 linhas, só
orquestração — as DECISÕES continuam nas funções STOCK do repo: `GP_Train`/`GP_Predict`/
`Infill_EIM`/`Optimizer_GA`/`Paretoset`). Mapa N.5 → implementação:

1. **(N.5-1)** sem `clearvars/close all/fprintf`; as 2 linhas `Hypervolume` não existem no
   classdef (e foram removidas do script — §3).
2. **(N.5-2)** `'N',100` no UserProblem — **INERTE** no c238 (o GA interno é 10D; o init é o
   DoE); registrado no manifesto. Orçamento = `Problem.maxFE` = 31D−1.
3. **(N.5-3)** init = `Problem.Evaluation(Problem.data.X0)` — o `lhsdesign` nativo é
   sobrescrito pelo global-override do cartão (DoE 11D−1 do ARTEFATO, D63/D87/D88; a
   avaliação é SEMPRE a ponte/FEBudget — nunca avaliação própria).
4. **(N.5-4)** `while Algorithm.NotTerminated(Population)` — término pela exceção
   `PlatEMO:Termination`; o hook (②) dispara no topo de cada iteração.
5. **(N.5-5)** re-scaling min-max de y POR ITERAÇÃO (paper §V-B(5)a) + guard
   `max(range,eps)` [IMPL]; front não-dominado via `Paretoset(sample_y)` sobre o y **CRU**
   (como o script :37/:61 — o min-max é monótono, o ND é idêntico).
6. **(N.5-6)** `GP_Train(train_x, y_scaled(:,ii), lb, ub, 1·ones(1,D), 1e-3·ones, 1e3·ones)`
   ∀ objetivo (ARD vetorial — casa com `params.json`); `train_*` = amostra DEDUPLICADA
   (guard chol, §4) — idêntica à amostra quando não há duplicata.
7. **(N.5-7)** `Optimizer_GA(@(x)-Infill_EIM(x,GP,ndf_scaled,criterion), D, lb, ub, 10D, 200)`
   — **`criterion` é VARIÁVEL** (L.9; via `Algorithm.ParameterSet('Euclidean')` — EIMe,
   default do driver; era literal hardcoded + var morta :51).
8. **(N.5-8)** `Population = [Population, Problem.Evaluation(infill_x)]` — lote=1/iter;
   pós-infill: **sync D89** `Problem.FE = bud.fe` + guarda (c) D60 (`stall` de iterações sem
   FE — SÓ LOGA `saldo_congelado`) + `c238_instrument(...)`.

**RNG:** nenhum `rng` interno no classdef — semeadura do harness (D59: `rng(semente)` DEPOIS
do Problem, ANTES do Solve). Consumidores: `lhsdesign` (init do GA interno), `randi`
(torneio/sinal SBX), `rand` (SBX/mutação); `fmincon` é determinístico.

## 3. Patches aplicados (por arquivo:linha, numeração STOCK)

**Em `algorithms/c238_EIM/` (repo do autor, content-hash — repos.lock re-hashado):**
- `EIM.m` — **NOVO** (o embrulho N.5, §2).
- `MultiObjective_EIM.m:40/:64` — **âncora `c238-hypervolume-rm`**: as 2 linhas
  `hypervolume = Hypervolume(non_dominated_front,ref_point);` viram o comentário-marcador
  `% [R1-c238] chamada ao mex Hypervolume removida (Windows-only — c238-hypervolume-rm)`
  (o mex `Hypervolume.mexw64` é Windows-only; crash em Mac/Linux logo após o init; era
  só log — fora do infill Euclidean). ⚠ o script fica com `fprintf` que referencia a
  variável `hypervolume` agora inexistente — IRRELEVANTE: o script NUNCA roda no harness
  (o entry é o classdef; o script começa com `clearvars`), registrado por honestidade.
- `Infill_EIM.m:1` — assinatura +`[y,u,s,n_nan]` (u/s já computados nas :7-11 — L.9;
  só-leitura) e **pós-:15**: guard **`EIM(isnan(EIM))=0`** [IMPL do cartão] (s=0 & f=u →
  0/0=NaN; NaN=0 = sem melhoria esperada) + `n_nan` p/ o evento `eim_nan`.
- `Optimizer_GA.m:1` — assinatura +`[pop_vari,pop_fitness]` (pop FINAL do GA — DEF-C2;
  só-leitura; seleção/operadores intactos).

**O que FICA stock (CÓDIGO — registrado no manifesto):** GA interno SBX+PM 10D×200
(**paper: DE/rand/1/bin 50×50 F=0,8 CR=0,8 ×4 — B10.2/D30, a ÚNICA divergência material**);
anti-clustering AUSENTE (B10.7 — telemetria `min_dist_infill` no jsonl, nada adicionado);
kriging próprio OK-Forrester (θ0=1, bounds [1e-3,1e3], MLE fmincon sqp em log10(θ),
single-start, MaxFunEvals=20D, nugget (10+n)·eps, X norm [0,1]); `GP_Predict.m:22` já é
`sqrt(max(mse,0))` STOCK; re-scaling min-max/iter (✓ paper); init/maxFE do repo (100/200)
sobrescritos pelo global-override 11D−1/31D−1.

**Integração [IMPL] (repo raiz):**
- `src/experiment.m` — `case 'c238' → run_c238` (corpo = cópia do run_b1/e7) +
  `ensure_paths_c238` + `rmpath_quiet`; manifesto com Balde B completo + divergências +
  `sigma_dict` (§5).
- `src/c238_instrument.m` (**novo**, molde do b1_instrument) — ver §5.
- `claude_code_context/artifacts/anchors.json` — âncora **`c238-hypervolume-rm`
  concretizada** (expect_after descritivo → literal do comentário-marcador; o literal NÃO
  contém "Hypervolume(" para o preflight patch-aware não ler STOCK); preflight **APLICADO**,
  exit 0. ⚠ **Sinalizada para seu veto** (precedente Sessão 1/c141/b3/b1/e7).
- `claude_code_context/artifacts/repos.lock` — `c238_eim.sha256_tree` re-hashado via
  `preflight.py --write` (o EIM.m novo + patches mudam o content-hash; precedente c141).

## 4. A varredura de sombra de path (obrigatória — lição R1-e7/S.8)

`find algorithms -name '<basename>'` para CADA arquivo chamado/patchado pelo c238:
- **`GP_Train.m` / `GP_Predict.m` / `Infill_EIM.m` / `Optimizer_GA.m` / `Paretoset.m` /
  `EIM.m` / `MultiObjective_EIM.m`: ÚNICOS** na árvore `algorithms/` — **nenhuma sombra
  PARA DENTRO do c238** ✔ (os asserts `which` do `ensure_paths_c238` ficam de rede).
- **SOMBRA REVERSA real (o achado desta varredura):** `c238_EIM/UniformPoint.m` (versão
  simplificada do Zhan, 38 linhas) e `c238_EIM/DTLZ2.m` (função, não classdef) **colidem
  com os do PlatEMO 4.15** (e com a árvore 4.1 do e74). Com a pasta prependada (o
  `ensure_paths_c238` E o próprio `ALGORITHM.Solve:81` prependam e nunca removem), um run
  posterior de b1/b3/e7/pisos **no MESMO processo MATLAB** resolveria `UniformPoint` para a
  cópia do Zhan — os adapters chamam `UniformPoint(100,M)` diretamente (run_b1:577/run_e7)
  → corrupção silenciosa. **Fix:** `run_c238` registra `onCleanup(@() rmpath_quiet(c238dir))`
  — a pasta SAI do path ao fim do run (normal OU erro). Durante o run do c238 nada chama
  `UniformPoint`/`DTLZ2` (GA usa `lhsdesign`; problema é UserProblem) — a precedência
  temporária é inócua.

## 5. Instrumentação produzida (para a SUA validação de fidelidade — D97)

Por run: **① real** (31D−1, init+opt) · **② pop** (membership da amostra por iteração —
cresce +1/iter; inclui o duplicado se houver cache-hit) · **③ surrogate (DEF-C2 — BO com
EA interno)**: a **pop FINAL do Optimizer_GA** por iteração de BO (10D linhas/iter; MMF1
800, DTLZ2 28.800, ZDT1 180.000 linhas), re-scorada por **1 chamada extra do Infill_EIM
pós-GA (0 FE — L.9)**: `pred_tipo=valor`, **`mu_j`/`sigma_j` POR OBJETIVO no ESPAÇO DO
MODELO** (y re-escalado min-max por iteração — paper; `sigma_j = sqrt(max(mse,0))` do
GP_Predict STOCK), `modelo=OK-Forrester`, `espaco_modelo=transformado`,
`transf_tipo=minmax`, **`transf_params={min,range}` da iteração** (C3, leitura D47 —
precedente e7: UMA linha por candidato; **cru = mu·range + min**, invertível; `range` é o
PÓS-guard `max(range,eps)`); `real_solution_id` nullable (double+NaN — fallback R1-00; o
infill escolhido e as cópias dele na pop final do GA apontam o id).

**`.jsonl` §17.5/S.7** (linha `c238_gen` por iteração): `criterion`, **`norm_min`/`norm_max`
(o min/max do y-scaling — C3) + `norm_range_efetivo`**, `n_amostra`/`n_treino`/`n_dedup`/
`n_front`, `theta_min/max/media` + `lnL` por objetivo, `ga_pop`/`ga_gens`, **`eim_best` +
`y_best`/`u_best`/`s_best` do escolhido + `infill_sid`**, `lote`(=1), `fe_iter`,
**`min_dist_infill`** (telemetria B10.7 — anti-clustering ausente), `stall_iters` (D60-c),
`n_eim_nan`, `n_range0`, tempos; eventos `guard`: **`eim_nan` (EIM NaN→0), `guard_range`
(max(range,eps)), `dedup_treino` (guard chol), `dup_infill` (cache-hit D89 — slot perdido),
`saldo_congelado` (stall≥2), `cache_hit`, `hard_stop`**. **Timing §17.6**: 1 retreino/iter
(`n_acumulado` = n_treino deduplicado, cresce +1/iter; `tempo_fit_s` = soma dos M GP_Train;
`tempo_busca_s` = GA).

**Números por problema (semente 0) — GUIA, não gate (D97):**

| Problema | FE | IGD_raw | \|ND\| | guards observados | âncora do paper (Anexo J) |
|---|---|---|---|---|---|
| MMF1 (d=2) | 61 | — (MMF: IGDX pós-hoc D99) | 16 | só cache_hit (probe) | — (paper não roda MMF) |
| DTLZ2 (d=12, M=3) | 371 | **2,3032e-1** (das-dennis 5151→esfera) | 57 | só cache_hit (probe) | DTLZ2 m=3 é **d=6**, HV mediano (Tab. I) |
| ZDT1 (d=30) | 929 | **4,9908e-2** (front analítico, 10k) | 27 | só cache_hit (probe) | ZDT1 m=2 **d=6**, 100 FEs: IGD médio EIMe=**0,0178** |

> ⚠ **CAVEATS para a sua leitura (D97 — eu não julgo):**
> 1. **d/orçamento divergem da âncora J** (paper roda TUDO em n=6, init 65=11n−1, maxFE
>    100/200; nós d=2/12/30, 31D−1) — a comparação justa é do **MECANISMO**: EIM-Euclidean
>    agregado por min sobre o front, kriging em y re-escalado, GA 10D×200 — tudo
>    reconstruível do `c238_gen` (norm_min/max, theta, eim_best, [y,u,s]) + ③ com
>    `transf_params`. Se quiser a âncora numérica do paper, um `ZDT1_d6` em
>    `FIDELITY_PROBLEMS` (precedente c217/DTLZ2_d15) fica como pendência — NÃO fiz (fora
>    do escopo do cartão).
> 2. **A pop final do GA converge**: muitas linhas da ③ por iteração são CÓPIAS bit-exatas
>    do best ((μ+λ) elitista) — no MMF1, 604/800 linhas apontam `real_solution_id` (o
>    infill avaliado). É o comportamento do código, não bug; a diversidade da ③ do c238 é
>    menor que a do b1 (GA geracional com truncamento).
> 3. **guards `eim_nan`/`guard_range`/`dedup_treino`/`dup_infill` NUNCA dispararam na
>    semente 0** (0 ocorrências nos 3 problemas; `n_range0=0` e `n_eim_nan=0` em 100% das
>    880 iterações) — só o `cache_hit` do probe (1×/run). O GA não propôs nenhuma duplicata
>    bit-exata aqui; se dispararem na bateria, são os hazards documentados (§8.5), não bug novo.
> 4. **IGD é raw** (fora do metrics.py; ref = analítico/das-dennis) — compute do SEU jeito da ① crua.
> 5. **Wall-clock semente 0**: MMF1 8 s · DTLZ2 686 s · **ZDT1 14.128 s (~3h55)** — o c238
>    é o **NOVO pior-caso de wall da R1** (§22.2: e7 4.343 s, b1 1.719 s, b3 1.570 s). O
>    grosso é o loop (163 min: fit mediana 11 s — fmincon 20D=600 avaliações da
>    verossimilhança com chol n×n POR OBJETIVO — + busca mediana 5,6 s — 60.000 GP_Predict
>    batched/iter); o resto (~70 min) é overhead de Population/③/hook. Extrapolação 30
>    sementes ZDT1 ≈ **118 h/core** — dimensionar no piloto (D84); candidato natural a
>    worker dedicado de longa duração no despacho da bateria.
> 6. Divergências código×paper mantidas (CÓDIGO): GA×DE (B10.2 — única material);
>    anti-clustering ausente (B10.7) — todas registradas no manifesto.

## 6. Como reproduzir

```
# MATLAB (raiz do repo):
addpath('src'); addpath(genpath('algorithms/_PlatEMO/PlatEMO'));
experiment('c238','MMF1',0,'main','data')     % idem ZDT1, DTLZ2
% ou pelo despachante (skip idempotente; c238 ja esta no roster default):
experiments('algorithms',{'c238'},'problems',{'MMF1','ZDT1','DTLZ2'},'seeds',0,'parallel',false)

# Python (env-main), gate objetivo:
python3 scripts/accept.py R1-c238 --alg c238 --problema MMF1 --semente 0   # idem ZDT1/DTLZ2
```
Outputs em `data/experiments/main/c238/` (gitignored — regeneráveis).

## 7. O que este fan-out acrescentou ao padrão (p/ e74, e103, pisos)

1. **O molde do embrulho N.5 está provado**: um repo standalone (não-PlatEMO) roda na infra
   do R1-00 com um classdef fino que chama as funções stock — precedente direto p/ o e103
   (que tem wrapper próprio N.3, worker dedicado, mas herda export/CP-init).
2. **Sombra REVERSA de path** é real e tem fix-padrão: prepend + asserts DURANTE o run +
   `onCleanup(rmpath)` ao sair (o `ALGORITHM.Solve:81` prependa a pasta do classdef e nunca
   remove — vale p/ QUALQUER algoritmo com pasta própria fora da árvore PlatEMO).
3. **Sync D89 pós-init E pós-infill** no classdef novo (não há linha nativa p/ ancorar — o
   embrulho já nasce com o sync).

## 8. Pendências / notas

1. **Commits `[R1-c238]`** (3): patches c238_EIM (EIM.m novo + 3 arquivos) · adapter
   run_c238 + c238_instrument + âncora + repos.lock · handoff + cartão. Nenhum blob.
2. **Para seu VETO (sinalizações desta sessão):** (a) âncora `c238-hypervolume-rm`
   concretizada com o comentário-marcador (§3); (b) ③ em ESPAÇO DO MODELO com
   `transf_params={min,range}` — leitura D47 da DEF-C3, precedente e7 (NÃO materializei as
   duas linhas por espaço); (c) `'N',100` inerte (N.5-2) — registrado, sem efeito no
   mecanismo; (d) a REMOÇÃO da pasta do path via onCleanup (§4 — mexe na ordem de resolução
   DEPOIS do run; e7/c141 mantêm suas pastas porque são sub-árvores do PlatEMO sem colisão
   reversa).
3. **Toolboxes em runtime**: Optimization (fmincon sqp) + Statistics (normcdf/normpdf/
   lhsdesign) — conforme o cartão; sem DL.
4. **A validação de fidelidade é SUA (D97)**: números da §5 são guia; o veredito (EIM
   euclidiano fiel? θ/lnL sadios? min-max coerente? convergência do GA aceitável?) é seu,
   lendo os `.jsonl` (`c238_gen`) + a ① + a ③ com `transf_params`.
5. **Hazards CONFIRMADOS pela revisão adversarial (documentados, NÃO consertados — D81):**
   (a) **crash latente STOCK** `Infill_EIM.m:25`: se o front não-dominado tiver **1 ponto**
   (uma amostra domina todas), `min(reshape(...,[1,num_x]))'` colapsa a ESCALAR e o
   `Optimizer_GA.m:17` estoura (`Index exceeds...`) — comportamento byte-idêntico ao repo
   oficial (confirmado empiricamente pelos verificadores), análogo ao crash latente do b3
   (UpdataArchive). NÃO disparou em MMF1/DTLZ2/ZDT1 semente 0; se disparar na bateria =
   **falha honesta** (rethrow → footer/manifesto failed) — conserto seria decisão de
   fidelidade SUA (D81). (b) **objetivo constante**: o guard `max(range,eps)` evita o NaN
   do scaling, mas a coluna vira 0 exato → `sigma2=0` → `lnL=+Inf` → o **fmincon aborta**
   ("undefined at initial point") → falha honesta; por isso o `guard_range` é emitido NA
   DETECÇÃO (antes do fit — fix desta sessão), nunca perdido. Probabilidade ~0 em objetivos
   contínuos. (c) **nota de infra (todos os 6+1 runners)**: erro nas etapas de export
   PÓS-Solve deixa o fid do `.jsonl` sem footer/fclose (o fluxo de erro DO Solve está
   correto) — padrão herdado idêntico a run_b1/run_e7; mudar é decisão de infra transversal
   SUA (candidato: `onCleanup(fclose)` como o pathGuard do c238).
