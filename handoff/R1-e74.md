# handoff/R1-e74.md — Rodada 1, fan-out: e74 CLMEA (árvore PlatEMO 4.1 PRÓPRIA — D95/N.0-4.1, worker de path dedicado)

**Data:** 2026-07-18 · **Env:** MATLAB **R2025a U1** (Mac arm64) + PONTE `pyenv` InProcess
(`~/ponte_teste/bin/python`; numpy 2.4.6 · pymoo 0.6.2 — round-trip → 6 verificado ANTES, D81;
Deep Learning Toolbox — newrbe/newpnn/ind2vec/vec2ind/sim; Statistics — pdist2). Leitor Python
independente no **env-main** (`.../mestrado_experimentos_dissertacao/bin/python`).

**Gate objetivo (encanamento, D97):** `accept.py R1-e74 --alg e74 --problema {MMF1,DTLZ2,ZDT1}
--semente 0` → **exit 0 (VERDE)** nos três: 4 saídas, **FE=31D−1 EXATO**, CP-init bit-a-bit. ✅
**Fidelidade:** instrumentada e entregue para a validação MANUAL do autor (D97) — **não é gate meu**.
**PROVA DE NÃO-CONTAMINAÇÃO DE PATH (o ponto nº 1 do cartão): ✅** — no MESMO processo MATLAB,
`c217 → e74 → nsga2 → b3` todos VERDES; após o e74 o `which` de ALGORITHM/UniformPoint/UserProblem
volta IMEDIATAMENTE à 4.15 e `which('CLMEA')`/`which('Hv_Select')` ficam VAZIOS (§4).
**Regressão:** R1-00 stub (accept_r1_00 7/7) + c217, c141, b1, b3, b4, e7, c238 + os 4 pisos
(todos ×MMF1) VERDES via accept.py; F0-01/02/03/04 exit 0; `preflight.py --write` exit 0
(e74-mask/e74-ndsort-obj = APLICADO; `e74_clmea` re-hashado); suíte **82 testes OK** (1 skip).
Gates do c262 NÃO rodados (faixa da sessão paralela).

**Escopo:** 9º cartão da R1 e **o mais delicado**: o e74 NÃO roda na `_PlatEMO` 4.15 — roda na sua
PRÓPRIA árvore PlatEMO **4.1 completa** (`algorithms/e74_CLMEA/CLMEA_Code/`, D95), com **518
basenames colidindo** com a 4.15 (medido por `comm` dos `find`), incluindo as CLASSES do núcleo
(ALGORITHM/PROBLEM/SOLUTION/UserProblem). Infra transversal INTOCADA. Eu NÃO julguei fidelidade (D97).

---

## 1. Resultado do gate (por problema, semente 0)

| Problema | D | M | maxFE | FE_final | EXATO | CP-init | ciclos | extremos | aceites s1/s2/s3 | cache_hits | wall | accept.py |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **MMF1** | 2 | 2 | 61 | **61** | ✅ | ✅ | 15 | 2/2 | 8/15 · 15/15 · 15/15 | 1 | **21 s** | **VERDE** |
| **DTLZ2** | 12 | 3 | 371 | **371** | ✅ | ✅ | 79 | 3/3 | 79/79 · 79/79 · 79/79 | 1 | **261 s** | **VERDE** |
| **ZDT1** | 30 | 2 | 929 | **929** | ✅ | ✅ | 211 | 2/2 | 176/211 · 211/211 · 211/211 | 1 | **485 s** | **VERDE** |

`cache_hits=1` = o probe do construtor absorvido (a receita N.0 do c217 vale INTACTA na 4.1:
`Initialization(1)` → `initFcn=DoE[0]` → o init em lote o reencontra como cache-hit D89).
**Contabilidade FE (L.8) fecha na ① exata:** MMF1 21+2+38=61 · DTLZ2 131+3+237=371 · ZDT1
329+2+598=929; `fe_index` contíguo, `solution_id` único. **Overshoot ZERO** (1 FE/bloco; os
extremos são ≤M; o hard-stop do FEBudget fica de rede — nunca disparou na semente 0).
**Wall p/ M7:** o e74 é LEVE — ZDT1 em ~8 min (vs **3h55 do c238**, o pior-caso da R1); o fit
`newrbe` no arquivo inteiro (s2) cresceu 56→851 ms/ciclo no ZDT1 (curva §17.6 gravada).

## 2. A estratégia de ISOLAMENTO da árvore 4.1

**Desenho (`ensure_paths_e74`, `src/experiment.m`):**
1. `prev = path` — captura INTEGRAL do estado do path do processo;
2. `rmpath(genpath(_PlatEMO))` — TODA pasta da 4.15 sai (warning-off; no-op se ausente);
3. `addpath(genpath(CLMEA_Code))` — a árvore 4.1 INTEIRA (o mesmo idioma do `platemo.m` 4.1,
   que faz `addpath(genpath(cd))` — processo dedicado por desenho);
4. **asserts `which -all`** sobre 15 símbolos-chave (ALGORITHM, PROBLEM, SOLUTION, UserProblem,
   CLMEA, OperatorDE, NDSort, UniformPoint, CalHV, SelectTrainData, Local_infill, Hv_Select,
   ClassifierSelect, Data_Process, DE): o 1º hit DEVE estar em `CLMEA_Code` **E zero** resoluções
   remanescentes em `_PlatEMO` — qualquer sombra ABORTA o run em vez de corromper em silêncio;
   o smoke completo vai no **header do `.jsonl`** (`which_smoke`);
5. `guard = onCleanup(@() path(prev))` — **restauração TOTAL** na saída do `run_e74` (normal OU
   erro): a 4.1 sai, a 4.15 (se estava) volta.

`src/` fica no path (zero colisão de basename com a CLMEA_Code — varredura 2026-07-18); a ponte
pyenv independe do path MATLAB. **Nota p/ a bateria:** o despacho roda o e74 em **processo MATLAB
dedicado** (o desenho canônico do cartão; `experiments.m:30` já anota). O `run_e74` torna até um
processo compartilhado seguro path-wise (provado na §4), mas o processo dedicado continua sendo a
regra do despacho — a rede de asserts é a garantia de que qualquer variação ABORTA, não corrompe.

## 3. Patches aplicados (por arquivo:linha, numeração STOCK)

**Em `algorithms/e74_CLMEA/CLMEA_Code/.../CLMEA/` (repo do autor; content-hash re-hashado):**
- `Local_infill.m:47` — **âncora `e74-mask` (🔴 D76 — ARTIGO)**: `x_offspring(Choose,:)` →
  **`x_offspring(index(Choose),:)`** (`Choose` indexa o SUBCONJUNTO `index` = front nível-1 dos
  offspring; o stock devolvia a linha errada). Literal do anchors.json já era exato.
- `ClassifierSelect.m:9` — **âncora `e74-ndsort-obj` (🔴 D30 — ARTIGO)**: ND-sort sobre DECISÃO →
  **`[y_label,~] = NDSort(y_obj_e74,inf);`** = NDSort sobre os **OBJETIVOS reais dos pais**;
  `y_obj_e74` = 3º output novo (read-only) do `Data_Process` com os objetivos alinhados
  linha-a-linha a `x_train` (o stock perdia o pareamento). O cap `>4→4` (:10) fica e passa a ser
  semanticamente ativo. ⚠ **CONCRETIZAÇÃO SINALIZADA P/ SEU VETO** (§8.2).
- `Data_Process.m:1` — assinatura +`y_obj_e74` (cópia pristina antes do Inf-out; decisão intacta).
- `Hv_Select.m:33/:41` — **D74**: CalHV interno **NORMALIZADO** — min-max do **front-1 do arquivo
  corrente** (`Ymin/Ymax` das :31-32 STOCK = ideal/nadir estimados, leitura do D69) + **ref-point
  1,1 por coordenada no espaço normalizado**; o `RefPoint=(Ymax−Ymin)·1.2` (escala CRUA) sai.
  `CalHV.m` **INTOCADO**. `range≤0` (front degenerado) NÃO consertado — guard `hv_range0` (0
  ocorrências na semente 0). + `hv_base` do arquivo p/ HV_gain (leitura). ⚠ leitura p/ veto (§8.3).
- `CLMEA.m:33-34` — **init injetado**: par `UniformPoint('Latin')`+re-escala+Evaluation(N) →
  `Arc = Problem.Evaluation(Problem.data.X0)` (X0 NATIVO 11D−1; par substituído JUNTO — princípio
  D94; desvio DELIBERADO do paper 100/200, registrado) + assert do hazard L.8 (`maxFE>|X0|`).
- `CLMEA.m:56/:66/:76` — **mins L.8/D20**: as 3 chamadas passam **`min(N,length(Arc))`**
  (≡ `min(100,|Arc|)` p/ D<100; N stock preservado) e a :76 também **`min(k_local,length(Arc))`**.
- `CLMEA.m` (pós-init/extremos/blocos) — **sync D89** `Problem.FE = Problem.data.bud.fe` ×5 +
  `e74_instrument` pós-decisão + **guarda D60-c** (ciclo sem FE ×≥2 → `saldo_congelado`, SÓ LOGA)
  + instrumentação do bootstrap (tempos, aceites, `dist_ext`, `f_ext` — leitura).
- Instrumentação read-only nas 3 estratégias (assinaturas +`inst_e74`; ZERO decisão alterada):
  ClassifierSelect (count, frac-nível-1, pop final+classes PNN, dist_dec, flag_copia, telemetria
  `n_desalinhado`), Hv_Select (pop final da DE, μ_RBF, pseudo-front+scores D74, Ymin/Ymax/
  hv_base), Local_infill (pop final, ŷ, Eucli, front-mask) + tic/toc §17.6 (fit×busca separados).

**O que FICA stock (registrado no manifesto):** `CR=1.0` no OperatorDE do `Hv_Select:14`
({1,0.5,1,20} vs {0.5,0.5,1,20} das demais — não-uniforme, CÓDIGO); `DataProcess` declara nome ≠
arquivo `Data_Process.m` (resolução por nome de arquivo — funciona); os `break` :62/:72/:82 são
CÓDIGO MORTO (o `NotTerminated` LANÇA `PlatEMO:Termination`, nunca devolve false — término
mid-ciclo via exceção engolida pelo Solve); loop do rank-learning sem guarda além de `count>50`;
dedup ε=1e-5 stock (rejeita SEM FE — slot perdido, logado); spreads PNN/RBF e DE do bootstrap
stock; **fix opcional do desalinhamento máscara×Parent NÃO aplicado** (D81 — só telemetria).

**Integração [IMPL] (repo raiz):**
- `src/experiment.m` — `case 'e74' → run_e74` (molde run_c238 SEM `once`: ponte **POR INDIVÍDUO**
  `e74_eval_one` [dec,obj,con] 1×D sob o FEBudget — N.0-4.1#1; SEM `'N'` no UserProblem — o CLMEA
  usa o N interno próprio; `CLMEA` sem `'parameter'` = defaults ≡ Balde B) + `ensure_paths_e74`.
- `src/e74_instrument.m` (**novo**) — ③/jsonl/timing por bloco (§5).
- `anchors.json` — `e74-ndsort-obj` **concretizada** (descritiva → literal; preflight APLICADO).
- `repos.lock` — `e74_clmea.sha256_tree` re-hashado via `preflight.py --write` (precedente c141/c238).

## 4. Prova de não-contaminação de path (obrigatória do cartão)

No **MESMO processo MATLAB** (com a 4.15 pré-carregada no path — o pior caso realista):
```
[1] c217  ok  9,5s  fe=61 cp=1   (4.15)
[2] e74   ok 14,0s  fe=61 cp=1   (4.1 — durante o Solve, path SÓ com a CLMEA_Code)
[3] nsga2 ok  0,5s  fe=61 cp=1   (4.15 de volta)
[4] b3    ok  4,7s  fe=61 cp=1   (4.15; usa UniformPoint = a classe de colisão)
```
Após CADA estágio, `which` de ALGORITHM/UniformPoint/UserProblem → **sempre a 4.15** fora do e74;
pós-sequência, `which('CLMEA')` e `which('Hv_Select')` **VAZIOS** (nenhum resíduo da 4.1) —
asserts do script, não inspeção visual. Direção inversa (e74 chamado com a 4.15 carregada):
os asserts `which -all` do `ensure_paths_e74` provam **zero resolução na 4.15 durante o run**
(smoke gravado no header de cada `.jsonl`). Os 12 gates accept.py pós-sequência seguem verdes.

## 5. Instrumentação produzida (para a SUA validação de fidelidade — D97)

Por run: **① real** (31D−1) · **② pop** (membership do ARQUIVO por chamada do NotTerminated —
~4 bumps/ciclo: topo + 1 por bloco; a cadência REAL do algoritmo) · **③ surrogate (híbrido
C1/B16.4)**: por ciclo, a **população final interna de CADA estratégia** + os extremos do boot:
s1 = pop do rank-learning (`pred_tipo=classe`, `pred_classe=nivel_k` do PNN, `sigma_0=dist_dec`);
s2 = pop final da DE global (`pred_tipo=valor`, `mu=μ_RBF-global`, `sigma_0=HV_gain` = score
CalHV D74 − hv_base [só pseudo-front], `pred_score=score` absoluto); s3 = pop final da busca
local (`mu=ŷ-RBF-local`, `sigma_0=Eucli` — dist mínima em OBJETIVOS); boot = `mu_i` da rede do
extremo i. Estratégia no `modelo_flag` (`PNN(s1)`/`RBF-global(s2)`/`RBF-local(s3)`/
`RBF-global(boot)`); `real_solution_id` nullable (double+NaN — fallback R1-00). **Dicionário
completo no `sigma_dict` do manifesto.** Volumes ③: MMF1 1.950 · DTLZ2 23.703 · ZDT1 63.302
linhas (schema §17.2 conferido coluna a coluna por pyarrow; ver §8.4/8.5 p/ as 2 leituras da C1).

**`.jsonl` §17.5/S.7:** linha **`e74_gen` por BLOCO** (estratégia, fe, |Arc|, n_cand, aceito,
slot_perdido, spr, n_treino, tempos; s1: count/frac_nivel1/n_desalinhado/flag_copia/cand_classe/
cand_dist_dec; s2: Ymin/Ymax/range0/hv_base/n_front/score{min,med,max}/score_best/hv_gain; s3:
n_front/cand_eucli) + `e74_boot`. Guardas: `dedup_slot_perdido`, `cand_vazio`, `hv_range0`,
`saldo_congelado` (D60-c), `extremo_rejeitado`, `cache_hit`/`hard_stop` (FEBudget).
**Timing §17.6:** 1 linha por RETREINO (boot + 3/ciclo; `n_acumulado` do s2 = |Arc| — a curva
O(n³) do newrbe; fit×busca separados). MMF1 46 · DTLZ2 238 · ZDT1 634 retreinos.

**Números por problema (semente 0) — GUIA, não gate (D97):**

| Problema | FE | IGD_raw | \|ND\| | frac_n1 med (s1) | count med | n_desalinhado med | hv_gain>0 | guards |
|---|---|---|---|---|---|---|---|---|
| MMF1 (d=2) | 61 | — (MMF: IGDX pós-hoc D99) | 14 | 0,53 | 51 | 7/23 | 15/15 | 7× cand_vazio |
| DTLZ2 (d=12, M=3) | 371 | **1,1770e-1** (das-dennis 5151→esfera) | 83 | 0,91 | 20 | 8/100 | 76/79 | nenhum |
| ZDT1 (d=30) | 929 | **9,6400e-2** (front analítico 10k) | 45 | 0,91 | 7 | 9/100 | 211/211 | 35× dedup_slot_perdido |

> ⚠ **CAVEATS para a sua leitura (D97 — eu não julgo):**
> 1. **d/orçamento divergem da âncora J** (paper: d∈{30,50,100,200} com init 100/200+M e 300 FEs,
>    20 reps; nós: 31D−1 com init 11D−1). A âncora J mais próxima é ZDT2 d=30 (9,9381e-3) e
>    DTLZ2 d=30 2-obj (9,7035e-2) — nosso DTLZ2 é d=12 **3-obj** e o init é ~3× menor. A
>    comparação justa é do **MECANISMO**: rank-learning pressiona nível-1 (frac_n1 0,53→0,91,
>    count caindo 51→7 — a pressão do paper aparece), HV-gain consistentemente >0, extremos
>    aceitos ∈[0,M] como método. Tudo reconstruível do `e74_gen` + ③.
> 2. **`cand_vazio` (MMF1, 7/15 ciclos)**: o rank-learning terminou SEM offspring nível-1 → s1
>    não propôs candidato. Edge do STOCK (D=2: PNN grosseiro), instrumentado, não consertado.
> 3. **`dedup_slot_perdido` (ZDT1, 35×, todos do s1)**: o candidato do rank-learning caiu a <ε
>    de um ponto do arquivo — exatamente o "no-op silencioso" que a SPEC previu (v2.2, bug novo
>    da máscara). A telemetria `n_desalinhado` (med 7–9%/ciclo) mede o desalinhamento
>    máscara×Parent; o fix re-sim é OPCIONAL e NÃO foi aplicado (decisão sua se quiser).
> 4. **IGD é raw** (fora do metrics.py; refs analíticas) — compute do SEU jeito da ① crua.
> 5. **Wall-clock semente 0:** MMF1 21 s · DTLZ2 261 s · ZDT1 **485 s** — o e74 NÃO é gargalo
>    (c238/ZDT1 = 14.128 s segue o pior-caso da R1). Extrapolação 30 sementes ZDT1 ≈ 4 h/core.
> 6. **f_ext do bootstrap pode ser NEGATIVO** (ex. MMF1: −1,54 no obj 2) — é a predição do
>    newrbe extrapolando; o extremo REAL avaliado está na ① (fantasia×verdade, material C3).
> 7. Divergências código×paper mantidas (CÓDIGO): CR=1.0 na s2; quotas/spreads/DE do código
>    (paper omisso) — registradas no manifesto.

## 6. Como reproduzir

```
# MATLAB (raiz do repo) — NAO precisa (nem deve) montar o path da 4.1 a mao:
addpath('src'); addpath(genpath('algorithms/_PlatEMO/PlatEMO'));   % 4.15 pode estar no path
experiment('e74','MMF1',0,'main','data')     % o run_e74 troca o path sozinho e RESTAURA ao fim
# idem DTLZ2, ZDT1

# Python (env-main), gate objetivo:
python3 scripts/accept.py R1-e74 --alg e74 --problema MMF1 --semente 0   # idem DTLZ2/ZDT1
```
Outputs em `data/experiments/main/e74/` (gitignored — regeneráveis).

## 7. O que este fan-out acrescentou ao padrão (p/ o e103 — o OUTRO worker dedicado)

1. **O molde do worker de path dedicado está provado ponta-a-ponta**: captura-integral →
   rmpath da outra árvore → addpath da própria → asserts `which -all` (aborta, não corrompe) →
   smoke no `.jsonl` → `onCleanup(path(prev))`. O e103 (N.3: `cd`+`addpath(genpath(cd))` que
   NUNCA remove) herda o desenho direto — inclusive a restauração do CWD, que lá também muda.
2. **Ponte POR INDIVÍDUO validada** (UserProblem 4.1 sem `once`): FEBudget por chamada preserva
   D89/D61/probe-absorvido SEM mudança na infra transversal. O `addCause` do CallFcn 4.1
   preserva o identifier `PlatEMO:Termination` (hard-stop mid-lote propaga limpo).
3. **Sync D89 como REDE mesmo sem duplicatas possíveis**: o ε-dedup do e74 garante
   obj.FE≡bud.fe por construção; o sync fica barato e uniforme com o padrão dos 8 anteriores.

## 8. Pendências / sinalizações PARA SEU VETO

1. **Commits `[R1-e74]`** (3): patches CLMEA_Code · adapter+instrument+âncora+repos.lock ·
   handoff+relatório. Nenhum blob. SEM INDEX (a torre marca).
2. **(a) Concretização da âncora `e74-ndsort-obj`** — a âncora descritiva dizia "NDSort sobre
   objetivos PREDITOS"; concretizei como **NDSort sobre os objetivos REAIS dos pais** (via
   `y_obj_e74`), porque (i) em pontos do ARQUIVO "predito ≡ real" (newrbe interpola exato; e
   não há regressor DENTRO do ClassifierSelect p/ "prever"); (ii) mantém o idioma NDSort que
   §22/K.5.2 prescrevem ("NDSort → objetivos"); (iii) a alternativa (re-sim do PNN) podia
   deixar o nível-1 VAZIO (crash novo em `randi(0,…)`) e o stock RE-computa por ND-sort, não
   por PNN. Nota registrada no próprio anchors.json.
3. **(b) Leitura do D74** — "min-max sobre o arquivo corrente" = **Ymin/Ymax do front-1** (as
   :31-32 STOCK, ideal/nadir do D69), fixo por chamada (comparabilidade entre candidatos j);
   não min-max de TODO o arquivo (máximo de ponto dominado não é nadir).
4. **(c) ③ do híbrido com pred_tipo POR LINHA** (`classe` p/ s1, `valor` p/ s2/s3/boot) e a
   estratégia no `modelo_flag` — o mock da §17.2 mostra UMA linha "hibrido" com as duas
   cabeças, mas nenhuma linha do e74 REAL tem as duas ao mesmo tempo; fabricar μ p/ a pop do
   s1 exigiria chamada de modelo que o mecanismo não faz. `pred_tipo=hibrido` NÃO é emitido.
5. **(d) `sigma_0` = pseudo-σ POR ESTRATÉGIA** (dist_dec | HV_gain | Eucli) — as "3 colunas de
   pseudo-σ" da I.8 não cabem no schema `sigma_0..sigma_{M-1}` p/ M=2; semântica por estratégia
   no `sigma_dict` (DEF-C4); o score absoluto do s2 foi p/ `pred_score`.
6. **(e) ② na cadência do NotTerminated** (~4 bumps/ciclo — topo + 1 por bloco): é a cadência
   REAL do CLMEA.m stock; cada bump re-grava o membership do arquivo (ZDT1: 521k linhas de ②,
   ints — comprimem bem). Alternativa (1 bump/ciclo) exigiria suprimir hooks do stock.
7. **A validação de fidelidade é SUA (D97)**: números da §5 são guia; o veredito (pressão de
   nível-1 fiel? HV_gain sadio? efeito do CR=1.0? desalinhamento tolerável sem o fix opcional?)
   é seu, lendo os `.jsonl` (`e74_gen`/`e74_boot`) + ① + ③ com o `sigma_dict`.
