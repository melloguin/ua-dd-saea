## c154-J27 — VEREDITO: **REFUTADO**

**Resumo em 3 linhas**
A ③-online e o `acqf_todos_restarts` **são perfeitamente alinháveis** por uma permutação determinística, trivial e legível no código: *restarts em ordem, com o vencedor removido e re-anexado no fim* (`linha j → restart j` se `j<best`, `j+1` se `j≥best`, `linha R−1 → best`). Reconstruí a "paisagem da aquisição" inteira que o achado deu por perdida — **76.535 linhas × (x, μ, σ, α, restart)**, exatamente o total de ③-online que o próprio relatório contou. Causa do falso-positivo: **o analista testou apenas a permutação-identidade e parou na observação parcial "o vencedor é a última linha"; nunca testou a permutação delete-and-append — que o relatório-irmão `c262.md` §2.14 já havia enunciado, provado e classificado como (2) DI-10.**

---

**O que tentei para derrubar (os 4 passos)**

**1 · DECISÃO.** Nenhuma D-xx/DI-xx enuncia a ordem, e o `sigma_dict` é mudo (confirmo o resíduo doc, abaixo). Mas **existe precedente vinculante e o analista não o conhecia**: `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/relatorios_config/c262.md` §2.14 — *"🔑 QUERY-JOIA 3: a ③ É o conjunto dos 10 restarts, reordenado"* — declara textualmente que *"a leitura ingênua — 'linha j da ③ ↔ restart j' — é falsa"*, formula a hipótese **σ = [i ≠ argmax] + [argmax]**, prova-a em **7.111 pares** de 21 células (violações: **σ 3,90% · identidade 42,99% · aleatória 64,66%**) e a classifica **(2) DI-10 · direta**. Mesmo protocolo F5.3b, mesmo escritor, aspecto idêntico — resolvido como (2) num config e escalado como (3) 🎯 no outro.

**2 · QUERY (reproduzi tudo com código independente).** As três medidas do analista sobrevivem (bloco = R = 5D em **1.673/1.673**; `acqf_escolhido == max(finitos)` bit-a-bit em **1.673/1.673**; marcada = última em **1.662/1.662**) — mas a *conclusão* não. Dois testes ortogonais:

*Teste A (exato — α é função determinística de x, logo linhas bit-idênticas exigem α bit-idêntico):* **206 pares** de linhas com `x+μ+σ` bit-idênticos em float32.

| rtol | **H1 (perm. real)** | H0 (ingênua) | permutação aleatória |
|---|---|---|---|
| 1e-13 | 73,30% | 33,98% | 2,91% |
| 1e-10 | 99,51% | 49,03% | 7,77% |
| **1e-8 … 1e-3** | **100,00%** | **50,00%** | **7,77%** |

Mediana de \|Δα\|/α sob H1 = **0,0 exato**; máximo **1,74e-9** (ruído de convergência do L-BFGS-B em float64). Sob H0: mediana **1,9e-3**, máximo **0,64**. **A conclusão é independente da tolerância** — H1 domina H0 em toda a varredura 1e-13→1e-3, e 100% por célula (DTLZ1 112 pares, MMF11_L 61, ZDT4 17, MMF4 14, MMF1 2).

*Teste B (livre de tolerância — α é suave em x): Spearman(‖Δx‖, \|Δα\|) sobre os C(R,2) pares por iteração,* mediana por célula: **H1 +0,433 · H0 +0,082 · nulo −0,000**, com **H1 > H0 em 11/11 células** (de +0,101 no BBOB_F5 a +0,835 no BBOB_F49).

*Os três contra-exemplos da "Evidência 3" caem individualmente* (`contraexemplos_do_analista.py`). MMF1 **it 4**, `best=0`: linhas 3, 6 e 8 têm x bit-idêntico (2,4210882; 0,5214041); sob H1 → restarts 4, 7, 9 com α = 1,1615414202491865 / …490899 / …491383 (12 dígitos iguais); sob H0 → restarts 3, 6, 8 com α = 1,16154 / **0,67536** / **0,99985** — três valores incompatíveis para o mesmo ponto: **H0 é aritmeticamente impossível.** MMF1 **it 1** é ainda mais fino: a linha 4 → restart 5 tem x = (1,9714297; 0,7457699), *quase* igual às cinco cópias de (1,9714286; 0,7457691), e o α correspondente diverge no 11º dígito (1,7137715166772676 vs …167290875) — **a magnitude da perturbação em x prevê a magnitude da perturbação em α**. MMF1 it 2: linhas 8/9 ↔ restarts 9/1, α 1,1950536316533575 / …753.

**3 · CÓDIGO.** O comportamento é **por construção e comentado**: `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/src/c154_jes.py`
- **:738-744** `for k in range(U_cand.shape[0]): if k == best: continue  # o escolhido entra após o FE` → grava os R−1 perdedores **na ordem dos restarts**;
- **:800-804** grava o vencedor **depois** do FE, já com `real_solution_id=sid`;
- **:686-691** ramo hard-stop: mesmo rito, vencedor por último sem `sid` (explica 1.673 blocos × 1.662 marcados).

Não é anômalo: é declarado no próprio código-fonte.

**4 · CONTROLE.** O padrão é do **escritor compartilhado da família BoTorch-Python**, não do c154: `src/c262_qnehvi.py:661` é a linha gêmea. Rodei a bateria idêntica no c262 (**5.201 iterações, 15 células, D=2…30**): **ρ_B mediano H1 +0,408 × H0 +0,078 × nulo +0,003**; teste A sobre **5.809 pares**: H1 **87,26%** × H0 53,83% × nulo 40,60% (o resíduo é o colapso float32 do ZDT1 D=30 que o `c262.md` já quantificou e explicou). **Se o "defeito" aparece em toda a família, é convenção — não é achado.**

---

**Por que caiu**
O achado afirma duas coisas: (i) "as duas séries não se alinham" e (ii) "**não** consigo reconstruir a paisagem (x, μ, σ, α) por restart". **(ii) é factualmente falso** e (i) só vale para a permutação-identidade. Entreguei a reconstrução: `c154_paisagem_reconstruida.csv`, **76.535 linhas / 1.673 iterações / 1.673 vencedores**, `restart` único por (problema, it) em 100%, com asserts de que `a[best] == acqf_escolhido` e `len(bloco) == R` fecham em 1.673/1.673. Bônus já colhido: ρ(α, σ̄) mediano por célula vai de **−0,679 (BBOB_F37)** a **+0,508 (ZDT4)** e ρ(α, μ̄) de **−0,733** a **+0,504** — a inversão de sinal entre problemas é um resultado sobre o regime exploração×explotação do JES que o relatório declarou indisponível. Checagem lateral: os **8 α não-finitos do BBOB_F49** mapeiam, sob H1, para as linhas 14/0/13/44/48/23/2/1 dos seus blocos, todas com μ/σ finitos — confirma DI-11.1 (o NaN vem do estimador LB, não de um candidato NaN) sem nenhuma contradição.

**Enquadramento e bússola D29**
Não há bug: nada a levar ao ARTIGO nem ao surrogate. O aspecto é **(2) — instrumentação nossa sob DI-10**, exatamente como o par c262. Se for preciso pintar: 🟠 **laranja (detalhe de implementação → segue o CÓDIGO)** — a ordem de escrita é escolha do nosso writer, o código é a fonte e ele está correto.

**Resíduo legítimo, rebaixado a (d) DEFEITO DE DOC — para a torre central.** O `sigma_dict` é leitura **obrigatória** por contrato (`CONTRATO_DE_DADOS.md` §10/R4 regra 3, DEF-C4) e **não declara a ordem do bloco `online`** — o §3 declara o invariante posicional só para `regime='sonda'`. Quem seguir o contrato à risca não é avisado. É o mesmo espírito do item 5 do §0 do `RELATORIO_F5` (doc-sync), e agora tem duas ocorrências (c154 e c262) ⇒ vira **regra de leitura da família**, não nota de rodapé.

---

**Impacto**
- **Nenhuma célula sai da análise. Nenhum número publicado muda** — IGD+/HV, ΔWAPE −21,8%, cobertura 0,950 nos infills, ranks, 51,24 h-core: intactos. O achado era sobre auditabilidade, e a auditabilidade está intacta.
- `c154.md` §2.1 linha **J27: (3) 🎯 → (2) DI-10**. §3 passa a **(1) 10/27 = 37,0% · (2) 17/27 = 63,0% · (3) 0/27 = 0,0%**.
- §7: some o item **(i)** dos três que impediam o 10 → **score 8,5 → 9,0** e **"ACEITAR + CAVEAT" → "ACEITAR"** (restam D75/1.383 disparos e a assimetria §20, ambos sancionados). Itens à F5.4: **1 → 0**.
- §9: cai o teto **T(x)** ("a semântica per-restart da ③ enquanto o escritor não for lido" — o escritor foi lido). A armadilha 5 deve ser **reescrita**: não "não parear", e sim "parear com a permutação delete-and-append".
- §8 passa a **"nenhum aspecto classe (3) no c154"**, alinhando com o placar dos 5 pilotos F5.3a ("ZERO classe (3)").
- **Ganho novo**: o teto da query-joia da família BoTorch está levantado. As análises `CONTRATO §9` "Contrafactual sem incerteza / Kendall-τ | ③ busca" tornam-se computáveis para **c154 e c262** com o pool de restarts inteiro (76.535 + ~5.2e5 linhas), não só para o ponto escolhido.

**Ação p/ F5.7 + custo**
1. **[recomendada, opção (a)]** Regra de leitura, texto apenas, **0 h-core, 0 re-run**: nova regra em `CONTRATO_DE_DADOS.md` §10/R4 — *"③ `regime='online'` dos configs BoTorch-Python (c154, c262): os candidatos vêm na ORDEM dos restarts com o vencedor removido e re-anexado no fim; `linha j → restart j (j<best) | j+1 (j≥best)`, `linha R−1 → best`, `best = argmax isfinite(acqf_todos_restarts)`"* + a mesma frase no `_sigma_dict()` de `src/c154_jes.py:211` e no gêmeo do c262 (afeta só runs futuros; os manifestos da rodada-42 estão congelados e **não precisam** de retrofit, porque a informação é 100% recuperável). Publicar `restart_de_linha(R, best)` como utilitário de leitura compartilhado. **Custo: ~30 min de texto + 6 linhas de código.**
2. **[rejeitar]** Opção (b) do relatório ("o escritor deve gravar o α por linha") — mexeria no schema da ③ (`CONTRATO §3`) e exigiria re-run dos 25 problemas × 2 configs por **zero** informação nova. **Não fazer.**
3. **[nota, custo 0 agora]** A regra **não se estende a q>1**: no lote (`src/c154_jes.py:720-736` + `:810-830`) o bloco fica `[perdedores dos passos 2..q][perdedores do passo 1][vencedor do passo 1][vencedores dos passos 2..q]` e `acqf_todos_restarts` cobre **só o passo 1**. Irrelevante hoje (batch/c154 fora do roster — DI-40), mas tem de entrar na regra se o batch voltar.

**Artefatos** (todos em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/baterias/f54/c154-J27/`): `refuta_j27.py` (1ª tentativa, teste de partição — registrada como *frágil*: float32 em x × rtol em α não formam partições comparáveis em D=10) · `refuta_j27_b.py` (testes A e B, roda `c154` e `c262`) · `sensibilidade_tolerancia.py` (varredura 1e-13→1e-3) · `reconstroi_paisagem.py` (a reconstrução) · `contraexemplos_do_analista.py` (os 3 contra-exemplos resolvidos) · CSVs: `c154_paisagem_reconstruida.csv` (76.535×23, 19 MB), `c154_paisagem_diag.csv`, `j27_pares_duplicados.csv` (206), `j27_testeAB_c154.csv`, `j27_testeAB_c262.csv`, `j27_por_celula.csv`, `j27_por_iteracao.csv`, `j27_iters_c154.csv`, `j27_iters_c262.csv`. Zero escrita fora desta pasta; `resultados_experimentos/` e `src/` intocados.