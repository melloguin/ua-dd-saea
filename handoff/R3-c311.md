# R3-c311 — HANDOFF (o QUÊ)

> Cartão **R3-c311 · TGPR-MO** (treed-GP escalável, autor · Python/DESDEO/GPy, OFFLINE, Mac).
> **FASE A COMPLETA** — runner + testes + pilotos + provas, faixa própria intocando a do b5.
> Fase B (wiring dispatch/accept/locks) aguarda o commit do b5 + o comando do autor.

## Entregáveis desta sessão (arquivos MEUS)
| Arquivo | O quê |
|---|---|
| `src/c311_tgprmo.py` (novo, ~470 linhas) | O runner OFFLINE. Molde `run_stubr3` REUSADO; mecanismo (treed-GP) por **VENDOR INTOCADO + 4 ganchos em runtime** (padrão e81). |
| `tests/test_c311.py` (novo, 14 testes) | Puros (env-main) + ganchos (env_c311) + runs completos (C311_SLOW). |
| `handoff/R3-c311*.md` (3) | Este + RELATÓRIO-EXECUÇÃO + REPASSE-A-TORRE. |
| `data/experiments/off/c311/**` | Os 3 pilotos completos (7 camadas + jsonl + manifesto cada). |

**NÃO tocado** (Fase B/torre): `experiment.py` · `scripts/accept.py` · `claude_code_context/**`
(anchors/envs/seeds/SPEC) · `requirements/**` · `cards/INDEX.md` · REGISTRO/PROGRESSO ·
`algorithms/c311_TGPR-MO/**` (vendored intacto, lacre `e3b3f802dad9`). Zero interseção com a
faixa do b5 (que rodou em paralelo a sessão inteira).

## O mecanismo (fluxo real, §22.4·3.3 / I.17 / L.17)
OFFLINE puro: o **dataset É o orçamento** (D90; `FEBudget` nasce esgotado, ① = as 31D−1). Duas
fases sobre UM surrogate treed-GP:
1. **CONSTRUÇÃO** (`treedGP_build`): árvore MSE (`min_samples_leaf`=10D, `max_depth`=100) por
   objetivo + laço RVEA `Imax=N/(10D)`(float→ceil) × `Gmax=50`; a cada iteração a folha de MAIOR
   impureza sem GP ganha 1 GP local (GPy Matérn 5/2 ARD, `optimize('bfgs')` único, sem restarts);
   `_refresh_population` (+1 ger/iteração); early-stop `delta=total_pts−seq[it−3]`, `it>5`.
2. **FINAL** (`treedGP_final`): RVEA `10×100=1000` gerações sobre o surrogate FIXO → ND final.
O ND final é avaliado 1× na verdade (⑦, fora do orçamento, DI-08/DI-13.9 — todos avaliados, ND
filtrado DEPOIS).

## Os 4 ganchos (monkeypatch em runtime; vendor bit-a-bit)
1. **σ das folhas** (B15.5, extensão nossa): expõe `σ=sqrt(var_GPy)` que o `treeGP.predict`
   descartava; μ BYTE-idêntico ao stock; NaN nas folhas só-árvore; σ NUNCA σ² (DI-16.9). A seleção
   `selection_type="mean"` (APD lê só `pop.fitness`) o ignora ⇒ não-perturbação por construção.
2. **predict_batch** (DI-16.13): vetoriza a predição da sonda (20.000 pts) por folha.
3. **snapshot + contador C311-11** (DI-16.19): ③ por geração com contador ÚNICO e monotônico
   atravessando as 2 fases (a fase se lê no `modelo_flag`, não colide em 1..50).
4. **lhs-determinismo** (env-fix): semeia o `lhs` da pop inicial do RVEA (o pyDOE do env é NOVO e
   não usa o `np.random` global) — ver REPASSE (a torre DEVE ratificar/pinar).

## As 7 camadas (comportamento REAL, verificado nos pilotos)
① dataset (D90) · ② **VAZIA por construção** (DI-16.17: pop RVEA = candidatos gerados ≠ dataset;
`real_solution_id`=NULL; declarado no `sigma_dict`) · ③ busca (μ/σ, `espaco_modelo='cru'`) +
**2 blocos de SONDA** `treedGP_build`+`treedGP_final`, 20.000 cada, **`geracao=NULL`**, ordem por
posição (DI-16.12) · ④ **1 linha por RETREINO** de construção (`fit_series` MULTI-linha, C311-09;
`tempo_geracao_s` EXCLUI a sonda, DI-13.10) · ⑤ manifesto + `sigma_dict` DEF-C4 completo · ⑥ jsonl
(header, `c311_build` por iteração com `n_folhas`/`profundidade`/`dict_gps`, 2 eventos `sonda`,
footer) · ⑦ `__final` (todos avaliados, ND pós-real, reconstituível da ③).

## Pilotos (exp='off', semente 0) — DETERMINÍSTICOS, todos VERDES
| problema | status | n_geracoes | n_final | ND-real | wall |
|---|---|---|---|---|---|
| MMF1  | ok | 1204 | 46 | 10 | 29 s |
| DTLZ2 | ok | 1204 | 84 | 77 | 70 s |
| ZDT1  | ok | 1204 | 50 | 50 | 49 s |

`n_geracoes=1204` = build (4×51=204) + final (1000). Gates: **auditar VERDE ×3 · final_eval
--check VERDE ×3 · determinismo ✅ · não-perturbação §3.1 ✅** (comandos e saídas no REPASSE).

## Pendências p/ a torre (detalhe no REPASSE)
1. **uso_id do c311** não catalogado no `seeds.json` — apliquei a receita L.17 (uso_id=0 default).
2. **pyDOE sem pin** no env_c311 (drift) ⇒ gancho lhs-determinismo — pinar OU ratificar.
3. **Granularidade da ④** (1 linha por retreino de construção; fase final no agregado).
4. Fidelidade (D97) = manual do autor, a posteriori — não me auto-aprovo.
