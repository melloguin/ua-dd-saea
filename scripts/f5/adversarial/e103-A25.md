## e103-A25 — VEREDITO: **REFUTADO como classe (3)** → rebaixado a **CONFIRMADO (d)** (defeito de instrumentação, escopo = espelho de leitura da F5 + bucket; **não** é desvio de fidelidade, **não** é célula corrompida)

**Resumo em 3 linhas**
O fato bruto reproduz (a ⑦ que a F5 leu tem 200 linhas), mas as três pernas do achado caem: (i) **existe decisão** — a **DI-41** (2026‑07‑28) diagnosticou este bug exato, classificou 🔴 e o **corrigiu no código** (`scripts/final_eval.py:121‑132`, commit `eb977f2`); (ii) a **árvore CANÔNICA** `ua-dd-saea/data/experiments/` tem **100 linhas em 45/45** — o arquivo de 200 linhas existe **só no espelho** `resultados_experimentos/`, montado de `_old/_bucket_raw`; (iii) o **impacto afirmado é aritmeticamente falso**: a duplicação é exata 2×, logo ND(200)=2·ND(100)=10 e a fantasia é **invariante em 0,05** — a leitura "deveria ser 0,10" está errada.
A única métrica que a duplicata realmente destrói é **`spacing`** (0,000000 contra 1,9583742395) — exatamente o que a DI‑41 previu e o que o relatório e103.md não viu.

---

**O que tentei para derrubar (os 4 passos, com os números)**

**1 · DECISÃO — derrubou.** `REGISTRO_DECISOES_IMPLEMENTACAO.md:1905`, **DI‑41 item 1**, textual: *"🔴 ⑦ do e103 com o DOBRO de linhas (bug de dado REAL, achado da rodada). […] O `final_eval` pegava as 200 linhas da última geração e avaliava cada ponto 2× na função real, gravando ⑦ com 200 linhas para 100 membros. Efeito grave e silencioso: `n_final` inflado e **`spacing` ZERADO** […] Fix: filtrar por UM `modelo_flag` (o 1º da ③), nunca por X […] ⑦ do smoke regenerada: 200→100 linhas, **21 ND**, portão VERDE"*. O achado A25 **não é um desvio novo nem inexplicado** — é o resíduo de um bug já sancionado, diagnosticado e fechado. Corroboração numérica da identidade: o `_bucket_raw` guarda **exatamente 5** ⑦ do e103 com 200 linhas (as "5 regeneradas" do PLACAR da PARTE A29) e uma delas é `exp_off_e103_MMF1_0__final.parquet` com **n=200, nd=42** → deduplicada dá **100 linhas, 21 ND**, o número literal citado na DI‑41.

**2 · QUERY — não derrubou o fato, derrubou o impacto.** Recenso independente das 45 células (`01_censo_camada7.py`, ND por força bruta própria, sem tolerância): cardinalidade 200 em **1/45** (`swap_medium-lhs_ZDT4`), 100 nas outras 44 ✓; `dupX_7 = 106` ✓; `nd_log ≡ nd_recomputado` em **45/45**; link posicional bit‑a‑bit contra o **subconjunto Kriging** em 44/45 e literal (200 linhas) só no alvo ✓. Ordem real da ③ é **intercalada** (`Kri,RBF,Kri,RBF…`), não "bloco K + bloco R" como o relatório descreve — detalhe irrelevante ao veredito. **O que quebra o impacto**: linhas pares ≡ ímpares bit a bit ⇒ ND(200)=2·ND(100) por construção (verificado: 10 = 2×5) ⇒ **fantasia 200 = 0,050000, fantasia 100 = 0,050000, Δ = 0,000e+00**. As 5 métricas oficiais via `src/metrics.py`: igd Δ=0, igd_plus Δ=0 (53,9489904908 nos dois), hv Δ=0, gd Δ=1,42e‑14, **spacing 0,000000 vs 1,9583742395**.

**3 · CÓDIGO — derrubou.** `scripts/final_eval.py:121‑132` já contém o filtro DI‑41 (`if len(distintos) > 1: sel = sel & (flags == distintos[0])`), e `:193` explica a persistência do artefato (`if os.path.exists(out_path) and not force: return skip`). Rodando `check_final` de hoje sobre a árvore canônica: **VERDE** no alvo — *"100 finais, 5 ND pós‑real, f reproduz problems.py, X reconstituível da ③ ger 99"* — e a reconstrução pelo caminho oficial bate com o arquivo canônico com **max|ΔX| = 0,0 e max|Δf| = 0,0**, `nd_pos_real` idêntico. O código está certo; o dado canônico está certo.

**4 · CONTROLE — separa perfeitamente.** `03/04`: **360 arquivos** do e103 (45 células × 8) e **245 ⑦** de 6 configs offline, espelho × canônico por md5/inode. Divergências: **2** — o `__final.parquet` (10 601 B / 200 linhas vs 9 383 B / 100 linhas) e seu sidecar (`n_final: 200` vs `100`) da célula‑alvo. **44/45 compartilham o inode** (nlink=3: `data/` + `_bucket_raw` + espelho); o alvo tem inodes distintos (espelho **32539937**, nlink 2, 21:42:51.516; canônico **32539959**, nlink 1, 21:42:52.741 — **1,2 s depois**). O gêmeo do inode obsoleto é `resultados_experimentos/_old/_bucket_raw/experiments/sweep-medium-lhs/e103/…__final.parquet`. Mecanismo fechado: a torre gerou **"os 88 arquivos novos"** (F5.1 §4.1) = **44 × 2** — a 45ª foi **pulada** por já existir (sem `--force`), então só as 44 foram propagadas ao bucket; o espelho herdou do bucket a versão pré‑fix.

---

**Por que caiu**
A causa do falso‑positivo é dupla e nomeável: **(a) leitura de espelho desatualizado** — a F5.3b leu `resultados_experimentos/`, que para 2 arquivos de 1 célula está defasado da árvore de produção; **(b) decisão desconhecida pelo analista** — a DI‑41 já havia nomeado, classificado e corrigido o mesmo defeito 15 minutos antes da passada da torre. E há um terceiro erro, do próprio relatório: a §8 prova‑2 conclui *"inflando o denominador da fantasia: 10/200 = 0,05 em vez de 10/100 = 0,10"* — ela esquece que o **numerador também dobra**. O número publicado (0,05) já era o correto.

**Enquadramento e bússola D29**
Enquadramento **(d) — defeito de instrumentação/distribuição**, não (a)/(b)/(c). O mecanismo do IBEA‑MS está intacto (①–⑥ da célula íntegras: ③ com 19 800 linhas = 99×200, censo de 107 registros, contrato limpo); nenhuma célula sai da análise. A **bússola D29 não arbitra**: a ⑦ é **🟢 extensão nossa declarada** (B7.5/DI‑08 — o artigo não tem essa camada), governada pelo `CONTRATO_DE_DADOS.md §7` e pela DI‑41, não pelo artigo nem pelo código do autor. O bug de código já foi 🔴‑classificado e fechado pela DI‑41; o que resta é **encanamento de propagação**, não fidelidade.

**Impacto**
- **Nada a excluir.** 45/45 células e103 permanecem na análise.
- **Números da dissertação que mudam: dois, ambos de contagem.** `endpoint_e103.csv` linha 31 (`swap_medium-lhs_ZDT4`): `n_final` **200 → 100** e `nd_pos_real` **10 → 5**; `sweep_e103.csv` (linha ZDT4/medium/lhs): `nd_pos_real` **10 → 5**.
- **Números que NÃO mudam** (medidos, não estimados): `fantasia` **0,05** (invariante, Δ=0), `igd_final` **53,94899049075502**, `hv_final` 0,0, `ganho_igd` **−0,4827267919185301**, `bate_dataset` False. Logo: §6.3 (rank 1,68/5, 17/25 — só células `off`), §6.4, §6.6 e a mediana de fantasia 0,31 do A24 ficam **intocadas**.
- **Correções obrigatórias no `f5/relatorios_config/e103.md`**: A25 (§2.1 e §2.2), §5 (linha "DEFEITO"), §7 item 5, §8 (prova 2 e "Impacto"), §9 armadilha **(27)** — todos afirmam ou implicam a leitura 0,10, que é falsa. Consequência secundária: o desconto de ~0,5 ponto no SCORE (9,3) foi aplicado a um defeito que não é do config nem do endpoint publicado.
- **Risco latente real** (o que a DI‑41 acertou e a F5 não viu): se a análise R4/D97 computar as 5 métricas oficiais a partir do espelho ou do bucket, o **`spacing`** dessa célula sai **0,0** em vez de 1,958 — erro de 100%, silencioso, numa das 5 métricas do estudo.

**Ação p/ F5.7 + custo**
1. **Ressincronizar 2 arquivos** (parquet + sidecar) do canônico → espelho → bucket para `sweep-medium-lhs/e103/ZDT4/42`. O `--force` do `final_eval` **não é necessário** (o canônico já é bit‑a‑bit o que o código de hoje produz). **Custo: < 1 min.**
2. **Rito de fechamento**: trocar "propagar os arquivos novos" por **propagação com preflight de content‑hash** (o item "preflight content-hash" já está no lote de refinamento pré‑M8 da DI‑42) — e/ou rodar o driver da ⑦ com `--force` incondicional. Isso mata a classe inteira: 4 dos 5 arquivos obsoletos do `_bucket_raw` são de semente 0 (`main/off · MMF1, DTLZ2, ZDT1`, todos 200 linhas) e entrariam no M8 pela mesma porta. **Custo: 1 h de código + re‑upload.**
3. **Varredura de integridade do espelho** antes de qualquer número final: md5 espelho × canônico nos **4 498 arquivos / 5,8 GB** (fiz 605: 360 do e103 + 245 ⑦ de 6 configs, 2 divergências). **Custo: ~10 min de I/O**, script trivial a partir de `03_espelho_vs_canonico.py`.
4. **Item para a torre central** (substitui o "item novo nº 9" proposto pelo relatório, que pedia um fix já existente): *"o `final_eval` pula ⑦ pré‑existente sem `--force`; toda correção de driver exige repropagação por hash, não por 'arquivos novos'"*.

**Evidências e scripts** (todos READ‑ONLY sobre os dados; nada escrito fora desta pasta): `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/baterias/f54/e103-A25/` — `01_censo_camada7.py`, `02_gate_e_metricas.py`, `03_espelho_vs_canonico.py`, `04_raio_de_alcance.py`, `05_fechamento.py`, `LEIA-ME.md` + CSVs `censo_camada7_45celulas.csv`, `impacto_metrico_camada7.csv`, `gate_check_final_hoje.csv`, `espelho_vs_canonico.csv`, `raio_camada7_todos_configs.csv`, `censo_bucket_raw_camada7.csv`.