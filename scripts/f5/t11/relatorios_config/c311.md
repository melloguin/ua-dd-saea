# RELATÓRIO DE FIDELIDADE T11 — c311 (TGPR-MO, Mazumdar et al. 2023 · Python/DESDEO/sklearn/GPy) · rodada T11 · comparação contra a F5.3a-v1.1

**VEREDITO EM UMA LINHA: SCORE 9,5/10 · ACEITAR · MANTEVE vs a F5 — com a composição do score TROCADA:** dois itens do teto T caíram (a **fórmula do early-stop foi LIDA no fonte e RECONSTRUÍDA do dado a 91,6%**, contra 5,4% na F5; o **determinismo bit-a-bit foi PROVADO** por um re-run ①②③⑦ byte-idêntico), e em troca apareceu **1 aspecto classe (3)** novo, criado pela própria T11 (`tempo_aval_real_s` no offline). O **mecanismo não mudou**: `git diff` sobre `algorithms/c311_TGPR-MO/` entre o commit da s42 e o HEAD é **vazio** — o vendor está intocado.

**⚠ ERRATA CONTRA O MEU BRIEFING.** A tarefa afirma "*smoke T11: NÃO PRESERVADO (rodou em tempdir removido) ⇒ as correções só podem ser verificadas NO CÓDIGO*", e o `LEIA-ME.md` da pasta de evidência diz "*Os smokes PYTHON não estão aqui*". **Estão.** Existe `/Users/gmello/Documents/python_repos/mestrado/evidencia_T11/smoke_python/` (criada hoje, 2026-07-31 10:45–11:41, POSTERIOR ao LEIA-ME), com 11 células Python — entre elas **`off/c311/exp_off_c311_BBOB_F17_42`**, célula PÓS-T11, semente 42, o **mesmo par (problema, semente)** que existe na s42. Isso transformou a análise: a correção da T11 neste config é verificável por **DADO MEDIDO**, não só por código lido. O `LEIA-ME.md` e a §15.2 do handoff devem ser corrigidos.

---

## 1. Ficha do mecanismo (condensada)

Mazumdar, López-Ibáñez, Chugh, Hakanen & Miettinen (2023), *Evolutionary Computation* 31(4):375–399 — **TGPR-MO**, surrogate treed-GP para MOPs **offline** (zero FE novas). Laço (Alg. 1 + §3.1/§3.3): K árvores CART-MSE (uma/objetivo, `N_min`, profundidade "não controlada") → construção iterativa em que, a cada rajada de `G_max`=50 gerações do RVEA, **um** GPR local (GPy Matérn 5/2 ARD) é instalado na folha visitada de **máxima perda H(Q)** → parada por `I_max = N/N_min` **ou** por "*todas as soluções caem em folhas com GPR*" → otimização final de **1.000 gerações sobre o surrogate congelado** → a variância do GPR é produzida e **nunca consumida** (§5: trabalho futuro).

**Divergências e extensões SANCIONADAS** (cadeia D29, bundle `alg_c311_tgprmo.md`, SPEC §434):
| marca | item | fonte |
|---|---|---|
| 🟠 impl→código | `max_depth = 100` (paper: "not controlled") | v2.2 do bundle |
| 🟠 nuance B15.8 | early-stop `delta = total_points − seq[counter−3]`, `counter>5` | código oficial |
| 🟠 bundle | `I_max` com float→**ceil** | `continue_evolution` |
| 🟠 DI-28c | `_refresh_population` ⇒ **51** gerações/iteração, não 50 | DESDEO |
| 🟢 **EXTENSÃO nossa** | **σ = `sqrt(var_GPy)`, NaN nas folhas sem GP** | B15.5/D30 + DI-16.9 · **SPEC §434 lista o c311 como o exemplo canônico de 🟢** |
| 🟢 obrigatória | `predict_batch(X)` NOVO p/ a sonda | C311-12/DI-16.13 |
| desenho | contador de `geracao` ÚNICO atravessando as 2 fases | C311-11/DI-16.19 |
| desenho | ② vazia · `real_solution_id` NULL · ⑦ = endpoint | DI-16.17 · DI-08 |
| desenho | tiers small=31D−1 (D90) · **big=50k, override exclusivo** (D38) · eixo LHS×MVNS (D51) | — |

**Corpus.** **s42 = 55 células** em `resultados_experimentos/c311/{label}/42/` — **54 válidas** + **1 REPROVADA-F5.1** (`swap_big-mvns_MMF16_20`, `gpy_bfgs_linalg`). Volume re-medido: **7.368.513 linhas ③** · **84.804 gerações** · **604 decisões** · 486.674 linhas ① · 3.260 linhas ⑦ · **0 linhas ②** · 0,90 h-core de wall (Σ 3.254 s), **máquina `mac` em 54/54** (`tempo_f52d.csv`) — o piso de ruído O-18 não se aplica às comparações internas. **Smoke T11 = 1 célula** (`off/BBOB_F17/42`, 8,55 s, HEAD `9ad0138`, env py3.8 / numpy 1.20.2 / sklearn 1.1.2 / GPy 1.9.9).

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.0 Tabela-resumo (25 aspectos + 3 no teto T)

| # | aspecto | classe | verificab. | resultado-síntese (54 células s42 · 604 decisões · 7,37M linhas ③ · +1 smoke T11) |
|---|---|---|---|---|
| A01 | orçamento = o DATASET, zero FE, tiers, `fe_treino_max` | (2) D90/D38/D51 | direta | `fe_final==maxfe==len(①)` **54/54**; `fase='init'` 100%; `fe_index` denso **54/54**; N exato por tier **54/54**; `fe_treino_max` = N−1 único **54/54** |
| A02 | proveniência do dataset | **(1)** +eco D63/D87/D90 | direta | ΔX e ΔF **bit-exatos em float32 54/54**; `x_hash`/`f_hash` ≡ sidecar **54/54**; `doe_hash ≡ cp_init.x_hash` 54/54; **49 artefatos distintos** |
| A03 | ledger: 1 fit/retreino · contador único · aritmética | (2) C311-09/-11/DI-16.19/DI-28c | direta | \|④\|=I_eff=\|`fit_series`\| **54/54**; `geracao`=51·i **54/54**; `n_ger = 51·I_eff+1000` **54/54**; contador **denso 1..n_ger 54/54**; ledger do ⑥ fecha **54/54** |
| A04 | sonda offline = 2 blocos × 20.000 | (2) DI-16.12/§17.2.2 | direta | 2 eventos na ordem build→final, 20.000 cada, **40.000 linhas** com `geracao` NULL: **54/54** |
| A05 | join POSICIONAL sonda × gabarito | (2) DI-16.13/§10.5 | direta | max\|ΔX\| = **1,907e-6** (pior caso, WFG bound 44) — ≤1 ULP-float32 da amplitude, **54/54** |
| A06 | WAPE/cobertura + **`n_sigma_valido` (I-11)** | (2) §5 + T11 parcial | direta | cobertura mediana **0,9512** (q1 0,9122 / q3 1,0000) sobre σ finito; **`n_sigma_valido` mediana 16.351/20.000, mínimo 100**, 8 pares <1.000, 59 pares = 20.000. **A coluna continua ausente do CSV oficial** |
| A07 | invariante dupla do timing | (2) DI-13.10 | direta | **0 violações** com tolerância ULP-relativa em 604 linhas ④; igualdade EXATA em **598/604**; sonda>0 em **exatamente 1 linha/célula** e a violação-com-sonda ocorre ali, 54/54 |
| A08 | hazard GPy · **B1/DI-42.1** · `motivos_parada.json` | (2) + **correção T11** | direta | **0 guards de LinAlgError em 54/54**; 2 `hard_error` = falha de upload; a 55ª célula tem a assinatura B1 completa (⑤ `ok` + `motivo_parada=gpy_bfgs_linalg`; ⑥ com footer `failed` do runner e `ok` do despachante) — **corrigida no ⑤ pela DI-42.1, residual no ⑥** |
| A09 | ② vazia, `real_solution_id` NULL | (2) DI-16.17 | direta | ② = 0 linhas 54/54; `real_solution_id` não-nulo em **0 de 7.368.513** linhas |
| A10 | ⑦ = endpoint DI-08 + erro de fantasia | (2) DI-08/DI-13.9/B7.5 | direta | ND recomputado ≡ `nd_pos_real` **54/54**; link (ger,linha)→③ **Δmax = 0,0** 54/54; `origem_solution_id` NULL 54/54; fantasia mediana **0,607** (0,020–1,000) |
| A11 | fase final: 1.000 ger sobre modelo CONGELADO | **(1)** §3.3/§4.1.5 | direta | 1.000 gerações exatas 54/54; **2 blocos de sonda BIT-IDÊNTICOS em 54/54** (0 diferenças em 2,16M×(D+2M) valores, NaN-aware) |
| A12 | σ POR-REGIÃO (NaN = folha sem GP) | (2) 🟢 B15.5/D30+DI-16.9 | direta | **σ 100% NaN nas gerações 1–50 em 54/54**; σ-NaN na sonda: mediana **0,000 (small) · 0,069 (medium) · 0,855 (big)** |
| A13 | curva de cobertura (monotonia/passo/teto) | **(1)** Alg. 1/§3.1 | direta | monotonia **54/54**; passo de `n_gps` ≤1 **54/54**; `n_gps ≤ n_folhas` 54/54; `n_folhas ≤ ⌊N/N_min⌋` **125/125**; cobertura final mediana 0,835/0,697/**0,097** |
| A14 | banda [N_min, 2N_min−1] + nós puros | **(1)** c/ nuance (2) §3.3 | direta (**reconstruída**) | **960/963 incrementos na banda (99,69%)**; as 3 exceções (328 · 835 · 2.295) batem folhas grandes da árvore reconstruída; **0 folhas abaixo de N_min em 125/125** |
| A15 | K árvores MSE, uma/objetivo, N_min=10D | **(1)** Alg. 1 l.1/§4.1.3 | direta (**reconstruída**) | `n_folhas` reconstruído ≡ logado em **125/125** |
| A16 | `max_depth = 100` (paper: "not controlled") | (2) 🟠 código/D29 | direta (**reconstruída**) | profundidade reconstruída ≡ logada **125/125**; máximo absoluto **26** — o teto **nunca foi encostado** |
| A17 | `I_max = ⌈N/(10D)⌉` (float→ceil) | (2) bundle | direta | I_eff ≤ I_max **54/54**; small **35/35 com I_eff = I_max = 4** (⌈3,05⌉=4, ceil provado) |
| A18 | `G_max=50` + `_refresh_population` ⇒ **51**; âncora 51i−1 | (2) DI-28c | direta (**agora também NO FONTE**) | 604/604 decisões em 51·i; a âncora 51i−1 do addGPs **lida em `BaseEA.py:66` + `c311_tgprmo.py:618`**, além dos 99,5%×46,0% da F5 |
| A19 | **early-stop: a FÓRMULA** 🎯→resolvido | (2) 🟠 B15.8/D29 | direta (**fórmula LIDA + RECONSTRUÍDA**) | `delta(k) = c(k)+c(k−1)`: **338/369 iterações elegíveis exatas (91,6%)**, 235/235 sentinelas, **573/604 = 94,9%**, **52/54 células a 100%**. Custo da divergência **quantificado**: 3.162 de 30.804 gerações (10,3%) |
| A20 | escolha da folha = argmax impureza entre visitadas sem GP | **(1)** Alg. 1 l.13–16 | direta (**reconstruída**) | `total_points_per_model` reconstruído ≡ logado em **1.488/1.495 (99,5%)**, 52/54 células a 100% |
| A21 | predição: μ do GP na folha com GP, média da folha fora | **(1)** §3.3 | direta | 63/66 pares a 100% onde σ é NaN × **4,6e-5** onde σ existe (contraste discriminativo, 2,5M predições — F5, não refeito) |
| A22 | RVEA: lattice 50 (M=2)/105 (M=3), pode encolher; α=2 | **(1)** §4.1.3 | direta (+eco de α) | teto **nunca excedido em 54/54**: máximo **50** (M=2) e **105** (M=3, atingido em 8 células); colapso a ≤10 nas gerações 1–50 |
| A23 | instrumentação nossa: σ export · `predict_batch` · telemetria DI-10 | (2) B15.5/C311-12/DI-10 | direta | `_predict_batch` lido (`c311_tgprmo.py:199-219`): μ do stock + σ da MESMA chamada GPy; speedup 33,4× mediano (F5); telemetria completa em 604/604 |
| A24 | **determinismo bit-a-bit + proveniência nova (T11)** | (2) D62/DI-28.3 + B-03/I-09 | direta (**T caiu**) | **①②③⑦ byte-idênticas** entre s42 (26/07) e smoke T11 (31/07, HEAD 9ad0138, tempdir): sha256 `07530706…`/`d0c59b0c…`/`f38c82a7…`/`021c23b2…`; `campanha_id` e `repo_hash` presentes no smoke, ausentes em 55/55 da s42 |
| A25 | **`tempo_aval_real_s` no offline** | **(3)** 🎯 | direta (**smoke**) | s42: `0.0` literal em 54/54. Pós-T11: **0,0003 s** — e é o laço de INJEÇÃO do dataset, não avaliação. Nos 5 offline Python: 61→0,0001 · 309→0,0003 (×2 configs) · 50.000→0,0572 ⇒ **~1,1 µs/linha, invariante ao problema** |
| **T1** | kernel/otimização do GP local (Matérn52 ARD, bfgs único, jitchol) | **T** | não-verif. | declarado 54/54; μ/σ não fingerprintam kernel sem reconstruir o GP sob py3.8 |
| **T2** | `selection_type='mean'` / σ nunca consumido na busca | **T** | não-verif. | contrafactual logicamente inverificável ex-post |
| **T3** | **checkpoint DI-43** (25 iterações OU 30 min) | **T** | não-verif. | ligado no c311 (`c311_tgprmo.py:497-500,648`); **não exercido** no smoke (I_eff=4, 8,55 s) nem observável na s42 (pré-T11) |

---

### 2.1 A01 — o orçamento É o dataset (e o `fe_treino_max` que nunca se move)

**(a) O artigo.** Abstract/§1: "*no new data is available during the optimization process*". O dataset (N, n, K) é a ENTRADA da Alg. 1; a função verdadeira só aparece como métrica (§4.1.6). N do paper ∈ {2.000, 10.000, 50.000}.

**(b) A SPEC.** **D90** redefine, no offline, que a ① **É** o dataset (`maxfe = fe_final = N`, 100% `fase='init'`). **D38** concede ao c311 — e só a ele — o tier **big = 50.000** (os demais offline dão OOM). **D51** abre o eixo **LHS × MVNS**, replicando §4.1.2 do paper. O tier **small = 31D−1** é a régua transversal do projeto, e é ela que nos põe **fora do envelope de projeto do paper** — o caveat científico central deste config. `fe_treino_max` = N−1 constante por desenho (DI-13.15).

**(c) Observado (t02_universal.csv, 54 células).** `fe_final == maxfe == len(①)` **54/54**; `fase=='init'` em 100% das 486.674 linhas; `fe_index` denso 0-based **54/54**; N exato por tier **54/54** (small 31D−1 de 61 a 929; medium 2.000 ×10; big 50.000 ×9); `③.fe_treino_max` com **valor único = N−1 em 54/54**, idêntico nas linhas de busca e nos dois blocos de sonda.

**(d) Mecanismo.** `load_offline_budget` (`standalone_harness.py:617-660`) injeta as N linhas e o `FEBudget` **nasce esgotado**; o `offline_guard` converte qualquer FE-na-busca em `OfflineBudgetViolation`. O treino nunca muda: nem a árvore (construída uma vez sobre os N pontos) nem os GPs locais (subconjunto FIXO da folha) consomem dado novo — por isso `fe_treino_max` é inútil como indicador de aprendizado aqui, e `total_points_per_model` é o indicador certo.

**(e) Veredito: (2) — desvio sancionado (D90 + D38 + D51 + DI-13.15); direta.**

### 2.2 A02 — proveniência: bit-exata, com 49 artefatos distintos

**(a) O artigo.** §4.1.2: datasets pré-coletados por LHS ou MVNS; a proveniência não é instrumentada.

**(b) A SPEC.** D63/D87/D90: dataset de artefato versionado com sidecar de hash; o ⑤ grava `cp_init_offline.{x_hash,f_hash}`; é a garantia de que os 5 algoritmos offline comeram a MESMA amostra (pré-requisito da F5.5).

**(c) Observado (t03_u2_u7.csv).** Carreguei os **49 artefatos distintos** correspondentes às 54 células: `(①[X].astype(f32) == artefato[X].astype(f32)).all()` **True em 54/54**, idem para F. `cp_init_offline.x_hash ≡ sidecar.x_hash` e `f_hash ≡ f_hash` em **54/54**; `doe_hash ≡ cp_init.x_hash` 54/54. **Auto-errata da F5:** a primeira passagem desta bateria deu 49/54 porque eu apontei as 5 células `swap_small-mvns_*` para o artefato sem sufixo — o artefato certo é `ds_{prob}_42_small_mvns.parquet`. Corrigido o caminho, fecha 54/54. Isto **é** a prova operacional de que o sweep leu a variante certa.

**(d) Mecanismo.** Sem re-escala nem roundtrip: a ① é cópia do artefato float32 (D53), logo o desvio é **exatamente zero**, não 1 ULP como nos configs online.

**(e) Veredito: (1) — conforme o artigo, com marca declarativa da proveniência; direta.**

### 2.3 A03 — o ledger fecha em três contadores independentes

**(a) O artigo.** Alg. 1 constrói um GPR/árvore por iteração; não define numeração global de gerações (as duas fases podem ser MOEAs distintos).

**(b) A SPEC.** **C311-09** (④ = 1 linha por iteração de CONSTRUÇÃO), **C311-11/DI-16.19** (contador de `geracao` único, monotônico, 1-based, atravessando as 2 fases, discriminadas só no `modelo_flag` — sem isso as ③ colidiriam em 1..50), **DI-28c** (+1 geração/iteração do `_refresh_population` ⇒ passo 51).

**(c) Observado.** `|④| == I_eff == |fit_series|` em **54/54** (Σ = 604 linhas); `④.geracao == 51·i` em **54/54**; `n_geracoes == 51·I_eff + 1000` em **54/54** (de 1.204 a 6.049); `set(③.geracao) == {1..n_ger}` — **denso, sem furos, 54/54**, totalizando **84.804 gerações**; as fases separam exatamente em 51·I_eff em **54/54**; e o ledger de eventos `1 + I_eff + 2 + n_footer + n_guard + n_retry == n_ev` fecha em **54/54**.

**(d) Mecanismo.** Três escritas independentes (o `fit_series` do ⑤, a linha do ④, o evento `decision` do ⑥) coincidem em 604/604 porque nascem da mesma volta do `while` de `run_treed_GP`. A ausência de ④ na fase final é a afirmação de congelamento vista pelo lado do custo.

**(e) Veredito: (2) — desenho sancionado; direta.**

### 2.4 A04/A05 — a sonda offline e o join posicional

**(a) O artigo.** Nada — instrumentação exclusivamente nossa.

**(b) A SPEC.** §17.2.2 + DI-16.12: no offline o modelo só muda em dois momentos observáveis, então a régua é **2 blocos de 20.000 pontos Sobol** (`treedGP_build` e `treedGP_final`), `geracao` NULL. DI-16.13/§10.5: a ③ **não tem `sonda_id`**; o casamento é **por POSIÇÃO** dentro do bloco, e o offline lê o gabarito INTEIRO (20.000) conferindo `x_hash` (não `x_hash_online`).

**(c) Observado.** **54/54**: exatamente 2 eventos `rec='sonda'` na ordem correta, `n_pontos=20.000`, **40.000 linhas** `regime='sonda'` com `geracao` 100% NULL. Join posicional: `max|ΔX|` global = **1,907e-6**, atingido nos WFG (bounds até [0,44]) — ≤1 ULP-float32 × amplitude; nos problemas com bounds [0,1] o resíduo cai a 2,98e-8. Se houvesse permutação, o resíduo saltaria à ordem da própria amplitude.

**(d) Mecanismo.** Gabarito float64 × ③ float32 sem arredondamento (D53): erro puro de representação, escalado linearmente pelo bound.

**(e) Veredito: (2) — instrumentação sancionada; direta.**

### 2.5 A06 — WAPE, cobertura, e a coluna que a T11 prometeu e não produziu

**(a) O artigo.** §4.1.6 mede **RMSE multivariado** e **HV** (ref 2√K). Não há WAPE nem calibração de σ (§5: a variância não é avaliada).

**(b) A SPEC.** §5 do protocolo congela WAPE por objetivo no espaço CRU e cobertura = fração com `|μ−f| ≤ 1,96σ`, **com a regra "onde σ é NULL por desenho → N/A"**. A **F5 recomendou** acrescentar a coluna `n_sigma_valido` porque `sonda_f52e.csv` reporta `n_validas=20.000`/`n_nan=0` em 250/250 linhas do c311 — números que descrevem o **μ**, não o σ.

**(c) Observado (t05_sonda_regua.csv, 125 pares).** `espaco_modelo='cru'` e `transf_tipo` NULL em 54/54 — nada a des-transformar. WAPE/corr reproduzem `sonda_f52e.csv` (ZDT1 0,000/0,027 corr 1,000/0,942 · DTLZ2 0,054/0,068/0,015 · WFG4 0,797/0,642 corr **−0,442**/0,403 · WFG2 corr **−0,601** · BBOB_F55 WAPE 0,058/0,055 com corr **≈0,09**). Cobertura mediana **0,9512** (nominal 0,95), q1 0,9122, q3 1,0000. **E a coluna que faltava, agora produzida:** `n_sigma_valido` mediana **16.351** de 20.000; **mínimo 100** (`swap_big-lhs_WFG9` obj1, cobertura 1,0000 — a F5 citou 105, que é o obj0); **8 pares abaixo de 1.000**; **59 pares em 20.000 cheios**. Por tier: 20.000 (small) · 18.621 (medium) · **2.906 (big)**.

**(d) Mecanismo.** A cobertura é bem definida por ponto, mas o **denominador varia de 20.000 a 100** conforme a fração do volume sob GP. A leitura correta é a tríade (cobertura, n_σ-válido, σ-NaN-share).

**(e) Veredito: (2) — definição congelada aplicada com a regra N/A; direta. A resposta da T11 a este item é PARCIAL — ver §4.**

### 2.6 A07 — a invariante dupla do timing e o ULP que finge ser violação

**(a) O artigo.** Nada. **(b) A SPEC.** DI-13.10: `tempo_geracao_s` mede o ciclo do ALGORITMO e **não inclui** a sonda ⇒ `fit+busca ≤ tempo_geracao_s` sempre, e `fit+busca+sonda` deve violar **exatamente** nas linhas com bloco de sonda.

**(c) Observado (t03).** Nas 604 linhas de ④: **0 violações com tolerância ULP-relativa float32**; a relação é **igualdade EXATA** (|Δ|≤1e-12) em **598/604** — no c311 a "geração" do ④ é a iteração inteira, cujo tempo é exatamente fit+busca. Com a tolerância FIXA de 1e-6 do protocolo aparece **1 falso-positivo**: `off/ZDT3`, Δ = **1,907e-6 s sobre 18,0 s** = 1 ULP-float32. `tempo_pred_sonda_s > 0` em **exatamente 1 linha por célula** (a última, 51·I_eff) e a violação-com-sonda ocorre ali em 54/54. Confirmado no smoke T11: linha 4 do ④, `0,000245 + 0,952410 = 0,952655` = `tempo_geracao_s` ao último dígito, com `tempo_pred_sonda_s = 0,24731`.

**(d) Mecanismo.** O cronômetro da sonda é parado fora do relógio da iteração — é precisamente o teste da DI-13.10. **Recomendação operacional (repetida da F5):** a tolerância fixa 1e-6 deve virar ULP-relativa acima de ~8 s.

**(e) Veredito: (2) — instrumentação sancionada, invariante fechada; direta.**

### 2.7 A08 — o hazard do GPy, o bug B1 e a correção DI-42.1

**(a) O artigo.** Nada: o paper roda `m.optimize('bfgs')` sem proteção.

**(b) A SPEC.** O bundle registra como **hazard** que o `optimize('bfgs')` do vendor está **sem `try`** (um `LinAlgError` do `jitchol` derruba a run) e prescreve `try` + dedup do dataset. A F5 acrescentou o achado do harness: **B1 — `status` mascarado** (`experiments.py`).

**(c) Observado — e este é o aspecto onde a T11 agiu.** Nas 54 válidas: `cache_hits=0` ≡ footer **54/54**; **zero guards de LinAlgError**; 2 eventos `hard_error`, ambos `ModuleNotFoundError: No module named 'google'` em `gcs.py:103` (falha de UPLOAD, não do run). A 55ª célula, `swap_big-mvns_MMF16_20`, tem a assinatura B1 **completa e agora exposta linha a linha** (t07_evidencia_t11.txt §4):
```
⑤  status = ok          motivo_parada = gpy_bfgs_linalg
⑥  footer status=failed  motivo=gpy_bfgs_linalg      ← o RUNNER, honesto
⑥  footer status=ok      <sem campo motivo>          ← o DESPACHANTE, a máscara
```
`guard gpy_bfgs_linalg` na **iteração 42**, `erro="not positive definite, even with jitter"`, ③ com 220.011 linhas só `treedGP_build` até a geração 2.141, **zero blocos de sonda**, ⑦ vazia.

**(d) Mecanismo e a correção.** O runner **sempre** foi honesto (`c311_tgprmo.py:740-742`, `except np.linalg.LinAlgError: status, motivo_parada = "failed", "gpy_bfgs_linalg"`, presente desde o commit original `4d8a997`). Quem mascarava era o despachante: como o runner captura e **retorna sem exceção**, o `experiments.py` fazia `status = 'ok'` e sobrescrevia. A **DI-42.1** (T11) corrige em `experiments.py:281`: `if man.get('status') != 'failed': man['status'] = status` — e o comentário do código **nomeia esta célula como a prova empírica**. Nota lateral verificada: o hazard **não custa retry** (o runner engole a exceção; o despachante não vê falha), então a ausência de `gpy_bfgs_linalg` da lista `NO_RETRY_SUBSTRINGS` (`experiments.py:84`) é inócua.

**(e) Veredito: (2) — guarda nossa instalada, baseline limpo, e o bug de harness que a F5 apontou foi CORRIGIDO (com um residual medido — §4).**

### 2.8 A09/A10 — ② vazia, e a ⑦ como o único encontro entre predito e real

**(a) O artigo.** §3.3: a população do RVEA é composta de pontos gerados por SBX/PM, que não pertencem ao dataset. §4.1.6: as soluções são avaliadas com a função verdadeira **apenas para medir**, e o paper reconhece que fazê-lo "*may not be possible in real life*".

**(b) A SPEC.** **DI-16.17**: ② vazia por construção, `real_solution_id` 100% NULL — o gate não pode exigi-las. **DI-08** eleva a avaliação final a **endpoint OFICIAL** do offline, materializado na ⑦, fora do orçamento, com `nd_pos_real` calculado **depois** da avaliação real (B7.5) e rastro `origem_geracao`/`origem_linha` (DI-13.9).

**(c) Observado.** ② com **0 linhas em 54/54**; `real_solution_id` não-nulo em **0 de 7.368.513** linhas. Na ⑦: ND recomputado ≡ `nd_pos_real` **exato em 54/54** (3.260 linhas); link posicional `③[ger=n_ger][origem_linha] == ⑦[X]` com **Δmax = 0,0** em 54/54; `origem_solution_id` NULL em 3.260/3.260. **Razão de fantasia** `nd_pos_real/n_final`: mediana **0,607** nas 54 (0,020–1,000); no tier `off` mediana **0,340**, com **1/50 no WFG5**, 2/50 em ZDT6/WFG9/BBOB_F5, até 50/50 no ZDT1.

**(d) Mecanismo.** A razão mede diretamente quanto o surrogate mentiu, e casa com a régua da sonda: WFG5 fantasia 0,02 ↔ WAPE 0,41/0,46 e corr 0,17/0,09; ZDT1 fantasia 1,00 ↔ WAPE 0,000/0,027 e corr 1,000/0,942. São a mesma grandeza vista de dois ângulos, medida por dois caminhos independentes.

**(e) Veredito: (2) — desenho e endpoint sancionados; direta.**

### 2.9 A11 — o congelamento, provado bit-a-bit

**(a) O artigo.** §3.3: o MOEA "*may simply continue to run from the last population for an additional number of generations **without further modifying the surrogates***"; §4.1.5: 1.000 gerações.

**(b) A SPEC.** Sem mudança no número. A instrumentação (DI-16.12) existe precisamente para transformar o congelamento — uma afirmação sobre o CÓDIGO — em afirmação **verificável no dado**.

**(c) Observado.** Fase final com **exatamente 1.000 gerações em 54/54**. E o teste fecha de forma absoluta: comparando as 20.000 linhas do bloco `treedGP_build` com as do `treedGP_final`, coluna a coluna (X, todos os μ, todos os σ, NaN tratado como igual a NaN), **as duas matrizes são BIT-IDÊNTICAS em 54/54 — 0 diferenças em 2.160.000 × (D+2M) valores**.

**(d) Mecanismo.** Se qualquer GP tivesse sido adicionado, removido ou re-otimizado, ao menos um dos 20.000 pontos Sobol mudaria de μ, e o teste é sensível ao último bit de float32. É **prova positiva**, não ausência de evidência — e é a assinatura mais barata e mais forte da família offline.

**(e) Veredito: (1) — conforme o artigo, provado por dado; direta.**

### 2.10 A12 — σ POR-REGIÃO: o NaN é informação

**(a) O artigo.** O GPR fornece variância (Eq. 4) e o paper a apresenta como a vantagem do GPR (§2.1), mas **nunca a consome**; §5 declara os testes de incerteza como trabalho futuro. Onde não há GPR, **não existe variância definida**.

**(b) A SPEC.** **🟢 B15.5/D30** exporta `sqrt(var_GPy)` — extensão nossa, declarada, para que o c311 tenha coluna de incerteza comparável aos 15 demais configs do eixo. **DI-16.9** congela a semântica: NaN nas folhas SEM GP, **POR-REGIÃO, não por-run** — e o `sigma_dict` do ⑤ declara isso antes de qualquer leitura da ③ (regra §10.3 / DEF-C4).

**(c) Observado.** **(i)** Nas gerações 1–50, antes de qualquer `addGPs`, σ é **100% NaN em 54/54** — nenhuma exceção. **(ii)** Na sonda uniforme, σ-NaN mediano por tier: **0,000 (small) · 0,069 (medium) · 0,855 (big)**; o extremo é `swap_big-lhs/WFG9` com 99,5% (100 pontos válidos de 20.000). **(iii)** Na busca da fase final a fração despenca — a população vive dentro das folhas com GP.

**(d) Mecanismo.** O contraste busca × sonda é o retrato exato do que o algoritmo promete: **o GP é construído só onde a população pisa**, isto é, na região de trade-off. A NaN-share não é falha de export: é a medida direta da fração do espaço que ficou sob a árvore pura. Ler NaN como bug reprovaria 54/54 células.

**(e) Veredito: (2) — extensão nossa sancionada com semântica congelada; direta.**

### 2.11 A13/A14 — a curva de cobertura e os nós puros que rompem a banda do §3.3

**(a) O artigo.** Alg. 1: um GPR/árvore/iteração, sem remoção; §3.1: "*there can be a maximum of N/N_min leaf nodes*"; §3.3, literal: "*These GPRs can have N_min to 2N_min − 1 samples, because a split at the node of a tree occurs when the subset size is larger than or equal to 2N_min*". A Fig. 6 plota exatamente a nossa `total_points_per_model`.

**(b) A SPEC.** Nada mudou (o parâmetro é o do paper). O bundle manda logar `len(dict_gps)`, `total_points_per_model_sequence` e as iterações efetivas.

**(c) Observado (t02, t06).** Monotonia de `total_points_per_model` **54/54**; passo de `n_gps` ≤1 **54/54**; `n_gps ≤ n_folhas` 54/54; `n_folhas ≤ ⌊N/N_min⌋` em **125/125**. Cobertura final do dataset: mediana **0,835 (small) · 0,697 (medium) · 0,097 (big)**; **18/54 células atingem cobertura completa de folhas** (15 small, 3 medium, **0 big**). Banda: **963 incrementos positivos** (838 se excluída a primeira iteração de cada par, a convenção da F5 — a diferença é exatamente 125 = os pares), **960 na banda (99,69%)**. As **3 exceções** são `swap_medium-mvns_ZDT4` it6/obj0 **+328** (banda [100,199]), `swap_big-mvns_DTLZ2` it21/obj2 **+835** e it52/obj1 **+2.295** (banda [120,239]) — e a árvore reconstruída dessas células tem folhas grandes de **328** (exato), **1.395** máx com 835 na lista, e **2.286** máx. **0 folhas abaixo de N_min em 125/125.**

**(d) Mecanismo — provado.** O CART do sklearn **não divide um nó puro**: se ≥2N_min pontos têm o MESMO valor de objetivo em float32, o nó vira folha grande e a garantia do §3.3 quebra. Não é exclusivo do MVNS: `off/DTLZ4` sob LHS tem folha de **251** pontos (banda [120,239]) por α=100 achatar f em float32 — ela só não aparece na lista porque **nunca ganhou GP**. E a queda da cobertura no big (9,7%) **não é infidelidade**: é a aritmética de N_min=10D fixo com N crescendo 25×, e **o próprio paper antecipa** ao observar sobre a Fig. 6 que "*the number of samples utilized converges before the maximum iterations*".

**(e) Veredito: (1) conforme o artigo em 960/963, com nuance (2) nas 3 exceções (comportamento do sklearn sobre dados sancionados D51/D53); direta, reconstruída.**

### 2.12 A15/A16 — as árvores e o `max_depth=100` materialmente inerte

**(a) O artigo.** Alg. 1 l.1 + §4.1.3: K árvores MSE com todos os dados, `N_min = 10n`. §3.1, literal: "*the **depth of the trees is not controlled***" — e o agente-4 repete a letra (§3.4-2), como esperado, porque descreve o PAPER.

**(b) A SPEC.** `min_samples_leaf = 10D` é "paper ✓". `max_depth = 100` é **🟠 impl→código** (a leitura v2.2 do código oficial; confirmada por mim agora em `surrogate_treedGP.py:30`: `DecisionTreeRegressor(max_depth=100, min_samples_leaf=self.min_samples_leaf)`).

**(c) Observado (t06_arvore.csv).** Reconstruí, para os **125 pares (célula, objetivo)**, um `DecisionTreeRegressor(criterion='squared_error', min_samples_leaf=10D, max_depth=100)` a partir da ①: **`n_folhas` reconstruído ≡ logado em 125/125** (de 2 a 393 folhas) e **profundidade reconstruída ≡ logada em 125/125**. Máximo absoluto de profundidade: **26** (`swap_big-mvns_DTLZ2`).

**(d) Mecanismo.** Que **todas** as 125 contagens batam prova simultaneamente o critério (MSE, não MAE nem Friedman-MSE), o `min_samples_leaf` (10D exato) e o uso do dataset **INTEIRO** (uma subamostra mudaria a contagem). E o desfecho mais forte possível para uma divergência 🟠: **26 ≪ 100**, o teto nunca foi encostado, logo `max_depth=100` e "profundidade não controlada" produzem **exatamente a mesma árvore em 125/125**.

**(e) Veredito: A15 (1) · A16 (2) com efeito NULO provado; direta.**

### 2.13 A17/A18 — o `ceil` e a decomposição 50+1, agora também no fonte

**(a) O artigo.** §4.1.3: "*I_max = N/N_min = N/10n*" e "*G_max was set to 50*". A Alg. 1 **não conta** a re-avaliação pós-substituição como geração.

**(b) A SPEC.** O código faz float→**ceil** dentro do `continue_evolution`; e **DI-28c** registra que `_refresh_population` re-avalia toda a população e o DESDEO conta isso como **mais uma geração** ⇒ passo real **51**.

**(c) Observado.** `I_eff ≤ I_max` **54/54**; o `ceil` está provado no small: (31D−1)/(10D) ∈ (3,0; 3,1) e as **35/35 células small rodam exatamente 4 iterações**, com `n_geracoes = 4×51+1000 = 1.204`. As 604 decisões estão na geração 51·i em 604/604. **NOVO nesta rodada:** a âncora que a F5 provou por discriminação estatística (99,5% em 51i−1 × 46,0% em 51i) está agora **LIDA NO FONTE**: `desdeo_emo/EAs/BaseEA.py:66` incrementa `_iteration_counter` no FIM de `iterate()`, e `src/c311_tgprmo.py:618` faz `Xs = evolver.population.individuals` **imediatamente após** `iterate()` — isto é, `_current_gen_count = 51(k−1)+50 = 51k−1`. A âncora deixou de ser inferência estatística e virou leitura.

**(d) Mecanismo.** O `ceil` faz o small rodar **4** iterações e não 3, e várias células small atingem cobertura completa na 3ª e ainda rodam a 4ª — 51 gerações que o critério do paper teria dispensado.

**(e) Veredito: (2) — desvios sancionados, com a cronologia interna provada por dado E por fonte; direta.**

### 2.14 ★ A19 — O EARLY-STOP: a fórmula que a F5 mandou para o teto T, LIDA E RECONSTRUÍDA

Este é **o achado central desta rodada**.

**(a) O artigo.** §3.1 dá dois critérios de parada da construção: "*Number of iterations has reached the predefined maximum of I_max*" **ou** "*All the solutions in the MOEA's population fall in the leaf nodes that have GPRs built*". O segundo é uma condição de **cobertura total da população**. O agente-4 transcreve-o fielmente (§3.4-5).

**(b) A SPEC.** O bundle registra a implementação como `delta = total_points − sequence[counter−3]` com `counter>5`, e a v2.2 **rebaixou B15.8 de "divergência" a "nuance" 🟠** com o argumento de que a regra implementa *aproximadamente* o critério do paper com persistência de 2 iterações e um piso de 6. **A F5 não conseguiu sustentar isso com dado:** testei 9 famílias de candidatos × 3 âncoras × 4 lags e o melhor acerto foi **20 de 369 = 5,4%**; a "janela de 2" foi promovida ao **teto T** como afirmação de código não verificável.

**(c) Observado — a leitura de fonte que resolve, e a reconstrução que confirma.**

A leitura (READ-ONLY, `algorithms/c311_TGPR-MO/desdeo_problem/surrogatemodels/surrogate_treedGP.py:45-49`):
```python
def addGPs(self, X_solutions):
    Y_solution_leaf = self.regr.apply(X_solutions)
    unique_solutions, count_solutions = np.unique(Y_solution_leaf, return_counts=True)
    unique_solutions = np.setdiff1d(unique_solutions, self.error_leaves)
    self.total_point_gps = unique_solutions.size      # ← CONTA FOLHAS, NÃO PONTOS
```
`total_point_gps` **não é uma contagem de pontos**: é o **número de folhas distintas visitadas pela população corrente que ainda NÃO têm GP**. O nome mente — e é exatamente por isso que as 9 famílias da F5, todas baseadas em pontos, falharam. E em `src/c311_tgprmo.py:620-637` o runner **acumula**: `total_points_all += models[i].total_point_gps` para cada objetivo, a cada iteração. Com `c(t) = Σ_j |folhas visitadas sem GP na iteração t|` e `C(k) = Σ_{t≤k} c(t)`, e sabendo que `_iteration_counter = k` após a k-ésima `iterate()`:

> **`delta(k) = C(k) − C(k−2) = c(k) + c(k−1)`**, para `k > 5`; sentinela `1` para `k ≤ 5`.

A reconstrução (`t01_delta_formula.py`, sobre as 54 células): previ `c(k)` a partir da árvore CART reconstruída + a população da geração 51k−1 da ③, e comparei com o `delta_total_point` LOGADO no ⑥.

| | F5 (v1.1) | **T11 (esta rodada)** |
|---|---:|---:|
| iterações elegíveis (k>5) acertadas | 20/369 = **5,4%** | **338/369 = 91,6%** |
| sentinelas (k≤5) acertadas | 604/604 (só o valor 1,0) | **235/235 = 100%** |
| total das 604 decisões | — | **573/604 = 94,9%** |
| células a 100% nas elegíveis | — | **52/54** |

Exemplo literal — `swap_medium-lhs_DTLZ2`, a série que a F5 citou (33, 26, 21, 17, 13, 7, 3, 1, 0): a reconstrução dá `c` = (…, 19, 14, 12, 9, 8, 5, 2, 1, 0, 0) e `c(k)+c(k−1)` = **33, 26, 21, 17, 13, 7, 3, 1, 0** — os nove valores, na ordem, sem uma exceção. Os **31 desencontros** estão em **2 células**: `swap_big-mvns_DTLZ2` (29) e `swap_big-lhs_ZDT4` (2) — **exatamente as mesmas 2 células** dos 7 resíduos de A20, e pela mesma causa declarada (desempate de threshold entre sklearn 1.9.0 do venv de análise e sklearn 1.1.2 do run, concentrado na célula com 8.539 empates float32).

**(d) Mecanismo — e o que isso faz com a classificação B15.8.** Três consequências, todas mensuráveis:

1. **O critério do código É o critério do paper**, com dois acréscimos: `delta(k)=0 ⟺ c(k)=c(k−1)=0 ⟺` "nas ÚLTIMAS DUAS iterações nenhuma folha visitada estava sem GPR". O paper pede `c(k)=0`. A v2.2 estava certa ao rebaixar B15.8 a nuance — e agora isso está **provado**, não argumentado.
2. **O custo da divergência está quantificado** (`t04_criterio.csv`): o critério do paper é satisfeito em **54/54 células**; o código roda **mediana 1 iteração a mais** (min 0, máx 6), totalizando **3.162 gerações de busca extras de 30.804 gerações de construção = 10,3%**. O caso extremo é `swap_big-lhs_WFG9`: `c(1)=44`, `c(k)=0` de k=2 em diante — o critério do paper era satisfeito na **2ª** iteração e o piso `it>5` obriga a rodar até a **6ª**, gastando **204 gerações** de busca inútil. É o retrato exato do que a F5 descreveu qualitativamente, agora com o número.
3. **A janela de 2 não é arbitrária — ela guarda contra uma não-monotonicidade REAL.** Em **5 das 54 células** `c(k)` volta a ser >0 depois de zerar (a população re-entra em folhas descobertas): `swap_big-lhs_DTLZ2` (…, 1, 0, **1**, 0, 0), `swap_medium-mvns_WFG9` (…, 0, 0, 0, 0, **1**, 0, 0), `swap_big-mvns_WFG9` (…, 3, 0, **15**, 0, 0). Um critério de 1 iteração pararia cedo e errado nessas 5. A persistência de 2 é, portanto, uma **robustificação defensável do critério do paper**, não um desvio arbitrário.

**(e) Veredito: (2) — desvio sancionado (🟠 B15.8/D29), com gatilho, piso E FÓRMULA provados. O item sai do teto T.**

### 2.15 A20/A21/A22 — a colocação de GP, a predição composta e o RVEA

**(a) O artigo.** Alg. 1 l.13–16: "*one GPR is built at the leaf node i\* with the maximum loss value*", com H(Q) = a perda MSE do nó (Eq. 6), e "*If multiple solutions fall within the same leaf node… only one GPR is built*". §3.3: "*if the decision vector… falls within the path of the leaf node where a GPR is already built, the posterior predictive mean of the GPR is used… **Otherwise, the prediction is the mean value at the leaf node***". §4.1.3: RVEA com os parâmetros de Cheng et al. (2016).

**(b) A SPEC.** Nenhuma mudança nas três regras; a única contribuição nossa é o **marcador**: DI-16.9 faz o σ ser NaN exatamente onde não há GP, o que transforma "qual ramo da regra foi usado" numa **coluna observável**.

**(c) Observado.** **A20:** a reconstrução da regra (candidatas = folhas visitadas pela população de 51k−1 menos as que já têm GP; escolhida = argmax de `tree_.impurity`) prevê a sequência inteira de `total_points_per_model`: **1.488/1.495 pares = 99,5%**, **52/54 células a 100%** (re-confirmado nesta rodada como controle do t01). **A21:** μ ≡ média-da-folha nos pontos σ-NaN em **63/66 pares a 100%** (66/66 ≥99,9%) contra **4,6e-5** nos pontos com σ válido — o contraste é o que dá poder discriminativo. **A22:** o teto do lattice **nunca é excedido em 54/54**; o máximo observado é **50** (M=2) e **105** (M=3, atingido em 8 células). Colapso a ≤10 indivíduos nas gerações 1–50 em 37/54 células, com recuperação a partir da geração 51.

**⚠ Auto-errata da F5:** eu havia escrito "máximo observado 104 nas células M=3, nunca 105". **Errado — 105 é atingido em 8 células** (MMF16_20, swap_big-lhs_DTLZ2, swap_big-lhs_MMF16_20, swap_big-mvns_DTLZ2, swap_medium-lhs_DTLZ2, swap_medium-lhs_MMF16_20, swap_medium-mvns_MMF16_20, swap_small-lhs_MMF16_20). O invariante que importa — **o lattice nunca é excedido** — segue intacto e agora é mais forte: ele é exatamente atingido.

**(d) Mecanismo.** O colapso populacional das gerações 1–50 é a assinatura da inicialização "sem GPRs" da Alg. 1: a árvore pura prediz constante por partes, e com 2–3 folhas a população inteira mapeia em 4–9 pontos distintos do espaço de objetivos, que é o que o APD consegue nichar. O bundle registra o contrapositivo como teste de sanidade: se a geração 1 já estivesse no lattice cheio, o run teria usado o `HybridTreeGP_v2` (a implementação ERRADA). Em 54/54 isso não aconteceu.

**(e) Veredito: A20 (1) · A21 (1) · A22 (1); direta.**

### 2.16 A23 — a instrumentação nossa: σ, `predict_batch`, telemetria DI-10

**(a) O artigo.** §2.1 apresenta a variância como a vantagem do GPR e §5 a deixa para trabalho futuro; o custo de predição não é discutido.

**(b) A SPEC.** 🟢 B15.5/D30 (σ export) · **C311-12/DI-16.13** torna **OBRIGATÓRIO** um `predict_batch(X)` NOVO, com proibição explícita de importar o `predict_new` da classe irmã · DI-10/S.7.1 exigem o estado do mecanismo no evento `decision`.

**(c) Observado.** O `_predict_batch` (`src/c311_tgprmo.py:199-219`) foi **lido**: agrupa por folha e chama `GPy.predict` uma vez por folha, com `σ = sqrt(max(var,0))` e NaN nas folhas só-árvore — nenhuma linha importada do `surrogate_hybridGP_v2.py`. O `_patched_predict` (linhas 222-240) mantém o laço 1-ponto-por-linha do stock, `Y_predict_mod[i] = pred[0][0]` **idêntico ao stock**, e a única adição é `pred[1][0][0]` — a variância que o GPy **já computou na mesma chamada**. Speedup medido na F5: **33,4× mediano** (10,4–67,7×), sonda de 40.000×M pontos em 0,21–0,65 s. Telemetria DI-10 completa (20 chaves) em **604/604** eventos, com `n_acumulado ≡ Σ total_points_per_model` em 604/604 e `f_best ≡ min(μ)` da geração 51i em 604/604.

**(d) Mecanismo.** A leitura do `_patched_predict` é o argumento mais forte disponível para a não-intrusão do patch de σ **sem** um run pareado: o μ é produzido pelo mesmo código, na mesma ordem, com a mesma chamada GPy. Combinado com o teste comportamental `test_sonda_NAO_perturba_a_busca` (que roda de verdade, §4) e com o re-run bit-idêntico (A24), a não-intrusão está fortemente sustentada, embora o contrafactual formal (rodar SEM o patch) permaneça no teto.

**(e) Veredito: (2) — extensões/mitigações nossas sancionadas, efeito medido; direta.**

### 2.17 A24 — determinismo bit-a-bit e proveniência: o item que SAIU do teto T

**(a) O artigo.** Nada.

**(b) A SPEC.** D62/DI-28 ratificam a semeadura (mesmo `s` em `np.random` e `random`, `uso_id=0`), com o gancho **DI-28.3** que injeta o `RandomState` global semeado no `lhs` do pyDOE, e `PYTHONHASHSEED=0` obrigatório. A F5 classificou isto como **T**: "*nada disso deixa rastro nas camadas — a prova é a re-execução bit-idêntica de ① e ③*". A T11 acrescentou `campanha_id` (B-03) e `repo_hash` (I-09) ao ⑤.

**(c) Observado — a prova que faltava.** O smoke T11 `off/c311/BBOB_F17/42` é um **re-run genuíno** da mesma célula da s42: `created_at` 2026-07-31T13:46 contra 2026-07-26T11:26, tempdir `/var/folders/.../sp_c311_etsjathb/`, HEAD `9ad0138`, wall 8,55 s contra 8,91 s. Os sha256 (`t07_evidencia_t11.txt` §1):

| camada | s42 (26/07) | smoke T11 (31/07) | |
|---|---|---|---|
| ① `__real.parquet` | `07530706e23cdddd…` | `07530706e23cdddd…` | **IDÊNTICO** |
| ② `__pop.parquet` | `d0c59b0cc8002dce…` | `d0c59b0cc8002dce…` | **IDÊNTICO** |
| ③ `__surrogate.parquet` | `f38c82a708d9fa14…` | `f38c82a708d9fa14…` | **IDÊNTICO** |
| ⑦ `__final.parquet` | `021c23b28b0bf267…` | `021c23b28b0bf267…` | **IDÊNTICO** |
| ⑦ manifesto | `7866647238704d1f…` | `7866647238704d1f…` | **IDÊNTICO** |
| ④ `__timing.parquet` | — | — | difere (wall medido, por natureza) |

O prefixo `07530706e23cdddd` é **o mesmo valor que a §5 do handoff T11 publica como o par G-6 do c311** — confirmando de passagem que aquele número é o sha256 da ①. E o ⑥ é idêntico evento a evento (4 `decision` + 2 `sonda` com valores byte-iguais), diferindo apenas no `dataset_path` (tempdir), no `params` (o campo novo do I-11) e por **um footer a menos** (o do despachante, ausente na invocação direta). O ⑤ ganhou `campanha_id="9ad0138aa9a4_2026-07-31"` e `repo_hash="9ad0138aa9a4…"` (ambos ausentes/`None` em **55/55** células da s42) e `schema_version` 1→2.

**(d) Mecanismo.** ①②③⑦ byte-idênticas em execuções separadas por 5 dias, em diretórios diferentes, sob um HEAD com 3 alterações no runner, provam **três coisas de uma vez**: (i) a disciplina de RNG D62/DI-28.3 é efetiva (o gancho do `lhs`, o SBX/PM, o `random_state=None` da árvore e o `PYTHONHASHSEED=0` reproduzem bit a bit); (ii) as três alterações da T11 no runner são **read-only** sobre a busca; (iii) o `Checkpointer` instanciado não perturba (embora não tenha disparado). **Este era o item 3 do teto T da F5 — ele cai.**

**(e) Veredito: (2) — convenção sancionada, agora PROVADA por re-execução; direta.**

### 2.18 ★ A25 — `tempo_aval_real_s` no offline: o único (3) 🎯

**(a) O artigo.** Nada — telemetria nossa.

**(b) A SPEC/decisão.** O item **I-02** (commit `83dbdac`) moveu o cronômetro de avaliação real para o **portão único** `src/budget.py:FEBudget.evaluate`. O racional está no próprio commit e é bom: o `sobol_batch` gravava `0.0` LITERAL quando o valor verdadeiro era 4,110 s (20,6% do wall). E a decisão sobre a família offline é **explícita**: *"A property devolve **None quando NENHUMA avaliação passou pelo portão**… os 4 runners OFFLINE + o stub passam a declarar NULL em vez de `0.0`: no offline o orçamento nasce ESGOTADO (a ① é o dataset, D90) e nenhuma avaliação real acontece dentro do run, então zero afirmaria que avaliar custou zero."*

**(c) Observado — o contrato é violado, e o número é semanticamente errado.** O ⑤ do smoke c311 traz `timing.tempo_aval_real_s = 0.0003` — **nem NULL, nem 0.0**. Medindo os 5 offline Python do smoke T11 (`t07_evidencia_t11.txt` §2):

| config | problema | N | `tempo_aval_real_s` | µs/linha |
|---|---|---:|---:|---:|
| b5m | MMF1 | 61 | 0,0001 | 1,64 |
| moead_media | MMF1 | 61 | 0,0001 | 1,64 |
| **c311** | BBOB_F17 | 309 | **0,0003** | **0,97** |
| b5r | BBOB_F22 | 309 | 0,0003 | 0,97 |
| treed_media | ZDT4 | 50.000 | 0,0572 | 1,14 |

O valor **escala com N e é invariante ao problema** (dois problemas diferentes com N=309 dão o mesmo número; N cresce 820× e o tempo cresce 572×). A causa está lida: `standalone_harness.py:653`, dentro de `load_offline_budget`, injeta o dataset com `bud.evaluate(X[i], lambda _x, _f=F[i]: _f)` — **N chamadas a um lambda que apenas devolve a linha do artefato**. O cronômetro do I-02 envolve exatamente esse `true_f(x)` (`budget.py:249-251`), e `_n_avals_cronometradas` chega a N, então a property **nunca** devolve `None`.

**A trava é decorativa.** O teste que deveria proteger isto (`tests/test_g7_instrumentacao.py:118-127`, `test_os_offline_declaram_NULL_em_vez_de_zero`) **lê o TEXTO DO FONTE**, não o comportamento:
```python
for nome in ("c311_tgprmo", "b5_prob", "piso_offline", "treed_media", "standalone_harness"):
    src = open(.../f"{nome}.py").read()
    self.assertNotIn("tempo_aval_real_s=0.0", src)
```
Ele nunca roda o caminho offline e nunca inspeciona o valor. É precisamente o padrão que a própria T11 diagnosticou em §4.2.1 — *"Um gate que nunca reprova é decorativo"* — reproduzido no teste que fecha o item.

**(d) Mecanismo e impacto.** O impacto no **mecanismo é ZERO**: o campo vive só no ⑤; ①②③④⑦ são bit-idênticas (A24). O impacto na **fidelidade é indireto mas real**: na F5, `tempo_aval_real_s == 0.0` em **54/54** foi a assinatura discriminativa do aspecto A01 — a evidência de que **nenhuma FE real acontece durante a busca**. Pós-T11 esse sinal está **corrompido**: um leitor de uma célula big verá 0,0572 s de "avaliação real" num run que não avaliou nada, e o número escala com o tamanho do dataset, dando a aparência de trabalho de avaliação proporcional. Trocou-se um zero *inofensivamente* errado por uma medida *enganosamente* errada. É um desvio **sem decisão que o sancione** — a decisão existente diz o contrário — e por isso classe (3).

**Conserto sugerido (1 linha, não aplicado — READ-ONLY):** não contar a injeção. Ou o `load_offline_budget` usa um caminho de carga que não passa pelo cronômetro, ou o `FEBudget` ganha um `evaluate(..., cronometrar=False)` para a carga do dataset. E o teste tem de ser **comportamental**: rodar um offline e afirmar `manifest['timing']['tempo_aval_real_s'] is None`.

**(e) Veredito: (3) — desvio inexplicado, periférico (telemetria do ⑤), com causa MEDIDA e efeito delimitado. 🎯 vai à F5.4.**

### 2.19 T1/T2/T3 — o que continua invisível

**T1 — kernel e otimização do GP local.** Matérn 5/2 ARD, sem White/normalizer/priors, `optimize('bfgs')` **único sem restarts**, `jitchol`. Agora tenho a leitura do vendor (`surrogate_treedGP.py:63-66`: `GPy.kern.Matern52(D, ARD=True)`, `GPRegression(X_leaf, Y_leaf)`, `m.optimize('bfgs')`) — mas μ e σ não fingerprintam o kernel a partir de 20.000 pontos sem reconstruir o próprio GP sob GPy 1.9.9/py3.8. **T — não-verificável nos dados; fora do denominador.**

**T2 — `selection_type='mean'` / o σ nunca consumido.** Declarado em 54/54; o contrafactual "*a busca teria sido idêntica sem a coluna σ*" é logicamente inverificável ex-post. **T.**

**T3 — o checkpoint DI-43 (NOVO).** `src/checkpoint.py` foi ligado ao c311 (`c311_tgprmo.py:497-500` e `:648`) com cadência **25 iterações OU 30 min**. Não é observável em lugar nenhum do meu corpus: a s42 é pré-T11, e o smoke tem `I_eff=4` e 8,55 s — **o checkpoint não disparou nenhuma vez**. Nas células big da s42 (`I_eff` até 99) ele dispararia até 3×. A T11 declara bit-identidade com cadência 1 travada por teste, e o meu re-run bit-idêntico é consistente com isso, mas **não testa o caminho** (o checkpoint não rodou). **T — não exercido; fora do denominador.**

---

## 3. Percentuais por classe (Botão 2)

**Total = 28 aspectos · T = 3 (T1 kernel/GPy · T2 `selection='mean'` · T3 checkpoint DI-43) · denominador = 28 − 3 = 25.**

| classe | aspectos | n | % |
|---|---|---:|---:|
| **(1)** conforme o artigo | A02, A11, A13, A14, A15, A20, A21, A22 | **8** | **32,0%** |
| **(2)** desvio/extensão sancionado (decisão citada) | A01, A03, A04, A05, A06, A07, A08, A09, A10, A12, A16, A17, A18, A19, A23, A24 | **16** | **64,0%** |
| **(3)** desvio inexplicado 🎯 | **A25 `tempo_aval_real_s` no offline** | **1** | **4,0%** |
| **T** (fora do denominador) | T1 · T2 · T3 | 3 | — |

**Marca declarativa visível (Botão 3):** apenas **A22** (α=2) e **A02** (proveniência) contam com eco declarativo, e nos dois há prova de dado redundante (teto do lattice fecha 54/54; ΔX bit-exato 54/54). **Nenhum aspecto do mecanismo é declarativo.**

**Contra a F5** (30 aspectos, denominador 27): (1) 29,6% → **32,0%**; (2) 70,4% → **64,0%**; (3) 0,0% → **4,0%**. ⚠ **Os denominadores diferem por granularidade** (fundi U3⊕U10, U1⊕U8 e C9⊕C10⊕C11; separei o `tempo_aval_real_s` e o determinismo/proveniência). A migração real é: **nenhum aspecto mudou de (1)↔(2)**, um aspecto NOVO entrou como (3), e **um item saiu do teto T** (A19/early-stop) enquanto **outro item saiu do T para (2)** (A24/RNG) e **um item novo entrou no T** (T3/checkpoint).

---

## 4. 🆕 AS CORREÇÕES DA T11 — implementadas? funcionam de fato?

| # | correção | toca o c311? | implementada? | **funciona de fato?** | evidência |
|---|---|---|---|---|---|
| **I-11** | aviso `n_sigma_valido` (resposta DIRETA à recomendação da F5) | **SIM** | ✅ texto no `params` do ⑤ | ⚠ **PARCIAL — o padrão do c217** | (i) código: `c311_tgprmo.py:510-524` · (ii) dado: presente no ⑤ do smoke, ausente em 55/55 da s42 · (iii) **a COLUNA não existe**: `grep -rn n_sigma_valido src/ scripts/ tests/` só acha o texto do próprio runner; o header de `f5/sonda_f52e.csv` continua `…,cobertura95,n_validas,n_nan` |
| **B-05/DI-42.1** | B1 — `status` mascarado | **SIM** (a célula-prova é minha) | ✅ | ✅ **no ⑤** · ⚠ **residual no ⑥** | (i) código: `experiments.py:281` `if man.get('status') != 'failed': man['status'] = status`, com a célula c311 nomeada no comentário · (ii) residual: `experiments.py:237` grava o footer do despachante no `finally`, **ANTES** da mescla, com o `status='ok'` local — medido na s42: ⑥ com footer `failed` (runner) + footer `ok` (despachante) |
| **T11-G6** | `motivos_parada.json` — fonte única | **SIM** | ✅ | ✅ | artefato novo classifica `gpy_bfgs_linalg` como **`"falha"`**, citando `sweep-big-mvns/c311/MMF16_20` como a prova empírica; consumido por `portao.py`/`accept.py`/`censo42.py` via `gates_proveniencia.classifica_motivo`. A triagem que a F5 fez **à mão** virou máquina |
| **I-02** | cronômetro no portão de avaliação | **SIM** | ✅ | ❌ **NÃO no offline** — ver §2.18 | valor observado 0,0003 (c311) contra o NULL contratado; escala com N em 5/5 offline; o teste que trava (`test_g7_instrumentacao.py:118`) só lê o TEXTO do fonte |
| **I-09** | `repo_hash` no ⑤ | SIM | ✅ | ✅ | `None` em 55/55 da s42 → `9ad0138aa9a464a7…` (= HEAD) no smoke |
| **B-03** | `campanha_id` no ⑤ | SIM | ✅ | ✅ | ausente em 55/55 da s42 → `9ad0138aa9a4_2026-07-31` no smoke |
| **DI-43** | checkpoint atômico periódico | SIM | ✅ (ligado) | ⬜ **não exercido** | `c311_tgprmo.py:497-500,648`; smoke com I_eff=4 e 8,55 s não dispara (cadência 25 its / 30 min) → **T3** |
| **A-8** | suíte `*_SLOW` do c311 | SIM | ✅ rodada | ✅ | 14 testes / 102 s, 4/4 OK. E são **comportamentais**, não de texto: `test_determinismo_bit_a_bit` (2 runs da mesma semente, ⑦ idêntica) e `test_sonda_NAO_perturba_a_busca` (③-busca **e** ⑦ idênticas com/sem sonda) — `tests/test_c311.py:270,283` |
| **A-6** | `portao.py --historico` | SIM | ✅ | ✅ | sem a flag, um ⑤ v1 da s42 seria reprovado por falta de `campanha_id`/`repo_hash` |
| **I-1/I-2/I-5/I-6** | `ref_ids`/`pmid_ids` · `REGRA_DO_ROTULO` · `y_treino_dist` · `sonda_estratificada` | **NÃO** | N/A | N/A **por desenho** | verificado no ③ e no ⑥ do smoke: o c311 não é classificador nem online. **Regra 12 do CONTRATO é vacuamente satisfeita** — o c311 só tem `regime='sonda'` (a régua Sobol), 40.000 linhas, zero `sonda_estratificada` |
| **I-4** | `NO_RETRY_SUBSTRINGS` | indireto | ✅ (`experiments.py:84`) | ✅ inócuo aqui | verificado: o runner **captura** o `LinAlgError` e retorna sem exceção ⇒ o despachante nunca entra no laço de retry. O hazard do c311 não custa retry, com ou sem a lista |

**A caça ao padrão do c217 ("campo presente, dado sentinela") deu UM resultado e MEIO.** O meio é o **I-11**: a regra foi escrita (e escrita bem — o texto cita os meus próprios números), mas o **número que ela manda reportar continua não sendo produzido por ninguém**. Eu o produzi: `t05_sonda_regua.csv`, 125 pares, com `n_sigma_valido` mediana 16.351, mínimo **100**, 8 pares abaixo de 1.000. O completo é o **I-02**: o campo existe, está preenchido, e o que ele contém não é o que o nome diz — pior que sentinela, porque um sentinela se reconhece e um `0,0572` não.

---

## 5. Comparação canônica (Entrega 2)

**Setup do paper** (§4.1.1–4.1.6 + agente-4 `_full`): **DBMOPP P1–P4**, K ∈ {3,5,7} × n ∈ {2,5,7,10} = 48 instâncias; datasets LHS e MVNS com N ∈ {2.000, 10.000, 50.000}; 288 casos × 31 sementes; métricas **HV** (ref 2√K) e **RMSE multivariado** + tempo de construção; Wilcoxon + Bonferroni. DTLZ aparece só na Fig. 3 e no suplementar, **sem tabela numérica**. **O paper não reporta IGD nem IGD+ em lugar nenhum.** Nada mudou na T11 — este bloco é fato do artigo.

| interseção | veredito | evidência |
|---|---|---|
| **DBMOPP P1–P4** (o corpo do paper) | **incomensurável (eixo X) — interseção VAZIA** | nenhum DBMOPP no nosso grid de 25 |
| **DTLZ2** (único nome em comum) | **incomensurável** | n 10≠12, K 2≠3, N 2.000≠{371, 2.000, 50.000}, HV-2√K ≠ IGD+/HV-normalizado D69, **e o paper não publica número** |
| demais 23 problemas | **incomensuráveis por inexistência no paper** | — |
| **Âncora J1 — escalabilidade da construção @50k** | **SENTIDO CONFIRMADO** | paper: build treed **3,16e1–1,91e2 s** × sparse 2,16e4–5,05e4 s × full OOM ≥10k. Nós, 9 células big: Σ`tempo_fit_s` **1,73–130,5 s** (mediana 29,2 s) — mesma ordem do treed, 2–4 ordens abaixo do sparse |
| **Âncora J2 — a cobertura converge antes de I_max (Fig. 6)** | **SENTIDO CONFIRMADO, agora com o número exato** | paper: "*the number of samples utilized **converges before the maximum iterations***". Nós: `I_eff/I_max` mediana **0,108 no big** e 0,824 no medium; **e o critério do paper (c(k)=0) é satisfeito em 54/54 células**, com o código gastando 10,3% de gerações a mais (§2.14) |
| **Âncora J3 — DTLZ consome mais iterações (Fig. 3)** | **SENTIDO CONFIRMADO** | no tier medium (o N da Fig. 3): DTLZ2 I_eff=14 (lhs) e 14 (mvns) × MMF16_20 10/10 · ZDT4 7/10 · WFG9 6/8 · ZDT1 6/7 — DTLZ2 é o maior consumidor nos dois eixos |
| **Nuance de qualidade declarada pelo paper (N pequeno)** | **caveat científico, NÃO comparação** | §4.3: "*full GPRs performed the best in hypervolume for a sample size of 2,000*". Nosso tier small (N = 61–929) está **1–2 ordens ABAIXO** do menor N do paper: rodamos o TGPR-MO **fora do envelope em que ele se justifica**, por decisão de desenho (D90). Não é infidelidade e **não autoriza** juízo comparativo |

**Placar: comparável 0 · âncoras direcionais 3/3 com sentido CONFIRMADO · incomensuráveis 25/25 problemas.** Botão 4 respeitado: **nenhum "artigo melhor" nem "nosso melhor"** — não há uma única grandeza reportada nos dois lados sobre o mesmo problema.

---

## 6. Saúde (s42 = corpus principal)

**Contrato.** `contrato_f52b.csv` e `integridade_f52a.csv` têm **ZERO linhas para c311**. `manifest.params` completo em 54/54 (o c311 nunca esteve entre os configs da não-conformidade transversal do ⑤).

**Trajetórias (re-medidas, 20 checkpoints × 54 células).** **0 violações de monotonicidade do IGD+ em 1.026 transições** e 0 do HV. Ganho IGD+ início→fim mediana **3,34×** (1,13–137,93×). **Ressalva obrigatória:** no offline a trajetória percorre a **acumulação do dataset** (fe 0..N−1), não a busca — é sanidade da métrica, **não** curva de convergência.

**Régua da sonda — BLOCO ÚNICO (regra 12 satisfeita vacuamente).** O c311 tem **só** `regime='sonda'` (a régua Sobol, 20.000 pontos fixos, `x_hash` conferido), **zero** `sonda_estratificada`. Os 2 blocos são bit-idênticos (A11), logo a régua é uma medição por célula. Quatro regimes no tier small:
- **(A) o treed-GP domina** — ZDT1 (WAPE 0,000/0,027; corr 1,000/0,942), DTLZ7 (0,000/0,000/0,058), ZDT3, MMF11_L, DTLZ2 (0,054/0,068/0,015; corr 0,995/0,992/0,999), MMF16_20 (0,032/0,096/0,000).
- **(B) a família WFG é o ponto cego** — WFG4 0,797/0,642 com corr **−0,442**; WFG2 0,666/0,462 com corr **−0,601**; WFG9 0,495/0,536 com corr 0,102/**−0,070**. **Correlação NEGATIVA em 3 dos 5 WFG.**
- **(C) BBOB heterogêneo** — BBOB_F55 WAPE baixo (0,058/0,055) com corr **≈0,09** (acerta a escala, erra a ordem); BBOB_F49 corr **−0,321** no obj0.
- **(D) DTLZ4** — corr 0,432 no obj0: é a única célula off em que `total_points` congela desde a iteração 1 ([120,120,120] em 4 iterações) porque α=100 achata f em float32 e a população nunca sai da primeira folha.

**Cobertura ±1,96σ, com o denominador visível (a coluna que faltava):** mediana **0,9512** (q1 0,9122, q3 1,0000) — o GP local é **bem calibrado onde existe** —, mas apoiada em `n_sigma_valido` mediana **16.351/20.000** e mínimo **100** (`swap_big-lhs_WFG9` obj1). Por tier: 20.000 · 18.621 · **2.906**.

**Métricas oficiais da ① EMPATAM POR DESENHO entre os 5 offline** (D69 lê o `f` da ①, e a ① dos cinco é o mesmo dataset). **Nunca usar para ranquear offline.** O endpoint discriminante é a ⑦:

| leitura | resultado (re-medido em `transversal_offline_camada7.csv`) |
|---|---|
| c311 vence o IGD+ da ⑦ entre os 5 offline | **3/25** — **DTLZ1** (72,25 × 75,32 do e103), **DTLZ2** (0,0408), **MMF16_20** (0,0265); **rank médio 3,52 de 5** (e103 1,68 · b5r 2,92 · b5m 3,12 · moead_media 3,76) |
| ⚠ **auto-errata da F5** | eu havia listado DTLZ7 como 3ª vitória — **errado**: no DTLZ7 o e103 vence (0,0544 × 0,0924). A 3ª é **DTLZ1** |
| ressalva de máquina | **e103 rodou em `vm3`**; c311/b5r/b5m/moead_media em `mac`. O piso O-18 (IGD+ ≤58,98%) contamina **apenas** c311×e103 — e é justamente o e103 que lidera |
| ablação c311 × treed_media (= c311 SEM os GPs locais), tier big | **os GPs locais melhoraram o endpoint em 7 das 9 células comparáveis** (DTLZ2-lhs 0,0255 × 0,1354; ZDT1-lhs 0,2174 × 2,3528; ZDT1-mvns 0,0445 × 1,9315; DTLZ2-mvns 0,0302 × 0,3842), pioraram em 2 (ZDT4-lhs, WFG9-mvns) |

**"Mais dado ⇒ melhor?" — duas respostas opostas.** No *nível dataset* (métrica da ①, comum aos 5) o IGD+ melhora small→medium→big em 8 de 9 séries. No *nível mecanismo c311*, mais dado ⇒ surrogate proporcionalmente **menos coberto**: `I_eff/I_max` 1,000 → 0,824 → **0,108**; fração do dataset sob GP 0,835 → 0,697 → **0,097**; σ-NaN da sonda 0,000 → 0,069 → **0,855**; **0/9 células big com cobertura completa** contra 15/35 no small. Em N=50.000 o "treed-GP" operante é majoritariamente **árvore pura com ilhas de GP na região de trade-off** — fiel ao código, coerente com a Fig. 6 do paper (âncora J2), e com contrapartida positiva medida (a ablação).

**Custo.** 54 células em **0,90 h-core** (Σ 3.254 s; mediana 33,6 s; máximo 327,0 s em `swap_big-lhs/DTLZ2`), máquina única `mac`, 1 core, threads pinadas (D79).

**Caveats de escrita (não de mecanismo).** **7 células sem footer** no ⑥ (4 `swap_small-lhs_*` + 3 `swap_small-mvns_*`) e **2 guards `hard_error`** com `ModuleNotFoundError: No module named 'google'` em `gcs.py:103` via `mirror_run` — o run científico terminou antes da exceção (as 7 têm header + 4 `decision` + 2 blocos de sonda, `n_geracoes=1204`, ③ densa, ⑦ presente, e passam todas as ~30 checagens desta bateria). **4 células com desvio-tolerância da ⑦** já aceitas pelo autor (ULP-float32 × número de condição).

---

## 7. SCORE, RECOMENDAÇÃO E VEREDITO

### **SCORE: 9,5/10 · RECOMENDAÇÃO: ACEITAR** (caveats **científicos**, não de fidelidade)
### **VEREDITO: MANTEVE vs a F5 — mas com a composição TROCADA**

*Prior F5.3a-v1.1: 9,5 · piloto v1.0: 8,5 · prior v2: 7,5.*

**A causa, nomeada, é dupla e ela se cancela:**

**(+0,5) EVIDÊNCIA NOVA, não mecanismo novo — dois itens do teto T caíram, e eram exatamente os que a F5 §7.5 nomeou como o que impedia o 10.**
1. **A fórmula do early-stop.** A F5 escreveu, textualmente, *"o que NÃO ficou provado: a fórmula interna de `delta`… a melhor taxa de acerto foi 20 de 369 = 5,4%… ela vai para o teto T"*, e sugeriu à F5.4 a leitura de "uma linha de código". A leitura foi feita: `total_point_gps` conta **FOLHAS**, não pontos (`surrogate_treedGP.py:49`) — a razão pela qual as 9 famílias falharam. Com a grandeza certa, `delta(k) = c(k)+c(k−1)` reconstrói **338/369 iterações elegíveis (91,6%)**, 235/235 sentinelas, **573/604 = 94,9%**, **52/54 células a 100%**, com os 31 desencontros concentrados nas mesmas 2 células de sempre e pela mesma causa declarada. E a divergência 🟠 B15.8 fica **inteiramente caracterizada**: é o critério do paper com persistência de 2 e piso de 6, custa **3.162 de 30.804 gerações (10,3%)**, e a janela de 2 **guarda contra uma não-monotonicidade real** (5/54 células em que `c(k)` volta a >0 após zerar).
2. **O determinismo bit-a-bit.** A F5 escreveu *"só re-execução bit-idêntica de ① e ③ prova"*. O smoke T11 preservado É essa re-execução: **①②③⑦ byte-idênticas** (sha256) entre 26/07 e 31/07, tempdir diferente, HEAD com 3 alterações no runner. A disciplina D62/DI-28.3 está provada, e de quebra a não-perturbação da T11 está provada **para este config, na ③ inteira**, não só na ①.
3. Somam-se, sem mover o score sozinhas: a âncora 51i−1 agora lida no fonte (A18), o `max_depth` e a árvore re-confirmados em 125/125, e a causa da única célula excluída **corrigida em código** (DI-42.1) com a célula nomeada no comentário.

**(−0,5) UM ASPECTO CLASSE (3) NOVO, criado pela própria T11.** `tempo_aval_real_s` no offline (§2.18): campo preenchido com uma grandeza que não é a que o nome declara, contrariando a decisão I-02 que o criou, com a trava sendo um teste que lê o texto do fonte e nunca o comportamento. Impacto no mecanismo: **zero** (①②③④⑦ bit-idênticas). Impacto na fidelidade: **destrói a assinatura discriminativa de A01** — o `0.0` em 54/54 que provava "nenhuma FE real na busca". Causa medida, conserto de 1 linha.

**Por que não sobe para 10:** (i) o (3) acima; (ii) 3 itens no teto T, um deles **novo** (o checkpoint DI-43, ligado ao c311 e nunca exercido no meu corpus); (iii) a célula `swap_big-mvns_MMF16_20` continua sendo dado ruim — a **causa do mascaramento** foi corrigida, o **dado** não (está na lista V2 do autor); (iv) o `n_sigma_valido` continua sem produtor; (v) o tier small roda **fora do envelope de projeto do paper** — caveat científico que o D97 precisa carregar, e que o próprio artigo antecipa.

**Por que não desce:** o mecanismo do TGPR-MO permanece **reconstruído dos dados e fechado**: árvore 125/125, colocação de GP 99,5% em 1.495 pares, predição composta 100% × 4,6e-5, congelamento bit-idêntico 54/54, ⑦ recomputada exata e ligada bit-a-bit 54/54, ledger fechado 54/54. **Zero (3) no laço do algoritmo.** O único (3) é telemetria do ⑤.

---

## 8. Aspectos classe (3) 🎯 — evidência completa

**A25 · `tempo_aval_real_s` do regime offline mede o laço de injeção do dataset, não avaliação real.**

| campo | conteúdo |
|---|---|
| **O que se esperava** | `NULL`. A decisão I-02 (commit `83dbdac`) é explícita: *"os 4 runners OFFLINE + o stub passam a declarar NULL em vez de `0.0`… no offline o orçamento nasce ESGOTADO (a ① é o dataset, D90) e nenhuma avaliação real acontece dentro do run, então zero afirmaria que avaliar custou zero"*. E `budget.py:192-202`: *"A property devolve **None quando NENHUMA avaliação passou pelo portão**"*. |
| **O que se observou** | `timing.tempo_aval_real_s = 0.0003` no ⑤ do smoke T11 do c311. Nem NULL, nem 0.0. |
| **A prova de que é o laço de injeção** | 5 configs offline Python do smoke: N=61 → 0,0001 (b5m e moead_media, **problemas diferentes, mesmo valor**) · N=309 → 0,0003 (**c311 e b5r, problemas diferentes, mesmo valor**) · N=50.000 → 0,0572. Custo por linha: 1,64 / 0,97 / 1,14 µs — **constante em 3 ordens de grandeza de N e invariante ao problema**. Uma avaliação real de BBOB_F17 e de BBOB_F22 não custaria o mesmo; um laço de N lambdas triviais, sim. |
| **A causa, em código** | `src/standalone_harness.py:653`, dentro de `load_offline_budget`: `bud.evaluate(X[i], lambda _x, _f=F[i]: _f)` — N chamadas ao portão com um `true_f` que apenas devolve a linha do artefato. `src/budget.py:249-251` cronometra exatamente esse `true_f(x)` e incrementa `_n_avals_cronometradas`, que por isso nunca é 0 e a property nunca devolve `None`. |
| **Por que passou** | `tests/test_g7_instrumentacao.py:118-127` (`test_os_offline_declaram_NULL_em_vez_de_zero`) faz `assertNotIn("tempo_aval_real_s=0.0", src)` sobre o **TEXTO** de 5 arquivos. Nunca roda um offline; nunca lê o valor. É o antipadrão que a própria T11 diagnosticou (§4.2.1: *"Um gate que nunca reprova é decorativo"*). |
| **Impacto medido** | Mecanismo: **zero** — ①②③④⑦ do smoke são byte-idênticas às da s42. Fidelidade: **destrói a assinatura de A01** (`tempo_aval_real_s == 0.0` em 54/54 era a evidência direta de "zero FE real na busca") e substitui um zero inofensivamente errado por uma medida enganosamente errada que **escala com N**, dando aparência de trabalho de avaliação proporcional ao dataset. |
| **Recomendação à F5.4/F5.7** | (a) não cronometrar a carga (`FEBudget.evaluate(..., cronometrar=False)` ou caminho de carga dedicado); (b) trocar o teste de texto por um **comportamental** que rode um offline e afirme `is None`; (c) enquanto não corrigido, **nenhuma análise de tempo pode usar `tempo_aval_real_s` dos configs offline** — nem para custo, nem para o heatmap da F5.2d. |
| **Verificabilidade** | **direta** — 5 medições independentes no smoke T11 + 2 leituras de código com arquivo:linha. |

**Achados menores, NÃO classe (3), registrados para o autor:**
1. **Residual do B1 no ⑥.** A DI-42.1 corrige o ⑤; `experiments.py:237` grava o footer do despachante no `finally`, **antes** da mescla, ainda com `status='ok'`. Medido na célula reprovada: dois footers, `failed` (runner) e `ok` (despachante). Baixa severidade (o ⑤ é o que os gates leem; a DI-13.1 documenta os 2 footers; o footer do runner carrega a verdade), mas a assimetria ⑤×⑥ deveria ser fechada ou declarada.
2. **`n_sigma_valido` sem produtor.** O aviso do I-11 está no ⑤ de toda célula futura; a coluna não existe em nenhum código nem no CSV oficial. Forneço o cálculo em `t05_sonda_regua.csv`.

---

## 9. Teto T, armadilhas e o que este corpus NÃO permite verificar

**T — o que continua exigindo código ou re-execução (3 itens; a F5 tinha 4):**
1. **Kernel e otimização do GP local** — Matérn 5/2 ARD sem White/normalizer/priors, `optimize('bfgs')` único sem restarts, `jitchol`. Lido no vendor, **invisível nos dados**: μ/σ não fingerprintam kernel sem reconstruir o GP sob GPy 1.9.9/py3.8.
2. **`selection_type='mean'` / não-consumo do σ** — contrafactual logicamente inverificável ex-post.
3. **🆕 O checkpoint DI-43** — ligado ao c311, cadência 25 iterações OU 30 min, **não disparou** no smoke (I_eff=4, 8,55 s) e inexistente na s42 (pré-T11). Nas células big (I_eff até 99) dispararia até 3×. A T11 declara bit-identidade travada por teste; **eu não a medi**.

**SAÍRAM do teto nesta rodada:** a **fórmula do `delta`** (lida no fonte + reconstruída a 91,6%) e a **disciplina de RNG** (re-run bit-idêntico de ①②③⑦). **Fortemente reduzida:** a não-intrusão do patch de σ — a leitura de `_patched_predict` mostra μ produzido pelo código do stock com a variância vinda da mesma chamada GPy, e o teste `test_sonda_NAO_perturba_a_busca` roda de verdade; falta apenas o run pareado formal SEM o patch.

**O que este corpus NÃO permite verificar:**
- **Nada sobre valores no smoke de outros regimes:** o smoke T11 do c311 é **1 célula, tier small, D=10, M=2, LHS**. Todo o comportamento medido no big (o regime em que o algoritmo se justifica) vem exclusivamente da s42 pré-T11 — válida para o mecanismo (vendor intocado, ③ bit-idêntica), **não** para a instrumentação nova.
- **O checkpoint, o `teto_s`/`_TetoWall` e o caminho de aborto** — nenhum foi exercido.
- **A campanha em escala** (695 células × 30 sementes): concorrência, disco, memória e teto em escala são outro regime (razão 4 do §8 do handoff).
- **O re-run da célula reprovada** — o mascaramento foi corrigido; o dado, não. Ela está na lista V2 do autor.
- **A comparação com o artigo** permanece **incomensurável** em 25/25 problemas; só as 3 âncoras direcionais valem.

**Armadilhas confirmadas (as 21 da F5 revalidadas; as NOVAS e as CORRIGIDAS):**
22. **🆕 `tempo_aval_real_s` do offline NÃO é tempo de avaliação** — é o laço de carga do dataset, ~1,1 µs/linha. Nunca usar em análise de custo.
23. **🆕 `total_point_gps` do vendor conta FOLHAS, não pontos** (`surrogate_treedGP.py:49`). Foi essa palavra que custou à F5 nove famílias de reconstrução e um item de teto.
24. **🆕 `c(k)` (folhas visitadas sem GP) NÃO é monotônico** — volta a >0 em 5/54 células. Qualquer análise que assuma monotonicidade da cobertura da população erra.
25. **🆕 A pasta `evidencia_T11/smoke_python/` EXISTE** e o `LEIA-ME.md` da própria pasta diz que não. Não confiar no LEIA-ME; listar o diretório.
26. **🆕 A T11 corrigiu o ⑤ do B1 mas não o ⑥** — a triagem por `status` continua proibida também pós-T11; use `motivo_parada` + `motivos_parada.json`.
27. **CORRIGE a armadilha 6 da F5** — eu escrevi *"`motivo_parada` não existe no c311"*. Existe: é gravado **quando não-nulo** (`standalone_harness.py:1275`), e é o campo que denuncia a célula reprovada. A leitura certa: ausente = fim normal; presente = classificar via `motivos_parada.json`.
28. **CORRIGE a nota do lattice** — o máximo de população em M=3 é **105** (8 células), não 104.
29. **CORRIGE a 3ª vitória da ⑦** — DTLZ1, não DTLZ7.
30. **CORRIGE o mínimo do `n_sigma_valido`** — 100 (`swap_big-lhs_WFG9` obj1), não 105 (que é o obj0; o AVISO do ⑤ cita 105 e está certo para o objetivo que cita).

---

**Artefatos desta análise** — todos em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/c311/`, **único local de escrita usado**; `resultados_experimentos/`, `evidencia_T11/`, `data/`, `src/`, `algorithms/` e os bundles permaneceram **READ-ONLY** (nenhuma célula rodada, `experiments.py` nunca invocado):

- `t01_delta_formula.py` → `t01_delta_iter.csv` (604), `t01_delta_cel.csv` (54) — **a reconstrução da fórmula do early-stop**
- `t02_universal.py` → `t02_universal.csv` (73 colunas × 54) — bateria universal + F1–F4 + C3/C8/C11
- `t03_u2_u7_correcoes.py` → `t03_u2_u7.csv` (54) — proveniência bit-exata e timing ULP-relativo
- `t04_criterio_paper_vs_codigo.py` → `t04_criterio.csv` (54) — **o custo da divergência B15.8**
- `t05_sonda_regua.py` → `t05_sonda_regua.csv` (125) — **a coluna `n_sigma_valido` que o I-11 pede e ninguém produz**
- `t06_arvore_banda.py` → `t06_arvore.csv` (125), `t06_incrementos.csv` (963) — C1/C2/F4
- `t07_evidencia_t11.txt` — os sha256 do re-run bit-idêntico, os 5 `tempo_aval_real_s`, os campos novos do ⑤ e a assinatura do B1

Insumos pré-computados consumidos sem recomputação: `f5/sonda_f52e.csv`, `f5/contrato_f52b.csv`, `f5/integridade_f52a.csv`, `f5/tempo_f52d.csv`, `f5/trajetorias/*_c311_*_42.json` (54), `f5/transversal_offline_camada7.csv`, `f5/transversal_big_ablacao.csv`. Artefatos da F5 citados e não refeitos: `f5/baterias/c311/b07_predicao.csv` (A21), `b10_tempo_sonda.csv` (A23), `b11_folha_iter.csv` (A20).

Interpretador: `/Users/gmello/Documents/python_venvs/mestrado_experimentos_dissertacao/bin/python` (pandas 2.3.3, pyarrow, **sklearn 1.9.0 ≠ sklearn 1.1.2 do run** — diferença declarada e responsável pelos 31 resíduos de A19 e 7 de A20, ambos nas mesmas 2 células).