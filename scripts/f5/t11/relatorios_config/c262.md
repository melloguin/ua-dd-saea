# RELATÓRIO DE FIDELIDADE T11 — c262 qNEHVI (Python/BoTorch) · protocolo v1.1 · formato RICO

## ⚠ ERRATA 0 — a premissa do meu próprio briefing está errada: **o smoke Python do c262 EXISTE**

O briefing, o `handoff/T11-RODADA-FINAL.md` §15.2 e o `evidencia_T11/LEIA-ME.md` afirmam os três que os smokes Python rodaram em tempdir removido e **não foram preservados**. Medido:

```
/Users/gmello/Documents/python_repos/mestrado/evidencia_T11/smoke_python/experiments/
  main/{c122,c149,c154,c262,e81} · batch/sobol_batch · off/{b5m,b5r,c311,moead_media}
  · sweep-big-mvns/treed_media          →  11 células, 49 parquets
```

Provenência conferida por mim: `exp_main_c262_MMF1_0.manifest.json` traz `repo_hash = 9ad0138aa9a464a7d9f89d247f81092f8c4ef26f`, que é o **HEAD do repo** (`git rev-parse HEAD`), commitado em **2026-07-31 10:13:57**; os arquivos foram escritos em **2026-07-31 10:47:28** — **34 minutos depois do commit que escreveu o §15.2 dizendo que eles não existiam**. `campanha_id = 9ad0138aa9a4_2026-07-31`, `schema_version = 2`, `status = ok`.

**Consequência para esta análise:** as correções da T11 neste config **não são "código lido"** — são **dado medido em artefato pós-fix**. Rodei os 6 portões eu mesmo sobre ele: `G-1 3⇄① OK 40/40 bit-idênticos (max|ΔX|=0)` · `G-2 header=1 footers=1 malformadas=0` · `G-3 campanha=9ad0138aa9a4_2026-07-31` · `G-4 6/6 camadas` · `G-7 ⑥ 8/8 campos · ⑤ 6/6 chaves` · `B-15 O-22 OK`.

Esta é a **errata nº 16/17** da campanha (o §3 do handoff titula "AS 15 ERRATAS" e lista 15 linhas, mas o §15.4 cita uma "ERRATA 16" que não está na tabela e o §15.7 diz "16 erratas" — a tabela está incompleta).

---

## 1. Ficha do mecanismo

qNEHVI = **MOBO por aquisição de hipervolume ruidosa**. Um `SingleTaskGP` independente por objetivo (`ModelListGP`), kernel **Matérn 5/2 ARD com gamma-prior** (🔵 D30 — restaura o paper contra o default moderno RBF; toca a incerteza), `Standardize(Y)`, **`train_Yvar = 1e-6` FIXO** (D40/B8.6a ⇒ **NEHVI colapsa em EHVI**, maquinaria *noisy* dormente — achado justo §16, amparado pelo §H.7 do próprio artigo). Aquisição **`qLogNEHVI`** (variante log, E.2) com `SobolQMCNormalSampler(128, seed=h1)`, `prune_baseline=True`, `alpha=0` (partição exata, M≤4), `cache_root=None`. Maximizador: `optimize_acqf` multi-start L-BFGS-B **10 restarts / 512 raw / maxiter 2000**, `return_best_only=False` ⇒ **10 candidatos por iteração na ③** (DI-10) e infill = argmax do α. **Refit do zero por iteração** (D44, MAP). Sem população: a ② é o dataset acumulado. Ref-point da aquisição = `nadir + 0,1·(nadir − ideal)` da tabela **S.5 CONGELADA** — **informação de oráculo declarada como vantagem informacional (D73)**, em contraste deliberado com o nadir observado do c149 (D96). Kernel C++ fusionado **OFF explícito** (DEF-L2/DI-05).

**Divergências sancionadas nomeadas:** 🔵 **D30** kernel RBF→Matérn 5/2 ARD (SPEC:434 — a única do config que toca o mecanismo que a tese mede) · **D40/B8.6a** `train_Yvar=1e-6` (rota (b) rejeitada) · **E.2** qNEHVI→qLogNEHVI (fidelidade de mecanismo, não bit-a-bit) · **E.2** restarts/raw 10/512 (o paper não os reporta) · **D63/Anexo J** DoE 11D−1 LHS ≠ 2(d+1) Sobol · **D21/D61** orçamento 31D−1 com hard-stop · **D73** ref de oráculo · **DEF-L2/DI-05** fused OFF.

**Corpora.** (A) **s42 = 21 células main**, 5.201 decisões, 5.314.010 linhas de ③, 2.631 blocos de sonda — o corpus do MECANISMO. (B) **smoke T11 = 1 célula** (MMF1/s0, D=2, 61 FE, 41 decisões, 44.410 linhas de ③) no HEAD — o corpus da INSTRUMENTAÇÃO NOVA. Excluídas e citadas: 3 ⚪ main + 5 ⚪ batch q10 + 1 🔴 `main/WFG1`.

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.0 Tabela-resumo (35 aspectos)

| # | aspecto | classe | verificab. | resultado-síntese (s42 21 células · smoke T11) | vs F5 |
|---|---|---|---|---|---|
| **U1** | orçamento FE = 31D−1 | (2) D21/D61 | direta | 21/21 exato; `fe_index` denso E ordenado 21/21 · smoke 61=31·2−1 | = |
| **U2** | DoE 11D−1 LHS do artefato | (2) D63/J | direta | 21/21; `max\|ΔX\|` **0,0** com cast float32 (**1,91e-6 em float64** — ver E2) | ✎ |
| **U3** | 1 fit/iteração, do zero | (1) | direta | ④=⑥=`fit_series`=**5.201** em 21/21 · smoke 41/41 | = |
| **U4** | cadência da sonda | (2) DI-12.5 | direta | fórmula exata 21/21; **2.631** blocos · smoke 22/22 | = |
| **U5** | join posicional sonda×gabarito | (2) §17.2.2 | direta | s42 `max\|Δx\|`=0,0 · smoke **1,19e-7** (eps-float32) | = |
| **U6** | WAPE + cobertura | (1) | direta | ΔWAPE mediano **−8,58%**, 38/46 melhoram; cobertura **0,9763**; **0 σ-NaN** | = |
| **U7** | invariante de timing | (2) §17.6/DI-13.10 | direta | inv-1 **5.201/5.201**; inv-2 **2.631/2.631**; **0** falso-positivo em 2.570 | = |
| **U8** | `fe_treino_max` monotônico | (1) | direta | não-decrescente **21/21** | = |
| **U9** | guards ≡ agregados | (2) DI-13 | direta | 0 linhas jsonl ruins; footer duplo 21/21; `n_retries`=0 21/21 | = |
| **U10** | aritmética entre camadas | (2) | direta | `n_ger=(fe−n_init)+cache+1` **21/21**; ②=ng+1 **21/21** · smoke 42=41+1 | = |
| **U11** | erro de fantasia nos infills | (1) | direta | cobertura mediana 0,9227; corr μ×f até 0,99998 | = |
| **F1** | 🔑 escolhido = argmax do α | (1) | direta | **5.201/5.201 BIT-A-BIT** (Δ=0,0) · smoke **41/41** Δ=0,0 | = |
| **F2** | 🔑 elo ⑥→③→① | (2) DI-10 | direta | última das 10 em **5.180/5.180**; `\|Δx\|`③×①=**0,0** · smoke 40/41 + G-1 40/40 | = |
| **F3** | 🔑 ordem da ③ (**I-12**) | (2) DI-10/I-12 | **direta + DECLARADA** | regra agora no `sigma_dict`; violações **277/7.111 (3,90%)** × identidade 42,99% × aleatória 64,97% | 🆕⬆ |
| **F4** | 🔑 identidade fechada do ruído | (2) D40+E.2 | direta | **11.220/11.220**, err rel máx **1,43e-7**, piso ativo 47,7% · smoke **80/80**, 9,97e-9 | = |
| **F5** | determinismo do refit | (1) | direta | pares HP idênticos ⟺ cache-hit: **21/21** (40/5.180) | = |
| **F6** | ARD vivo | (1) | direta | **11.266/11.266** · smoke **82/82** | = |
| **S1** | kernel Matérn 5/2 ARD | (1) 🔵 D30 | **direta (objeto)** | `MaternKernel(nu=2.5, ard_num_dims=D)`+`GammaPrior(3,6)`/`(2, 0.15)` — probado no objeto | ⬆ |
| **S2** | `train_Yvar=1e-6` ⇒ EHVI | (2) D40/B8.6a | direta | 21/21 + identidade F4; σ>0 em 100% das 5,3 M linhas | = |
| **S3** | `qLogNEHVI` | (2) E.2 | direta | α<0 em **3.575/5.201 (68,7%)** · smoke **41/41** negativo | = |
| **S4** | MC = 128 qMC | (1) ✓ paper G.2 | direta-declar. | 128 em 21/21 e no smoke | = |
| **S5** | `prune_baseline=True` | (1) ✓ paper fn.4 | indireta forte | identidade com a caixa do ref **5.179/5.180** | = |
| **S6** | ref = S.5 congelada (oráculo) | (2) D73 | direta | ≡`metrics.reference_bounds` **bit-a-bit 21/21**; álgebra Δ=**0,0** | = |
| **S7** | 10 restarts / 512 raw / 2000 | (2) E.2 | direta-declar.+corrob. | 10 valores de α e 10 linhas ③ em **5.201/5.201** | = |
| **S8** | `Standardize` + `cache_root` | (2) L.10 | **direta (objeto)** | `Standardize` PROVADO no objeto; **`cache_root=None` resolve para `_cache_root=True`** (ver E1) | ✎⬆ |
| **S9** | backend + fused OFF + pin | (2) N.2/DEF-L2/D79 | direta-declar. | botorch 0.18.1, sha256 idêntico 21/21; fallback do fused **reproduzido por mim** | ⬆ |
| **S10** | sinal/domínio + RNG | (2) §5.5/D62 | direta | x nativo 21/21; `h1≠h2` 5.201/5.201 | = |
| **S11** | hard-stop + cache-hit | (2) D61/D57/D89 | direta | 21 hard-stops; 40 cache-hits (ZDT1 39 + ZDT6 1) | = |
| **S12** | `params` no ⑤ | (2) F5.2b | direta | **s42: 0/21** (21 linhas em `contrato_f52b.csv`) → **smoke: PRESENTE** | 🆕✅ |
| **S13** | teto wall-clock | (2) DI-38a→**DI-43/44** | direta | 0 disparos nas 21; **8 ⚪ + 🔴 com ZERO parquets**; critério de projeção **removido** | 🆕⬆ |
| **N1** | 🆕 guard `fit_retries` | (2) T11-A4 | direta (código+dado) | implementado `c262_qnehvi.py:641-655`; **0 disparos** (0 retries em 5.201+41) | 🆕 |
| **N2** | 🆕 truncamento-com-dado + checkpoint | (2) DI-43/44/G5 | direta | `checkpoint` no ⑤ e `n_checkpoints` no footer do smoke; **0/21 na s42** | 🆕 |
| **N3** | 🆕 proveniência (campanha/repo/schema) | (2) G3/I-09 | direta | s42 `repo_hash=None`, `schema_version=1` → smoke sha real + v2 | 🆕 |
| **T1** | hp internos da acqf | **T→(2)** | direta-declar.+**controle** | `acqf_hp` 10 campos; **7/10 confirmados no objeto BoTorch** | 🆕✅ |
| **T2** | **sequential greedy q>1** | **T** | não-verificável | q=1 em 21/21; batch q=10 **abortado 5/5** | = |

Legenda: `=` reproduz a F5 · `✎` corrige a F5 · `⬆` verificabilidade sobe · `🆕` aspecto novo · `✅` teto/lacuna fechada.

---

### 2.1 — U1 · Orçamento e contabilidade do FE

**(a) O artigo.** Regime *expensive*, ~200–224 FE por run (Figs. 3/4, §G.2); o orçamento **é** o critério de parada, sem hard-stop declarado.
**(b) A SPEC.** `maxFE = 31D−1` com hard-stop exato (D21/D61, `BudgetExhausted`), uniforme nos 25 problemas — protocolo transversal do estudo, não do paper.
**(c) Observado.** `len(①) == maxfe == manifest.fe_final == footer.fe_final == 31D−1` em **21/21** (Σ 7.946 FE); `fe_index` denso **E** ordenado 0-based em 21/21. D ∈ {2,7,10,12,22,30}, maxFE ∈ [61, 929]. No smoke T11: 61 = 31·2−1, `fe_final = maxfe = 61`, footer `hard_stopped=True`.
**(d) Mecanismo.** O `Budget` intercepta a avaliação; candidato inédito com saldo zero ⇒ decisão `caminho='hard_stop'`, sonda obrigatória, fecha. Nada muda com a T11.
**(e) Veredito.** **(2)** D21/D61 · **direta**.

### 2.2 — U2 · DoE inicial · ✎ correção da minha F5

**(a) O artigo.** §G.2, verbatim: `2(d+1)` pontos de uma sequência Sobol *scrambled* — 14 pontos para d=6 (verificado no .md do artigo, offset 214.062).
**(b) A SPEC.** `11D−1` pontos **LHS carregados do artefato** `data/doe/{problema}/doe_{problema}_42.parquet` (D63/Anexo J), nunca regenerados, idênticos entre os 16 algoritmos — é o que torna a comparação transversal justa (para d=6 seriam 65, 4,6× o paper).
**(c) Observado.** `(fase=='init').sum() == 11D−1` em **21/21** (Σ 2.806). O diff contra o artefato: com cast **float32** dá `max|ΔX| = 0,0` em 21/21; **em float64 puro dá 1,907e-6**. ✎ **A F5 escreveu "0,0 bit-a-bit (float32)" sem explicitar que a igualdade exige o cast** — o artefato é float64 e a ① é float32 (`pq.read_schema`: `x0: float`). O resíduo é exatamente a quantização float32 (D53), não perda de identidade.
**(d) Mecanismo.** O adapter carrega o parquet e injeta o bloco como FE consumidos; o export grava float32 sem arredondamento.
**(e) Veredito.** **(2)** D63/Anexo J · **direta** (com a ressalva de dtype agora explícita — é a mesma armadilha da regra 11 do CONTRATO).

### 2.3 — U3 · Um fit por iteração, do zero

**(a) O artigo.** §9: modela cada saída com um GP independente de kernel Matérn 5/2 ARD e infere os hiperparâmetros por MAP (verificado, offset 38.783). O loop BO padrão reajusta a cada iteração.
**(b) A SPEC.** D44 explicita **refit *from scratch*** (re-MAP, sem warm-start) — não é desvio, é a explicitação do que o paper faz.
**(c) Observado.** `len(④) == n_geracoes == len(fit_series) == #rec 'timing'` em **21/21** (Σ **5.201**); no smoke, 41 = 41 = 41.
**(d) Mecanismo.** Novos `SingleTaskGP` por iteração sob `torch.manual_seed(h(run,it))`; `fit_series` e `timing` saem do mesmo callback ⇒ a reconciliação tripla exclui fit escondido ou reaproveitado.
**(e) Veredito.** **(1)** conforme o artigo · **direta**.

### 2.4 — U4 · Cadência da sonda

**(a)/(b)** A sonda não existe no paper: é instrumentação deste estudo (§17.2.2/DI-12.5) — 2.000 pontos fixos, predição em `g==1 ∨ g%2==0` **e sempre na última geração** (finalProbe).
**(c) Observado.** O conjunto de gerações com `rec='sonda'` bate **exatamente** com a fórmula em **21/21** (Σ **2.631** blocos). Só ZDT1 (ng=640) e ZDT6 (ng=202) têm ng par ⇒ **o finalProbe foi exercitado 19 vezes**. No smoke: 22 blocos, 44.000 linhas = 22 × 2.000, com ng=41 ímpar ⇒ finalProbe exercitado.
**(d) Mecanismo.** No ramo do hard-stop o runner emite a sonda **antes** de propagar a exceção.
**(e) Veredito.** **(2)** DI-12.5 · **direta**.

### 2.5 — U5 · Join posicional sonda × gabarito

**(a)/(b)** O `sigma_dict` declara a régua na ORDEM do artefato, join **por posição** (regra 5 do CONTRATO §10).
**(c) Observado.** s42: `max|Δx| = 0,0` em 21/21. Smoke T11: **1,1920929e-7** = eps-float32 exato — mesma causa da U2 (o gabarito é float64, a ③ é float32). Ordem preservada; nenhum `sort` intermediário.
**(e) Veredito.** **(2)** §17.2.2/DI-09 · **direta**.

### 2.6 — U6 · WAPE e cobertura (o surrogate aprende?) · ✎ correção de cauda

**(a) O artigo.** §2 apoia a EHVI na incerteza preditiva bem-calibrada do GP.
**(b) A SPEC.** Definição congelada §5 do protocolo: WAPE por objetivo no espaço CRU (`transf_tipo` é NULL em 100% das linhas ⇒ já é o cru) e cobertura = fração com `|μ−f| ≤ 1,96σ`.
**(c) Observado (46 célula-objetivo, 2.631 blocos, insumo `f5/sonda_f52e.csv`).** ΔWAPE 1º→último bloco: **mediana −8,58%**, **38/46 melhoram**. Campeões: ZDT6-obj0 **−91,3%** (0,0882→0,0077), DTLZ4-obj1 −76,0%, DTLZ4-obj2 −69,9%, WFG2-obj1 −59,8%. Cobertura final **mediana 0,9763**, **40/46 ≥ 0,90**. **`n_nan = 0` e `n_validas = 2000` em 100% dos blocos**. ✎ **A lista de piores da F5 tem um erro de cauda**: os 6 abaixo de 0,90 são DTLZ3-obj0 0,8360 · BBOB_F17-obj1 0,8585 · MMF11_L-obj1 0,8590 · **DTLZ1-obj1 0,8885** · BBOB_F49-obj0 0,8895 · DTLZ3-obj1 0,8960. A F5 omitiu DTLZ1-obj1 e listou BBOB_F22-obj0 (0,9040, que está **acima** de 0,90). A contagem-título (40/46) é idêntica.
**(c') Smoke T11 (MMF1/s0).** obj0: WAPE 0,01484 → 0,00508, cobertura 0,9525 → 0,9660. obj1: WAPE 0,92686 → 0,51132, cobertura **0,5630 → 0,9110**. O GP aprende e a calibração *melhora* ao longo do run — a mesma assinatura da s42, num artefato pós-T11.
**(d) Mecanismo.** O `train_Yvar=1e-6` colapsa o posterior **nos observados**, mas a sonda mede **fora** do suporte: a calibração medida é a do GP genuíno, não artefato do nugget.
**(e) Veredito.** **(1)** conforme o artigo · **direta**.

### 2.7 — U7 · Invariante de timing

**(a)/(b)** `CONTRATO §17.6` (DI-13.10): `tempo_geracao_s` **exclui** a sonda ⇒ (inv-1) `fit+busca ≤ tempo_geracao_s` sempre; (inv-2) `fit+busca+sonda > tempo_geracao_s` **exatamente** nas gerações com sonda.
**(c) Observado (re-medido).** inv-1: **5.201/5.201**. inv-2: **2.631/2.631**, com **0 falsos-positivos** nas 2.570 gerações sem sonda. `tempo_pred_sonda_s > 0` em 2.631/2.631 dentro e `== 0` em 2.570/2.570 fora.
**(e) Veredito.** **(2)** §17.6/DI-13.10 · **direta**.

### 2.8 — U8 · `fe_treino_max` e o treino = dataset inteiro

**(a)** BO padrão ajusta o GP a todo `Dn`; nem janela nem esquecimento. **(b)** DI-09/A1 exige o carimbo; a regra 9 do CONTRATO avisa que ele é **não**-monotônico em b1/b4/c217 — no c262 tem de ser monotônico. **(c)** Série **não-decrescente em 21/21**, passo +1 por infill. **(d)** `train_X` é o dataset inteiro; o carimbo é `n_train − 1`. Prova por dado que não há subamostragem — contraste direto com a família NN-ensemble. **(e)** **(1)** · **direta**.

### 2.9 — U9 · Guards ≡ agregados e integridade

**(c) Observado.** **0 linhas jsonl não-parseáveis** em 21/21; footer **duplo** em 21/21 (runner + despachante, não duplicação); `n_retries = 0` e `fallback_ativado = False` em 21/21; guards emitidos = `hard_stop` (21) + `cache_hit` (40). `integridade_f52a.csv`: **ZERO linhas de c262**. **(e)** **(2)** DI-13 · **direta**.

### 2.10 — U10 · Aritmética entre camadas (o off-by-one do config)

**(c) Observado.** `n_geracoes == (fe_final − n_init) + cache_hits + 1` em **21/21** (ZDT1: 640 = (929−329) + 39 + 1). A ② tem **ng+1 blocos em 21/21** — a lei `②(0) ≡ ②(1) = n_init`, `②(g) = n_train(g)`. No smoke: **42 blocos para 41 gerações** — o off-by-one canônico se reproduz idêntico no HEAD. **(e)** **(2)** · **direta**.

### 2.11 — U11 · Erro de fantasia nos infills

**(c)** Cobertura ±1,96σ nos 5.180 infills: mediana **0,9227**. Correlação μ×f positiva e alta em 20/21 células (até 0,99998); o único negativo é DTLZ4-obj0 (−0,162), cujo obj1/obj2 dão +0,85/+0,73 ⇒ é comportamento (α=100 concentra as amostras), **não sinal trocado**: dupla negação daria erro ~2|f| e correlação ≈ −1 em todos os objetivos. **(e)** **(1)** · **direta**.

---

### 2.12 — F1 · 🔑 QUERY-JOIA 1: o escolhido **É** o argmax da aquisição

**(a) O artigo.** §2: a otimização numérica encontra o maximizador da aquisição; §7: o SAA torna a aquisição determinística sob base samples fixos, otimizada por L-BFGS-B multi-start.
**(b) A SPEC.** DI-10 obriga logar `acqf_todos_restarts` (a paisagem da aquisição) e `acqf_escolhido`; L.10 usa `return_best_only=False`, que **expõe** os 10 valores e torna a decisão auditável.
**(c) Observado.** `acqf_escolhido == max(acqf_todos_restarts)` **bit-a-bit**: `|Δ| = 0,0` em **5.201/5.201** decisões da s42 (incluídos os 21 hard-stops), `n_restarts == len(acqf_todos_restarts) == 10` em 5.201/5.201. **No smoke T11 (HEAD): 41/41, Δmáx = 0,0.**
**(d) Mecanismo.** `optimize_acqf(..., return_best_only=False)` devolve `(cands, acq_vals)`; o runner faz `best = argmax(acq_vals)` e grava `float(acq_vals[best])`. As duas grandezas saem do MESMO array na MESMA iteração ⇒ igualdade exata por construção — e é por isso que é prova forte: qualquer re-otimização, re-avaliação ou fallback silencioso entre "escolher" e "logar" quebraria a igualdade bit-a-bit. Que ela feche **também no HEAD**, com 233 testes e 56 commits de diferença, é a prova de que a T11 não tocou o laço de decisão.
**(e) Veredito.** **(1)** conforme o artigo · **direta**.

### 2.13 — F2 · 🔑 QUERY-JOIA 2: o elo ⑥ → ③ → ①

**(b)** O `sigma_dict` declara `real_solution_id` preenchido só na linha do candidato **escolhido que de fato consumiu FE**.
**(c) Observado.** A linha marcada é a **última das 10** em **5.180/5.180** gerações com FE; nas 21 de hard-stop **nenhuma** linha é marcada (D61: computado, nunca avaliado). O x da marcada contra o x da ① por `solution_id`: **`max|Δx| = 0,0` em 5.180/5.180**. `real_solution_id` NULL em 100% das 5.262.000 linhas de sonda. **No smoke:** 40/41 (a 41ª é o hard-stop), `|Δx| = 0,0`, e o **G-1 independente confirma 40/40 bit-idênticos com 0 ids órfãos**.
**(e) Veredito.** **(2)** DI-10 (instrumentação nossa; mecanismo do paper) · **direta**.

### 2.14 — F3 · 🔑 QUERY-JOIA 3 → **A CORREÇÃO MAIS IMPORTANTE DA T11 NESTE CONFIG**

**(a)/(b)** Este aspecto não existe no paper: é a semântica do nosso log. Na F5 eu **descobri e provei** que a leitura ingênua ("linha j da ③ ↔ restart j") é falsa e que a permutação real é σ = [i ≠ argmax] + [argmax]. Era um achado do analista, **vivo apenas no meu relatório** — um agente fresco reprovaria um run perfeito.
**(b') O que a T11 mudou.** A regra virou **dado**: o `sigma_dict` do manifesto ganhou a chave **`ordem_terceira_online`** (medido: **NOVA no smoke, ausente em 21/21 da s42**), com o texto normativo, e ganhou **utilitário de código** — `src/botorch_harness.py:169 restart_de_linha(n_linhas, best)`.
**(c) Observado — verifiquei a regra, não a aceitei.** Reimplementei o teste de refinamento da F5 usando **a função declarada**, não a minha hipótese: duas linhas da ③ com x quase-idêntico (restarts que convergiram ao mesmo ótimo local) têm de receber α quase-idêntico sob o mapeamento correto. Sob o critério exato da F5 (duplicata `≤1e-6·escala`, violação `>1e-6·escala`), em **7.111 pares** das 21 células:

| mapeamento | violações | % |
|---|---|---|
| **regra I-12 declarada** | **277** | **3,90%** |
| identidade (leitura ingênua) | 3.057 | 42,99% |
| permutação aleatória | 4.620 | 64,97% |

**Reprodução exata dos números da F5** — a regra que a T11 escreveu no dado é **funcionalmente idêntica** à permutação que eu havia provado de forma independente. Sob um critério **muito mais duro** (duplicata bit-a-bit em float32, violação relativa >1e-12) os 5.978 pares dão I-12 18,82% × identidade 49,23% × aleatória 68,95% — a dominância se mantém e o resíduo é o colapso float32 (D53) já dissecado na F5. No smoke T11: 61 pares, 3 × 17 × 56.
**(c') ✎ Errata contra o texto da regra.** A `ordem_terceira_online` afirma que casar por índice ingênuo dá "**89%** de falso-mismatch". Medido no c262/s42: `best == última linha` em **872/5.201 = 16,8%** ⇒ falso-mismatch **83,2%**, não 89%. A direção está certa; o número não é o deste config (deve vir do c154, onde a regra é compartilhada). O qualificador `isfinite` da regra é **inerte aqui**: 0/5.201 iterações têm α não-finito e o argmax simples e o argmax-isfinite coincidem em 5.201/5.201.
**(e) Veredito.** **(2)** DI-10/I-12 · **direta e agora DECLARADA** — a verificabilidade sobe de "descoberta pelo analista" para "declarada no dado + utilitário versionado + verificada por dois critérios".

### 2.15 — F4 · 🔑 QUERY-JOIA 4: a identidade fechada do ruído sob `Standardize`

**(a) O artigo.** §9, verificado no .md (offset 38.918): para todos os problemas assume-se que as variâncias de ruído são **observadas** (exceto ABR) — o ruído é fixado, não inferido.
**(b) A SPEC.** DEF-B8.6 fechada em **D40 rota (a)**: `train_Yvar = 1e-6` (≡ protocolo do paper com ruído verdadeiro ≈ 0 no nosso benchmark determinístico). O bundle registra o detalhe crítico: o `Standardize` divide por std². Isso gera uma identidade fechada e recomputável só dos dados.
**(c) Observado.** `noise_j == max(1e-6, 1e-6 / Var_{ddof=1}((−f_j)[:n_train]))`, com `Var` sobre as PRIMEIRAS `n_train` linhas da ① em ordem de `fe_index`: fecha em **11.220/11.220 objetivo-iterações**, erro relativo máximo **1,429e-7**. Piso 1e-6 do gpytorch **ativo em 5.347/11.220 (47,7%)**. **No smoke T11: 80/80, erro relativo máximo 9,97e-9.** Corroboração independente no objeto: com dados de brinquedo padronizados, `likelihood.noise = 2,937e-6` — exatamente `1e-6/Var(Y_std)`.
**⚠ Armadilha reencontrada:** o `modelo_hp` é **aninhado** (`{mll_final, por_objetivo:[…]}`). Minha primeira query devolveu **0/0** por ler o nível errado — é literalmente a **ERRATA 6 da própria T11** ("`mll_final` ausentes → ANINHADOS em `modelo_hp`; o gate aferia POSIÇÃO, não PRESENÇA"). Registro para quem reler.
**(d) Mecanismo.** Uma query prova cinco coisas: (i) `train_Yvar` é 1e-6 na escala **original** (D40), não na padronizada; (ii) `Standardize(Y)` está ativo e divide por std²; (iii) o treino é **exatamente** as primeiras `n_train` linhas em ordem de `fe_index` (sem subamostragem); (iv) o sinal interno é `−f`; (v) o piso 1e-6 do gpytorch morde — origem exata das `NumericalWarning` declaradas no `warning_filter`.
**(e) Veredito.** **(2)** D40/B8.6a + E.2 · **direta** (identidade fechada — o grau mais forte disponível).

### 2.16 — F5 · Determinismo do refit

**(b)** D44: refit *from scratch*. Consequência testável: **dataset inalterado ⇒ hiperparâmetros bit-idênticos**.
**(c) Observado.** Pares consecutivos do vetor `(lengthscale_med, outputscale, mll_final)`: pares bit-idênticos ⟺ a iteração foi cache-hit. **Identidade exata em 21/21 células** — 40 pares iguais, e os 40 são precisamente os 40 cache-hits (39 ZDT1 + 1 ZDT6). Nas 19 células sem cache-hit, **0 pares iguais**. No smoke: 0 pares iguais, 0 cache-hits — a identidade se mantém trivialmente.
**(d) Mecanismo.** O `manual_seed` muda a cada iteração mas `fit_gpytorch_mll` parte dos priors e roda L-BFGS determinístico ⇒ o dataset é o único input que importa. Warm-start faria os pares de cache-hit divergirem; não-determinismo (threads/BLAS) faria NENHUM par fechar. Ambas refutadas por dado.
**(e) Veredito.** **(1)** · **direta**.

### 2.17 — F6 · ARD vivo e curva de aprendizado

**(c)** `lengthscale_max > lengthscale_min` em **11.266/11.266** objetivo-iterações da s42 e **82/82** no smoke — ARD vivo em 100% dos fits, de D=2 a D=30. A direção da evolução difere por problema: MMF1 **encolhe** (multimodal — smoke s0: [0,245; 0,135] → [0,155; 0,037]; s42: [0,318; 0,111] → [0,132; 0,043], **mesmo sentido em duas sementes**), DTLZ2/WFG5/ZDT1 alargam. **(e)** **(1)** · **direta**.

---

### 2.18 — S1 · Kernel Matérn 5/2 ARD FORÇADO · ⬆ de declarativa para DIRETA

**(a) O artigo.** §9, verbatim no .md: um GP independente por saída com **kernel Matérn 5/2 ARD**, hiperparâmetros por MAP.
**(b) A SPEC.** D30 (bússola D29, 🔵 versão→artigo): o default do `SingleTaskGP` mudou para RBF ARD sem `ScaleKernel` (priors dim-scaled LogNormal); sem forçar, desviaríamos do paper **silenciosamente**. Rota Gamma-legada, fiel ao paper, compartilhada com o e81.
**(c) Observado — evidência nova.** A F5 classificou isto como **direta-declarativa** (config-echo + ARD vivo). Instrumentei o objeto que `_build_models` de fato constrói:

```
ScaleKernel(MaternKernel)  ·  nu = 2.5  ·  ard_num_dims = D  ·  lengthscale.shape = (1, D)
lengthscale_prior = GammaPrior(3.0, 6.0)   outputscale_prior = GammaPrior(2.0, 0.15)
outcome_transform = Standardize    likelihood = FixedNoiseGaussianLikelihood(FixedGaussianNoise)
```

Isso **nomeia a classe instanciada**, não a string do log: ν=2,5 (não 1,5, não RBF), ARD com um lengthscale por dimensão, e os priors Gamma legados — a rota fiel ao paper, não a dim-scaled. Somado ao `header.params.kernel` 21/21 e ao ARD vivo 11.266/11.266.
**(e) Veredito.** **(1)** 🔵 D30 (restaura o paper) · **direta** ⬆ (era direta-declarativa na F5).

### 2.19 — S2 · `train_Yvar=1e-6` — o NEHVI que vira EHVI

**(a) O artigo.** O trabalho INTEIRO é sobre ruído (Abstract: propõe a NEHVI para superar a limitação da EHVI sob ruído). Mas o **§H.7** apresenta explicitamente o caso **noiseless** (verificado no .md, offset 123.064: a Fig. 15 mostra a qNEHVI competitiva com a qEHVI em benchmarks sem ruído).
**(b) A SPEC.** D40 rota (a): os 25 problemas são determinísticos, o "ruído verdadeiro observado" é 0, e o caminho oficial do BoTorch é `train_Yvar=full_like(Y,1e-6)`. Rota (b) (inferir o ruído) **rejeitada**. Consequência sancionada e reportável: **NEHVI → EHVI, a maquinaria noisy fica dormente** (§16). Contraste deliberado com o c154.
**(c) Observado.** 1e-06 em 21/21 e no smoke; identidade F4 confirma o valor **na escala original**. `σ > 0` em **100% das 5.314.010 linhas** da ③ (mín global 1,24e-4) — o posterior colapsa nos observados mas **não degenera** em NaN/0 fora deles (contraste direto com o c154). `warning_filter` declarado no header 21/21.
**(d) Mecanismo.** Com `Yvar` fixo e minúsculo, a integral sobre a fronteira incerta degenera na fronteira observada ⇒ αNEHVI ≡ αEHVI. **O paper prevê e ampara isso** (a NEHVI se reduz à EHVI no caso sem ruído, §5.1; H.7 mede o regime).
**(e) Veredito.** **(2)** D40/B8.6a (H.7 ampara) · **direta**.

### 2.20 — S3 · `qLogNEHVI` (variante em escala log)

**(a)** A aquisição do paper usa o operador `[·]⁺` hard (Eq. 1/3): α ≥ 0 por construção. **(b)** E.2 registra: "qNEHVI" = qLogNEHVI 0.18.1, **fidelidade de mecanismo, não bit-a-bit** (maximizadores ~equivalentes; gradiente estável, linhagem LogEI). **(c)** `params.acqf = 'qLogNEHVI L.10'` 21/21 **e a assinatura numérica confirma**: α ∈ [−53,579; +22,233], **negativo em 3.575/5.201 (68,7%)** — impossível no qNEHVI cru. 100% negativo nos problemas normalizados (ZDT/DTLZ/MMF/WFG), 0% nos BBOB de escala grande. No smoke (MMF1, escala [0,1]): **41/41 negativo**, α ∈ [−2,493; −0,296]. **(d)** `log(HVI esperada)` < 0 sempre que a melhoria esperada é < 1 na escala bruta — o sinal do α é um **teste de identidade da variante que não depende de declaração**. **(e)** **(2)** E.2 · **direta**.

### 2.21 — S4 · MC = 128 amostras quasi-MC

**(a)** §G.2, verificado verbatim no .md: todas as funções de aquisição MC usam N = 128 amostras quasi-MC. **(b)** L.10 passa o sampler explícito com seed (`SobolQMCNormalSampler(128, seed=h1)`) — o default do BoTorch para MOO já é 128, mas o explícito torna a estimativa reprodutível. **(c)** `params.mc_samples = 128` em 21/21 e no smoke; `h1` distinto por iteração e sempre ≠ `h2` (5.201/5.201). Confirmado no objeto: `acqf.sampler.sample_shape == (128,)`, `sampler.seed == h1`. **(d)** O determinismo do SAA (Teorema 1) depende de base samples fixos por iteração. **(e)** **(1)** ✓ paper §G.2 · **direta-declarativa** (com o objeto conferido).

### 2.22 — S5 · `prune_baseline=True` e o `n_baseline`

**(a)** Nota de rodapé 4 (verificada no .md, offset 29.090): para maior eficiência, pode-se podar `Xn` removendo pontos dominados com alta probabilidade, estimada por MC. **(b)** L.10 liga `prune_baseline=True`; DI-10 obriga logar `n_baseline` **pós-prune**. **(c)** `n_baseline ≤ n_train` em 5.180/5.180. Testei a identidade `n_baseline > 0 ⟺ existe ponto do treino ESTRITAMENTE dentro da caixa do ref`: fecha **5.179/5.180**. Três células têm `n_baseline = 0` em **todas** as iterações — DTLZ1 (141/141), DTLZ3 (241/241), ZDT4 (201/201) —, exatamente as três em que nenhum FE real entra na caixa do ref S.5. O único mismatch é DTLZ4 it 23 (`n_baseline=1`, 0 pontos estritamente dentro; melhor ponto a −0,001678 da fronteira, 0,15% fora). **(d)** O prune do BoTorch **não é geométrico**: estima por MC quem tem probabilidade positiva de ser não-dominado. Com `train_Yvar=1e-6` o posterior nos observados é quase-degenerado, então o critério probabilístico quase sempre coincide com o geométrico — daí 5.179/5.180. **O único mismatch é a assinatura do prune probabilístico do paper**: prune desligado daria `n_baseline == n_train` sempre; prune geométrico daria 5.180/5.180. **(e)** **(1)** ✓ paper fn.4 · **indireta forte** (o objeto **não expõe** `prune_baseline` como atributo — ver §4, achado do teste vazio).

### 2.23 — S6 · Ref-point = tabela S.5 CONGELADA (oráculo D73)

**(a) O artigo.** §G.4, verificado no .md (offset 95.609): o ref é escolhido a partir do ponto nadir **noiseless** componente a componente e da faixa da fronteira, pela heurística comum `r = f_nadir − β·(f_ideal − f_nadir)`; a Tabela 2 fixa um ref por problema.
**(b) A SPEC.** Mesma heurística, mesmo β=0,1, **mas** o par (ideal, nadir) vem da tabela **S.5 CONGELADA** (`src.metrics.reference_bounds`) em escala BRUTA. É **informação de ORÁCULO** — o algoritmo conhece de antemão os extremos verdadeiros — e por isso **declarada como vantagem informacional (D73)**, em contraste deliberado com o c149 (D96, nadir observado). É a mesma natureza do que o paper faz (a Tab. 2 vem de um pool noiseless), mas nós **declaramos**.
**(c) Observado (re-medido).** `header.ideal_s5`/`nadir_s5` == `metrics.reference_bounds(problema)` **bit-a-bit (Δ=0,0) em 21/21**. A álgebra `acqf_ref_f == nadir + 0,1·(nadir − ideal)` fecha com **Δ máximo = 0,0 em 21/21**. No smoke, `acqf_ref_f = [1,09995; 1,07536]` para o MMF1 — idêntico ao valor da s42.
**(d) Mecanismo + consequência medida.** O ref oráculo tem efeito **operacional** visível: em DTLZ1, DTLZ3 e ZDT4 nenhum FE cai dentro da caixa e o `n_baseline` é 0 em 100% das iterações — a aquisição opera **sem baseline**, guiada só pela melhoria contra o ref. A vantagem informacional vira desvantagem operacional em problemas mal-escalados. Achado de comportamento para o D97, não infidelidade.
**(e) Veredito.** **(2)** D73 · **direta**.

### 2.24 — S7 · `num_restarts` / `raw_samples` / `maxiter` / `init_batch_limit`

**(a)** O paper **não reporta** esses valores (§G.2 detalha init, MC e greedy, não o L-BFGS-B multi-start). **(b)** E.2 é explícita: não estão no paper → fixar 10/512 (tutorial) e **registrar a fonte**; L.10 acrescenta `maxiter=2000`, `init_batch_limit=32`. **(c)** 21/21 no `params` e **corroborado por comportamento**: `len(acqf_todos_restarts) == 10` em 5.201/5.201 e a ③ tem **exatamente 10 linhas por geração** (52.010 = 10 × 5.201, conjunto de tamanhos de bloco = `[10]`). No smoke: 410 = 10 × 41. **(d)** `num_restarts` **materializa 10 objetos observáveis** por iteração; `raw_samples`/`maxiter`/`init_batch_limit` seguem declarativos. **(e)** **(2)** E.2 (fonte declarada) · **direta-declarativa + corroborada**.

### 2.25 — S8 · `Standardize(Y)` e `cache_root` · ✎ **A F5 ERROU O MECANISMO**

**(a) O artigo.** Silente sobre transformação de saída e sobre cache de Cholesky.
**(b) A SPEC.** E.2: `Standardize(Y)` é escolha nossa, a registrar. L.10 fixa `cache_root=None`. A **tabela do bundle** traz `| c262 | cache_root | Específico | True | código oficial |` e a nota v2.2 diz "`cache_root=None→True` (resolvido p/ SingleTaskGP+Standardize)".
**(c) Observado — e é aqui que a F5 se contradiz.** O dado diz `params.cache_root = null` em 21/21 e no smoke — o código passa `None`, conforme L.10. **Mas medi o que o BoTorch faz com esse `None`:**

```
cached_cholesky.py:87-88   if cache_root is None: cache_root = supports_cache_root(model)
objeto construído:         acqf._cache_root = True    supports_cache_root(ModelListGP) = True
```

**`cache_root=None` NÃO desliga o cache — ele resolve para `True` e o cache fica LIGADO.** A F5 §2.25(d) escreveu o oposto ("`cache_root=None` **desliga** o cache da raiz de Cholesky… mais lento, mais determinístico entre máquinas; coerente com DEF-L2") e classificou o conflito bundle×L.10 como puro **doc-sync**. Medido: **não há conflito** — a receita L.10 diz o que se PASSA (`None`), a tabela do bundle diz o que RESULTA (`True`), e as duas estão certas em níveis diferentes. O que estava errado era a minha narrativa causal. O argumento de determinismo Mac×Linux repousa **inteiramente no kernel fusionado** (S9), não no `cache_root`.
**(c') `Standardize` ⬆.** Na F5 era prova **indireta** (só via identidade F4). Agora é **direta**: o objeto instanciado por `_build_models` expõe `outcome_transform = Standardize()`.
**(e) Veredito.** **(2)** L.10 · **direta** (Standardize ⬆ de indireta para direta; `cache_root` com o mecanismo **corrigido**). O item de doc-sync que a F5 mandou para a F5.7 **deve ser retirado**: não era divergência de documento, era erro de leitura minha.

### 2.26 — S9 · Backend, kernel fusionado e pin · ⬆ corroboração empírica nova

**(a)** §G.1: a implementação de referência é o BoTorch (Balandat é autor do paper e líder da lib). **(b)** N.2/DEF-L2/DI-05: BoTorch **OFICIAL 0.18.1** (não o fork `_BoTorch`, cuja `__version__` é "Unknown"), com o kernel C++ fusionado **DESLIGADO explicitamente** — o achado S.3#9 provou que o `csrc/logei_fused.cpp` também está no wheel do PyPI, então o motivo do oficial é a **rastreabilidade do pin** e o motivo do OFF é o **caminho Python puro determinístico Mac×Linux**. D79 pina threads=1.
**(c) Observado.** 21/21: `botorch 0.18.1` (≡ `repos.lock`), `torch 2.11.0+cu130`, `gpytorch 1.15.2`, `python 3.11.9`; `botorch_record_sha256 = e08d996f…6f4b60` **idêntico nas 21 células**; `fused_kernel = 'OFF-explicito (python puro; DEF-L2/S.3#9)'` 21/21 e no smoke; `torch_num_threads=1`, `float64`, `OMP/OPENBLAS/MKL/NUMEXPR = 1`.
**(c') Evidência nova — o risco do DEF-L2 é REAL e eu o reproduzi.** Ao construir a acqf **sem** chamar `disable_fused_kernel()` primeiro, o BoTorch tentou compilar o kernel fusionado nesta máquina e falhou: `clang: error: the clang compiler does not support '-march=native'` → `Falling back to the pure-Python implementation`. É **exatamente** o cenário que a DEF-L2 antecipa: no Mac arm64 o JIT falha com **fallback silencioso** (um `UserWarning`), enquanto no Linux compilaria — numérica assimétrica entre máquinas. O `disable_fused_kernel()` do runner não é cerimônia: é o que impede essa assimetria.
**(e) Veredito.** **(2)** N.2/DEF-L2/DI-05/D79 · **direta-declarativa (com sha) + corroboração empírica do risco**.

### 2.27 — S10 · Sinal, domínio e RNG

**(a)** §G.3: os problemas são tratados como maximização (objetivos de minimização multiplicados por −1); §7: SAA fixa os base samples. **(b)** §5.5: o adapter devolve `−f` e trabalha em `[0,1]^D`; **o export sempre grava f de minimização em escala NATIVA**. L.10/D62: `manual_seed(h(run,it))` antes de construir modelo+acqf, mais `h1` (sampler) e `h2` (optimize_acqf); retry do `optimize_acqf` desloca o RNG — **registrar**. **(c)** x da ③ em bounds **nativos** 21/21 (MMF1 [−1; 2,945], WFG [0; 44], BBOB [−5; 5]); `espaco_modelo` e `transf_tipo` NULL em 100%. `torch_seed`/`h1`/`h2` distintos por iteração, `h1 ≠ h2` em 5.201/5.201. 154 registros `optimize_acqf_warning` contra 152 somados nas decisões — a diferença de 2 é a iteração de hard-stop do BBOB_F5, cuja decisão não carrega a chave; a reconciliação fecha 21/21. No smoke: 1 `optimize_acqf_warning`. **(e)** **(2)** §5.5/D62/L.10 · **direta**.

### 2.28 — S11 · Hard-stop D61 e os cache-hits D89

**(c)** 1 hard-stop por célula (**21/21**), `motivo='infill inédito com saldo zerado (D21/D61)'`, guard com `fe==maxfe`, `footer.hard_stopped=True` 21/21 — **e no smoke também**. **40 cache-hits** (ZDT1 39 + ZDT6 1), todos com `dist_min_arquivo = 0,0` e `fe == n_train` (sem +q). O ponto repetido do ZDT1 é o `solution_id=498` com **30/30 coordenadas exatamente nos limites**. **(d)** Em `[0,1]^30` com 600+ pontos, o maximizador trava num vértice e o L-BFGS-B clampa; a dedup por X bit-a-bit reconhece e não gasta FE — o cenário previsto em **D89**. Prova tripla de que não é bug de contabilidade: (i) `n_geracoes` fecha (U10); (ii) o refit repete os HP bit-a-bit exatamente nessas iterações (F5); (iii) `fe_final` bate exato. **(e)** **(2)** D61/D57/D89 · **direta**.

### 2.29 — S12 · `params` no ⑤ · ✅ **NÃO-CONFORMIDADE FECHADA PELA T11**

**(a)/(b)** O `CONTRATO §5` lista `params` como **obrigatório** no manifesto. A F5.2b registrou a não-conformidade em 7 configs / 197 células, das quais **21 são do c262**.
**(c) Observado — o antes e o depois.** s42: `'params' in manifest` = **False em 21/21**; `contrato_f52b.csv` tem exatamente 21 linhas de c262, todas `camada 5, CHAVES, faltam ['params']` (verificado). Smoke T11: **`params` PRESENTE no ⑤**, com 11 chaves, e — o ponto que importa — **`header.params == manifest.params` byte a byte** (medido `True`). O código explica: `_params_efetivos(q)` em `src/c262_qnehvi.py:504-515` é chamado **duas vezes com o mesmo retorno**, na linha 597 (header do ⑥) e na 907 (⑤), com o comentário "fonte ÚNICA do `params` do header do ⑥ E do ⑤ (o CONTRATO §5 o exige e 24 células de c262 na s42 não o tinham no ⑤)".
**(d) Mecanismo.** Era heterogeneidade entre writers (o runner escrevia no header, o `new_manifest` não replicava). A T11 unificou a fonte, o que elimina a classe inteira do defeito — não só a instância.
**(e) Veredito.** **(2)** F5.2b · **direta**. Para o corpus s42 a não-conformidade é fato consumado, com fallback provado célula a célula; para o código de hoje ela **não existe mais**.

### 2.30 — S13 · Teto de wall-clock · ⬆ **o critério que matou 8 células foi REMOVIDO**

**(a)** §H.1 reporta wall-times de 0,58 s a 546,8 s por batch, em **GPU V100** e com d ≤ 8; o paper nunca encontra a parede.
**(b) O que era.** `_WallClockProjector` (DI-11.3/DI-36/DI-38a) com **dois** critérios de aborto: projeção (`decorrido + projetado > teto`) e relógio. **(b') O que a T11 fez.** DI-43/DI-44: **a projeção NÃO ABORTA MAIS — virou aviso** (`wall_projection_warning`), o relógio é o único critério de aborto, o teto passa a **12 h**, e a doutrina vira **truncamento-com-dado** — o run vai até o relógio e fecha `failed`/`teto_wall` **gravando as camadas parciais** ("curva parcial é o dado", DI-37.1). Código: `src/c262_qnehvi.py:876-926` (o teto agora fecha pelo `write_run_outputs`).
**(c) Observado — por que isso importa TANTO neste config.** Medi as 9 células fora da análise:

| célula | critério | decorrido | projetado | fe | progresso | parquets |
|---|---|---|---|---|---|---|
| main/ZDT3 | **projeção** | 13.891 s (3,9 h) | 29.832 s | 736/929 | 79,2% | **0** |
| main/MMF16_20 | **projeção** | 24.155 s (6,7 h) | 19.047 s | 524/619 | 84,7% | **0** |
| main/DTLZ7 | **projeção** | 15.524 s (4,3 h) | 27.795 s | 478/681 | 70,2% | **0** |
| batch/WFG9 q=10 | **projeção** | 14.506 s (4,0 h) | 28.989 s | 1461/2241 | 65,2% | **0** |
| batch/ZDT1 q=10 | **projeção** | 23.080 s (6,4 h) | 157.984 s | 946/2329 | 40,6% | **0** |
| batch/DTLZ2 q=10 | **projeção** | 7.825 s (2,2 h) | 168.610 s | 301/2131 | 14,1% | **0** |
| batch/MMF16_20 q=10 | **projeção** | 6.980 s (1,9 h) | 167.660 s | 369/2219 | 16,6% | **0** |
| batch/ZDT4 q=10 | **projeção** | **237 s (4 min)** | 52.138 s | 209/2109 | 9,9% | **0** |
| main/WFG1 🔴 | ModelFittingError | — | — | 281/681 | 41,3% | **0** |

**As 9 abortaram sem UM parquet, e as 8 ⚪ abortaram TODAS pelo critério de projeção — o critério que hoje não aborta mais.** Todas as 8 estavam **abaixo** de 12 h de decorrido (a mais avançada, batch/ZDT1, a 6,4 h; a mais brutal, batch/ZDT4, a **4 minutos**). Sob o código de hoje nenhuma teria abortado ali: rodariam até 12 h e fechariam **com as camadas escritas**. As 3 main tinham projeção de 43.319 s / 43.202 s / 43.723 s — a **0,03–1,2% de 12 h** — ou seja, entregariam a curva praticamente inteira.
**(d) Mecanismo — e a correção quantitativa da F5 que segue valendo.** O `INVENTARIO` atribuía a parede ao refit de GP `n³`. O dado mede outra coisa: nas 21 células o fit é **10,5% (mediana)** do custo `fit+busca` e **a busca é 90,6%** do agregado; o expoente empírico de `t_busca` vs n chega a **3,96 (ZDT1)** contra 1,52 (mediana) do `t_fit`. **O que explode é a aquisição de hipervolume** (partição de caixas × 128 amostras MC × 10 restarts), não o Cholesky. O modelo do projetor era mal-especificado nos dois termos — o que é exatamente por que abortar por projeção era frágil, e por que removê-lo é a decisão certa.
**(e) Veredito.** **(2)** DI-43/DI-44 (revoga DI-38a) · **direta**.

### 2.31 — N1 🆕 · O guard de `fit_retries` — implementado, **não exercitado**

**(a)/(b)** Não existe no paper. Nasce de uma recomendação **explícita da minha F5 §9(h)**: "promover `fit_retries > 0` a alerta amarelo — no c262 ele é um preditor de falha com 1 falso-negativo em 5.241 iterações observadas", derivada da dissecação do 🔴 `main/WFG1` (11 retries em 7 iterações a partir da it 27, contra **0 em 5.201** nas células saudáveis, morrendo na it 41).
**(c) Observado.** **Implementado** — `src/c262_qnehvi.py:641-655`, com o racional citando o WFG1 nominalmente:

```python
fit_retries, modelo_hp = _fit_models(model)
if fit_retries:
    log.guard("fit_retries", it=it, n_retries=int(fit_retries), n_train=int(n_train),
              motivo="fit_gpytorch_mll precisou de tentativa extra (condicionamento "
                     "do GP) — precursor do ModelFittingError")
```

**Mas não disparou.** No smoke: 1 único guard, `hard_stop`; `fit_retries` presente em 40/41 decisões, **soma 0**. Na s42: `fit_retries` soma **0 em 5.201 iterações**, `guard 'fit_retries'` **0 vezes** (nem existia). A condição é `if fit_retries:` sobre o `int` devolvido por `_fit_models` (contagem de warnings de retry) ⇒ dispara determinística e necessariamente quando `fit_retries > 0`. Sobre o filme do WFG1, teria emitido **7 guards** (uma por iteração com retry) a partir da it 27 — **14 iterações antes** da morte.
**(d) Por quê.** O smoke é MMF1 D=2 com 61 FE: o GP nunca chega perto do mau condicionamento (D=22 + n≈281 + nugget no piso é o discriminante que a F5 isolou). **O guard está certo e o smoke não tinha como exercitá-lo.**
**(e) Veredito.** **(2)** T11-A4 · **direta quanto à implementação, NÃO-EXERCITADO quanto ao disparo**. É a única das três correções deste config cujo *efeito* não pôde ser medido — honestidade obriga a dizer que o poder discriminante segue sendo uma inferência de código, não uma observação.

### 2.32 — N2 🆕 · Checkpoint atômico e truncamento-com-dado

**(b)** G5/DI-43: checkpoint atômico a cada **25 iterações OU 30 min**, em 8 runners, com kill-test; `fe_final` do ⑤ declarado **PISO**; as 5 escritas são atômicas uma a uma e o ⑤ vai por último. **(c)** Smoke T11: `checkpoint` no manifesto, `rec='checkpoint'` (1 registro), `n_checkpoints=1` no footer. s42: **`checkpoint` ausente em 21/21, `n_checkpoints` ausente no footer em 21/21** — instrumentação genuinamente nova. **(d)** É a metade "não perder o já feito" da mesma doutrina do S13. **(e)** **(2)** DI-43/G5 · **direta**.

### 2.33 — N3 🆕 · Proveniência: `campanha_id`, `repo_hash`, schema v2

**(c) Observado — e cuidado com a leitura de presença.** Na s42 a chave `repo_hash` **existe em 21/21** — mas o **valor é `None`** (medido diretamente) e `schema_version = 1`. No smoke T11: `repo_hash = 9ad0138a…f26f` (= HEAD real), `schema_version = 2`, `campanha_id = 9ad0138aa9a4_2026-07-31` (ausente em 21/21 da s42). **Este é o padrão que a torre mandou procurar — campo presente, dado sentinela — só que a T11 o encontrou ANTES e o preencheu.** É o achado I-09 do handoff, e neste config ele está **fechado**. **(e)** **(2)** G3/I-09 · **direta**.

### 2.34 — T1 · Hiperparâmetros internos da aquisição · ✅ **TETO LEVANTADO**

Ver §4.1 — o aspecto migra de **T** para **(2) direta-declarativa com controle**.

### 2.35 — T2 · Sequential greedy (q>1) — a contribuição-título · **T, inalterado**

O paper **se chama** "**Parallel** Bayesian Optimization…"; a Eq. (6), o §6.1 (CBD para greedy sequencial), o Apêndice F (bound submodular ≤ (1/e)α\*, verificado no .md offset 23.483) e as Figs. 4/6 são todos sobre batch. A receita DI-37.2 manda "batch q=10 = receita IDÊNTICA, sem knob". **No dado analisável isso segue 100% não-exercitado**: `q=1` em 21/21 do main (`lote_solution_ids` de tamanho 1 em 5.180/5.180) e as 5 células batch (q=10 confirmado no `header.params`) abortaram 5/5, a mais avançada em 65,2% dos infills, **sem um parquet**. O racional profundo do M.7 ("batch e ruído são o MESMO mecanismo") permanece não testado por dado nosso. **É o maior buraco de verificabilidade deste config e a razão inteira do desconto do score.** **T** — mas com o caminho de fechamento agora **aberto** pela DI-43/44 (§2.30) e pelo `_lote_greedy_sequencial` já escrito e documentado (`c262_qnehvi.py:343+`, que reimplementa o greedy nativo porque o BoTorch proíbe `sequential=True` + `return_best_only=False`).

---

## 3. % por classe

**Denominador = 35 aspectos − 2 T = 33 verificáveis.** (U12 é N/A por regime online — não há camada ⑦ — e fica fora do denominador, como na F5.)

| classe | contagem | % (denom. 33) | vs F5 (denom. 30) |
|---|---|---|---|
| **(1)** conforme o artigo | **10** — U3, U6, U8, U11, F1, F5, F6, S1, S4, S5 | **30,3%** | 33,3% (mesmos 10) |
| **(2)** desvio/adição sancionada, cada um com D-xx/DI-xx citado | **23** — U1, U2, U4, U5, U7, U9, U10, F2, F3, F4, S2, S3, S6, S7, S8, S9, S10, S11, S12, S13, **N1, N2, N3** | **69,7%** | 66,7% (20) |
| **(3)** desvio inexplicado 🎯 | **0** | **0,0%** | 0,0% |
| **T** teto declarado (fora do denominador) | **2** — T2 (batch q>1) e o resíduo de código interno (CBD/warnings suprimidas) | — | 2 (T1, T2) |

A queda de 33,3%→30,3% na classe (1) é **puramente aritmética** (3 aspectos novos, todos (2) por serem instrumentação nossa); nenhum aspecto mudou de (1) para (2) nem o contrário. Aspectos com marca **direta-declarativa**: S4, S7, S9, T1 (S1 **saiu** da lista — virou direta). Prova **indireta forte**: S5.

---

## 4. 🆕 AS CORREÇÕES DA T11 — implementadas? funcionam de fato?

A F5 fez **três** recomendações nomeadas para o c262. A T11 executou as três (item **A4** da tabela suprema: "8+2 hp da acqf (`ACQF_HP` fonte única) · `params` · guard de `fit_retries`"). Verifiquei uma por uma, procurando o padrão do c217 (**campo presente, dado sentinela**).

### 4.1 · T1 → `acqf_hp` no `params` · ✅ **REAL, com controle no objeto**

**A recomendação da F5 (§2.31):** "promover esses 8 campos ao `params` do header custa 8 chaves e converte T em direta-declarativa".
**Implementado?** Sim, e melhor que o pedido — **10 campos**, não 8, e como **fonte única**:

```python
# src/c262_qnehvi.py:113   "São a FONTE ÚNICA — `_make_acqf` os consome daqui."
ACQF_HP = {prune_baseline, alpha, cache_pending, max_iep, incremental_nehvi,
           cache_root, tau_relu, tau_max, fat, eta}
# :317   return qLogNoisyExpectedHypervolumeImprovement(..., **ACQF_HP)
# :514   "acqf_hp": dict(ACQF_HP)     dentro de _params_efetivos()
```

**Funciona de fato — não é config-echo.** O teste do padrão-c217 tem três camadas e passou nas três:

1. **`ACQF_HP` (código) == `acqf_hp` (⑤ do smoke)**: `True`, dict a dict.
2. **7 dos 10 hp confirmados contra o objeto BoTorch construído** por `_make_acqf`:

| hp | logado | no objeto | veredito |
|---|---|---|---|
| `alpha` | 0.0 | 0.0 | CONFIRMA |
| `tau_relu` | 1e-06 | 1e-06 | CONFIRMA |
| `tau_max` | 0.001 | 0.001 | CONFIRMA |
| `fat` | True | True | CONFIRMA |
| `cache_pending` | True | True | CONFIRMA |
| `max_iep` | 0 | 0 | CONFIRMA |
| `incremental_nehvi` | True | True | CONFIRMA |
| `cache_root` | None | **`_cache_root = True`** | **resolve** (§2.25) |
| `prune_baseline` | True | não exposto | prova indireta (S5) |
| `eta` | 0.001 | não exposto | **INERTE** — `acqf.constraints is None` ⇒ o `eta` só escala o sigmóide de restrições, e não há restrições nos nossos 25 problemas |

3. **Controle negativo**: injetei `alpha=0.5` e o objeto passou a expor 0.5 ⇒ a leitura **discrimina**; não é um atributo que devolve sempre o mesmo.

**O que ainda falta.** (i) `cache_root` merece uma **nota de leitura** no `params` ou no `sigma_dict`: gravar `null` e ter cache **ligado** é justamente a armadilha que me pegou na F5 — sugiro logar o valor **efetivo** (`_cache_root`) ao lado do passado. (ii) O `eta` deveria ser marcado como inerte-sem-restrições, senão vira ruído para quem audita. (iii) **Nenhum teste da suíte referencia `acqf_hp`** (`grep -rn "acqf_hp" tests/` = vazio): os 10 campos entraram no dado sem um gate que reprove se alguém editar `ACQF_HP` sem editar a receita.

### 4.2 · S12 → `params` no ⑤ · ✅ **REAL, com fonte única**

Ver §2.29. s42 **0/21** → smoke **presente**, e `header.params == manifest.params` byte a byte, via `_params_efetivos(q)` chamado nos dois pontos de escrita. Fecha as **21 linhas** de c262 em `contrato_f52b.csv`. Bônus não pedido: o `q` agora flui `write_run_outputs(q=int(q)) → new_manifest(q=…)`, o que corrige também o **item 7 do teto da F5** (os 5 manifestos batch abortados registravam `q=1` enquanto o `header.params.q` correto era 10 — **re-medido e confirmado: q_manif=1 × q_header=10 nas 5**). ⚠ **Resíduo**: a rota `_manifesto_failed_teto` (aborto **sem** dado, `c262_qnehvi.py:406-423`) ainda chama `new_manifest` sem `q` ⇒ default 1. Ela ficou reservada a "aborto sem dado", mas se for exercitada o defeito volta.

### 4.3 · §9(h) → guard de `fit_retries` · ⚠ **IMPLEMENTADO, NÃO EXERCITADO**

Ver §2.31. Implementado exatamente como recomendei, com o WFG1 citado no comentário. **Zero disparos** — porque houve **zero retries** em 5.201 iterações da s42 e 41 do smoke. O poder discriminante (11 retries em 7 iterações antes da morte do WFG1, contra 0 em 5.201 saudáveis) é herdado da minha F5, não re-observado. **Falta**: um teste que force `fit_retries > 0` e verifique que o guard aparece no ⑥ — hoje não existe (`grep -rn "fit_retries" tests/` = vazio).

### 4.4 · 🆕 Não pedida pela F5, mas a mais valiosa: **I-12 no `sigma_dict`**

Ver §2.14. A **armadilha (a)** da F5 — a que reprovaria um run perfeito — virou chave declarada (`ordem_terceira_online`) + utilitário (`restart_de_linha`). **Verifiquei a regra e ela reproduz exatamente a permutação que eu havia provado de forma independente** (277/7.111 = 3,90% × 42,99% × 64,97%). É a diferença entre um achado que vive num relatório e uma regra que viaja com o dado. Errata contra o texto: o "89% de falso-mismatch" mede **83,2%** neste config.

### 4.5 · 🔎 O padrão do c217 (campo presente, dado sentinela) — **procurei e NÃO achei neste config**

| campo | c217 (o padrão) | c262 (medido) |
|---|---|---|
| identidade da referência | `pmid_ids = [-1,-1,-1,-1,-1]` em 42/42 — **sentinela** | `acqf_ref_f` = valor real, ≡ `metrics.reference_bounds` bit-a-bit 21/21 |
| hp da aquisição | — | `acqf_hp` com 10 valores reais, 7 confirmados no objeto, controle negativo passa |
| proveniência | — | `repo_hash` era **`None` na s42** (sentinela!) → **sha real de HEAD** no smoke |
| regra do rótulo | `REGRA_DO_ROTULO` ✅ (e corrige a leitura da F5) | `ordem_terceira_online` ✅ e **verificada por dois critérios** |
| guard novo | — | `fit_retries` **implementado, 0 disparos** ⚠ (única ressalva) |

**Veredito da caça:** o único campo do c262 que já foi sentinela (`repo_hash = None`) foi **preenchido** pela T11. Nenhum campo novo entrou vazio ou falso. A única ressalva honesta é o guard não-exercitado — que é "condição nunca ocorreu", não "dado mentiroso".

### 4.6 · Achados **contra** o trabalho da T11 neste config

1. **Uma asserção decorativa no teste oficial.** `tests/test_c262.py:125`:
   ```python
   self.assertTrue(acqf.prune_baseline if hasattr(acqf, "prune_baseline") else True)
   ```
   Medido: **`hasattr(acqf, 'prune_baseline')` é `False`** ⇒ a asserção é `assertTrue(True)` — **passa sempre, para qualquer valor de `prune_baseline`**. É literalmente o anti-padrão que a própria T11 elegeu lição nº 1 do §4.2 ("um gate que nunca reprova é decorativo"), sobrevivendo dentro do arquivo de teste do config que a campanha tocou. As outras 5 asserções do mesmo teste (`alpha`, `tau_relu`, `tau_max`, `fat`, `sampler`) são **reais** — provei com o controle negativo.
2. **Nenhum gate para os campos novos** (`acqf_hp`, `fit_retries`): 0 ocorrências em `tests/`.
3. **A documentação da própria evidência está errada** (ERRATA 0): §15.2 e o LEIA-ME negam a existência dos smokes Python que existem, com `campanha_id` e no HEAD.
4. **Contagem de erratas inconsistente** no handoff: §3 titula "AS 15 ERRATAS" e lista 15; §15.4 cita uma "ERRATA 16" que não está na tabela; §15.7 diz 16.

---

## 5. Comparação canônica (Entrega 2)

**Setup do paper** (verificado no .md do artigo, não no resumo do agente-4): 9 problemas (BraninCurrin d2·M2, DTLZ2 d6·M2, ZDT1 d4·M2, VehicleSafety d5·M3, CarSideImpact d7·M4, AutoML d8·M2, ConstrainedBraninCurrin, SphereEllipsoidal d5·M2, ABR d4·M2); **~200–224 FE**; init `2(d+1)` Sobol *scrambled*; **100 réplicas** (20 para PESMO); métrica = **log hypervolume difference** contra a PF verdadeira, média ± 2 SEM, **sem testes de hipótese**; ruído principal 1–10% da faixa; **§H.7 = benchmarks noiseless** (o nosso regime).

| problema | paper | nosso (s42) | veredito |
|---|---|---|---|
| **DTLZ2** | d=6, **M=2**, 200 FE, ref [−1,10]^M | D=**12**, **M=3**, 371 FE, ref normalizado D69 | **incomensurável (eixo M + dimensão 2× + orçamento 1,86×)** — faixa-guia J: log-HV-diff ≈ −2,2 |
| **ZDT1** | d=4, M=2, 200 FE | D=**30**, M=2, **929 FE** | **incomensurável (dimensão 7,5× + orçamento 4,6×)** — J ≈ −2,4 |
| **SphereEllipsoidal** ↔ nossos BBOB_F1/F5/F17/F22/F37/F49/F55 | d=5, M=2, 200 FE, ruído 5% | D=**10**, M=2, 309 FE, **noiseless** | **incomensurável (dimensão 2× + regime de ruído + métrica)** — J ≈ 7,2–8,4 |
| BraninCurrin, VehicleSafety, CarSideImpact, AutoML, ABR, ConstrBC | — | fora dos 25 | **sem interseção** |

**Nenhuma célula comparável no sentido estrito** (Botão 4 — em nenhum caso se afirma "artigo melhor"): a métrica difere (log-HV-diff em escala bruta com ref da Tab. 2 × IGD+ com ref normalizado D69), orçamento e dimensão diferem por 2–7,5×, e o paper não publica tabela numérica dos benchmarks sintéticos.

**Âncoras direcionais — re-executadas por mim sobre `metricas_finais_f52c.csv`, 4/4 CONFIRMADAS:**

| sentido do paper | rival análogo | resultado re-medido |
|---|---|---|
| qNEHVI ≫ métodos por **entropia** (§9.1: PESMO/MESMO/PFES são os piores) | **c154** (JES) | c262 melhor em **10/11** ✓ |
| qNEHVI > **Thompson/TSEMO/TS-TCH** (§9.1: TS-TCH no meio do pelotão) | **e81** (qPOTS) | c262 melhor em **19/21** ✓ |
| qNEHVI > **ParEGO/qNParEGO** (Figs. 3 e 15a) | **b1** (ParEGO) | c262 melhor em **18/20** ✓ |
| qNEHVI > **HV-greedy com diversidade / DGEMO** (H.7) | **b3** (K-RVEA) | c262 melhor em **17/21** ✓ (análogo frouxo, registrado como tal) |

Onde o paper dá direção, a direção se reproduz sob o **nosso** envelope (31D−1, noiseless, D até 30) em **4/4**, com margens largas. Segue o resultado canônico mais forte do fan-out.

---

## 6. Saúde em escala

| eixo | resultado (s42 = corpus principal) |
|---|---|
| **Integridade** | `integridade_f52a.csv`: **ZERO linhas de c262**. 0 linhas jsonl ruins em 21/21; footer duplo 21/21; `n_retries=0` 21/21; `fallback_ativado=False` 21/21 |
| **Trajetórias** | 399 transições, **0 violações** de monotonicidade de IGD+ e HV (F5.2, insumo) |
| **Sonda — régua Sobol** (regra 12: **o c262 não tem bloco estratificado** — `sonda_estratificada` medido **0/21**; a régua é o único bloco, e é comparável entre configs) | ΔWAPE 1º→último: mediana **−8,58%**, **38/46** melhoram · cobertura final mediana **0,9763**, **40/46 ≥ 0,90** · **0 σ-NaN** em 2.631 blocos × 2.000 pontos, `n_validas=2000` sempre |
| **Sonda no artefato T11** | 22 blocos × 2.000, 0 NaN; obj0 WAPE 0,0148→0,0051 (cob. 0,953→0,966), obj1 0,927→0,511 (cob. **0,563→0,911**) |
| **Posição vs pisos** (IGD+; c262=v6, pisos=vm3 ⇒ piso de ruído **58,98%** aplicável) | bate **além** do ruído em **7/21** (ZDT6 −100,0%, ZDT1 −99,8%, BBOB_F1 −98,9%, BBOB_F5 −96,0%, ZDT4 −86,5%, DTLZ2 −82,0%, BBOB_F22 −67,9%) · bate **dentro** do ruído em 9/21 · perde dentro em 1/21 (BBOB_F17 +5,2%) · perde **além** em **4/21** (DTLZ4 +109,6%, DTLZ3 +73,9%, DTLZ1 +71,5%, WFG4 +62,8%) |
| **Rank** | **rank médio 3,71** entre 16 algoritmos (mediana por problema); **1º lugar em 8/21**, top-3 em 11/21 |
| **Custo** | 28,89 h-core nas 21 (ZDT1 8,86 h). A **aquisição consome 90,6%** de (fit+busca); expoente empírico de `t_busca` até **3,96** |
| **Guardas** | `fit_retries` 0/5.201 · cache-hits 40 (todos D89-legítimos) · `optimize_acqf_warning` 154, reconciliados 21/21 |

As 4 derrotas além do ruído são os mesmos problemas com estrutura global fraca / mal-escalados em que o ref oráculo S.5 fica **fora** do alcance dos FE reais (DTLZ1, DTLZ3 com `n_baseline=0` em 100% das iterações) — comportamento explicado, não anomalia.

---

## 7. SCORE e VEREDITO

## **SCORE: 9,7 / 10** — recomendação: **ACEITAR** · **VEREDITO: MELHOROU** (9,5 → 9,7)

### A causa NOMEADA da melhora

**Não é mais evidência de mecanismo, não é amostra diferente, não é sorte de semente.** O mecanismo é **exatamente o mesmo** — provei-o re-executando as quatro query-joias sobre as mesmas 21 células e obtendo **os mesmos números até o último dígito** (argmax 5.201/5.201 Δ=0,0 · elo ③→① 5.180/5.180 Δ=0,0 · ruído 11.220/11.220 err 1,429e-7 · I-12 277/7.111 = 3,90%), e reproduzindo-as **também no artefato do HEAD** (41/41, 40/41 + G-1 40/40, 80/80). A melhora é de **duas correções reais** e de **verificabilidade**:

1. **Fechou 2 dos 3 itens que compunham o desconto de 0,5 da F5.** A F5 disse, literalmente, que o desconto era "exclusivamente de verificabilidade e contrato: (i) T2 batch; (ii) T1 — 8 hiperparâmetros internos não estão no `params`; (iii) a não-conformidade de contrato do ⑤". **(ii) e (iii) estão fechados e medidos em artefato**, não prometidos: `acqf_hp` com 10 campos de fonte única (7/10 confirmados contra o objeto BoTorch + controle negativo) e `params` no ⑤ com `header == manifest` byte a byte.
2. **A armadilha nº 1 do config virou regra de dado.** O `ordem_terceira_online` (I-12) + `restart_de_linha` transformam o achado mais frágil da F5 — que vivia só no meu relatório e que faria um agente fresco reprovar um run perfeito — em contrato machine-readable, e eu **verifiquei** que a regra declarada é a mesma que provei de forma independente.
3. **Verificabilidade que eu mesmo levantei nesta rodada**: S1 sai de direta-declarativa para **direta** (kernel `MaternKernel nu=2.5`, `ard_num_dims=D`, `GammaPrior(3,6)/(2,0.15)` lidos do objeto instanciado); `Standardize` sai de indireta para direta; o risco do DEF-L2 deixou de ser teórico (reproduzi o fallback silencioso do kernel fusionado nesta máquina).
4. **O caminho para fechar T2 abriu.** As 8 ⚪ abortaram **todas pelo critério de projeção**, sem um parquet, uma delas aos **4 minutos** — e esse critério **não aborta mais** (DI-43/44). Não é dado novo hoje, mas muda a expectativa da próxima rodada de "5/5 batch sem nada" para "5/5 batch com curva parcial".

### Por que **não** 10

**T2.** O *sequential greedy* q>1 — que é a contribuição-título do paper, a Eq. (6), o §6.1, o Apêndice F e as Figs. 4/6 — segue **100% não exercitado** por dado nosso: q=1 em 21/21 e batch abortado 5/5. Metade do mecanismo publicado continua sem prova. Somam-se três achados menores desta rodada (§4.6): a asserção vazia do `prune_baseline` em `tests/test_c262.py:125`, a ausência total de gate para `acqf_hp`/`fit_retries`, e o guard de `fit_retries` implementado mas com **0 disparos**. O saldo: desconto de **0,3**, contra 0,5 na F5.

### Justificativa em 5 linhas

Todos os 33 aspectos verificáveis são (1) ou (2), cada (2) ancorado em decisão citada, **0 na classe (3)** — pelo segundo ciclo consecutivo, agora com o mecanismo re-medido do zero e confirmado bit-a-bit também no HEAD. As duas identidades fechadas do config (argmax da aquisição; ruído sob `Standardize`) fecham em 100% nos dois corpora, com erro relativo máximo 1,43e-7 (s42) e 9,97e-9 (smoke). As três recomendações que a F5 fez ao autor foram implementadas, e duas delas eu consegui **provar que funcionam de fato** — não apenas que o campo existe — por controle contra o objeto BoTorch vivo e por controle negativo. Contra o meu próprio trabalho anterior levanto três erratas medidas (a mais séria: `cache_root=None` **liga** o cache, não desliga), e contra a documentação da campanha, duas (os smokes Python existem; o "89%" da regra I-12 é 83,2% aqui). O único bloqueio real ao 10 é o mesmo de sempre e não é de fidelidade: **o batch nunca rodou**.

---

## 8. Aspectos classe (3) 🎯

**NENHUM.** Zero aspectos vão à verificação adversarial. Quatro candidatos foram levantados nesta rodada e **descartados com evidência**:

1. **"`acqf_hp` é config-echo decorativo" (o padrão-c217).** **Descartado**: `ACQF_HP` é a **fonte única** consumida por `_make_acqf` (`**ACQF_HP`, linha 317) e gravada por `_params_efetivos` (linha 514); `ACQF_HP == acqf_hp` do ⑤ dict a dict; **7/10 confirmados contra o objeto**; controle negativo (`alpha=0.5`) discrimina. Os 3 não confirmados têm explicação medida: `cache_root` resolve, `prune_baseline` não é atributo (prova indireta S5), `eta` é **inerte** (`constraints is None`).
2. **"`cache_root` divergente entre bundle e receita" (item que a F5 mandou para a F5.7).** **Descartado, e a F5 estava errada na direção**: `cache_root=None` → `supports_cache_root(ModelListGP)=True` → `_cache_root=True`. A receita L.10 descreve o passado, a tabela do bundle descreve o resolvido; ambas corretas. **Retirar o item de doc-sync da F5.7** e trocá-lo por "logar o valor efetivo".
3. **"O guard de `fit_retries` nunca dispara ⇒ está quebrado".** **Descartado**: a condição é `if fit_retries:` sobre um `int` de contagem de warnings; `fit_retries` é **0 em 5.201 + 41 iterações** porque nenhuma delas precisou de retry. Não-disparo aqui é ausência de condição, não gate morto. (Continua **sem controle positivo** — é a recomendação, não um (3).)
4. **"O smoke não existe, logo as correções são só código"** (a premissa do briefing). **Descartado**: existe, está no HEAD, passa 6/6 gates re-rodados por mim.

---

## 9. Teto de verificabilidade (T) e armadilhas

**T — o que este corpus NÃO permite verificar:**

1. **`sequential greedy` q>1** (Eq. 6, App. F, Figs. 4/6): não exercitado. q=1 em 21/21, batch 5/5 abortado sem parquet. **O maior buraco.** Fecha-se rodando as 5 células batch sob a DI-43/44.
2. **A matemática interna da CBD / partição de caixas** (§6, App. D — o coração computacional do paper). **Parcialmente levantado nesta rodada**: o objeto expõe `partitioning = FastNondominatedPartitioning`, que é a partição **exata** coerente com `alpha=0`; mas o algoritmo de decomposição em si segue sendo código, coberto por `repos.lock` (botorch 0.18.1 pypi) + `botorch_record_sha256` idêntico nas 21.
3. **Contagem efetiva das `NumericalWarning` do gpytorch**: filtradas pelo `warning_filter` declarado, **não contadas**. O contador que existe (`acqf_warnings`) é de outra grandeza (retry do `optimize_acqf`).
4. **Não-perturbação da sonda no c262**: o handoff §5 registra ✅ BIT-IDÊNTICA com hash `75c52d8b082b8093`, mas **as pastas `g6_com`/`g6_sem` só contêm os 9 pares MATLAB** — para este config a evidência é **(ii) veredito registrado**, não (iii) artefato. Não é dúvida sobre o resultado (a T11 provou o par em 19/19 configs); é a classe da evidência que muda, e ela é a premissa da minha autorização para usar a s42 como corpus de mecanismo.
5. **O poder discriminante do guard `fit_retries`** (§4.3) — inferido da F5, não re-observado.
6. **A resolução float32 da ① e da sonda** (regra 11 do CONTRATO): diferenças abaixo dela não são conclusivas — foi o que gerou os 1,91e-6 da U2 e os 1,19e-7 da U5.

**Armadilhas confirmadas na escala (para quem reler estes dados):**

- **(a) A ordenação da ③** — a linha marcada é a **ÚLTIMA** do bloco de 10 e o índice do argmax **não** corresponde a ela (`best == última` em só **16,8%**). Casar por índice reprova um run perfeito. **Agora resolvida no dado**: use `sigma_dict.ordem_terceira_online` / `botorch_harness.restart_de_linha(R, best)`. ⚠ **não vale para q>1**.
- **(b) A geração de hard-stop escreve 10 linhas na ③ sem nenhuma marcada** e não faz a ② crescer. Parece bloco corrompido; é D61. A sonda **é** emitida nessa geração.
- **(c) `②(0) ≡ ②(1) = n_init`** — dois blocos do mesmo tamanho no começo; `②(g)` é o estado NO INÍCIO da geração g. Reproduzido no smoke (42 blocos / 41 gerações).
- **(d) `fe ≠ n_train + q` nas 40 iterações de cache-hit** (vale `fe == n_train`).
- **(e) `n_baseline = 0` em 583 iterações consecutivas** (DTLZ1 141/141, DTLZ3 241/241, ZDT4 201/201) parece prune quebrado; é o ref oráculo S.5 fora do alcance dos FE reais.
- **(f) α negativo em 68,7%** — assinatura da variante **log**, não erro de sinal.
- **(g) `motivo_parada` NÃO existe nos manifestos OK** do c262 (só nos `failed`) — **confirmado também no smoke T11**. Use `footer.hard_stopped`. É a quarta variante de campo de término do projeto.
- **(h) `modelo_hp` é ANINHADO** (`por_objetivo`) — ler o nível errado devolve 0/0 e faria concluir "campo ausente". É a **ERRATA 6 da própria T11** reencontrada.
- **(i) `repo_hash` existe na s42 mas vale `None`** — testar presença de chave em vez de valor dá falso-verde. O mesmo padrão do `pmid_ids` do c217, em outra camada.
- **(j) `cache_root: null` no log significa cache LIGADO.** 🆕 Nova, e pegou a própria F5.
- **(k) 🔴 `main/WFG1`** — `ModelFittingError` na it 41 (`n_train=281`, 41,3% do orçamento), `n_retries=2`, após 998,87 s. Filme íntegro até a it 40 (pára-e-loga D81 funcionou). Assinatura que antecipa a morte: **11 `fit_retries` em 7 iterações** a partir da it 27, contra **0 em 5.201** nas células saudáveis; `t_fit` 0,59 s → 8,02 s; nugget padronizado 4,6e-4 → 3,2e-6. O colapso do `outputscale` **não** é discriminante (ZDT1/ZDT6/WFG5 colapsaram tanto ou mais e sobreviveram); o discriminante é **D=22 + n≈281 + nugget no piso + escada de retries já ativa**. **A T11 implementou o alerta que a F5 pediu** — ele teria disparado 7 vezes, 14 iterações antes da morte.

---

### Rodapé de artefatos

**Escrita (única permitida)**: `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/c262/`
**Scripts** (READ-ONLY sobre dados; nenhum invoca `experiments.py`, nenhum toca `data/experiments`): `t11_A_smoke.py` (smoke T11: params/acqf_hp/guards/joias/camadas) · `t11_B_s42.py` (21 células: U1–U10, delta de instrumentação, joias 1 e 2) · `t11_C_joia4.py` (identidade do ruído, ARD, DoE com cast float32, determinismo do refit) · `t11_D_controle_acqfhp.py` (**controle**: `acqf_hp` × objeto BoTorch + controle negativo) · `t11_E_saude.py` (régua Sobol, pisos, rank, sonda do smoke) · `t11_F_sigma_u7_ancoras.py` (diff do `sigma_dict`, U7, 4 âncoras) · `t11_G_i12.py` (verificação da regra I-12 em dois critérios).
**Evidência**: `c262_t11_s42.csv` (21×80) · `c262_t11_joia4.csv` · `c262_t11_i12.csv` · `c262_t11_sonda.csv` (46 célula-objetivo) · `c262_t11_pisos.csv`.
**Dados lidos (read-only)**: `/Users/gmello/Documents/python_repos/mestrado/resultados_experimentos/c262/{problema}/42/` (21 células) · `/Users/gmello/Documents/python_repos/mestrado/resultados_experimentos/_old/_bucket_raw/experiments/{main,batch}/c262/` (as 9 fora da análise) · `/Users/gmello/Documents/python_repos/mestrado/evidencia_T11/smoke_python/experiments/main/c262/` (o smoke do HEAD) · `f5/{sonda_f52e,metricas_finais_f52c,contrato_f52b,integridade_f52a,tempo_f52d}.csv` · `src/c262_qnehvi.py`, `src/botorch_harness.py`, `src/manifest.py`, `tests/test_c262.py` (leitura) · artigo `.md` (verificação verbatim dos §9/§G.2/§G.4/§H.7/fn.4).