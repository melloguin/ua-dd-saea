# handoff/R1-b3.md — Rodada 1, fan-out: b3 K-RVEA (built-in PlatEMO 4.15)

**Data:** 2026-07-16 · **Env:** MATLAB **R2025a** (Mac arm64) + PONTE `pyenv` InProcess
(`~/ponte_teste/bin/python`; numpy 2.4.6 · pymoo 0.6.2 — verificada ANTES, D80/D81).
Leitor Python independente no **env-main** (`.../mestrado_experimentos_dissertacao/bin/python`).

**Gate objetivo (encanamento, D97):** `accept.py R1-b3 --alg b3 --problema {MMF1,ZDT1,DTLZ2}
--semente 0` → **exit 0 (VERDE)** nos três: 4 saídas, **FE=31D−1 EXATO**, CP-init bit-a-bit. ✅
**Fidelidade:** instrumentada e entregue para a validação MANUAL do autor (D97) — **não é gate meu**.
**Regressão:** F0-01/02/03/04, R1-00 (stub), R1-c217 (MMF1/ZDT1/DTLZ2 — já com o c217-fix-log
`f8f9835` incorporado) e R1-c141 (MMF1) seguem VERDES; `preflight.py` exit 0 (b3-guard = APLICADO).

**Escopo:** 2º fan-out do padrão do caso-modelo c217. **O padrão generalizou de novo sem NENHUMA
mudança na infra transversal** (zero edição em FEBudget/RunBuffer/hook_output/write_*/despachante):
`run_b3` = cópia do `run_c217` com os deltas do b3; `c217_batch_eval` (D61) reusado como está.
Eu NÃO julguei fidelidade (D97). **Coexistência com o c217-fix (sessão paralela): conjuntos de
escrita disjuntos confirmados** — o fix tocou só `src/c217_instrument.m`+handoff; código b3 foi
escrito antes, e MATLAB/commits só DEPOIS do `[c217-fix-log]` fechar (git log verificado).

---

## 1. Resultado do gate (por problema, semente 0)

| Problema | D | M | maxFE=31D−1 | FE_final | EXATO | CP-init | ciclos | ramos (APD×incerteza) | u_ef | \|A1\| | cache_hits | accept.py |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **MMF1** (gate) | 2 | 2 | 61 | **61** | ✅ | ✅ | 8 | APD 8/8 | 5/5 | 21 (=NI) | 2 | **VERDE** |
| **ZDT1** (gate) | 30 | 2 | 929 | **929** | ✅ | ✅ | 120 | APD 120/120 | 5/5 | 329 (=NI) | 3 | **VERDE** |
| **DTLZ2** (gate) | 12 | 3 | 371 | **371** | ✅ | ✅ | 48 | **incerteza 44** / APD 4 | 5/5 | 131 (=NI) | 1 | **VERDE** |

`cache_hits`: 1 = o probe absorvido (receita N.0); MMF1 teve **+1** e ZDT1 **+2 duplicatas REAIS
de infill** (PopDec parte de A1Dec → membro do arquivo re-selecionado) — **o sync D89 foi
exercitado de verdade** e o FE fechou exato mesmo assim. `u_efetivo=5` em 100% dos ciclos
(nenhum cluster vazio; guarda `updata_next0` armada, **não disparou**). **|A1| constante = NI**
nos 3 runs (arquivo fixo — o payoff da Fig. 9 do paper reproduzido na camada §17.6: curva de
fit PLANA: ZDT1 med 3,36 s/max 4,11 s por retreino com n=329; DTLZ2 0,29/0,42 s; MMF1 0,01 s).

## 2. Patches aplicados (por arquivo:linha)

**Em `algorithms/_PlatEMO/.../K-RVEA/` (built-in 4.15; init STOCK `NI=11D−1` na :32 — sem patch):**
- `KRVEA.m:33-34` — **injeção do DoE** (D63/D87/D88): `A2 = Problem.Evaluation(Problem.data.X0)`
  substitui o PAR `UniformPoint('Latin')` + re-escala **JUNTO** (classe D94 — X0 NATIVO direto,
  a re-escala nunca roda).
- `KRVEA.m` (fit) — `tic/toc` nos M `dacefit` + `n_treino=|A1|` (§17.6). Decisões intactas.
- `KRVEA.m` (w-loop, pós-seleção) — snapshot `{PopDec,PopObj,MSE(index,:)}` da pop SELECIONADA
  de CADA geração interna (DEF-C2 — captura, não decide).
- `KRVEA.m` (fim do ciclo) — `[PopNew,sel,NumV2,Flag] = KrigingSelect(...)`;
  `[A1,nzero] = UpdataArchive(...)`; **sync D89** `Problem.FE = Problem.data.bud.fe` (herdado do
  c217/PCS:56 — obrigatório: o b3 propõe duplicatas, ver §1); chamada `b3_instrument(...)`.
- `KrigingSelect.m:1` — **patch 1 linha (L.2)**: assinatura exporta `index` (:64)/`NumV2` (:15)/
  `Flag` (:42) — variáveis que a função JÁ computa; decisões intactas.
- `UpdataArchive.m:61` — **guarda do crash latente** (N.3): `nzero=sum(Next==0)` +
  `A1 = [Total(Next(Next~=0)),New]` — filtra zeros de clusters vazios do kmeans ANTES de indexar
  (mesmo idioma do KrigingSelect:64); caso sem zeros = **bit-idêntico ao stock**; 2º output
  `nzero` só-leitura → evento `guard updata_next0` no `.jsonl`.

**Integração [IMPL] (repo raiz):**
- `src/experiment.m` — `case 'b3' → run_b3` (corpo = cópia do `run_c217`): ponte → `load_doe` →
  **check dedup-DoE** (L.2: "1º fit sem dedup"; duplicata no artefato → guard `dedup_doe` +
  falha honesta — não disparou; o LHS-maximin é livre de duplicatas por construção) → FEBudget
  ANTES do Problem → UserProblem `'N',100` com `initFcn=DoE[0]` (probe absorvido) → reuso do
  `c217_batch_eval` (D61) → `rng(semente)` depois → `KRVEA('save',-20,...)` **sem `'parameter'`**
  (α=2/wmax=20/μ=5 = defaults do código = paper) → export 4 camadas → CP-init → manifesto →
  falha honesta (D23/D81). + `ensure_paths_b3` (PlatEMO no path, N.0.1).
  **`'N',100` = nº de vetores de referência** (D20 — paper não prescreve N p/ M=2): UniformPoint
  ajusta p/ **100 (M=2) / 91 (M=3)** → δ=0.05·N = **5 / 4,55** (hardcoded no call-site
  KRVEA:79 — S.2#6); N_vetores/δ efetivos no header do jsonl e no manifesto.
- `src/b3_instrument.m` (**novo**, molde do c141_instrument) — ver §4.
- `claude_code_context/artifacts/anchors.json` — âncora `b3-updataarchive-guard` **concretizada**
  (placeholders `<...>` → literais reais `A1 = [Total(Next),New];` → `A1 = [Total(Next(Next~=0)),New];`)
  no precedente da Sessão 1/c141; `preflight.py` = **APLICADO**, exit 0. ⚠ **Sinalizado para seu
  veto** (re-endereçamento de âncora). `repos.lock` **intocado** (PlatEMO é git-pinned; preflight verde).

## 3. O que este 2º fan-out re-confirmou do padrão c217 (p/ b1, b4, e7, c238)

1. **`run_<alg>` por cópia segue funcionando**: deltas = N (semântica de vetores), sem
   'parameter', check dedup-DoE e o instrument próprio. Infra transversal INTOCADA.
2. **O sync D89 deixou de ser teórico**: 1ª vez que um fan-out observa duplicata REAL de infill
   (MMF1 +1, ZDT1 +2) — sem o sync, o `NotTerminated` teria parado o run curto e o gate
   reprovaria (exatamente o cenário ZDT1-926 do handoff c217 §3).
3. **`c217_batch_eval`/probe/CP-init**: reproduziram-se sem ajuste (3º algoritmo seguido).
4. **Guardas armadas ≠ disparadas**: `updata_next0` e `dedup_doe` logam quando dispararem na
   bateria (nenhum disparo na semente 0 — kmeans não devolveu cluster vazio).

## 4. Instrumentação produzida (para a SUA validação de fidelidade — D97)

Por run: **① real** (31D−1, init+opt) · **② pop** (membership do A2 por ciclo, todas as
gerações) · **③ surrogate** (DEF-C2/B3.2): a pop SELECIONADA de **CADA geração interna** do
RVEA-sobre-o-surrogate (wmax=20/ciclo — TODAS; MMF1 12.162 linhas, ZDT1 221.339, DTLZ2 63.807),
`pred_tipo=valor`, `modelo=GP-DACE`, `mu_j` = objetivos preditos pelos DACEs, **`sigma_j =
sqrt(max(MSE_j,0))` por objetivo (B3.2** — unidade de export; **o critério interno do switch usa
a MÉDIA das MSEs SEM raiz**, código oficial, intocado), `real_solution_id` nullable (membros do
arquivo → id; offspring → NULL; coluna double+NaN — fallback R1-00, consolidação re-casta).
⚠ **Decisão de carimbo (sinalizada p/ seu veto):** TODAS as linhas ③ de um ciclo levam
`geracao = CICLO` (a predição é datada pelo RETREINO — §17.1; join direto com a ② por geração,
D31); os blocos por geração interna ficam em ORDEM de linha, com as contagens em `pop_por_w`
na linha `b3_gen` do `.jsonl` → o filme é 100% reconstruível sem coluna extra no schema §17.2.

**`.jsonl` §17.5/S.7** (linha `b3_gen` por ciclo): `n_treino` (=|A1| no fit), `arquivo`
(|A1| pós-update — encolheria p/ NI−5+u se u<5; ficou =NI), `u_alvo`/`u_efetivo`,
**`NumV1`/`NumV2`/`Flag`/`delta`/`ramo` (APD×incerteza) + `motivo`** (a desigualdade que
disparou), `index` exportado (KrigingSelect:64), `lote`, `pop_por_w`, `nzero_updata`,
`tempo_fit_s`; + eventos `guard` (`cache_hit`, `hard_stop`, `updata_next0`, `dedup_doe`).
**Timing §17.6**: 1 retreino/ciclo, `n_acumulado` = |A1| **constante = NI** → curva plana.

**Números por problema (semente 0) — GUIA, não gate (D97):**

| Problema | FE | IGD_raw | \|ND\| | switch observado | âncora do paper (Anexo J) |
|---|---|---|---|---|---|
| MMF1 (d=2) | 61 | — (MMF: IGDX é pós-hoc D99) | 14 | APD 8/8 (M=2, δ=5) | — |
| ZDT1 (d=30) | 929 | **1,8818e-2** (front analítico, 10k) | 104 | APD 120/120 | — (paper não roda ZDT) |
| DTLZ2 (d=12, M=3) | 371 | **3,5273e-1** (das-dennis→esfera, 5050) | 48 | **incerteza 44/48**, APD 4/48 | **0,155** @ d=10, 109 init, 300 FEs |

> ⚠ **CAVEATS para a sua leitura (D97 — eu não julgo):**
> 1. **Orçamento/d divergem do paper**: âncora J é a d=10 (NI=109, 300 FEs); nosso protocolo
>    roda d=12 (NI=131, 371 FEs). A comparação justa é do **mecanismo** (switch APD×incerteza,
>    u efetivo, |A1| fixo — tudo no `.jsonl`), não do IGD cru.
> 2. **O switch se comportou como a v2.2 previu**: M=3 → δ=4,55 com Flag INTEIRO ⇒ incerteza
>    sse Flag≥5 (DTLZ2: 44/48 ciclos em incerteza); M=2 → δ=5 e Flag≤5 comum (APD dominou em
>    MMF1/ZDT1 — 100%). **Estado 'incerteza' NUNCA disparou em M=2, semente 0** — vigie na
>    bateria (se nunca disparar em nenhum M=2, é comportamento, não bug: Flag=NumV2−NumV1
>    raramente excede 5 com 100 vetores em 2-obj).
> 3. **IGD é o raw** (calculei fora do `metrics.py`: ZDT1 = front analítico 10k; DTLZ2 =
>    das-dennis 5050 → esfera) — compute do SEU jeito a partir da ① crua.
> 4. **Fidelidades ABERTAS conhecidas mantidas (não são bug)**: gestão do A1 vs Alg. 4 do paper
>    (B3.5 — `unique` lex + teto NI + mu fixo, código oficial); bug de índice do ramo (a)
>    alcançável só D≤9 (B3.5, intocado); overshoot nativo ≤+4 da :80 (o hard-stop do wrapper
>    corta exato — D21); predictor MSE por ponto em chamada 1-a-1 (idioma do KRVEA, mantido).
> 5. **Wall-clock semente 0**: MMF1 13,6 s · DTLZ2 139,6 s · ZDT1 **1.570 s (~26 min)** — o
>    pior-caso de timing do GP da R1 (§22.2) confirmado; extrapolação p/ 30 sementes do ZDT1
>    ≈ 13 h/core (o piloto §22.5 dimensiona).

## 5. Como reproduzir

```
# MATLAB (raiz do repo):
addpath('src'); addpath(genpath('algorithms/_PlatEMO/PlatEMO'));
experiment('b3','MMF1',0,'main','data')     % idem ZDT1, DTLZ2
% ou pelo despachante (skip idempotente; b3 já está no roster default):
experiments('algorithms',{'b3'},'problems',{'MMF1','ZDT1','DTLZ2'},'seeds',0,'parallel',false)

# Python (env-main), gate objetivo:
python3 scripts/accept.py R1-b3 --alg b3 --problema MMF1 --semente 0   # idem ZDT1/DTLZ2
```
Outputs em `data/experiments/main/b3/` (gitignored — regeneráveis).

## 6. Pendências / notas

1. **Commits `[R1-b3]`** (3): patches K-RVEA (3 arquivos `_PlatEMO`, rastreados pela raiz) ·
   adapter `run_b3` + `b3_instrument` + âncora concretizada · handoff + cartão. Nenhum blob.
2. **Para seu VETO (sinalizações desta sessão):** (a) âncora `b3-updataarchive-guard`
   concretizada com literais (§2); (b) carimbo `geracao=ciclo` na ③ com `pop_por_w` no jsonl
   (§4). Ambos documentados no manifesto (`sigma_dict`).
3. **Toolboxes em runtime**: kmeans/pdist2 (Statistics) — conforme N.3; sem DL.
4. **A validação de fidelidade é SUA (D97)**: os números da §4 são guia; o veredito (switch
   fiel? u/arquivo coerentes? σ com cara certa?) é seu, lendo os `.jsonl` (`ramo`/`Flag`/
   `NumV1`/`NumV2`) + a ①.
