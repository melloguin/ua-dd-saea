# handoff/R1-b4.md — Rodada 1, fan-out: b4 CSEA (built-in PlatEMO 4.15)

**Data:** 2026-07-16 · **Env:** MATLAB **R2025a** (Mac arm64) + PONTE `pyenv` InProcess
(`~/ponte_teste/bin/python`; numpy 2.4.6 · pymoo 0.6.2 — verificada ANTES, D80/D81).
Leitor Python independente no **env-main** (`.../mestrado_experimentos_dissertacao/bin/python`).

**Gate objetivo (encanamento, D97):** `accept.py R1-b4 --alg b4 --problema {MMF1,ZDT1,DTLZ2}
--semente 0` → **exit 0 (VERDE)** nos três: 4 saídas, **FE=31D−1 EXATO**, CP-init bit-a-bit. ✅
**Fidelidade:** instrumentada e entregue para a validação MANUAL do autor (D97) — **não é gate meu**.
**Regressão:** F0-01/02/03/04, R1-00 (stub), R1-c217 (MMF1/ZDT1/DTLZ2), R1-c141 (MMF1) e
**R1-b3 (MMF1/ZDT1/DTLZ2)** seguem VERDES; `preflight.py` exit 0 (b4-cap109 e b4-cpu = APLICADO).

**Escopo:** 3º fan-out do padrão do caso-modelo c217, na MESMA sessão do R1-b3. **Infra
transversal de novo INTOCADA** (zero edição em FEBudget/RunBuffer/hook_output/write_*/
despachante): `run_b4` = cópia do `run_b3`/`run_c217` com os deltas do b4. Eu NÃO julguei
fidelidade (D97).

---

## 1. Resultado do gate (por problema, semente 0)

| Problema | D | M | maxFE | FE_final | EXATO | CP-init | ger. | ramos (1/2/3/4) | stalls 0-FE | lote max | n_treino ini→fim | cache_hits | accept.py |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **MMF1** | 2 | 2 | 61 | **61** | ✅ | ✅ | 50 | 10/0/0/40 | 9 | 2 | 21→62 | 3 | **VERDE** |
| **ZDT1** | 30 | 2 | 929 | **929** | ✅ | ✅ | 475 | 140/0/0/335 | 69 | 10 | 329→929 | 2 | **VERDE** |
| **DTLZ2** | 12 | 3 | 371 | **371** | ✅ | ✅ | 256 | 19/0/0/237 | 16 | 1 | 131→370 | 1 | **VERDE** |

`cache_hits`: 1 = probe absorvido; MMF1 +2 e ZDT1 +1 **duplicatas reais de infill** → sync D89
exercitado (FE exato mesmo assim). **`n_treino` CRESCE com o arquivo** (o patch arquivo-inteiro
B4.6 em ação — contraste com o |A1|=NI constante do b3). **Stalls 0-FE existem e são design**
(gate L>0.9 devolve lote vazio; logados como `guard stall_0fe` — D60-c monitora): 9/69/16
gerações. Guarda `randperm_dim` armada, **não disparou** (impossível no nosso grid: linhas ≥ D).
Wall-clock: MMF1 33 s · DTLZ2 175 s · ZDT1 729 s.

## 2. Patches aplicados (por arquivo:linha)

**Em `algorithms/_PlatEMO/.../CSEA/`:**
- `CSEA.m:29` — **cap 109 removido** (âncora `b4-cap109`): `N = min(11D−1,109)` → `11*Problem.D-1`
  (fiel ao paper; o 109 era a regra congelada em d=10 e reduziria o DoE p/ D≥10). Preflight APLICADO.
- `CSEA.m:30-31` — **injeção do DoE** (D63/D87/D88): `Population = Problem.Evaluation(Problem.data.X0)`
  substitui o PAR `UniformPoint('Latin')` + re-escala (classe D94; `initFcn` NÃO alcança o CSEA — L.3).
- `CSEA.m:47` — **`ExecutionEnvironment` `'auto'` → `'cpu'`** (âncora `b4-cpu`; N.2.4 — com 'auto'
  os workers disputariam GPU e o rng não reproduziria o stream GPU). Preflight APLICADO.
- `CSEA.m:58-59` — **treino no ARQUIVO INTEIRO** (🔵 ARTIGO, B4.6/D30): `Input = Arc.decs;
  Output = GetOutput(Arc.objs, Ref.objs)` (o cap nativo = Population de tamanho N handicaparia o
  classificador); `rr`/`tr` derivam do arquivo rotulado; `Ref` continua da Population (stock).
- `CSEA.m` (fit) — `tic/toc` no `trainNetwork` + `n_treino=|Arc|` (§17.6). **Rede/treinador
  STOCK** (H=2D, zscore+BatchNorm+ReLU+sigmoide+MSE; adam lr=1e-3, 100 épocas, batch 32, rede
  NOVA/Glorot por geração — divergência em bloco vs paper LM/T=500 **mantida no CÓDIGO**, registrada).
- `CSEA.m` (fim do ciclo) — `[Next,sasinfo] = SurrogateAssistedSelection(...)`; **sync D89**
  `Problem.FE = Problem.data.bud.fe` pós-avaliação; chamada `b4_instrument(...)`.
- `SurrogateAssistedSelection.m:15/:24/:36` — **Balde C SUBSTITUI os operadores nativos
  `{1,15,1,5}` (η_c=15, η_m=5) por `{1,20,1,20}` (SBX 1/20 + PM 1/D/20)** — §6.2:445/473/811 +
  §6.4:523 da SPEC: o Balde C é global herdado pelos EAs; **c217/c122 são as exceções nomeadas
  ("respeita-se como Balde B") e o b4 NÃO está entre elas** — a nota do b4 diz explicitamente
  "SUBSTITUI". *(Por isso o patch não aparece no `params.json`: operadores são Balde C global,
  não Balde B por-config.)* ⚠ **Sinalizado para seu veto** (é o único patch desta sessão que
  muda comportamento de busca por PROTOCOLO, não por fidelidade-ao-paper).
- `SurrogateAssistedSelection.m:30` — **guard do bug latente**: `randperm(length(Next))` →
  `randperm(size(Next,1))` (`length()` = max(dim) excederia as linhas se D>|Pop|+6; caso
  não-degenerado = MESMO sorteio, bit-idêntico) + flag logada. `:42` — `randi(end)` →
  `ii = randi(size(Next,1))` (idêntico; só captura o índice p/ o L do selecionado).
- `SurrogateAssistedSelection.m:1` — 2º output `info` (ramo 1-4, L dos selecionados, flag do
  guard) — só captura; decisões INTACTAS (D97).

**Integração [IMPL] (repo raiz):**
- `src/experiment.m` — `case 'b4' → run_b4` (corpo = cópia do run_b3): **`'N',50` do paper
  (B4.2 — load-bearing: N limita o cap da Population via `RefSelect(Arc,Problem.N)` e o nº de
  gerações sob orçamento)**; CSEA **sem `'parameter'`** (K=6/gmax=3000 = defaults = paper);
  ponte → `load_doe` → FEBudget ANTES do Problem → probe absorvido → `c217_batch_eval` (D61) →
  `rng` depois → export 4 camadas → CP-init → manifesto (rede + **z-score da featureInputLayer
  registrado** — N.2.4; operadores; treino; gate 4 ramos) → falha honesta. + `ensure_paths_b4`.
- `src/b4_instrument.m` (**novo**) — ver §4.
- Âncoras: **nenhuma re-endereçada** (b4-cap109/b4-cpu já tinham literais corretos). `repos.lock`
  intocado.

## 3. O que este 3º fan-out re-confirmou (p/ b1, e7, c238)

1. Padrão `run_<alg>` por cópia: 4º algoritmo seguido sem tocar a infra transversal.
2. Sync D89 de novo material (duplicatas reais em MMF1/ZDT1).
3. O overshoot nativo do CSEA (FE ∈ [maxFE, maxFE+11]) **nunca aconteceu**: o hard-stop do
   wrapper corta o lote no meio (D61) — FE exato nos 3.
4. 1º algoritmo com **gerações 0-FE legítimas** no set: o watchdog D60-c tem o que monitorar
   (evento `stall_0fe` por geração no `.jsonl`).
5. 1º classificador do fan-out: o ③ 'classe' (DEF-C1) fluiu pelo `write_surrogate` sem ajuste
   (rsi int32 — todos os candidatos do ③ são avaliados, como no c217).

## 4. Instrumentação produzida (para a SUA validação de fidelidade — D97)

Por run: **① real** (31D−1) · **② pop** (membership do Arc por geração — cresce até 31D−1) ·
**③ surrogate** (DEF-C1, cartão b4 "L dos selecionados"): 1 linha `classe` por candidato do
**lote SELECIONADO** p/ avaliação real (MMF1 42, ZDT1 601, DTLZ2 240 linhas): `pred_classe` =
"bom"/"ruim" (leitura binária L≷0,5), **`pred_confianca` = L ∈ (0,1)** (sigmoide da FNN),
μ/σ/score NULL, `modelo=FNN`, `real_solution_id` **int32 sempre presente** (instrument roda
PÓS-avaliação). A semântica da SELEÇÃO (ramo 3 selecionaria L<0,1 — modelo invertido) está no
campo `ramo` do `.jsonl`, não na classe.

**`.jsonl` §17.5/S.7** (linha `b4_gen` por geração): **(p0,p1,rr,tr)**, `ramo` (1 confiável /
2 anti-loop / 3 invertido / 4 inconclusivo) + **`motivo`** (a desigualdade exata que disparou),
`lote` (**0 = stall**), `L_sel`, `arquivo`, `n_treino`, **`ref_ids`** (as K=6 referências da
geração — contexto §17.4); eventos `guard`: `stall_0fe`, `randperm_dim`, `cache_hit`,
`hard_stop`. **Timing §17.6**: 1 retreino/geração, `n_acumulado = |Arc|` **crescente** —
ZDT1 mediana 1,44 s/retreino (n 329→929), DTLZ2 0,60 s, MMF1 0,24 s.

**Números por problema (semente 0) — GUIA, não gate (D97):**

| Problema | FE | IGD_raw | \|ND\| | regimes observados | âncora do paper (Anexo J) |
|---|---|---|---|---|---|
| MMF1 (d=2) | 61 | — (MMF: IGDX pós-hoc D99) | 11 | ramo 1×10, ramo 4×40, 9 stalls | — |
| ZDT1 (d=30) | 929 | **3,9599e-1** (front analítico, 10k) | 32 | ramo 1×140, ramo 4×335, 69 stalls | ZDT1 d=30: 900 FEs (§IV — IGD não tabulado p/ comparação direta) |
| DTLZ2 (d=12, M=3) | 371 | **2,4086e-1** (das-dennis→esfera, 5050) | 45 | ramo 1×19, ramo 4×237, 16 stalls | **1,89e-1** (±1,13e-2) @ d=10, 109 init, 300 FEs |

> ⚠ **CAVEATS para a sua leitura (D97 — eu não julgo):**
> 1. **Ramos 2 (anti-loop) e 3 (invertido) NUNCA dispararam na semente 0** — p1 nunca excedeu
>    b=1−tr. Se nunca dispararem na bateria, é comportamento (p1 alto exige classificador
>    consistentemente invertido — raro), não bug; vigie pelo `ramo` do `.jsonl`.
> 2. **Ramo 4 (inconclusivo → 1 aleatório) domina** (67-93% das gerações): decorre de p0/p1
>    contínuos (MAE por classe) com o 0,4 hardcoded e tr pequeno sob desbalanceamento (rr
>    perto de 0/1 → tr→0 → a=tr aperta). Confira com a Tabela do paper (regimes R1/R2/R3).
> 3. **M=2 (MMF1/ZDT1): a projeção radial é quase-degenerada** (W₂=(−1, 1,22e−16) — grade
>    ~1-D; flag de análise §15.1, pré-registrada). Nenhum crash do RadarGrid ocorreu.
> 4. **Orçamento/d divergem do paper** na âncora DTLZ2 (d=12/371 vs d=10/300) — compare o
>    MECANISMO (p0/p1/regimes/L) e não o IGD cru; IGD é o raw (compute do SEU jeito da ①).
> 5. **Divergência em bloco treinador código×paper mantida (CÓDIGO)**: adam/100 épocas/rede
>    nova por geração vs LM/T=500/warm-start do paper — decisão prévia da SPEC, registrada no
>    manifesto; H=2D é a config que a ablação do próprio paper aponta como melhor p/ M=3 (M.3).

## 5. Como reproduzir

```
# MATLAB (raiz do repo):
addpath('src'); addpath(genpath('algorithms/_PlatEMO/PlatEMO'));
experiment('b4','MMF1',0,'main','data')     % idem ZDT1, DTLZ2
% ou pelo despachante (skip idempotente; b4 já está no roster default):
experiments('algorithms',{'b4'},'problems',{'MMF1','ZDT1','DTLZ2'},'seeds',0,'parallel',false)

# Python (env-main), gate objetivo:
python3 scripts/accept.py R1-b4 --alg b4 --problema MMF1 --semente 0   # idem ZDT1/DTLZ2
```
Outputs em `data/experiments/main/b4/` (gitignored — regeneráveis).

## 6. Pendências / notas

1. **Commits `[R1-b4]`** (3): patches CSEA (2 arquivos `_PlatEMO`) · adapter `run_b4` +
   `b4_instrument` · handoff + cartão. Nenhum blob.
2. **Para seu VETO:** a substituição Balde C `{1,15,1,5}→{1,20,1,20}` (§2 — decisão da SPEC
   §6.2/§6.4:523, mas é patch de PROTOCOLO que muda a busca; o `.jsonl`/manifesto registram).
3. **Toolboxes em runtime**: Deep Learning (trainNetwork/predict/sigmoidLayer etc.) +
   Statistics (pdist2 no RefSelect) — conforme L.3; licenças contendidas por worker na bateria.
4. **A validação de fidelidade é SUA (D97)**: números da §4 são guia; o veredito (gate 4-ramos
   fiel? p0/p1 coerentes? stalls aceitáveis?) é seu, lendo os `.jsonl` (`ramo`/`p0`/`p1`/
   `L_sel`/`stall_0fe`) + a ①.
