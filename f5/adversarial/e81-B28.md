## e81-B28 — VEREDITO: CONFIRMADO(d) — defeito de instrumentação, **porém DUPLICATA de decisão já ratificada (DI-42.7 → DI-42.2, autor, 2026-07-28)**

**Resumo em 3 linhas**
O fenômeno é real e eu o reproduzi de ponta a ponta, inclusive com **prova causal em sandbox**: a causa é `tests/test_batch_q10.py:168`, que chama `experiments._run_one("batch","e81","ZDT4",42,"data")` com `data_root` **relativo**, gravando 2 pares header/footer na célula de PRODUÇÃO a cada execução da suíte. O que **cai** é a tese de novidade do relatório ("a não-conformidade que nenhuma fase anterior pegou"): a PARTE A29 do `REGISTRO_DECISOES_IMPLEMENTACAO.md` (2026-07-28) já nomeia o arquivo exato no item **DI-42.7**, e a PARTE A31 registra a **ratificação do autor**. Dano aos dados = **zero, provado por SHA-256**; nenhuma célula sai da análise, nenhum número publicado muda — mas o vazamento **está vivo e crescendo** (90 → 94 → 104 pares).

---

**O que tentei para derrubar (os 4 passos, com números)**

**1 · Por DECISÃO — parcialmente derrubou.** Achei duas entradas que o analista não conhecia:
- `REGISTRO_DECISOES_IMPLEMENTACAO.md:1893` (PARTE A29, tabela DI-42, 2026-07-28), item **42.7**, textual: *"**Teste unitário escreve em `data/` de PRODUÇÃO (`test_batch_q10.py`) — poluiu o ⑥ de `exp_batch_e81_ZDT4_42` com 90 pares header/footer** | Corrigir já (tempdir)"*.
- `:1990` (PARTE A30) consolida-o em **DI-42.2** — *"células comprometidas da s42 [4: c311 big-mvns LinAlgError mascarada; moead ⑥ 2-escritores; treed ⑥ truncado; **e81-batch ⑥ poluído pelo teste**]"* — e `:2000` (PARTE A31) traz a ratificação: *"**.2=(c) NOVA DECISÃO DO AUTOR — NÃO re-rodar nada**: … as 4 células comprometidas ficam em **QUARENTENA documentada** (sem mutação de dado agora) e serão reavaliadas após a 2ª RODADA DE FIDELIDADE"*. O conserto de código já está no lote T11 (A30: *"teste→tempdir"*).
Isto **não sanciona** o comportamento (é quarentena, não desenho), então o achado sobrevive como fato — mas a frase do §5 do relatório ("**A não-conformidade encontrada por ESTA análise, que nenhuma fase anterior pegou**") é **falsa**, e a recomendação do §8 ("ler o caminho de resume/no-op do despachante… verificar se é achado transversal") já estava respondida há 24 h.

**2 · Por QUERY — não derrubou; reproduzi com código próprio e ampliei.** Contagem independente: 747 linhas, `{header:95, fit:200, sonda:101, decision:200, guard:56, footer:95}`, **0 linhas ruins** — bate. Separando por *shape*: **1 header do runner + 94 do despachante**, **1 footer do runner + 94 do despachante**; 100% dos espúrios **após** a linha 559 (footer do runner); janela `2026-07-24T12:22:28.863Z → 2026-07-28T18:42:14.673Z`; `tempo_total_s ∈ [0,0001; 0,8034]`, soma 6,6858 s; 100% com `status=ok, n_retries=0, stack_trace=null`. Agrupando por gap > 60 s dão **32 rajadas** {2:23, 4:5, 6:2, 8:2} = 94 — leitura equivalente à do analista (47 execuções × 2 pares). **Achado novo da minha varredura**: a cópia **viva do sítio** (`ua-dd-saea/data/experiments/batch/e81/`) já está em **105 headers / 105 footers**, última em `2026-07-29T01:59:34Z`. A série 90 (A29) → 94 (consolidado) → **104 pares** (sítio) prova que o vazamento **não foi corrigido** (HEAD `dac8896`, 2026-07-29 10:14; `tests/test_batch_q10.py` intocado desde 2026-07-25).

**3 · Por CÓDIGO — não derrubou; identificou o mecanismo por construção.** `experiments.py:100` grava `log.header(...)` **antes** do adapter e `:182` grava `log.footer(...)` em `finally` — sempre, mesmo com `_adapter` mockado; `src/audit_log.py:55` abre em `open(path, "a")` com `makedirs(exist_ok=True)`; `experiments.py:208-239` entra no ramo **MESCLA** e reescreve `updated_at` + `timing.tempo_total_despachante_s`. **Prova causal controlada** (`repro_controlado.sh`, sandbox fora do repo): rodar `tests.test_batch_q10.TestTetoFiadoPeloDespachante` cria `data/experiments/batch/e81/exp_batch_e81_ZDT4_42.jsonl` com **exatamente 2 headers + 2 footers**, com o **mesmo conjunto de chaves** dos 94 espúrios (`alg, exp, modo_rapido, problema, rec, run_id, semente, ts` / `n_retries, rec, stack_trace, status, tempo_total_s, ts`). 2 pares por execução × 47 execuções = 94. Não é resume/no-op do despachante em produção, como o §8 hipotetizou — é **teste**.

**4 · Controle positivo/negativo — o mais forte.** Varri **os 666 `.jsonl` das 24 configs** (`scan_hf.py` → `scan_hf.csv`): 355 arquivos com `1 header + 1 footer` (rito standalone), 300 com `1 header + 2 footers` (rito despachante), 9 sem footer, 1 com 3 footers — e **exatamente 1** com multiplicidade: `e81/q10_ZDT4/42`. Dentro do e81, **29/30 são (1 header, 1 footer runner, 1 footer despachante)**. Não é convenção; é singular. E é o único `_run_one` da suíte com raiz real — os demais (`test_roster_bateria.py:48/51`, `test_di13.py:24/37`, `test_di21_contrato.py:202`) usam tempdir.

---

**Por que não caiu (e o que caiu junto)**

Não caiu porque é fisicamente reproduzível e causalmente fechado. Caiu, do relatório: **(i)** a novidade; **(ii)** a hipótese de causa (resume/no-op do despachante → é teste unitário); **(iii)** um erro numérico na própria Evidência 5: *"③=277.043 (202.000 sonda + 75.043 online)"* é **q10_DTLZ2**; o ③ real de `q10_ZDT4` é **204.678 = 202.000 sonda + 2.678 online** (confusão de célula na query).

**Dano nulo aos dados — provado, não afirmado.** As 4 camadas da cópia consolidada e da cópia do sítio (que sofreu **mais 10 pares** depois da consolidação) são **bit-idênticas por SHA-256**: ① 2.109 linhas, ② 222.800, ③ 204.678, ④ 200. Independentemente: `|lote| = 10` em **200/200** gerações e **55 linhas `+qmaximin`** em 28 gerações — a célula é pós-fix `2fa542f`.

**Dano real, e é só este:** `manifest.timing.tempo_total_despachante_s = 0,0002 s` é **valor fabricado** pelo último no-op (nas outras 29 células é `tempo_total_s + 3..8 s`); `updated_at` 4 dias à frente de `created_at`; e todo consumidor que lê o **último** footer é enganado — `scripts/progress.py:95` (`next(reversed(tail))`) e o padrão `recs[-1]` de `scripts/accept.py:794`. O gate do e81 escapou porque `check_r3_e81` (`scripts/accept.py:1268-1292`) filtra `rec == "decision"` e nunca olha header/footer — precisão que corrige o §8 ("os gates não parseiam o ⑥": parseiam, mas só as decisões).

**Achado colateral novo (proveniência), verificado e benigno.** `q10_ZDT4` tem `created_at = 2026-07-24T12:21:10Z` (09:21:10 −03) — **3 dias antes** das 4 irmãs q=10 (2026-07-27T00:08…02:57Z). É o **smoke T6-batch**: o commit `2fa542f` (2026-07-24 09:21:53 −03, **43 s depois**) diz textualmente *"ACHADO no 1º smoke real q=10 do e81 (batch/ZDT4/42)"* e *"Pós-fix: … {10: 200} (2000/2000). wall ~35 min"* — casa com `tempo_total_s = 2076,75 s`. O resume a leu como PRONTA e a varredura de 07-27 a pulou. **Risco de deriva de build: NULO** — o único commit posterior a tocar `src/e81_qpots.py` é `c2a522d`, e o delta é **exclusivamente o rito straddle**, inalcançável aqui (2109 − 109 = 2000 = 200 × 10; **0 guards `hard_stop_lote`** no ⑥). `params`, `sigma_dict`, `env` e `algo_version` diferem das irmãs **apenas** nos itens dependentes de D — mesmo padrão do controle `q10_DTLZ2 × q10_ZDT1`.

---

**Enquadramento e bússola D29**

**(d) DEFEITO DE INSTRUMENTAÇÃO** — o mecanismo qPOTS e as 4 camadas estão certos; quem mente é o ⑥ e dois campos do ⑤. **Não é (c)**: nenhuma célula sai. **Não é (a) no eixo de fidelidade**: a bússola D29 **não se aplica** — o código culpado é `tests/test_batch_q10.py:168`, teste de infraestrutura, fora do runner, do surrogate e do artigo; não é 🔴 bug do autor original, nem 🔵 versão, nem 🟠 detalhe de implementação, nem 🟣 erratum, nem 🟢 extensão. É **higiene de suíte** (a A29 já pontuou a dimensão `testes` em 6/10). Ação de código existe e já está no T11.

---

**Impacto**

- **Células excluídas: nenhuma.** `q10_ZDT4` permanece na análise, sob a quarentena documentada da **DI-42.2(c)**.
- **Números publicados: nenhum muda.** `tempo_f52d.csv` linha 539 usa `tempo_total_s = 2076,7516` (não o campo corrompido); o batch e81 de 9,24 h-core continua correto; `gates_f51.csv` VERDE nas 2 linhas de ZDT4; `integridade_f52a.csv` e `contrato_f52b.csv` seguem com 0 linhas de e81.
- **Score:** o −0,5 do relatório é defensável pelo invariante de auditoria, mas **não pelo argumento de novidade**. Sugiro manter 9,5 e reescrever a justificativa como "duplicata confirmada de DI-42.7, com causa-raiz refinada e vazamento demonstrado ATIVO", em vez de "achado inédito".
- **Correções obrigatórias no `e81.md`**: §5 (retirar "nenhuma fase anterior pegou"; citar DI-42.7/DI-42.2); §8 Evidência 5 (③ = 204.678, não 277.043); §8 "Verificação pedida à F5.4" (a causa não é o despachante).
- **Transversalidade**: o padrão **não** existe em c154/c122/c149/c262 — 666/666 arquivos varridos, ocorrência única.

---

**Ação para a rodada perfeita (F5.7) + custo**

| # | ação | custo |
|---|---|---|
| 1 | `tests/test_batch_q10.py::TestTetoFiadoPeloDespachante._adapter_kwargs` → `tempfile.TemporaryDirectory()` no lugar do literal `"data"` (item "teste→tempdir" já no T11) | **10 min** |
| 2 | Guarda de suíte: teste que falha se qualquer teste criar/alterar arquivo sob `<repo>/data/experiments/` (snapshot de mtimes em `tearDownModule`) — fecha a **classe** do defeito, não a instância | **30 min** |
| 3 | Check estrutural na F5.2a: por ⑥, `count(header) == 1` e `count(footer) ≤ 2`, com o footer do runner obrigatoriamente portando `fe_final` — varre os 666 arquivos e teria pego isto, os 9 sem footer e o de 3 footers | **20 min** |
| 4 | Anotar (sem mutar — **DI-42.2(c)**) `q10_ZDT4.timing.tempo_total_despachante_s` como **inválido/None** no caveat da célula, e registrar a proveniência "smoke T6-batch de 2026-07-24, build `2fa542f`, equivalente comportamental a `c2a522d`" | **15 min** |
| 5 | Endurecer `scripts/progress.py:95` e o padrão `recs[-1]` de `scripts/accept.py:794` para **exigir `fe_final`** ao eleger o footer do runner (hoje pegam o último, que pode ser do despachante) | **20 min** |
| 6 | **NÃO** limpar o ⑥ agora (truncar na linha 559 restauraria o invariante) — vetado pela DI-42.2(c) "sem mutação de dado agora"; reavaliar após a 2ª rodada de fidelidade | 0 |

**Custo total: ~1 h 35 min, sem re-run.**

**Artefatos** (todos em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/baterias/f54/e81-B28/`): `scan_hf.py` + `scan_hf.csv` (varredura de controle dos 666 `.jsonl`), `analise_b28.py` (dissecção sítio × consolidado), `repro_controlado.sh` (prova causal em sandbox), `evidencias_b28.json` (todos os números acima).