# RELATÓRIO DE FIDELIDADE — b3 (K-RVEA, PlatEMO 4.15) · **validação T11** · comparação contra a F5

**Corpora auditados.** **(A) s42 — mecanismo:** 25/25 células de `/Users/gmello/Documents/python_repos/mestrado/resultados_experimentos/b3/{problema}/42/` — **1.443 ciclos · 7.215 seleções · 10.856 linhas de ① · 3.776.308 linhas de ③** (2.254.308 online + 1.522.000 de sonda em 761 blocos) · 25/25 em `vm3` (`tempo_f52d.csv`, wall 9.220,2 s = 2,56 h-core). **(B) smoke T11 — instrumentação nova:** `evidencia_T11/smoke_matlab/experiments/main/b3/MMF1` (1 célula, 8 ciclos) + o **par §3.1** `g6_com` × `g6_sem` (MMF1). Toda a re-medição desta rodada é minha, sobre os dados; onde reaproveito insumo da F5 eu digo.

**Fato que organiza o relatório:** para o b3 a instrumentação T11 é **um único campo novo no ⑥ (`adapt_delta_V`) e dois no ⑤ (`campanha_id`, `sonda.desligada`)** — medido campo a campo (`smoke_joia.py`): dos 11 campos que o `contrato_61.json` exige do b3, **a s42 já trazia 10**; do ⑤, só faltava `campanha_id`. E o par g6 fecha, com dado, o único T estrutural que a F5 não pôde fechar.

---

## 1. Ficha do mecanismo (condensada)

Chugh, Jin, Miettinen, Hakanen & Sindhya, IEEE TEVC 22(1):129-142. DoE LHS NI=11n−1 → **A1** (treino, tamanho FIXO NI) e **A2** (não-dominados). Um Kriging DACE por objetivo sobre A1. Ciclo: (B) wmax=20 gerações do RVEA sobre o surrogate (SBX+PM, APD com penalidade angular, vetores adaptativos Va); (C) Alg. 3 escolhe u=5 por clustering dos vetores ativos — **mín-APD** (convergência) ou **máx-incerteza** (diversidade) conforme ∆Vf ≶ δ sobre os vetores FIXOS Vf; (D) Alg. 4 repõe |A1|=NI por clustering; retreina; w←1. FEmax=300 no paper.

Nossa instância (MATLAB R2025a): patch de DoE D94 (`KRVEA.m:33-36`), **DoE livre de duplicatas** (DEF-A8), guard do crash latente (`UpdataArchive.m:66-70`), hard-stop exato sobre o overshoot nativo ≤+4, instrumentação DI-10/L.2. **Divergências sancionadas mantidas no CÓDIGO (D29 🟠 impl→código):** critério = **média das VARIÂNCIAS sem raiz** (`KrigingSelect.m:56` — `Uncertainty = mean(MSE(current,:),2)`); operando do switch = **arquivo real × pop predita, ambos contra V0** (`KRVEA.m:98` + `KrigingSelect.m:23`); **ausência do "1º update sempre APD"**; **bug de índice do ramo (a) do `UpdataArchive`** (`:41-51` + `:70`); N=100/91 (D20), regpoly1+corrgauss, θ0=5, bounds [1e-5,100], warm-θ (D30).

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.1 Tabela-resumo (25 aspectos; IDs preservam a F5 para comparação 1:1)

| # | aspecto | classe F5 | **classe T11** | verificab. | resultado-síntese (re-medido) |
|---|---|:---:|:---:|---|---|
| U1/B11 | Orçamento, término, overshoot ≤+4, hard-stop | (2) | **(2)** | direta | 31D−1 exato 25/25; `fe_index` denso 25/25; resto∈[0,4] 25/25; `hard_stop` ⟺ resto>0 **25/25** |
| U2 | DoE 11D−1 do artefato, livre de duplicatas | (1) | **(1)** | direta | init=11D−1 25/25; `doe_hash`≡sidecar 25/25 (F5: ΔX=0,0 exato) |
| U3/B2 | 1 retreino por ciclo · wmax=20 | (1) | **(1)** | direta | `len(pop_por_w)`=20 em **1.443/1.443**; ④ = 1.443 linhas ≡ 1.443 ciclos |
| U4/U7 | Cadência da sonda + finalProbe · invariante de timing | (2) | **(2)** | direta | fórmula {1}∪{pares≤G}∪{G} fecha **25/25**; `fit+busca ≤ tempo_geracao` **1.443/1.443, 0 violações** |
| **U5** | **Não-perturbação da sonda** | **T** | **(2)** 🆕 | **direta** | par g6: ①②③-online **BIT-IDÊNTICOS**; ⑥ 8/8 ciclos idênticos, **max\|Δapd_sel\| = 0** |
| U6/F5 | WAPE, cobertura e calibração de σ (régua Sobol) | (2) | **(2)** | direta | ΔWAPE mediano **+4,40%** (n=56 pares célula×obj); cobertura 0,932→0,924; **0 NaN** em 1.522.000 medições |
| U8 | `fe_treino_max` | (2) | **(2)** | direta | ≡ fe_anterior−1 **1.443/1.443**; **monotônico 25/25** (≠ b1/b4/c217, regra 9) |
| U9/N1 | Reconciliação entre camadas · ledger · c0=1 | (2) | **(2)** | direta | guards≡manifesto 25/25; **c0=1 em 25/25**; ⑥ 0 linhas malformadas, header+footer 25/25 |
| U10 | Erro de fantasia dos infills | (2) | **(2)** | direta | mediana 0,2076 (0,000–1,438); otimista 0,577, >50% em **18/25** |
| F1 | **Query-joia `index`→③→σ do selecionado** | (1) | **(1)** | direta | **7.215/7.215**, erro rel. máx **5,92e-8** (ULP do float32) |
| F2 | Critério = média das VARIÂNCIAS, sem raiz | (2) | **(2)** | direta | identidade em **7.215/7.215**, erro rel. máx **4,31e-16** |
| F3 | Cadeia seleção → avaliação real | (2) | **(2)** | direta | 7.215 X localizados na ①: 7.004 novos + 211 pré-existentes, **0 miss** |
| F4 | Separação estatística ramo ⇔ σ/APD | (1) | **(1)** | direta | percentil de meanMSE 0,916 (incerteza) × 0,355 (APD) |
| F6 | Curva de θ / MLE | **T** | **T** | não-verif. | `modelo_hp` traz regr/corr/bounds/n/tempo — **θ nunca é logado** |
| B1/B10 | N vetores · DACE · σ de export | (2) | **(2)** | direta-declarativa | 100 (M=2)/91 (M=3) 25/25; **0 σ<0, 0 σ NaN, 0 μ NaN** em 3.776.308 linhas |
| B3 | u=5 com mín{u,\|Va^a\|} | (1) | **(1)** | direta | `u_alvo`=`u_efetivo`=5 e `n_vetores_vazios`=0 em **1.443/1.443** |
| B4 | δ=0,05·N e a regra do switch | (1) | **(1)** | direta | δ 25/25; Flag≡NumV2−NumV1 e ramo≡(Flag≤δ) em **1.443/1.443** |
| B5 | Operando do switch: arquivo real × pop predita | (2) | **(2)** | direta | NumV1(k)=NumV2(k−1) em só **84/1.418 (5,9%)** |
| B6 | Ausência do "1º update sempre APD" | (2) | **(2)** | direta | **5/25 células iniciam em `incerteza`**; 343/1.443 ciclos (23,8%) usam incerteza |
| B7 | \|A1\| ≡ NI fixo (Fig. 9) | (1) | **(1)** | direta | `n_treino`≡`arquivo`≡11D−1 em **1.443/1.443** |
| B8/B9 | Poda do A1 (proxy `dist_min_arquivo`) · pop reinicia do arquivo | (2) | **(2)** | indireta | 7.196/7.215 (99,74%) com desigualdade OK, resíduo ≤1,18e-5 (ruído float32); igualdade em 65,9%; rsid 0,556→0,063 |
| **B12/B13** | **Guard `nzero` · ramo (a) do `UpdataArchive`** | **T** | **(2)** 🆕 | **direta-forense** | `nzero`=0 em 1.443/1.443; **ramo (a) DISCRIMINADO: dispara em MMF1/MMF4/MMF11_L, não dispara em DTLZ1, impossível em 21/25** |
| B14 | Miolo do RVEA interno (operadores, k-means, `randi`) | **T** | **T** | não-verif. | sem rastro recuperável |
| **N2** | **`adapt_delta_V` — a instrumentação NOVA da T11** | — | **(2)** 🆕 | direta | recomputo independente casa a **≤6,3e-6** relativo; **9 de 10 eventos por ciclo**, o maior fica de fora |
| **N3** | **Reprodutibilidade cross-máquina e a navalha do dedup** | — | **(2)** 🆕 | direta | ③ **100% bit-idêntica** vm3×Mac (12.709 online + 10.000 sonda); ① 60/61; divergência única no dedup float64 do último FE |

### 2.2 Blocos narrativos

**U1/B11 — Orçamento, término e o overshoot.** (a) O paper fixa FEmax=300 (§IV-A.3), i.e. 191 FEs de otimização depois de NI=109. (b) A SPEC substitui por `maxFE=31D−1` com hard-stop exato (D21/D61), fração DoE/orçamento constante em 35,5% — decisão de comparabilidade transversal; e DEF-A2 registra que o lote de u=5 pode ultrapassar em até +4 (`KRVEA.m:104`). (c) Re-medido em `remede_s42.py`: `len(①)==31D−1` e `fe_final==maxfe` em 25/25, `fe_index` denso 0-based em 25/25, `solution_id` únicos em 25/25, `footer.termino='normal'` em 25/25 (mapa de término MATLAB, nunca `status`). `resto = maxfe − fe(último ciclo)` ∈ [0,4] em **25/25** e o guard `hard_stop` está presente **se e somente se** resto>0, em **25/25** (16 com, 9 sem). (d) O ciclo G=n+1 começa, sonda, e a exceção cai dentro de `Problem.Evaluation` (`KRVEA.m:104`) — **antes** de `b3_instrument` (`:114`), o que explica sem resíduo por que esse ciclo não tem linha no ④ nem evento `b3_gen`, mas tem bloco de sonda. (e) **(2)** — direta.

**U2 — DoE.** (a) §III-A: "an initial population of size NI ... using e.g. the Latin hypercube sampling", NI=11n−1 (§IV-A.1) — fórmula idêntica à nossa. (b) Divergência só de PROVENIÊNCIA (D63/D88: LHS vem do artefato, nunca regenerado) mais a decisão específica do b3 de exigir DoE **sem duplicatas**, porque `dacefit.m:93` aborta com `Multiple design sites are not allowed` (DEF-A8). (c) `(fase=='init').sum()==11D−1` em 25/25; `doe_hash`≡sidecar em 25/25; 0 duplicatas de X na ① em 25/25 (`sid_unicos`). (e) **(1)** — direta.

**U3/B2 — A estrutura do ciclo.** (a) Alg. 2 passos 3-10 + §IV-A.7 ("for simplicity we adopt a prefixed frequency"; o paper mostra K-RVEA insensível a wmax). (b) Sem mudança. (c) `len(pop_por_w)==20` em **1.443/1.443 ciclos**; ④ com 1.443 linhas ≡ 1.443 eventos `b3_gen` ≡ `len(fit_series)`; `geracao` sequencial 1..n em 25/25. (d) `geracao` na ③ é o **CICLO**: cada valor carrega 20 sub-blocos concatenados em ordem de linha, cujos tamanhos só existem em `pop_por_w` — sem essa chave a query-joia é impossível. (e) **(1)** — direta.

**U4/U7 — Cadência da sonda e o relógio do ciclo.** (a)/(b) Instrumentação nossa (DI-12.5 finalProbe; DI-13.10 timing). (c) A fórmula fechada `blocos = {1} ∪ {pares ≤ G} ∪ {G}` com `G = n_ciclos + 1{hard_stop}` bate **exata em 25/25** (`cadencia_ok`), todos os 761 blocos com 2.000 pontos; e `tempo_fit_s + tempo_busca_s ≤ tempo_geracao_s` em **1.443/1.443 linhas, zero violações**. (d) O relógio da geração no b3 **desconta a sonda** por construção (`b3_instrument.m:75`: `tempo_geracao_s = max(tger_s − tps, 0)`) — por isso a segunda perna do DI-13.10 (violação exatamente nas gerações com sonda) aparece aqui em versão b3-específica, contra `tempo_geracao_s + sonda`. (e) **(2)** — direta.

**U5 — Não-perturbação da sonda. 🆕 O T da F5, fechado com dado.** (a)/(b) Contrato 🔴 §3.1: a sonda consome tempo e não pode alterar a trajetória. Na F5 isto era **T** ("exige run pareado"). (c) O par existe agora e eu o medi inteiro (`g6_par.py`): `g6_com` × `g6_sem` da mesma célula (main/b3/MMF1/42) dão **① bit-idêntica** (61 linhas, hash `06691c98538abb6a` nos dois), **② bit-idêntica** (369 linhas, `398d65fd68237134`), **③ online bit-idêntica** (12.709 linhas nos dois; o lado "com" carrega +10.000 linhas de `regime='sonda'`), ④ com as mesmas 8 linhas, e **os 8 eventos `b3_gen` idênticos em TODOS os campos de decisão** (`geracao,fe,n_treino,arquivo,u_alvo,u_efetivo,NumV1,NumV2,Flag,ramo,lote,index,pop_por_w,nzero_updata,fe_treino_max`), com **max\|Δ apd_sel\| = 0** — zero exato, não "próximo de zero". Os três sinais que a T11 exige de um par válido estão todos presentes: ⑤ `sonda.desligada` false→**true**, `n_blocos` 5→**0**, `tempo_pred_sonda_s` 0,2558→**0**, e a ③ **encolhe** 22.709→12.709 linhas (a sonda RODOU do lado "com"). (d) A sonda do b3 é chamada em `KRVEA.m:67`, depois do fit e **antes do primeiro consumo de RNG** do ciclo (`OperatorGA`), e o `b3_sonda` usa o mesmo handle `@predictor` do fit; não há caminho pelo qual ela toque o gerador. (e) **(2)** — direta. **T eliminado.**

**U6/F5 — A régua Sobol: WAPE, cobertura e calibração.** (a)/(b) Régua nossa; `espaco_modelo='cru'` e `transf_params` NULL em 100% das linhas, então a des-transformação é a identidade. (c) Do `sonda_f52e.csv` com `bloco` convertido a INT (armadilha 8 da F5): ΔWAPE 1º→último bloco por (célula,objetivo), n=56 — **mediana +4,40%**, q1 −64,2%, q3 +47,4%, **piora em 28/56**. Cobertura ±1,96σ por célula: mediana **0,932 → 0,924**; **14/25 terminam ≥0,90**; **6 terminam <0,60** (DTLZ7, MMF11_L, MMF4, ZDT1, ZDT3, ZDT4); **0 NaN** e `n_validas`=2.000 em todos os 1.706 registros. (d) A "sonda chata" tem causa estrutural: |A1| é FIXO em 11D−1, logo o Kriging é retreinado 1.443 vezes com o MESMO n; o que muda é a distribuição do treino, e o efeito líquido sobre um gabarito Sobol global é quase nulo. As 6 células sub-cobertas são diagnóstico, não patologia: DTLZ7/ZDT1/ZDT3 têm WAPE final 0,000-0,004 e o MSE do DACE encolhe rápido demais sob interpolação quase-exata. (e) **(2)** — direta.

**U8 — `fe_treino_max`.** (a) Não existe no paper. (b) A regra 9 do CONTRATO admite não-monotonicidade em b1/b4/c217 (subamostram o treino). (c) Aqui `fe_treino_max ≡ (fe do ciclo anterior) − 1` em **1.443/1.443** e é **estritamente monotônico em 25/25** — os dois testes fecham nas 25 células. (d) Assimetria do Alg. 4: a poda só atinge `A1 \ New` — os u recém-avaliados são imunes por construção (`UpdataArchive.m:70`: `A1 = [Total(...), New]`), logo o índice máximo do treino é sempre o último FE. (e) **(2)** — direta.

**U9/N1 — Reconciliação, ledger e o c0.** (a)/(b) D89 (cache-hit = 0 FE, sem linha na ①), D57 (dedup por X), DEF-C2. (c) `cache_hit` do ⑥ ≡ `manifest.cache_hits` em **25/25**; **exatamente um** `cache_hit` com `fe=1` em **25/25** (o c0, mesma assinatura estrutural do b1); ⑥ com **0 linhas malformadas** e header+footer em 25/25 (b3 não é o caso do `b1/WFG1`, §D-F da T11). (d) O cache-hit não gera linha na ① nem consome FE, então o algoritmo precisa de MAIS ciclos para esgotar o orçamento — é por isso que DTLZ7 roda 98 ciclos onde a aritmética ingênua previa 88. (e) **(2)** — direta.

**U10 — Erro de fantasia dos infills.** (a)/(b) Aspecto nosso (U11 do protocolo). (c) Sobre os 7.004 infills que abriram linha nova: erro relativo mediano por célula **0,2076** (0,000 em DTLZ7 a 1,438 em DTLZ4); fração otimista (μ<f) mediana **0,577**, >50% em **18/25**. (d) O viés otimista é esperado num seletor que, no ramo APD, minimiza uma quantidade construída sobre μ — minimizar herda o ruído negativo do modelo. (e) **(2)** — direta.

**F1 — A query-joia da família GP-BO, re-executada.** (a) Alg. 3 passo 8: seleção por "maximum uncertainty ... average of the standard deviations obtained from Kriging models" (§III-C). (b) A instrumentação L.2 exporta `index` (`KrigingSelect.m:69`, patch de 1 linha) e `sigma_sel` (DI-10). (c) Re-medido do zero (`joia_s42.py`): para cada ciclo, cortar a **última sub-população** da ③ (as `pop_por_w[-1]` linhas finais), indexar com `index` **1-based** e comparar σ_j com `sigma_sel.sqrtmse_por_objetivo` — **7.215/7.215 seleções**, erro **relativo** máximo **5,916e-8**, exatamente o ULP do float32 em que a ③ é gravada (D53). Todas as 25 células fecham. (d) A ordem de linha da ③ dentro de um ciclo É a ordem das 20 gerações internas, e `index` aponta para a população que entrou no Alg. 3. **A checagem tem de ser RELATIVA**: em BBOB_F17 σ chega a ~1e6 e o critério absoluto reprova 5 células fiéis. (e) **(1)** — direta. **O uso da incerteza está PROVADO pelos dados.**

**F2 — O critério é a média das VARIÂNCIAS, sem raiz.** (a) O paper diz literalmente "average of the standard deviations". (b) O código oficial faz outra coisa — e eu li a linha: `KrigingSelect.m:56`, `Uncertainty = mean(MSE(current,:),2); [~,best] = max(Uncertainty);`. A raiz só existe como unidade de EXPORT (B3.2, `b3_instrument.m:63`). A SPEC v5.2.1 corrigiu a própria prosa e a DI-18 congelou a leitura; bússola D29 🟠 impl→código. (c) `criterio_meanMSE[i] ≡ média_j(sqrtmse[i][j]²)` em **7.215/7.215**, erro relativo máximo **4,31e-16** — identidade aritmética, não aproximação. (d) Não é cosmético: a média de variâncias pondera quadraticamente o objetivo mais incerto; em WFG/BBOB o critério passa a ser dominado por um único objetivo. É desvio material do texto do paper e comportamento exato do código oficial, agora provado por dado nas duas rodadas (a mesma identidade fecha no smoke a 2,2e-16). (e) **(2)** — direta.

**F3 — Cadeia seleção → avaliação real.** (a) Alg. 2 passo 8. (b) D57 + D89. (c) Dos 7.215 X selecionados, **0 não encontrados** na ①: 7.004 abrem linha nova, **211 caem em linha pré-existente** (o RVEA re-propõe um ponto do arquivo). (d) É o mesmo fenômeno de B9: a pop interna parte de A1, e ~3% das seleções recaem sobre um sobrevivente real. (e) **(2)** — direta.

**F4 — O ramo realmente troca o critério.** (a) Alg. 3 passos 5-8, mutuamente exclusivos. (b) Sem mudança. (c) Percentil do meanMSE do selecionado dentro da população interna final: ramo **incerteza** (n=1.715) mediana **0,916**; ramo **APD** (n=5.500) mediana **0,355**; nenhum selecionado é o argmax GLOBAL nos dois ramos — e é assim que tem de ser, porque o Alg. 3 escolhe o argmax **dentro do cluster** (5 clusters sobre ~70 ⇒ percentil esperado 14/15 = 0,933 contra 0,916 medidos). O espelho vale no APD: nas 7 células mistas o `apd_sel` mediano é sempre MAIOR sob incerteza. (e) **(1)** — direta.

**F6 — Curva de aprendizado dos hiperparâmetros.** `hp_gp_b3` (`b3_instrument.m:190-192`) emite `regr`, `corr`, `theta_bounds`, `n`, `tempo_fit_s` — **e nada de θ**. Nem o θ final existe (diferente do b1). Bounds [1e-5,100], θ0=5 e a trajetória do warm-θ continuam não verificáveis por dado; a única evidência é indireta (o 1º fit custa 1,9-2,2× o mediano). **T** — e a T11 não mexeu nisso.

**B1/B10 — Os parâmetros declarativos e o σ de export.** (a) §IV-A.5 remete N ao material suplementar (indisponível) e não prescreve N para M=2 — o paper é many-objective; §II-B, Eqs. 7-16, dá o DACE. (b) D20 fixa N=100 (M=2)/91 (M=3), o que arrasta δ=5,00/4,55; D30 mantém regpoly1+corrgauss, θ0=5, warm-θ; B3.2 fixa o σ de export. (c) Eco em `header` e em `manifest.params` em 25/25 (19 células com 100, 6 com 91); e a saúde numérica: **0 σ negativos, 0 σ NaN, 0 μ NaN** em 3.776.308 linhas × M. σ=0 exato em **6.825 valores, todos em MMF11_L** (D=2, interpolação exata sobre clone). `params` presente em 25/25 — b3 nunca esteve entre os configs da ERRATA 11 da T11. (d) O guard `max(MSE,0)` está instalado e nunca precisou clampar; a ausência total de NaN num config que treina 1.443×M DACEs é o sinal de que "DoE livre de duplicatas" resolveu o DEF-A8 na raiz. (e) **(2)** — direta-declarativa (θ é T).

**B3 — u=5 e a cláusula mín{u,|Va^a|}.** (a) §IV-A.4 + Alg. 3 passo 1. (b) Sem mudança. (c) `u_alvo = u_efetivo = 5` e `n_vetores_vazios = 0` em **1.443/1.443** — a cláusula de degeneração **nunca ligou**, nem em D=2 com 100 vetores. Li a linha que a implementa: `KrigingSelect.m:25`, `NCluster = min(mu, size(V,1)-NVa)`. (d) A seleção ambiental do RVEA devolve um indivíduo por vetor não-vazio, então |Va^a| ∈ [5,N]; o caso patológico do paper (DTLZ5/6, ~70% dos vetores vazios) não está no nosso grid. (e) **(1)** — direta.

**B4 — δ=0,05·N e a regra do switch.** (a) §IV: "if the change in the number of the empty reference vectors is smaller than δ ... convergence, otherwise ... uncertainty". (b) Sem mudança de valor; com M=3 (N=91) δ=4,55 e Flag inteiro ⇒ "incerteza sse Flag ≥ 5". (c) δ≡0,05·N em 25/25; `Flag ≡ NumV2 − NumV1` (com sinal) em **1.443/1.443**; **`ramo ≡ (Flag ≤ δ ? APD : incerteza)` em 1.443/1.443 — 100%, sem exceção**, e o campo `motivo` reproduz a aritmética por extenso. (e) **(1)** — direta.

**B5 — O OPERANDO do switch (o achado comportamental central).** (a) O paper é inequívoco: ∆Vf = |Vf^ia|_tu − |Vf^ia|_tu−1, os dois medidos sobre os indivíduos, comparando a atualização atual com a **anterior**. (b) O código compara **arquivo real × população predita**, ambos contra V0 — eu li as duas chamadas: `KRVEA.m:98` (`[NumVf,~] = NoActive(A1Obj,V0)`) e `KrigingSelect.m:23` (`[NumV2,~] = NoActive(PopObj,V0)`). 🟠 impl→código. (c) `NumV1(k) = NumV2(k−1)` em apenas **84 de 1.418 transições (5,9%)** — e as 84 são coincidência numérica (concentram-se em BBOB_F17, onde os dois contadores saturam). (d) Daqui sai o **travamento** do switch: o sinal de (NumV2−NumV1) é quase-estático por par (geometria, N), então **16/25 células rodam 100% APD, 2/25 rodam 100% incerteza (DTLZ3 48/48, MMF16_20 80/80) e só 7/25 alternam**; no agregado, **343/1.443 ciclos (23,8%)** usam incerteza. (e) **(2)** — direta.

**B6 — A ausência do "1º update sempre APD".** (a) §III-C: "In the first update of the Kriging models, APD is always used". (b) O código oficial não tem essa guarda — conferido: não há nenhum teste de primeiro ciclo em `KrigingSelect.m`; e como NumV1 vem do arquivo (B5), o primeiro Flag é bem-definido em vez de indefinido. 🟠 impl→código. (c) **5/25 células iniciam o ciclo 1 no ramo `incerteza`**: DTLZ1 (Flag=+5), DTLZ3 (+32), MMF16_20 (+41), WFG5 (+7), ZDT1 (+10). (d) A regra do paper existe porque no desenho dele |Vf^ia|_{tu−1} não está definido no 1º update; no nosso operando ele está. Efeito material: DTLZ3 e MMF16_20 **nunca** passam pelo ramo de convergência. (e) **(2)** — direta. **É o desvio paper×implementação de maior consequência comportamental do config.**

**B7 — |A1| ≡ NI fixo: a Fig. 9 na nossa escala.** (a) §III (2ª contribuição) e Alg. 4: "the maximum size of the training data ... is set to NI"; a Fig. 9 mostra tempo de treino CONSTANTE. (b) Sem mudança. (c) `n_treino ≡ arquivo ≡ 11D−1` em **1.443/1.443 ciclos e 25/25 células**. (d) A âncora empírica do próprio artigo se confirma: o drift do tempo de fit ao longo do run é **negativo em 21/25 células** (F5) — o fit fica mais barato, nunca cresce como O(n³), porque o warm-θ entra do 2º ciclo em diante. (e) **(1)** — direta.

**B8/B9 — A poda do A1 e o reinício da população.** (a) Alg. 4 passos 3-7 (um sobrevivente aleatório por cluster) e Alg. 2 (w←1). (b) Instrumentação DI-10 `dist_min_arquivo` (`b3_instrument.m:201-211`, espaço de decisão nativo) + `KRVEA.m:69` (`PopDec = A1Dec`). (c) Re-medido em `b8_dmin.py`: como A1 ⊆ ①, a distância logada tem de ser **≥** a distância à ① inteira — **7.196/7.215 (99,74%)** satisfazem, e os 19 resíduos têm violação relativa ≤ **1,18e-5**, ou seja, ruído de armazenamento float32 do X na ③ (a régua logada é float64 nativa); **igualdade exata em 4.752 (65,9%)**. E o `real_solution_id` da ③ online cai de **0,556 na 1ª sub-população para 0,063 na 20ª** (medianas), total agregado **0,120**. (d) É a única janela para dentro do Alg. 4: em ~2/3 dos casos o vizinho mais próximo do candidato sobreviveu à poda e em ~1/3 já tinha sido descartado — a poda não é degenerada. E o ciclo começa ancorado no real e termina 94% sintético, o que explica os 211 cache-hits de F3. **Armadilha:** a janela do ciclo é `[fe_anterior, fe)`, nunca `[fe−5, fe)`. (e) **(2)** — indireta.

**B12/B13 — Os dois problemas do `UpdataArchive`. 🆕 O VD-b3, discriminado.** (a) Alg. 4 do paper: agrupar os vetores inativos em NI−u clusters e manter, por cluster, **uma solução associada àquele cluster**. (b) Duas coisas nossas/do código: (i) o guard do crash latente que instalamos (`UpdataArchive.m:66-70`, `nzero = sum(Next==0); A1 = [Total(Next(Next~=0)), New]`), bit-idêntico ao stock quando não há zeros; (ii) o defeito declarado no bundle (B3.5) e reaberto pela T11 como **VD-b3 🔴 fechado como forense**: no ramo (a), `current = find(IDX==i)` são posições em **Via** (vetores) e `Next(i)=current(best)` é usado para indexar **Total** (soluções) — `UpdataArchive.m:41-51` + `:70`. A T11 (§D-D) afirma que "`nzero=0` em 1.619/1.619 NÃO discrimina qual ramo rodou, porque nem `size(Via,1)` nem `NI−mu` são logados", e que só 2 linhas read-only resolveriam. **Confirmo a premissa e refuto a conclusão.** (c) Três medições minhas. **(i) `nzero`**: 0 em **1.443/1.443** ciclos das 25 células — e a leitura de código mostra por que isso não discrimina nada: `nzero` é inicializado em `:19` e só é reatribuído dentro do bloco `if length(Total)>NI` (`:69`), então o valor 0 é compatível com os dois ramos **e** com o `else` que nem poda. *(Nota de denominador: os 1.619 do handoff correspondem a 28 células de `data/experiments` — inclui 3 células de **semente 0** e exclui as 3 pré-retrofit; o corpus oficial da s42 são **25 células / 1.443 ciclos**. O veredito `nzero=0` é o mesmo nos dois recortes.)* **(ii) Alcance ESTRUTURAL, agora com prova aritmética:** `associate` tem uma entrada por solução de `Total`, logo `size(Via,1) = |unique(associate)| ≤ |Total| ≈ NI`; como o ramo (a) exige `size(Via,1) > NI−mu` e `size(Via,1) ≤ N`, ele é **impossível quando NI−mu ≥ N**. Nos nossos parâmetros isso elimina **21 das 25 células** por construção (D=10: 104 > 100; D=12: 126 > 91; D=20: 214 > 91; D=22: 236 > 91/100; D=30: 324 > 100). **(iii) Discriminação por RECONSTRUÇÃO** (`vdb3_forense.py`): no ciclo 1 tudo é conhecido — A1 = o DoE (na ①), `New` = os 5 infills (via `index`→③→①), e **V = V0·(max−min do último sub-bloco da ③)**, porque o stock adapta em `KRVEA.m:91-93` a cada `ceil(wmax·0,1)=2` gerações e a última adaptação usa o PopObj de w=20, que é exatamente o último sub-bloco. Reproduzi `UniformPoint` NBI (determinístico, sem RNG) e `NoActive` em Python e computei `size(Via,1)`: **MMF1 = 18 > 16 ⇒ ramo (a) RODOU · MMF4 = 19 > 16 ⇒ RODOU · MMF11_L = 19 > 16 ⇒ RODOU · DTLZ1 = 47 ≤ 71 ⇒ ramo (b), o correto**. O veredito é robusto: perturbando a faixa em ±1e-7 e ±1e-5 (o ruído do float32 é ~1e-7) `size(Via,1)` não muda em nenhuma das 4 células (`vdb3_robustez.py`); só a 1e-2 — 10⁵× o ruído real — o MMF1 chega a oscilar. Para os ciclos ≥2 a composição de A1 não é logada; um Monte Carlo sobre composições ADMISSÍVEIS (120 sorteios/ciclo de subconjuntos de tamanho NI da ① contendo os 5 infills imunes) dá exposição ao ramo (a) de **63% (MMF1), 97% (MMF4), 92% (MMF11_L) e 0,0% (DTLZ1, 3.360 sorteios, nenhum)**. (d) Por que importa e quanto: o índice **nunca sai dos limites** (porque `size(Via,1) ≤ |Total|`), então o defeito é silencioso — não derruba o run, não muda |A1| (que segue NI em 1.443/1.443) e não aparece em `nzero`. O que ele muda é **quais** NI−mu dos NI antigos sobrevivem: em vez do representante do cluster no espaço de objetivos, sobrevive a solução cuja POSIÇÃO em `Total` (ordem lexicográfica do `unique(...,'rows')`) coincide com a posição de um vetor em `Via` (ordem angular) — duas ordens sem relação. Em D=2 isso é descartar 5 de 21 pontos por um critério arbitrário em vez de um critério de diversidade; o efeito observável é nulo nos indicadores (MMF4 e MMF11_L batem o melhor piso com razão 0,59; MMF1 fica em 0,79). (e) **(2)** — direta-forense. **Sancionado**: declarado no bundle B3.5, classificado 🟠 impl→código (D29) e registrado como D-D na T11 — mas agora com o ramo **nomeado célula a célula** em vez de "alcançável em D≤9".

**B14 — Miolo do RVEA interno.** θ_APD=(21/20)^α, SBX 1/20 + PM 1/D/20, k-means++ em `KrigingSelect:26` e `UpdataArchive:42/:55`, `randi` por cluster em `:46/:59`, e a ordem lex do `unique` que define o pareamento posicional do `OperatorGA`. As ~28.860 populações intermediárias existem na ③; os operadores que as geraram, não. **T**.

**N2 — 🆕 `adapt_delta_V`: a instrumentação nova, verificada e com o buraco nomeado.** (a) O "R" do K-RVEA é a adaptação dos vetores pelo range dos objetivos (§III-B); o paper não instrumenta isso. (b) A T11 acrescentou o campo (`b3_instrument.m:101` + `:126-161`) por DERIVAÇÃO, não por patch: recomputa `V0=UniformPoint(N,M)` e aplica a fórmula do stock (`KRVEA.m:92`) sobre os snapshots que já estão em memória — sem tocar a árvore vendorizada, sem re-lacre de âncora, sem consumo de RNG (NBI é determinístico). É o item 4 do A3/I-05. (c) Duas medições. **Primeiro, o campo NÃO é sentinela** (o padrão que o c217 exibe com `pmid_ids=[-1,…]`): 72 valores em 8 ciclos, faixa 0 a 22,26, apenas 3 zeros — e os 3 são legítimos (faixa idêntica entre adaptações consecutivas). **Segundo, o valor está CERTO**: recomputei `‖V0·(faixa_w − faixa_{w−2})‖_F` de forma independente, direto dos 20 sub-blocos da ③ do smoke, e bate o logado com erro relativo **≤ 6,3e-6** em 8/8 ciclos (o resíduo é o float32 da ③). **Terceiro, e é a limitação:** o laço de `:147-157` só emite diferenças ENTRE eventos do mesmo ciclo — são **9 valores para 10 eventos de adaptação por ciclo**, e o que fica de fora é o **primeiro** (w=2), justamente o maior, porque `V` **persiste entre ciclos** (é inicializado uma única vez em `KRVEA.m:31`, fora do laço). Medi o evento omitido: 9,30 · 22,55 · 22,58 · 22,01 · 23,53 · 29,41 · 17,95 nos ciclos 2-8, contra 0,44-13,83 do primeiro valor logado de cada ciclo — **o salto não logado é tipicamente 2-50× maior que os logados**. (d) A escolha é conservadora e defensável (o estado de V no fim do ciclo anterior não está no escopo da função), mas quem usar `adapt_delta_V` como "quanto os vetores se moveram no ciclo" vai subestimar sistematicamente. (e) **(2)** — direta, com a lacuna quantificada.

**N3 — 🆕 Reprodutibilidade cross-máquina e a navalha do dedup.** (a)/(b) Nada disto é do paper; é a pergunta "a busca da s42 é a busca que o código de hoje faz?", que a T11 responde no atacado (instrumentação read-only, 19/19 pares §3.1) e que aqui eu respondo no varejo, para o b3. (c) Comparei a célula main/b3/MMF1/42 em três versões (`cmp_runs.py`): s42 (vm3, 26/07) × smoke T11 (Mac, 30/07) × os dois lados do par g6. Resultado: **a ③ é 100% BIT-IDÊNTICA** entre vm3 e Mac — 12.709 linhas online com Δ=0 em todas as colunas, e os 5 blocos de sonda compartilhados (`fe_treino_max` 20/25/35/45/55, 2.000 pontos cada) com **max\|Δμ\| = 0 e max\|Δσ\| = 0**; a ① é idêntica em **60 das 61 linhas**; os 8 eventos `b3_gen` têm `index`, `pop_por_w`, `NumV1`, `NumV2`, `Flag` e `ramo` **idênticos**. A divergência é uma só, e é cirúrgica: em float64 o `apd_sel` difere na 11ª casa (0,2442356658802806 × 0,24423566587675838, ~1,3e-11 relativo) — invisível no float32 exportado —, e isso bastou para o **dedup bit-a-bit de X (D57) dar vereditos opostos no 5º e último infill do ciclo 8**. Provei que é o MESMO ponto: a linha 60 da ① do smoke é bit-idêntica (float32) à solução 31 da ① da s42 — em `vm3` ele foi cache-hit (`cache_hits`=2, `fe`=60, sobra 1 FE, ciclo 9 abre, sonda no bloco `ftm=59` e o hard-stop mata), no Mac ele foi ponto novo (`cache_hits`=1, `fe`=61, resto 0, sem 9º ciclo e sem `hard_stop`). (d) É a **regra 11 do CONTRATO ao contrário**: a política D53 (float32 sem arredondamento) é lossy na leitura, e aqui o dado exportado é *mais* estável que a decisão — dois pontos que o parquet não distingue foram tratados como distintos por uma das máquinas. Consequência prática para a campanha: para o b3, a reprodutibilidade cross-máquina é **exata ao float32 em todo o run**, com uma navalha de ±1 FE no ledger e ±1 bloco de sonda quando um infill cai exatamente sobre um ponto do arquivo. Não é defeito e não altera nenhuma conclusão de fidelidade; é caveat de reprodução bit-a-bit e deve entrar na F5.7. (e) **(2)** — direta.

---

## 3. Percentual por classe (Botão 2)

**Total = 25 aspectos · T = 2 (F6, B14) · denominador = 25 − 2 = 23.**

| classe | aspectos | n | % | F5 (denom. 27) |
|---|---|---:|---:|---:|
| **(1)** conforme o artigo | U2, U3/B2, F1, F4, B3, B4, B7 | 7 | **30,4%** | 29,6% |
| **(2)** desvio/extensão sancionado | U1/B11, U4/U7, **U5**, U6/F5, U8, U9/N1, U10, F2, F3, B1/B10, B5, B6, B8/B9, **B12/B13**, **N2**, **N3** | 16 | **69,6%** | 70,4% |
| **(3)** desvio inexplicado 🎯 | — | 0 | **0,0%** | 0,0% |
| **T** (fora do denominador) | F6 (θ nunca logado) · B14 (miolo do RVEA) | 2 | — | 4 |

**Leitura honesta do movimento:** os percentuais praticamente não se mexem porque **o mecanismo não mudou**. O que mudou é a **cobertura**: o teto T caiu de 4/31 (12,9%) para **2/25 (8,0%)**, porque U5 e B12/B13 saíram de "não-verificável" para "medido", e os dois aspectos novos (N2, N3) entram já verificados. Nenhum aspecto migrou para pior.

---

## 4. 🆕 AS CORREÇÕES DA T11 — o que tocou o b3, item a item

| melhoria T11 | toca o b3? | implementada? | **funciona de fato?** | o que falta |
|---|---|---|---|---|
| **§3.1 par COM/SEM sonda** | **SIM** | sim (`g6_com`/`g6_sem`) | ✅ **MEDIDO por mim**: ①②③-online bit-idênticas, ⑥ com 8/8 ciclos idênticos e **Δapd_sel = 0**; ⑤ com `desligada` true, `n_blocos`→0, ③ 22.709→12.709 | nada — fecha o U5 |
| **`adapt_delta_V`** (⑥, DI-10/A3-I-05.4) | **SIM** (único campo novo) | sim (`b3_instrument.m:101,126-161`) | ✅ **verificado por recomputo independente** (≤6,3e-6 rel., 8/8 ciclos); **não é sentinela** (72 valores, faixa 0-22,26) | ⚠ **loga 9 de 10 eventos de adaptação por ciclo** e omite o maior (o inter-ciclo, 9,3-29,4 contra 0,44-13,8 dos logados), porque `V` persiste entre ciclos (`KRVEA.m:31`) |
| **`campanha_id`/`repo_hash`/schema v2 no ⑤ (G3)** | SIM | sim | ✅ `campanha_id="t11-smoke-195f64e"`, `repo_hash` presente; **0 manifestos com `campanha_id` na s42** ⇒ nenhum smoke escreveu em produção — confirmado nas 25 células | — |
| **G-7 (`contrato_61.json`)** | SIM | sim | ✅ **conferido por mim**: os 11 campos exigidos do b3 (`apd_sel, sigma_sel, n_vetores_vazios, adapt_delta_V, fe, f_best, n_front1, tempo_fit_s, tempo_busca_s, modelo_hp, dist_min_arquivo`) estão TODOS no ⑥ do smoke; os 6 do ⑤ idem. Na s42 falta **só** `adapt_delta_V` e `campanha_id` | — |
| **I-3 `tempo_aval_real_s` não-nulo** | SIM | sim | ✅ 0,0238 s no smoke (e 0,0239 s na s42 — já existia) | — |
| **I-4 `NO_RETRY` (`experiments.py:84-88`)** | **parcialmente** | sim | ⚠ **das 3 substrings, a 1ª (`least squares problem is underdetermined`) vive em `dacefit.m:102`, que é o DACE do próprio b3** — mas o b3 é **estruturalmente imune**: a condição é `p > mF` com p=D+1 (regpoly1) e mF=\|A1\|=11D−1, e 11D−1 > D+1 para todo D≥1 | 🔴 **o modo de falha REAL do b3 não está na lista**: `Multiple design sites are not allowed` (`dacefit.m:93`, DEF-A8) é 100% determinístico (mesmo DoE + mesma semente ⇒ mesma duplicata) e hoje seria **retriado 3×**. Recomendação para o M8: acrescentar a substring |
| I-1 identidade da referência (`ref_ids`/`pmid_ids`) | **não** | — | N/A — b3 não usa referência rotulada | — |
| I-2 `REGRA_DO_ROTULO` | **não** | — | N/A — b3 não é classificador (`pred_classe`/`pred_score`/`pred_confianca` NULL em 25/25) | — |
| I-5 `y_treino_dist` · I-6 sonda estratificada | **não** | — | N/A — b3 tem **só** `regime='sonda'` (régua Sobol) e `online`; **regra 12 não se aplica** (não há bloco estratificado a separar) | — |
| **VD-b3 (D-D): "2 linhas read-only resolveriam"** | **SIM** | ❌ **não implementada** (retirada junto com a ERRATA 10) | ✅ **desnecessária**: eu discriminei o ramo **sem tocar no código**, por reconstrução (§B12/B13). O ramo (a) roda em MMF1/MMF4/MMF11_L e não roda em DTLZ1; é impossível em 21/25 | se o autor quiser o ramo em TODOS os ciclos (não só no 1), aí sim bastam 2 linhas: logar `size(Via,1)` e `NI−mu` |

**Procurei o padrão do c217 (campo presente, dado sentinela) e não o encontrei no b3** — o único campo novo tem valores reais, aritmeticamente verificáveis contra a ③. O que encontrei foi o padrão vizinho: **campo correto, cobertura incompleta** (9/10 eventos no `adapt_delta_V`).

---

## 5. Comparação canônica (Entrega 2) — inalterada vs F5

Setup do paper (§IV-A + Tabelas I-III): DTLZ1-7, **n=10**, k=3..10, NI=109, **FEmax=300**, IGD cru, Wilcoxon 0,05; 10 runs (§IV-A.2) vs 25 (§IV-B) — inconsistência do próprio paper, registrada na âncora J. Interseção: as 5 células DTLZ com M=3.

| nosso × paper | veredito | sentido do paper | sentido executado |
|---|---|---|---|
| DTLZ2 D=12, 371 FE × n=10, 300 FE | âncora direcional | K-RVEA ↑ RVEA e ↑ ParEGO | vs pisos: b3 IGD+ **0,142** bate os 4 (melhor 0,170) — **CONFIRMA ✓**; vs b1 0,090 → não confirma (+58%, na ordem do piso O-18, 1 semente) |
| DTLZ7 D=22, 681 FE | âncora direcional | K-RVEA ↑ RVEA e ↑ ParEGO | **0,0508 × 1,125 (nsga3) = 22,2× melhor — CONFIRMA ✓✓**; vs b1 empate (+4,1%) |
| DTLZ1 D=7, 216 FE | âncora direcional | K-RVEA ≈ RVEA, ↓ ParEGO | vs pisos 2,38× pior (o "≈" não se sustenta); **vs b1 CONFIRMA ✓** |
| DTLZ3 D=12, 371 FE | âncora direcional | K-RVEA ≈ RVEA, ↓ ParEGO | vs pisos 3,29× pior; **vs b1 CONFIRMA ✓** |
| DTLZ4 D=12, 371 FE | âncora direcional | K-RVEA ≈ RVEA | +52% vs nsga3 — **dentro do piso (58,98%) ⇒ "≈" CONFIRMA ✓** |
| WFG (M=2), ZDT, MMF, BBOB | **incomensurável (eixo X)** | WFG só no suplementar; demais ausentes | — |

**Placar: comparável 0 · direcional 5/5 · incomensurável 20.** Botão 4 respeitado: em nenhum ponto se declara "artigo melhor" sob incomensurabilidade. A regularidade qualitativa central do paper reproduz-se — K-RVEA domina onde o front é bem-comportado (DTLZ2/DTLZ7) e perde nos multimodais (DTLZ1/DTLZ3), pela razão que o próprio paper dá (os vetores forçam distribuição sobre convergência).

---

## 6. Saúde (s42 = corpus principal)

- **Contrato e integridade:** `contrato_f52b.csv` e `integridade_f52a.csv` têm **ZERO linhas de b3**; `gates_f51.csv` traz as **25 células**. Re-conferido por mim no ⑥: **0 linhas malformadas, header e footer em 25/25**.
- **Posição vs pisos** (`metricas_finais_f52c.csv`, re-agregado): b3 bate o melhor dos 4 pisos em **18/25**, razão mediana **0,730**, **rank IGD+ médio 6,36** entre ~16 algoritmos do main. Vitórias decisivas: ZDT1/ZDT3/DTLZ7 (0,04-0,05), BBOB_F55 0,09, ZDT6 0,24. Derrotas decisivas: DTLZ3 3,29 · DTLZ1 2,38 · WFG4 2,34 · WFG9 2,08 — os multimodais e os WFG íngremes, o modo de falha que o paper antecipa. As razões em (0,63; 1,59) **não são lidas como sinal** (piso O-18, 1 semente).
- **Régua Sobol (regra 12 — bloco único, sem estratificado a separar):** ΔWAPE mediano **+4,40%**, piora em 28/56 pares (célula,objetivo); cobertura por célula 0,932→0,924, 14/25 ≥0,90 e 6 <0,60; **0 NaN** em 1.706 registros × 2.000 pontos.
- **Trajetórias** (`f5/trajetorias/`, reaproveitado): IGD+ monotônico em 25/25 (0 violações em 475 transições); ganho mediano 11,5×.
- **Custo:** 9.220,2 s (2,56 h-core) para as 25 células, `vm3` em 25/25 — o piso de ruído entre máquinas não se aplica **dentro** do config.

---

## 7. Score, recomendação e **VEREDITO**

### **SCORE: 9,8/10 · ACEITAR** · **VEREDITO: MELHOROU** (F5 9,6 → T11 9,8)

**A causa nomeada é EVIDÊNCIA NOVA, não mecanismo novo — e não é "amostra diferente":** o corpus da s42 é o mesmo, e **todas as 27 checagens verificáveis da F5 reproduziram com o mesmo número na re-medição** (1.443 ciclos, 7.215 seleções, 3.776.308 linhas, joia 7.215/7.215 a 5,92e-8, critério a 4,31e-16, ΔWAPE +4,40%, 18/25 vs pisos, rank 6,36, 9.220,2 s em vm3). O que subiu o score foram três coisas medidas nesta rodada:

1. **Dois T fechados com dado (4 → 2).** O **U5** (não-perturbação da sonda) deixou de ser "exige run pareado" e virou prova: ①②③-online bit-idênticas e **Δapd_sel = 0** nos 8 ciclos do par g6. O **B13** (ramo (a) do `UpdataArchive`) deixou de ser "sem rastro, alcance 4/25" e virou **discriminação célula a célula**: impossível em 21/25 por aritmética, **rodou** em MMF1/MMF4/MMF11_L, **não rodou** em DTLZ1, com robustez testada a 10⁵× o ruído do float32.
2. **A decisão em aberto da T11 sobre o b3 (D-D) ficou respondida sem tocar em código.** O handoff diz que "medir isso exigiria 2 linhas read-only"; a reconstrução determinística de `UniformPoint`+`NoActive`+`V=V0·faixa` mostrou que o dado já bastava para o ciclo 1, e o Monte Carlo dá a exposição dos demais (63%/97%/92%/0%). E ficou provado que o defeito é **index-safe por construção** (`size(Via,1) ≤ |Total|`) — não derruba run, não muda |A1|, não aparece em `nzero`.
3. **A instrumentação nova foi auditada e passou** — `adapt_delta_V` não é sentinela e bate um recomputo independente a 6,3e-6; e a reprodutibilidade cross-máquina do b3 é **exata ao float32 em todo o run**.

**O que impede o 10** (e é o mesmo de antes, menos dois itens): (i) **θ nunca é logado** (F6) — bounds, θ0 e a trajetória do warm-θ seguem sem verificação por dado; (ii) o **miolo do RVEA** (B14); (iii) três achados novos, todos pequenos e todos com recomendação: a lacuna do `adapt_delta_V` (9/10 eventos), a ausência da substring DEF-A8 no `NO_RETRY`, e a navalha do dedup float64 que muda o ledger em ±1 FE entre máquinas; (iv) a cauda de sub-cobertura em 6 células e a leitura de 1 semente.

**Se o score não tivesse mudado eu diria "manteve" — e seria legítimo.** Não é o caso aqui porque a régua do FRAMEWORK-v2 pontua explicitamente pelo que está **verificado**: sair de 4 para 2 itens não-verificáveis, com um deles sendo o 🔴 estrutural aberto do próprio handoff, é ganho real de confiança sobre a MESMA implementação.

---

## 8. Aspectos classe (3) 🎯

**Nenhum.** Zero desvios inexplicados, como na F5. Os três não-fechamentos brutos desta rodada foram todos resolvidos com causa numérica provada e viraram armadilha de protocolo: (1) a "divergência" s42×smoke, que é a navalha do dedup float64 num único ponto (o mesmo X, provado bit-a-bit em float32); (2) os 19 "furos" do `dist_min_arquivo`, que são ruído de armazenamento float32 do X na ③ (violação relativa ≤1,18e-5); (3) o `nzero=0` que "não discriminava", resolvido por reconstrução.

**Um item vai à F5.7 como verificação dirigida (não classe 3, porque é declarado):** o ramo (a) do `UpdataArchive` **roda de fato** em MMF1, MMF4 e MMF11_L — as três células D=2 do grid. Recomendação: (a) registrar no caveat da dissertação que, no tier D=2, a gestão do A1 do b3 não é o Alg. 4 do paper e sim o comportamento do código oficial (🟠 D29 + B3.5); (b) se o autor quiser o número em TODOS os ciclos das 30 sementes, são as 2 linhas read-only que a T11 já dimensionou — logar `size(Via,1)` e `NI−mu` em `UpdataArchive.m:41`.

---

## 9. Teto T + armadilhas + o que este corpus NÃO permite verificar

**T (2 itens):** valores de θ e a trajetória da MLE (`hp_gp_b3` loga regr/corr/bounds/n/tempo, **nunca θ**) · miolo do RVEA interno (θ_APD=(21/20)^α, SBX 1/20 + PM 1/D/20, k-means++ em `KrigingSelect:26`/`UpdataArchive:42,55`, `randi` em `:46,59`, ordem lex do `unique`).

**O que este corpus NÃO permite verificar, mesmo com a T11:** (i) a composição de A1 em qualquer ciclo ≥2 — é o que limita o VD-b3 ao ciclo 1 exato + Monte Carlo; (ii) o comportamento sob 30 sementes (tudo aqui é 1 semente; `nzero` e `n_vetores_vazios` continuam sentinelas dormentes, valiosos justamente para o M8); (iii) o ramo `incerteza` em D=2 nas células MMF (elas rodam 100% APD, então o cruzamento "bug do arquivo × ramo de incerteza" nunca é exercitado); (iv) se as correções PYTHON da T11 funcionam — os smokes rodaram em tempdir removido, e para o b3 isso é irrelevante (stack MATLAB), mas vale dizer que nenhuma afirmação minha depende deles.

**Armadilhas confirmadas na re-medição** (as 10 da F5 seguem válidas; destaco as que voltaram a morder): 1. `geracao` na ③ é o **CICLO**, com 20 sub-blocos em ordem de linha cujos tamanhos só existem em `pop_por_w`. 2. `index` é **1-based** e aponta para a **última** sub-população. 3. A identidade de σ exige tolerância **RELATIVA** (σ ~1e6 em BBOB_F17). 4. `lote ≡ u_efetivo ≡ 5` nunca é o nº de FEs novos; o ledger é `5·n_ciclos − ΣΔfe = cache-hits de ciclo`. 5. A janela do ciclo é `[fe_anterior, fe)`. 6. `hard_stop ⟺ resto>0` (16 com, 9 sem). 7. O bloco extra da sonda em G=n+1 **não tem linha no ④** (a exceção cai em `KRVEA.m:104`, antes da instrumentação em `:114`). 8. `bloco` no `sonda_f52e.csv` é **STRING** — sem `astype(int)` o ΔWAPE inverte de sinal. 9. `c0=1` em 25/25. **11 (nova):** o `dist_min_arquivo` logado é float64 nativo e o X da ③ é float32 — comparações entre os dois têm piso de ~1e-5 relativo. **12 (nova):** `nzero=0` **não** significa que a poda rodou — a variável é inicializada em `UpdataArchive.m:19` e o ramo `else` (`:71-73`) nem entra no bloco que a atualiza.

---

**Artefatos preservados** (único local de escrita usado — `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/b3/`; interpretador `/Users/gmello/Documents/python_venvs/mestrado_experimentos_dissertacao/bin/python`; dados, código-fonte, artigos e bundles em READ-ONLY, nenhuma célula executada, `experiments.py` nunca invocado):
`remede_s42.py` → `remede_s42.csv` (re-medição das 25 células: 20 invariantes por célula) · `joia_s42.py` → `joia_s42.csv` (query-joia 7.215/7.215 + saúde da ③) · `vdb3_forense.py` → `vdb3_forense.csv` (**discriminação do ramo do `UpdataArchive`**) · `vdb3_mc.py` → `vdb3_mc.csv` (exposição por ciclo) · `vdb3_robustez.py` (sensibilidade do veredito) · `g6_par.py` (par COM×SEM, todas as camadas) · `cmp_runs.py` (s42 × smoke × g6) · `adapt_dv.py` (recomputo do `adapt_delta_V` + evento omitido) · `smoke_joia.py` (mecanismo e G-7 no smoke) · `b8_dmin.py` (poda do A1) · `vs_pisos_t11.csv`.
*(A pasta contém também `bat*.py`/`cmp_r42.py`/`b3_smoke_*.csv`, gravados por outra sessão antes desta; nenhum número deste relatório vem deles.)*