# RELATÓRIO DE FIDELIDADE T11 — e7 EDN-ARMOEA (2ª volta · protocolo v1.1, formato RICO)

**Config**: e7 EDN-ARMOEA (Guo, Wang, Gao, Jin, Ding, Chai — IEEE TSMC:Systems 52(4):2084-2097, 2022 · DOI 10.1109/TSMC.2020.3044418 · **PlatEMO 4.15 built-in**, MATLAB R2025a, N.2.1) · **Corpus A (mecanismo)**: 25 células s42, `resultados_experimentos/e7/{p}/42/` · **Corpus B (instrumentação nova)**: 3 células T11 (`smoke_matlab/e7/MMF11_L` + o par `g6_com`×`g6_sem` de `MMF1`) · **Prior F5**: **10/10 · ACEITAR · 0 aspectos (3)**.

**Volume re-auditado nesta volta** (tudo re-executado, sem citar a F5 de memória): **2.328 ciclos** `e7_gen` · 2.303 transições · **6.984 infills** de ciclo completo (+36 do ciclo abortado) · **6.837.200 linhas da ③** (4.423.200 busca + 2.414.000 sonda em 1.207 blocos) · 909.590 da ② · 10.856 da ① · 2.353 da ④ · **81.988.800 coordenadas de X** contra o box nativo · 2.705 medições de sonda · **+ 3 células T11** (78.100 linhas de ③, 39 ciclos) · **+ 1 par COM/SEM sonda comparado camada a camada**.

**Os dois achados materiais desta volta** (ambos MEDIDOS, ambos contra a minha própria F5):
1. **T1 — o teto mais crítico do config — está FECHADO.** O par `g6_com`×`g6_sem` prova que a sonda não toca o RNG: ① com **sha256 idêntico** (`1e1060eeaae5fa49…`), ② idêntica, e — o teste que só o e7 permite — **③ online bit-idêntica INCLUINDO `mu_*` e `sigma_*` (max\|Δ\| = 0,0 em 24.700 linhas)**. Com MC-dropout, μ/σ iguais ⇒ máscaras Bernoulli iguais ⇒ **consumo de RNG pela sonda = 0**.
2. **A11 estava ERRADO na F5 (e está errado na SPEC).** A normalização `[−1,1]` do paper **EXISTE no código** (`EDNARMOEA.m:38-39` e `:121-123`, `mapminmax` em X **e** Y; `Estimate.m:13` aplica, `:18` reverte). O que a F5 chamou de "divergência" era o **ponto de observação** do log (pós-`reverse`), não o espaço de treino. **A11 sai de (2) e vira (1)** e a contagem de divergências código×paper cai de **6 para 5**.

---

## 1. Ficha do mecanismo (condensada)

Laço de gestão de modelo sobre **uma única** rede densa `D→40→40→M` (ReLU na 1ª oculta, **tanh** na 2ª — `testNet.m:9,12`; o comentário `%Sigmoid` do código é errado; saída **linear**, `:13`). Por **ciclo**: (i) `SelectTrainData` reconstrói o treino capado em **`11D−1`** (prioridade aos recém-avaliados + máximo ângulo mínimo por `pdist2 cosine`), **transladando Y pelo mínimo do arquivo** (`SelectTrainData.m:18`) ⇒ esquecimento por desenho; (ii) X e Y transladado vão a **`mapminmax` → [−1,1]** (`EDNARMOEA.m:121-123`) e a rede é atualizada por **SGD puro sem momentum**, 8.000 passos (`updatemodel.m:4` hardcoded), `lr=0,01`, `wd=1e−5` só em W (`trainNet.m:39` hardcoded), `batchsize=D` — warm restart do MESMO modelo, 80.000 passos só no 1º treino; (iii) predição **MC-dropout T=100** (`Estimate.m:15-21`) → μ = média, σ = desvio **populacional** com guard `sqrt(max(s²−μ².²,0))` (`Estimate.m:29`), **de-normalizados** por `mapminmax('reverse',…,qs)` ⇒ o μ observável está no **espaço transladado**; (iv) motor **AR-MOEA** (IGD-NS, W por NBI, R adaptativo), **19 gerações internas** (`while w<wmax`, `:93`) com pop `Problem.N=100` **nova e aleatória por ciclo** no box nativo (`:82`), sem warm-start; (v) infill: `kmeans(PopObj,Ke=3)` (`IndividualSelect.m:14`) com **gatilho dual** `flag = RatioOld−Ratio < δ` (`EDNARMOEA.m:112`, `δ=0,05` de `ParameterSet(0.05,20,3)`, `:28`, **`<` estrito**): `flag=1` ⇒ convergência = `min sqrt(sum(PopObj.^2,2))` por cluster (`IndividualSelect.m:26`, **sem re-translação**); `flag=0` ⇒ incerteza = `max mean(PopMSE,2)` (`:19`); (vi) **sem dedup de infill** (duplicata = cache-hit, 0 FE, slot perdido — D89/D60(c), com contador `stall_ciclos`). Orçamento `31D−1` com hard-stop exato.

**Divergências código×paper sancionadas — agora 5, não 6:** **δ=0,05 com `<` estrito** (paper 0,08 com `>`) · **Ke=3** (paper `k=5`) · **`Problem.N=100` ⇒ NW=91 em M=3** (paper `P=50`, via **D20**) · **init de pesos `N(0,1/fan_in)` Marsaglia** (paper `U[−0,5;0,5]`) — as quatro do **DEF-B6.6**, todas → CÓDIGO — · **dropout 0,1/0,1 revertido ao ARTIGO por D30** (código shipped `[0.2,0.5]`). ❌ **SAI a 6ª** ("treino transladado × `[−1,1]` do paper", SPEC §1784/§1878 e bundle I.4): **é falsa** — ver A11. Transversais: orçamento **31D−1** (D21/D61), **DoE do artefato** (D63/D88 + patch D94), sonda (DI-12.5/DI-21/§3.1).

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.1 Tabela-resumo — 25 aspectos classificáveis + 5 no teto T

| # | Aspecto | Classe | Verificab. | Resultado re-medido (25 células s42 · 3 do T11) |
|---|---|---|---|---|
| A01 | Orçamento `31D−1` + hard-stop exato | **(2)** D21/D61 | direta | 25/25 exatos (61…929) · `fe_index` E `solution_id` densos 25/25 · smoke 3/3 |
| A02 | DoE `11D−1` do artefato, bit-a-bit | **(2)** D63/D88/D94 | direta | 25/25 · `max\|ΔX\|` **2,97e-8 → 1,91e-6** = ULP-float32 relativo ao box · `doe_hash` ⑤≡header 25/25 |
| A03 | `Ke=3` infills/ciclo | **(2)** B6.6 | direta | `lote==3` ∧ `fe_ciclo==3` em **2.328/2.328** · `(fase=='opt')==20D` 25/25 |
| A04 | Gatilho dual `δ=0,05`, `<` ESTRITO | **(2)** B6.6 | direta | flag⇔desigualdade **2.328/2.328** · `motivo` coerente **2.328/2.328** · contrafactual `>0,08`: 644→**436** |
| A05 | `Ratio=\|Wv\|/NW`; N=100⇒NW=91 (M=3) | **(2)** D20 | direta | quantização `Ratio·NW ∈ ℤ`, erro máx **7,105e-15** em 2.328/2.328 |
| A06 | `wmax=20` ⇒ **19** gerações × N=100 | **(2)** código | direta | ③busca `==1900·C` em 25/25 · `pop_por_w` = 19 valores de 100 em **2.328/2.328** |
| A07 | T=100 MC-dropout; σ POPULACIONAL | (1) | declarativa + direta | T=100 em 2.328 · **σ>0 e não-NaN em 6.837.200/6.837.200** (mín **1,5735e-3**) · 0 σ nulo |
| A08 | Dropout 0,1/0,1 ativo na inferência | **(2)** D30 (🔴→ARTIGO) | declarativa + indireta | `dropP==[0.1,0.1]` em 2.328/2.328 **e** 39/39 do smoke · CV(σ) intra-pop 0,304 |
| A09 | Rede única 40/40 + SGD 8e4/8e3 | (1) | declarativa + indireta forte | `neuronN=40`, `batch==D` 25/25 (2…30) · razão `fit_inicial/fit_update` **10,26 (9,81–10,95) em 25/25** |
| **A11** | 🔄 **Normalização `[−1,1]` + translação pelo ideal** | **(1)** ⟵ era (2) | **direta (código+dado)** | `mapminmax` em X e Y (`EDNARMOEA.m:38-39,121-123`) · **Spearman(log10 escala, WAPE) = −0,053** com escala variando **3,62e7×** · `μ_max/amplitude` mediana **1,034** |
| A13 | Ciclo 1 cru + cadeia `ymin[c+1]==f_best[c]` | (1) | direta | erro **0,0 (igualdade exata)** em **2.303/2.303** transições · `transf_params`③≡`ymin`⑥ erro **0,0** em 3.515 blocos · `espaco_modelo` correto em **3.535/3.535** |
| A14 | Cap `11D−1` + esquecimento | (1) | direta | `n_treino==11D−1` em **2.328/2.328** · `fe_treino_max` monotônico 25/25 · ΔWAPE **+0,66 %** (26/56 séries melhoram) |
| A15 | Sem dedup + cache-hit do init + guarda D60(c) | **(2)** D89/D60(c) | direta | `n_dup_infill=0` ∧ **`stall_ciclos=0`** em 2.328 · `cache_hits=1` (fe=1) ⑤≡⑥≡footer 25/25 · `dist_min_arquivo>0` nos 6.984 (mín **4,214e-3**) |
| A16 | Hard-stop, overshoot, ciclo abortado | **(2)** D21/D61 | direta | guard nas **20** truncadas, ausente nas **5** exatas · `C==⌊20D/3⌋` 25/25 · **7.020−6.984=36** fecha na unidade |
| A17 | Cadência da sonda + **finalProbe** | **(2)** DI-12.5/DI-21 | direta | fórmula sobre `Gstart` fecha **25/25** · finalProbe em **16** células · 1.207 blocos × 2.000, 0 falhas · join posicional `max\|ΔX\|` ≤ **1,907e-6** |
| A18 | k-means k=3, 1 infill/cluster | **(2)** Ke=3 | contagem direta; partição **T4** | `n_clusters_efetivo==3` em 2.328/2.328 |
| **A19** | 🔑 **QUERY-JOIA — identidade decisória dos 2 ramos** | (1) | **direta** | **conv 1.684/1.684 (100,00 %)** · **inc 644/644 (100,00 %)** · 6.984/6.984 infills localizados · refutadores **695/1.684 (41,3 %)** e **1.215/1.684 (72,1 %)** |
| A20 | Guard PopStd `sqrt(max(·,0))` | **(2)** `Estimate.m:29` | direta | `n_std_neg=0` em 2.328 · **0 σ nulo em 6,84 M linhas** |
| A21 | Sem warm-start de pop entre ciclos | (1) | **direta** | interseção de X = **0** contra o BLOCO INTEIRO de 1.900 linhas em **2.303/2.303** fronteiras |
| A22 | Aritmética entre camadas + relógios (U7/U8/U10) | (1) | direta | 5 igualdades fecham 25/25 · `fe`/`fe_treino_max` **2.328/2.328** · **0 violações de U7** + espelho **1.187 = 1.207−20** |
| A25 | Erro de fantasia dos infills (U11) | (1) | direta | WAPE global **0,2300** · 0,0456 (DTLZ7) a **0,7798** (BBOB_F22) |
| A26 | `n_front1` ≡ \|ND\| pós-infills + `real_solution_id` | (1) | **direta** | **2.248/2.248 (100 %) em 24 células** (DTLZ4 0/80 = underflow float32, D53) · conjunto por ciclo exato **2.328/2.328**, 23.040 linhas / 6.984 IDs |
| A28 | Perfil de custo (§III-C), RAM (D86), bounds | (1) | **direta** | **91,75 % busca × 2,60 % fit** · tempo/ciclo **20,90–27,64 s** em D=2…30 · RSS máx **2.098 MB** · **0 X fora do box em 81.988.800** (1,342 % sobre o bound) |
| **A29** | 🆕 **Não-perturbação do RNG pela sonda + proveniência T11** | **(2)** §3.1/B-03 | **direta** ⟵ era **T1** | ①,② **sha256 idênticos** · ③online **bit-idêntica incl. μ/σ** · `desligada=true`, `n_blocos`→0, ③ **40.700→24.700** · `campanha_id`+`repo_hash`+schema v2 |
| **A31** | 🆕 **Reprodutibilidade: determinismo intra-máquina × horizonte cross-máquina** | **(2)** O-18/D53 | **direta** | intra-máquina: 3 pares bit-idênticos · cross-máquina (vm3→Mac): bit-idêntico até **FE 38 (MMF11_L)** e **FE 53 (MMF1)**, depois diverge |
| T2 | Máscaras de dropout e nº REAL de passos SGD | **T** | declarativa | echo + razão 10,26× |
| T3 | Init de pesos `N(0,1/fan_in)` (divergência mantida) | **T** | declarativa | echo em `⑤.params` 25/25 + 3/3 |
| T4 | Atribuição exata infill↔cluster do k-means | **T** | labels não gravados | coberto pela versão invariante à partição (A19) |
| T5 | Operadores SBX/PM e IGD-NS das 19 gerações | **T** | não-verificável | só 1.900=19×100 e a queda de ‖μ‖ (2.271/2.328) |
| T6 | `loss_treino` (DI-12.1b) | **T** | 🟠 **sentinela DECLARADA** | `"INACESSIVEL (DI-12.1)"` em **2.328/2.328 (s42) e 39/39 (T11)** — gate G-7 verde sobre sentinela |

### 2.2 Blocos narrativos

---

**A01 — Orçamento e hard-stop.** **(a)** §IV-A-2 do paper: *"the maximum number of function evaluations FEmax is 11d+119"* — 120 infills fixos, 24 ciclos com `k=5`, qualquer que seja `d`. **(b)** D21/D61 substituem por `maxFE = 31D−1 = 11D−1 (init) + 20D (infills)`, com hard-stop exato via `MException('PlatEMO:Termination')`: decisão transversal dos 22 configs, para que todos recebam o MESMO orçamento por problema. **(c)** Re-medido: `len(①)==31D−1` em **25/25** (61 · 216 · 309 · 371 · 619 · 681 · 929); `manifest.maxfe==fe_final==31D−1` e `footer.fe_final==footer.maxfe` 25/25; `fe_index` **e** `solution_id` densos 0-based 25/25; `(fase=='init')==11D−1` e `(fase=='opt')==20D` 25/25. Nas 3 células T11: 61/61 FE, 21 init + 40 opt, exato. Zero overshoot. **(d)** O contador é único (wrapper de FE, D57/D61) e o `Evaluation` do PlatEMO delega — o orçamento não se desdobra. **(e)** **(2)**, direta. Consequência para a §5: 440 infills em D=22 contra 120 do paper (3,7×).

---

**A02 — DoE do artefato, bit-a-bit.** **(a)** Alg. 2 L1: `X ← LhsDesign(d, 11d−1)`, gerado dentro do run. **(b)** D63/D88 impõem um artefato ÚNICO LHS-maximin compartilhado pelos 22 configs (é o que torna as comparações pareadas), e **D94** especifica o patch: substituir `EDNARMOEA.m:31–:32` **JUNTAS** — a :32 re-escala e causaria dupla-escala. O código confirma: `EDNARMOEA.m:36` é `A = Problem.Evaluation(Problem.data.X0)`, com o comentário `[R1-e7] injecao do DoE (D63/D87/D88): substitui o PAR gera+re-escala das :31-:32 JUNTAS`. **(c)** Re-medido contra `data/doe/{p}/doe_{p}_42.parquet`: `max|ΔX|` = **2,968e-8** (boxes `[0,1]`) a **1,907e-6** (WFG, box `[0,2i]`, i até 22) — em todos os casos o ULP-float32 **relativo à amplitude do box**, i.e. o roundtrip de armazenamento (D53); `doe_hash` do ⑤ ≡ header 25/25, e no smoke T11 o `doe_hash` de MMF11_L é o MESMO da s42 (`bc5b5476bae0…`). **(d)** Se a :32 tivesse sobrevivido o ΔX seria da ordem da própria amplitude. **(e)** **(2)**, direta.

---

**A03 — `Ke=3`.** **(a)** §IV-A-2: *"the number of individuals to be evaluated by the real functions in each generation k is set to 5"*. **(b)** O built-in 4.15 usa **3** — `EDNARMOEA.m:28`, `[delta,wmax,Ke] = Algorithm.ParameterSet(0.05,20,3)`: as três divergências numéricas do config estão numa linha só. D29 classifica 🟠 impl→**CÓDIGO** (DEF-B6.6); mudar `Ke` mudaria o número de ciclos e de retreinos por orçamento. **(c)** `lote==3` ∧ `fe_ciclo==3` em **2.328/2.328**; a ② cresce exatamente +3/geração em **2.353/2.353**; `params.Ke==3` 25/25 e 3/3 no T11. **(d)** `C = ⌊20D/3⌋` (13 em D=2 … 200 em D=30) e é a causa direta do truncamento de A16. Com `k=5` haveria 40 ciclos em D=10 em vez de 66 — 40 % menos retreinos. **(e)** **(2)**, direta.

---

**A04 — Gatilho dual `δ=0,05` com `<` estrito.** **(a)** §III-B-1: *"if the difference of r_{i−1} and r_i … larger than a given threshold, i.e., r_{i−1} − r_i > delta, then the diversity of the population should be enhanced"*, com `delta = 0,08` (§IV-A-2, ratificado em §IV-D-5). **(b)** O código usa `δ=0,05` **e** inverte a comparação: `EDNARMOEA.m:112` é `flag = RatioOld-Ratio < delta`, e `IndividualSelect.m:22` põe a **convergência** no `else` de `flag==0` — ou seja, `flag=1` ⇒ convergência. Sancionado em DEF-B6.6. **(c)** Reconstruí a desigualdade dos campos brutos `RatioOld`, `Ratio`, `delta` e comparei com o `ramo` logado: **`((RatioOld−Ratio) < delta) == (ramo=='convergencia')` em 2.328/2.328 ciclos**, e o `motivo` (que traz a desigualdade por extenso) coerente em **2.328/2.328**. O teste é forte: **70 ciclos caem a menos de 1e-9 do limiar** e a reconstrução em IEEE-754 acerta o ramo do MATLAB em todos. Nas 3 células T11: 39/39. **(d)** **Contrafactual medido com o `>` ESTRITO do paper** (`RatioOld−Ratio > 0,08`) sobre os MESMOS 2.328 ciclos: o ramo de incerteza cairia de **644 (27,7 %) para 436 (18,7 %)** — o δ do artigo cortaria **32,3 % da exploração**. A distribuição é não-degenerada em **25/25** células (20,0 % em DTLZ2 a 38,5 % em MMF1): a incerteza está efetivamente em uso em toda parte, que é o contrafactual G5 do agente-4 ("sem ŝ recai em GPC-ARMOEA"). **(e)** **(2)**, direta.

---

**A05 — `Ratio = |Wv|/NW` e a pop interna N=100.** **(a)** §III-B-1 define `r = |W^v|/|W|` sobre os pontos de referência fixos; o paper fixa `P=50`. **(b)** **D20** manda usar o default do PlatEMO `Problem.N=100` (`EDNARMOEA.m:29`: `W = UniformPoint(Problem.N,Problem.M)` — NBI), compartilhado com b1 e b3; `N=100` com `M=3` dá **NW=91** (maior número simplicial ≤100). **(c)** `NW` constante dentro de cada célula e ≡ `⑤.params.NW` em 25/25 — **100 nas 17 células M=2, 91 nas 8 M=3** — e a assinatura decisiva: **`Ratio·NW` é INTEIRO com erro máximo 7,105e-15 em 2.328/2.328**. Ex.: DTLZ2 `Ratio = 0,6923076923076923 = 63/91`. **(d)** A quantização prova NOS DADOS que `Ratio` é literalmente uma contagem sobre NW referências. Como `Ratio` alimenta A04, `N` é load-bearing por via indireta: com NW=50 o passo de quantização dobra (0,02→0,04) e quase iguala o próprio `δ=0,05`, tornando o gatilho grosseiro. **(e)** **(2)** D20, direta.

---

**A06 — `wmax=20` ⇒ 19 gerações internas × N=100.** **(a)** §IV-A-2 fixa `iter=20`; Alg. 2 L6 chama `AR−MOEA(N,iter,Net)`. **(b)** O laço efetivo é `while w < wmax` (`EDNARMOEA.m:93`) ⇒ **19** iterações — off-by-one do código, mantido (bundle v2.2). **(c)** Duas vias independentes: o bloco `online` da ③ tem **exatamente 1.900 linhas em 2.328/2.328 ciclos** (`③busca == 1900·C` em 25/25, Σ 4.423.200) e o campo `pop_por_w` do ⑥ é uma lista de **comprimento 19 com todos os valores 100** em **2.328/2.328**. No smoke: 24.700 = 13×1.900, exato. **(d)** É por isso que a "pop final" de §III-C são as **últimas 100 linhas** do bloco — a janela que A19 usa. **(e)** **(2)**, direta.

---

**A07 — T=100 e σ POPULACIONAL.** **(a)** §III-C: *"the dropout in EDN takes place not only at training time, but at test time as well"*; §IV-A-3 fixa `itertest=100`; Eq. (11) define ŝ com estimador **populacional**. **(b)** Nada mudou. `Estimate.m:15` é `for i=1:100`, e `:27-29` calcula `var_pop = s2 - mu.^2; std = sqrt(max(var_pop,0))` — divisão por T, não T−1. O `sigma_dict` do manifesto crava a semântica e é idêntico na s42 e no smoke T11 (diff de schema: 0 caracteres de diferença). **(c)** Declarativa: `modelo_hp.T==100` em **2.328/2.328** + 39/39. Comportamental: **σ estritamente positivo, não-NaN, não-zero em TODAS as 6.837.200 linhas** (mín global **1,5735e-3**, MMF11_L; máximos por célula até 20,03). Neste config σ=0 ou NULL seria **BUG** (o inverso do RBF puro do e74): significaria máscaras congeladas. CV de σ̄ dentro da pop final: mediana das 25 = **0,304** (faixa 0,122–0,710) — a incerteza discrimina. **(d)** Dropout ativo no teste ⇒ T saídas distintas ⇒ variância > 0. **(e)** **(1)**; direta-declarativa para T + direta para o comportamento.

---

**A08 — Dropout 0,1/0,1, invertido, ativo na inferência.** **(a)** §IV-D-3: *"We set both dropout probabilities to 0.1 in this work"*, contra o 0,2/0,5 de Srivastava. **(b)** O código shipped usa `dropP=[0.2,0.5]`; **D30** classifica 🔴 bug→**ARTIGO** e manda patchar, com o racional explícito de que *o dropout toca o σ que a tese mede*. É a **única** divergência código×paper deste config resolvida a favor do artigo. O patch está visível em `trainmodel.m:9` (`Params.dropP=[0.1,0.1]`) com o comentário `[R1-e7] dropout 0.1 do PAPER (fidelidade D30 - ARTIGO)`. **(c)** `modelo_hp.dropP == [0.1,0.1]` em **2.328/2.328 ciclos das 25 células** (1 único valor distinto) e em **39/39 eventos das 3 células T11**; `header.dropP == "[0.1,0.1] (D30 ARTIGO)"` 25/25. O elo comportamental é o de A07. **(d)** O log lê o parâmetro efetivo do `trainmodel`, não uma constante do harness — se o patch tivesse falhado o echo mostraria `[0.2,0.5]`. **(e)** **(2)** D30, direta-**declarativa** para o valor + indireta para a atividade.

---

**A09 — Rede única 40/40 e o treino SGD.** **(a)** §III-C: *"there is only one single network model to be updated in EDN rather than multiple NN models"*; §IV-A-3 fixa `J=K=40`, `lr=0,01`, `wd=1e−5`, `batchsize = d`, `itertrain=8e4`, `iterr=8e3`; Alg. 3 L2-5 mostra um mini-batch por passo — SGD puro. **(b)** Nada mudou; o bundle registra que `decay` (`trainNet.m:39`) e `run/batch` do update (`updatemodel.m:3-4`) são **hardcodes que ignoram Params**. O código confirma: `W{k}=W{k}-(decay*W{k}+dW{k})/N*learnR` — decay só em W, B sem decay, **nenhum termo de momento**. **(c)** Echo idêntico em 2.328 ocorrências: `neuronN=40`, `passos_init=80000`, `passos_update=8000`, `learnR=0,01`, `decay_efetivo=1e-05`; **`batchsize == D` em 25/25** (2,7,10,12,20,22,30 — muda com o problema, provando que não é constante do harness). A evidência indireta forte: `tempo_fit_inicial_s/tempo_fit_s` do ciclo 1 = **10,26 mediana, faixa 9,81–10,95, em 25/25**, contra o esperado 10,0 — sem outlier. O custo aritmético `80.000+8.000·(C+1)` dá **1.688.000 passos em D=30**, exatamente a previsão B6.4, e **20.824.000** na campanha. `modelo_flag=='EDN-MCdropout'`, **um único valor** em 6.837.200 linhas (se houvesse ensemble o contrato exigiria por-membro, como no c149). **(d)** ⚠ **auto-errata**: a F5 afirmou "`tempo_fit_inicial_s` **ausente** em G[1:]". Re-medido: a chave está **presente com valor `null`** em **2.303/2.328** eventos (e 24/26 no smoke). Funcionalmente equivalente, factualmente diferente — quem gatear por presença-de-chave erra. **(e)** **(1)**, declarativa + indireta forte (o nº REAL de passos é T2).

---

**A11 — 🔄 A NORMALIZAÇÃO `[−1,1]` DO PAPER EXISTE: a divergência da F5 e da SPEC é FALSA.** **(a)** O paper prescreve **duas** operações distintas sobre Y, e a F5 leu como se fossem alternativas. §III-B-2 (linha 200 do .md): *"the objective value set Y of the augmented data set (X,Y) is **normalized with its minimum**"* — a **translação pelo ideal**, dentro do `SelectTrainData`. Alg. 2 L4 e §III-A (linha 150): *"both the decision variables and the objective values of the training data are **scaled within the range [−1,1]**, denoted as (Xs,Ys)"* — a **normalização**, depois. E §III-C fecha o argumento arquitetural: *"The activation function of the second hidden layer is the Tanh function, which makes the EDN well suited for the regression of **normalized** training data"*. **(b)** A SPEC §1784 diz *"treino em [−1,1] (paper) × transladado-pelo-ideal (código)"* e o bundle I.4 repete *"treino transladado × [−1,1] do paper"*; a F5 (minha) elevou isso a aspecto de classe (2) com três "provas" (magnitude dos μ, X nativo, `transf_tipo='translacao'`). **(c) O código faz AS DUAS COISAS, e as três "provas" da F5 caem uma a uma.** (i) `SelectTrainData.m:18`: `tr_y = tr_y - repmat(Range(1,:),NA,1)` — a translação pelo mínimo, §III-B-2 ✓. (ii) `EDNARMOEA.m:121-123` (e `:38-39` no init): `[tr_xx,ps]=mapminmax(tr_x'); [tr_yy,qs]=mapminmax(tr_y')` — `mapminmax` mapeia **cada linha** (cada variável / cada objetivo) para **[−1,1]**, em X **e** Y, refeito a cada ciclo, Alg. 2 L4 ✓. (iii) `Estimate.m:13`: `x=mapminmax('apply',PopDec',ps)` — o candidato é normalizado ANTES da rede; `:18`: `sum_y=mapminmax('reverse',sum_y',qs)` — a predição é **de-normalizada de volta** ao espaço transladado. **O μ que a ③ grava é pós-`reverse`** — o log observa o mecanismo do lado de fora da normalização, e a magnitude nativa dos μ é consequência do `reverse`, não ausência de normalização. Idem para o X: `EDNARMOEA.m:82` sorteia `PopDec` no box NATIVO (por isso a ③ tem X nativo, A28), e a normalização acontece dentro do `Estimate`. **Duas medições independentes confirmam nos dados**: **(α) invariância de escala** — a magnitude dos objetivos varia **3,62e7×** entre as 25 células (2,10 em DTLZ2 a **7,63e7** em BBOB_F17) e a correlação de Spearman entre `log10(escala)` e o WAPE de fantasia é **−0,053** (Pearson +0,225), estatisticamente nula; BBOB_F17 (escala 7,6e7) tem WAPE **0,229**, MELHOR que MMF1 (escala 7,6, WAPE 0,660). Uma rede treinada com `lr=0,01` e init `N(0,1/fan_in)` sobre alvos de magnitude 1e7 sem normalização não converge a WAPE 0,23 — divergiria. **(β) a assinatura do `reverse`** — `μ_max / amplitude(objetivos do arquivo)` tem mediana **1,0345** e faixa **0,833–1,820** nas **25/25** células, ao longo de 7 ordens de grandeza de escala: exatamente o que `mapminmax('reverse')` produz quando a saída da rede vive em [−1,1] (a saída é linear, `testNet.m:13`, então o leve transbordo é extrapolação). **(d)** A causa do erro é o ponto de observação: a instrumentação foi colocada **pós-`reverse`** (correto — é lá que o μ que decide existe, `IndividualSelect.m:23`), e a F5 inferiu o espaço de TREINO a partir do espaço do LOG. O `transf_tipo='translacao'` gravado em 6.837.200/6.837.200 linhas está **certo** — ele descreve o que separa o μ logado do μ cru, e é isso que o `sigma_dict` promete. Desmorona também a cadeia causal que a F5 propôs para o WAPE 0,78 do BBOB_F22 ("sem normalização o SGD opera em escala 1e7 e a rede aprende a média"): o BBOB_F17, de escala 35× maior, tem WAPE 0,229. **(e)** **Classe (1)** — paper ✓ código ✓ —, verificabilidade **direta** (código com arquivo:linha + duas assinaturas nos dados). 🎯 **ESCALAÇÃO DOCUMENTAL para o autor**: SPEC §1784 e §1878 e o bundle I.4 afirmam uma divergência que não existe; corrigi-los baixa a contagem de divergências código×paper do e7 de 6 para 5 e **aumenta** a fidelidade declarada do config. **DEF-B6.6 não é afetada** — ela nunca listou este item (suas quatro são δ, k, P, init de pesos).

---

**A13 — Ciclo 1 cru e a cadeia da translação, exata.** **(a)** §III-B-2 + Alg. 2 L10: a translação usa o mínimo por objetivo do arquivo acumulado, atualizado ao fim de cada ciclo. **(b)** O bundle teve uma inconsistência cosmética (tabela: "do 2º re-treino em diante"; L.4/M.5: "a partir do 1º retreino, ciclo 1 treina cru"). O código resolve: `EDNARMOEA.m:37` faz `tr_y = A.objs` **sem** translação no init, e só a partir do `SelectTrainData` do fim do ciclo é que `ymin` existe. C3 mandou materializar `min(A.objs)` por ciclo, criando dois campos que devem estar **defasados de exatamente um ciclo**. **(c)** Re-medido: `max|ymin[c+1] − f_best[c]|` = **0,0 — igualdade em ponto flutuante — em 2.303/2.303 transições** das 25 células, e **0,0 em 12/12 no par g6 e no smoke**. Fecho independente: `transf_params` gravado na ③ (blocos online **e** de sonda) contra `ymin` do `e7_gen` do mesmo ciclo → erro máximo **0,0 em 3.515 blocos**. E `espaco_modelo == 'cru'` na geração 1 ∧ `'transformado'` em toda geração ≥2, em **3.535/3.535 blocos (regime, geração)**, com `transf_params` **único dentro de cada bloco** em 3.535/3.535. `ymin` do ciclo 1 = `[0,0,…]` em 25/25. **(d)** É o que autoriza toda comparação μ×verdade (`cru = μ + ymin`) e prova que ③ e ⑥ são da MESMA execução. **(e)** **(1)**, direta.

---

**A14 — Cap de treino `11D−1` com esquecimento.** **(a)** §III-B-2: *"The number of the training data (X1,Y1) is limited to 11d−1 … priority is given to the newly evaluated solutions"*, depois completa por máximo ângulo mínimo (arccos). **(b)** Nada mudou — é mecanismo do paper. `SelectTrainData.m:21` inicializa `Choose = [false(1,NA-N2), true(1,N2)]` (os N2 recém-avaliados entram primeiro) e `:22-26` completa por `min(max(Cosine))`. O que a SPEC fez foi **instrumentar** e **prever** a consequência. **(c)** `n_treino == 11D−1` em **2.328/2.328** e `④.n_acumulado` constante 25/25 (21 em MMF1 … 329 em ZDT1); `fe_treino_max == 11D−1+3(c−1)−1` em **2.328/2.328**, monotônico não-decrescente 25/25 — entram recentes, saem antigos. Do lado da régua: ΔWAPE do 1º ao último bloco = **+0,66 % (mediana por objetivo, 56 séries, 26 melhoram)**, **+2,36 % (mediana por célula)**; em **2.414.000 linhas de sonda o modelo não melhora**. **(d)** Cap constante + prioridade aos recentes ⇒ janela deslizante de tamanho fixo ⇒ modelo local e transitório ⇒ a régua global (2.000 pontos fixos) não pode melhorar. É comportamento **fiel**, e o protocolo §3 o codifica. **(e)** **(1)**, direta.

---

**A15 — Sem dedup de infill, o cache-hit do init e a guarda (c) do D60.** **(a)** O paper não trata do caso: Alg. 2 L8-9 avalia `Xnew` e concatena. **(b)** **D89** define a semântica (cache-hit ⇒ **0 FE**; a duplicata desperdiça o **SLOT**, não o orçamento) e a **guarda (c) do D60** manda **logar** — `n_dup_infill` por ciclo + o contador `stall_ciclos` (ciclos consecutivos com 0 FE), incrementado em `EDNARMOEA.m:132` e emitido como evento `guard 'saldo_congelado'` em `e7_instrument.m:80-81`. **(c)** `Σ n_dup_infill = 0` **e** `Σ stall_ciclos = 0` em **2.328 ciclos** (e 39/39 no T11); `cache_hits == 1` no ⑤, no footer e como **um único evento `guard`** em **25/25**, sempre `name='cache_hit'`, `solution_id=0`, `fe=1` — i.e. **durante o INIT**, o "ruído esperado" catalogado na Etapa 0, não uma duplicata de infill. Prova positiva: `dist_min_arquivo` dá a distância mínima de cada infill ao arquivo pré-existente — **6.984 valores, mínimo global 4,214e-3**, zero nulos. **(d)** A pop interna é nova e aleatória a cada ciclo (A21) sobre um contínuo em float32: a chance de o k-means devolver um X bit-idêntico a um dos ≤929 pontos do arquivo é desprezível. **Duas guardas instaladas, corretas, e que nunca precisaram disparar** — o mesmo padrão de A20. **(e)** **(2)** D89/D60(c), direta.

---

**A16 — Hard-stop, overshoot e o ciclo abortado.** **(a)** Com `k=5` e 120 FE o paper fecha em 24 ciclos exatos. **(b)** Com `Ke=3` e `20D` infills o fecho só é exato se `20D ≡ 0 (mod 3)`; o bundle prevê `overshoot = (3−(20D mod 3)) mod 3` e D21/D61 exige hard-stop **exato** — o ciclo final morre no meio do lote. **(c)** O guard `hard_stop` está presente **exatamente nas 20 células com `20D mod 3 ≠ 0`** (D ∈ {2,7,10,20,22}) e **ausente nas 5 exatas** (DTLZ2/3/4 D=12, ZDT1/ZDT3 D=30) — 25/25; `C == ⌊20D/3⌋` em 25/25 (13·46·66·80·133·146·200). A aritmética fecha por caminho independente: `Σ ①(fase=opt) = 7.020` contra `3 × 2.328 = 6.984`, diferença **36** = `Σ(20D − 3C)` sobre as 20 truncadas (18+2+12+3+1). **(d)** Efeito colateral sancionado: nas 20 truncadas o ciclo abortado **retreinou e sondou** mas não tem linha ④, nem ③busca, nem `e7_gen` — o writer de ciclo flusha no fim e o `MException` perde o buffer, enquanto a sonda flusha na hora. Artefato de flush, coberto pelo portão ⚪ aborto-sancionado; ① e sonda daqueles ciclos estão íntegras. **(e)** **(2)** D21/D61, direta.

---

**A17 — Cadência da sonda e o finalProbe.** **(a)** Instrumentação nossa; o paper não tem sonda. **(b)** §17.2.2 + DI-12.5/DI-21: online `k=2` ⇒ blocos em `g==1 ∨ g%2==0`, **mais a última geração mesmo se ímpar**, 2.000 pontos fixos de `data/sonda/sonda_{p}.parquet`. O disparo está em `EDNARMOEA.m` logo após o `updatemodel` e **antes do 1º `rand` da busca**, com save/restore de RNG que o próprio cabeçalho marca como *LOAD-BEARING no e7: MC-dropout*. **(c)** A fórmula tem de ser avaliada sobre as gerações que **de fato começaram**: `Gstart = C + [hard_stop]`. Com essa definição `{1} ∪ {pares ≤ Gstart} ∪ {Gstart}` reproduz o conjunto observado **exatamente em 25/25 células** e coincide com `⑤.sonda.geracoes` 25/25; o **finalProbe foi exercido em 16 células** (as 7 BBOB, ZDT4, ZDT6, DTLZ1, DTLZ7 e as 5 WFG). Totais: **1.207 blocos, todos com exatamente 2.000 linhas**, `n_falhas=0` e `ok=true` em 1.207/1.207; join **posicional** com o gabarito (U5, regra 5 do §10): `max|ΔX|` de **2,98e-8 a 1,907e-6** (WFG) em **1.207/1.207 blocos**. No T11: cadência observada `[1,2,4,6,8,10,12,14]` ≡ prevista ≡ `⑤.sonda.geracoes`, 3/3. **(e)** **(2)** DI-12.5/DI-21, direta.

---

**A18 — k-means k=3, 1 infill por cluster.** **(a)** §III-B-1: os candidatos da pop final são particionados por k-means e o melhor de cada cluster vira FE; `k` = nº de infills = 5 no paper. **(b)** `Ke=3` ⇒ k=3 (`IndividualSelect.m:14`, `[IDX,~]=kmeans(PopObj,Ke)`). A instrumentação declara honestamente que os labels são arbitrários e que o mapeamento infill↔cluster é **posicional**. **(c)** `n_clusters_efetivo == 3` em **2.328/2.328** — o campo é `length(unique(IDX))` e cairia se a pop final degenerasse a menos de 3 grupos; nunca ocorreu, nem nas células D=2. **(d)** A reconstrução independente da partição fica em **T4** (o k-means++ do MATLAB e o do sklearn convergem a ótimos locais diferentes quando os clusters não são bem separados). A prova de que a **regra** está certa vem de A19, que é invariante à partição. **(e)** **(2)**; a atribuição exata fica em **T4**.

---

**A19 — 🔑 QUERY-JOIA: a identidade decisória dos DOIS ramos fecha 100 %, agora com o código à vista.** **(a)** §III-B-1 define a semântica: convergência ⇒ *"the solution having the minimum Euclidean distance to the origin in the objective space in each cluster"*, com a ressalva *"Note that all objective values should be translated by the minimum objective values before calculating the Euclidean distance"*; incerteza ⇒ *"the solution that has the maximum mean uncertainty in each cluster"*. É o coração do método e a base do contrafactual G5 do agente-4. **(b)** Nada foi mudado; a SPEC tornou a decisão auditável (C3 loga `ymin`; §17 grava X/μ/σ de cada candidato; DI-10 marca a linha escolhida com `real_solution_id`). **(c)** Query re-executada nos **2.328 ciclos das 25 células**: localizar os 3 infills do ciclo na ① (`solution_id ∈ [11D−1+3(c−1), +3)`), casá-los **por CONJUNTO de X em float32** contra as **últimas 100 linhas** do bloco de 1.900, e testar se o extremo GLOBAL da janela está entre os 3. Resultados: **6.984/6.984 infills localizados** (100 %); **ramo convergência: argmin global de ‖μ‖ entre os escolhidos em 1.684/1.684 (100,00 %)**; **ramo incerteza: argmax global de σ̄ em 644/644 (100,00 %)**. Fecha em **25/25 células**, de MMF1 (8 conv / 5 inc) a ZDT1 (145/55). **Dois refutadores derrubados de novo**: "translação pelo mín da POP final" fecha **695/1.684 (41,3 %)**; "espaço CRU (`μ+ymin`)" fecha **1.215/1.684 (72,1 %)**. No corpus T11: **11/11 + 2/2** (smoke MMF11_L), **8/8 + 5/5** (g6_com) e **8/8 + 5/5** (g6_sem, com a sonda DESLIGADA) — 39/39. **(d) A confirmação nova é o CÓDIGO, e ela fecha o caso.** `IndividualSelect.m:26` é literalmente `Convergence = sqrt(sum(PopObj.^2,2))` e `:29` é `[~,ind]=min(Convergence(Pindex))` — `PopObj` é o μ **pós-`mapminmax reverse`**, i.e. o espaço TRANSLADADO, **sem** re-translação; `:19` é `Uncertainty = mean(PopMSE,2)` e `:21` `[~,ind]=max(...)`. Ou seja: o que a F5 estabeleceu por 2.328 ciclos de dados é exatamente o que as duas linhas do código fazem, e os dois refutadores testam hipóteses que não existem no fonte. **(e)** **(1)**, **direta**, agora com dupla ancoragem (dado + arquivo:linha).

---

**A20 — Guard `PopStd = sqrt(max(s²−μ².²,0))`.** **(a)** Não está no paper: a Eq. (11) escreve `ŝ = √(E[ŷᵀŷ] − ŷ_popᵀŷ_pop)`, cujo radicando pode dar negativo em ponto flutuante quando a variância é ~0 — bug numérico latente. **(b)** O bundle registra o guard (`Estimate.m:29`, com o comentário `[R1-e7] guard (N.3): var populacional pode dar <0 por erro float -> sqrt complexo silencioso`) e o instrumenta com `nneg`. **(c)** `Σ n_std_neg = 0` em **2.328 ciclos** (e 39/39 no T11) — guard instalado, nunca disparou; corolário em escala: **zero σ nulo em 6.837.200 linhas** (se o clamp tivesse agido teríamos σ exatamente 0). **(d)** Não dispara porque dropout 0,1/0,1 numa rede 40/40 gera dispersão suficiente entre as T=100 passagens em todos os regimes — inclusive nas células degeneradas (MMF11_L, σ mín 1,57e-3). **(e)** **(2)**, direta.

---

**A21 — Sem warm-start de população entre ciclos (o agente-4 refutado).** **(a)** O paper é omisso: Alg. 2 L6 só chama `AR−MOEA(N,iter,Net)`. **O agente-4 preencheu a lacuna errado**: §3.3-2 afirma *"Há uma população P de tamanho N=50 que persiste e é evoluída"* — ele descreve o AR-MOEA standalone (Alg. 1) e generaliza ao uso embutido. **(b)** O bundle L.4 decide contra ele com base no código, e o código confirma sem ambiguidade: `EDNARMOEA.m:82`, `PopDec = repmat(upper-lower,popsize,1).*rand(popsize,D)+repmat(lower,popsize,1)` **dentro do laço de ciclos**, e o `RefPoint` zerado (`UpdateRefPoint(PopObj,W,[])`, `:84`). **(c)** Interseção de X entre as **primeiras 100** linhas do bloco do ciclo `c+1` e o **BLOCO INTEIRO de 1.900 linhas** do ciclo `c` (todas as 19 populações intermediárias) = **0 em 2.303/2.303 fronteiras**, nas 25 células. Nenhum indivíduo de nenhuma geração interna sobrevive ao ciclo seguinte. **(d)** Cada ciclo é uma busca independente de 19 gerações sobre um surrogate recém-treinado, sem memória evolutiva — combinado com o cap de treino (A14) faz do e7 um método deliberadamente **sem estado**, e explica por que 91,75 % do custo está na busca. **(e)** **(1)**, direta. **Um protocolo que "validasse" persistência de população estaria validando o comportamento errado.**

---

**A22 — Aritmética entre camadas e os três relógios (U7/U8/U10).** **(a)/(b)** Contrato §17 + a peculiaridade registrada em L.4: *"ciclo 1: treina em y CRU e roda 8k passos redundantes de update"* — por isso a ④ tem a **geração 1 DUPLICADA** (1ª linha = treino inicial de 80k, com `tempo_busca_s` NULL; 2ª = o update de 8k). **(c)** Cinco igualdades, todas re-medidas em **25/25**: (i) `⑥ e7_gen == C` (Σ 2.328); (ii) `④ == C+1` linhas (Σ 2.353) **com `geracao==1` aparecendo exatamente 2× em 25/25**; (iii) `manifest.n_geracoes == C+1` 25/25; (iv) `|②(g)| == 11D−1+3(g−1)` em **todas** as gerações (Σ 909.590); (v) `③busca == 1.900·C` (Σ 4.423.200) e `③sonda == 2.000·1.207` (2.414.000). **U8**: `fe == 11D−1+3c` e `fe_treino_max == 11D−1+3(c−1)−1` em **2.328/2.328**, monotônicos 25/25, e o mesmo valor constante dentro de cada bloco `(regime, geração)` da ③ em **3.535/3.535**. **U7** (DI-13.10): **0 violações** de `fit+busca ≤ tempo_geracao` em 2.328 linhas, e a assinatura-espelho — **1.187 linhas violam ao somar `tempo_pred_sonda_s`**, e 1.187 é exatamente `1.207 − 20`, os blocos de sonda que têm linha ④ (os 20 ausentes são os do ciclo abortado, A16). A dupla assinatura fecha sem resíduo. **(d)** A combinação `n_treino` CONSTANTE + `fe_treino_max` MONOTÔNICO é a assinatura formal do esquecimento, e é distinta do não-monotônico de b1/b4/c217 (regra 9 do §10). **(e)** **(1)**, direta.

---

**A25 — Erro de fantasia dos infills (U11).** **(a)/(b)** A linha da ③ marcada com `real_solution_id = s` é o candidato que virou o FE `s`; `μ + ymin` é a predição des-transladada e `f` na ① é a verdade. O elo obrigatório é a des-translação. **(c)** Re-medido na definição da F5 (1 linha por infill, janela da pop final, média sobre cópias): WAPE global `Σ|μ+ymin − f|/Σ|f|` = **0,2300** (F5: 0,2302 — diferença de 0,1 %, do tratamento das cópias elitistas); extremos: DTLZ7 **0,0456**, ZDT6 0,0745, ZDT1 0,0871, BBOB_F5 0,0885, DTLZ2 0,1300 · MMF1 **0,6598**, BBOB_F22 **0,7798**. **(d)** Bate com a sonda por construção — DTLZ7 é a célula mais bem modelada nos dois testes e MMF1 a pior. O ponto científico: **o modelo que ESCOLHE é o mesmo que ERRA na régua** — e é por isso que o e7, com regra decisória fechando 100 % (A19), tem desempenho modesto em baixa dimensão. Isso separa **fidelidade do mecanismo** (perfeita) de **qualidade do surrogate** (regime-dependente). ⚠ E, corrigindo A11(d) da F5: essa qualidade **não** é função da escala dos objetivos (Spearman −0,053). **(e)** **(1)**, direta.

---

**A26 — `n_front1` e `real_solution_id`: as duas identidades de arquivo.** **(a)/(b)** Campos de instrumentação nossa (DI-19.2 / DI-10 + D57). **(c)** `n_front1`: testei H1 (|ND| **antes** dos infills) → 1.517/2.328; **H2 (|ND| depois)** → **2.248/2.328**; H3 (|ND| da pop final em μ) → 9/2.328. A decomposição é o achado: as 80 falhas são **todas de DTLZ4**, logo **H2 fecha 2.248/2.248 = 100 % nas outras 24 células**. `real_solution_id`: os valores **distintos** somam **6.984** = 3×2.328, e o **conjunto por ciclo é exatamente `{11D−1+3(c−1), +1, +2}` em 2.328/2.328**; zero linhas marcadas no regime `sonda` (25/25). Mas o número de **linhas** marcadas é **23.040** — 3,30 por infill. **(d)** As duas explicações. DTLZ4: com `α=100`, `x^α` colapsa e os objetivos assumem 1,06e-265 / 1,02e-220 — o `f_best` do ⑥ (double, do MATLAB) os exibe, mas a ① é exportada em **float32 (D53)**, cujo menor denormal é ~1,4e-45: eles fazem **underflow para 0 exato** (86 zeros em `f0`, 76 em `f1` de 371 linhas), criando empates que destroem dominância. Em DTLZ2 (mesmo D, mesmo M, sem α=100) H2 fecha 80/80. É **exatamente a regra 11 do CONTRATO §10** ("dominância sobre ① é LOSSY"), agora com um caso extremo nomeado. `real_solution_id`: `solution_id` é dedup-por-X (D57), então se o mesmo X aparece em várias linhas do bloco todas recebem a marca — é o **elitismo do AR-MOEA medido**: cada infill sobrevive em média **3,26 das 19 gerações internas** (máximo 19), e a duplicação é entre gerações, quase nunca dentro de uma. **(e)** **(1)** ambos — os campos medem o que devem medir; a única discordância é artefato metrológico do export sancionado.

---

**A28 — Perfil de custo (§III-C), RAM (D86) e bounds.** **(a)** §III-C faz a afirmação central de escalabilidade: o GP custa `O(N³·m·iters)` enquanto *"the computational complexity for constructing the EDN is O(itertrain · batchsize · J · K)"*. **(b)** D43/B6.4 mandam medir e nunca reduzir; D86/§22.5.7 acrescenta o monitoramento de RAM (campo `rss_mb`). **(c)** Re-medido nas 25 células: **91,75 % busca · 2,60 % fit · 3,36 % sonda · 0,0055 % avaliação real** — o oposto exato do c149 (60–87 % no fit). Tempo por ciclo **praticamente CONSTANTE: 20,90 s (ZDT3) a 27,64 s (DTLZ4), mediana 23,46 s**, em D=2…30 e M=2…3. `rss_mb` máximo **2.098 MB** (WFG2), mediana 1.544 MB, mínimo 1.001 MB — sem crescimento explosivo. Wall total da semente 42: **15,06 h-core**. **Bounds, re-medidos contra o box NATIVO exato de cada problema** (`[0,1]^D` DTLZ/ZDT1/3/6/MMF16_20 · `[-5,5]^D` BBOB · `[0,2i]` WFG · `[1,3]×[-1,1]` MMF1 · `[-1,1]×[0,2]` MMF4 · `[0.1,1.1]²` MMF11_L · ZDT4 misto): **0 coordenadas fora em 81.988.800** da ③ online e **0 em 201.056** da ①, com excesso relativo máximo **2,38e-8** (ULP-float32); **1,342 %** exatamente sobre um bound — a assinatura normal de clamp de SBX/PM (compare com os 100 % do caminho `fallback` do c149, que é degenerescência). ⚠ **Armadilha metodológica que eu mesmo pisei nesta volta**: derivar o box do `min/max` do artefato de sonda produz **1.124.197 falsos "fora"** (1,37 %), porque uma amostra Sobol de 2.000 pontos nunca toca os extremos do box. Quem for reproduzir precisa do box verdadeiro. **(d)** O custo é dominado por `19 gerações × N=100 × T=100` passagens forward numa rede de 353 parâmetros — **independente de D e de N_treino**, porque a rede tem tamanho fixo e o cap de treino também. **Não existe parede `n³` em lugar nenhum**, exatamente como §III-C promete, e a evidência é o tempo-por-ciclo plano num range de 15× em D. **(e)** **(1)**, direta.

---

**A29 — 🆕 NÃO-PERTURBAÇÃO DO RNG PELA SONDA: o T1 da F5 está FECHADO (+ a proveniência T11).** **(a)** Não é do paper — é o invariante 🔴 do `CONTRATO §3.1`. **(b)** Na F5 este era **o teto mais crítico de toda a bateria para este config**, e a razão é específica do e7: nas famílias GP a predição da sonda é determinística e o risco nem existe; aqui **σ é estocástico**, o `Estimate` consome `2×T = 200` máscaras Bernoulli por chamada (ordem de RNG documentada em L.4), e o próprio código marca o save/restore como *LOAD-BEARING*. Se a sonda consumisse um único bit de RNG, as máscaras do ciclo seguinte mudariam e o run inteiro divergiria. A F5 declarou: *"a prova exige um par de runs com/sem sonda — indecidível com 1 run"*. **A campanha T11 produziu esse par.** **(c) Medido por mim, camada a camada, no par `evidencia_T11/g6_{com,sem}/experiments/main/e7/exp_main_e7_MMF1_42`:** ① **sha256 do arquivo inteiro IDÊNTICO** — `1e1060eeaae5fa499511a315b6f498a4f94e17d49127350ffb2d8ee6debfe02d` nos dois lados (o mesmo hash que o handoff §5 declara, agora re-medido) — e `max|Δ|` = **0,0** em todas as 7 colunas numéricas das 61 linhas; ② `__pop.parquet` **sha256 idêntico**, 567 linhas; ⑥ `e7_gen`: `ramo`, `Ratio`, `RatioOld`, `f_best`, `ymin`, `n_front1` **idênticos elemento a elemento nos 13 ciclos**. E o teste que **só o e7 permite**: a ③ **online** tem 24.700 linhas dos dois lados e `max|Δ| = 0,0` em **todas** as colunas numéricas — **inclusive `mu_0`, `mu_1`, `sigma_0`, `sigma_1`**. Com MC-dropout, μ e σ idênticos em 24.700 linhas × 4 colunas ⇒ as 100 máscaras de cada linha foram bit-idênticas ⇒ **a sonda consumiu exatamente zero RNG**. Os **três sinais independentes** do §4.2 do handoff também fecham: `⑤.sonda.desligada = true` e `n_blocos = 0` no lado SEM; a ③ **encolhendo 40.700 → 24.700** (= exatamente os 16.000 = 8 blocos × 2.000 da sonda); e a busca inalterada. Bônus: o lado SEM reproduz **todas** as invariantes estruturais, incluindo a query-joia **8/8 + 5/5**. Rodei também `gates_proveniencia` diretamente sobre as três pastas T11 (smoke, g6_com, g6_sem): **6/6 OK em cada**, com `G-1 3x1` fechando 345/345 e 308/308 bit-idênticos. **Proveniência T11**: o diff de schema s42×smoke mostra que a campanha acrescentou **exatamente dois campos** ao e7 — `campanha_id` no ⑤ (`t11-smoke-195f64e` / `195f64ed0a69_2026-07-30`) com `schema_version 1→2` e `repo_hash 195f64ed0a69dc3f…`, e `sonda.desligada` (o mecanismo do próprio G-6). Zero campos novos no ⑥, na ③, na ④ e na ①; `params` e `sigma_dict` **byte-a-byte idênticos**. **(d)** O desarme é um guard nos 4 caminhos de emissão, não um "não-chamar" — o `n_blocos=0` com `desligada=true` prova que o caminho foi exercido. **(e)** **(2)** (instrumentação nossa, §3.1/DI-12.5), verificabilidade **direta** — era **T1, declarada não-verificável na F5**.

---

**A31 — 🆕 Reprodutibilidade: bit-determinismo intra-máquina × o horizonte cross-máquina.** **(a)/(b)** Não é do paper. É a premissa desta rodada: *"a busca da rodada-42 é a mesma busca que o código de hoje faria"*. Para o e7 essa premissa merece número, porque a s42 de MMF1/MMF4/MMF11_L rodou na **vm3** (F5 §5, re-confirmado pelos timestamps do ⑥: `2026-07-26T12:51:53Z` e `12:53:35Z`, janela vm3) e o smoke T11 rodou no **Mac** (`2026-07-30T21:45:27Z` / `22:35:08Z`). **(c)** **Intra-máquina, mesmo código: determinismo perfeito** — os 3 pares COM/SEM (A29) e a igualdade de todas as invariantes. **Cross-máquina, 4 dias e uma campanha depois**: o DoE é bit-idêntico (`doe_hash` igual; os **21 FE do init** batem exatamente nas duas células), os ciclos iniciais também, e então diverge. **MMF11_L: bit-idêntico até o FE 38 (ciclos 1–6); diverge no FE 39, ciclo 7** — os campos `RatioOld/Ratio/f_best/n_front1/dist_min_arquivo` do ⑥ divergem todos no ciclo 7, `ymin` no 8 (defasagem de A13, coerente). **MMF1: bit-idêntico até o FE 53 (ciclos 1–11); diverge no FE 54, ciclo 12** — e mesmo assim a sequência de **ramos é idêntica nos 13 ciclos**. `n_treino` e `fe_treino_max` batem 13/13 nas duas (são funções de `c`, não do estado numérico). **(d)** A causa é aritmética, não algorítmica: `80.000 + 8.000·c` passos de SGD e `19 × 100 × 100` passagens forward por ciclo acumulam diferenças de arredondamento entre BLAS de arquiteturas diferentes (vm3 x86 × Mac arm64), até que um `argmin` de A19 ou uma fronteira de k-means vire. Isso **não** afeta nenhuma das identidades estruturais deste relatório — todas são invariantes por ciclo, verificadas separadamente em cada corpus e verdadeiras nos dois. Afeta o que já estava sancionado: o **piso de ruído entre máquinas** (O-18: HV ≤1,55 %, IGD+ até 58,98 %) e a regra 11 do §10 (float32). **(e)** **(2)** O-18/D53, direta.

---

## 3. Percentual por classe

**Total: 30 aspectos. T = 5 (fora do denominador, §9). DENOMINADOR = 25.**

| Classe | Aspectos | % (n/25) |
|---|---|---|
| **(1) conforme o artigo** | A07 · **A11** 🔄 · A09 · A13 · A14 · A19 · A21 · A22 · A25 · A26 · A28 | **11/25 = 44,0 %** |
| **(2) desvio sancionado** (decisão citada) | A01 (D21/D61) · A02 (D63/D88/D94) · A03 (B6.6) · A04 (B6.6) · A05 (D20) · A06 (código) · A08 (D30) · A15 (D89/D60c) · A16 (D21/D61) · A17 (DI-12.5/21) · A18 (Ke=3) · A20 (`Estimate.m:29`) · **A29** 🆕 (§3.1/B-03) · **A31** 🆕 (O-18/D53) | **14/25 = 56,0 %** |
| **(3) desvio inexplicado 🎯** | — | **0/25 = 0,0 %** |
| **T (teto declarado)** | T2 máscaras/passos · T3 init de pesos · T4 partição k-means · T5 operadores internos · T6 `loss_treino` | 5, à parte (era 6) |

**Comparação like-for-like com a F5** (que usava denominador 28, com A22/A24, A26/A27, A09/A10 e A12/A13 separados e T1 no teto): a única migração de classe é **A11 (2)→(1)**, que leva a F5 de `(1) 15/28 = 53,6 % · (2) 13/28 = 46,4 %` para **`(1) 16/28 = 57,1 % · (2) 12/28 = 42,9 %`**. A queda aparente de %(1) na tabela acima é **efeito de denominador**: os 2 aspectos novos (A29, A31) são ambos instrumentação/metrologia nossa, portanto (2) por construção. **(3) permanece 0,0 % nas duas contagens.**

**Marca declarativa visível (Botão 3)**: A07 (T=100), A08 (valor do dropout), A09 (passos/lr/wd/arquitetura), A29 (proveniência) — **todos com corroboração indireta anexada** (σ>0 em 6,84 M linhas; CV(σ)=0,304; razão 10,26× em 25/25; `batchsize==D` variando com o problema; ① bit-idêntica no par g6).

---

## 4. 🆕 AS CORREÇÕES DA T11 — o que tocou o e7, e o que de fato funciona

O e7 **não recebeu nenhuma correção de mecanismo na T11** — é fato, e é o que se esperava: ele não aparece na tabela de divergências do §15.3 do handoff, não tem achado 🔴 em aberto, e não está entre os itens da FASE A. O que a campanha lhe deu foram **duas coisas globais**, uma delas de altíssimo valor.

| # | Melhoria T11 | Toca o e7? | Implementada? | **Funciona de fato?** | O que ainda falta |
|---|---|---|---|---|---|
| 1 | **G-6 · par COM/SEM sonda (§3.1)** | **SIM — o item mais importante** | ✅ `sonda.desligada` no ⑤ + guard nos 4 caminhos de emissão | ✅✅ **MEDIDO POR MIM**: ① e ② com **sha256 idêntico**; ③ online **bit-idêntica em 24.700 linhas incluindo `mu_*`/`sigma_*`**; ⑥ `e7_gen` idêntico nos 13 ciclos; ③ encolhe 40.700→24.700 (= 8×2.000); `n_blocos` 8→0. **Fecha o T1 da F5** | Nada. 1 célula (MMF1, D=2). Estender a um D alto seria luxo, não necessidade — o mecanismo do desarme não depende de D |
| 2 | **B-03/I-09 · `campanha_id` + `repo_hash` + schema v2 no ⑤** | SIM | ✅ | ✅ presente e povoado nas 3 células T11 (`t11-smoke-195f64e`, `195f64ed0a69dc3f6c64`, `schema_version=2`); **ausente nas 25 da s42** (`schema_version=1`) — a torre mediu certo | Nada para o e7. É rastreabilidade, não fidelidade |
| 3 | **G-7 / errata 6 · gate de contrato em PROFUNDIDADE** | SIM — o e7 foi um dos 3 falso-positivos | ✅ `gates_proveniencia.py:463-478` (`_chaves` recursiva até prof. 4) | ⚠ **funciona como gate, mas o gate ficou verde sobre uma SENTINELA** — ver o parágrafo 🎯 abaixo | O gate afere PRESENÇA, não USABILIDADE. Ver §9/T6 |
| 4 | **I-3 `tempo_aval_real_s`** | SIM | — (**já existia**) | ✅ não-nulo, mas **também na s42**: `timing.tempo_aval_real_s = 0,026463 s` em MMF11_L/s42 e `0,019866 s` no smoke. **Não é ganho da T11 para este config** | — |
| 5 | **I-4 `NO_RETRY` (`experiments.py:84`)** | ⚪ N/A no efeito | — | `n_retries = 0` e `fallback_ativado = false` em **25/25** da s42 e 3/3 do T11 — nunca houve retry para retirar | — |
| 6 | **I-1 identidade da referência (`ref_ids`/`pmid_ids`)** | ⚪ **N/A** | — | O e7 não é classificador par-a-par; não há Pmid nem referência a identificar. `pred_classe`/`pred_score`/`pred_confianca` são **NULL em 100 % das 6.837.200 linhas** por desenho (`pred_tipo='valor'`) | — |
| 7 | **I-2 `REGRA_DO_ROTULO`** | ⚪ **N/A** | — | idem — o rótulo da sonda do e7 é o próprio `f` do gabarito, join **posicional** (regra 5), verificado com `max\|ΔX\|` ≤ 1,907e-6 em 1.207/1.207 blocos | — |
| 8 | **I-6 `sonda_estratificada` / regra 12** | ⚪ **N/A** | — | **medido**: os regimes da ③ do e7 são exatamente `{online, sonda}` na s42 (25/25) e no T11 (3/3). Zero linhas `sonda_estratificada`. **A regra 12 do §10 não tem como ser violada aqui** | — |
| 9 | **I-5 `y_treino_dist`** | ⚪ **N/A** | — | não consta do `contrato_61.json` do e7 (que exige `n_clusters_efetivo` + `loss_treino` além do mínimo comum) | — |

**🎯 O padrão do c217 no e7 — encontrei um, e ele é DIFERENTE.** A busca foi sistemática: varri **todos os 43 campos** do `e7_gen` (achatando `modelo_hp`) nos **2.328 eventos da s42 e nos 39 do T11**, classificando cada um por sempre-nulo / sentinela `−1` / sempre-NaN / string constante / constante / variável. Resultado: **zero campos sempre-nulos, zero sentinelas `−1`, zero sempre-NaN**. Os campos constantes são todos constantes **por desenho e por mecanismo** (`fe_ciclo=3`, `lote=3`, `n_clusters_efetivo=3` — o `Ke=3`; `n_dup_infill=0`, `stall_ciclos=0`, `n_std_neg=0` — guardas que nunca dispararam, cada uma com prova positiva independente; `pop_por_w=[100]×19`; o echo de `modelo_hp`). **A única sentinela do config é `modelo_hp.loss_treino = "INACESSIVEL (DI-12.1)"`, em 2.328/2.328 (s42) e 39/39 (T11).** E aqui está a diferença material com o c217: (i) ela é **declarada em texto legível**, não um `−1` mudo; (ii) ela é **justificada com arquivo:linha** — `src/e7_instrument.m:197-200`: *"a loss esta COMENTADA em trainNet.m:17 … re-habilita-la seria computacao nova no laco de 8e4/8e3 passos … e o proxy via testNet **consome RNG** (dropout.m:4). Registrado como INACESSIVEL, nao omitido"*; e (iii) — o fecho bonito desta rodada — **a prova §3.1 que acabei de medir (A29) RETROATIVAMENTE VALIDA a decisão DI-12.1**: o motivo declarado para barrar o proxy era consumo de RNG, e o par g6 agora demonstra que qualquer consumo de RNG apareceria como diferença bit-a-bit em `mu_*`/`sigma_*`. Barrar o proxy foi o que preservou a não-perturbação. **Veredito: o gate G-7 do e7 é VERDE sobre uma sentinela, mas é uma sentinela sancionada, justificada e cuja remoção quebraria um invariante 🔴.** Fica em **T6**, com o teto explicitado — não vira (3).

**Errata contra o handoff (re-medida, conforme a instrução §15.7-5).** `handoff/T11-CONFORMIDADE.md` §2 linha 15 lista o e7 como `⚪ falta (MATLAB) | — | ⚪ falta`; `handoff/T11-RODADA-FINAL.md` §5 linha 225 lista `✅ · ✅ 6/6 · ✅ 1e1060eeaae5fa49`. **O RODADA-FINAL está certo e o CONFORMIDADE está STALE**: os artefatos existem, eu rodei `gates_proveniencia` sobre eles (6/6 OK) e re-medi o hash. Quem ler o CONFORMIDADE como estado atual conclui que o e7 não tem prova §3.1 — e tem, a mais forte da bateria MATLAB.

---

## 5. Comparação canônica (Entrega 2)

**Setup do paper** (§IV-A + Anexo J): DTLZ1-7 e WFG1-9; `d ∈ {20,40,60,100}` com `m=3`, e `m ∈ {3,5,10,20}` com `d=40`. Orçamento `11d−1` LHS + **120 FEs reais** (`FEmax = 11d+119`), `k=5` ⇒ **24 ciclos**. **20 runs**, IGD com **1000 pontos de referência no espaço CRU**, Wilcoxon 5 %. Números no MD (Tab. II, `d=20, m=3`): DTLZ1 350,0 · DTLZ2 **0,647** · DTLZ3 1064 · DTLZ4 0,844 · DTLZ7 **2,959** (d=40: 4,574). **WFG só no suplemento — ausente do nosso MD.**

**Ressalva metodológica que domina a seção**: nosso IGD é computado no espaço **NORMALIZADO** (D69, ref set próprio) e o do paper no **CRU com 1000 refs**. A normalização **encolhe sistematicamente** o IGD. Portanto o teste direcional só é informativo **na direção do fracasso**.

| Grupo (nosso) | Células | Veredito | Sentido executado |
|---|---|---|---|
| **DTLZ2 / DTLZ3 / DTLZ4** (D=12, M=3) | 3 | **âncora direcional** — mesma família e M; D 1,7× menor; infills 240 vs 120 **mas orçamento TOTAL 371 vs 339 (+9,4 %)** — a interseção mais justa | IGD **0,2082 / 299,95 / 0,3981** vs paper d=20 **0,647 / 1064 / 0,844** → **sentido PASSA 3/3** (potência baixa) |
| **DTLZ7** (D=22, M=3) | 1 | **âncora direcional** — D 22 ≈ 20 (+10 %); total 681 vs 339 (2,0×) | IGD **0,8046** vs **2,959** → **PASSA com folga**; melhor célula do config nas duas réguas internas (fantasia 0,0456; sonda 0,136) |
| **DTLZ1** (D=7, M=3) | 1 | **incomensurável (eixo D)** — 2,9× menor que o menor `d` do paper | registro informativo: 79,38 vs 350,0 — **sem valor comparativo** |
| **WFG1/2/4/5/9** (D=22, M=2) | 5 | **incomensurável (eixo M)** — o paper roda WFG com `m ≥ 3` e `k = m−1`, e só no suplemento | — |
| **Escalabilidade** (§III-C / M.5) | 25 | **comparável em ESTRUTURA DE CUSTO** (a afirmação do paper é assintótica) | Paper: `O(itertrain·batchsize·J·K + m·P³)`, **sem parede `N³`**. Medido: tempo/ciclo **20,90–27,64 s constante em D=2…30**, 91,75 % do wall na busca, fit em 2,60 %, **1,688 M passos SGD em D=30 = a previsão B6.4** → **PASSA** |
| **Ablação do gatilho** (Tab. I / G5) | 25 | **âncora qualitativa** | ramo de incerteza dispara em **644/2.328 (27,7 %)** e em **25/25 células** (20,0 %–38,5 %) ⇒ o método **não** degenera em GPC-ARMOEA em nenhuma célula. Com o `δ=0,08 >` do paper cairia a **436 (18,7 %)** → **PASSA** |
| **🆕 Normalização (§III-A / Alg. 2 L4)** | 25 | **comparável em MECANISMO** (novo nesta volta) | Paper: `(Xs,Ys) ← Normalize(X1,Y1)` em `[−1,1]`. Medido: escala dos objetivos varia **3,62e7×** e o WAPE **não correlaciona** (Spearman −0,053); `μ_max/amplitude` = **1,034 mediana** em 25/25 → **PASSA — a divergência declarada não existe** |
| ZDT1/3/4/6, MMF1/4/11_L/16_20 | 8 | **sem interseção** (famílias ausentes do paper) | — |
| BBOB_F1/5/17/22/37/49/55 | 7 | **sem comparação com literatura por regra** (front empírico) | — |

**Veredito canônico: 4 âncoras direcionais numéricas (DTLZ2/3/4/7), todas no sentido certo com potência baixa declarada; 3 âncoras estruturais (custo, ablação do gatilho e — novo — normalização) que PASSAM com evidência forte; 19 células incomensuráveis ou sem interseção. ZERO células estritamente comparáveis — NUNCA "artigo melhor"** (Botão 4). Fidelidade quantitativa contra as tabelas do artigo continua impossível sem re-executar no setup dele — e mesmo então a âncora seria contra o **CÓDIGO** (com Ke=3, δ=0,05, N=100 e init `N(0,1/fan_in)` mantidos), não contra a tabela do paper.

---

## 6. Saúde (s42 = corpus principal)

**6.1 · Contrato e integridade.** `integridade_f52a.csv`: **0 linhas** com `alg=e7`. `contrato_f52b.csv`: **0 linhas**. Verificação própria: `status='ok'`, `n_retries=0`, `fallback_ativado=false`, `footer.termino='normal'` em **25/25**; `params` (12 chaves), `sigma_dict` (3 chaves) e o bloco `sonda` (11 chaves) presentes e completos em 25/25. Gates F5.1: 25/25 `exit=0`. Gates T11 sobre o corpus novo: **6/6 em 3/3**.

**6.2 · Ganho do MECANISMO sobre o DoE** (o endpoint honesto; o e7 é online, não há ⑦). **Ganho de IGD+ positivo em 25/25 células, mediana +54,5 %**: >50 % em 13 células (BBOB_F55 +89,2 %, BBOB_F5 +85,2 %, BBOB_F1 +83,5 %, ZDT1 +81,0 %, BBOB_F17 +75,9 %, DTLZ7 +74,1 %…), 25–50 % em 11, e **mínimo +11,3 % (WFG2)**. `|ND|` cresce em 20/25 (MMF16_20 50→111; DTLZ2 40→94; WFG1 18→77), igual em 3, cai em 2 (ZDT4 8→1, ZDT6 7→4 — o front converge para poucos pontos que dominam os do DoE; o IGD+ melhora 39,7 % e 46,5 %). Contraste com o c149, que tem ganho 0,00 % em 13/25.

**6.3 · A sonda — RÉGUA SOBOL, bloco único.** ⚠ **Regra 12 do §10 é vácua aqui**: medi os regimes da ③ e o e7 tem **apenas `online` e `sonda`** — zero linhas `sonda_estratificada`, na s42 (25/25) e no T11 (3/3). Não há bloco separado a reportar. 2.705 medições, **0 NaN, 2.000 válidas em 100 % dos 1.207 blocos**.

| Padrão | Evidência re-medida |
|---|---|
| **NÃO aprende globalmente — FIEL (esquecimento, A14)** | ΔWAPE 1º→último bloco: **+0,66 % (mediana por objetivo, 56 séries; 26 melhoram)** · **+2,36 % (mediana por célula; 11/25 melhoram)** — mesmo sinal e ordem do +6 % oficial da F5.2e |
| **Melhoram de verdade** | WFG5 **−23,3 %** · MMF16_20 −11,8 % · ZDT6 −7,5 % · DTLZ3 −6,6 % · DTLZ4 −5,0 % · WFG9 −4,1 % |
| **Multimodal D=2 (regime anti-EDN; agente-4 E3=7)** | MMF1 WAPE final **0,487**, corr **0,366** · MMF4 0,287 — 21 pontos de treino em D=2 multimodal; **esperado, NÃO infidelidade** |
| **DTLZ4: o pior WAPE do config** | **1,882** final, corr 0,319 — a degenerescência `α=100` (mesma causa de A26) |
| **WFG1: o Δ% enganoso** | +554 % sobre base minúscula; o WAPE **absoluto** 0,0575 continua o 3º melhor do config. Corr desaba para **0,202** (o *flat region*) |
| **σ otimista, mas o MELHOR da família NN** | cobertura95 **0,829** (mediana por objetivo) / **0,803** (por célula) / **0,8425** (todas as 2.705 medições) contra nominal 0,95. Melhor ZDT1 0,947, DTLZ7 0,930; pior WFG9 0,496, MMF1 0,527. **Compare: c149 (deep ensemble) dá 0,360** |
| **correlação final** | mediana **0,859**; melhores MMF11_L 0,975, ZDT1 0,965, DTLZ7 0,957; piores WFG1 0,202, BBOB_F55 0,283, WFG9 0,300 |

Cobertura 0,80–0,84 **não é reprovação** — é a família (protocolo §3). O paper mede a qualidade da incerteza (Tabs. SV-SVII) mostrando o GP **colapsando** em alta dimensão enquanto o EDN mantém incerteza discriminativa; nosso CV(σ) intra-população de **0,304** é a medida direta dessa discriminação.

**6.4 · O EA interno de fato evolui.** Média de ‖μ‖ da 1ª janela de 100 contra a 19ª, dentro de cada bloco de 1.900: a população melhora em **2.271/2.328 ciclos (97,6 %)**, mínimo por célula 67,1 %. Combinado com A21 e A06, certifica que as 19 gerações internas são busca real (os operadores em si ficam em T5).

**6.5 · Trajetórias.** 20 checkpoints × 25 células: **0 violações de monotonicidade em 475 transições** (pior "salto" = **0,0**, igualdade exata). Melhora do 1º ao último checkpoint: mediana **85,4 %**, mínimo **52,2 %** (ZDT6), máximo **98,9 %** (BBOB_F17).

**6.6 · Posição vs pisos** (IGD+, `metricas_finais_f52c.csv`; pisos = moead/nsga2/nsga3/smsemoa). Bate o melhor piso em **9/25**: BBOB_F55 **+80,7 %**, BBOB_F1 +38,1 %, DTLZ7 +38,0 %, ZDT6 +33,6 %, DTLZ4 +24,4 %, BBOB_F5 +24,2 %, BBOB_F37 +22,0 %, MMF4 +17,5 %, ZDT1 +10,5 %. Das **16 derrotas, 12 estão dentro do piso de ruído entre máquinas** (IGD+ ≤58,98 %, O-18) — e três delas (MMF1 −17,0 %, MMF11_L −12,5 %, e MMF4 que é vitória) rodaram literalmente em **máquina diferente**, o que A31 acaba de quantificar. **Só 4 derrotas são conclusivas**: DTLZ1 −75,1 %, **DTLZ3 −131,1 %**, WFG2 −69,9 %, WFG4 −91,1 %. **1 vitória conclusiva**: BBOB_F55 +80,7 %. **Rank médio 9,16 de 17** — coerente com o prior 7,0 e com o regime-alvo declarado pelo próprio paper (high-dim **many**-objective).

**Comportamento de 1 semente ≠ infidelidade** (caveat 8): o mecanismo está provado fiel (§2, identidade decisória em 100 %); a nota mediana de comportamento é do **regime**, e §6.2 mostra o mecanismo adicionando valor em **25/25** células.

---

## 7. SCORE, recomendação e VEREDITO

# SCORE: 10 / 10 — recomendação: **ACEITAR**
# VEREDITO vs F5: **MANTEVE (10/10 = teto da régua) — com dois ganhos materiais que o score, já saturado, não consegue expressar**

**Por que o score não mudou:** a régua do FRAMEWORK-v2 §F5.3b dá 10 quando *"tudo (1)/(2), cada (2) ancorado em decisão documentada; contrato perfeito; saúde limpa"*. A F5 já estava lá, e nada nesta volta produziu um aspecto (3), um 🔴 novo ou uma anomalia de saúde. **"Manteve" é o veredito correto e esperado: o mecanismo não mudou — o e7 não recebeu uma única linha de correção de algoritmo na T11.** Re-executei as invariantes centrais e **todas reproduzem número por número**: joia 1.684/1.684 e 644/644, refutadores 695 e 1.215, gatilho 2.328/2.328, quantização 7,105e-15, `ymin` com erro 0,0 em 2.303, 6.837.200 linhas com σ>0, U7 com 0 violações e o espelho 1.187 = 1.207−20, 0 X fora do box em 81.988.800.

**As duas melhoras, com a causa NOMEADA:**
1. **CORREÇÃO REAL DA CAMPANHA (verificabilidade)** — o **T1**, que a F5 declarou *"o teto MAIS crítico de toda a bateria"* para este config, saiu de **não-verificável** para **provado por medição direta**: o par `g6_com`×`g6_sem` dá ①/② com sha256 idêntico e a ③ online bit-idêntica **incluindo `mu_*`/`sigma_*`** em 24.700 linhas. Para um config cujo σ é estocástico, essa é a prova mais forte possível de que a sonda não toca o RNG. T cai de 6 para 5, e o que saiu era o único item T ancorado num invariante 🔴 do CONTRATO.
2. **ERRO MEU, CORRIGIDO (fidelidade medida)** — **A11 migra de (2) para (1)**. A normalização `[−1,1]` do paper existe no código (`mapminmax` em X e Y, `EDNARMOEA.m:38-39,121-123`; `Estimate.m:13,18`), com duas assinaturas independentes nos dados (invariância de escala em 3,62e7× de amplitude, Spearman −0,053; `μ_max/amplitude` = 1,034 em 25/25). A contagem de divergências código×paper do e7 cai de **6 para 5**, e a SPEC §1784/§1878 + bundle I.4 precisam de correção. `%(1)` no denominador da F5 sobe de **53,6 % para 57,1 %**.

**Não é MELHOROU porque:** o score é o mesmo (teto) e a **amostra do mecanismo é a mesma** (as mesmas 25 células da s42, mais 3 células D=2 do T11 que confirmam mas não ampliam). **Não é PIOROU porque:** nenhuma medição desta volta contradiz o run — as duas correções vão na direção de MAIS fidelidade, e os dois itens que escalo (a errata documental da SPEC e o CONFORMIDADE stale) são de **documentação**, não de execução.

---

## 8. Aspectos classe (3) 🎯

**NENHUM.** Nada vai à verificação adversarial por este config — igual à F5.

**Três itens de ESCALAÇÃO ao autor, todos documentais/metrológicos, nenhum de execução:**

1. 🎯 **A SPEC afirma uma divergência que não existe.** `SPEC_experimentos_v5.2.md` §1784 (*"treino em [−1,1] (paper) × transladado-pelo-ideal (código)"*) e §1878 (*"Divergências: … treino transladado × [−1,1] do paper"*), propagados no bundle `alg_e7_ednarmoea.md` I.4. **Evidência de refutação, completa**: `SelectTrainData.m:18` faz a translação (§III-B-2 do paper, linha 200 do .md); `EDNARMOEA.m:38-39` e `:121-123` fazem `mapminmax` em X **e** Y → `[−1,1]` (Alg. 2 L4, linha 150/1201 do .md); `Estimate.m:13` aplica ao candidato e `:18` reverte a predição; `testNet.m:12` usa tanh, que o paper §III-C justifica *"for the regression of normalized training data"*. Assinaturas nos dados: Spearman(log10 escala, WAPE) = **−0,053** com escala variando **3,62e7×**; `μ_max/amplitude` = **1,0345** mediana, faixa 0,833–1,820, em **25/25 células**. **Recomendação**: corrigir §1784/§1878 e regenerar o bundle. **DEF-B6.6 não é afetada** (nunca listou este item). Impacto: aumenta a fidelidade declarada do config.
2. ⚠ **`handoff/T11-CONFORMIDADE.md` §2 está STALE** para os 13 configs MATLAB, e para o e7 em particular (diz `⚪ falta` para smoke e par G-6). Os artefatos existem, passam 6/6 no `gates_proveniencia` e o hash bate. Quem usar o CONFORMIDADE como estado atual conclui que falta a prova §3.1 mais forte da bateria.
3. ⚠ **DTLZ4 · a ① não permite recomputar dominância** com a fidelidade do run: objetivos de 1e-220…1e-265 fazem **underflow para 0 exato** no export float32 (D53) — 86 zeros em `f0` e 76 em `f1` de 371 linhas. É a **regra 11 do §10** no seu caso extremo. Foi o que fez `n_front1` "falhar" 80/80 ali enquanto fecha 2.248/2.248 nas outras 24. Não afeta IGD+/HV (espaço normalizado) nem qualquer métrica oficial. **Recomendação**: se a R4 precisar de dominância recomputada em DTLZ4, exportar `f` em float64 nessa célula (ou marcá-la como não-recomputável).

---

## 9. Teto T + armadilhas + o que este corpus NÃO permite verificar

**T — 5 itens (era 6), fora do denominador:**

- **T2 — Máscaras de dropout e nº REAL de passos SGD.** Só echo (`dropP`, `passos_init/update`) + a razão 10,26×. As máscaras Bernoulli e a ordem de consumo do RNG (L.4: probe → LHS → init Marsaglia → minibatches → máscaras → pop → torneio/GA → kmeans++) não deixam rastro em parquet. **A29 mitiga parcialmente**: o par g6 prova que a SEQUÊNCIA de máscaras é reprodutível bit-a-bit sob máquina fixa, embora não revele os valores.
- **T3 — Init de pesos `N(0,1/fan_in)`** (Marsaglia polar, `iniA.m`; `B{1}=0,1` const em `trainmodel.m:4`). Divergência código×paper mantida. Só declarativo em `⑤.params.init_pesos` (25/25 + 3/3); o estado inicial não sobrevive ao run.
- **T4 — Atribuição exata infill↔cluster do k-means.** Labels não gravados; `cluster_por_infill` declara o mapeamento posicional. **Coberto pela versão invariante à partição (A19), que fecha 100 %.**
- **T5 — Operadores SBX 1/20 + PM 1/D/20, IGD-NS e a adaptação de R nas 19 gerações internas.** Observáveis: a contagem 1.900 = 19×100, o `pop_por_w`, e a queda de ‖μ‖ em 2.271/2.328. Os operadores em si, não.
- **T6 — `loss_treino` (DI-12.1b).** ⟦**atualizado na T11**⟧ O campo está presente e **aninhado em `modelo_hp`** — a errata 6 do handoff corrigiu o gate G-7, que aferia POSIÇÃO e não PRESENÇA, e o e7 era um dos 3 falso-positivos. **Mas o gate ficou VERDE sobre a string-sentinela `"INACESSIVEL (DI-12.1)"`, constante em 2.328/2.328 (s42) e 39/39 (T11).** É sentinela sancionada, justificada em `src/e7_instrument.m:197-200` (a loss está comentada em `trainNet.m:17`; re-habilitá-la seria computação nova no laço de 8e4/8e3 passos; o proxy via `testNet` **consome RNG**, `dropout.m:4`) e agora retroativamente validada por A29. **A curva de treino do e7 permanece inobservável — por decisão, não por omissão.**

**O que este corpus NÃO permite verificar (mesmo com a T11):**
- **A correção NUMÉRICA de qualquer valor.** Os 6 portões conferem estrutura; o handoff §15.1 é explícito e eu confirmo: *"Um algoritmo pode estar profundamente errado e passar nos 6 portões."* O que este relatório prova é **conformidade de MECANISMO** (a decisão é a do paper, os parâmetros são os sancionados, a aritmética fecha), não que os 80.000 passos de SGD produziram os pesos certos.
- **Generalização de A29 além de D=2.** O par g6 existe para **uma** célula (MMF1). O desarme não depende de D, mas o dado é de 1 célula.
- **Multi-semente.** Tudo aqui é semente 42, 1 run por célula. `metricas_finais_f52c` sem desvio-padrão ⇒ nenhuma afirmação de desempenho é estatística (caveat 8).
- **A hipótese "o built-in 4.15 ≡ o código do autor do paper"** (SPEC §1784 ⚠). Só uma reprodução no setup do Anexo J resolveria — e a âncora seria contra o CÓDIGO, não contra a tabela.

**Armadilhas confirmadas na escala** (as 8 mais caras; as 16 da F5 seguem valendo, com **duas correções minhas**):
1. **A chave de evento do ⑥ é `rec`**, não `"evento"`; o evento de geração é `e7_gen`. **Não existe `motivo_parada` no ⑤** (confirmado na s42 **e** no smoke T11) — o término está em `footer.termino == 'normal'`. Triagem por `status` sozinho é o bug B1.
2. **`mu_*` da ③ está no ESPAÇO DO MODELO** (transladado). Toda comparação com verdade exige `mu + ymin`. ⟦**refinado**⟧ E o espaço do modelo é o de saída do `mapminmax('reverse')` — a rede treina em `[−1,1]`; **não confundir o espaço do LOG com o espaço de TREINO** (foi exatamente esse o erro do A11 da F5).
3. **`geracao` da ③ é o CICLO**, não a geração interna. O bloco tem **1.900 = 19×100**; a **pop final é o `tail(100)`**.
4. 🔑 **Ramo de convergência: `argmin ‖μ‖` SEM re-translação** (`IndividualSelect.m:26`). "Translação pelo mín da POP" fecha 41,3 %; "espaço cru (`μ+ymin`)" fecha 72,1 %. A leitura certa fecha **100,00 %**.
5. 🔑 **Casar infill×pop-final por POSIÇÃO produz falso desvio.** A pop final tem X duplicado em **1.008/2.328 ciclos** (elitismo do AR-MOEA) e, com MC-dropout, cópias do mesmo X têm μ/σ diferentes. Casar por **conjunto de X**.
6. **A cadência da sonda deve ser avaliada sobre `Gstart = C + [hard_stop]`**, não sobre `n_geracoes = C+1`; com `n_geracoes`, 5 células parecem ter um bloco faltando.
7. ⟦**auto-errata NOVA**⟧ **`tempo_fit_inicial_s` está PRESENTE com valor `null` em `G[1:]`** (2.303/2.328), não ausente — a F5 escreveu "ausente". Um gate por presença-de-chave reprova um run fiel.
8. ⟦**auto-errata NOVA**⟧ **Derivar o box do problema do `min/max` do artefato de sonda gera falso-alarme de bounds**: uma amostra Sobol de 2.000 pontos nunca toca os extremos, e o teste acusa **1.124.197 coordenadas "fora"** (1,37 %) que estão dentro. Com o box nativo verdadeiro: **0 fora em 81.988.800**, excesso relativo máximo 2,38e-8.
9. **Validar `δ=0,08` / `k=5` / `P=50` / `U[−0,5;0,5]` / `FEmax=11d+119` do paper (ou do agente-4, que só conhece o paper) contra o run REPROVA um run FIEL.** O contrato é CÓDIGO + D30 + D20 + D21/D61. ⟦**e a recíproca agora vale**⟧: validar **`Normalize([−1,1])`** contra o run **PASSA** — quem confiar na SPEC §1784 e "confirmar" a divergência estará confirmando um erro documental.
10. **σ = 0 ou NULL neste config seria BUG** (inverso do RBF puro do e74): σ>0 em 6.837.200/6.837.200 é a prova de que o dropout está ativo na inferência.

---

**Artefatos desta análise** (diretório único de escrita): **`/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/e7/`**

*Scripts (7)*: `01_g6_naoperturbacao.py` (a prova T1) · `02_diff_s42_smoke.py` (o delta real da instrumentação T11) · `03_reprodutibilidade.py` (s42 × T11, cross-máquina) · `04_remedida_s42.py` (re-medida das invariantes nas 25 células, incl. a query-joia) · `05_ajustes.py` (U11 na definição F5, contrafactual estrito, CV(σ), magnitude dos μ) · `06_cacada_sentinela.py` (caçada do padrão c217 em 43 campos × 2.367 eventos) · `07_smoke_estrutural.py` (as invariantes no corpus T11).

*Evidência (12)*: `CONSOLIDADO_T11_e7.json` · `g6_e7.json` (o par COM/SEM, camada a camada) · `remedida_e7.csv` (25 células × 78 colunas) + `remedida_e7.json` · `ajustes_e7.json` · `bounds_e7.json` (box nativo por problema) · `escala_e7.csv` (a prova de invariância de escala do A11) · `sentinelas_e7.json` · `smoke_estrutural_e7.json` · `diff_schema_e7.json` · `reprodutibilidade_e7.json` · `sonda_resumo_e7.csv` · `pisos_e7.csv`.

*Corpora (read-only)*: `resultados_experimentos/e7/{problema}/42/` (25 células) · `evidencia_T11/{smoke_matlab,g6_com,g6_sem}/experiments/main/e7/` (3 células). *Artefatos*: `ua-dd-saea/data/doe/{p}/doe_{p}_42.parquet` · `data/sonda/sonda_{p}.parquet` · `claude_code_context/artifacts/contrato_61.json`. *Insumos pré-computados F5*: `f5/{metricas_finais_f52c, sonda_f52e, contrato_f52b, integridade_f52a}.csv` e `f5/trajetorias/main_e7_*_42.json`. *Código lido (não tocado)*: `algorithms/_PlatEMO/…/EDN-ARMOEA/{EDNARMOEA,IndividualSelect,SelectTrainData}.m` e `Dropout/{Estimate,testNet,trainNet,trainmodel,updatemodel}.m` · `src/e7_instrument.m` · `scripts/gates_proveniencia.py`. *Bundle*: `claude_code_context/10_rodada1_matlab/alg_e7_ednarmoea.md`. *Interpretador*: `/Users/gmello/Documents/python_venvs/mestrado_experimentos_dissertacao/bin/python`. **Nenhuma célula foi executada; `data/experiments` não foi tocada; `experiments.py` não foi invocado.**