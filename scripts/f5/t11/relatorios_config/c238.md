# RELATÓRIO DE FIDELIDADE T11 — c238 (EIM · Zhan/Cheng/Liu 2017 · MATLAB standalone → `classdef EIM < ALGORITHM`) · re-validação sobre a F5

**Corpus.** (A) **s42 — mecanismo**: 25/25 células de `resultados_experimentos/c238/{problema}/42/` — **7.020 iterações EGO · 10.856 FE · 1.299.400 linhas de pool na ③ · 7.070.000 linhas de sonda (3.535 blocos) · 15.740 componentes-geração de θ/lnL · 259.880.000 avaliações da aquisição**, TODAS re-medidas hoje por scripts escritos do zero (`c1`,`c4`,`c6`,`c7`,`c8`). (B) **T11 — instrumentação**: `evidencia_T11/smoke_matlab/main/c238/MMF1` (1 célula, 61 FE) + o par **`g6_com` × `g6_sem`** (2 células). Máquinas: 7 células Mac / 18 vm3 (O-19; `f5/tempo_f52d.csv` re-lido: `{'vm3': 18, 'mac*': 7}`), smoke e par G-6 no **Mac**.

**Prova de que a s42 continua válida para o mecanismo — medida, não assumida.** `git log -1 -- src/c238_instrument.m src/c238_sonda.m` ⇒ **`941901f`, 2026-07-21**; `algorithms/c238_EIM` ⇒ **`b3675ef`, 2026-07-24**. Ambos **anteriores** à s42 (26/07) e à T11 (29–30/07). Os únicos `.m` tocados na T11 que alcançam o c238 são compartilhados: `src/experiment.m` (`schema_version=2`, `campanha_id`, `repo_hash` — :3067/3068/3080), `src/SondaState.m` (+283 linhas: gêmeo estratificado — que o c238 não usa — e a chave `desligada`, default `false`, :141-142) e `src/FEBudget.m` (**+20 linhas, um único acessor read-only `arquivoX()`** para a sonda estratificada). **Nenhuma linha do algoritmo, da instrumentação ou da aritmética de FE do c238 mudou.**

---

## 1. Ficha do mecanismo

Zhan, Cheng & Liu, *IEEE TEVC* 21(6):956–975 (2017). EGO multiobjetivo **sequencial q=1**: DoE LHS 11n−1 → por iteração (i) 1 Kriging independente por objetivo sobre **y re-escalado min-max a [0,1]** (§V-B(5)a — verificado no código: `MultiObjective_EIM.m:31,57` `sample_y_scaled=(y−min(y))./(max(y)−min(y))`); (ii) frente ND por `paretofront` (FEX-17251, dominância fraca sequencial); (iii) **matriz EI k×m** `EI_i^j=(f_i^j−ŷ_i)Φ(λ)+s_iφ(λ)`, `λ=(f_i^j−ŷ_i)/s_i` (eq. 22); (iv) agregação **EIMe euclidiana** `y=min_j‖EI^j‖₂` (eq. 23 / Apêndice A); (v) maximização sobre a caixa → **1 FE real**. Propriedades declaradas: **N1/N2** (§IV-C) e **amostragem densa** (§IV-D).

**Nossa instância** (bundle `alg_c238_eim.md`; I.9/L.9/N.5/E.5): kriging **próprio estilo Forrester, não-DACE** (`GP_Train.m`: μ constante, kernel gaussiano ARD, **nugget (10+n)·eps**, X normalizado a [0,1], MLE concentrada por **`fmincon` sqp em log₁₀θ, single-start, MaxFunEvals=20D, tol 1e-20**, θ₀=1, bounds [1e-3,1e3]); **GA nativo do repo no lugar do DE** (pop=10D, 200 gerações — `MultiObjective_EIM.m:52`) — **DEF-B10.2/D30, a única divergência material**; **anti-clustering do paper AUSENTE no repo** (B10.7 FECHADA, 🟠 impl→código); guards [IMPL] `EIM(isnan)=0` (`Infill_EIM.m:19-21`), `max(range,eps)`, dedup de treino; **crash latente stock `Infill_EIM.m:25`→`Optimizer_GA.m:17` deliberadamente não consertado** (D81); init/maxFE do repo (100/200) sobrescritos por **11D−1 / 31D−1** (D21/D61/D63).

**Divergências sancionadas nomeadas:** D30+DEF-B10.2 (GA×DE) · B10.7 (anti-clustering ausente) · E.5/D29 (kriging Forrester + motor de MLE) · D21/D61/D63/D88 (orçamento e DoE) · D53 (float32) · D57/D89 (solution_id/cache) · DEF-C2/C3 (pop final e log do re-scaling) · DI-10/DI-12.5/DI-13.10/DI-18 · N.5/L.9/I.9 (embrulho) · D81 (crash stock). **`SPEC:811` continua sendo a única divergência ⚠ ABERTA do config** — e a §5 abaixo a fecha com número.

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.1 Tabela-resumo (29 aspectos · T = 1 · denominador = 28)

> O quadro é o **mesmo da F5** (para permitir comparação linha a linha) com **4 aspectos novos** (T1 promovido de T, N1, N2, N3). A1/A2/A3 continuam sub-itens de U10/E10/E7.

| # | aspecto | classe | verificab. | resultado-síntese (re-medido hoje) | vs F5 |
|---|---|---|---|---|---|
| U1 | FE = 31D−1, hard-stop exato | (2) | direta | 25/25; `fe_final==maxfe` 25/25; `termino='normal'` 25/25; **Σ=10.856** | ≡ |
| U2 | DoE 11D−1 do artefato | **(1)** | direta | 25/25; **max\|ΔX\| = 0,0 EXATO** em float32 (1,91e-06 vs float64 = ½-ULP); `doe_hash` ≡ recalculado 25/25 | ≡ |
| U3 | 1 fit por iteração | **(1)** | direta | len(④)=len(⑥ gens)=n_infills = **7.020** | ≡ |
| U4 | cadência da sonda + finalProbe | (2) | direta | 25/25 `g==1∨g%2==0`; finalProbe 25/25; **3.535 blocos**, 7.070.000 linhas, `n_falhas=0` | ≡ |
| U5 | join posicional sonda×gabarito | (2) | direta | ULP-float32 × escala dos bounds (1,192e-07 no MMF1); `x_hash`/`f_hash` ≡ gabarito | ≡ |
| U6 | WAPE/cobertura (def. §5) | (2) | direta | 7.926 medições; **σ-NaN = 0**, `n_validas=2.000` em 7.926/7.926 blocos-objetivo | ≡ |
| U7 | `fit+busca ≤ tempo_geracao_s` | (2) | direta | **0 violações em 7.020/7.020** | ≡ |
| U8 | `fe_treino_max` conforme regime | **(1)** | direta | monotônico 25/25; `== n_amostra−1` em **7.020/7.020** | ≡ |
| U9 | guards ⑥ ≡ ⑤ | (2) | direta | censo `{guard:1}` 25/25 ≡ `cache_hits=1` 25/25 | ≡ |
| U10 | aritmética entre camadas (A1 ledger) | (2) | direta | ② = n_gens+1 em 25/25; tamanho (init−1)+g em 25/25; 0 duplicatas; **c0 = 1 SEMPRE** | ≡ |
| U11 | erro de fantasia do infill | (2) | direta | otimista **73,84%**; cobertura 2σ **76,27%** (15.740 componentes); erro no espaço do modelo mediano 0,0836 | ≡ |
| E1 | **query-joia: identidade EIMe (Ap. A)** | **(1)** | direta | **7.020/7.020 @\|Δ\|<1e-5** · med **2,17e-09** · p99 7,01e-08 · máx 5,81e-06 | ≡ |
| E2 | argmax≡infill e planura do pool | **(1)** | direta | 4.783/7.020 (68,1%); **gap<1e-6 em 7.000/7.020** (máx 7,26e-05); med/máx do pool 0,999998 | ≡ |
| E3 | contrafactual σ→0 + N2 | **(1)** | direta | σ decide em **5.316/7.020 (75,7%)**; **77,0% sem as 3 células D=2**; N2 em **7.020/7.020** | ≡ + refino |
| E4 | frente ND = `paretofront` | **(1)** | direta c/ teto f32 | bracket 7.020/7.020; exata **6.557/7.020**; discordantes DTLZ4 240 · ZDT6 185 · MMF11_L 38 | ≡ |
| E5 | amostragem densa / anti-clustering ausente | **(1)** | direta | `min_dist_infill` mín **1,508e-03**; **0 gens <1e-8**; **0 de 1.299.400** linhas de pool coincidem com ponto antigo | ≡ |
| E6 | saturação EIM≡0 (underflow) | **(1)** | direta | **189 gens, todas BBOB_F1** (94,5% da célula); pool med/máx = **0,000000** ali | ≡ |
| E7 | re-scaling min-max de y | **(1)** | direta | desvio rel. máx `norm_min` 5,71e-08 / `norm_max` 5,08e-08; guard `max(range,eps)` **Δ=0 exato** | ≡ |
| E8 | kriging Forrester (≠ DACE) | (2) | direta-declarativa | `modelo_flag='OK-Forrester'` 100%; `pred_classe/score` NULL; **0 σ nulos** | ≡ |
| E9 | θ₀=1, bounds, motor de MLE | (2) | bounds direta · miolo T | **0 violações de bound** em 15.740; **6.256 piso / 1.124 teto**; **lnL sobe em 51/56 pares — 5 CAEM** | **CORRIGIDO** |
| E10 | GA interno × DE do paper (B10.2) | (2) | direta-declarativa | `ga_pop==10D` e `ga_gens==200` em **7.020/7.020** ⇒ **259.880.000** aval. da aquisição | ≡ |
| E11 | ③ = pop final (DEF-C2) + `eim_mediana_pool` | (2) | direta | 10D linhas/geração em 7.020/7.020; identidade da mediana **6.888/7.020** @rel<1e-5 | ≡ |
| E12 | EIMe hardcoded | (2) | direta-declarativa | literal `"Euclidean"` em 7.020/7.020 + eco em header e `params` | ≡ |
| E13 | calibração de σ na sonda | (2) | direta | WAPE melhora em **13/25** células; cobertura mediana 0,911→0,916; **MMF1 obj₁ 0,722→1,718** | ≡ |
| E14 | guards [IMPL] + crash stock | (2) | direta | `n_eim_nan=n_range0=n_dedup=0` em 7.020; **`n_front==1` 0 vezes** (mín. 2) | ≡ |
| **T1** | **não-perturbação da sonda (§3.1)** | **(2)** | **direta** | **①②bit-idênticas · ③ online max\|Δ\|=0 em 12 colunas · 27/27 campos do ⑥ com Δ=0,0** | **T → (2)** 🆕 |
| **N1** | **degeneração numérica do kriging** | **(2)** | **direta** | **118/15.740 comp.-geração com lnL≤0 ⇒ σ̂² ≥ 1 (Y∈[0,1]); máx σ̂² ≥ 9,0e+04; 3 células, todas D=2** | **NOVO** 🎯 |
| **N2** | **proveniência de campanha (B-03/I-09)** | **(2)** | **direta-declarativa** | s42: **0/25** com `campanha_id`, `repo_hash` vazio, `schema=1`. T11: **3/3** com os três + `sonda.desligada` | **NOVO** |
| **N3** | **reprodutibilidade entre máquinas (O-16/O-18)** | **(2)** | **direta** | init 21/21 Δ=0; **40/40 infills DIFEREM**, \|ΔX\| até 2,0 = 100% da caixa; **ΔHV 9,7% > piso 1,55%** | **NOVO** 🎯 |
| **T2** | operadores internos do GA | **T** | não-verif. | só o pool final é logado; ~200 lotes intermediários irrecuperáveis | ≡ |

---

### 2.2 Blocos narrativos

**U1 — orçamento de FE · (2) · direta.** *(a)* O paper fixa 100 FE (m=2) / 200 FE (m≥3) com n=6 (§V-B(1)c) e o repo hardcoda init=100/maxFE=200. *(b)* D21/D61 impõem `maxFE=31D−1` com hard-stop por `MException('PlatEMO:Termination')`, para uniformizar o orçamento por dimensão entre os 24 configs. *(c)* Re-medido em `c7_estrutura_s42.py`: `len(①)==31D−1` em **25/25** (61 no MMF1 … 929 no ZDT1, **Σ=10.856**), `fe_index` denso 0-based em 25/25, `solution_id` único em 25/25, `fe_final==maxfe` 25/25, `termino='normal'` 25/25, `cp_init=true` 25/25. *(d)* q=1 é nativo; o embrulho N.5 troca o `while evaluation<maxfe` por `NotTerminated` e o wrapper aborta no FE exato ⇒ overshoot zero. *(e)* **(2)**, direta.

**U2 — DoE 11D−1 do artefato · (1) · direta.** *(a)* §V-B(1)b: "The number of initial design points is set to be 11n−1 = 65". *(b)* Mesma fórmula; muda só a **proveniência** — o DoE é carregado de `data/doe/{problema}/doe_{problema}_42.parquet` (D63/D88), nunca regenerado. *(c)* `(fase=='init')` == 11D−1 em 25/25. Verificação bit-a-bit re-executada: arredondando o artefato float64 para float32, **max\|ΔX\| = 0,0 EXATO em 25/25**; contra o float64 cru, 1,91e-06 = exatamente ½-ULP float32 na escala dos bounds do WFG. `sha256` do artefato ≡ `manifest.doe_hash` em **25/25**. *(d)* O harness escreve a ① em float32 (D53); a igualdade exata só existe nesse espaço — e existe. *(e)* **(1)**.

**U3 — um fit por iteração · (1) · direta.** *(a)* O laço do paper reconstrói os m Kriging a cada ciclo. *(b)* Nada mudou. *(c)* `len(④) == n_gens(⑥) == n_infills` em 25/25 ⇒ **7.020 fits**; `modelo_hp.n` == `n_amostra` em toda geração — sem cache nem reuso. *(e)* **(1)**.

**U4 — cadência da sonda · (2) · direta.** *(a)* Instrumentação nossa; o paper não tem sonda. *(b)* DI-12.5: `g==1 ∨ g%2==0` + **finalProbe**. *(c)* Cadência OK em 25/25, finalProbe em 25/25, `len(geracoes)==n_blocos==#eventos sonda` em 25/25 — **3.535 blocos × 2.000 pontos = 7.070.000 linhas**, `n_falhas=0`. *(e)* **(2)**.

**U5 — join posicional · (2) · direta.** *(a)–(b)* O contrato não grava `sonda_id`; o join é posicional e precisa de prova (regra 5 do R4). *(c)* No MMF1 (21 blocos), `max|X_sonda − X_gabarito| = 1,192e-07` = ULP-float32 na faixa [1,3]×[−1,1]; `manifest.sonda.x_hash`/`f_hash` ≡ sha256 do gabarito (`c5`/`b5`). *(e)* **(2)** — ordem PROVADA.

**U6 — WAPE e cobertura · (2) · direta.** *(c)* `f5/sonda_f52e.csv` traz **7.926 linhas** de c238; `n_validas=2.000` em **7.926/7.926** e `n_nan` somado = **0** — zero σ-NaN, ao contrário da família treed. `modelo_flag` na sonda: `{'OK-Forrester': 7926}`. **Armadilha específica reconfirmada**: a ③ mistura DOIS espaços — sonda em `espaco_modelo='cru'` com `transf_params=NULL`, online em `'transformado'`/`minmax` com `{min,range}` da iteração. *(e)* **(2)**.

**U7 — invariante de tempo · (2) · direta.** *(c)* **0 violações em 7.020/7.020** — no c238 `tempo_pred_sonda_s` é campo próprio do ④ e já está dentro de `tempo_geracao_s` (DI-13.10, `c238_instrument.m:105-110`). Wall total do config 60.298 s; ZDT1 sozinho 14.284,6 s. *(d)* A premissa "expensive" está invertida no benchmark analítico: `tempo_aval_real_s` do MMF1 é 0,0335 s contra 15,44 s de wall. *(e)* **(2)**.

**U8 — `fe_treino_max` · (1) · direta.** *(c)* Monotônico em **25/25** (ao contrário de b1/b4/c217 — regra 9 do R4) e `== n_amostra−1` em **7.020/7.020**: arquivo inteiro, sem subamostragem, `n_dedup=0`. *(e)* **(1)**.

**U9 — reconciliação de guards · (2) · direta.** *(c)* Censo do ⑥ em 25/25: `{header:1, guard:1, sonda:n_blocos, c238_gen:n_gens, footer:1}`; o único guard é o `cache_hit` e bate com `manifest.cache_hits=1` em 25/25. Contadores internos somam 0 em 7.020 gerações. *(e)* **(2)**.

**U10 — aritmética entre camadas + A1 · (2) · direta.** *(c)* ② tem `n_gens+1` gerações em 25/25, tamanho `(init−1)+g` em 25/25, **0 duplicatas** na última; `n_gens = n_opt + cache_hits − c0` fecha com **`c0 = 1` em 25/25**. **Sub-item A1 (ledger PÓS-infill)**: `f_best(g) ≡ norm_min(g+1)` em **6.995/6.995** e `n_front1(g) ≡ n_front(g+1)` em **6.995/6.995** — dois campos gravados DEPOIS da FE. *(d)* `c0=1` é a mesma assinatura do b1: lei do harness. Aqui com `dup_infill=0` — nenhum FE perdido. *(e)* **(2)**.

**U11 — erro de fantasia · (2) · direta.** *(c)* Sobre 15.740 componentes (μ des-transformado por `μ·range+min` da própria iteração): viés **otimista em 73,84%**, cobertura 2σ **76,27%**, erro mediano no espaço do modelo **0,0836**. *(d)* Viés intrínseco ao EI — o critério premia exatamente onde o modelo prevê melhoria; a sonda, que amostra sem seleção, dá cobertura 0,916. *(e)* **(2)**.

**E1 — QUERY-JOIA: identidade fechada do EIMe · (1) · direta.** *(a)* Apêndice A verbatim: `lamda=(F−1*u)./(1*s); EIM=(F−1*u).*gausscdf(lamda)+1*s.*gausspdf(lamda); y=min(sqrt(sum(EIM.^2,2)))`. *(b)* Nada mudou (`Infill_EIM.m` chama `EIM_Euclidean` por literal). *(c)* **Re-implementada hoje do zero em float64** (`c1_mecanismo_s42.py:eim_euclidean`) e aplicada geração a geração às 25 células, contra a frente ND reconstruída da ① e re-escalada com o `{min,range}` da própria iteração: **\|Δ\| < 1e-5 em 7.020/7.020 (100%)**, mediana **2,17e-09**, p99 7,01e-08, máximo 5,81e-06 (MMF1). Pelo critério RELATIVO, 6.889/7.020; restrito às gerações de frente EXATA, **6.556/6.557 (99,98%)**. As 131 falhas relativas são **130 de ZDT6 + 1 de ZDT1** — células cujo `eim_best` desce a 1e-05…1e-28, onde o piso absoluto constante do export float32 (~2e-09) vira 4e-05 em termos relativos. *(d)* Reprodução **dígito por dígito** da F5, com script independente. *(e)* **(1)** — **o uso da incerteza está PROVADO pelos dados**: a fórmula que decidiu cada um dos 7.020 infills é, dentro do float32, a eq. (22)+(23) do artigo.

**E2 — argmax ≡ infill e planura do pool · (1) · direta.** *(c)* argmax exato em **4.783/7.020 (68,1%)**; nas 2.237 restantes o **gap relativo** é <1e-6 em **7.000/7.020**, pior caso **7,26e-05**. A razão **mediana/máximo do pool é 0,999998** (p05 0,995324) e **mínimo/máximo 0,999998**. *(d)* O GA roda 200 gerações (μ+λ) elitistas sobre 10D indivíduos; o pool final chega achatado e o desempate cai abaixo da precisão do export float32. O critério correto neste config é o **gap**, não o índice. *(e)* **(1)**.

**E3 — contrafactual σ→0 e N2 · (1) · direta, com refino novo.** *(a)* §IV-C prova N1/N2; o agente-4 registra o contrafactual como o coração do G5. *(c)* (i) **σ→0⁺** colapsa o EIMe em `min_j‖max(F^j−u,0)‖₂`: o argmax **muda em 5.316/7.020 (75,7%)**, de 0,5% (BBOB_F1) a **97,3% (WFG4)**. (ii) **N2** (σ×1,01): o EIM nunca diminui, em **7.020/7.020 gerações**, para todos os pontos do pool. Fração de exploração `s·φ`: mediana global **0,2548** (mediana das medianas por célula 0,2128). **Refino novo (`c8`)**: excluindo as 3 células D=2, onde o pool colapsa (A2) e o contrafactual é estruturalmente incapaz de trocar o argmax, sobe para **77,0%**; excluindo também BBOB_F1 (E6), **79,2%**. *(d)* Em 3 de 4 iterações a **incerteza**, não a média predita, determinou o ponto avaliado. *(e)* **(1)**.

**E4 — a frente ND e o teto float32 · (1) · direta com teto.** *(c)* Três semânticas reconstruídas (fraca-sequencial do `paretofront`, padrão, estrita): `n_front` cai **dentro do bracket em 7.020/7.020** e bate EXATO em **6.557/7.020 (93,4%)**. As 463 discordantes são **DTLZ4 240 · ZDT6 185 · MMF11_L 38** — objetivos que saturam em float32. *(d)* **Decisivo**: nessas células a identidade EIM fecha mesmo assim, porque `min_j‖EI^j‖` é invariante a réplicas na frente. *(e)* **(1)** com **teto declarado**: a frente float64 do MATLAB não é bit-recuperável da ① float32 nessas 3 células. Aplica-se a **regra 11 nova do CONTRATO** (dominância sobre ① é *lossy*) — que confirma a leitura da F5 em vez de mudá-la.

**E5 — amostragem densa e o anti-clustering ausente · (1) · direta.** *(a)* §IV-D: em ponto amostrado s=0 ⇒ EIM=0 ⇒ "the next point selected… would be anywhere but the sampled points"; §V-B(5)c prescreve o remédio (dist<1e-8 → argmax σ). *(b)* **B10.7 FECHADA: o remédio NÃO EXISTE no repo**; seguimos o código (🟠) e instrumentamos `min_dist_infill`. *(c)* O hazard **nunca se materializou**: mínimo global **1,508e-03** (ZDT1), medianas por célula de 0,0696 (MMF11_L) a 38,8 (WFG5), **0 gerações <1e-8** (nenhuma sequer <1e-6). Das **1.299.400** linhas de pool, **ZERO** coincidem com ponto avaliado ANTERIORMENTE; as 8.576 marcadas referenciam o próprio infill da geração. `min_dist` logado ≡ recomputado (máx \|Δ\| 2,87e-06 = float32). *(e)* **(1)** — confirmação empírica em escala da propriedade teórica do paper; **a divergência aberta `SPEC:811` na parte do anti-clustering está MEDIDA E ARQUIVADA como não-materializada nesta semente**.

**E6 — saturação `EIM ≡ 0` · (1) · direta.** *(c)* `eim_best == 0` EXATO em **189 gerações, todas BBOB_F1**, a partir da geração 8 de 200 (94,5% da célula); ali a razão med/máx do pool é **0,000000** — a paisagem inteira é nula. σ ∈ [4,9e-05; 1,9e-03] (≠0) com \|λ\| ≥ 184,7 ⇒ `Φ(−184)` e `φ(−184)` underflowam para 0 exato em IEEE-754 double. *(d)* A garantia do paper é verdadeira em aritmética exata e **vazia em ponto flutuante** quando o GP fica bom demais (sonda de BBOB_F1: WAPE 1,3e-04, corr 1,000000). Leitura para o D97: **em 193 dos 200 infills de BBOB_F1 o EIM não decidiu nada** — busca quase-aleatória na caixa. Ainda assim IGD+ 0,0276, rank 4/17, ganho 60,2×. **Comportamento FIEL** (a fórmula do Apêndice A tem exatamente essa propriedade), caveat de 1ª ordem. *(e)* **(1)**.

**E7 — re-scaling min-max de y · (1) · direta.** *(a)* §V-B(5)a — é **do paper**. *(c)* `norm_min`/`norm_max` ≡ mínimo/máximo por objetivo do arquivo pré-infill com desvio relativo máximo **5,71e-08 / 5,08e-08** em 7.020 gerações; `norm_range_efetivo ≡ max(Δ,eps)` com desvio **0,0 exato**. **Sub-item A3**: 3.292/6.995 subidas de `eim_best`, das quais 4,0% coincidem com mudança de escala, 15,3% com troca de frente e **80,7% com nenhuma das duas** — o DI-18 atribui o ruído à re-escala, e a causa dominante é o **remodelo do GP**. *(e)* **(1)**; toda leitura de μ/σ online exige des-transformar com o `transf_params` da PRÓPRIA linha.

**E8 — kriging Forrester, não DACE · (2) · direta-declarativa.** *(a)* §V-B(2): DACE `regpoly0`+`corrgauss`. *(b)* O repo do 1º autor **reimplementa** o kriging; adotamos o código (🟠, D29/E.5). *(c)* `modelo_flag='OK-Forrester'` único em 100% das linhas da ③ (online + sonda, 25 células + smoke); `pred_tipo='valor'`, `pred_classe`/`pred_score`/`pred_confianca` 100% NULL; **0 σ nulos**. Eco declarativo em 3 sítios (`params.kriging`, `sigma_dict`, `header`). *(e)* **(2)**.

**E9 — θ₀, bounds e o motor de MLE · (2) · bounds direta, miolo T · ⚠ CORRIGE A F5.** *(a)* §V-B(2)c-d: θ₀=1, θ∈[10⁻³,10³] — **exatamente** o que usamos (`MultiObjective_EIM.m:47`). O motor do paper é o `boxmin` do DACE. *(b)* O repo usa `fmincon` sqp em log₁₀θ, single-start, **MaxFunEvals=20D**, tol 1e-20 (`GP_Train.m:9-11`) — declarado no bundle como "mais rápido e mais sujeito a ótimo local". *(c)* Sobre **15.740 componentes-geração**: **0 violações de bound**; **6.256 saturações no piso 1e-3, 1.124 no teto 1e3**; `lnL` final < máximo da trajetória em **31/56** pares (célula,objetivo) — assinatura do single-start caindo em ótimos locais distintos. **E aqui a F5 errou**: ela afirmou "lnL sobe em 25/25 células"; a re-medição por par (célula,objetivo) dá **51/56 sobem e 5 CAEM** — MMF1 obj₀ 25,7 → **−165,0**, MMF1 obj₁ 27,8 → **−300,9**, MMF4 obj₀ 24,6 → **−178,5**, MMF4 obj₁ 32,1 → **−302,2**, MMF11_L obj₁ 27,0 → **−20,6**. Complemento: em **63 de 15.740** componentes-geração (0,4%) o `fmincon` devolve **θ ≡ θ₀ = 1 EXATO** — não andou —, concentrados nas mesmas 3 células D=2 (MMF4 obj₀: 24 de 40 fits). *(d)* Com D=2, MaxFunEvals=20D = **40 avaliações** para um sqp de 2 variáveis com gradiente por diferenças finitas: ~13 iterações. Onde a superfície é numericamente plana (N1), o otimizador sai sem sair do lugar. *(e)* **(2)**; o **interior** do sqp permanece **T**.

**E10 — GA interno × DE do paper · (2) · direta-declarativa.** *(a)* §V-B(4): DE/rand/1/bin, pop 50, 50 iterações, F=0,8, CR=0,8, ×4 ⇒ **10.000 avaliações/infill**. O próprio paper (§IV, 1º §) motiva o custo assumindo um GA 100×100 — trata o otimizador interno como intercambiável. *(b)* **DEF-B10.2/D30**: GA nativo do repo (`MultiObjective_EIM.m:52`, `Optimizer_GA(...,10*num_vari,200)`). *(c)* `ga_pop==10D` e `ga_gens==200` em **7.020/7.020**; a ③ tem exatamente 10D linhas/geração em 7.020/7.020 (1.299.400 linhas); total **259.880.000** avaliações da aquisição = 4.000 (D=2) a 60.000 (D=30) por infill, contra 10.000 fixos do DE ⇒ **2× a 6× mais busca interna em D≥10**, menos só nas 3 células D=2. *(d)* O risco seria sub-resolver o argmax; a medida mostra o contrário, e E2 mostra o pool achatado no topo — o GA converge. **Sub-item A2 (colapso em D=2)**, re-medido em `c8`: cópias bit-a-bit do vencedor no pool final — MMF1 **741/800 (92,6%)**, MMF4 **703/800 (87,9%)**, MMF11_L **697/800 (87,1%)**; em D≥20 cai a 0,45% (1 linha/geração). *(e)* **(2)** — e a divergência ⚠ ABERTA `SPEC:811` fica **medida**: ver §5.

**E11 — ③ = pop final (DEF-C2) e `eim_mediana_pool` (DI-10) · (2) · direta.** *(c)* Identidade da mediana recomputada ≡ `eim_mediana_pool` em **6.888/7.020 @rel<1e-5** (as mesmas 132 gerações de cauda do E1). A razão `mediana/best` é ~1,000000 porque o GA colapsa ⇒ **o DI-10 é quase redundante neste config**; o sinal útil é o contraste ENTRE células. *(e)* **(2)**.

**E12 — EIMe hardcoded · (2) · direta-declarativa.** *(a)* O paper propõe três agregações (eqs. 23/25/28) e **não elege default**; §VI-D recomenda EIMe/EIMm para frentes discretas. *(b)* O driver chama `EIM_Euclidean` por literal (var `criterion` morta na :51). *(c)* `criterion == "Euclidean"` em **7.020/7.020** eventos + eco no header e em `params`; nenhuma variante alternativa em lugar nenhum. *(d)–(e)* **(2)**; **consequência para a canônica: temos 1 dos 6 braços do paper**, o que elimina todos os vereditos direcionais internos dele.

**E13 — calibração de σ na sonda · (2) · direta.** *(c)* Re-lido de `f5/sonda_f52e.csv` com ordenação NUMÉRICA de bloco (armadilha: `bloco` é `object`; ordenar como string dá o bloco 99 como "último"). **DTLZ2 aprende**: obj₀ 0,1593→0,0976 (cob 0,853→0,932), obj₁ 0,1457→0,0763 (0,829→0,955), **obj₂ 0,0979→0,0098 (0,837→0,999)**. **MMF1 degenera**: obj₁ 0,7222→**1,7177**, cobertura 0,940→0,594, `|μ|máx` global **1.882,3**. Agregado: WAPE melhora em **13/25 células**, cobertura mediana 0,911→0,916, 21 de 56 pares terminam <0,90. Extremos: DTLZ4 obj₁ 2,013→**7,845** com corr → 0,143; BBOB_F17 chega a μ 7,8e+07. *(d)* Padrão do kriging estacionário: aprende onde a paisagem é suave, explode fora do suporte. **A causa da explosão está agora provada em N1.** *(e)* **(2)**.

**E14 — guards [IMPL] e o crash latente stock · (2) · direta.** *(c)* `n_eim_nan=0`, `n_range0=0`, `n_dedup=0` somados sobre **7.020 gerações**; `range` mínimo efetivo 0,280 (WFG1) — longe de eps. O crash stock **não foi exercitado**: `n_front` mínimo global **2**, `n_front==1` em **0 de 7.020**. *(d)* Guardas instaladas e silenciosas + hazard stock intocado ⇒ o run é comportamentalmente compatível com o repo oficial nesta semente. *(e)* **(2)**.

---

**T1 — não-perturbação da sonda · era T, agora (2) · DIRETA 🆕.** *(a)* Não há análogo no paper; é a invariante 🔴 do `CONTRATO §3.1`. *(b)* A F5 a colocou no balde **T** porque "exige um run pareado; nenhum rastro nos dados a decide". A T11 construiu o par: **G6b — o gêmeo MATLAB da flag `UA_DD_SAEA_SONDA_OFF`** (`SondaState.m:141-142`, guards `if obj.desligada, return; end` em :183/199/257; declaração `desligada` no ⑤ em :353/372) — "desarmar, não sumir". *(c)* Medido hoje em `c3_correcoes_t11.py` sobre `g6_com` × `g6_sem` (c238/MMF1/42, ambos no Mac, mesmo `repo_hash 195f64ed0a69`): **① `__real.parquet` sha256 IDÊNTICO — `43f533269741c82f…`, que é exatamente o hash declarado no handoff §5 para o c238** ✅; **② `__pop.parquet` sha256 IDÊNTICO** (`b93cc44b…`, 1.681 linhas); ③ **encolhe** 42.800 → 800 linhas (sonda 42.000 → 0) e as 800 linhas `regime='online'` têm **max\|Δ\| = 0 em 12 colunas numéricas**; ④ difere só no relógio. E — o teste que o hash sozinho não dá — **os 27 campos do evento `c238_gen` são bit-idênticos, Δ = 0,0 exato nos 40 valores de cada um**: `eim_best`, `eim_mediana_pool`, `theta_min/max/media`, `lnL`, `norm_min/max/range`, `u_best`, `s_best`, `n_front`, `n_front1`, `min_dist_infill`, `infill_sid`, `fe_treino_max`… Os três sinais do §4.2 estão todos presentes (`desligada=true` no ⑤, `n_blocos` 21→0, ③ encolhendo). *(d)* A sonda do c238 chama `GP_Predict` sobre um modelo já treinado e não toca RNG, orçamento nem estado — e agora isso está PROVADO, não argumentado. *(e)* **(2) · direta** — **sai do balde T**. É a única melhoria estrutural que a T11 traz a este config.

**N1 — degeneração numérica do kriging quando R fica quase-singular · (2) · direta · 🎯 CAVEAT DE 1ª ORDEM 🆕.** *(a)* O paper prescreve θ∈[10⁻³,10³] e Kriging gaussiano; não discute condicionamento. *(b)* O repo usa **nugget (10+n)·eps** (`GP_Train.m:18`) — para n=60, 1,55e-14 — e MLE single-start com MaxFunEvals=20D (E9). Ambos são "fidelidade-ao-código" (E.5/D29). *(c)* **Argumento rigoroso, derivável só do ⑥, sem proxy** (`c6_sigma2_implicado.py`): (i) `MultiObjective_EIM.m:31,57` garante **Y ∈ [0,1] EXATO** ⇒ Var(Y) ≤ 0,25; (ii) `GP_Train.m:24` grava `lnL = −0,5·n·log(σ̂²) − Σlog|diag(L)|` com `R` PD de diagonal 1+nugget, logo por Hadamard `|R| ≤ (1+nug)^n` ⇒ `−0,5·log|R| ≥ 0` ⇒ **`σ̂² ≥ exp(−2·lnL/n)`**; (iii) em particular **`lnL ≤ 0 ⟹ σ̂² ≥ 1`**, isto é, a variância de processo estimada é maior que a **amplitude inteira** de uma variável confinada a [0,1]. Medido: **118 de 15.740 componentes-geração (0,75%) têm lnL ≤ 0**, em **3 de 25 células, todas D=2** — MMF1 (48/80 componentes, σ̂² ≥ **9,0e+04**), MMF4 (47/80, σ̂² ≥ **2,8e+04**), MMF11_L (23/80, σ̂² ≥ 21,7). Começa na geração **6 de 40** (MMF1, n_treino=26) e **10 de 40** (MMF4, n_treino=30) — ou seja **85% e 75% do run rodam com pelo menos um objetivo numericamente inválido**. As outras 22 células são impecáveis: σ̂²_cota máxima **0,078** (BBOB_F55), abaixo dos 0,25 possíveis. Corroboração independente (`c4_condicionamento.py`, proxy isotrópico θ=θ_média, formula verbatim do `GP_Train.m`): **cond(R) na última geração = 2,69e+15 (MMF1) · 2,58e+15 (MMF4) · 9,09e+15 (MMF11_L)**, contra 1,0–1,1e+02 nas células saudáveis de mesmo n; correlação log₁₀(cond)×log₁₀(θ) = **−0,749**. *(d)* Mecanismo completo: **D=2 + 60 pontos comprimidos no quadrado unitário + θ preso em 1** (o `fmincon` devolve θ₀ exato em até 24 de 40 fits ali, E9) ⇒ o kernel gaussiano dá `R ≈ 𝟙𝟙ᵀ` ⇒ cond ~1e15 ⇒ o nugget de 1,5e-14 não regulariza ⇒ `R⁻¹` amplifica ⇒ `σ̂²` explode ⇒ `lnL` mergulha e **μ/σ da sonda saem em 1.882** (E13). É a mesma "degeneração numérica real no fim do MMF1" que a F5 registrou como prior CONFIRMADO — mas agora com **causa provada, cota rigorosa e escopo exato**. *(e)* **(2)** — a **causa** é integralmente sancionada (nugget e motor de MLE são do código oficial; os bounds de θ são do paper), mas a **consequência** não estava declarada em lugar nenhum. **Não vira classe (3)** porque não há desvio inexplicado: cada elo é código citado por arquivo:linha. Vira **caveat de leitura de 1ª ordem** — ver §8.

**N2 — proveniência de campanha · (2) · direta-declarativa 🆕.** *(a)–(b)* Não há análogo no paper; é B-03 (`campanha_id`) + I-09 (`repo_hash`) + schema v2 do ⑤, escritos em `experiment.m:3067-3080`. *(c)* Varredura das 25 células da s42 (`c3`): **`schema_version=1` em 25/25, `campanha_id` AUSENTE em 25/25, `repo_hash` VAZIO em 25/25, `sonda.desligada` AUSENTE em 25/25**. Nos 3 artefatos T11: `schema=2`, `campanha_id` presente (`t11-smoke-195f64e` / `195f64ed0a69_2026-07-30`), `repo_hash=195f64ed0a69…` idêntico nas duas pernas, `sonda.desligada` presente. Os **9 gates de proveniência** rodam **6/6 VERDES** nas três (`G-1 3x1 626/626 bit-idênticos, 0 ids órfãos · G-2 header=1 footers=1 malformadas=0 · G-3 · G-4 6/6 camadas · G-7 ⑥ 6/6 campos + ⑤ 6/6 chaves · B-15`). *(d)* É rastreabilidade para a campanha FUTURA: **o dado da s42 não a tem e não terá**. *(e)* **(2)**, direta-declarativa.

**N3 — reprodutibilidade entre máquinas · (2) · direta · 🎯 ESCALAÇÃO 🆕.** *(a)–(b)* O paper assume determinismo dada a semente; nossa O-16 (mesma máquina ⇒ bit-identidade) está **rompida só neste config** (O-19: 7 Mac / 18 vm3) e o piso O-18 declara HV ≤1,55% / IGD+ ≤58,98% como ruído. *(c)* O smoke T11 dá um par cruzado **inédito e limpo**: `main/c238/MMF1/42` no **Mac** (T11) × na **vm3** (s42), **com o código do c238 provadamente idêntico** (git, acima), **mesmo MATLAB** (`25.1.0.2973910 R2025a Update 1` nos dois manifests) e **mesmo DoE** (`doe_hash` igual). Medido: as **21 linhas de init são bit-idênticas (ΔX = 0, Δf = 0)**; **as 40 linhas de infill diferem TODAS**, com \|ΔX\| chegando a **2,0 — 100% da amplitude da caixa** em x₀∈[1,3]. A divergência nasce no **primeiro fit**: com conjunto de treino bit-idêntico, `fmincon` devolve θ_média = **992,18 (Mac)** × **718,37 (vm3)** para obj₀, com `lnL` = 25,672417 × 25,672515 — **Δ de 9,8e-05 nat**. É crista plana da verossimilhança amplificada por LAPACK diferente. **Consequência nos indicadores**: IGD+ **0,074549 (Mac)** × **0,041409 (vm3)** = **+80,0%**; HV **0,709418 × 0,785507 = −9,69%**. *(d)* **O piso O-18 não cobre este config nesta célula**: 9,69% em HV é **6,3× o piso declarado de 1,55%**. Faz sentido — D=2 com apenas 40 infills é o regime mais caótico do grid. *(e)* **(2)** metrológico, **escalado ao autor**: nenhuma comparação de HV entre células de máquinas diferentes do c238 é segura sob o piso atual.

**T2 — operadores internos do GA · T.** SBX dis_c=10 por-variável p=0,5, PM dis_m=20 pm=1/D, torneio k=2, seleção (μ+λ): o ⑥ declara pop e gerações, a ③ guarda **apenas a população final**; as ~200 populações intermediárias e as 259.880.000 avaliações de aquisição são irrecuperáveis. Verificável só por código/re-run. **Fora do denominador.**

---

## 3. Percentual por classe (Botão 2)

**Total = 29 aspectos · T = 1 (T2) · denominador = 29 − 1 = 28.**

| classe | aspectos | n | % |
|---|---|---:|---:|
| **(1) conforme o artigo** | U2, U3, U8, E1, E2, E3, E4, E5, E6, E7 | **10** | **35,7%** |
| **(2) desvio/extensão sancionado** | U1, U4, U5, U6, U7, U9, U10, U11, E8, E9, E10, E11, E12, E13, E14, **T1**, **N1**, **N2**, **N3** | **18** | **64,3%** |
| **(3) desvio inexplicado 🎯** | — | **0** | **0,0%** |
| T (fora do denominador) | T2 | 1 | — |

> **Leitura da variação vs F5 (10/15 sobre 25 = 40%/60%).** O conjunto de aspectos **(1)** é **idêntico e integralmente reconfirmado**. A queda de 40,0% → 35,7% é **puramente de denominador**: entraram 4 aspectos que, por natureza (instrumentação nossa e metrologia), só podem ser **(2)**. Nenhum aspecto migrou de (1) para (2) ou (3). *Nota de contagem*: A1/A2/A3 seguem como sub-itens de U10/E10/E7.
>
> Decisões sancionadoras citadas: **D21/D61** · **D63/D88** · **D53** · **D57/D89** · **D30+DEF-B10.2** · **B10.7** · **DEF-C2** · **DEF-C3** · **DI-10** · **DI-12.5** · **DI-13.10** · **DI-18** · **N.5/L.9/I.9/E.5** · **D81** · **B-03/I-09** (novas, T11) · **G6b** (nova, T11) · **O-18/O-19**.

---

## 4. 🆕 AS CORREÇÕES DA T11

| # | correção T11 | toca o c238? | implementada? | **funciona de fato?** | o que ainda falta |
|---|---|---|---|---|---|
| **G6b** | gêmeo MATLAB de `UA_DD_SAEA_SONDA_OFF` (§3.1 sai de 10 → 19 configs) | **SIM** | ✅ `SondaState.m:141-142,183,199,257,353,372` | ✅ **PROVADO, não só o campo**: `desligada=true`, `n_blocos` 21→0, ③ 42.800→800, ① e ② com **sha256 idêntico** (`43f533269741c82f`), ③ online **max\|Δ\|=0**, **27/27 campos do ⑥ com Δ=0,0** | nada — **fecha o T1 da F5** |
| **B-03/I-09** | `campanha_id`, `repo_hash`, `schema_version=2` | **SIM** | ✅ `experiment.m:3067,3068,3080` | ✅ **medido**: s42 **0/25**, artefatos T11 **3/3**; `repo_hash` idêntico nas duas pernas do par | vale só para dado FUTURO; a s42 não os tem |
| **G1/B-11** | ⑥ em append `O_APPEND`+`flock`, 1 linha = 1 `os.write` | SIM (writer compartilhado) | ✅ | ✅ **preventivo, não corretivo aqui**: varri as 25 células da s42 e o smoke — **0 linhas malformadas em 10.630 + 64**. O dano do B-11 atingiu `b1/WFG1`, não o c238 | nada |
| **G6/gates de proveniência** | 9 gates novos (G-1…G-7, B-15) | **SIM** | ✅ `scripts/gates_proveniencia.py` | ✅ **6/6 VERDES** no smoke e nas 2 pernas. O **G-1 3×1** audita justamente o A2 do c238: `626/626 bit-idênticos, max\|ΔX\|=0, 0 ids órfãos` | os gates conferem **estrutura**; nenhum confere **valor** (§15.1 do handoff, e é verdade) |
| **G7/I-02** | cronômetro `tempo_aval_real_s` | SIM | ✅ | ⚠ **NÃO É NOVO PARA O c238**: o campo já é **não-nulo em 25/25 células da s42** (0,0335 s … 1,107 s). A T11 não o acrescentou aqui | — |
| **I-07 (`params` no ⑤)** | 7 configs sem `params` | **NÃO** | — | ✅ **c238 nunca esteve na lista**: 13/13 chaves em **25/25** células da s42, e as 13 são **idênticas** às do smoke (0 diferenças) — confirma a **ERRATA 11** | — |
| **A11 sonda estratificada** | 4 classificadores | **NÃO** | — | ✅ **AUSENTE por desenho e verificado no dado**: `regime` na ③ do smoke = `{'sonda':42000,'online':800}`, sem `sonda_estratificada`. **Regra 12 do CONTRATO não se aplica** ao c238 | — |
| **A2/A7/A9 `REGRA_DO_ROTULO`, `ref_ids`, `pmid_ids`, `y_treino_dist`** | b4/c217/c122 | **NÃO** | — | ✅ **buscados no dado, não supostos**: `REGRA_DO_ROTULO`, `y_treino_dist`, `pmid_ids`, `ref_ids`, `n_ref`, `p_wrong_stats`, `margem_3sigma_stats`, `e0_trace`, `adapt_delta_V`, `n_baseline` — **todos ausentes do ⑥ e do ⑤**, como manda `contrato_61.json` (`di10=["eim_mediana_pool"]`) | — |
| **G-7 / CONTRATO §6.1** | contrato aferido por config | SIM | ✅ `contrato_61.json` | ✅ os 8 campos exigidos do c238 (`fe`, `f_best`, `n_front1`, `tempo_fit_s`, `tempo_busca_s`, `modelo_hp`, `dist_min_arquivo`, `eim_mediana_pool`) presentes em **40/40 eventos** | — |
| **A8/DI-45** | fix do e74 | **NÃO** | — | — | — |
| **G5/checkpoint** | truncamento-com-dado | não exercitado no c238 (0 abortos) | — | célula mais cara do config = ZDT1, 14.284,6 s = **3h58 < teto 12 h** | permanece não exercitado aqui |

### 4.1 · Procurei o padrão do c217 (campo presente, dado sentinela) — **não há no c238**

O achado da torre no c217 (`pmid_ids = [−1,−1,−1,−1,−1]` em 42/42) foi caçado sistematicamente aqui: varri **todos os 40 campos** do evento `c238_gen` no smoke e na s42, mais as 30 chaves do ⑤. Nenhum campo do c238 é sentinela — cada um foi confrontado com um recomputo independente e fechou: `eim_best` (E1, 7.020/7.020), `eim_mediana_pool` (E11, 6.888/7.020), `n_front` (E4, bracket 7.020/7.020), `min_dist_infill` (E5, max\|Δ\| 2,87e-06), `norm_min/max/range` (E7, Δ=0 no guard), `lnL` (recomputado do `GP_Train.m` com desvio 2,56e-07 no g=1), `f_best`/`n_front1` (A1, 6.995/6.995), `fe_treino_max` (U8, 7.020/7.020), `ga_pop`/`ga_gens` (E10, 7.020/7.020), `criterion` (E12, 7.020/7.020). **O ⑥ do c238 é auto-consistente com a ① e a ③ em todos os campos verificáveis.**

### 4.2 · Erratas que ESTA análise levanta

1. **Contra a minha própria F5 (E9):** "a lnL cresce do início ao fim em 25/25 células" — **falso** na granularidade (célula, objetivo): **51/56 sobem, 5 caem**, para −20,6 … −302,2. A agregação por célula escondeu o colapso das três células D=2. Idem "fica abaixo do máximo da própria trajetória em 25/25": o número por par é **31/56**.
2. **Contra a F5 (E9, menor):** saturações de θ eram "6.260 piso / 1.133 teto"; com o critério exato (`θ_min ≤ 1e-3`, `θ_max ≥ 1e3`) dá **6.256 / 1.124**. Diferença de definição, não de dado.
3. **Contra o handoff T11 — 17ª errata.** `handoff/T11-CONFORMIDADE.md` §2, linha 73: `| 11 | c238 | matlab | ⚪ falta (MATLAB) | — | ⚪ falta |`. A **linha 39 do mesmo arquivo** declara o §3.1 FECHADO para o c238 com o hash `43f53326`, e o `T11-RODADA-FINAL.md` §5 marca smoke ✅, portões ✅ 6/6, par ✅. **Medido por mim: smoke existe, 6/6 gates verdes, par bit-idêntico.** A tabela §2 é fotografia pré-MATLAB não atualizada.
4. **Contra o enquadramento do handoff (I-3):** `tempo_aval_real_s` é apresentado como ganho do G7 ("não-nulo nos 15 smokes MATLAB ✅"); no c238 ele já era **não-nulo em 25/25 células da s42**. Verdadeiro, mas não novo aqui.
5. **Armadilha de leitura descoberta hoje:** a coluna `bloco` de `f5/sonda_f52e.csv` é `object` (string). Ordenar por ela dá o bloco "99" como último e **inverte conclusões** (dá "27/56 pares melhoram" em vez de 29/56, e DTLZ2 obj₂ 0,0979→0,0636 em vez de →0,0098). Converter para numérico é obrigatório.

---

## 5. Comparação canônica (Entrega 2)

**Setup do paper** (§V-A/B; agente-4 `_resumo:107`): 12 instâncias — ZDT1/2/3 (m=2) e DTLZ2/5/7 (m=3,4,6) — **todas com n=6**; DoE 65 (11n−1); **100 FE (m=2) / 200 FE (m≥3)**; 10 execuções; HV com refs da Tab. I + IGD sobre grades; t pareado + Wilcoxon α=0,05. **Todas as comparações do paper são INTERNAS** (EIMe/EIMm/EIMh × EIe/EIm/EIh) — não há MOEA na bateria dele, e nós só temos o braço EIMe (E12).

| nosso × paper | veredito | por quê | sentido executado |
|---|---|---|---|
| **DTLZ2** (D=12, M=3, 371 FE) × DTLZ2 (n=6, m=3, 200 FE) | **incomensurável (eixo X)** — o caso mais próximo | M idêntico; fração de DoE 35,3% × 32,5% e FE/dim 30,9 × 33,3 **quase iguais**; mas **D é 2×** e o orçamento 1,86× | faixa-guia J: HV do paper 15,026 (ref 2,5³, não-normalizada) × nosso **0,411901** (ref normalizada, D69) — **grandezas de universos distintos**; sem veredito |
| **DTLZ7** (D=22, M=3, 681 FE) × DTLZ7 (n=6, m=3, 200 FE) | **incomensurável** (D 3,7×; FE 3,4×) | — | **âncora direcional executável**: §VI-D recomenda EIMe/EIMm para **frentes discretas**. IGD+ **0,109522** × 1,124709 (nsga3) = razão **0,0974 (10,3× melhor)**, rank 6/15 — **SENTIDO CONFIRMA ✓** |
| **ZDT3** (D=30, M=2, 929 FE) × ZDT3 (n=6, m=2, 100 FE) | **incomensurável** (D 5×; FE 9,3×; DoE 35,4% × 65%) | — | mesma âncora de frente descontínua: IGD+ **0,104839** × 0,344263 = razão **0,3045**, rank 4/15 — **SENTIDO CONFIRMA ✓** |
| **ZDT1** (D=30, M=2, 929 FE) × ZDT1 (n=6, m=2, 100 FE) | **incomensurável** (D 5×; FE 9,3×) | — | faixa-guia J: IGD do paper 0,0178 × nosso IGD+ **0,094777** — **NUNCA "artigo melhor"**. Posição interna: rank 7/16, razão 0,2134 vs nsga2 |
| ZDT2, DTLZ5, DTLZ2/5/7 m=4 e m=6 | **ausentes do nosso grid** | — | — |
| DTLZ1, DTLZ3, DTLZ4, ZDT4, ZDT6, MMF×4, WFG×5, BBOB×7 (21 células) | **incomensuráveis** (fora do envelope n=6 / suítes "fine for Kriging") | — | ver âncora declarativa |

**Âncora direcional declarativa — a única do paper inteiro que dá para executar.** §V-A é explícito: *"The (multiobjective) EGO algorithm has poor performance on objective functions which are difficult for the Kriging model to approximate… extremely multi-modal"* — e por isso o paper **evitou deliberadamente** essas suítes. Nosso grid tem os três casos que ele evitou: **DTLZ1** (g com 100·Σcos(20πx)), **DTLZ3** e **ZDT4** (Rastrigin). Re-executado hoje: são **exatamente os 3 piores resultados absolutos do config** (IGD+ 71,507 · 226,405 · 51,488, com **HV=0 nos três**) e 3 das 10 células em que o c238 perde do melhor piso (razões 1,577 · 1,745 · 1,173). **SENTIDO CONFIRMA ✓.**

**Placar: comparável 0 · direcional-qualitativa 3/3 confirmadas · incomensuráveis 25 · zero declarações de "artigo melhor" (Botão 4 respeitado).**

### 5.1 · A divergência ⚠ ABERTA `SPEC:811` — fechada com número

A nota de calibração pede avaliar se o GA-em-vez-de-DE e o anti-clustering ausente **mudam o mecanismo que a tese mede** (o surrogate e o uso da incerteza). Resposta medida:

- **GA × DE.** Não muda o mecanismo, e o desvio é **para mais**: 2000·D avaliações/infill = **259.880.000** no total, **2× a 6× o orçamento fixo de 10.000 do DE** em D≥10 (menos apenas nas 3 células D=2). E o argmax é resolvido: gap<1e-6 em **7.000/7.020** e pool med/máx 0,999998 (E2). O efeito colateral genuíno é **perda de diversidade interna em D=2** (A2, 87–93% do pool é cópia do vencedor) — sem custo em FE. O próprio paper trata o otimizador interno como intercambiável (§IV, 1º §, "GA 100×100").
- **Anti-clustering ausente.** O hazard **não se materializou em 7.020 iterações**: `min_dist_infill` mínimo **1,508e-03** (5 ordens acima do gatilho 1e-8 do paper), **0 gerações <1e-8**, **0 de 1.299.400 linhas de pool** coincidindo com ponto antigo, `dup_infill=0` em 25/25. A propriedade §IV-D do paper opera sozinha.

**Recomendação ao autor: reclassificar `SPEC:811` de ⚠ ABERTA para 🟠 CÓDIGO documentado + MEDIDA**, com os dois números acima como lastro.

---

## 6. Saúde (s42 = corpus principal)

| célula | máq. | IGD+ c238 | melhor piso | razão | rank | ganho traj. | viol. monot. | kriging N1 |
|---|---|---:|---|---:|---:|---:|---:|:--:|
| ZDT6 | vm3 | 0,04109 | nsga2 6,4457 | **0,0064** | 2/16 | 217,9× | 0 | ok |
| BBOB_F5 | vm3 | 0,01013 | smsemoa 0,1193 | **0,0849** | 3/17 | 90,5× | 0 | ok |
| DTLZ7 | **mac** | 0,10952 | nsga3 1,1247 | **0,0974** | 6/15 | 26,9× | 0 | ok |
| BBOB_F55 | vm3 | 0,06776 | nsga2 0,6061 | **0,1118** | 3/16 | 20,5× | 0 | ok |
| BBOB_F1 | vm3 | 0,02764 | nsga2 0,1360 | **0,2033** | 4/17 | 60,2× | 0 | ok (E6) |
| ZDT1 | vm3 | 0,09478 | nsga2 0,4441 | **0,2134** | 7/16 | 40,4× | 0 | ok |
| ZDT3 | vm3 | 0,10484 | nsga2 0,3443 | **0,3045** | 4/15 | 24,0× | 0 | ok |
| WFG5 | **mac** | 0,03016 | nsga2 0,0931 | **0,3241** | **1**/16 | 18,0× | 0 | ok |
| MMF11_L | vm3 | 0,01196 | nsga3 0,0232 | 0,5165 | **1**/17 | 47,9× | 0 | **🎯 23/80** |
| MMF16_20 | **mac** | 0,01891 | smsemoa 0,0316 | 0,5980 | **1**/15 | 45,3× | 0 | ok |
| MMF1 | vm3 | 0,04141 | nsga3 0,0570 | 0,7263 | 5/17 | 67,9× | 0 | **🎯 48/80** |
| BBOB_F22 | vm3 | 0,12200 | smsemoa 0,1591 | 0,7667 | 7/17 | 53,7× | 0 | ok |
| DTLZ2 | vm3 | 0,13101 | smsemoa 0,1701 | 0,7703 | 5/16 | 12,4× | 0 | ok |
| MMF4 | vm3 | 0,03160 | nsga3 0,0377 | 0,8380 | 10/17 | 11,6× | 0 | **🎯 47/80** |
| BBOB_F37 | vm3 | 0,24458 | smsemoa 0,2539 | 0,9632 | 10/17 | 4,8× | 0 | ok |
| ZDT4 | vm3 | 51,4881 | nsga2 43,898 | 1,1729 | 9/17 | 3,1× | 0 | ok |
| WFG9 | **mac** | 0,11597 | nsga3 0,0955 | 1,2139 | 8/16 | 6,0× | 0 | ok |
| WFG2 | **mac** | 0,15036 | nsga2 0,1228 | 1,2248 | 6/16 | 4,4× | 0 | ok |
| WFG1 | **mac** | 0,64038 | smsemoa 0,4954 | 1,2927 | 10/14 | 1,7× | 0 | ok |
| DTLZ1 | vm3 | 71,5072 | nsga2 45,346 | 1,5769 | 7/17 | 7,6× | 0 | ok |
| DTLZ3 | vm3 | 226,405 | smsemoa 129,77 | 1,7447 | 7/16 | 4,0× | 0 | ok |
| BBOB_F17 | vm3 | 0,03742 | nsga2 0,0206 | 1,8151 | 16/17 | 64,6× | 0 | ok |
| BBOB_F49 | vm3 | 0,42312 | nsga2 0,2071 | 2,0435 | 15/17 | 3,2× | 0 | ok |
| WFG4 | **mac** | 0,10731 | smsemoa 0,0444 | 2,4157 | 12/16 | 3,1× | 0 | ok |
| DTLZ4 | vm3 | 0,76704 | nsga3 0,2824 | 2,7162 | 13/15 | 1,6× | 0 | ok |

- **Trajetórias:** IGD+ não-crescente em **25/25 células — 0 violações em 475 transições**. Ganho mediano **18,0×**, máximo 217,9× (ZDT6), mínimo 1,60× (DTLZ4).
- **Posição:** bate o **melhor** dos 4 pisos em **15/25**; **rank médio 6,88 de 17**; **3 primeiros lugares absolutos** (WFG5, MMF11_L, MMF16_20).
- **Contrato:** `f5/integridade_f52a.csv` → **0 linhas** para c238 (de 111 no total); `f5/contrato_f52b.csv` → **0 linhas** (de 197). ⑥ com footer, `status='ok'`, `cp_init=true`, `termino='normal'` em 25/25; **0 linhas malformadas em 10.630**.
- **Sonda — régua Sobol (`regime='sonda'`), o único bloco comparável entre configs.** 7.926 medições (célula×bloco×objetivo), `n_validas=2.000` em 100%, **σ-NaN = 0**, `modelo_flag='OK-Forrester'` em 7.926/7.926. WAPE melhora em **13/25 células** e **29/56 pares (51,8%)**; cobertura mediana **0,928 → 0,948** por par (0,911 → 0,916 por célula), com **21/56 pares terminando <0,90**. Extremos: DTLZ7 obj₀ 2,2e-04 → 2,1e-04 com corr 1,000000; DTLZ4 obj₁ 2,013 → **7,845** com corr → 0,143.
- **Bloco `sonda_estratificada`: NÃO EXISTE no c238** (verificado no dado, `c3.4`). **Regra 12 do CONTRATO não se aplica** — nada foi misturado.
- **Piso de ruído:** ver **N3** — a medição cruzada Mac×vm3 do MMF1 dá **ΔHV 9,69%**, acima do piso O-18 (1,55%). Toda razão entre células de máquinas diferentes deste config deve ser lida com essa margem, não com 1,55%.

---

## 7. Score, recomendação e VEREDITO

### **SCORE: 9,5/10 · ACEITAR** (mesma nota, com 3 caveats de LEITURA nomeados)

### **VEREDITO: MANTEVE**

**A causa, nomeada:** **o mecanismo não mudou, e isso é PROVADO por duas vias independentes.** (i) **Git**: `src/c238_instrument.m`, `src/c238_sonda.m` e `algorithms/c238_EIM/` estão congelados em `941901f` (21/07) e `b3675ef` (24/07) — anteriores à s42 e à T11; os únicos `.m` compartilhados tocados na campanha são cabeçalho do ⑤, um acessor read-only de 20 linhas para uma sonda que o c238 não tem, e um kill-switch de sonda desligado por padrão. (ii) **Dado**: a re-medição integral das 25 células com scripts escritos hoje do zero reproduz **todos** os números da F5 — 7.020/7.020 na identidade EIMe com mediana 2,17e-09, 4.783/7.020 no argmax, 5.316/7.020 no contrafactual, 6.557/7.020 na frente exata, 189 saturações todas em BBOB_F1, 259.880.000 avaliações da aquisição, c0=1, 6.995/6.995 no ledger, ΣFE=10.856.

**O que puxou para cima:** **T1 saiu do balde T** — a invariante 🔴 do CONTRATO §3.1 deixou de ser não-verificável e passou a estar provada com uma força maior do que o handoff reivindica: não só ① e ② com sha256 idêntico, mas **os 27 campos do evento de geração do ⑥ com Δ = 0,0 exato** — θ, lnL, `eim_best`, `u_best`, `s_best`, `norm_min/max`, `n_front` — isto é, **a trajetória interna inteira**, não apenas o resultado. Some-se a proveniência de campanha (N2) e 6/6 gates verdes.

**O que puxou para baixo:** **N1** — a degeneração numérica do kriging em 3 de 25 células (12% do corpus, **incluindo MMF11_L, que é um dos 3 primeiros lugares absolutos do config**), com cota rigorosa σ̂² ≥ 1 em 118/15.740 componentes-geração e até ≥ 9,0e+04. **Não é infidelidade** (cada elo é código do próprio autor do paper, citado por arquivo:linha, com bounds de θ que são do artigo), e a F5 já a tinha em embrião (E13, "degeneração numérica real no fim do MMF1 — CONFIRMADA e AMPLIADA"); o que a T11 acrescenta é **causa, cota e escopo**. É agravamento de caveat, não de classe. Soma-se **N3**: o piso O-18 é insuficiente para este config.

**Por que 9,5 e não 10:** três caveats de **leitura**, zero de fidelidade — (a) **BBOB_F1**: o critério satura em 0 exato a partir da 8ª de 200 iterações (94,5% da célula), então aquela célula mede DoE + busca quase-aleatória, não EIM; (b) **N1**: MMF1/MMF4/MMF11_L rodam a segunda metade com pelo menos um objetivo cujo kriging é numericamente inválido; (c) **N3/O-19**: 7 Mac / 18 vm3, com divergência cruzada medida acima do piso declarado. **Por que não menos que 9,5:** 0 aspectos classe (3), contrato 100% limpo em ambas as fases, a query-joia fecha em 7.020/7.020, os dois contrafactuais confirmam que a incerteza é load-bearing, a propriedade central §IV-D do paper está confirmada em escala, e todos os 18 desvios têm decisão sancionadora citável com assinatura medida conforme o declarado.

---

## 8. Aspectos classe (3) 🎯

**Nenhum aspecto de FIDELIDADE em classe (3).** Nada segue à verificação adversarial F5.4 por desvio inexplicado.

**Duas ESCALAÇÕES ao autor (D97), ambas fora do eixo de fidelidade:**

**🎯 E-1 · N1 — o kriging degenerado nas 3 células D=2.** *Evidência completa*: `Y ∈ [0,1]` por construção (`MultiObjective_EIM.m:31,57`) + `lnL = −0,5n·log σ̂² − Σlog|diag L|` (`GP_Train.m:24`) + Hadamard sobre `R = R₀ + I·(10+n)·eps` (`GP_Train.m:18`, PD, diagonal 1+nug) ⇒ **`σ̂² ≥ exp(−2·lnL/n)`, e `lnL ≤ 0 ⟹ σ̂² ≥ 1`**. Medido em 15.740 componentes-geração: **118 (0,75%) com lnL ≤ 0**, em MMF1 (48/80, a partir da geração 6/40, σ̂² ≥ 9,0e+04), MMF4 (47/80, geração 10/40, σ̂² ≥ 2,8e+04) e MMF11_L (23/80, geração 15/40, σ̂² ≥ 21,7). As outras 22 células: σ̂²_cota ≤ 0,078. Corroboração: cond(R) 2,7e+15 / 2,6e+15 / 9,1e+15 nessas três contra ~1e+02 nas saudáveis; corr log₁₀(cond)×log₁₀(θ) = −0,749. Co-causa medida: `fmincon` devolve **θ ≡ θ₀ = 1 EXATO** em 63/15.740 componentes, 60% deles em MMF4 obj₀ — com MaxFunEvals = 20D = **40 avaliações** para D=2. **Recomendação**: não é re-run — é **caveat declarado na leitura do D97** para MMF1/MMF4/MMF11_L ("o surrogate perde validade numérica na segunda metade do orçamento"), e item para o `PLANO_RODADA_PERFEITA` (nugget adaptativo ou `MaxFunEvals` com piso, ambos ⚠ divergência nova do código oficial ⇒ decisão de fidelidade do autor, D81).

**🎯 E-2 · N3 — o piso O-18 não cobre este config.** *Evidência completa*: par cruzado `main/c238/MMF1/42` Mac (T11) × vm3 (s42), com código do c238 provadamente idêntico (git), mesmo MATLAB R2025a Update 1 (ambos os manifests) e mesmo `doe_hash`. Init 21/21 com **ΔX = 0 e Δf = 0**; **40/40 infills diferem**, \|ΔX\| até **2,0 = 100% da amplitude de x₀∈[1,3]**. Origem no primeiro fit: θ_média 992,18 (Mac) × 718,37 (vm3) com Δ lnL = 9,8e-05 nat. Indicadores: **IGD+ +80,0%** (0,074549 × 0,041409) e **HV −9,69%** (0,709418 × 0,785507). O piso declarado é HV ≤1,55% ⇒ **excedido em 6,3×**. **Recomendação**: registrar um piso específico para o regime D=2 / orçamento ≤61 FE, ou marcar as células D=2 do c238 como não-comparáveis entre máquinas.

---

## 9. Teto de verificabilidade (T) e armadilhas

**T (exige código/re-execução — confirmado invisível nos dados):** operadores internos do GA (SBX dis_c=10 por-variável p=0,5, PM dis_m=20 pm=1/D, torneio k=2, seleção (μ+λ)) e as ~200 populações intermediárias · as **259.880.000** avaliações da aquisição (só o pool final sobrevive) · o miolo do `fmincon` sqp (trajetória, nº efetivo de avaliações; só θ_min/max/média e lnL são logados — e **θ nunca é logado componente a componente**, o que impede recomputar R exatamente com o θ ARD real) · a frente float64 que o MATLAB usou nas 3 células saturantes (a ① é float32, D53) · contabilidade de RNG (`lhsdesign` do DoE e do init do GA, `randi`/`rand` dos operadores).

**O que este corpus NÃO permite verificar, mesmo com o smoke:** (i) nenhuma das **cinco outras agregações** do paper (EIMm eq.25, EIMh eq.28, EIe/EIm/EIh) — logo, **zero** dos vereditos direcionais internos do artigo; (ii) o **DE 50×50** do paper — não existe braço; (iii) o **anti-clustering** — o remédio nunca foi escrito, só o hazard foi instrumentado; (iv) **correção numérica dos valores** — os 6 gates conferem estrutura, e o handoff §15.1 está certo ao dizer que um algoritmo pode estar errado e passar em todos; (v) o comportamento sob **q>1** e sob **30 sementes** (a s42 é 1 semente); (vi) se a divergência Mac×vm3 é atribuível a máquina **isoladamente** — o par cruzado disponível é 1 célula, e o smoke não foi rodado na vm3.

**Armadilhas CONFIRMADAS (as 10 da F5 continuam válidas) + 4 NOVAS:**

1. `f_best` e `n_front1` são campos **PÓS-infill**: `f_best(g) ≡ norm_min(g+1)` e `n_front1(g) ≡ n_front(g+1)`, ambos 6.995/6.995. Lê-los como pré-infill erra por uma iteração.
2. A ③ do c238 mistura **DOIS espaços**: online `transformado`/`minmax` com `transf_params={min,range}` da iteração; sonda já em `cru` com `transf_params=NULL`.
3. Identidade do critério: usar o **ABSOLUTO** (\|Δ\|<1e-5), nunca o relativo, onde `eim_best` desce a 1e-05–1e-28 (ZDT6, ZDT1).
4. **`EIM ≡ 0` por underflow** é real e maciço: 189 gerações de BBOB_F1 com σ∈[4,9e-05;1,9e-03] e \|λ\|≥184,7. Filtrar esse regime antes de qualquer "o critério guiou?".
5. `real_solution_id` no pool **não é clone de ponto antigo**: 8.576/8.576 apontam para o próprio infill; em D=2 até 92,6% do pool é o vencedor replicado.
6. `n_front` recomputado da ① não bate onde o objetivo satura (DTLZ4 α=100, ZDT6 sin⁶) — é teto float32 (**regra 11 do CONTRATO**), não desvio. Reportar sempre o bracket.
7. **`c0 = 1` é lei também aqui** (2º config): 1 `cache_hit` em `fe=1`, dentro do init, em 25/25 — e `dup_infill = 0`, nenhum FE perdido.
8. **DI-18 precisa ser corrigido**: o salto de `eim_best` vem do **remodelo do GP** (80,7% das subidas), não da re-escala (4,0%).
9. **DI-10 é quase redundante no c238**: `eim_mediana_pool/eim_best` ≈ 1,000000 porque o GA colapsa.
10. **c238 é o único config main com máquinas MISTAS** (7 Mac / 18 vm3, O-19).
11. 🆕 **`bloco` em `f5/sonda_f52e.csv` é STRING.** Ordenar sem `pd.to_numeric` troca o último bloco pelo "99" e inverte conclusões da sonda (§4.2, item 5).
12. 🆕 **`lnL ≤ 0` é o detector barato de kriging degenerado** neste config — cota rigorosa σ̂² ≥ 1 com Y∈[0,1]. Vale para qualquer config que use `GP_Train.m` do repo do c238.
13. 🆕 **O contrafactual σ→0 é estruturalmente inútil em D=2**: com 87–93% do pool sendo cópia bit-a-bit do vencedor (A2), não há para onde o argmax migrar. Os 2,5–7,5% medidos ali não medem a importância de σ — o número honesto do config é **77,0% (sem D=2)**.
14. 🆕 **`tempo_aval_real_s` já existia no c238 pré-T11** (25/25 células); não confundir com ganho de instrumentação da campanha.

---

### Rodapé — artefatos desta análise

Escritos exclusivamente em **`/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/c238/`** (dados, artigos, bundles e código do algoritmo permaneceram read-only; nenhuma célula foi rodada; `experiments.py` nunca foi invocado).

**Scripts desta sessão** — `t1_smoke_instrumentacao.py` (manifesto/⑥/③ smoke×s42 + par G-6) · **`c1_mecanismo_s42.py`** (query-joia + contrafactuais + E2/E4/E5/E6/E7/E11/E14/U8/U11/A1 nas 25 células) · `c2_mle_condicionamento.py` (motor de MLE, θ≡θ₀, trajetória de lnL) · **`c3_correcoes_t11.py`** (as correções da T11 campo a campo + os 3 sinais do par G-6 + campos não-aplicáveis) · `c4_condicionamento.py` (cond(R) verbatim do `GP_Train.m`) · `c5_saude_sonda.py` (métricas, trajetórias, sonda, tempo) · **`c6_sigma2_implicado.py`** (a cota rigorosa σ̂² ≥ exp(−2·lnL/n)) · `c7_estrutura_s42.py` (U1/U2/U4/U7/U9/U10) · `c8_pontas.py` (A2, exploração, sonda).
**Scripts de sessão anterior nesta mesma pasta, revisados e reaproveitados** — `b1_estrutura_smoke.py`, `b2_divergencia.py`, `b3_eim_identidade.py`, `b4_par_sonda.py`, `b5_sonda_e_saude.py`, `b6_saude.py`, `b7_mle_ridge.py`, `b8_mle_gap.py`, `b9_mle_sanidade.py`.
**Evidências (CSV)** — `mecanismo_s42.csv` (7.020×49) · `estrutura_s42.csv` · `sigma2_cota_s42.csv` (15.740) · `mle_theta_s42.csv` · `cond_s42.csv` · `a2_colapso_ga.csv` · `quinto_s42.csv` · `saude_c238_t11.csv` · `sonda_c238_t11.csv` · `eim_gens_t11.csv` · `mle_gap.csv` · `mle_sanidade.csv`. **Logs** — `t1_log.txt`, `c1_log.txt`…`c8_log.txt`, `c8b_log.txt`, `b1_log.txt`…`b9_log.txt`.
**Insumos pré-computados consumidos (não recomputados)** — `f5/metricas_finais_f52c.csv`, `f5/trajetorias/main_c238_*_42.json` (25), `f5/sonda_f52e.csv` (7.926 linhas c238), `f5/contrato_f52b.csv`, `f5/integridade_f52a.csv`, `f5/tempo_f52d.csv`.
**Fontes de código citadas por linha** — `algorithms/c238_EIM/GP_Train.m:9-11,18,20-24` · `algorithms/c238_EIM/MultiObjective_EIM.m:31,39,47,52,57,63` · `algorithms/c238_EIM/Infill_EIM.m:19-21,25` · `src/SondaState.m:141-142,183,199,257,353,372` · `src/experiment.m:3067,3068,3080` · `src/FEBudget.m:127-146` · `src/c238_instrument.m:105-110,127,136-137` · `claude_code_context/artifacts/contrato_61.json:configs.c238`.