# RELATÓRIO DE FIDELIDADE T11 — c122 θ-DEA-DP (Python) · validação da rodada T11 vs F5

**Analista c122 · 2026-07-31 · protocolo v1.1 (Etapa 7, formato RICO) · READ-ONLY**
**Corpora:** (A) **s42 — 25/25 células** em `resultados_experimentos/c122/{prob}/42/` (10.856 FE · 7.022 decisões · 3.537 blocos de sonda · 7.435.480 linhas de ③) — o corpus do MECANISMO; (B) **smoke T11 — 1 célula PRESERVADA** (`main/c122/MMF1/s0`, 41 gerações, 44.000 linhas `sonda` + 10.500 `sonda_estratificada` + 3.632 de busca) — o corpus da INSTRUMENTAÇÃO NOVA.

> 🚨 **CORREÇÃO DO BRIEFING (medida, não inferida).** O briefing e o `evidencia_T11/LEIA-ME.md` (mtime **10:13**) afirmam que os smokes Python **não foram preservados**. **Foram.** A pasta `/Users/gmello/Documents/python_repos/mestrado/evidencia_T11/smoke_python/` existe (mtime **10:45**, 32 min depois do LEIA-ME) e contém `experiments/main/c122/exp_main_c122_MMF1_0.*` — 6 arquivos, manifesto com `campanha_id=9ad0138aa9a4_2026-07-31`, `repo_hash=9ad0138a…`, `schema_version=2`. **Toda a §4 deste relatório é evidência (iv) MEDIDA NO SMOKE, não (i) código lido.** O LEIA-ME está stale; corrigi-lo é trabalho de 1 linha.

---

## 1. Ficha do mecanismo (condensada)

θ-DEA-DP (Yuan & Banzhaf, TEVC 26(1):159-173, 2022): motor **θ-DEA** (N vetores Das-Dennis → N clusters; θ-dominância PBI, θ=5) assistido por **duas FNN 2×200 par-a-par de 3 classes** sobre `[x_i,x_j]` normalizado a [−1,1] — **Pareto-Net** e **θ-Net**. Por iteração: cluster escolhido em **rodadas de ordem embaralhada** (`PerCounter`), **N\*=7000** ofertas SBX+PM, preseleção em 2 estágios (Q1≻Q2≻Q3 ou Q5; corte `Q_max=300` por `p_sum`; **EDN** `e(z)=Σ_j I(t̂=1)·p̂` somado sobre as 2 redes, `z*=argmax`) → **1 FE**. Retreino gateado por `acc_min` vs γ=0,9, dosado por `E_upd=(1−acc_min/γ)E_init`, janela `T_max=11n+24`. Incerteza = **max-softmax**, usada para *gerir confiabilidade* — **sem bônus de exploração**.

**Divergências sancionadas nomeadas:** orçamento **31D−1 hard-stop** (D21/D61, não 250) · **DoE 11D−1 do artefato** (D63/D88) · **DEF-B11.1 + D73b** (f_min/f_max fixos do front VERDADEIRO +10% — vantagem informacional DECLARADA) · gate `>=` do warm-start ⟦v2.2⟧ · erratum **T′=min** ⟦v2.2⟧ · **cap anti-spin 10** (E.7; o paper não capeia, fn. 6) · **Step 14 ausente no código oficial** (D29 🟠 impl→código) · `random.sample` da survival (D30 / SPEC §434 🟠 IMPL) · ③-busca = TOP-100 por e(z) (DI-16.3 A+) · `visualization=False` + float32 forçado (N.1.1/E.7). **Sem cabeça de valor:** `mu_*/sigma_*` NULL em 100% das 7.435.480 linhas de ③ da s42.

---

## 2. DISSECAÇÃO DOS ASPECTOS (o CORE)

**Verificabilidade:** D=direta · DD=direta-declarativa · I=indireta · T=teto. **Classe:** (1) conforme o artigo · (2) desvio sancionado · (3) inexplicado 🎯 · T teto.
**Evidência:** (iii)=dado s42 · (iv)=dado do smoke T11 · (i)=código lido · (ii)=veredito registrado pela T11.

### 2.1 Tabela-resumo

| # | aspecto | classe | verif. | resultado-síntese (re-MEDIDO) |
|---|---|---|---|---|
| A1 | Orçamento 31D−1, hard-stop | (2) D21/D61 | D (iii) | `maxfe=31D−1 ∧ fe_final=maxfe` **25/25**; `n_ger = maxfe−(11D−1)+cache_hits` **25/25** |
| A2 | DoE 11D−1 LHS do artefato | (2) D63/D88 | D (iii) | `doe_hash≡sidecar` 25/25; max\|ΔX\| 1,9e-6 (roundtrip float32) |
| A3 | 12 hiperparâmetros da Tab. III | **(1)** | DD+D (iii,iv) | 12/12 em 25/25 + no smoke; N\*=7000 e Q_max=300 confirmados OPERACIONALMENTE |
| A4 | q=1 estrito, arquivo append-only | **(1)** | D (iii) | 1 `real_solution_id`/geração em **7.022/7.022** |
| A5 | Pool 7000 + acordo das 2 redes | **(1)** | D (iii) | `n_acordo+n_desacordo=7000` em **5.029/5.029** (⚠ corrige a F5: eram 5.047) |
| A6 | Estágio 1: Q1≻Q2≻Q3 + ramo Q5 | **(1)** | D (iii) | prioridade **5.047/5.047** no ramo `categorias`; Q1 3.552 · Q2 1.178 · Q3 317 · Q5 1.975 |
| A8 | Corte Q_max=300 por `p_sum` | **(1)** | D (iii) | `n_cands=min(\|Q\|,300)` em **7.022/7.022** (inclui o ramo distinct) |
| A9 | **Estágio 2: EDN + argmax (query-joia)** | **(1)** | D (iii,iv) | argmax do bloco em **6.440/6.440** (s42) e **41/41** (smoke) |
| A10 | Simetrização Eq. 4 | **(1)** | DD+I (i,iii) | `pred_confianca` ∈[0,434;1,0]; `nn_predict_dom` avalia [u,v] e [v,u] |
| A11 | Rodadas embaralhadas (`PerCounter`) | **(1)** | D (iii) | **668/668** rodadas = permutações; 1ª em ordem natural 25/25 |
| A12 | Gate `acc_min>=γ` (código) × `>` (paper) | (2) ⟦v2.2⟧ | D (iii) | **13.843/13.843** avaliações de gate (P+S) consistentes — 100% |
| A13 | `E_upd=ceil((1−acc/γ)E_init)` | (2) código | D (iii) | **8.538/8.538** retreinos, só na aritmética IEEE do código |
| A14 | `T_max` + erratum T′=**min** + monotonicidade | (2) ⟦v2.2⟧ | D (iii) | `T_max=11D+24` 25/25; **0 quedas** de `fe_treino_max` em 25/25 |
| A15 | Classe ausente: sentinela `acc=1` → cold start | (2) código oficial | D+I (iii) | ≥1 classe em acc=1,0 em 7,5–80,5%; **ZDT4: θ-Net nunca nasce** (201/201) |
| A17 | f_min/f_max fixos (DEF-B11.1/D73b) | (2) | DD+D (iii) | constantes em 25/25 e ≡ manifesto; causa provada do colapso de ZDT4 |
| A18 | x∈[−1,1], float32 forçado, `visualization=False` | (2) N.1.1/E.7 | DD (iii) | 25/25; `pinning.default_dtype=float64` coexiste (armadilha declarativa) |
| A20 | Anti-spin: cap 10 × fn. 6 sem cap | (2) E.7 | D (iii) | 890 `spin_resample`; cap só em WFG1 (9×); reconciliação 25/25 |
| A21 | **Step 14 do Alg. 1 não existe no código** | (2) 🟠 | D (iii) | e(z) do bloco-1 > 2N em **22/25** e ≤2(11D−1) em **25/25** |
| A22 | Survival θ-ND truncando a N | (2) D30 | I (iii) | \|P\|=N em 7.022/7.022; infill sobrevive 26,4–71,4% |
| A23 | ③-busca = TOP-100 por e(z) | (2) DI-16.3 A+ | D (iii,iv) | `linhas(g)=min(100,n_cands)` **7.022/7.022** e **41/41** |
| A24 | `pred_confianca`/`pred_score`: 2 semânticas; μ/σ NULL | (2) sigma_dict | D (iii) | sonda saturada 1,0 em 99,3% de 7,07 M; μ/σ NULL 100% |
| A26 | Sonda: cadência, finalProbe, join posicional | (2) DI-09/12.5 | D (iii,iv) | 3.537/3.537 blocos com 2.000 linhas; smoke max\|ΔX\|=**1,19e-7** |
| A28 | Cache-hit + timing + reconciliação | (2) D57/D89/DI-13.10 | D (iii) | 2 células com 1 hit; `fit+busca ≤ tempo_geracao` 100% em 25/25 |
| **N1** | 🆕 **[I-01+I-1] `n_ref` REAL + `ref_ids`** | (2) | D (iv) | g=1 → **n_ref=21=11D−1**, g≥2 → 11=MU; **252 ids reais, 0 sentinelas** |
| **N2** | 🆕 **[I-2+I-5] `REGRA_DO_ROTULO` + `y_treino_dist`** | **(3)** 🎯 | D (iv) | presentes e usáveis, **mas a regra aponta o evento ERRADO** (14/22 blocos) |
| **N3** | 🆕 **[I-6] `sonda_estratificada`** | **(3)** 🎯 | D (iv) | funciona (AUC +0,052), **mas grava e(z) em `mu_0` e `pred_score` NULL 10.500/10.500** |
| **N4** | 🆕 Resíduo declarativo do I-01 | **(3)** 🎯 | D (iv) | `manifest.sonda.n_ref=11` e `sigma_dict` ainda dizem "**tamanho FIXO**" |
| **N5** | 🆕 [I-3/I-4/G3] encanamento global | (2) | D (i,iii,iv) | `tempo_aval_real_s` não-nulo 25/25+smoke; NO_RETRY inerte aqui; `campanha_id` ✅ |
| T1 | CE ponderada (Eq. 3) + Kaiming + sem dropout | **T** | — | nada exportado além de `weight_decay` |
| T2 | `random.sample` do front excedente (D30 / SPEC §434) | **T** | — | só \|P\|=N é observável |
| T3 | Desempate do Pareto-rep por ‖w_i−w_j‖ | **T** | — | representantes não exportados |

### 2.2 Blocos narrativos

**A1 — Orçamento 31D−1 com hard-stop.** (a) §IV-A-4 fixa MaxEval=**250** (m=2,3). (b) D21/D61 substituem pelo invariante global 31D−1, conduzido por `bud.fe<bud.maxfe` para que um cache-hit não gaste iteração. (c) Re-medido hoje, célula a célula (`remedida_s42.py`): `maxfe==31D−1` e `fe_final==maxfe` em **25/25**, de MMF1 (D=2 → 61 FE) a ZDT1/ZDT3 (D=30 → 929 FE), somando 10.856 FE; `n_geracoes == maxfe−(11D−1)+cache_hits` fecha **25/25**; `n_retries=0` em 25/25. (d) 1 FE/iteração + teste do saldo antes de gerar ⇒ nem sobra nem overshoot. (e) **(2) · D21/D61 · direta (iii).** Nosso orçamento vai de 0,24× a 3,7× o do paper — origem primária da incomensurabilidade da §5.

**A2 — DoE 11n−1 do artefato.** (a) §III-A Step 2 prescreve exatamente 11n−1 por LHS. (b) A única mudança é a FONTE (artefato congelado, D63/D88) para tornar as comparações intra-problema pareadas. (c) `doe_hash≡sidecar` 25/25; max\|ΔX\| ≤1,9e-6 (WFG, bounds até 44 → ULP-float32). (d) Diferença residual = roundtrip float32 do export (D53). (e) **(2) · direta (iii).**

**A3 — Os 12 hiperparâmetros da Tab. III.** (a) Tab. III: p_c=1,0; p_m=1/n; η_c=30; η_m=20; N\*=7000; D=3; U=200; λ=1e-5; E_init=20; B=32; T_max=11n+24; γ=0,9; Q_max=300; Tab. II: N=11/15, θ=5. (b) Nada mudado; `NUM_LAYERS=2` = 2 ocultas + logits-3 = profundidade 3 do paper. (c) Eco em `params` nas 25 células E no smoke T11 (`params` com as 19 chaves, `T_max=11D+24`, `indpb==1/D` numérico). **Não são só declarados:** N\*=7000 é confirmado por `n_acordo+n_desacordo` (A5) e Q_max por A8. (e) **(1) · DD+D (iii,iv).**

**A4 — q=1 estrito.** (a) Alg. 1 passos 18-21; §III-E: "only a single offspring solution is picked out". (b) Nativo, sem desvio. (c) **7.022/7.022** gerações com exatamente 1 `real_solution_id` na ③-online; 7.074.000/7.074.000 linhas de sonda com NULL. (e) **(1) · direta (iii).**

**A5 — Pool 7000 e o acordo das redes ⟦ERRATA CONTRA MIM MESMO⟧.** (a) §III-A: "N\* ≫ N, so that good candidate solutions are very likely to be included". (b) A SPEC acrescenta telemetria (e(z) não existe sobre os 7.000 — DI-16.3 A+). (c) `n_acordo+n_desacordo==7000` em **5.029/5.029** decisões com agregados não-nulos. **A F5 escreveu 5.047/5.047 — over-contagem de 18**: as 18 decisões `categorias` de ZDT4 têm os agregados do pool **NULL** porque a θ-Net não existe e o acordo não é computável. Conclusão idêntica (100%), denominador corrigido. Acordo mediano: 94,3% (DTLZ2, MMF16_20) a **38,4% (ZDT6)**. (e) **(1) · direta (iii).**

**A6 — Estágio 1: prioridade e o ramo Q5 ⟦ARMADILHA NOVA MEDIDA⟧.** (a) §III-D-1: "Q1 is selected with the highest priority, followed by Q2 and Q3"; se x\*_j é nulo, cai-se no ramo de p_sum sobre os θ-não-dominados. (b) Sem mudança; instrumentação grava `n_q1/n_q2/n_q3` PRÉ-truncamento. (c) Recomputei a regra e comparei com a categoria gravada: **5.047/5.047 (100%)** no ramo `categorias`; Q1 3.552 (50,6%) · Q2 1.178 (16,8%) · Q3 317 (4,5%) · Q5 1.975 (28,1%); `cat=='None' ⇔ caminho=='distinct'` 25/25 e os 4 agregados do pool NULL em 1.975/1.975. **Armadilha que a primeira passada desta bateria pegou:** no ramo `distinct` o campo `n_q1` **não é NULL** — carrega o tamanho da lista de distintos (chega a 300) e `n_q2=n_q3=0`; testar a prioridade sem condicionar em `caminho` reprova 1.975 gerações falsamente (a minha 1ª medida deu 5.056/7.022 por isso). (e) **(1) · direta (iii).**

**A8 — Corte Q_max=300.** (a) §III-D-1, corte por `p_sum(z)`. (b) Sem mudança. (c) Com o ramo `distinct` tratado corretamente, `n_cands == min(tamanho da categoria, 300)` fecha em **7.022/7.022 (100%)**; 292 cortes efetivos em 21/25 células. A *fórmula* do critério permanece indireta (`scf` não é exportado por candidato), mas `pool_scf_max=2,0` é exatamente `p_θ+p` saturados. (e) **(1) · direta (iii).**

**A9 — O EDN e o argmax (a query-joia).** (a) Eqs. 6-7: `e_sum(z_i)=e(z_i)+e_θ(z_i)`, "the solution with maximal value is selected". (b) `pred_score := e(z)`; TOP-100 gravado. (c) Re-medido: o escolhido é o **argmax do seu bloco** em **6.440/6.440** blocos comparáveis da s42 (\|Δ\| < 1e-4 contra `⑥.e_z_escolhido`) e **41/41** no smoke T11. Os NULL de `e_z_escolhido` são exatamente `n_cands==1` (curto-circuito de `selection.py:148-149`) e os 9 do cap anti-spin. (d) O fork replica `select_best_individual` termo a termo. **Onde a identidade fecha, o uso da incerteza está PROVADO pelos dados.** (e) **(1) · direta (iii,iv).**

**A10 — Simetrização da Eq. 4.** (a) §III-C-1: p̂(u,v)=max{p(u,v),p(v,u)}. (b) Sem mudança. (c) Não há verificação direta (as duas probabilidades direcionais não saem); a evidência é declarativa (`sigma_dict`→`select_best`→`nn_predict_dom`, que inverte as metades, remapeia por `3−id` e toma `where(max_y>max_sy,…)`) + indireta (piso empírico 0,434 ≫ 1/3). (e) **(1) · DD+I (i,iii).**

**A11 — Rodadas de ordem embaralhada.** (a) §III-A: "all clusters are considered in rounds… their order is shuffled for each round". (c) Mesclando cronologicamente os `cid` dos guards `spin_resample` com os das decisões e segmentando em blocos de N: **668/668 rodadas completas são permutações exatas**; 1ª rodada em ordem natural em 25/25. (d) Sem mesclar os spins o teste reprova falsamente (ZDT1 daria 3/54) — cada re-sample consome um cluster que nunca vira decisão. (e) **(1) · direta (iii).**

**A12 — O gate `>=` do código × `>` do paper.** (a) §III-C-2: "If acc_min **>** γ… we just keep it unchanged". (b) `model_update.py` escreve `>=`; ⟦v2.2⟧ fechou a borda (D29 🟠 impl→código). (c) Re-medido com denominador CHEIO: `(min(accs)>=0,9) ≡ skip` em **13.843/13.843** avaliações de gate (P+S) — 100%. O número da F5 (6.821) era o de PARES de redes; 6.821×2 + 201 (ZDT4 só-P) = 13.843 exato. (d) A diferença `>`/`>=` só importa em D pequeno, onde `acc` assume poucos racionais — e ali o comportamento é o do código. (e) **(2) · direta (iii).**

**A13 — `E_upd = ceil((1−acc/γ)·E_init)`.** (a) Eq. 5. (b) O paper não especifica a inteirização; o código faz `ceil(max_adjust*((thr−acc)/thr))`. (c) **8.538/8.538 (100%)** reproduzindo a ORDEM DAS OPERAÇÕES do código; com a forma algebricamente equivalente a taxa cai a 99,4–99,7% em 5 células, todas com `acc=0,72` (IEEE dá 4,000000000000002 por um caminho e 4,0 pelo outro). **Evidência forte de fork bit-fiel.** (e) **(2) · direta (iii).**

**A14 — T_max, o erratum T′ e a monotonicidade.** (a) O paper imprime T′=**max**{|A|,T_max}−1, semanticamente impossível. (b) `get_start_pos` move o INÍCIO ⇒ T′=**min** (⟦v2.2⟧, D29 🟣). (c) `T_max=11D+24` 25/25; **0 quedas** de `fe_treino_max` em 25/25; a identidade `=|A|−1` reprova exatamente as 2 células com cache-hit. (d) A U8 tem de ser testada com o predicado do config (regra 9 do CONTRATO nomeia b1/b4/c217, **não** o c122). (e) **(2) · direta (iii).**

**A15 — Classe ausente: sentinela `acc=1` e o cold start (o caso ZDT4).** (a) §III-E antecipa: "an extreme case is that all solutions… are in the same cluster. In this case, there will be no training example belonging to class 3". (b) O código resolve por sentinela (`acc_for_class` devolve 1 para conjunto vazio ⇒ **infla o skip**) e, se `compute_class_weight` devolve None, **a rede não nasce** ⇒ seleção aleatória. Nada nosso: D29 🟠. (c) ≥1 classe em acc=1,0 entre 7,5% (WFG4-P) e 80,5% (MMF11_L-P); as três simultâneas até 22,9% (DTLZ2-S). **ZDT4: `fit_inicial.s_net=false`, `accs_s=null` e `reinit_s=true` em 201/201**; 183/201 iterações caem em Q5 com `n_cands=1` (infill ALEATÓRIO) e as 18 restantes são Q3 puro com `cid=0`. (d) Causa geométrica: com `f_max=[1,1;1,1]` (A17) e f₂ real ∈[92,3;243,0] no init, o ângulo normalizado fica entre 89,50° e 90,00° — todos os 109 pontos num único cluster ⇒ classe 3 vazia ⇒ θ-Net natimorta. **É a profecia do §III-E realizada.** (e) **(2) · design do código + DEF-B11.1 · direta (iii).**

**A17 — Normalização com f_min/f_max fixos (DEF-B11.1 + D73b).** (a) §III-A: "θ-DEA-DP uses the same method as ParEGO to normalize… at the beginning". (b) O código NUNCA seta limites reais (`init_obj_limits`: f_min←0, f_max←f_min+1), o que quebra PBI fora do cubo unitário; **DEF-B11.1 declarou o patch OBRIGATÓRIO e FIEL**, entregue pela assinatura (repo intocado, D30), com margem de 10% sobre o front VERDADEIRO = vantagem informacional DECLARADA (D73b). (c) `params.f_min/f_max` constantes em 25/25 e ecoados nas 7.022 decisões. (d) Contraste instrutivo: DTLZ1 também estoura o teto, mas com M=3 e objetivos de magnitude comparável a direção normalizada continua espalhada ⇒ **0 ocorrências de Q5 e as duas redes vivas**. O colapso exige a **assimetria** de escala, não a magnitude. (e) **(2) · DD+D (iii).**

**A18 — Integração declarada.** (a) O paper não fala de normalização de variáveis; o Alg. 1 conta TODAS as avaliações. (b) `x_norm=[-1,1]` (🟠 código); **float32 EXIGIDO** pelo forward (`prediction.py:24` faz `.float()`); **`visualization=False`** — no `tdeadp_main.py` oficial ela vem `True` e o visualizador gasta FE reais NÃO contadas. (c) Eco em 25/25 e no smoke. (d) **Armadilha declarativa:** o mesmo manifesto traz `env.pinning.default_dtype='torch.float64'` (default do processo) ao lado de `params.torch_default_dtype=float32` (dtype efetivo). (e) **(2) · DD (iii,iv).**

**A20 — Anti-spin: cap 10 × fn. 6 sem cap.** (a) Nota 6: "we just resample the set of candidate solutions Q" — sem cap. (b) E.7 autorizou cap 10 → fallback pelo melhor `p_sum`, com o guard carregando o texto "DECLARADO — E.7; o paper nao capeia". (c) **890** `spin_resample`, 731 deles em WFG1 (62,4% das chamadas do contador); o cap disparou **9 vezes, todas em WFG1**, sempre com `tentativas=10`; reconciliação `footer.n_spin_total ≡ Σ n_spin ≡ nº de guards` em 25/25. (d) Nessas 9 gerações `n_cands=0` e a ③ **não tem linha** — o TOP-100 é o topo do ranking de e(z), e ali não existe e(z). 9 de 10.856 FE (0,083%). (e) **(2) · E.7 · direta (iii).**

**A21 — O Step 14 do Alg. 1 não existe no código oficial.** (a) Step 14 manda selecionar N elites por ordenação θ-não-dominada ANTES do laço. (b) `scalar_dom_ea_dp` só trunca ao FIM da 1ª iteração; o fork herda (D29 🟠). (c) Evidência quantitativa re-medida: `max e(z)` no bloco-1 vale **369,39 (ZDT1)**, 342,25 (WFG1), 325,25 (DTLZ7) — todos ≫ o teto 2N; **>2N em 22/25 células** (as 3 exceções são MMF1/MMF4/MMF11_L, D=2, onde 11D−1=21≈2N=22), com **63,41%** dos 2.000 pontos do bloco-1 acima de 2N em média; `≤2(11D−1)` em **25/25**; a partir de g≥2, `≤2N` em **25/25**. (e) **(2) · D29 🟠 · direta (iii).**

**A22 — Survival θ-não-dominada.** (a) Step 26 + §III-E ("the survival selection always uses the real fitness" — o surrogate é preseleção pura). (b) `sel_scalar_dea` trunca o front excedente por `random.sample` (D30; SPEC §434 🟠 IMPL). (c) \|②(g)\|=N em **7.022/7.022** grupos; o infill sobrevive 26,4%–71,4% (mediana 50,3%). (e) **(2) · indireta (iii)** — a REGRA de desempate é T2.

**A23 — TOP-100 por e(z) (DI-16.3 A+).** (a) Contrato de dados, sem prescrição de paper. (b) A opção A+ corrigiu a redação original: e(z) é grandeza INTRA-conjunto, definida só sobre a categoria vencedora já truncada; sobre os 7.000 o código computa OUTRA coisa (`scf`), e um e(z) real ali custaria ~4,9e7 pares/iteração. (c) `linhas③-online(g)==min(100,n_cands)` em **7.022/7.022** (s42) e **41/41** (smoke); 361.480 linhas. (e) **(2) · direta (iii,iv).**

**A24 — `pred_confianca`/`pred_score`: duas semânticas; μ/σ NULL.** (a) §II-C p(x)=max_k p_k; §III-C-1 p̂ = max entre direções. (b) Na busca a matriz é INTRA e a diagonal é lixo (`conf[i,i]=1`), excluída pelo fork; na sonda é INTER (2.000×n_ref). O `sigma_dict` declara "mu_*/sigma_* NÃO se aplicam ao c122". (c) Busca: mín global 0,434, medianas 0,870–1,000; **sonda: 1,0 em 99,3% de 7,07 M linhas**; `mu_0/mu_1/sigma_0/sigma_1/pred_classe/espaco_modelo/transf_*` NULL em **100%** das 7.435.480 linhas da s42 ⇒ U6/U11 clássicos N/A e `sonda_f52e.csv` com 0 linhas c122 **por desenho**. (d) Saturação é aritmética (máximo sobre 11–15 valores com piso 1/3), não epistêmica. (e) **(2) · direta (iii).** ⚠ Esta declaração é o que o **N3** viola.

**A26 — Sonda: cadência, finalProbe e join posicional.** (a) Instrumentação (§17.2.2/DI-09/DI-12.5). (c) Cadência `g=1 ∨ g%2==0` + última obrigatória, exata em 25/25; **3.537/3.537** blocos com 2.000 linhas; finalProbe em 2/25 (as duas com cache-hit, `n_ger` ímpar). No smoke T11: 22 blocos (21 de cadência + 1 finalProbe em g=41), `sonda_x_hash` ≡ `x_hash_online` do sidecar, e o join posicional contra o gabarito dá **max\|ΔX\| = 1,19e-7** nos 22 blocos. (e) **(2) · direta (iii,iv).**

**A28 — Cache-hit, timing e reconciliação.** (a) O código oficial faz `evaluations+=1` e `archive.append` incondicionalmente (não tem dedup). (b) D57/D89 + DI-21.1: **no hit o arquivo NÃO cresce** (anexar estouraria o `rel_map`, dimensionado em `maxfe` — `IndexError` MEDIDO). (c) 2/25 células com 1 hit (MMF11_L, ZDT4), com a cadeia inteira fechando (guards, `n_acumulado` congelado, paridade ímpar → finalProbe); `fit+busca ≤ tempo_geracao` em **100% de 25/25**; `tempo_pred_sonda_s>0 ⇔ geração com bloco` 100%; 2 registros de footer por célula (runner+despachante). (e) **(2) · direta (iii).**

**N1 — 🆕 [I-01 + I-1] O `n_ref` REAL e a identidade da referência — o conserto do A27 da F5.** (a) Sem prescrição de paper. (b) A F5 classificou como **(3) 🎯**: o manifesto e os 3.537 recs declaravam `n_ref=N` (tamanho FIXO, a premissa da DI-16.2), mas o bloco-1 media contra a população NÃO truncada (11D−1). E faltava a IDENTIDADE da referência — sem ela o rótulo verdadeiro é irreconstruível. A T11 respondeu com `_meta_referencia` (`c122_thetadeadp.py:608-638`), chamada nos 3 sítios (`:858` cadência, `:1001` decisão, `:1049` finalProbe). (c) **MEDIDO NO SMOKE:** o rec `sonda` de g=1 traz `n_ref=21` (=11D−1), `n_ref_nominal=11`, `n_ref_truncada=false` e `ref_ids=[0..20]`; de g=2 em diante, `n_ref=11`, `n_ref_truncada=true`. **`ref_ids` carrega ids REAIS — 252 ids nos 22 blocos de sonda e 451 nas 41 decisões, ZERO negativos** (contraste com o `pmid_ids=[-1,-1,-1,-1,-1]` do c217 em 42/42: aqui **não há sentinela**). `len(ref_ids)==n_ref` em 22/22 e 41/41. Teste de teto independente: `max e(z)` do bloco-1 do smoke = **25,2798 > 2N=22** e ≤ 2·21=42 — ou seja, **na única célula que a F5 declarou não-discriminante (MMF1), a semente 0 discrimina e confirma a referência 21**. (d) O mecanismo é o A21 + a ordem "sonda ANTES da decisão"; a T11 corrigiu **só o metadado**, sem mexer em QUANDO a sonda dispara (o que quebraria a cadência comum aos 18 configs). (e) **(2) · sancionado por I-01/I-1 · direta (iv). O achado (3) principal da F5 está fechado no nível do rec.**

**N2 — 🆕 [I-2 + I-5] A `REGRA_DO_ROTULO` existe, é usável — e aponta para o evento ERRADO.** (a) Instrumentação. (b) O I-2 nasceu de um número duro do b4: três definições plausíveis de "rótulo verdadeiro" sobre as MESMAS 280 gerações dão acurácia 0,350 / 0,999 / 0,995 — "acurácia do classificador" não é um número enquanto a regra não estiver escrita. (c) **MEDIDO:** `sigma_dict.REGRA_DO_ROTULO` está no manifesto do smoke e é operacional — **eu a executei** (`regra_do_rotulo.py`): reconstruí o rótulo verdadeiro dos 2.000 pontos de cada bloco (f do artefato → normalização DEF-B11.1 → `pareto_dominance` + PBI/θ=5 com os 11 vetores Das-Dennis → agregação idêntica ao e(z)) e comparei com o `pred_score` gravado. Funciona: correlação e AUC bem definidas em 22/22 blocos. `y_treino_dist_p/_s` (I-5) também estão presentes em 41/41 decisões, com contagem real (g=1: 462 pares computados de 484, classes 272/95/95 na Pareto-Net e 410/26/26 na θ-Net). **MAS** a regra diz "com r na população de referência (`ref_ids` do **evento de geração**)", e o evento de geração do c122 é o `decision` — que carrega o `ref_ids` **PÓS-survival** (`:1001`, emitido depois de `pop=sel_scalar_dea(...)` na `:956`), enquanto a sonda mediu contra a população **PRÉ-survival** (`:848`). Medido: `sonda(g).ref_ids == decision(g).ref_ids` em **8/22**; `== decision(g−1).ref_ids` em **21/22**. **A leitura literal da regra devolve o conjunto errado em 14/22 blocos (63,6%)** — trocando 12 dos 21 membros em g=1 e 1–2 dos 11 nos demais. (d) A regra foi escrita para o molde do b4 e não acompanhou o fato de que, no c122, o rec `sonda` carrega o seu PRÓPRIO `ref_ids` (correto) ao lado de um `ref_ids` homônimo e diferente no rec vizinho. (e) **(3) 🎯 · não-sancionado (nenhuma DI autoriza a ambiguidade) · direta (iv).** Severidade **BAIXA**: o dado correto está no próprio rec e o impacto numérico é pequeno (na s42, ρ com ②(g−1) = 0,0589 vs ②(g) = 0,0575 em 3.512 blocos). Conserto: 6 palavras — "…`ref_ids` **do próprio rec `sonda`**…".

**N3 — 🆕 [I-6] A sonda estratificada funciona… e grava no lugar errado (o padrão do c217, com outra roupa).** (a) Instrumentação. (b) A régua Sobol responde "o modelo é bom GLOBALMENTE?", não "ele acerta ONDE a decisão acontece?"; o I-6 amostra ~500 pontos perto do arquivo (σ_rel=0,05), com `uso_id=91` próprio e sob `preserve_all_rng`. (c) **MEDIDO no smoke:** 21 blocos × 500 = **10.500 linhas** com `regime='sonda_estratificada'`, `n_arquivo` crescendo 21→61, `prevalencia_nd_no_bloco` 6,4%–9,4%. **O bloco entrega o que promete:** exercendo a REGRA_DO_ROTULO, o AUC do e(z) contra "z domina ≥1 referência" é **0,6274 médio (mediana 0,6089)** no bloco estratificado contra **0,5751 (mediana 0,5464)** na régua, ρ 0,206 vs 0,117, e o estratificado é melhor em **17/21** gerações pareadas (ΔAUC médio **+0,0516**). **MAS** — `standalone_harness.py:950-956` monta a linha com `mu=`/`sigma=` **ignorando o `pred_tipo`**, ao contrário da irmã `emit_sonda_block` (`:846-854`), cuja própria docstring (`:808-813`) manda: *"as linhas vão para `pred_classe`/`pred_score` + `pred_confianca`, **NUNCA para `mu_*`/`sigma_*`** (que significam outra coisa e envenenariam a leitura da ③ pela R4)"*. Resultado medido na ③ do smoke: **`mu_0` preenchido em 10.500/10.500 e `pred_score` NULL em 10.500/10.500**, com `pred_tipo='score'` na mesma linha e o `sigma_dict` do MESMO manifesto declarando "mu_*/sigma_* NÃO se aplicam ao c122". A confiança foi para `sigma_0`. **Assimetria provada contra os outros classificadores** (mesma campanha, mesmos smokes): c217 → `pred_score` 11.000/11.000, `mu_0` 0; b4 → `pred_classe` 11.500/11.500, `mu_0` 0. **O c122 é o único dos quatro fora do contrato** — e é o único Python, porque no MATLAB o `SondaState.m:298` delega a montagem da linha ao `predict_fn` do config. (d) Por que passou: o guard do writer (`export.py:468-478`) só falha se `len(mu) > M`; aqui `len=1 < M=2`, o caso CURTO legítimo do b1 mono-output — a única guarda que pegaria o erro é exatamente a que legitima a exceção do b1. E os 5 testes do bloco (`tests/test_a2_c122.py:220-260`) são **todos `inspect.getsource` + `assertIn`** — checam o TEXTO da função, nenhum checa a LINHA emitida: o "gate decorativo" que o próprio §4.2 do handoff T11 diz ter aprendido a evitar. (e) **(3) 🎯 · não-sancionado (viola o CONTRATO §3 "Score par-a-par (c217, c122): `pred_score`" e o `sigma_dict` do run) · direta (iv).** Severidade **MÉDIA**: nenhuma conclusão atual depende disso (o bloco é novo e inédito), e o dado é 100% recuperável por regra de leitura — mas em escala de campanha são ~3.535 blocos × 500 = **1,77 M linhas/semente ≈ 53 M linhas em 30 sementes** na coluna errada, e qualquer query transversal por `mu_0.notna()` passa a colher e(z) do c122 como se fosse μ. **Conserto: uma ramificação de 4 linhas em `emit_sonda_estratificada`, idêntica à da irmã.**

**N4 — 🆕 O resíduo declarativo do I-01.** (a) Instrumentação. (b) O I-01 corrigiu os recs, não as declarações agregadas. (c) **MEDIDO no manifesto do smoke T11:** `sonda.n_ref = 11` (`c122_thetadeadp.py:1087`) e `sigma_dict["regime=sonda"] = "…e(z) vs a POPULACAO SELECIONADA corrente (n_ref=11) — **referencia de tamanho FIXO**…"` (`:741-743`) — no MESMO run cujo rec de g=1 diz `n_ref=21, n_ref_truncada=false`. (d) A regra 3 do CONTRATO manda ler o `sigma_dict` ANTES de tocar a ③ (DEF-C4): o leitor que cumpre o contrato lê primeiro a afirmação falsa. (e) **(3) 🎯 · direta (iv).** Severidade **MUITO BAIXA** — é o mesmo defeito do A27 da F5 encolhido de **3.537 recs/25 células** para **2 strings por célula**, com a verdade adjacente em cada bloco. Conserto: `"n_ref": MU` → `{"n_ref_nominal": MU, "n_ref_real": "por bloco (ver rec)"}`.

**N5 — 🆕 Encanamento global da T11 no c122.** (c) **I-3 `tempo_aval_real_s`**: não-nulo em **25/25** da s42 (Σ 3,5604 s de 148.721,9 s = 0,0024% — o perfil clássico de SAEA com problema-alvo barato) e 0,0021 s no smoke. **I-4 `NO_RETRY`** (`experiments.py:84-88`): os 3 padrões são `least squares problem is underdetermined` (b1), `random_search_optimizer falhou nas 3 tentativas` (c154) e `ModelFittingError` (BoTorch) — **nenhum é modo de falha do c122**, que fez `n_retries=0` em 25/25 e `status=ok` em 25/25. Inerte aqui, e corretamente inerte. **G3 proveniência**: `campanha_id`/`repo_hash`/`schema_version=2` presentes no smoke e **ausentes em 25/25 da s42** — confirmando, para o c122 especificamente, a medida da torre de que a s42 é PRÉ-T11. **§3.1 não-perturbação**: verdicto registrado (`be06b54124e9c100`), **não re-mensurável aqui** — o par COM/SEM do c122 é Python (via `naoperturbacao.py`) e não está em `g6_com/g6_sem`, que só contém os 9 MATLAB. (e) **(2) · (i,iii,iv,ii).**

**T1/T2/T3 — inalterados.** Perda CE ponderada por frequência inversa (Eq. 3), Kaiming e ausência de dropout — nada exportado além de `weight_decay=1e-5`. O `random.sample` do front excedente (D30 / SPEC §434 🟠) — só \|P\|=N é observável. O desempate do Pareto-representante por ‖w_i−w_j‖ (§III-B) — representantes não exportados. **A T11 não mexeu em nenhum dos três.**

---

## 3. % por classe

**Denominador = total − T = 31 − 3 = 28.**

| classe | n | % (sobre 28) | itens |
|---|---|---|---|
| **(1) conforme o artigo** | **8** | **28,6%** | A3, A4, A5, A6, A8, **A9 (query-joia)**, A10, A11 |
| **(2) desvio sancionado** | **17** | **60,7%** | A1, A2, A12, A13, A14, A15, A17, A18, A20, A21, A22, A23, A24, A26, A28 + **N1** (I-01/I-1) + **N5** (I-3/I-4/G3) |
| **(3) inexplicado 🎯** | **3** | **10,7%** | **N2** regra aponta o evento errado · **N3** estratificada em `mu_0` · **N4** resíduo declarativo do `n_ref` |
| **T (fora do denominador)** | 3 | — | T1 perda/Kaiming/dropout · T2 `random.sample` · T3 desempate do Pareto-rep |

**Comparação com a F5** (28 aspectos, mesmo denominador): (1) 32,1%→28,6% · (2) 64,3%→60,7% · (3) **3,6%→10,7%**. **O aumento de (3) NÃO é degradação de mecanismo — é ampliação de superfície:** os 3 itens de hoje vivem inteiramente na instrumentação NOVA (2) ou no resíduo do antigo (1); **zero** tocam o laço de busca. O A27 da F5 (3.537/3.537 recs com rótulo falso) virou N4 (2 strings por célula).

---

## 4. 🆕 AS CORREÇÕES DA T11 — implementada? funciona de fato? o que falta?

| item | implementada? | **funciona de fato?** (não "o campo existe") | o que ainda falta | evidência |
|---|---|---|---|---|
| **I-01 `n_ref` REAL** | ✅ `c122_thetadeadp.py:635`, 3 sítios | ✅ **SIM.** g=1→21 (=11D−1), g≥2→11 (=MU); `n_ref_truncada` coerente 22/22. Teto independente confirma: `max e(z)` g=1 = 25,28 > 2N=22 | as 2 declarações agregadas (**N4**) | (iv) smoke, `t11_b.py` |
| **I-1 `ref_ids`** | ✅ `:638` | ✅ **SIM — dado REAL, não sentinela.** 252 ids na sonda + 451 nas decisões, **0 negativos**, `len(ref_ids)==n_ref` em 63/63 recs. **≠ c217** (`pmid_ids=[-1×5]` em 42/42) | robustez: `_meta_referencia` só anexa ids que resolvem — um `sid=None` encolheria a lista **em silêncio**. Conferir sempre `len(ref_ids)==n_ref` | (iv) smoke |
| **I-2 `REGRA_DO_ROTULO`** | ✅ `:751-763` | ⚠ **PARCIAL.** Executei-a fim-a-fim (é operacional), mas ela **aponta o evento errado**: 8/22 vs 21/22 de casamento | 6 palavras de conserto (**N2**) | (iv) `regra_do_rotulo.py`, `offbyone.py` |
| **I-5 `y_treino_dist`** | ✅ `:577-605` | ✅ **SIM.** 41/41 decisões com contagem real (462 de 484 pares; 272/95/95 P e 410/26/26 θ em g=1 → 1.852/784/784 em g=41). Separa "classificador ruim" de "problema desbalanceado" | — | (iv) smoke |
| **I-6 `sonda_estratificada`** | ✅ `:875-887` | ⚠ **O BLOCO SIM, A COLUNA NÃO.** Ganho real e medido (AUC 0,627 vs 0,575; melhor em 17/21); **mas `mu_0` 10.500/10.500 e `pred_score` 0/10.500** — único dos 4 classificadores fora do contrato | ramificação de 4 linhas em `standalone_harness.py:950` (**N3**) | (iv) `t11_c.py`, `t11_d.py` |
| **I-3 `tempo_aval_real_s`** | ✅ | ✅ não-nulo 25/25 (s42) + smoke | — | (iii,iv) |
| **I-4 `NO_RETRY`** | ✅ `experiments.py:84` | ✅ 3 padrões determinísticos — **nenhum é do c122** (inerte, corretamente) | — | (i) código |
| **G3 proveniência** | ✅ | ✅ `campanha_id`/`repo_hash`/`schema 2` no smoke; ausentes em 25/25 da s42 | — | (iii,iv) |
| **§3.1 não-perturbação** | ✅ | ⚪ **registrado, não re-medido** (`be06b54124e9c100`) — o par do c122 é Python e não está em `g6_com/g6_sem` | preservar o par Python | (ii) veredito |

### 4.1 · Erratas MEDIDAS contra os números da T11 (a instrução mandou desconfiar — desconfiei)

| # | a T11 diz | eu MEDI | onde |
|---|---|---|---|
| **E-a** | "Os 11 smokes PYTHON **não estão preservados**" (LEIA-ME + §15.2 + briefing) | **ESTÃO** — `evidencia_T11/smoke_python/` com c122/MMF1/s0 completo (6 arquivos, 650 KB de ③) | `ls`, manifest |
| **E-b** | "prevalência 7,4% no c122 **contra 0,4% da régua — 18× mais positivos**" (CONTRATO §10 regra 12; `test_a2_c122.py:180-188`) | **NÃO REPRODUZ.** ND-no-bloco da régua Sobol de MMF1: **5,65%** em n=2.000 (a fatia online REAL), 2,81% em n=20.000, **9,60% em n=500**. Ou seja: **a n IGUALADO (500), a régua tem MAIS ND que o bloco estratificado** (9,60% vs 6,4–9,4%). O "7,4%" é o **primeiro** bloco (média dos 21 = 7,86%) | `regra_do_rotulo.py` |
| **E-c** | (implícito em E-b) o bloco estratificado se justifica pela **prevalência** | A justificativa que SOBREVIVE é outra: a **prevalência de positivos reais é igual** (0,310 vs 0,300), mas o **poder discriminante é maior** — AUC 0,6274 vs 0,5751, melhor em **17/21** blocos pareados. **A feature é boa; o número que a justifica no documento está errado** | idem |
| **E-d** | (minha própria F5) "pool: 5.047/5.047" | **5.029/5.029** — as 18 decisões `categorias` de ZDT4 têm agregados NULL (θ-Net inexistente) | `remedida_s42c.py` |
| **E-e** | (minha própria F5) "gate: 6.821/6.821" | **13.843/13.843** avaliações P+S (6.821 pares × 2 + 201 só-P de ZDT4). Mesma conclusão, denominador dobrado | `remedida_s42.py` |

---

## 5. Comparação canônica (Entrega 2) — **inalterada** (mesmo corpus s42)

**Setup do paper** (§IV-A, Tab. I-II): ZDT1–ZDT4 (m=2,n=10); DTLZ1 **modificado** (2π, m=2, n=6); DTLZ2/4/7 (m=3,n=8); WFG6/7 (m=3,n=10). Métrica **IGD** (não IGD+), 21 execuções, mediana, MaxEval **250**.

| grupo | veredito | fundamento |
|---|---|---|
| **ZDT4 (D=10, M=2, 309 FE)** | ✅ **COMPARÁVEL** | mesma n=10, mesmo m=2, orçamento 1,24× — a única interseção estrita |
| ZDT1, ZDT3 (D=30) | **incomensurável (eixo X)** | dimensão 3× **e** orçamento 3,7× |
| DTLZ2/DTLZ4 (D=12), DTLZ7 (D=22) | **incomensurável — âncora direcional** | n 1,5×–2,75×, orçamento 1,5×–2,7×; mesma família e M |
| **DTLZ1** | **incomensurável — variante DIFERENTE** | paper 2π/m=2/n=6 vs nossa canônica 20π/M=3/D=7: nem o sentido compara |
| ZDT6, DTLZ3, WFG1/2/4/5/9, MMF×4, BBOB×7 | sem referência numérica | ausentes do paper (ou só no suplementar sem números) |

**ZDT4 (a única quantitativa).** Paper: melhor 1,05e+1 · mediana 2,80e+1 · pior 3,68e+1. Nosso: **1,935e+1** — **dentro do envelope do paper e 0,69× a mediana dele**, com 1,24× de orçamento e n=1 semente ⇒ veredito honesto = "compatível com a faixa", nunca "melhor". **Âncora J confirmada**: o paper admite que "on ZDT4… it indeed fails to approach the PF"; nosso run reproduz a falha na mesma ordem **e com o mecanismo explicado** (A15: θ-Net natimorta, 91,0% dos infills aleatórios).
**Sentido: 6/6 posições intra-família reproduzidas** — ZDT1<ZDT3≪ZDT4 (paper 1,61e-2 / 6,05e-2 / 2,80e+1; nosso 2,00e-2 / 5,31e-2 / 1,93e+1) e DTLZ7<DTLZ2<DTLZ4 (9,43e-2/1,23e-1/2,12e-1 vs 7,35e-2/1,749e-1/1,812e-1). *(Método: a comparação de sentido usa a métrica DELE — na nossa IGD+ primária DTLZ2 e DTLZ4 trocam de lugar.)*

---

## 6. Saúde (s42 = corpus principal)

| dimensão | resultado (25 células) |
|---|---|
| **vs pisos (IGD+)** | **bate o melhor piso em 20/25 (80%)**; rank médio **4,72 de 17**. As 5 que não batem: DTLZ1 1,206× · DTLZ3 1,445× · WFG2 1,472× · WFG4 1,549× — **todas dentro do piso de ruído O-18 (≤1,5898×)**; só **WFG1 (1,926×)** fica claramente fora |
| **trajetórias** | IGD+ monotônico não-crescente — **0 violações em 475 transições**; ganho ponta-a-ponta mediano **12,08×** |
| **régua da família (Sobol, s42)** | ρ(e(z), dominância real) mediano por célula **+0,135**, positivo em 22/25; ≥0,50 em 6 células (ZDT1 +0,728, BBOB_F37 +0,763). **`e(z)→0` não é morte do surrogate**: em ZDT1 97,0% dos blocos não têm NENHUM ponto do gabarito dominando NENHUM membro **na verdade** |
| **bloco estratificado (SEPARADO — regra 12)** | **só existe no smoke.** 21 blocos × 500, prevalência ND 6,4–9,4%; AUC 0,6274 vs 0,5751 da régua; **não entra em nenhuma estatística da régua e não é comparável entre configs** |
| **contrato** | `integridade_f52a` e `contrato_f52b` com **0 linhas c122** na s42; `sonda_f52e` 0 linhas **por desenho** (A24). ⚠ **NOVO:** com o N3, a ③ do código de HOJE deixa de ser 100% conforme |
| **custo/máquina** | 41,31 h-core, **máquina v6 em 25/25** (roster homogêneo); `teto_wall` nunca disparou; 91,4% do tempo em fit |

---

## 7. SCORE e VEREDITO

### **SCORE: 9,5 / 10 — ACEITAR + CAVEAT** · **VEREDITO: MANTEVE**

**A causa nomeada do "manteve" é uma soma de duas parcelas que se cancelam:**

- **+ (correção REAL, não cosmética):** o **único (3) da F5** — a referência do bloco-1 contradizendo o `n_ref` declarado em 3.537/3.537 recs — está **fechado no nível do rec** (N1), com `ref_ids` de **dado real** (o teste que o c217 reprovou), e a instrumentação nova **habilita uma medição que a F5 não podia fazer**: eu executei a `REGRA_DO_ROTULO` e produzi AUC/ρ do classificador contra a verdade, bloco a bloco. Isso vale, sozinho, +0,3.
- **− (defeito NOVO de contrato):** o **N3** tira do c122 exatamente o pilar que sustentava a nota alta na F5 — "contrato 100% conforme, zero defeitos". A ③ de hoje grava, num regime inteiro, `e(z)` em `mu_0` e `pred_score` NULL, contra o `sigma_dict` do próprio run e contra o CONTRATO §3. Vale −0,3.
- **= mecanismo INALTERADO e re-medido em escala maior que a F5:** 8 aspectos **(1)** incluindo a query-joia; identidades fechando **7.022/7.022** (TOP-100, Q_max), **6.440/6.440** (argmax), **5.047/5.047** (prioridade), **13.843/13.843** (gate), **8.538/8.538** (E_upd), **668/668** (rodadas). **Nada no laço de busca mudou entre a F5 e hoje** — e nada nas correções da T11 podia mudar (são read-only, sob `preserve_all_rng`, com a não-perturbação registrada em 19/19 configs).

**A recomendação vem com uma condição barata e com prazo:** *aplicar, ANTES do disparo do M8, a ramificação de 4 linhas em `emit_sonda_estratificada` (`standalone_harness.py:950`) que já existe na função irmã*. Sem ela, a campanha grava ~**53 M linhas em 30 sementes** na coluna errada — recuperáveis por regra de leitura, mas por que pagar isso quando o conserto é gratuito hoje? **Com esse conserto + as 6 palavras do N2 + o `n_ref` do N4, o c122 vai a 9,8–10.**

---

## 8. Aspectos classe (3) 🎯 — evidência completa

### N3 — `sonda_estratificada` grava `e(z)` em `mu_0`/`sigma_0` (o padrão do c217, com outra roupa)
- **Afirmação.** As 10.500 linhas `regime='sonda_estratificada'` do smoke têm `mu_0` **preenchido em 10.500/10.500** e `pred_score` **NULL em 10.500/10.500**, com `pred_tipo='score'` na mesma linha.
- **Números.** `mu_0`: média 1,864 · mediana 1,116 · máx 24,777 (= e(z), teto 2·n_ref) · `sigma_0`: média 0,979, mín 0,721 (= max-softmax). `mu_1`/`sigma_1` NULL (o caso "curto" do b1). 21 blocos, 500 linhas cada, gerações 1,2,4,…,40.
- **Contradições diretas.** (i) `sigma_dict` do mesmo manifesto: *"pred_tipo: score (par-a-par) — **mu_\*/sigma_\* NÃO se aplicam ao c122**"*; (ii) CONTRATO §3 (linha 123): *"Score par-a-par (c217, c122): `pred_score` = a nota agregada… e(z) no c122"*; (iii) a docstring da função irmã (`standalone_harness.py:808-813`): *"…NUNCA para `mu_*`/`sigma_*` (que significam outra coisa e **envenenariam a leitura da ③ pela R4**)"*.
- **Assimetria provada (mesma campanha, mesmos smokes):** c217 `pred_score` 11.000/11.000 · b4 `pred_classe` 11.500/11.500 · **c122 `mu_0` 10.500/10.500**. O c122 é o único Python entre os 4 classificadores — no MATLAB o `SondaState.m:298` delega a linha ao `predict_fn` do config, no Python o helper a monta.
- **Causa-raiz, arquivo:linha.** `standalone_harness.py:949-956` chama `surrogate_row(..., mu=mu[i], sigma=sigma[i], pred_tipo=pred_tipo, ...)` **sem ramificar por `pred_tipo`**, enquanto `emit_sonda_block:846-854` ramifica. O guard `len(mu)>M` do writer (`export.py:468-478`) não pega porque `len=1<M=2` é o caso legítimo do b1.
- **Por que passou nos testes.** Os 5 testes do bloco (`tests/test_a2_c122.py:220-260`) usam `inspect.getsource(...)` + `assertIn` — aferem o TEXTO da função, nunca a LINHA emitida. É a definição de "gate decorativo" do §4.2 do próprio handoff T11.
- **Impacto.** Nenhuma conclusão atual (o regime é inédito). Em campanha: ~1,77 M linhas/semente ≈ 53 M em 30 sementes; toda query transversal por `mu_0.notna()` colhe e(z) do c122 como se fosse μ. 100% recuperável por regra de leitura. **Conserto: 4 linhas.**

### N2 — a `REGRA_DO_ROTULO` aponta o `ref_ids` do evento errado
- **Afirmação.** A regra manda usar "`ref_ids` do **evento de geração**"; o evento de geração do c122 é o `decision`, cujo `ref_ids` é **pós-survival**, enquanto o bloco de sonda mediu contra a população **pré-survival**.
- **Números (22 blocos do smoke).** `sonda(g).ref_ids == decision(g).ref_ids` → **8/22**; `== decision(g−1).ref_ids` → **21/22** (g=1 não tem `decision(0)`). **14/22 blocos (63,6%)** receberiam outro conjunto pela leitura literal: em g=1 trocam **12 dos 21** membros, nos demais **1–2 dos 11** (9–18%).
- **Causa, arquivo:linha.** `c122_thetadeadp.py:848` (sonda no TOPO do laço, `pop` pré-`sel_scalar_dea`) × `:956` (survival) × `:1001` (`ref_ids` da decisão). Confirmação cruzada com a ②: `sonda(g).ref_ids == ②(g−1)` em 21/22.
- **Impacto numérico.** Pequeno: na s42 a mesma escolha muda ρ de 0,0589 (correto) para 0,0575 (literal) sobre 3.512 blocos. **Conserto: 6 palavras** — apontar o `ref_ids` do próprio rec `sonda`/`sonda_estratificada`.

### N4 — resíduo declarativo do I-01
- **Afirmação.** `manifest.sonda.n_ref = 11` e `sigma_dict["regime=sonda"] = "…(n_ref=11) — referência de tamanho **FIXO**…"` no MESMO run cujo rec de g=1 declara `n_ref=21, n_ref_truncada=false`.
- **Números.** 2 strings por célula (contra as 3.537/3.537 recs falsas da F5 = redução de **~1.769×** no alcance). Sítios: `c122_thetadeadp.py:741-743` e `:1087`.
- **Por que ainda importa.** A regra 3 do CONTRATO (DEF-C4) manda ler o `sigma_dict` **antes** da ③: quem cumpre o contrato lê primeiro a afirmação falsa.

---

## 9. Teto (T), armadilhas e o que estes corpora NÃO permitem verificar

**Teto T (3, inalterados):** T1 perda CE ponderada por frequência inversa + Kaiming + ausência de dropout (só `weight_decay` é exportado) · T2 o `random.sample` que trunca o front excedente (D30 / SPEC §434) · T3 o desempate do Pareto-representante por ‖w_i−w_j‖. *Itens que NÃO ficaram no teto e poderiam ter ficado:* Q_max (provado pelo corte), N\*=7000 (por `n_acordo+n_desacordo`), rodadas embaralhadas (668/668), erratum T′=min (monotonicidade), gate `>=` (13.843/13.843), e agora **a identidade da referência da sonda** (`ref_ids`, saiu do teto pela T11).

**Armadilhas confirmadas (todas medidas; as 5 primeiras são NOVAS desta rodada).**
1. 🆕 **`sonda_estratificada` do c122 está em `mu_0`/`sigma_0`, não em `pred_score`/`pred_confianca`** (N3). Query por `pred_score.notna()` perde o bloco inteiro; query por `mu_0.notna()` colhe e(z) achando que é μ.
2. 🆕 **`ref_ids` é MULTISET, não conjunto** — medido: 1/22 blocos (g=4) tem 11 entradas e **10 distintas** (a população contém a MESMA solução duas vezes). `set(ref_ids)` perde uma referência e viesa o e(z) verdadeiro.
3. 🆕 **Há DOIS `ref_ids` por geração e eles diferem** (N2): o do rec `sonda` (pré-survival, correto para a sonda) e o do `decision` (pós-survival).
4. 🆕 **`_meta_referencia` só anexa ids que resolvem** — um `solution_id_of` que devolvesse None encolheria a lista **em silêncio**. Conferir sempre `len(ref_ids)==n_ref` (63/63 no smoke).
5. 🆕 **Não há bloco estratificado no finalProbe** — 22 blocos `sonda` × **21** `sonda_estratificada` no smoke (o probe de `:1044` não chama o irmão). Toda análise pareada perde a última geração.
6. **`n_q1` NÃO é NULL no ramo `distinct`** — carrega o tamanho da lista de distintos (até 300). Testar a prioridade sem condicionar em `caminho` reprova 1.975 gerações falsamente.
7. A ③ tem **três** regimes com semânticas incompatíveis de score (`online` intra-categoria ≤2·n_cands · `sonda` ≤2·n_ref · `sonda_estratificada` em `mu_0`). A coluna `regime` é obrigatória em qualquer filtro; **regra 12: nunca misturar `sonda` com `sonda_estratificada`**.
8. **O `cid` da decisão não é a sequência do contador** — cada `spin_resample` consome um cluster; e nas gerações de `spin_cap` o `cid` REPETE o do último spin.
9. **`e_z_escolhido = NULL` não é falha** (573 com `n_cands=1` + 9 com `n_cands=0`); em ZDT4 são 91,0% das iterações.
10. **Nem todo FE tem linha na ③** — 9 infills de WFG1 (cap anti-spin, fallback por `p_sum`), por desenho da DI-16.3 A+.
11. **`fe_treino_max` é MONOTÔNICO no c122** — a regra 9 do CONTRATO nomeia b1/b4/c217, não este; e a identidade `=|A|−1` reprova as 2 células com cache-hit.
12. **`acc=1,0` pode significar "classe ausente"**, não "acurácia perfeita" — só INFLA o skip (até 22,9% das decisões com as três em 1,0).
13. **Modelo `None` é estado normal** — em ZDT4 a θ-Net nunca existiu em 201 iterações, com 201 re-inits falhos (previsto no §III-E do paper).
14. **`pinning.default_dtype='torch.float64'` contradiz `params.torch_default_dtype=float32`** no mesmo manifesto.
15. **`pred_confianca` da sonda satura em 1,0 (99,3% de 7,07 M linhas)** e não é régua de incerteza utilizável; a da busca (diagonal excluída) é.

**O que estes corpora NÃO permitem verificar.**
- **A s42 não tem NADA da T11** — medido no c122: `ref_ids`, `REGRA_DO_ROTULO`, `sonda_estratificada`, `y_treino_dist`, `campanha_id` ausentes em **25/25**; `n_ref` só como string `n_ref={N}` em **3.537/3.537** recs. **Toda a §4 depende do smoke.**
- **O smoke é UMA célula: MMF1, D=2, M=2, semente 0, 41 gerações, 61 FE.** Não diz nada sobre o comportamento da instrumentação nova em **M=3** (clusters 15, onde a sentinela `acc=1` satura), em **D=22/30** (onde `ref_ids` cresce e `solution_id_of` é mais exercido), nem sobre a interação com **cache-hit em ZDT4** ou com o **cap anti-spin de WFG1**. Em particular, a errata **E-b** (prevalência) foi medida só em MMF1 e é fortemente dependente de problema e de n.
- **A semente é 0, não 42** — a busca do smoke não é a da s42; só a instrumentação é comparável.
- **A não-perturbação do c122 não é re-mensurável aqui** (par Python não preservado; `g6_com/g6_sem` só tem os 9 MATLAB). Veredito (ii), não (iv).
- **Continua fora de alcance:** perda/inicialização/dropout (T1), quem sobrevive ao `random.sample` (T2), o desempate do Pareto-rep (T3), e o *valor* numérico das predições internas das redes (só o agregado e(z) sai).

---

*Insumos pré-computados usados sem recomputo: `f5/metricas_finais_f52c.csv` · `f5/trajetorias/` · `f5/contrato_f52b.csv` · `f5/integridade_f52a.csv` · `f5/tempo_f52d.csv` · `f5/sonda_f52e.csv` (0 linhas c122, correto por desenho) · `f5/baterias/c122/c122_saude_escala.csv` e `c122_sonda_verdade.csv` (minhas medidas da F5).*
***Evidência NOVA desta rodada, em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/c122/`:*** `remedida_s42.py` → `remedida_s42.csv` (re-medida independente das 25 células: orçamento, gate, E_upd, pool, campos T11, teto do bloco-1) · `remedida_s42b.py`/`remedida_s42c.py` → `remedida_s42_identidades.csv` (prioridade, Q_max, TOP-100, argmax) · `t11_smoke_c122.py`, `t11_b.py`, `t11_c.py` (manifesto, recs e colunas da ③ do smoke) · `t11_d.py` (assimetria c122×c217×b4 no bloco estratificado) · `regra_do_rotulo.py` → `regra_do_rotulo.csv` (a REGRA_DO_ROTULO EXECUTADA: 22 blocos de régua + 21 estratificados, ρ/AUC/prevalência contra a verdade) · `offbyone.py` (o off-by-one do N2) · `residuais.py` (declarações nominais remanescentes, timing, n_retries). **Nenhum arquivo fora desta pasta foi criado ou modificado; nenhuma célula foi executada; `experiments.py` não foi invocado.**