## b5m-A25 — VEREDITO: CONFIRMADO (d) — DEFEITO DE INSTRUMENTAÇÃO/DOC (com a severidade REBAIXADA: metade do achado é falso-positivo e a consequência declarada é falsa)

**Resumo em 3 linhas**
O fato bruto sobrevive a todos os ataques: `p_wrong_stats` está ausente em **30.165/30.165** eventos e o molde b5 é o **único dos 21 configs** que loga **0/N** dos seus campos DI-10 específicos — reproduzi a medição do analista número por número, nas 45/45 células.
Mas as duas conclusões que ele pendura no fato caem: (i) a metade "PESOS de decomposição no HEADER" tem **custo analítico zero** — reconstruí os 50/105 vetores do zero, só de M, e com eles reproduzi a própria A10 dele (**356.137 grupos, 99,8470%, 545 exceções**) sem ler uma linha do log; (ii) a "consequência provada" (2 dos 4 itens do teto T) é **falsa** — o campo contratado (min/med/máx por geração) não verificaria nem a identidade da Eq. 8 (A12) nem S=1000/truncnorm (A13).
E a "refutação da rejeição do DI-10" dele é o erro mais grave do achado: o que ele reconstruiu é o **turnover líquido de slots**, não a contagem de substituições — **80,20%** dos offspring gerados pelo motor são invisíveis na ③. O enquadramento correto é (d): mecanismo íntegro, log e §6.1 é que enganam.

---

**O que tentei para derrubar (os 4 passos, com os números)**

**1. Por DECISÃO — falhou em derrubar, mas achou uma contradição no CONTRATO.**
Varri DI-01..DI-40, CONTRATO §6.1, SPEC §S.7.1, `00_fundacao/03_contrato_export.md`, os handoffs R3-b5 e o `sigma_dict`. **Nenhuma decisão sanciona a omissão.** O que existe:
- `CONTRATO_DE_DADOS.md:308` (idêntico em `SPEC_experimentos_v5.2.md:2892` e `03_contrato_export.md:341`) é vinculante e explícito para a linha `b5`.
- **DI-30/B3 (ratificada)** só sanciona a *linha de logging* do `moead_media` ("mantém 'b5', ⑥ estruturalmente idêntico ao do b5m") — não o conteúdo da linha b5. Circular: aponta o piso para uma linha que ninguém cumpriu.
- **A contradição:** a mesma §6.1, 4 linhas abaixo, rejeita *"contagem de substituições por geração do MOEA/D"* como inacessível sem patch invasivo. `probabilities` (exigido) e `selection` (rejeitado) nascem em **linhas adjacentes da mesma função** — `ProbMOEAD_select.py:82-89`. As duas entradas não podem ser ambas certas.
- Origem provável da contradição: **DI-16.20** registra que o mapeamento DI-10 do b5 foi feito *"já que o repo do b5 não está montado"* — a linha foi escrita sem o código na mão.
- CLAUDE.md §0 item 2 confirma que a leitura do CONTRATO era obrigatória (e diz textualmente que os bundles do R3 têm "zero menções" a DI-10) ⇒ a sessão R3-b5 tinha a obrigação e o material; o `handoff/R3-b5_REPASSE-A-TORRE.md:63` declara *"⑥ tem 1 evento `b5_gen` por geração (DI-10)"*, que **lê como conformidade** e é o que enganou a torre.

**2. Por QUERY — falhou; a evidência dele é impecável.** Re-executei com código independente, 45/45 células (não amostra), busca por padrão **amplo** (`p_?wrong|probab|peso|weight|vetor|lattice|neighbor|decompos|substitu|replace|selection`) sobre header, `sigma_dict` achatado e todos os `rec`, para pegar renomeações: `b5m` **30.165 eventos gen, 22 chaves de header, chaves de decision invariantes** (`caminho;f_best;fe;geracao;motivo;n_ds_membros;n_front1;rec;tempo_fit_s;ts`), **0 hits** fora de 2 casamentos textuais em `sigma_dict[fidelidade_por_modo]` e `[rampa_theta]`. Bate exatamente com o relatório.
*Um erro factual do relatório §5, que uso contra ele adiante:* o `sigma_dict` do b5m tem **18 chaves, e nenhuma é `lattice`** — o relatório diz "25 chaves … com … lattice". `N_lattice` existe é no `moead_media` (20 chaves), cujo texto documenta o lattice **do b5m**.

**3. Por CÓDIGO — o comportamento é por construção, e o locus é o laço mais quente do config.**
`src/b5_prob.py:428-433` chama `log.decision(...)` só com `H.minimo_comum_di10(...)`; `src/b5_prob.py:318` monta o header antes de `evolver = EVOLVER(...)` (`:374`). O ⑥ é escrito **post-hoc** no replay dos archives (o motor é caixa-preta: `iterate()` roda 10 gerações sem gancho — declarado no `sigma_dict["④_1_linha"]` e no handoff §A.4). E `ProbMOEAD._next_gen` chama `_select` **1× por indivíduo**: medi `pop × (n_ger−1)` = **40.000 chamadas/run** (pop 50) / 39.900 (pop 105), **1.798.600 no config inteiro** — ou seja, `do()` é chamado uma vez por FE-surrogate. É o hot-loop do DI-12.1 (precedente e7/`loss_treino`, barrado a 8e4 iterações).
**Mas o critério do DI-12.1 é comportamento, não pureza — então medi:** capturar min/med/máx de `probabilities` custa **0,403 s/run (0,0141%)** reduzindo na hora, **0,022 s (0,0008%)** acumulando, e `n_substituicoes` custa **0,050 s (0,0018%)**, contra `tempo_busca_s` mediano de **2.868,2 s** (min 2.488,3 / máx 3.584,7). **O teto de hot-loop NÃO barra este caso** — logo o defeito documental é a linha "Rejeitados", não a exigência.

**4. Controle positivo/negativo — o mais duro contra a refutação.** Varri 1 célula de cada um dos 25 diretórios de config e cruzei com a tabela §6.1: **b1 3/3 · b4 3/3 · c217 3/3 · c238 1/1 · c122 2/2 · c149 2/2 · e81 2/2 · c311 2/2 · e103 2/2 · e74 2/2 · nsga3 3/3 · moead 3/3 · nsga2 2/2 · smsemoa 2/2**; parciais com causa conhecida: **e7 2/3** (`loss_treino` barrado por DI-12.1), **b3 3/4**, **c154/c262 2/3**, **c141 1/2**. **Zeros absolutos: só o trio desdeo — b5m 0/2, b5r 0/1, moead_media 0/3.** O piso ONLINE `moead` grava um `rec="decomposicao"` **com `vetores` + `N_lattice` em 25/25 células** — a prova de que o campo "vetores no header" é implementável e foi implementado em outro lugar. **Não é convenção: é gap de um molde (o b5, que serve 3 configs).**
*(Subproduto fora do escopo A25: `treed_media` não tem NENHUM evento por-geração em 10/10 células — só header+sonda+footer. Item para o relatório do treed_media.)*

---

**Por que não caiu (e o que caiu junto)**

Não caiu o **fato**: 0/30.165, 0/45 headers, único molde 0/N entre 21, nenhuma decisão cobrindo. Caiu tudo o que estava pendurado nele:

1. **"PESOS de decomposição no HEADER" — falso-positivo por REDUNDÂNCIA DETERMINÍSTICA.** `BaseEA.py:177` fixa `lattice_res_options=[49,13,7,5,4,3,3,3,3]`; `ReferenceVectors._create` é simplex-lattice puro. Reimplementei em 15 linhas, sem importar o desdeo: **`pop == n_vetores` em 45/45** (H=49→50 em M=2; H=13→105 em M=3) e, com as vizinhanças reconstruídas (`distance_matrix`+`argsort[:, :20]`, fixas desde o `__init__`), reproduzi a **A10 do próprio analista: 356.137 grupos, 355.592 contidos em 1 vizinhança = 99,8470%, 545 exceções, 99,9992% em ≤2 vizinhanças**. Ou seja: **ele só conseguiu produzir a A10 porque os pesos são recuperáveis** — o campo que ele acusa de faltar não lhe custou nada. O contrato já os chama "determinísticos — 1×". **Perda analítica: zero.**
2. **"Consequência provada: 2 dos 4 itens do teto T existem por causa desta omissão" — FALSO.** O campo contratado é min/med/máx **por geração** (3 números para ~1.000 valores de P_wrong). Não verifica a identidade `Σ1[a>b]/S² ≡ ∫pdf·CDF` por comparação (**A12**) nem S=1000/truncnorm(−3,3) (**A13**) — A13 é contrafactual sobre amostras descartadas por desenho, e o campo nem toca nisso. Auditar A12 de verdade exigiria **800.000 valores/run → 1,08 bilhão nas 45×30**. **Com o §6.1 cumprido à risca, A12 e A13 continuariam no balde T.**
3. **"A rejeição do DI-10 está empiricamente refutada" — REFUTADA, com número.** O motor gera `pop` offspring por geração (1 por `i`); a ③ guarda só a população pós-geração. Somando as 45 células: **1.798.600 offspring gerados × 356.137 grupos visíveis ⇒ 80,20% (1.442.463) são invisíveis**. A reconstrução **não distingue** "`len(selection)=0`" de "substituiu e foi sobrescrito na mesma geração" — é um **limite inferior não-identificável**, não uma contagem. Casos que mostram a lacuna: `swap_medium-lhs_ZDT4` turnover **0,9987** com só **19,51 grupos/geração de 50 offspring** (61,0% invisíveis); `swap_medium-lhs_MMF16_20` turnover 0,9930 com **50,11 de 105** (52,3%); `DTLZ2` **13,75 de 105** (86,9%). A rejeição do §6.1 continua de pé **pelo argumento certo (não-identificabilidade)** — o que a derruba é a medição de custo do passo 3, não a reconstrução dele.
4. **O que sobra de valor real, e é do próprio analista:** `p_wrong_stats` teria tornado a cadeia **A8** observável no dado em vez de inferida — em DTLZ3 (`0 substituições em 380 transições`, confirmado: **0 grupos**) o campo marcaria min=med=máx=0,0 e o NaN-collapse apareceria sozinho. O pedido de flag `vetores_degenerados` é o item substantivo do A25 e deve ser preservado.

**Enquadramento e bússola D29**
**(d) DEFEITO DE INSTRUMENTAÇÃO/DOC**, em três camadas, nenhuma de mecanismo:
- **Runner** — `src/b5_prob.py:428-433` (só o mínimo comum) e `:318` (header antes do evolver, `:374`). Vale para `b5r`, `b5m` e, por herança do molde, `moead_media`.
- **Declaração** — o `sigma_dict` é o dicionário DEF-C4 cuja função é *"declarar TODAS as exceções"*; ele declara `modelo_hp`/`dist_min_arquivo`/`tempo_busca_s` NULL e **omite esta**. É o que caracteriza (d): o log engana sobre a própria cobertura.
- **Contrato** — §6.1 linha `b5` × §6.1 "Rejeitados" são inconsistentes entre si (mesma função, linhas 82-89).
- **Gate** — nenhum gate confere campos DI-10 específicos de config algum (`scripts/accept.py:1657 check_r3_b5` tem 8 itens, todos estruturais; nenhum teste em `tests/` afere a tabela §6.1). Os 18/21 que acertaram acertaram por disciplina da sessão, não por trava.
**D29: 🟢 verde — extensão nossa** (o enriquecimento DI-10 não existe no paper). Não é 🔴/🔵/🟠/🟣: não toca artigo, código do autor, surrogate nem decisão do algoritmo. A ressalva é que a extensão **não foi declarada** — o que a joga em (d) e não em "verde declarada".

**Impacto**
- **Células excluídas: ZERO.** Nenhum número publicado muda: ①③④⑤⑦, sonda, IGD+, HV, `nd_pos_real` e a ablação D77 não dependem de campos opcionais do ⑥. `integridade_f52a` segue com 0 linhas para b5m.
- **O mecanismo continua PROVADO operante sem o campo**: 356.137 grupos, 99,8470% em vizinhança-20, 31,25% multi-slot (agregado: 111.277/356.137; 1,7333 slots/grupo) — tudo reconstruído por mim, independentemente.
- **Correções obrigatórias no relatório b5m** antes de ele virar insumo do D97: (1) §8/A25 — retirar "2 dos 4 itens do teto T existem por causa desta omissão" (falso) e a frase "a rejeição está empiricamente refutada" (refutada: 80,20% invisível); (2) §9/T item 1 — A12 e A13 permanecem em T **mesmo com o §6.1 cumprido**; (3) §5 — o `sigma_dict` do b5m tem **18** chaves e **não** tem `lattice` (é do `moead_media`), o que enfraquece a frase "fallback verificado ⇒ zero perda analítica" para o `params` ausente do ⑤ (item já conhecido, RELATORIO_F5 §5.2, 197 células/7 configs — não é parte do A25).
- **Score 8,0 / ACEITAR+CAVEAT**: mantido, mas o item 3 da §7 ("é o item que segura o score abaixo de 8,5") deveria ser reponderado — metade do achado tem custo zero comprovado.

**Ação para a rodada perfeita (F5.7) + custo**
1. **[FAZER — alta relação valor/custo]** Acumulador módulo-nível em `ProbMOEAD_select.py:82-89` chaveado por `pop.gen_count`, lido no replay em `b5_prob.py:428`. **~6 linhas**, read-only, sem RNG ⇒ ①③⑦ **bit-idênticas**. Sobrecarga **medida**: 0,0141% (rota A) / 0,0008% (rota B) de 2.868,2 s. Inclui `n_substituicoes` (0,0018%). **Custo de dados = ZERO se entrar ANTES da bateria de 30 sementes** — os campos pegam carona. Custo de engenharia: 1 sessão (patch + nova âncora + re-lacre `repos.lock/b5_desdeo` + gate de não-perturbação, que já existe e fechou 53/53 na DI-29).
2. **[FAZER — custo zero, valor zero, só contrato]** `rec="decomposicao"` com `N_lattice` + `vetores` logo após `b5_prob.py:374`, **replicando o padrão já em produção no piso `moead`** (25/25 células). 1 linha. Declarar no `sigma_dict` que os vetores são determinísticos de M.
3. **[FAZER — o item de maior valor científico]** flag `vetores_degenerados` quando `norm(reference_vectors.values)==0` (fecha A8 no dado). ~2 linhas, mesmo patch do item 1.
4. **[TORRE — doc-sync]** decidir a contradição do §6.1: manter `p_wrong_stats` na linha `b5` **e retirar** "contagem de substituições por geração do MOEA/D" da lista de Rejeitados (a medição de custo a desqualifica), **ou** o inverso. Registrar como DI-4x. Também: a linha `b5` exige `p_wrong_stats` do **b5r**, onde a grandeza não existe (mode 7 = `Prob_APD_Select_v3`, média-MC do APD, sem vetor de P_wrong) — separar b5r de b5m na tabela. Custo: 1 doc-sync.
5. **[TORRE — fecha o buraco de classe]** teste de contrato que lê a tabela §6.1 e afere as chaves observadas no ⑥ dos 21 configs (~40 linhas, `tests/test_di21_contrato.py`). É o que teria pego este achado e os parciais de b3/c141/c154/c262.
6. **[NÃO FAZER]** re-rodar a s42 do b5m só por isto: **45 células = 134.721 s = 37,4 h-core** (mediana 2.981,9 s), **1.123 h-core em 30 sementes**, para ganhar um diagnóstico que não muda número nenhum. Se o autor quiser P_wrong auditável já, uma **réplica instrumentada de 3 células** (`DTLZ3` degenerado + `swap_medium-lhs_ZDT4` turnover 0,999 + `DTLZ2` saudável) custa **~2,5 h-core** e responde A8 + a parte auditável de A12.

**Evidências e scripts** (todos em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/baterias/f54/b5m-A25/`, único local de escrita; dados lidos read-only):
`a25_01_controle_di10.py`/`a25_02_controle_di10_v2.py` → `a25_controle_di10_v2.csv` (25 configs × DI-10 esperado/encontrado) · `a25_03_reproducao.py` → `a25_reproducao.csv` (235 células de b5m/b5r/moead_media/c311/moead, chaves de header/sigma/decision) · `a25_04_substituicoes.py` → `a25_substituicoes.csv` (45×13, offspring gerados × grupos visíveis × invisibilidade) · `a25_05_pesos_reconstruidos.py` → `a25_pesos.csv` (vetores de decomposição reconstruídos do zero + contenção na vizinhança-20) · `a25_06_custo_instrumentacao.py` (benchmark da sobrecarga em 3 rotas contra `tempo_busca_s` real).