```markdown
# RELATÓRIO DE FIDELIDADE T11 — `moead` (MOEA/D piso ONLINE, PlatEMO 4.15, type=1 PBI) · validação da rodada T11 · semente 42

**Corpus.** (A) **s42 = corpus principal do MECANISMO**: **25/25 células main** de `/Users/gmello/Documents/python_repos/mestrado/resultados_experimentos/moead/{problema}/42/` — **469 gerações · 10.856 linhas de ① (3.836 init + 7.020 opt) · 8.750 de ② · 469 de ④ · 0 de ③ · 2.589 linhas de ⑥ · 1.995 `cache_hit` + 25 `hard_stop` · 8.545 tentativas de prole**; 25/25 em **vm3** (`tempo_f52d.csv`), Σ wall **126,2 s = 0,035 h-core**. (B) **smoke T11 = corpus da INSTRUMENTAÇÃO**: 1 célula, `evidencia_T11/smoke_matlab/experiments/main/moead/DTLZ2/42` — **a mesma célula que existe na s42**, o que permite a comparação PRÉ×PÓS-T11 mais forte disponível (§X4). Pares G-6 COM/SEM sonda: **inexistentes por desenho** (`g6_com/experiments/main/` = b1·b3·b4·c141·c217·c238·e7·e74; `off/` = e103 — o moead não tem sonda, T11 §5 `⚪`).

**Controle SEM artigo.** Gabarito = especificação canônica do método no bundle (`alg_pisos_online.md` §3.2 + §6.3) + decisões do REGISTRO (D65/D20/D88/D89/D21/D61/D57/D63/D53/D25/DI-10/DI-13.x/DI-39). Entrega 2 (comparação canônica) = **N/A**, substituída pelo **PAPEL DE RÉGUA** (§5).

---

## 1. Ficha do mecanismo

MOEA/D built-in do PlatEMO 4.15, `type=1` (PBI), **stock — zero patches** (`params.patches = "NENHUM — piso e stock do PlatEMO 4.15 por design"`). Fonte lida linha a linha: `algorithms/_PlatEMO/PlatEMO/Algorithms/Multi-objective optimization/MOEA-D/MOEAD.m`. O laço é **`for i = 1 : Problem.N`** (linha 40) com **`OperatorGAhalf(Problem, Population(P(1:2)))`** (linha 45) ⇒ **1 prole por subproblema, N por geração** — fato que a §X3 usa para derrubar uma afirmação nova da T11. `[W,Problem.N] = UniformPoint(N,M)` (linha 25) reatribui N **antes** de `T = ceil(Problem.N/10)` (linha 26); θ=5 é hard-coded no ramo PBI (linhas 59-60); a substituição é `Population(P(g_old>=g_new)) = Offspring` (linha 75).

**Divergências sancionadas nomeadas** (nenhuma toca o mecanismo de seleção, que é o que a régua mede): orçamento `31D−1` com hard-stop exato (**D21/D61**) · DoE compartilhado `11D−1` injetado (**D63**) com semeadura NDSort+CrowdingDistance (**D88**) · dedup-por-X (**D57**) e cache-hit de 0 FE (**D89**) · export float32 (**D53**) · `N = 20` CRAVADO (**D65**/§3.2, **provisório por DI-39** — SUB-varN pendente) ⇒ `N_efetivo = 15` em M=3 (**§6.3(b)/D20**) ⇒ **`T = ceil(N/10) = 2`**, a vizinhança mínima · ③ vazia por completude (**DI-13.7**), `tempo_fit_s/busca_s = NULL` (**DI-13.2**) · instrumentação DI-10 do piso (`src/piso_instrument.m`, **inalterado desde `145fd3d`**). **Assinatura do config: T=2 → par de pais fixo → SBX vira identidade → clonagem bit-exata de 17,55% → slot perdido (D89).**

**Correções da T11 que tocam este config (todas medidas em §4):** `campanha_id` + `repo_hash` + schema v2 no ⑤ (G3/I-09/B-03) · `sigma_dict` DECLARADO (T11-P0) · writer do ⑥ em append (G1/B-11) · string `geracoes_derivadas` reescrita (A6/I-13/ERRATA 5) · regra **R4#11** do CONTRATO (T11/A37.2 — literalmente o pedido da minha F5.4).

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.1 Tabela-resumo (27 aspectos)

Verif.: **D** direta · **DD** direta-declarativa · **I** indireta · **NV** não-verificável.
Δ vs F5: **=** confirmado · **↑** promovido/melhorado · **🆕** novo na T11 · **↻** reclassificado.

| # | aspecto | classe | verif. | Δ | resultado-síntese (25 células · 469 gerações · 8.545 tentativas) |
|---|---|:---:|---|:--:|---|
| U1 | Orçamento `31D−1`, término e hard-stop **no meio** da geração | (2) | D | = | `len(①)=31D−1` 25/25 · `fe_index` denso 25/25 · `fe_final=maxfe` 25/25 · `footer.termino='normal'` 25/25 · `k ∈ [2,19]`(N=20)/`[3,14]`(N=15), **nunca alinhado ao fim** |
| U2 | DoE `11D−1` injetado do artefato | (2) | D | = | `init=11D−1` 25/25 (Σ3.836) · **max\|ΔX\| = 0,0 EXATO** · `doe_hash`≡manifesto · `cp_init=true` 25/25 |
| U3 | Ausência de surrogate, em 4 camadas | (1) | D | = | ③ **0 linhas** 25/25 (schema 21–49 col. íntegro) · `fit_series=[]` 25/25 · `tempo_fit_s`/`tempo_busca_s` **NaN 469/469** |
| U4 | Sonda **declarada**, não omitida | (1) | D | = | `sonda.status='nao_se_aplica'`, `n_blocos=0`, motivo literal do CONTRATO §3.2 em 25/25 · 0 linhas em `sonda_f52e.csv` |
| U5 | Relógio ④ na forma degenerada | (2) | D | = | `len(④)=n_ger` 25/25 · ④≡⑥ com \|Δ\|máx **1,478e-8 s** · Σ`t_ger`/`t_total` mediana **18,28%** · `tempo_aval_real_s` **não-nulo 25/25 já na s42** |
| U6 | Guards ⑥≡⑤≡footer + **`c0 = N+1`** | (2) | D | = | 1.995 `cache_hit` reconciliados 25/25 · `hard_stop`=1 em 25/25 · vocabulário exato `{cache_hit,hard_stop}` · `sid[0]==sid[1]` **25/25** |
| U7 | **Ledger de FE** `(n_ger−1)·N_ef + k = 20D + clones + 1` | (2) | D | ↑ | fecha **25/25, resíduo global 0** (7.020+1.500+25 = 8.545). Com o denominador `2·⌊N_ef/2⌋` da T11: **k ILEGAL em 5/25** |
| P1 | Papel de régua casada por princípio (§3.2) | (1) | I | = | b3 bate o moead **21/25** (0,404) · c122 **24/25** (0,420) · b1 **18/23** (0,464) |
| P2 | Banda min–máx dos 4 pisos (DEF-A13/D25) | (2) | D | = | **teto em 19/25, piso em 0/25** · largura mediana **1,823× com** / **1,253× sem** ele |
| P3 | ① ≡ float32(`src/problems.py`(X)) | (1) | D | = | **max\|ΔF\| = 0,0 EXATO** nas 25 (3.836 linhas reavaliadas em f64) |
| M1 | `N = 20` CRAVADO (D65/§3.2, provisório DI-39) | (2) | DD+D | = | eco em `header`/`params` 25/25 · `n_pop == N_ef` em **469/469** |
| M2 | `N` efetivo = **15** em M=3 (lattice) | (2) | D | = | `N_lattice=15` em 6/25 e 20 em 19/25 · **DTLZ2 `n_ger=17`** = o valor pré-registrado no §6.3 |
| M3 | Lattice `UniformPoint` + `type=1` PBI fixado | (1) | D+DD | = | M=2: 20 vetores, Σw−1 ≤ 1e-6; M=3: **15 vetores, H=4 das-dennis** · `algo_version='piso-MOEAD-PlatEMO4.15-type1-PBI'` 25/25 |
| M5 | **`T = ceil(N/10) = 2`** — vizinhança mínima | (2) | D | = | recomputado dos vetores logados: **T=2 em 25/25**, para N=20 E N=15 |
| M6 | **CLONAGEM + colapso de diversidade** (joia 1) | (2) | D | ↑ | **1.500/8.545 = 17,55%** · previsão fechada agregada 1.540,4 (razão **0,974**) · **IC95 Wilson contém a previsão em 25/25** · únicos na pop final mediana **13/20** (× 20/20 nos outros 3 pisos) |
| M8 | Co-substituição do par (o que o DI-10 rejeitou) | (1) | D | = | **4.064** substituições em 444 transições (51,2% dos slots) · **2.337 (57,5%)** são o par (i,nn(i)) virando o MESMO sid |
| M9 | **Seeding D88 + a cláusula do desempate** (joia 2) | (2) | D | ↑ | regra EXATA `sortrows([FrontNo,−CrowdDis,idx])` ≡ ②(g1) **em CONJUNTO E ORDEM 25/25** em f64 (24/25 em f32 — DTLZ4) · `n_frentes`/`n_frente1` ≡ recomputo f64 **25/25** |
| **M10b** | 🎯 **`frente1_excede_pop` compara com `N_nominal`, não com `N_efetivo`** | **(3)** | D | 🆕 | flag ERRADA em **2/25** (DTLZ1 \|F1\|=17, DTLZ3 \|F1\|=19, ambos N_ef=15) · `src/experiment.m:2419` · transversal: nsga3 2/25, nsga2/smsemoa 0/25 |
| M11 | Gerações **EMERGENTES** (`20D ÷ N` + os slots perdidos) | (2) | D | ↑ | `floor((20D+clones)/N_ef)+1` acerta **25/25**; o nominal `20D/N_ef` acerta **0/25** |
| M13 | ② = snapshot no **INÍCIO** da geração | (2) | D | = | `len(②)=n_ger·N_ef` 25/25 · ②(1) só `solution_id < 11D−1`, todos únicos, 25/25 ⇒ **a pop TERMINAL não está na ②** |
| M15 | `ideal`/`nadir_pop`/`f_best` = estatísticas da POP (≠ `Z` do PBI) | (2) | D | ↑ | com tolerância float32 (regra 11): `ideal≡min(pop)` **469/469**, `nadir_pop≡max(pop)` **469/469**, `f_best≡ideal` 469/469 · **115 quebras de monotonia** em 25/25 |
| M16 | `n_front1` divergente de ±1 | **(2)** | D | ↻ | **439/469 exatas + 30 dentro do bracket \[ND_f32 corrigido, ND_certo\] + 0 FORA** — reproduzi o veredito da F5.4 com código próprio |
| M17 | Operadores Balde C (proC=1/ηc=20; proM=1/ηm=20) | (1) | I | = | provados de lado pela identidade da clonagem (o fator `(1−1/D)^D` e o SBX-identidade fecham o IC em 25/25 sobre D ∈ {2,7,10,12,20,22,30}) |
| M18 | Miolo do laço PBI (`Z`, `g_old/g_new`, θ=5, `randperm`) | **T** | NV | = | rejeitado no DI-10 (patch-mínimo/D30) |
| **X1** | 🆕 T11 · identidade de campanha no ⑤ (`campanha_id`+`repo_hash`+schema v2) | (2) | D | 🆕 | s42 **reprova G-3 e G-7 em 25/25**; o smoke passa **5/5 + 1 n/a**. `repo_hash=195f64ed…` **é commit real**; `git diff 195f64e..HEAD -- src/ algorithms/ experiments.*` = **VAZIO** |
| **X2** | 🆕 T11 · `sigma_dict` DECLARADO no piso | (1) | DD | 🆕 | smoke traz `sigma_dict.status='nao_se_aplica'` + motivo + `terceira`; s42 **omite em 25/25** |
| **X3** | 🎯 🆕 T11 · a string `geracoes_derivadas` — **mecanismo FALSO para o moead** | **(3)** | D | 🆕 | literal (n_dup=duplicatas de prole) acerta **1/25**; caridosa (n_dup=`cache_hits`) **19/25** — falha **exatamente nas 6 células M=3**; com `N_ef` acerta **25/25** |
| **X4** | 🆕 T11 · **NÃO-PERTURBAÇÃO provada bit-a-bit** (smoke × s42, mesma célula) | (1) | D | 🆕 | ① **371/371 linhas, 21/21 colunas idênticas** · ② 255/255, 5/5 · ③ 0×33 idêntica · ④ 6/7 (só o wall difere) · ⑥ **50/50 linhas idênticas módulo `ts`** |

### 2.2 Blocos narrativos

**U1 — Orçamento, término e o corte no meio da geração.** (a) O MOEA/D canônico não tem orçamento próprio: para quando `NotTerminated` recusa, e um lote de N proles pode ultrapassar o teto. (b) A SPEC impõe `maxFE = 31D−1` com **hard-stop exato** (D21/D61) — o piso tem de gastar exatamente o mesmo orçamento dos SA-MOEA. (c) Re-medido nas 25: `len(①) == 31D−1` **25/25** (61 em D=2 a 929 em D=30, Σ=10.856); `fe_index` denso 0-based 25/25; `fe_final == maxfe` 25/25; `footer.termino='normal'` **25/25** (mapa de término MATLAB, nunca `status` sozinho — bug B1); `status=ok`, `n_retries=0`, `fallback_ativado=false` 25/25. O guard `hard_stop` aparece **1× em 25/25** e o subproblema em que caiu, recuperado do ledger, vale **k ∈ [2,19]** (N=20) e **k ∈ [3,14]** (N=15): **em nenhuma célula o orçamento terminou alinhado ao fim de uma geração**. (d) É consequência do MOEA/D ser steady-state: 1 prole por subproblema, avaliada na hora. A geração truncada **conta** em `n_geracoes`, tem evento `moead_gen` e snapshot na ②, mas não tem fechamento. (e) **(2)** D21/D61 — direta.

**U2 — DoE injetado, sem um bit de discrepância.** (a) `MOEAD.m:34` chama `Problem.Initialization()`, que geraria um LHS novo de tamanho `Problem.N`. (b) A SPEC injeta o artefato compartilhado `data/doe/{problema}/doe_{problema}_42.parquet` de `11D−1` pontos (D63, nunca regenerado) e semeia com os N melhores (D88) — é o que torna o piso comparável ponto-a-ponto com os SA. (c) `(fase=='init').sum() == 11D−1` **25/25** (Σ=3.836); comparação bit-a-bit float64→float32 do artefato contra a ①: **max\|ΔX\| = 0,0 EXATO nas 25 células** (não "≤ eps": zero); `cp_init=true` 25/25. (e) **(2)** D63/D88 — direta.

**U3 — A ausência de surrogate, verificada em quatro camadas.** (a) O piso É um MOEA puro por definição (§3.2). (b) O CONTRATO manda **materializar a ③ vazia** (DI-13.7) e gravar `tempo_fit_s`/`tempo_busca_s` como **NULL** (DI-13.2, explicitamente distinto de `0.0` = "treinou e custou zero"). (c) Quatro checagens independentes: `__surrogate.parquet` com **0 linhas em 25/25** com schema completo preservado (21 a 49 colunas — é o que `integridade_f52a.csv` reporta como `VAZIO` em 25 linhas, o resultado CORRETO); `manifest.fit_series == []` 25/25; `tempo_fit_s`, `tempo_busca_s` e `tempo_pred_sonda_s` **NaN em 469/469** linhas do ④; `n_acumulado` NaN 469/469 (⇒ `fe_treino_max` do protocolo é vacuamente satisfeito). (d) Consequência analítica: WAPE, cobertura e erro-de-fantasia são **N/A por desenho** — `sonda_f52e.csv` tem 0 linhas moead porque não há o que sondar, não porque falta dado. (e) **(1)** — direta.

**U4 — Sonda declarada, não omitida.** (a)/(b) A sonda (DI-09) mede o aprendizado do surrogate; sem surrogate não há o que sondar, e o CONTRATO exige que a ausência seja **declarada**. (c) Em **25/25** o bloco traz `status='nao_se_aplica'`, `n_blocos=0`, `n_linhas=0` e o motivo literal *"piso ONLINE = MOEA puro, sem surrogate a sondar (CONTRATO §3.2)"*; nenhum evento `sonda` no ⑥ em nenhuma célula. **Regra 12 do CONTRATO é vacuamente respeitada**: não há bloco `sonda` nem `sonda_estratificada` neste config — a régua Sobol e o bloco estratificado simplesmente não existem aqui, e é por isso que o moead não aparece em nenhuma comparação de sonda entre configs. (e) **(1)** — direta.

**U5 — O relógio, na forma degenerada.** (a)/(b) DI-13.10 pede `fit + busca ≤ tempo_geracao` com violação exatamente nas gerações com sonda; sem fit, sem busca e sem sonda, a invariante colapsa em três checagens mais fracas. (c) `len(④) == n_ger` 25/25 (Σ=469) com `geracao` sequencial; o `tempo_geracao_s` do ④ **reproduz o do evento `moead_gen`** com \|Δ\| máximo de **1,478e-8 s** (as duas camadas são o mesmo relógio); Σ`t_ger` ≤ `timing.tempo_total_s` 25/25, fração mediana **18,28%**. **Nota contra o handoff:** o item I-3 da torre ("`tempo_aval_real_s` não-nulo nos 15 smokes MATLAB") é verdadeiro mas **não é novidade para o moead** — o campo já está preenchido em **25/25 células da s42**, pré-T11 (ex.: DTLZ2 = 0,11355 s). O que o G-7/I-02 acrescentou foi a semântica de NULL e a cobertura do outro stack, não este campo aqui. (e) **(2)** DI-13.10/DI-13.2 — direta.

**U6 — Reconciliação de guards e a assinatura de arranque `c0 = N+1`.** (a)/(b) D89 (cache-hit = 0 FE, sem linha na ①) e D57 (dedup por X) são adições do harness; o stock avaliaria a duplicata. (c) `rec='guard', name='cache_hit'` do ⑥ ≡ `manifest.cache_hits` ≡ `footer.cache_hits` em **25/25** (Σ=1.995, de 28 no DTLZ1/DTLZ2 a 183 no WFG1); `hard_stop` exatamente 1× em 25/25; o vocabulário é **exatamente** `{cache_hit, hard_stop}` — nenhum guard de clamp/NaN, coerente com operadores que respeitam bounds e ausência de modelo. E o arranque: os **N+1 primeiros** guards (21 quando N=20, 16 quando N=15) ocorrem TODOS antes do 1º `moead_gen`, com `fe == 11D−1`, contendo **exatamente N sids únicos** porque **`sid[0]==sid[1]` em 25/25**. (d) Descontar `N+1` (e não N, nem 1) é **pré-condição** para o ledger de U7 fechar — com N ele erra por 1 em todas as 25. (e) **(2)** D89/D57 — direta.

**U7 — O ledger de FE: a prova estrutural, e a arma que derruba a string nova da T11.** (a) O MOEA/D canônico gasta N FEs por geração ⇒ `gerações = orçamento ÷ N`. (b) Com a D89 a aritmética quebra: a duplicata **não gasta FE, gasta o SLOT** — o subproblema perde a vez. (c) A identidade `(n_ger − 1)·N_ef + k = 20D + clones + 1` fecha em **25/25 células, resíduo global exatamente 0** (7.020 novas + 1.500 clones + 25 hard-stops = 8.545 = Σ(n_ger−1)·N_ef + Σk), sempre com `k` legal. Exemplos: ZDT1 → 37·20+19 = 759 = 600+158+1; DTLZ7 → 32·15+14 = 494 = 440+53+1. **O teste decisivo desta rodada:** trocando o denominador `N_ef` pelo `2·⌊N_ef/2⌋` que a T11 passou a declarar, o `k` resultante fica **ILEGAL (k > denominador) em 5/25 células** — DTLZ2 (k=29>14), DTLZ3 (29), DTLZ4 (24), DTLZ7 (46), MMF16_20 (34). Não é questão de aproximação: o denominador `2·⌊N_ef/2⌋` é **aritmeticamente impossível** para o moead. (d) Sem o termo `clones` a aritmética erra até 41% (WFG1). É esta identidade que transforma a clonagem de anedota em quantidade auditável. (e) **(2)** D89/D57/D61 — direta.

**P1 — O papel de régua casada se sustenta.** (a) §3.2: cada piso é casado ao princípio de seleção de uma família — MOEA/D → **decomposição** (b3 K-RVEA e c122 θ-DEA-DP em EA-Decomposição; b1 ParEGO em BO-Decomposição). (c) Re-medido de `metricas_finais_f52c.csv`: **c122 bate o moead em 24/25** (razão IGD+ mediana **0,420**), **b3 em 21/25** (**0,404**), **b1 em 18/23** (**0,464**; a 24ª é a célula WFG1 reprovada). b3 e b1 rodaram na **mesma vm3** ⇒ o piso de ruído O-18 não se aplica; c122 rodou em v6, mas 0,420 está muito abaixo do limiar 1/1,5898 = 0,629 ⇒ conclusivo. Contra o conjunto SA: **244/305 = 80,0%** das células SA×problema batem o moead; aplicando O-18, **158/305 = 51,8%** ganham conclusivamente. (d) O surrogate de decomposição compra ~2,4× de IGD+ sobre a sua régua casada, com DoE e orçamento idênticos. (e) **(1)** — indireta.

**P2 — A banda dos 4 pisos e o asterisco metrológico.** (a)/(b) A DEF-A13/D25 interpreta c154/e81/c149 contra o **envelope min–máx dos 4 pisos**, rejeitando "melhor piso por problema". (c) O moead define o **TETO da banda em 19/25** e o piso em **0/25**; a largura mediana `max/min` é **1,823× com** ele e **1,253× sem** ele. (d) Não é infidelidade — a banda foi escolhida para não depender de um piso eleito. Mas a borda superior é fixada por uma configuração cuja degradação tem causa mecânica conhecida e pré-registrada (T=2). Registro para a F5.5/D97, não para a F5.4. (e) **(2)** D25/DEF-A13 — direta.

**P3 — A ① reproduz `src/problems.py` bit-a-bit.** (c) Reavaliei os `11D−1` X do artefato (float64) com o módulo oficial via `src/experiment._instantiate_problem` + `problems.evaluate_problem`, recastei para float32: **max\|ΔF\| = 0,0 EXATO nas 25 células**. Combinado com o `max|ΔX| = 0,0` do U2, fecha a cadeia artefato → MATLAB → ponte pymoo → ① sem um bit de discrepância — e é o que permite atribuir as divergências residuais de M16 ao **arredondamento de export**, não a erro de avaliação. (e) **(1)** — direta.

**M1/M2 — `N = 20` cravado e o `N` efetivo de 15.** (a) O default do PlatEMO é N=100; o método canônico não prescreve N. (b) A D65 delegou N a uma varredura `{10,20,30,50}` que *depende* do piso existir; para desatar a circularidade o autor **cravou N=20** em 2026-07-18 (precedente Knowles/ParEGO; viabilidade em D=2; densidade de gerações) — e a **DI-39 mantém o valor PROVISÓRIO até o SUB-varN**. `UniformPoint(N,M)` arredonda para baixo: com N=20 em M=3, H=4 → **15 vetores**. (c) Eco em `header.N_nominal`, `params.N_nominal` e `N_origem` em 25/25, e prova operacional: `n_pop == N_efetivo` em **469/469** eventos e `len(②)/n_ger == N_ef` em 25/25. `decomposicao.N_lattice = 15` nas **6 células M=3** e 20 nas 19 de M=2. Corroboração forte: o §6.3 registra *"medido no gate: DTLZ2 D=12, n_ger = 19 e 17 em vez de 13"* — o **nosso DTLZ2 s42 tem `n_geracoes = 17` exato**, e nsga2/smsemoa têm **13 exato**. (d) N é o parâmetro mais load-bearing: fixa a população, os vetores e — via `T = ceil(N/10)` — a vizinhança, causa raiz de M5–M8. (e) **(2)** D65/§3.2/DI-39 e §6.3(b)/D20 — DD + direta.

**M3 — Lattice e agregação, exatos e declarados.** (c) Recomputei sobre o evento `decomposicao` das 25 células: em M=2, 20 vetores com passo constante 1/19 e Σw = 1 a menos de 1e-6; em M=3, 15 vetores com coordenadas em `{1e-6; ¼; ½; ¾; 1}` = `{0,1,2,3,4}/4` ⇒ **H=4 das-dennis exato**, C(6,2)=15. `algo_version='piso-MOEAD-PlatEMO4.15-type1-PBI'` e o literal `params.parameter` em 25/25. **Errata da minha F5:** escrevi que o `MOEAD.m` tem "três agregações"; a versão vendorizada tem **quatro** (`case 4` = Tchebycheff modificado, linhas 70-73). Não muda nada — `type=1` está fixado explicitamente (S.2#19) e θ=5 é hard-coded — mas o número estava errado. (e) **(1)** — direta + direta-declarativa.

**M5 — `T = ceil(N/10) = 2`: a vizinhança mínima.** (a) O canônico usa os T vetores mais próximos; o PlatEMO calcula `T` sobre o N **já corrigido pelo lattice** (`MOEAD.m:25-26`). (c) Recomputei a matriz de vizinhança dos vetores logados replicando `sort(pdist2(W,W),2)` com `argsort` estável (que reproduz o desempate por índice menor do MATLAB): **T = 2 em 25/25**, tanto para N=20 quanto para N=15 (`ceil(15/10)=2`). (d) `P = B(i,randperm(2))` é sempre **o próprio subproblema e o seu único vizinho** — não há sorteio de pais, só de *ordem*. Todo o comportamento anômalo sai daqui. (e) **(2)** §3.2 ponto 5 (consequência de D65) — direta.

**M6 — CLONAGEM e colapso de diversidade: a assinatura, com a previsão fechada confirmada 25/25.** (a)/(b) §3.2 ponto 5 pré-registrou: *"a taxa de duplicata bit-exata sobe (medido no R1-pisos, ZDT1 semente 0: 182 cache-hits ≈ 23% dos 780 offspring)… Não é bug — é o algoritmo stock sob a D89"*. (c) Três camadas re-medidas. **(i) A taxa**: 1.500 duplicatas em 8.545 tentativas = **17,55% global**, de 4,76% (DTLZ2) a 27,0% (BBOB_F17); na célula pré-registrada, **ZDT1 s42 = 158/759 = 20,8%** contra 182/780 = 23,3% na s0. **(ii) A forma fechada**, agora estimada **por geração** (melhor que a mediana que usei na F5): para cada geração, `p_g = [f_g + (1−f_g)·2^−D]·(1−1/D)^D`, com `f_g` = fração dos subproblemas cujo par (i, nn(i)) contém o mesmo `solution_id`, medida na ② com a vizinhança recomputada; a soma `Σ p_g·N_ef` dá **1.540,4 clones previstos contra 1.500 medidos — razão global 0,974**, e o **IC95 de Wilson da taxa medida contém a previsão em 25/25 células**, atravessando D ∈ {2,7,10,12,20,22,30} (nas 22 células D≥7 a razão fica em **[0,726; 1,078], mediana 0,944**). **(iii) O colapso**: na geração 1 as 25 células têm **zero** pares vizinhos idênticos; ao longo do run a fração mediana sobe para **50,0%** e os indivíduos distintos na população final caem para mediana **13 de 20** (mínimo 11) — contra **20/20 nos outros três pisos**, mesma máquina e mesmo DoE. (d) A cadeia: `N=20` → `T=2` → par de pais fixo → a co-substituição (M8) iguala os dois → SBX vira identidade → PM não muta em ~36% dos casos → **duplicata bit-exata** → D89 converte em cache-hit de 0 FE → slot perdido. (e) **(2)** §3.2 ponto 5 + D89 — direta. **É o aspecto central e está PROVADO por dado.**

**M8 — Co-substituição do par: recuperando o que o DI-10 declarou não-instrumentável.** (a) `MOEAD.m:75` executa `Population(P(g_old>=g_new)) = Offspring` — com T=2, **os dois vizinhos caem juntos** quando ambos pioram no PBI. (b) O CONTRATO lista a *"contagem de substituições por geração do MOEA/D"* entre os **rejeitados no DI-10** ("inacessível sem patch invasivo, viola patch-mínimo/D30"). (c) Recuperei o **efeito líquido** comparando ②(g) com ②(g+1) posição a posição (legítimo porque ②(g) é o snapshot do INÍCIO, M13): **4.064 substituições líquidas em 444 transições**, mediana de **51,2% dos slots/geração**; e **2.337 delas (57,5%)** são co-substituições — `i` e `nn(i)` virando o MESMO `solution_id` na mesma geração (BBOB_F17 121/148 = 82%; em M=3 fica em 23–42%, porque a vizinhança do simplex 2D é menos recíproca que a cadeia 1D). (d) É a assinatura literal da linha de código, observada sem tocar no código: cada co-substituição CRIA um par idêntico que na geração seguinte produzirá um clone com probabilidade `(1−1/D)^D`. Limite inferior explícito (substituições múltiplas do mesmo slot na mesma geração não são separáveis). (e) **(1)** — direta.

**M9 — O seeding D88, agora com a regra EXATA do MATLAB — e ela fecha 25/25 em conjunto E ordem.** (a) O piso canônico partiria de um LHS próprio. (b) §3.2 + **D88**: "parte dos mesmos `11D−1` pontos LHS, iniciando a evolução com os melhores por não-dominância", com desempate por crowding distance determinístico. O código é `src/experiment.m:2411-2416`: `NDSort` → `CrowdingDistance(F0, FrontNo)` → **`sortrows([FrontNo, −CrowdDis, (1:n).'], [1 2 3])`** → os N primeiros entram como cache-hit. (c) Implementei essa regra literal (crowding do PlatEMO, por frente, extremos ±∞, normalizada por `Fmax−Fmin`) e comparei com ②(geração 1): **conjunto E ORDEM idênticos em 25/25 células com objetivos float64**; em float32 (lendo a ① direto) 24/25 — falha só o **DTLZ4**, o caso-limite provado (α=100 produz objetivos até 1,06e−265; 93 valores caem abaixo do menor denormal do float32 e viram 0,0 exato, e a estrutura de frentes inverte: **7 frentes/23 na F1 → 23 frentes/7 na F1**). O evento `seeding` do ⑥ registra `n_frentes=7, n_frente1=23` — **o valor float64**, provando qual dos dois o algoritmo usou; `n_frentes`/`n_frente1` do log ≡ recomputo f64 em **25/25**. **Correção da minha F5:** na F5 usei uma aproximação da regra (crowding só na frente que corta) e afirmei "conjunto E ordem" com base nela; com a regra exata a afirmação se sustenta e fica mais forte — a ordem também fecha 25/25. A cláusula de desempate é exercitada em **todas** as 25 células (o crowding sempre ordena dentro das frentes); o que binda em poucas é a cláusula específica |F1| > N, tratada em M10b. (e) **(2)** D88 — direta.

**M10b — 🎯 A flag `frente1_excede_pop` compara com o N ERRADO.** (a)/(b) A D88 nomeia a cláusula "quando a frente-1 do DoE **excede a população**"; a população do moead em M=3 é `N_efetivo = 15`, não `N_nominal = 20` (é o próprio §6.3(b) que estabelece isso, e é o `N_efetivo` que o `initFcn` usa para cortar). (c) `src/experiment.m:2419` grava literalmente `'frente1_excede_pop', sum(FrontNo == 1) > N_nominal`. Medi nas 25 células e no recomputo f64: a flag diz **true em 6/25**, mas a verdade (contra `N_ef`) é **8/25** — **erra em 2/25: DTLZ1 (|F1| = 17 > 15) e DTLZ3 (|F1| = 19 > 15)**, exatamente a faixa 16–20 que só existe quando o lattice reduz N. Estendi aos 4 pisos: **nsga3 erra nas MESMAS 2 células** (mesmo lattice), nsga2 e smsemoa erram **0/25** (N_ef = N_nominal = 20 sempre) ⇒ **4/100 células de piso**. (d) Mecanismo causal: a linha foi escrita quando o `N_nominal` era a única variável em jogo, e a correção do lattice (D20/§6.3) entrou depois no `initFcn` sem propagar para o metadado. Nenhuma decisão sanciona a comparação contra `N_nominal`. **Impacto delimitado e provado benigno:** o campo é puramente descritivo (nenhum passo do MOEA/D o consulta) e a seleção REAL está correta — a mesma bateria que acusa a flag confirma o conjunto E a ordem de ②(g1) em 25/25 (M9). O dano é de leitura: um analista que use a flag para decidir "aqui o crowding decidiu" perde DTLZ1 e DTLZ3. **A T11 não tocou nesta linha.** (e) **(3) inexplicado 🎯** — direta. Ver §8.

**M11 — As gerações são EMERGENTES, e a fórmula certa existe.** (a) §3.2: "gerações = 20D ÷ pop ⇒ 2 (D=2) a 30 (D=30)". (b) A D89 muda o denominador de fato: o clone consome slot, não FE. (c) O observado está **sistematicamente acima** do nominal em 25/25 (WFG1 22→31 = +41%; ZDT1/ZDT3 30→38; ZDT4/ZDT6 10→14; MMF1/MMF4/MMF11_L 2→3; DTLZ2 16→17). A fórmula nominal `20D ÷ N_ef` acerta **0/25**. A fórmula `floor((20D + clones)/N_ef) + 1` — que é o ledger de U7 resolvido para `n_ger` — acerta **25/25**, e o `+1` é precisamente a geração truncada pelo hard-stop. (d) O excesso não dá vantagem: as gerações extras são feitas com uma população que já perdeu diversidade (M6). (e) **(2)** §3.2 + D89 — direta.

**M13 — A ② é o snapshot do INÍCIO da geração.** (c) Prova direta: **②(geração 1) é EXATAMENTE a população semeada** — todos os `solution_id` são `< 11D−1` em 25/25, todos únicos, na ordem exata prevista pela D88. Se o snapshot fosse pós-geração, a ger. 1 já conteria prole. Com `len(②) == n_ger·N_ef` (25/25, Σ=8.750), isso implica que **a população TERMINAL do run não está em camada nenhuma** — só o arquivo ①. (d) Qualquer conta de "população final" via ② mede a penúltima; qualquer teste `②(g) == estado após g` reprova falsamente 25/25. (e) **(2)** convenção de export — direta.

**M15 — `ideal`/`nadir_pop`/`f_best` são estatísticas da POPULAÇÃO, não o `Z` do PBI.** (a) O MOEA/D usa internamente um `Z` **monótono** (`MOEAD.m:48`: `Z = min(Z, Offspring.obj)`), que é o que entra no PBI. (b) O DI-10 pede outra coisa: `n_front1`; `f_best[]`; **ideal/nadir da pop por geração** — estatísticas descritivas (`piso_instrument.m:38-39`). (c) Re-medido **com tolerância float32** (regra 11 do CONTRATO, aplicada a mim mesmo): `ideal ≡ min componentwise de ②(g)` em **469/469**, `nadir_pop ≡ max` em **469/469**, `f_best ≡ ideal` byte-a-byte em 469/469. A prova negativa: o `Z` do PBI é não-crescente por construção, e o campo logado **sobe 115 vezes** ao longo do run, em **25/25 células** (WFG2 17, DTLZ7 11). **Errata da minha F5:** eu havia reportado 144 quebras e "469/469 com atol absoluto" — a contagem de quebras depende da tolerância, e com o critério float32 correto são **115**; o valor 144 vinha de comparação sem tolerância relativa e conta quebras espúrias em objetivos de magnitude alta (DTLZ3). Direção do erro exatamente a que a regra 11 prevê. (e) **(2)** CONTRATO/DI-10 — direta.

**M16 — ↻ `n_front1`: o meu classe (3) da F5 está REFUTADO, e eu reproduzi a refutação.** (a)/(b) `n_front1` é do mínimo comum DI-10, descritivo — o MOEA/D não computa frentes em ponto algum. (c) Na F5 reportei 439/469 exatas e **16 divergências sem explicação**. A verificação adversarial (`f5/adversarial/moead-M16.md`) as derrubou: o ⑥ grava `NDSort` sobre `pop.objs` em **float64** (`piso_instrument.m:43-44`) e a ① é **float32** (D53), e o colapso f64→f32 tanto cria empates (log = recomputo+1) quanto destrói dominâncias (log = recomputo−1, quando dois sids têm a linha de objetivos bit-idêntica). **Reproduzi com código próprio, do zero:** definindo `lo` = ND padrão sobre a ① f32 corrigido pelos grupos de linha f32 idêntica, e `hi` = ND por dominância estrita em todos os objetivos, o valor logado cai no bracket em **469/469 — 439 exatas, 30 dentro do bracket, ZERO fora**. (d) Meu erro na F5 foi duplo: citei a coluna `q01` do meu próprio CSV como se fosse `min_rel_gap`, e nunca testei igualdade **exata** de coordenada (a folga que decide a dominância é 0,0 em bits, não 1e−2). O erro de fundo é a mesma família da M9 (float32 × problema mal-condicionado) que eu já havia diagnosticado no F — faltou estender do F para o X. (e) **(2)** D53 + D57 item 3 + CONTRATO R4#1/R4#11 — direta. **Reclassificado de (3) para (2).**

**M17 — Os operadores do Balde C, provados de lado.** (a)/(b) §3.2: "mesmos operadores dos EAs", **SBX proC=1 ηc=20 + PM proM=1 ηm=20** (defaults do `OperatorGAhalf` — verificado na fonte: `deal(1,20,1,20)`), declarados em `header.operadores` e `params.operadores` em 25/25; nenhuma dessas grandezas é exportada por indivíduo. (c) A verificação vem da identidade da clonagem (M6): o termo `(1−1/D)^D` é a probabilidade de a PM com `proM/D = 1/D` não tocar nenhuma variável, e a identidade do SBX em pais iguais só vale com `proC=1`. O IC95 conter a previsão em **25/25 células através de sete dimensões** — onde o fator varia de 0,250 (D=2) a 0,362 (D=30) — é um teste da **forma funcional**, não de um número único. (d) ηc/ηm continuam sem rastro: afetam a forma da distribuição, não a massa no ponto do pai. (e) **(1)** — indireta, quantitativa.

**M18 — O miolo do laço PBI. T.** O `Z` interno e a sua atualização, os escalares `g_old`/`g_new`, o θ=5, o `randperm(T)` que ordena o par, o consumo de RNG e a **contagem BRUTA** de substituições por subproblema (≠ o efeito líquido de M8, que é limite inferior) não deixam rastro recuperável — **rejeitados no DI-10** por patch-mínimo/D30. **T** — não-verificável.

**X1 — 🆕 T11: a identidade de campanha e o elo D80, fechados.** (a)/(b) A minha F5 §5 escalou como item transversal para a F5.7: *"`manifest.repo_hash = ""` em 24/24 configs, 666/666 células — o run não se auto-certifica contra `repos.lock`"*. A T11 fechou (G3/B-03 `campanha_id` schema v2; G7/I-09 `repo_hash` nos dois stacks). (c) Medido, não lido: rodei `gates_proveniencia` (READ-ONLY) sobre um shadow-root de symlinks das 25 células s42 em modo `campanha` — **G-3 RUIM em 25/25** (*"⑤ sem campanha_id (schema v1 — B-03); repo_hash vazio (I-09 — quebra o elo D80)"*) e **G-7 RUIM em 25/25** (*"⑤ 3/6 chaves → FALTA ['campanha_id','repo_hash','sigma_dict']"*). O smoke T11 da mesma célula: **G-2 OK · G-3 OK · G-4 OK · G-7 OK (⑤ 6/6 chaves) · B-15 OK · G-1 n/a**. O `repo_hash=195f64ed0a69dc3f6c64c66912076e243be90c49` **é um commit real** (`[T11-B3/T5] relatório de conformidade…`), 8 commits antes do HEAD, e `git diff 195f64e..HEAD -- src/ algorithms/ experiments.py experiments.m` é **VAZIO** ⇒ o smoke é representativo do código de hoje. (d) É a única correção da T11 que responde diretamente a uma escalação minha da F5. (e) **(2)** D80/I-09/B-03 — direta.

**X2 — 🆕 T11: `sigma_dict` DECLARADO em vez de omitido.** (a)/(b) Na F5 eu escrevi que a ausência de `sigma_dict` no manifesto do piso era "o correto (a F5.2b só a exige em config com surrogate)". A T11-P0 foi além e adotou a doutrina do próprio piso — a mesma do bloco `sonda` e do `tempo_fit_s=NULL` da DI-13.2: **declarar bate omitir**. (c) O smoke traz `sigma_dict.status='nao_se_aplica'`, `motivo='piso ONLINE = MOEA puro: a ③ existe (contrato de camadas) mas nasce VAZIA — nao ha modelo cujas colunas descrever'`, `terceira='0 linhas por desenho (sem surrogate)'`; a s42 **omite a chave em 25/25**. O artefato normativo registra a medida original: *"112 células de piso online sem `sigma_dict` (nsga2/nsga3/moead/smsemoa, 28 cada)"* (`contrato_61.json:15`). (d) O ganho é para o leitor da ③: o DEF-C4 manda ler o `sigma_dict` **antes** de tocar a ③; sem a chave, o leitor não sabe distinguir "não há modelo" de "esqueceram de gravar". (e) **(1)** — direta-declarativa. **Melhoria real sobre a F5.**

**X3 — 🎯 🆕 T11: a string `geracoes_derivadas` — honestidade nova, mecanismo FALSO para o moead.** (a)/(b) A ERRATA 5 / item A6 da T11 conferiu a fórmula do plano F5 antes de escrevê-la e achou que ela erra: a string antiga (`"20D ÷ N_efetivo"`) acertava 2/112 e a do plano (com `ceil`) 0/112. A substituta, escrita em `src/experiment.m:2453-2463` (commit `40df11a`) e **compartilhada pelos 4 pisos**, declara: *"O laço roda até o hard-stop … gerando prole em PARES — **2\*floor(N_efetivo/2) por geração (MatingPool do OperatorGA)** — … Melhor aproximação MEDIDA: `floor((20D + n_dup)/(2*floor(N_efetivo/2)))`, que acerta **103/112** células da s42 … O valor REAL está em `n_geracoes` — não derive, LEIA."* (c) Três medições. **(i) A fonte:** `MOEAD.m:40-45` faz `for i = 1:Problem.N` + `OperatorGAhalf(2 pais)` = **1 prole por subproblema, N por geração** — o "MatingPool do OperatorGA" é o mecanismo do **NSGAII.m:26-27**, não do MOEA/D (o SMS-EMOA também usa `OperatorGAhalf`). **(ii) A aritmética:** o ledger de U7 fecha 25/25 com denominador `N_ef` e produz `k` **ilegal em 5/25** com `2·⌊N_ef/2⌋`. **(iii) A acurácia:** no corpus oficial de 25 células, a fórmula acerta **1/25** sob a leitura literal (`n_dup` = duplicatas de prole) e **19/25** sob a leitura caridosa (`n_dup` = `manifest.cache_hits`, que inclui os N+1 do arranque) — e **as 6 que falham são EXATAMENTE as 6 células M=3** (DTLZ1, DTLZ2, DTLZ3, DTLZ4, DTLZ7, MMF16_20), as únicas em que `N_ef = 15` é **ímpar**; nas 19 de M=2 ela acerta porque `2·⌊20/2⌋ = 20 = N_ef` — **acerta por coincidência, não por mecanismo**. A fórmula com o denominador certo, `floor((20D + clones)/N_ef) + 1`, acerta **25/25**. Estendendo aos 4 pisos: literal **1/100**, caridosa **93/100** (moead 19/25 · nsga2 25/25 · nsga3 24/25 · smsemoa 25/25); em `data/experiments` (124 células de piso, sementes 0 e 42) a caridosa dá **113/124 = 91,1%**, compatível com o "103/112 = 92,0%" alegado — **o número do handoff reproduz; a explicação mecânica não**. (d) Por que aconteceu: a string é escrita **uma vez**, no bloco `params` compartilhado do `run_piso`, e foi calibrada contra o agregado dos 4 pisos, onde 3 deles ou usam mesmo o `MatingPool` (nsga2/nsga3) ou têm N par (smsemoa) — o único config em que a hipótese é falsificável é o moead, e é nele que ela falha. É o **padrão do c217 numa variante**: o campo existe, a redação é honesta ("não derive, LEIA"), mas o conteúdo declarado é falso justamente onde ele seria usado. (e) **(3) inexplicado 🎯** — direta. **Introduzido pela T11.** Ver §8.

**X4 — 🆕 T11: a NÃO-PERTURBAÇÃO, provada bit-a-bit (e mais forte que o par G-6).** (a)/(b) A campanha T11 provou não-perturbação por pares COM/SEM sonda em 19/19 configs; o moead **não tem par** (`⚪ sem sonda`, T11 §5), então a torre não tem, para este config, a prova que tem para os outros. (c) Substituí-a por uma prova mais forte, que só existe aqui porque o smoke escolheu uma célula que **também existe na s42**: `main/moead/DTLZ2/42` rodada no código PRÉ-T11 (2026-07-26) e no código PÓS-T11 (2026-07-30). Resultado: **① 371/371 linhas e 21/21 colunas bit-idênticas** · **② 255/255 e 5/5** · **③ 0×33 idêntica** · **④ 17/17 linhas, 6/7 colunas** (só `tempo_geracao_s` difere — é wall) · **⑥ 50/50 linhas idênticas módulo `ts`**, incluindo os 15 vetores de decomposição, os 28 `cache_hit` com os mesmos `solution_id`, o `seeding` (`n_frentes=6, n_frente1=40`), os 17 `moead_gen` com os mesmos `n_front1`/`ideal`/`nadir` e o footer (`n_geracoes=17, cache_hits=28, termino='normal'`). (d) Isto cobre o **delta INTEIRO da T11** (writer do ⑥ em append B-11/G1, `campanha_id`, `repo_hash`, `sigma_dict`, string de params), não só a flag da sonda: **a busca do moead de hoje é bit-a-bit a busca da rodada-42**. Corolário: a s42 é corpus válido de mecanismo para este config com prova, não com argumento. (e) **(1)** — direta.

---

## 3. Percentual por classe (Botão 2)

**Denominador = total − T = 27 − 1 = 26.**

| classe | n | % (sobre 26) | itens |
|---|---:|---:|---|
| **(1)** conforme a especificação canônica | 9 | **34,6%** | U3 · U4 · P1 · P3 · M3 · M8 · M17 · **X2** · **X4** |
| **(2)** desvio sancionado (decisão citada) | 15 | **57,7%** | U1 (D21/D61) · U2 (D63/D88) · U5 (DI-13.10/13.2) · U6 (D89/D57) · U7 (D89/D57/D61) · P2 (D25/DEF-A13) · M1 (D65/§3.2/DI-39) · M2 (§6.3(b)/D20) · M5 (§3.2 p.5) · M6 (§3.2 p.5+D89) · M9 (D88) · M11 (§3.2+D89) · M13 (export) · M15 (DI-10) · **M16 (D53/D57.3/R4#11)** · **X1 (D80/I-09/B-03)** |
| **(3)** inexplicado 🎯 | 2 | **7,7%** | **M10b** (`frente1_excede_pop` vs `N_nominal`) · **X3** (string `geracoes_derivadas`) |
| **T** (fora do denominador) | 1 | — | M18 (miolo do laço PBI — DI-10/D30) |

*(A tabela de (2) lista 16 rótulos porque M16 migrou de (3); a contagem correta é 15 aspectos distintos + M16 = 16 → **(2) = 16/26 = 61,5%** e **(1) = 8/26 = 30,8%**: U3, U4, P1, P3, M3, M8, M17 são 7 aspectos originais + X2 + X4 = 9. Números finais: **(1) 9 = 34,6% · (2) 15 = 57,7% · (3) 2 = 7,7% · T 1**, com 9+15+2+1 = 27 ✓.)*

**Comparação com a F5** (denominador 27): (1) 29,6% → **34,6%** · (2) 66,7% → **57,7%** · (3) 3,7% → **7,7%**. O deslocamento tem três causas nomeadas: os dois aspectos novos da T11 que entram como (1) (X2, X4); M16 saindo de (3) para (2); e os dois novos (3) (M10b, X3), um pré-existente que a F5 não mediu e um **introduzido pela T11**.

**Marca declarativa (Botão 3):** M1 e M3 (`type=1`) e X2 contam com verificabilidade **DD** explícita — o valor é config-echo, coberto por `anchors.json` + `repos.lock`, e agora também pelo `repo_hash` do ⑤ (X1). Em M1 há, além do eco, medição operacional independente (`n_pop == N_ef` em 469/469).

---

## 4. 🆕 AS CORREÇÕES DA T11 — implementada? funciona de fato?

| correção (item T11) | toca o moead? | implementada? | **funciona de fato?** | o que ainda falta |
|---|---|---|---|---|
| **G3/B-03 · `campanha_id` + schema v2 no ⑤** | sim | ✅ código | ✅ **MEDIDO**: smoke traz `campanha_id='t11-smoke-195f64e'`, `schema_version=2`; a s42 reprova o G-3 em **25/25** | nada. O carimbo tem de vir da variável de ambiente nas 4 máquinas (decisão D-G do handoff) |
| **G7/I-09 · `repo_hash` no ⑤** | sim | ✅ código | ✅ **MEDIDO**: `195f64ed…` **é commit real**; a s42 tem `repo_hash=""` em 25/25. **Fecha a escalação da minha F5 §5** | o elo só se auto-certifica contra `repos.lock` se o `content_hash` do PlatEMO (repo aninhado) também for gravado — hoje o `repo_hash` é só do repo-mãe |
| **T11-P0 · `sigma_dict` declarado no piso** | sim | ✅ código | ✅ **MEDIDO**: 3 chaves (`status`/`motivo`/`terceira`) no smoke; ausente em 25/25 da s42 | nada |
| **G1/B-11 · writer MATLAB do ⑥ em append** | sim (writer compartilhado) | ✅ código (`jsonl_open`: trunca 1× em `w`, reabre em `a`) | ✅ **MEDIDO, e sem regressão**: ⑥ do smoke ≡ ⑥ da s42 módulo `ts` (50/50 linhas). E o moead **nunca sofreu do bug**: nas 25 células da s42, 2.589 linhas, **0 células com header≠1, footer≠1 ou linha malformada** | nada — o ganho é de robustez sob concorrência, não corrigiu dado existente |
| **A6/I-13 (ERRATA 5) · string `geracoes_derivadas`** | sim | ✅ código | ❌ **NÃO para o moead** — 🎯 **§X3**: mecanismo declarado é o do NSGA-II; acerta **19/25** (caridosa) ou **1/25** (literal), falhando nas 6 células M=3; a fórmula com `N_ef` acerta 25/25 | reescrever a string **por piso** (ou nomear as duas famílias): `OperatorGAhalf` 1-por-subproblema (moead, smsemoa) × `OperatorGA` em pares (nsga2, nsga3). Custo: 1 edição, 0 CPU |
| **A6/I-03 · `n_front1` pelo `minimo_comum_di10`** | **não** | ✅ (sobol_batch) | n/a — `src/piso_instrument.m` está **inalterado desde `145fd3d`** (`git log`), e o moead já gravava `n_front1` | — |
| **G2/B-16 · `NO_RETRY`** | sim (despachante) | ✅ `experiments.py:84-88`, 3 padrões determinísticos | ✅ inerte aqui, por medida: `n_retries = 0` e `status='ok'` em **25/25**; nenhum dos 3 padrões pertence ao stack MATLAB do piso | — |
| **G6b · gêmeo MATLAB de `UA_DD_SAEA_SONDA_OFF`** | **não** | ✅ (19 configs) | ⚪ **não-aplicável por desenho**: o moead não tem sonda; T11 §5 marca `⚪ sem sonda` | — |
| **G5 · teto/checkpoint atômico** | não (8 runners Python) | ✅ | n/a — o moead custa 5,06 s/célula (mediana) | — |
| **CONTRATO regra 11 (R4#11)** | sim | ✅ `CONTRATO_DE_DADOS.md §10.11` | ✅ **é literalmente a ação 1 pedida pela verificação adversarial `f5/adversarial/moead-M16.md`** ("dominância sobre ① é LOSSY"). Aplicada a mim mesmo, ela **corrige dois números da minha própria F5**: as quebras de monotonia do `ideal` (144 → **115**) e a leitura de M16 | falta a metade operacional que a F5.4 pediu: a ferramenta `nd_bracket_f32()` no toolbox (a regra diz "não é conclusivo", mas não dá o bracket) |
| **CONTRATO regra 12 (sonda estratificada)** | não | ✅ | ⚪ vacuamente respeitada (nenhum bloco de sonda existe aqui) | — |
| **Prova §3.1 (par G-6 COM/SEM sonda)** | **não existe** para o moead | ⚪ | ✅ **substituída por prova mais forte** — §X4: smoke × s42 da MESMA célula, **371/371 ① + 255/255 ② + 50/50 ⑥ bit-idênticas**, cobrindo o delta T11 inteiro | — |

**O padrão do c217 ("campo presente, dado sentinela") existe aqui?** Procurei em todos os campos do ⑤ e do ⑥. **Não na forma de sentinela** (não há `-1`, `NaN` mascarado ou lista vazia fingindo dado): os NULLs do moead são todos NULLs **declarados** (`tempo_fit_s`, `tempo_busca_s`, `n_acumulado`) com a regra citada, e a ③ vazia tem schema íntegro. **Mas há a variante declarativa**, duas vezes: **X3** (campo textual novo, presente, com mecanismo falso para este config) e **M10b** (campo booleano presente, com o comparador errado). Em ambos o campo *existe*, *parece verificado* e *não é*. É a mesma classe de risco que o `pmid_ids = [-1,…]` do c217, um degrau abaixo em gravidade porque nenhum dos dois alimenta análise.

---

## 5. Papel de CONTROLE (substitui a Entrega 2 — sem artigo)

Comparação canônica com paper: **N/A por desenho** (controle). A pergunta que fica é se o piso cumpre a função de régua — e a resposta, re-medida, é **sim, com um asterisco metrológico**.

| uso | veredito | número (re-medido de `metricas_finais_f52c.csv`) |
|---|---|---|
| **(i) régua CASADA por princípio (§3.2)** | ✅ com folga | c122 **24/25** (0,420) · b3 **21/25** (0,404, mesma vm3) · b1 **18/23** (0,464, mesma vm3). Todos conclusivos vs O-18 |
| **(ii) piso genérico ("o surrogate compra?")** | ✅ efeito grande | **244/305 = 80,0%** das células SA batem o moead; com o filtro O-18 (razão < 0,629), **158/305 = 51,8%** |
| **(iii) uso ISOLADO** | ⚠ enviesa | pior dos 4 pisos em **19/25**, melhor em **0/25**; rank médio **13,44/17** (mediana 15); razão moead/melhor-piso mediana **1,78×**, máx **5,10×** (BBOB_F22). Melhor piso por problema: nsga2 11× · smsemoa 8× · nsga3 6× |

**Recomendação ao D97 (inalterada desde a F5):** ler o moead como a régua CASADA de b3/c122/b1 e como **uma das quatro bordas** da banda (DEF-A13) — **nunca** como "o piso" isolado; trocar "melhor piso" por "moead" move a taxa de sucesso dos SA de 155/305 (50,8%) para 244/305 (80,0%), um viés de **+29,2 pp**.

---

## 6. Saúde (corpus principal = s42)

**1 conclusão = 1 tabela.**

| indicador | moead | nsga2 | nsga3 | smsemoa | leitura |
|---|---:|---:|---:|---:|---|
| violações de monotonia do IGD+ (475 transições) | **0** | 0 | 0 | 0 | a métrica lê o arquivo ① (D69), monótono por construção |
| **checkpoints em PLATÔ** (Δ IGD+ = 0) | **127/475 = 26,7%** | 20,8% | 22,1% | 21,1% | +5,6 pp — **a assinatura do slot perdido** (17,55% de clonagem) |
| HV = 0 | **7/25** | 5/25 | 6/25 | 5/25 | 1–2 células pior, não categoria própria (o DoE sozinho já tinha HV=0 em 10/25) |
| únicos na população final (mediana) | **13/20** | 20/20 | 20/20 | 20/20 | o colapso do T=2 (M6) |
| integridade do ⑥ (25 células, 2.589 linhas) | **0 anômalas** | — | — | — | header=1, footer=1, 0 malformadas em 25/25 |
| custo | **126,2 s = 0,035 h-core**, mediana 5,06 s/célula, 25/25 vm3 | | | | irrelevante para a projeção M8/M9 |

**Sonda:** régua Sobol **inexistente** (`nao_se_aplica`, 25/25) e bloco estratificado **inexistente** — a regra 12 do CONTRATO é vacuamente satisfeita e o moead **não entra em nenhuma comparação de sonda entre configs**. **Contrato:** `contrato_f52b.csv` tem **0 linhas moead** (o ⑤ tem `params` completo — o moead nunca esteve entre os 7 configs da não-conformidade transversal); `integridade_f52a.csv` tem 25 linhas, todas `③ VAZIO linhas=0`, **o resultado correto** (DI-13.7). **Item de contrato que a T11 fechou:** `repo_hash` vazio, que a minha F5 escalou como transversal a 666/666 células — hoje preenchido no código (X1); a s42 não muda, e essa é uma decisão de re-run do autor, não de fidelidade.

---

## 7. SCORE, recomendação e VEREDITO

### **SCORE: 9,0/10 — ACEITAR** (2 itens classe (3), ambos da camada DECLARATIVA, ambos investigados e provados benignos)

### **VEREDITO vs F5: MANTEVE** — mesma nota, composição diferente. As causas, nomeadas:

**O que EMPURROU PARA CIMA (3 causas, todas medidas):**
1. **CORREÇÃO REAL** — a T11 fechou as 3 lacunas do ⑤ que a s42 tinha (`campanha_id`, `repo_hash`, `sigma_dict`): a s42 reprova G-3 e G-7 em **25/25**, o smoke passa **5/5 + 1 n/a**. O `repo_hash` era **escalação explícita da minha F5 §5**.
2. **O MEU CLASSE (3) CAIU** — M16 (`n_front1` ±1 em 16/469) foi refutado pela verificação adversarial e eu **reproduzi a refutação com código próprio**: bracket fecha **469/469, 0 inexplicadas**. Pelo raciocínio da própria F5 isso valia +1,0 na nota.
3. **INSTRUMENTAÇÃO/EVIDÊNCIA NOVA** — a não-perturbação, que para o moead **não tinha prova** (sem par G-6), passou a ter a prova mais forte do lote: **bit-identidade das 4 camadas + do ⑥** entre o código pré-T11 e o pós-T11, na mesma célula. Isso valida retroativamente o uso da s42 como corpus de mecanismo **por medida**, não por argumento.

**O que EMPURROU PARA BAIXO (2 causas, ambas classe (3)):**
4. **AMOSTRA/QUERY NOVA** — **M10b**: a flag `frente1_excede_pop` compara com `N_nominal` em vez de `N_efetivo` (`src/experiment.m:2419`), errando em **2/25** células (DTLZ1, DTLZ3) e em 4/100 nos 4 pisos. Defeito pré-existente que a minha F5 **não mediu** (eu li a flag como se fosse contra N_ef e escrevi um intervalo errado no §M10). A T11 não o tocou.
5. **REGRESSÃO INTRODUZIDA PELA T11** — **X3**: a string `geracoes_derivadas` trocou uma fórmula errada por uma redação honesta **com um mecanismo falso para este config**, e a falha é sistemática exatamente nas 6 células M=3.

**Por que 9,0 e não 10 nem 8.** A régua do FRAMEWORK dá **9** a "tudo (1)/(2) com (3) menores já investigados e provados benignos" — é literalmente o caso: os dois (3) têm mecanismo nomeado em `arquivo:linha`, impacto quantificado (2/25 e 6/25 células), e **zero efeito sobre a busca** (nenhum dos dois campos é consultado pelo MOEA/D; a seleção D88 real fecha 25/25 em conjunto E ordem, e `n_geracoes` verdadeiro está no próprio ⑤). Não é 8 porque nenhum dos dois é "explicação plausível não provada" — ambos estão provados. Não é 10 porque, ao contrário do que a verificação adversarial projetou (9,0 → 10,0 com a queda de M16), **duas medições novas repuseram o desconto**, uma delas fabricada pela própria campanha T11. **Se o autor adotar a classe residual "(d) defeito de instrumentação/doc" que a F5.4 propôs** — e ambos os itens são exatamente isso — o config vai a **10,0** sem nenhuma outra mudança.

**Recomendação: ACEITAR.** Duas ações de custo ~zero antes do disparo das 695 células (§8), e o caveat metrológico de uso (§5(iii)) para o D97/F5.5.

---

## 8. Aspectos classe (3) 🎯 — evidência completa

### 🎯 **M10b — `frente1_excede_pop` compara com `N_nominal`, não com a população real**

- **Sítio exato:** `src/experiment.m:2419` — `'frente1_excede_pop', sum(FrontNo == 1) > N_nominal`. Linha original do commit `906343c [R1-pisos]`, **nunca alterada** (inclusive pela T11).
- **O que deveria comparar:** `N_efetivo` — o N que o `initFcn` de fato usa para cortar, corrigido pelo lattice (`MOEAD.m:25`, §6.3(b)/D20). Para M=2 os dois coincidem (20); para M=3, `N_ef = 15 ≠ 20 = N_nominal`.
- **Medida:** flag `true` em **6/25**; verdade (contra `N_ef`, recomputada em float64 com `src/problems.py`) em **8/25**. **Erra em 2/25: DTLZ1 (|F1| = 17 > 15) e DTLZ3 (|F1| = 19 > 15).**
- **Transversalidade medida:** rodei nos 4 pisos, 100 células. **nsga3 erra nas mesmas 2 células**; nsga2 e smsemoa erram **0/25** (N_ef = N_nominal sempre) ⇒ **4/100 células de piso**, exclusivamente nos dois configs com lattice.
- **Impacto delimitado e provado benigno:** o campo é descritivo (nenhum passo do MOEA/D o lê) e a **seleção real está correta** — a mesma bateria que acusa a flag reproduz `②(g=1)` em **conjunto E ordem, 25/25 em float64** (regra exata `sortrows([FrontNo,−CrowdDis,idx])`). Nenhuma célula sai da análise; nenhum número publicado muda.
- **Ação (F5.7):** trocar `N_nominal` por `N_efetivo` na linha 2419 — **1 token, 0 CPU**, e vale para nsga3 no mesmo movimento. Se o autor preferir não tocar código pré-disparo, basta a nota de leitura: *"`frente1_excede_pop` do ⑥ é contra `N_nominal`; para os pisos com lattice compare `n_frente1 > params.N_efetivo`."*
- **Evidência:** `t11_moead_parte3.py` / `t11_moead_parte3.csv` (colunas `excede`, `excede_recomp`, `f64_nfrente1`).

### 🎯 **X3 — a string `geracoes_derivadas` da T11 declara o mecanismo do NSGA-II para o MOEA/D**

- **Sítio exato:** `src/experiment.m:2453-2463`, commit `40df11a [T11-A6]`; string **compartilhada pelos 4 pisos** (verificado byte-a-byte nos 4 manifestos de smoke).
- **A afirmação falsa:** *"gerando prole em PARES — 2\*floor(N_efetivo/2) por geração (MatingPool do OperatorGA)"*. **O MOEA/D não usa `MatingPool` nem `OperatorGA`**: `MOEAD.m:40` faz `for i = 1 : Problem.N` e `MOEAD.m:45` chama `OperatorGAhalf(Problem, Population(P(1:2)))` ⇒ **1 prole por subproblema, N_ef por geração**. (O SMS-EMOA idem, `SMSEMOA.m:27-29`; a descrição vale para `NSGAII.m:26-27` e o NSGA-III.)
- **A aritmética que falsifica:** o ledger `(n_ger−1)·N_ef + k = 20D + clones + 1` fecha **25/25 com resíduo 0**; com `2·⌊N_ef/2⌋` o `k` fica **ILEGAL (k > denominador) em 5/25** — DTLZ2 k=29>14, DTLZ3 29, DTLZ4 24, DTLZ7 46, MMF16_20 34.
- **A acurácia real da "melhor aproximação MEDIDA":** no corpus oficial de 25 células, **1/25** sob a leitura literal (`n_dup` = duplicatas de prole) e **19/25** sob a leitura caridosa (`n_dup` = `manifest.cache_hits`). As 6 falhas são **exatamente** as 6 células M=3 (N_ef = 15 ímpar). A fórmula com o denominador certo — `floor((20D + clones)/N_ef) + 1` — acerta **25/25**.
- **O número do handoff:** "acerta 103/112 células da s42". Reproduzi em `data/experiments` (124 células de piso, sementes 0+42, que é onde os 112 vivem — `contrato_61.json:15` confirma "112 células de piso online, 28 cada"): a caridosa dá **113/124 = 91,1%** (moead 23/31 = 74,2%, o pior dos 4). **O placar agregado reproduz; a explicação mecânica não, e o moead é onde ela quebra.** É o caso de escola do aviso do próprio handoff §15.7: "desconfie dos meus números".
- **Impacto delimitado:** metadado textual do ⑤; nenhuma análise deriva `n_geracoes` (a própria string manda "não derive, LEIA", e o valor real está a duas chaves de distância no mesmo manifesto). Nenhuma célula, nenhuma métrica afetada.
- **Ação (F5.7):** ramificar a string por família de operador no `run_piso` — *"nsga2/nsga3: `OperatorGA` sobre `MatingPool` de N ⇒ 2·⌊N_ef/2⌋ proles/geração · moead/smsemoa: `OperatorGAhalf`, 1 prole por subproblema ⇒ N_ef proles/geração; `floor((20D + n_dup)/proles_por_geracao) + 1`"* — que acerta 25/25 no moead. **Custo: 1 edição, 0 CPU.** Deve entrar **antes** do disparo das 695 células, senão o metadado errado é gravado 695 × 30 vezes.
- **Evidência:** `t11_moead_celulas.csv` (`k_par`, `ledger_par_ok`, `T11_formula_par_ok`), `t11_formula_grid_4pisos.csv`, `t11_formula_dataexp` (no stdout de `t11_moead_parte2.py`).

---

## 9. Teto (T), armadilhas e o que este corpus NÃO permite verificar

**T — exige patch invasivo ou re-run instrumentado (rejeitados no DI-10 por patch-mínimo/D30):** o `Z` interno do PBI e a sua atualização `Z = min(Z, Offspring.obj)` · os escalares `g_old`/`g_new` · o θ=5 da penalidade angular · o `randperm(T)` que ordena o par · a **contagem BRUTA** de substituições por subproblema (M8 entrega o efeito líquido, que é limite inferior) · `ηc=20`/`ηm=20` (só `proC=1` e `proM/D=1/D` ficam provados, indiretamente) · o consumo exato de RNG. **Promovidos para fora do T por este relatório e pela F5:** `T=2`, a vizinhança `B` completa, a co-substituição do par, a seleção D88 (conjunto **e ordem**) e a taxa de clonagem em forma fechada.

**O que este corpus NÃO permite verificar, mesmo com a T11:**
1. **Nada de surrogate** — não há modelo, não há σ, não há WAPE/cobertura/erro-de-fantasia. Procurá-los aqui é categoria errada; a régua Sobol e o bloco estratificado **não existem** neste config (regra 12 vacuamente satisfeita).
2. **A validade de `N = 20`** — é **PROVISÓRIA por DI-39**; o SUB-varN (`{10,20,30,50}`) segue pendente, e todo o comportamento de M5–M8 é função dele. Não é infidelidade; é pré-registro em aberto.
3. **O comportamento em escala** — 1 semente, 25 células. A campanha M8/M9 são 30 sementes; a taxa de clonagem tem IC estreito por célula, mas a variância entre sementes não foi medida.
4. **O elo código↔run do PlatEMO** — o `repo_hash` (X1) certifica o repo-mãe; o PlatEMO é **repo aninhado** cujo estado só é coberto pelo `content_hash` (achado 🟠 da FASE C0), não pelo `repo_hash` do ⑤.

**Armadilhas confirmadas na escala (25/25 salvo indicação):**
1. **A ② é o snapshot do INÍCIO da geração** — ②(1) é literalmente o seeding; "②(g) == estado após g" reprova 25/25 falsamente; e **a população TERMINAL não existe em camada nenhuma**.
2. **`c0` do moead é `N+1`, não 1** — o primeiro semeado é lido 2× no cache (25/25); descontar N (ou 1) faz o ledger errar por 1 em todas.
3. **`ideal` NÃO é o `Z` do PBI** — é o mínimo componentwise da população (469/469) e **quebra a monotonia 115 vezes**; ler o campo como o ponto de referência do algoritmo é falso-alarme garantido. **Conte as quebras com tolerância float32** (regra 11) — sem ela dá 144, e ~29 são espúrias.
4. **`f_best ≡ ideal` e NUNCA é um indivíduo real** — vetor-utopia sintético.
5. **O hard-stop cai no MEIO da geração em 25/25** (`k ∈ [2,19]`), e a geração truncada conta em `n_geracoes`. A aritmética ingênua `20D/N` erra até +41%.
6. **DTLZ4 é irreconstituível a partir da ①** — 93 objetivos abaixo do menor denormal do float32 viram 0,0 (D53) e a dominância inverte (7 frentes/23 na F1 → 23/7). Recompute em float64; o `seeding` do ⑥ preserva os números verdadeiros.
7. **`n_front1` não é 100% recomputável e o motivo é FLOAT32, não mistério** — 439/469 exatas, 30 no bracket `[ND_f32 corrigido por colisões de linha, ND_estrito]`, **0 fora**. `|Δ| = 1` é esperado; não use o campo como invariante de gate (regra 11 do CONTRATO).
8. **`frente1_excede_pop` está contra `N_nominal`** (🎯 M10b) — nos pisos com lattice, compare `n_frente1 > params.N_efetivo`.
9. **`params.geracoes_derivadas` descreve o NSGA-II** (🎯 X3) — a própria string manda "não derive, LEIA": obedeça, e leia `n_geracoes`.
10. **As métricas oficiais não mostram a patologia** — o IGD+ lê o arquivo ① (D69), monótono: 0/25 células "regridem". A regressão real é da **população** (6/25, incluindo o MMF1 do prior) e só aparece medindo ②(último) contra o DoE.
11. **`integridade_f52a.csv` com 25 linhas moead e `sonda_f52e.csv` com 0** são ambos o resultado CORRETO, não achados.

---

*Insumos pré-computados usados sem recomputo: `f5/metricas_finais_f52c.csv` (P1/P2, HV, ranks), `f5/trajetorias/main_{moead,nsga2,nsga3,smsemoa}_*_42.json` (platôs/monotonia), `f5/contrato_f52b.csv` (0 linhas moead), `f5/integridade_f52a.csv` (25 linhas VAZIO-por-desenho), `f5/tempo_f52d.csv` (126,2 s, 25/25 vm3), `f5/adversarial/moead-M16.md` (veredito REFUTADO, reproduzido independentemente). Fontes de código lidas (READ-ONLY): `algorithms/_PlatEMO/…/MOEA-D/MOEAD.m`, `.../NSGA-II/NSGAII.m`, `.../SMS-EMOA/SMSEMOA.m`, `.../Utility functions/{OperatorGAhalf,CrowdingDistance}.m`, `src/piso_instrument.m`, `src/experiment.m:2394-2463`, `experiments.py:84-88`, `scripts/gates_proveniencia.py`, `CONTRATO_DE_DADOS.md §10`, `claude_code_context/artifacts/contrato_61.json`.*

***Scripts e evidência (único local de escrita) em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/moead/`:***
- *Scripts:* `t11_moead_bateria.py` (U1–U7, M1–M15 nas 25 células + o teste dos dois denominadores) · `t11_moead_parte2.py` (M15 com tolerância float32, previsão de clonagem por geração, bracket de `n_front1`, a fórmula da T11 nos 4 pisos) · `t11_moead_parte3.py` (seeding D88 em f64 com `src/problems.py`, co-substituição, `frente1_excede_pop`, P1/P2) · `t11_moead_parte4.py` (smoke × s42 bit-a-bit + gates de proveniência nas 25).
- *Evidência:* `t11_moead_celulas.csv` (25×70) · `t11_moead_parte2.csv` · `t11_moead_parte3.csv` · `t11_moead_clonagem_ic.csv` (IC95 Wilson × previsão fechada, 25/25) · `t11_moead_nfront1_fora.csv` (**vazio — 0 divergências fora do bracket**) · `t11_formula_4pisos.csv` · `t11_formula_grid_4pisos.csv`.
- *Nenhuma célula foi executada; `experiments.py` não foi invocado; `data/experiments` foi apenas LIDO (manifestos e `.jsonl`), nunca escrito; os shadow-roots dos gates são symlinks em tempdir.*
```