## c122-A27 — VEREDITO: CONFIRMADO (d) DEFEITO DE INSTRUMENTAÇÃO/DOC — *mas o achado NÃO é inédito nem "não-sancionado por desconhecimento": já está registrado e **decidido** (DI-42 Bloco 2 → PARTE A31, `.4=(a)`)*

**Resumo em 3 linhas**
O mecanismo do achado é real e eu o reproduzi de forma independente com três testes (teto aritmético, massa da distribuição e **custo computacional**, este último ortogonal e meu): no bloco g=1 a referência do e(z) da sonda tem **11D−1** membros, não os `n_ref=N` declarados em 25/25 manifestos e 3.537/3.537 recs. O algoritmo está correto por construção (`src/c122_thetadeadp.py:746` fotografa `pop` no topo do laço; a truncagem só ocorre em `:826`), a sonda não perturba nada (`preserve_all_rng`, `standalone_harness.py:820`) — o que é falso é **a etiqueta**, gravada como constante `MU` em 7 sítios. A tentativa de derrubar por DECISÃO não só falhou como achou o oposto: a torre já confirmou este item textualmente em `REGISTRO_DECISOES_IMPLEMENTACAO.md:1945` e o **autor já ratificou em 2026-07-28** (`.4=(a) fix c122 g=1 no T11 + caveat s42`).

**O que tentei para derrubar (os 4 passos, com os números)**

1. **Por DECISÃO — falhou, e com reviravolta.** Nenhuma D/DI/CONTRATO/`sigma_dict` sanciona a exceção; ao contrário, `DI-16.2` (registro:644-655) crava a referência «de tamanho **FIXO**… o arquivo inteiro cresceria de 11D−1 a 31D−1 e o score subiria SÓ pela escala, **falsificando a curva**» e o `CONTRATO_DE_DADOS.md:195` manda «Logar `n_ref` no jsonl». **Porém**, o achado **já existe** na base: registro:1945 — «c122 **bloco de sonda g=1 usa a população NÃO-truncada** e grava `n_ref=MU` errado (quebra a comparabilidade prometida pela DI-16.2 — **este é o único do bloco 2 que é defeito de instrumentação, não comportamento**)» — e registro:1992 lista-o entre as **6 decisões genuínas** DI-42.2-.7, com a ratificação do autor na PARTE A31: «**.4=(a) fix c122 g=1 no T11 + caveat s42**». Logo, a classe **(3) "inexplicado"** é imprecisa em dois sentidos: o mecanismo está *explicado* (é o A21, Step 14 ausente, 🟠 sancionado) e o item já tem *dono, decisão e cartão* (T11). O que a F5.4 acrescenta é a quantificação e a especificação do fix.
2. **Por QUERY — falhou; a evidência é reprodutível e sobrevive a formulações novas.** Re-executei com código próprio (`refuta_A27.py`, 25 células, `A27_blocos.csv` = 3.537 blocos): bloco-1 excede o teto H0 (=2N) em **22/25**; excede o teto H1 (=2·(11D−1)) em **0/25**; blocos g≥2 violando 2N: **0/3.512**. Bato o analista dígito a dígito (ZDT1 369,388 · WFG1 342,249 · DTLZ7 325,247 · ZDT3 289,150 · WFG2 254,918 · DTLZ4 235,121 · ZDT6 209,659). Massa, não outlier: fração de pontos do bloco-1 acima de 2N = **mediana 0,706** (mín 0,074 WFG… DTLZ2; máx **0,916** WFG1; ZDT1 0,892 ≡ os 89,2% do relatório). **Formulação nova nº 1 (a mais apertada): ZDT4 tem UMA rede só** (`fit_inicial: p_net=True, s_net=False`; `accs_s` NULL em 201/201) ⇒ o teto deixa de ser 2·n_ref e vira **n_ref exato**: bloco-1 `max e(z) = 108,944778` = **99,949% de 109 = 11D−1** (folga 0,055), com 68,5% dos 2.000 pontos acima do teto H0 (=11); e nos g≥2 `max e(z) = 11,000000` **exato**, 0/202.000 violações. Identificação bilateral do tamanho da referência, sem tolerância a arbitrar. **Formulação nova nº 2 (ortogonal, não usa e(z)): custo.** `tempo_pred_sonda_s(g=1)/mediana(g≥2)` vai de **2,42 a 31,57** (ZDT1 25,404 s vs 0,805 s) e correlaciona **0,9990** com a razão prevista por H1 = (11D−1)/N; normalizando pelo custo **por ponto-de-referência**, g=1 e g≥2 custam o mesmo (razão 1,034–1,345, mediana **1,139** — o resíduo é warm-up do 1º forward). Sob H0 a razão teria de ser ≈1,0 em todas. Testes que tentei e **não** discriminaram (registro honesto): ρ(sonda, verdade) com referência ②(g−1) vs ②(g) — 16/25 a favor de `prev`, |Δ| ≤ 0,054 (as duas populações diferem em ≤1 membro).
3. **Por CÓDIGO — falhou: é comportamento por construção, incondicional.** `src/c122_thetadeadp.py:746` `estado_sonda.update(pop=list(pop), …)` no topo do laço → `:750` `sonda_due` → `:753` `_sonda_predict`, cujo kernel (`:545-554`) faz `e_z += Σ_j m` sobre **|pop|** (daí o teto 2·|ref|); a truncagem só acontece em `:826` `pop = sel_scalar_dea(pop + [escolhido], MU)`. Não há ramo em D nem em g — o que fecha também as **3 células D=2** (MMF1/MMF4/MMF11_L), onde o teto (22 vs 42) não discrimina e o teste de custo apenas corrobora (2,42–2,57 vs 1,91 previsto). O `n_ref=MU` constante é escrito em **7 sítios**: `:660` (sigma_dict), `:698` (header), `:756` (motivo da sonda), `:865` (decisão), `:910` (finalProbe), `:946` (manifesto), `:965` (retorno).
4. **Controles.** *Negativo:* c122 é o **único de 24 configs** cujo manifesto sequer declara `sonda.n_ref` (varredura em `resultados_experimentos/*`) — logo não é convenção do projeto. *Positivo:* o instrumento do b4 grava o tamanho **REAL** por geração (`src/b4_instrument.m:107`: `'n_refs', double(size(Ref.decs,1))`) — a convenção do projeto é logar o tamanho efetivo, e o c122 é a exceção. *De contenção:* `|②(g=1)| = N` em **25/25** ⇒ a referência do bloco-1 **não** é recuperável da ②, mas é recuperável **exatamente** da ① `fase='init'` (11D−1 linhas, 25/25) ⇒ correção pós-hoc sem re-execução.

**Por que não caiu**
Porque as três provas são independentes entre si e uma delas é livre de modelo e quase sem folga (ZDT4: 108,9448 contra o teto exato 109, e 11,000000 exato nos demais blocos), e porque o código produz o comportamento sem ramo condicional. E porque a única rota de refutação que restava — "há decisão que sanciona" — devolveu o inverso: há decisão que **confirma** o defeito e manda corrigi-lo.

**Enquadramento e bússola D29**
**(d) defeito de instrumentação/doc**, não (a)/(b)/(c). Não é (a) bug: a sonda roda sob `preserve_all_rng()` e o `tempo_geracao_s` a exclui — zero efeito na busca, no orçamento e nas invariantes de timing (A29 100%). Não é (c) dado corrompido: o e(z) do bloco-1 é numericamente **correto** para a referência que de fato foi usada; só o rótulo mente, e a referência verdadeira é reconstruível bit-a-bit. Não é (b): o desenho pretendido (`DI-16.2`, tamanho fixo) **não** foi cumprido no bloco-1. **Bússola D29: fora da bússola** — a sonda é extensão nossa (🟢 verde, declarada), sem prescrição no paper; o mecanismo subjacente que a dispara (Step 14 ausente do `scalar_dom_ea_dp`) é o A21, já 🟠 laranja (detalhe de implementação → segue o CÓDIGO) e sancionado. **Nenhuma consequência de fidelidade ao artigo.**

**Impacto**
Nenhum número publicado muda e **nenhuma célula sai da análise**. Atinge **25 de 3.537 blocos (0,707%)** e **50.000 de 7.074.000 linhas de ③-sonda (0,707%)**; `sonda_f52e.csv` tem 0 linhas c122 e as métricas (IGD/IGD+/HV/spacing) vêm da ①/⑦. Verifiquei o artefato do analista: `bateria3_sonda_verdade.py` já trata g=1 com `np.arange(ini)` (=11D−1) e o resumo filtra `geracao>1` (**3.512** blocos) ⇒ a §6(ii) do relatório c122 está limpa (a prose "3.537 blocos" é imprecisa, o cálculo não). Risco residual, único e real: **leitura futura da curva de e(z)** — a queda do bloco-1 para o bloco-2 é de **mediana 16,5×** e chega a **72,3×** (ZDT3) / 61,6× (ZDT1); lê-la como "o surrogate degradou" é erro. E, se não for corrigido, o M8 replica o rótulo errado em 25 células × 30 sementes = **750 blocos**. Custo colateral trivial já pago: **210,2 s** de sonda a mais nos 25 blocos-1 (fora do `tempo_geracao_s`).

**Ação para a rodada perfeita (F5.7 / cartão T11) + custo**
Já é item decidido do T11 (`.4=(a)`); a F5.4 entrega a especificação exata — **sem re-execução, 0 h-core**:
1. `src/c122_thetadeadp.py:746` — capturar `n_ref_real = len(pop)` e propagá-lo: motivos `:756` e `:910` passam a `f"…; n_ref={n_ref_real}"`, mais um campo **literal** `n_ref` no evento `sonda` (hoje `standalone_harness.py:860-866` grava `n_pontos`, não `n_ref` ⇒ +1 kwarg pass-through no helper). ~6 linhas, **~20 min**.
2. `:946` manifesto — trocar `"n_ref": MU` por `{"n_ref_pos_truncagem": MU, "n_ref_bloco1": 11*D-1, "n_ref_regime": "len(pop) por bloco"}`; idem `:965`. **~5 min**.
3. `:660` `sigma_dict` — declarar a exceção do bloco-1 e apontar o A21 como causa. **~5 min**.
4. Teste unitário: `n_ref(g=1) == 11D−1 ∧ n_ref(g≥2) == N` no smoke. **~10 min**.
5. **Rejeitar** a alternativa "emitir a sonda após a truncagem": mudaria o que a sonda mede ("o modelo COM QUE esta geração decide" — no g=1 os 7.000 offspring nascem mesmo da população de 11D−1), quebraria a cadência g=1 comum aos 18 configs e exigiria re-rodar. 
6. Dado da s42 (congelado): caveat no dossiê D97 + coluna derivada `n_ref_real = 11D−1 se g==1 senão N` nos scripts da R4, e **descartar o bloco-1 de qualquer curva de e(z)** (armadilha nº 3 do §9 do relatório c122). **0 min de CPU**.
7. **Bônus para a torre (achado meu, colateral):** a `DI-42.2` («evento `sonda` do ⑥ não grava o campo `fe`») lista o **c122 entre os afetados — e isso é FALSO**: os recs trazem `"fe": 329/330/332…` (runner `:752` passa `fe=bud.fe`; harness `:860` grava). Corrigir o escopo da DI-42.2 antes que o T11 "conserte" o que não está quebrado. **~5 min**.

**Evidências preservadas** em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/baterias/f54/c122-A27/`: `refuta_A27.py` → `A27_celulas.csv` (25 células × 24 col.) + `A27_blocos.csv` (3.537 blocos) + `log_refuta_A27.txt` · `controles_A27.py` → `A27_controles.csv` + `log_controles_A27.txt` (massa, custo por referência, |②(g1)|, varredura transversal dos 24 configs) · `zdt4_umanet_A27.py` → `log_zdt4_umanet_A27.txt` (o teste de teto exato com uma rede só). Nenhuma escrita fora dessa pasta; dados de `resultados_experimentos/` acessados em modo leitura.