Análise completa. Segue o relatório.

# RELATÓRIO DE FIDELIDADE T11 — `nsga2` (NSGA-II piso online, PlatEMO 4.15) · protocolo v1.1 · corpus s42 (25 células) + smoke T11 (1 célula)

**Escopo re-medido do zero**: as **25 células** de `resultados_experimentos/nsga2/{problema}/42/` (10.856 FE · 376 gerações · 7.520 linhas da ② · 732 registros de guard) **+ a célula do smoke T11** `evidencia_T11/smoke_matlab/experiments/main/nsga2/exp_main_nsga2_DTLZ2_42.*`. Nada foi citado da F5 sem re-medição; onde a F5 errou, está declarado (§10).
**O achado que organiza tudo**: o smoke T11 e a célula da s42 são **a MESMA célula** (`main/nsga2/DTLZ2/42`), e o dado prova que o código de hoje reproduz a busca de então **bit-a-bit** — ① e ② com **0 colunas divergentes de 21+5**, `sha256(X)` e `sha256(f)` idênticos, e **0 de 42 registros do ⑥ semanticamente divergentes**. O nsga2 não entra nos pares g6 (não tem sonda), mas ganhou uma prova mais forte que o par: um **par cross-campanha**.

---

## 1. Ficha do mecanismo

**NSGA-II stock do PlatEMO 4.15** (`repos.lock` → `b686ca2`), **sem uma linha de patch** (`params.patches = "NENHUM — piso e stock do PlatEMO 4.15 por design"`, 25/25 + smoke). Seleção por dominância de Pareto (NDSort) + crowding distance; variação pelo `OperatorGA` stock do Balde C (SBX `proC=1, dis_c=20` + PM `proM=1, dis_m=20`); substituição elitista geracional (P∪Q → N).

**Papel (§3.2/D25)**: **régua**, não competidor — responde *"o surrogate compra alguma coisa?"*. Casado por princípio de seleção ao eixo **EA-Dominância (b4, c217, c141, e74) + BO-Melhoria-de-Pareto (c238)**; com os outros 3 pisos forma a banda min–máx dos BO-especiais (DEF-A13/D25).

**Divergências sancionadas nomeadas** (todas do NSGA-II *stock*, nenhuma do "artigo" — não há artigo): `maxFE = 31D−1` com hard-stop (**D21/D61**) · DoE `11D−1` injetado do artefato (**D63**) · seeding por NDSort+crowding com desempate por índice (**D88**) · `N = 20` cravado e **provisório** (**§3.2 / D65 / DI-39**) · duplicata gasta SLOT, não FE (**D89**) + dedup por X (**D57**) · export float32 sem arredondamento (**D53**) · ③ vazia, `tempo_fit_s` NULL, série §17.6 vazia (**DI-13.2/13.7/16.1**) · sem ⑦ no regime online (**DI-08**) · sem sonda (**CONTRATO §6 l.210**).

**Gabarito**: **controle sem artigo**. A cadeia de precedência §0 colapsa em `dados → params/manifesto → bundle alg_pisos_online.md → decisões D/DI`. Classe **(1)** = *idêntico ao NSGA-II stock*; classe **(2)** = *desvio do stock exigido pelo protocolo do estudo, com decisão citada*.

**O delta T11, medido campo a campo** (`smoke_vs_s42_nsga2.py`): o ⑤ ganhou **`campanha_id`** e **`sigma_dict`** (chaves NOVAS), **`repo_hash`** `''` → `195f64ed0a69dc3f6c64c66912076e243be90c49`, **`schema_version`** 1 → 2, e a string **`params.geracoes_derivadas`** foi re-escrita. **Nada mais.** O ⑥ é idêntico; a ①/② são idênticas; `src/piso_instrument.m` (o writer do ⑥ dos pisos) **não foi tocado pela T11** (`git log` → último commit `145fd3d [DI09-R1c]`, pré-T11).

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.1 Tabela-resumo (26 aspectos · 1 linha cada)

Verif.: **D** direta · **DD** direta-declarativa (config-echo; elo coberto por `anchors.json`+`repos.lock`) · **NV** não-verificável.

| # | aspecto | classe | verif. | resultado-síntese (25 células s42 · 376 gerações · 10.856 FE · + smoke T11) |
|---|---|:---:|---|---|
| **T1** | **Não-perturbação cross-campanha (smoke T11 ≡ s42)** | **(1)** | D | ① e ② **bit-idênticas** (0 col. divergentes de 21+5; `sha X`=`1dadd0a6220960ca`, `sha f`=`bfc193460c79ed0c` nos dois); ⑥ com **0/42 registros divergentes** fora de `ts`/`tempo_geracao_s`; `n_geracoes`=13, `cache_hits`=25, `fe_final`=371 iguais |
| U1 | Orçamento FE e término | (2) D21/D61 | D | `len(①)=31D−1=maxfe=fe_final` **25/25**; `fe_index` denso 0-based 25/25; 10.856 `solution_id` únicos; `footer.termino='normal'` 25/25; 1 footer 25/25; `hard_stop` em 24/25 (MMF4 fecha exato) |
| U2 | DoE 11D−1 LHS do artefato | (2) D63 | D | **ΔX = 0,0 EXATO** 25/25; `doe_hash`≡sidecar 25/25; `cp_init=true` 25/25; init = prefixo contíguo 25/25; fases={init,opt} |
| U3 | Ausência de surrogate + **`sigma_dict` agora DECLARADO** | (2) DI-13.2/13.7/16.1 + **G-7** | D | ③ **0 linhas** 25/25 e no smoke; `tempo_fit/busca/pred_sonda` NULL em **376/376** (e 13/13 no smoke); `n_acumulado` NaN 376/376; `fit_series=[]` 25/25; **`sigma_dict` ausente em 25/25 da s42 → presente e `nao_se_aplica` no smoke** |
| U4 | Sonda inexistente (U4+U5+U6) | (2) CONTRATO §6 l.210 | D | `sonda={status:'nao_se_aplica', n_blocos:0, n_linhas:0}` **25/25 + smoke**; **0** registros `rec:'sonda'`; `sonda_f52e.csv` 0 linhas nsga2 |
| U7 | Invariantes de timing ④↔⑥↔⑤ | (2) DI-13.10 | D | `max\|Δ tempo_geracao_s\|` = **1,34e-08 s** em 376/376; `geracao` sequencial 25/25; Σ≤total 25/25; `tempo_aval_real ≤ Σ` 25/25 (mediana 0,257) |
| U9 | Guards ≡ ⑤ ≡ footer · bloco de seeding | (2) D89/D57 | D | `cache_hit(⑥)≡manifest≡footer` **25/25**; **732 guards = 708 `cache_hit` + 24 `hard_stop`**; bloco de seeding = **21 = N+1** em 25/25, com 20 `solution_id` distintos, todos `< 11D−1` |
| U10 | Aritmética entre camadas · `n_ger = D+1` | **(1)** | D | ② com 20 linhas/geração 25/25 (7.520); `manifest ≡ len(⑥ gen) ≡ len(④) ≡ n_grupos(②)` 25/25; **`n_ger = D+1` EXATO 25/25**; `set(②) ⊆ set(①)` 25/25 |
| U12 | Camada ⑦ ausente | (2) DI-08 | D | 6 camadas 25/25 + smoke; gate G-4 "6/6 camadas" OK |
| **P1** | **Seeding D88 — a query-joia do piso** | (2) D88/§3.2 | D | Recomputo NDSort+crowding+índice reproduz **②(g=1) BIT-A-BIT em 24/25 via ① float32**; DTLZ4 restaurado em **float64** (fórmula validada **393/393**, `n_frentes` 7 / `n_frente1` 23 ≡ ⑥, seleção 20/20) ⇒ **25/25**; `frente1_excede_pop` exato 25/25 (True nas 6 com \|F1\|>20) |
| P2 | `N = 20` cravado · `N_efetivo` | (2) D65/§3.2/§6.3 + DI-39 | DD+D | `N_nom=N_ef=20` no ⑤/header/footer 25/25; `n_pop=20` em **376/376**; contraste re-medido nos 100 pisos: **M=3 → nsga2 20, smsemoa 20, nsga3 15, moead 15** |
| P3 | Gerações derivadas · **string I-13 re-escrita** | (2) §3.2 + **I-13/A24** | D | infill = **20D exato** 25/25; a fórmula NOVA acerta **24/25** no nsga2 e **93/100** nos 4 pisos (não 103/112 — §10); a antiga acerta **0/100**; a forma fechada exata do nsga2 é **D+1 (25/25)** |
| P4 | Elitismo P∪Q→N e turnover | **(1)** | D | `②(g+1) ⊆ ②(g) ∪ {avaliados na janela}` em **351/351 transições, 0 violações**; turnover nunca zero (mín 3 em WFG5/WFG9, máx 14 em ZDT3, média **8,43/20**) |
| P5 | Monotonia do `ideal` / `f_best` | **(1)** | D | **0 violações em 787 transições** (geração×objetivo); 299 melhoras (38,0%) / 488 estagnações (62,0%); `f_best ≡ ideal` 25/25; `nadir_front1 ≤ nadir_pop` 25/25 |
| P6 | `n_front1` e a convergência | **(1)** | D | `n_front1 ≤ N` **376/376**; 17/25 saturam, **6 já na geração 1**; 8/25 nunca (BBOB_F17 13, F49 15, F55 13, MMF1 14, MMF11_L 14, WFG2 17, ZDT4 10, ZDT6 10) |
| P7 | Clone-hazard D89 (slot × FE) | (2) D89 | D | **LEDGER FECHA 25/25 por DOIS caminhos independentes**: `resto ≡ dups nos rounds completos` (por ordem de linha) e `resto ≡ Σ(20−Δfe_g)`; **Σ resto = 175**; 183 dups de prole em 7.020 slots = **2,607%** (0,00% MMF4 → 8,50% BBOB_F1) |
| P8 | ② ≡ população do ⑥ | **(1)** | D | `ideal`/`nadir_pop` recomputados do join ②→① com erro **relativo ≤ 5,717e-08** (ULP float32) 25/25; `n_front1` bate 326/376, as 50 divergências são 100% do export (§N2) |
| P9 | Operadores do Balde C (SBX/PM) | **T** | NV | `dis_c=20`/`dis_m=20`/`proC=1`/`proM=1` só existem como eco de string; a grandeza não é exportada |
| P10 | Miolo do stock (torneio + `EnvironmentalSelection`) | **T** | NV | crowded-comparison, `randperm`, truncagem interna: sem assinatura recuperável; só o RESULTADO (②) é auditável |
| P11 | Casamento NSGA-II ↔ dominância/Pareto | **(1)** | DD | `principio='dominancia/Pareto'` + `casamento='EA-Dominancia (b4,c217,c141,e74) + BO-Melhoria-de-Pareto (c238) — §3.2'` idênticos 25/25 + smoke |
| N1 | Stock puro (sem patch, sem parâmetro) | **(1)** | DD | 6 literais idênticos em 25/25 + smoke (`patches`, `parameter`, `surrogate`, `algo_version`, `header.piso=true`, `header.surrogate=false`) |
| **N2** | **Colapso de dominância pelo export float32** | (2) D53 → **regra 11** | D | **50/376 gerações (13,3%)** divergem; **49 ENCOLHEM, 1 aumenta** (MMF4 g2: 16×15); DTLZ4 inverte 7/23 → 23/7; **impacto no endpoint: IGD+ e HV BIT-IDÊNTICOS** sob a troca de filtro; **virou a REGRA 11 do CONTRATO (T11/A37.2)** |
| N3 | Par X duplicado na ① do MMF4 | (2) D53/D57 | D | **1 par em 10.856 linhas** (sid 19 `init` × sid 30 `opt`), X e f bit-idênticos em float32 (`b6e707bf0cbb313f` / `b6e7073f0940663f`) |
| N4 | Bounds, NaN e sanidade numérica | **(1)** | D | **0 NaN** em X e f nas 25 células; X dentro dos bounds 25/25; nenhum guard além de `cache_hit`/`hard_stop` |
| N5 | Contrato do ⑥ na linha "pisos" | **(1)** | D | os 4 campos exigidos presentes em **376/376** + 13/13 do smoke, mais `nadir_front1` de brinde — **mas o gate G-7 novo só cobre 2 deles** (§4.5, provado por mutante) |
| N6 | Elo código↔run · **`repo_hash`/`campanha_id`** | (2) **I-09/B-03** | DD+D | `repo_hash=''` em **25/25** da s42 → **preenchido no smoke** com commit real (`195f64e`, verificado no `git log`); `campanha_id` e `schema_version=2` novos; **o HEADER do ⑥ segue sem `run_id`/`ambiente`/`params`/`sigma_dict`** (CONTRATO §6 l.296) em 25/25 **e no smoke** |

### 2.2 Blocos narrativos

**T1 — A não-perturbação, provada por um par cross-campanha (o aspecto NOVO).** (a) Não há prescrição de artigo: o que está em jogo é a premissa da própria tarefa — *a s42 mede o mecanismo de hoje?* (b) A T11 declara que só acrescentou instrumentação READ-ONLY e prova isso em 19/19 configs pelos pares `g6_com`×`g6_sem`; o nsga2 **não está nesses pares** (§5 da T11: `⚪ sem sonda`), então a premissa chegaria aqui por analogia. (c) Mas o smoke MATLAB do nsga2 caiu em `main/nsga2/DTLZ2/42` — **exatamente uma das 25 células da s42**. Comparei-as: a ① tem 371 linhas dos dois lados e **nenhuma das 21 colunas difere** (`sha256(X)[:16] = 1dadd0a6220960ca` e `sha256(f)[:16] = bfc193460c79ed0c` iguais); a ② tem 260 linhas e **nenhuma das 5 colunas difere**; o ⑥ tem 42 linhas dos dois lados e, ignorando `ts` e `tempo_geracao_s`, **0 dos 42 registros diverge** — inclusive o `rec:'seeding'` (`n_doe=131, n_frentes=6, n_frente1=40, frente1_excede_pop=true`), os 26 guards (idênticos, `x_key` a `x_key`) e o footer. (d) O mecanismo causal é trivial e verificável no código: os três commits T11 que tocam `src/experiment.m` na região do piso (`4796a03` sigma_dict, `40df11a` string do `geracoes_derivadas`, `83dbdac` repo_hash) alteram **só o struct do manifesto**, e `src/piso_instrument.m` não foi tocado pela campanha. (e) **(1)** — direta. **Isto é mais forte que o par g6: prova reprodutibilidade DA BUSCA entre duas campanhas separadas por 4 dias e um retrofit de instrumentação, e não apenas a inércia de uma sonda.**

**U1 — Orçamento e término.** (a) O NSGA-II canônico não tem orçamento prescrito; o que faz as vezes de artigo é o precedente Knowles/ParEGO do §3.2. (b) A SPEC impõe `maxFE = 31D−1` com hard-stop exato (D21/D61) — 61 (D=2) a 929 (D=30), fração DoE/orçamento constante em 35,3%. (c) `len(①) == 31D−1 == maxfe == fe_final` em **25/25**; `fe_index` denso 0-based 25/25; **10.856 `solution_id` únicos**; `footer.termino='normal'` 25/25 com **exatamente 1 footer** (o `mapa_termino.json` agora declara isto normativamente: `{"familia":"matlab","evento_geracao":{"rec":"nsga2_gen"},"campo_termino":"termino","n_footers_esperado":[1]}`). O guard `hard_stop` em **24/25**; a exceção é o MMF4. (d) O invariante forte re-medido: **`hard_stop` presente ⟺ `resto > 0`**, verdadeiro em **25/25** — o guard é a assinatura do clip do lote de 20 no meio, e o MMF4 não o tem porque seus 2 rounds fecham 61 FE cravados com **zero** duplicata de prole. (e) **(2)** D21/D61 — direta.

**U2 — DoE do artefato.** (a) O stock inicializa com `Initialization` aleatória própria. (b) D63 substitui por um LHS `11D−1` carregado de `data/doe/{problema}/doe_{problema}_42.parquet`, nunca regenerado — é o que faz todos os 21 configs partirem do mesmo ponto. (c) Comparação bit-a-bit do artefato (float64→float32) contra `fase=='init'`: **max\|ΔX\| = 0,0 EXATO nas 25 células**, não "≤ eps"; `doe_hash` do manifesto ≡ `doe_hash` do sidecar em 25/25; `cp_init=true` 25/25; init é prefixo contíguo (`fe_index` 0..11D−2) 25/25. (d) A injeção acontece antes do `Solve`, no espaço do problema, sem re-escala: não há aritmética entre artefato e ①, por isso o zero é exato. (e) **(2)** D63 — direta.

**U3 — A ausência de surrogate, e a lacuna que a T11 fechou.** (a)/(b) O piso é MOEA **puro**. A DI-13.7 estreitou o assert `piso_com_surrogate` à ③; a DI-13.2 tornou `tempo_fit_s` NULLABLE só para os 4 pisos ONLINE (o piso OFFLINE moead_media **tem** GP — DI-16.1). (c) Cinco checagens fecham na s42: ③ com **0 linhas em 25/25**; `tempo_fit_s`/`tempo_busca_s`/`tempo_pred_sonda_s` **NULL em 376/376** linhas do ④ (e os três homólogos NULL no `timing` do ⑤); `n_acumulado` **NaN em 376/376** (idioma double+NaN do MATLAB, D-02/DI-21 — não `0.0`, que significaria "treinou e custou zero"); `fit_series=[]` 25/25; **`sigma_dict` AUSENTE em 25/25**. No smoke T11, as quatro primeiras são idênticas (③ 0 linhas, 13/13 do ④ NULL, 13/13 NaN, `fit_series=[]`) e a quinta **mudou**: `sigma_dict = {status:'nao_se_aplica', motivo:'piso ONLINE = MOEA puro: a ③ existe (contrato de camadas) mas nasce VAZIA — nao ha modelo cujas colunas descrever', terceira:'0 linhas por desenho'}`. (d) O racional está escrito no próprio commit (`4796a03`, `experiment.m:2501-2510`): *"omiti-lo dava 112 celulas (28 x 4 pisos) sem a chave, e quem itera as chaves do §5 nao distingue 'nao se aplica' de 'esqueceram'"* — é o mesmo argumento do `tempo_fit_s=NULL` e do bloco `sonda`: **declarar a não-aplicabilidade é informação; omitir é silêncio**. Foi exatamente a leitura da F5 (armadilha nº1: "qualquer bateria que exija `sigma_dict` reprova 25/25 falsamente"), e a T11 removeu a armadilha na fonte. (e) **(2)** DI-13.2/13.7/16.1 + G-7 — direta.

**U4 — Sonda: N/A por desenho, declarada.** (a)/(b) A tabela do CONTRATO §6 (l.210) lista os 4 pisos ONLINE como *"NÃO TÊM SONDA (sem modelo)"*. (c) O bloco `sonda` do ⑤ traz em **25/25 + smoke** `status='nao_se_aplica'`, `n_blocos=0`, `n_linhas=0` e o motivo literal; **0** registros `rec:'sonda'` nos 26 ⑥. O código explicita a sobrescrita (`experiment.m:2494-2500`): sem ela o `fill_manifest_timing` gravaria `status='artefato_ausente'`, *"o que seria FALSO em dois sentidos: o artefato EXISTE (25/25 problemas do grid) e a ausência aqui é por DESIGN"*. (d) Absorve U4 (cadência), U5 (join posicional) e U6 (WAPE/cobertura) de uma vez, e **torna a regra 12 do CONTRATO vacuosa neste config**: não há bloco `sonda` nem `sonda_estratificada` a separar (medido: `'sonda_estratificada'` não aparece em nenhum dos 25 manifestos nem no smoke). (e) **(2)** CONTRATO §6 — direta.

**U7 — Invariantes de timing.** (a)/(b) DI-13.10 pede `fit+busca ≤ tempo_geracao_s`; com fit e busca NULL a desigualdade é vacuosa, e o que resta é a consistência ④↔⑥↔⑤. (c) Casei linha a linha o `tempo_geracao_s` do ④ com o do evento `nsga2_gen`: **376/376**, `max\|Δ\| = 1,34e-08 s` (ULP do float32 do parquet contra o float64 do JSON); `geracao` sequencial 1..n 25/25; `Σ tempo_geracao ≤ timing.tempo_total_s` 25/25; `tempo_aval_real_s ≤ Σ` 25/25 (fração mediana 0,257). (d) O resíduo é a informação: **Σ tempo_geracao = 15,72 s contra 128,65 s de wall — 87,8% do relógio é overhead fora do laço evolutivo** (mediana 4,11 s/célula). MMF1 foi a primeira célula da campanha e custou **19,36 s** (arranque frio + JIT); ZDT3 custou **1,02 s** numa sessão quente. O smoke T11 da mesma célula DTLZ2 custou **4,312 s** contra **4,329 s** na s42 — 0,4% de diferença, i.e. o wall também é reprodutível. **Confirma a armadilha: `~1 s/célula` é startup do MATLAB, não "rápido demais".** (e) **(2)** DI-13.10 — direta.

**U9 — Guards e a anatomia do bloco de seeding.** (a)/(b) D89 (duplicata não gasta FE, gasta o SLOT) + D57 (dedup por X bit-a-bit). (c) `cache_hit` do ⑥ ≡ `manifest.cache_hits` ≡ `footer.cache_hits` em **25/25**; **732 registros de guard = 708 `cache_hit` + 24 `hard_stop`**; todo `solution_id` de cache-hit existe na ① 25/25. O achado estrutural: em **25/25 células existem exatamente `N + 1 = 21` cache-hits com `fe == 11D−1`** — os 20 pontos eleitos pela D88 re-submetidos ao avaliador ao construir a população (0 FE, como `params.seeding` promete), **mais uma duplicata de arranque** (o primeiro guard logado duas vezes com o mesmo `solution_id` e o mesmo `x_key`). Os 21 têm **20 `solution_id` distintos**, todos `< 11D−1`, em 25/25. Re-medi a generalização nos 4 pisos: o bloco é **`N_efetivo + 1`** — 21 onde N_ef=20 e **16** onde N_ef=15 (nsga3/moead em M=3). (d) É o `c0 = 1` documentado no b1/b3, empilhado sobre o bloco de N: propriedade do wrapper de FE, transversal ao stack MATLAB. Sem descontá-lo o ledger erra por 21 em toda célula. (e) **(2)** D89/D57 — direta.

**U10 — `n_geracoes = D+1` é lei, e agora com o mecanismo provado.** (a)/(b) Sem mudança: a ② é o snapshot da população por geração. (c) Três fechamentos: ② com `n_geracoes` grupos de **exatamente 20 linhas** (7.520 em 25/25); `manifest.n_geracoes ≡ len(⑥ gen) ≡ len(④) ≡ n_grupos(②)` 25/25 (376=376=376=376); `set(②.solution_id) ⊆ set(①)` 25/25; e **`n_ger = D + 1` EXATO em 25/25**. (d) O mecanismo, agora fechado: o registro `g=1` sai com `fe = 11D−1` (verificado 25/25) e cada round de prole emite um registro; o incremento de FE por geração é **≤ 20** (medido: mín 16, máx 20; **242 de 351 transições com exatamente 20**), e `Σ(20 − Δfe_g) = 175 ≡ Σ resto` (25/25 por célula). Com `p < 20` duplicatas, os D rounds completos consomem `20D − p` FE, o round D+1 arranca, consome as `p` restantes e **morre no `hard_stop` antes de emitir registro** ⇒ **registros = 1 + D**, sempre. Quando `p = 0` (MMF4) os D rounds fecham exato e não há round extra — o mesmo `1 + D`. (e) **(1)** — direta. **Ponto para o protocolo geral: aqui NÃO há off-by-one na ② (≠ c217) — provado pelo recomputo de ideal/nadir a ULP (§P8).**

**U12 — Sem camada ⑦.** (a)/(b) A ⑦ é o endpoint do regime offline (DI-08); em online toda avaliação é real e o endpoint é a ① (D69/D70). (c) 6 camadas em 25/25 e no smoke (gate G-4 "6/6 camadas" OK). (d) É por isso que o nsga2 não sofre o achado sistêmico dos 5 offline (métricas empatam por desenho porque a ① é o mesmo dataset): aqui a ① é o produto do run. (e) **(2)** DI-08 — direta.

**P1 — A QUERY-JOIA: a seleção D88, reproduzida bit-a-bit fora do MATLAB.** (a) O §3.2 prescreve *"parte dos mesmos 11D−1 pontos LHS, iniciando a evolução com os melhores por não-dominância"*; a **D88** fecha a lacuna do desempate: *"crowding distance, determinístico (o critério nativo do NSGA-II) — dois runs da mesma semente selecionam o mesmo subconjunto"*. (b) Sem regra de desempate a régua deixaria de ser reprodutível. (c) Implementei NDSort + CrowdingDistance (normalizada por fronteira, extremos ±∞) com `lexsort(front↑, cd↓, índice↑)` sobre o bloco `init` da ① e comparei o conjunto dos 20 eleitos contra `②(g=1)`: **coincidência bit-a-bit em 24/25**, e `n_frentes`/`n_frente1` do `rec:'seeding'` reproduzidos exatamente nas mesmas 24. A 25ª é o **DTLZ4**, e o motivo é do export: recomputando f em **float64** a partir do X do artefato (fórmula PlatEMO `x^100`, validada **393/393 bit-idêntica após o cast**), obtenho `n_frentes=7` e `n_frente1=23` — **idêntico ao ⑥** — e a seleção volta a bater **20/20**; sobre a ① float32 dá 23 fronteiras / 7 na frente-1 e acerta só 10/20. Logo **a joia fecha em 25/25**. A flag `frente1_excede_pop` é exata em 25/25 — True exatamente nas 6 células com \|F1\|>20 (MMF16_20 50, DTLZ2 40, WFG5 36, WFG4 30, DTLZ4 23, DTLZ7 21), que são as em que o desempate por crowding é exercitado NA FRENTE 1. (d) O `initFcn` entrega os `11D−1` já avaliados, o PlatEMO roda seu `EnvironmentalSelection` uma vez para cortar a 20, e o resultado é função determinística de (F do DoE, N). Reproduzi-la fora do MATLAB **prova que o corte é o do NSGA-II nativo e não uma heurística ad-hoc**. (e) **(2)** D88/§3.2 — direta.

**P2 — `N = 20`: cravado, provisório, e não-uniforme entre os pisos.** (a) O default do PlatEMO é N=100 (§6.3/D20). (b) O §3.2 crava 20 com quatro justificativas (precedente ParEGO 10–50; interseção única entre `~20–25` e o pré-registro `{10,20,30,50}` da D65; viabilidade `20 ≤ 11D−1` para todo D≥2; densidade de 2 a 30 gerações). A **DI-39 (2026-07-25)** declara o valor **PROVISÓRIO para as 100 células de piso da rodada-42** e mantém o `SUB-varN` como pré-requisito do M8 — *"se a varredura eleger N≠20, as células de piso da semente 42 são descartadas e re-rodadas"*. **Re-li o REGISTRO: a DI-39 NÃO foi movida pela T11** (a mesa da T11 fechou DI-43/DI-44/DI-45; nenhuma toca o N dos pisos). (c) `N_nominal = N_efetivo = 20` no ⑤, no header e no footer em **25/25 + smoke**; `n_pop = 20` em **376/376**; ② com 20 linhas/geração em 376/376. Re-medi o ponto discriminante do §6.3 nas **100 células dos 4 pisos**: em M=3 o `N_efetivo` é **20 para nsga2 e smsemoa, 15 para nsga3 e moead** — o `UniformPoint(20,3)` arredonda para H=4 ⇒ 15 vetores, e o NSGA-II não usa vetores. (d) Como orçamento = pop × gerações é fixo, N é o único grau de liberdade do piso; N=20 dá exatamente D rounds de evolução — pouquíssimo, e é *o ponto*: a régua corre no mesmo orçamento apertado. (e) **(2)** D65/§3.2/§6.3 + DI-39 — direta-declarativa (valor injetado) + direta (teto observado em 376 gerações).

**P3 — Gerações derivadas, e a string que a T11 corrigiu (I-13/A24).** (a)/(b) §3.2: *"gerações = 20D ÷ pop"*. A s42 gravava no `params` a string `"20D ÷ N_efetivo (o DoE 11D-1 e gasto ANTES do Solve)"`. A T11 (`40df11a`) a substituiu por uma nota longa que declara o valor **EMERGENTE**, dá o mecanismo (prole em pares `2*floor(N_ef/2)`, cada duplicata **adia** o fim) e a melhor aproximação medida `floor((20D + n_dup) / (2*floor(N_ef/2)))`, terminando com *"O valor REAL de cada run esta em `n_geracoes` — nao derive, LEIA"*. (c) O orçamento de infill é **20D exato em 25/25**. Testei as três fórmulas: sobre as **25 células do nsga2**, a nova acerta **24/25** (erra só o ZDT1: prevê 32, real 31 — `cache_hits=40` cruza o degrau de 40); a antiga acerta **0/25**; a forma fechada **D+1 acerta 25/25**. Estendendo às **100 células dos 4 pisos**: nova **93/100** (nsga3 25/25, smsemoa 25/25, nsga2 24/25, **moead 19/25**), antiga **0/100**, D+1 **72/100** (só vale onde N_ef=20). (d) A nova string é **estritamente melhor** que a antiga e o conselho final ("leia `n_geracoes`") é o correto; mas para o nsga2 existe forma fechada exata (§U10) e a nova fórmula perde uma célula. O número publicado ("103/112") não é reproduzível — §10. (e) **(2)** §3.2 + I-13/A24 — direta. **Armadilha preservada: prever `n_geracoes = 20D/N` reprova 25/25 falsamente.**

**P4 — Elitismo e turnover: o coração do NSGA-II, sem tocar no código.** (a) NSGA-II canônico: `R_t = P_t ∪ Q_t`, ordena por fronteiras, trunca em N por crowding. (b) Sem mudança (stock). (c) Testei a implicação observável `②(g+1) ⊆ ②(g) ∪ {solution_id avaliados na janela [fe(g), fe(g+1))}`: **351/351 transições, 0 violações** — nenhum indivíduo aparece do nada. O turnover (multiconjunto) é **nunca zero** em 351 transições: mín **3** (WFG5, WFG9), máx **14** (ZDT3), média **8,43 de 20** (~42%/geração). Sobrevivência do DoE até o fim: **0 em 22/25**, positiva só nas três de D=2 (MMF4 6, MMF1 4, MMF11_L 3). (d) Turnover positivo em toda transição prova que a variação produz prole aceita; turnover < 20 prova seleção **elitista** e não geracional-pura; juntos com a inclusão em R_t são a assinatura completa da `EnvironmentalSelection`. (e) **(1)** — direta.

**P5 — Monotonia do `ideal`: 787 transições, zero violações.** (a) Consequência lógica do elitismo: o ponto ideal é monotonicamente não-crescente por componente. (b) Sem mudança; `f_best[]`/`ideal` são instrumentação da linha "pisos" do CONTRATO §6.1. (c) Sobre 351 transições × M objetivos = **787 comparações: 0 violações**, 299 melhoras estritas (38,0%), 488 estagnações (62,0%). `f_best ≡ ideal` componente a componente em **25/25** — o campo é o **vetor de melhores por objetivo**, não o vetor de uma solução. `nadir_front1 ≤ nadir_pop` em 25/25, e o `nadir_front1` **não é monotônico** (a frente-1 muda de composição). (e) **(1)** — direta.

**P6 — `n_front1`: o filme da convergência.** (a)/(b) Instrumentação da linha "pisos". (c) `n_front1 ≤ N` em **376/376**. **17/25 saturam** alguma vez; **6 já na geração 1** (DTLZ2, DTLZ4, DTLZ7, MMF16_20, WFG4, WFG5 — populações já mutuamente não-dominadas); **8/25 nunca** (BBOB_F17 máx 13, F49 15, F55 13, MMF1 14, MMF11_L 14, WFG2 17, ZDT4 10, ZDT6 10). (d) O padrão separa dois regimes: onde M=3 ou o front é largo, o NSGA-II perde pressão seletiva quase de imediato e só a crowding distance decide — o modo de falha clássico em many-objective, e a razão de o estudo ter um NSGA-III casado ao eixo indicador (D25). Onde o front é estreito/multimodal a pressão persiste. **É comportamento canônico sob N=20, não infidelidade.** (e) **(1)** — direta.

**P7 — Clone-hazard: o ledger fecha por dois caminhos.** (a)/(b) D89: duplicata bit-exata **não consome FE, consome o SLOT**; D57 faz o dedup por X. O bundle §3.2 ponto 5 registra o efeito colateral aceito no MOEA/D e adverte *"não é bug — é o algoritmo stock sob a D89"*. (c) Descontado o bloco de seeding, a identidade **`resto = maxfe − fe(último registro) ≡ nº de duplicatas de prole nos rounds COMPLETOS`** fecha em **25/25** por dois métodos independentes: (i) contando os guards que precedem o último registro de geração no ⑥ e (ii) somando `Σ(20 − Δfe_g)` — os dois dão o mesmo vetor de 25 valores (Σ = **175**). Total: **183 duplicatas de prole em 7.020 slots = 2,607%**, das quais **175 nos rounds completos e 8 no round truncado**; por célula, de **0,00% (MMF4)** a **8,50% (BBOB_F1, 17/200)**. Contraste re-medido nos 4 pisos: **smsemoa 17 · nsga2 183 · nsga3 180 · moead 1.500** duplicatas de prole. (d) SBX com `dis_c=20` concentra filhos perto dos pais; com pool de pais mínimo (T=2 no MOEA/D) os clones explodem; no NSGA-II o torneio binário sobre 20 mantém diversidade de pares. Reflexo na ②: **107 clones de `solution_id` dentro da mesma geração, em 69 das 376** (máx 5) — **a ② é MULTICONJUNTO**. (e) **(2)** D89 — direta. **O prior "não é bug, é o stock" segue confirmado por dado.**

**P8 — A ② é a população do ⑥.** (a)/(b) Sem mudança. (c) Recomputei `ideal` e `nadir_pop` do join `②(g)→①` contra o ⑥: erro **RELATIVO máximo 5,717e-08 em 25/25** (ULP do float32 da ① contra o float64 do JSON); `n_front1` recomputado bate em **326/376 (86,7%)**, e as 50 divergências são inteiramente do export (§N2). (d) O erro relativo é a régua correta: em BBOB_F17 o ideal é ~4,9e+05 e o desvio ABSOLUTO chega a 2,98e-02 — quem checar tolerância absoluta reprova a célula. (e) **(1)** — direta.

**P9 — Operadores do Balde C.** O header declara `"Balde C: OperatorGA stock (SBX proC=1 dis_c=20; PM proM=1 dis_m=20)"` e o `params` repete — mas as grandezas não deixam assinatura recuperável (a ② traz o resultado da seleção, não a genealogia da prole; a ③ está vazia). O único vestígio indireto é a taxa de clone (2,607%), compatível com `dis_c=20` mas não discriminante entre 20 e 15. **T** — não-verificável sem código/re-run.

**P10 — O miolo do stock.** Torneio binário por crowded-comparison, ordem do `randperm`, truncagem da última fronteira e contabilidade de RNG não deixam rastro. O **resultado** é auditável e fecha (P1/P4/P5/P8); a **mecânica**, não. **T**.

**P11 — O casamento declarado.** `principio='dominancia/Pareto'` e `casamento='EA-Dominancia (b4, c217, c141, e74) + BO-Melhoria-de-Pareto (c238) — §3.2'`, idênticos em 25/25 e no smoke. É o eco do mapa re-mapeado v2.0, e o que dá **atribuição limpa** do ganho ao surrogate — contra este piso, não contra "o melhor piso por problema" (rejeitado no §3.2 como máximo de baselines correlacionados). **(1)** — direta-declarativa.

**N1 — Stock puro.** `patches="NENHUM — piso e stock do PlatEMO 4.15 por design"`, `parameter="nenhum (NSGA-II stock nao expoe parametro)"`, `surrogate="NENHUM (piso = MOEA puro)"`, `algo_version='piso-NSGAII-PlatEMO4.15'`, `header.piso=true`, `header.surrogate=false` — **os seis literais idênticos em 25/25 e no smoke**. É o config com a menor superfície de divergência do estudo: não há paper contra o qual divergir nem patch cuja âncora possa quebrar. Confirmei no código: `piso_spec('nsga2')` (`experiment.m:2562-2568`) devolve `classe='NSGAII'` e `parameter={}` — o `ParameterSet` do PlatEMO nunca é tocado. **(1)** — direta-declarativa.

**N2 — O colapso de dominância pelo export float32 — o achado da F5, que a T11 promoveu a REGRA.** (a)/(b) D53 manda exportar float32 **sem arredondamento**; o algoritmo opera em float64. (c) Re-medido hoje: o `n_front1` recomputado da ①(float32) diverge do ⑥(float64) em **50 de 376 gerações (13,3%)**, e a direção é quase absoluta — **49 ENCOLHEM a frente-1, 1 a aumenta** (MMF4 geração 2: 16 contra 15, e essa é o par duplicado do §N3). Distribuição: DTLZ4 13, ZDT3 14, ZDT1 10, DTLZ7 9, MMF1 2, ZDT6 1, MMF4 1. O caso extremo é o DTLZ4 (α=100): 357 zeros exatos em float32 contra 59 em float64 (o menor \|f\| não-nulo em float64 é 2,67e-322, muito abaixo do menor normal float32), e a frente única vira cadeia de dominância — **7 fronteiras/23 na frente-1 viram 23/7**. (d) A aritmética explica a direção: arredondar só transforma "não-dominado por pouco" em "dominado", nunca o contrário. **Impacto metrológico, re-medido com `src/metrics.py` no endpoint DTLZ4/nsga2**, isolando o efeito do FILTRO (mesmos valores f32, mudando apenas QUEM entra: 12 pontos pelo filtro f32 contra 25 pelo filtro correto): **IGD+ = 0,34804684231745553 e HV = 0,087056999548987574 nos DOIS — BIT-IDÊNTICOS**. Os descritores de forma, esses, mudam muito: **`n_nd` 12→25 (+108%), `spacing` 0,4326→0,2518 (−41,8%), `GD` 0,2901→0,3449 (+18,9%), IGD 0,4478→0,4422 (−1,3%)**. (e) **(2)** D53 — direta. **O que mudou na T11: isto deixou de ser um caveat do meu relatório e virou a REGRA 11 do `CONTRATO_DE_DADOS.md §10`** (*"DOMINÂNCIA sobre ① ou ⑦ é LOSSY … diferenças abaixo da resolução do float32 não são conclusivas"*, marcada `[T11/A37.2 — o "R4#10" do plano]`, introduzida no commit `8123e92`). **`src/metrics.py` NÃO foi alterado** (`git log` → último commit `68f8230`, pré-T11): a mitigação é normativa/documental, não de código — o que é a escolha certa sob D97, mas precisa ser dito.

**N3 — O único par de X duplicado em 10.856 linhas.** (a)/(b) D57: dedup por X bit-a-bit em float64. (c) Em 24 células o nº de X distintos iguala o nº de linhas; no **MMF4** há **1 par**: `solution_id 19` (`init`, `fe_index` 19) e `solution_id 30` (`opt`, `fe_index` 30), com X e f **bit-idênticos em float32** (`x` hex `b6e707bf0cbb313f`; `f` hex `b6e7073f0940663f`). (d) **Não é falha do dedup**: o `x_key` do guard é calculado sobre X em float64; se fossem iguais lá, o segundo teria virado cache-hit sem linha nova. Foi um **quase-clone de SBX** que passa o dedup em float64 e **colide depois do cast** (ULP relativo do float32 ≈ 6e-8). É também a origem da única divergência de `n_front1` no sentido oposto (§N2). Uma causa, dois efeitos, ambos fechados. (e) **(2)** D53/D57 — direta.

**N4 — Sanidade numérica.** **0 NaN** em X e em f nas 25 células; X dentro dos bounds em 25/25 (BBOB [−5,5] com contato exato na borda — o clamp do adapter funcionando; DTLZ/ZDT/MMF16_20 [0,1]; WFG [0,2i], máx observado 44,0 = 2×22). Nenhum guard de clamp/NaN disparou: os únicos nomes nas 25 células são `cache_hit` e `hard_stop`. **(1)** — direta.

**N5 — O contrato do ⑥ para a linha "pisos" — dado conforme, gate incompleto.** (a)/(b) CONTRATO §6.1 l.335 exige dos pisos *"só o mínimo comum + `n_front1`; `f_best[]`; ideal/nadir da pop por geração; os VETORES de decomposição do moead/nsga3 no HEADER"*. (c) Os quatro campos estão em **376/376** eventos da s42 e **13/13** do smoke, e o writer entrega um **a mais** (`nadir_front1`), que é o que permitiu distinguir a monotonia do ideal da não-monotonia do nadir da frente (§P5). A cláusula dos vetores não se aplica ao NSGA-II. (d) **Mas o gate G-7 que a T11 criou para aferir exatamente isto só cobre 2 dos 4** — ver §4.5, onde a lacuna está provada por mutante. Para o DADO isto é irrelevante (nada falta); para a CAMPANHA de 30 sementes é relevante. (e) **(1)** — direta (para o dado).

**N6 — O elo código↔run: a lacuna que a T11 fechou no ⑤ e não fechou no ⑥.** (a)/(b) O CONTRATO §6 (l.296) lista para o `header` do ⑥: `run_id, alg+versão, problema, D, M, semente, regime, maxFE, doe_hash, ambiente, params, sigma_dict, ts`; o §5 lista para o ⑤ `params, sigma_dict, timing, doe_hash` + (T11) `campanha_id, repo_hash`. (c) **Na s42**: `repo_hash = ''` em **25/25** (a F5 mediu o mesmo nos 13 configs MATLAB), sem `campanha_id`, `schema_version=1`, `sigma_dict` ausente, e o header do ⑥ sem `run_id`/`ambiente`/`params`/`sigma_dict`. **No smoke T11**: `repo_hash='195f64ed0a69dc3f6c64c66912076e243be90c49'` — commit real, conferido no `git log` (`[T11-B3/T5] relatório de conformidade dos 24 configs`) —, `campanha_id='t11-smoke-195f64e'`, `schema_version=2`, `sigma_dict` presente; o gate G-3 dá **OK** e o G-7 dá **"⑤ 6/6 chaves"**. **O header do ⑥ continua sem os quatro campos**, no smoke tanto quanto na s42. (d) A correção está no código com o racional escrito (`experiment.m:3076-3080`, I-09): *"era '' em 666/666 celulas da rodada-42, transversal aos 13 configs MATLAB. Sem ele, ~10.350 celulas/campanha ficam sem dizer QUAL codigo as produziu"* — é literalmente o item que a F5 escalou. O elo permanece, para a s42, `repos.lock` (PlatEMO `b686ca2`) + `anchors.json` (trivial aqui: não há patch). (e) **(2)** I-09/B-03 + doc-sync — direta-declarativa + direta.

---

## 3. Percentuais por classe (Botão 2)

**Denominador = total − T = 26 − 2 = 24.** Os T ficam fora e são listados à parte (§9).

| classe | n | % (sobre **24**) | itens |
|---|---:|---:|---|
| **(1)** conforme o método canônico (NSGA-II stock) | 10 | **41,7%** | **T1**, U10, P4, P5, P6, P8, P11, N1, N4, N5 |
| **(2)** desvio sancionado (com D-xx/DI-xx citado) | 14 | **58,3%** | U1 (D21/D61) · U2 (D63) · U3 (DI-13.2/13.7/16.1 + G-7) · U4 (CONTRATO §6) · U7 (DI-13.10) · U9 (D89/D57) · U12 (DI-08) · P1 (D88) · P2 (D65/§3.2/§6.3+DI-39) · P3 (§3.2+I-13) · P7 (D89) · N2 (D53→regra 11) · N3 (D53/D57) · N6 (I-09/B-03) |
| **(3)** inexplicado 🎯 **no mecanismo** | **0** | **0,0%** | — (a lacuna do gate G-7 é de INSTRUMENTAÇÃO e está em §4.5/§8) |
| **T** (fora do denominador) | 2 | — | P9 operadores SBX/PM · P10 miolo do stock |

Marca declarativa (Botão 3): **P2, P11, N1 e N6** contam com verificabilidade `direta-declarativa` **visível na tabela**; nos quatro há ainda perna **direta** (teto de N observado em 376 gerações; ausência de patch consistente com o stock provado em P1/P4/P5/P8; `repo_hash` conferido contra o `git log`).

**Delta vs F5**: 25 → 26 aspectos (**+T1**, o par cross-campanha). Classe (1) 9→10, classe (2) 14→14, classe (3) 0→0, T 2→2. Nenhuma reclassificação: **nenhum aspecto mudou de classe**.

---

## 4. 🆕 AS CORREÇÕES DA T11 — o que toca o `nsga2`

Quatro correções tocam o config; **as quatro são de INSTRUMENTAÇÃO do ⑤ e as quatro funcionam de fato**. Nenhuma correção de mecanismo era devida (a F5 fechou com 0 classe (3)) e nenhuma foi feita — **provado**, não suposto (§T1).

| # | correção | implementada? | funciona de fato? | o que ainda falta |
|---|---|---|---|---|
| 4.1 | **`repo_hash` no ⑤ (I-09)** | ✅ `experiment.m:3080` + `repo_hash_corrente()` :3125 | ✅ **MEDIDO**: `195f64ed…c49` no smoke, commit existente no `git log`; s42 `''` em 25/25 | não confere árvore SUJA (`git rev-parse HEAD` sem `--porcelain`) e cai para `''` em silêncio se o `system()` falhar |
| 4.2 | **`campanha_id` + `schema_version=2` (B-03)** | ✅ `experiment.m:3067-3068` + `campanha_id_corrente()` :3099 | ✅ **MEDIDO**: `t11-smoke-195f64e` no smoke, ausente em 25/25 da s42; gate G-3 OK | o default (`{commit12}_{data UTC}`) muda à meia-noite — a env `UA_DD_SAEA_CAMPANHA_ID` é normativa e é item aberto D-G da T11 (4 máquinas) |
| 4.3 | **`sigma_dict` declarado nos pisos (G-7)** | ✅ `experiment.m:2501-2510` | ✅ **MEDIDO**: `status='nao_se_aplica'` + motivo no smoke; **ausente em 25/25 da s42** — era a armadilha nº1 da F5 | nada. Fecha a lacuna exatamente como escrita |
| 4.4 | **string `geracoes_derivadas` (I-13/A24)** | ✅ `experiment.m:2546-2557` | ⚠️ **funciona, com número errado**: a fórmula acerta **24/25** no nsga2 e **93/100** nos 4 pisos — não "103/112"; o denominador 112 não é reproduzível | trocar por `n_ger = D + 1` (exato 25/25, mecanismo provado em §U10) para nsga2/smsemoa; o conselho final ("leia `n_geracoes`") já é o correto |
| 4.5 | **gate G-7 / `contrato_61.json`** | ✅ artefato + gate | ❌ **cobre 2 dos 4 campos da linha "pisos"** — provado por mutante | incluir `ideal` e `nadir_pop` (e `nadir_front1`) em `configs.nsga2.di10` |
| 4.6 | **`mapa_termino.json` (I-08)** | ✅ artefato normativo | ✅ declara para nsga2 `rec='nsga2_gen'`, `campo_termino='termino'`, `n_footers=[1]` — bate com 25/25 da s42 e com o smoke | a contagem `n_footers_observado {"1": 28}` não é reproduzível (medi **31 células com footer, 0 sem**, por piso, em `data/experiments`) |
| 4.7 | **`jsonl_open` trunca+append (B-11)** | ✅ `experiment.m:3334-3353`, usado pelo `run_piso` (:2364) | ✅ profilático aqui: **0 linhas malformadas e 1 footer em 25/25**; o bug atingiu b1/WFG1 e moead_media, não o nsga2 | nada |
| 4.8 | **`NÃO-APLICÁVEL` × `NÃO-AFERÍVEL` no gate** | ✅ commit `67b8541` | ✅ **MEDIDO**: no smoke o G-1 sai `n/a — ③ sem nenhuma linha marcada`, não falso-verde nem falso-vermelho | nada |
| 4.9 | **regra 11 do CONTRATO (dominância float32)** | ✅ `CONTRATO §10`, commit `8123e92` | ✅ é o achado N2 da F5 promovido a norma | **`src/metrics.py` não foi tocado** — a mitigação é documental; `n_nd`/`spacing`/`GD` do DTLZ4 seguem distorcidos em TODOS os configs |
| — | **`NO_RETRY` (I-4)** | ✅ `experiments.py:84` | **N/A ao nsga2** — o despachante Python não roda MATLAB (`experiments.py:490`: *"os MATLAB rodam via experiments.m"*) | — |
| — | **rito de teto (`teto_wall`, DI-43/44)** | ✅ só no stack **Python** | **N/A ao nsga2** e declarado: `scripts/lote3s.sh:71` — *"so vale para o stack PYTHON. O stack MATLAB nao tem teto de wall-clock nenhum (lacuna declarada e aceita)"* | irrelevante na prática: 4,63 s de mediana, 128,65 s nas 25 células |
| — | `REGRA_DO_ROTULO` · `pmid_ids` · `sonda_estratificada` · `y_treino_dist` | — | **N/A** — sem surrogate, sem sonda, sem rótulo (0 ocorrências em 25/25 e no smoke) | — |

### 4.5 — A lacuna do G-7, PROVADA (o "padrão c217" deste config, na camada do gate)

`contrato_61.json` → `configs.nsga2.di10 = ["f_best", "n_front1"]`. O CONTRATO §6.1 l.335 exige dos pisos **`n_front1`; `f_best[]`; ideal/nadir da pop por geração**. Copiei o smoke para um tempdir, **removi `ideal`, `nadir_pop` e `nadir_front1` dos 13/13 eventos de geração** e re-rodei os gates (`probe_g7_piso.py`):

```
[CONTROLE — smoke intacto]        G-7 contrato61  OK   ⑥ 3/3 campos · ⑤ 6/6 chaves
[MUTANTE — 3 campos removidos]    G-7 contrato61  OK   ⑥ 3/3 campos · ⑤ 6/6 chaves
                                  (e os outros 5 gates também: 6/6 verdes)
```

**Causa raiz identificada**: o `_meta` do artefato diz que ele veio de *"extração automática dos identificadores em crase da tabela §6.1 + curação manual"* — e na l.335 `n_front1` e `f_best[]` estão **em crase**, enquanto *"ideal/nadir da pop por geração"* **não está**. A extração automática, portanto, não podia pegá-los, e a curação manual não os repôs. É o mesmo gênero do padrão do c217 (campo/regra existe, o dado que a sustenta não), transposto para a camada do gate: **o gate criado precisamente para impedir que "campo ausente atravesse cinco gates verdes" deixaria passar a perda dos dois campos que carregam o filme da convergência do piso** (§P5/§P6 — 787 + 376 medições saem deles).

**Não é classe (3) do mecanismo**: o dado da s42 e do smoke tem os campos em 376/376 e 13/13. É lacuna de **cobertura**, e na F5 essa cobertura era **zero** (nenhum gate olhava o ⑥). A T11 melhorou de 0 para 2 de 4 campos; o conserto é uma linha no artefato.

### 4.6 — Os campos-sentinela do ⑤ MATLAB (o padrão c217, na camada do manifesto)

Procurando "campo presente, dado sentinela", achei **três** no ⑤ do nsga2 — todos constantes de fonte, nenhum medido:

- **`n_retries = 0`** — `experiment.m:3073`, escrito uma vez, **nunca reatribuído** em nenhum caminho do arquivo. A F5 citou "`n_retries=0` em 25/25" como sinal de saúde: **não é evidência de nada**. (O `status`, esse, é real: `st_str = "ok"; if ~ok_flag, st_str = "failed"` — `experiment.m:487`, com `ok_flag` = FE exato + `cp_ok`.)
- **`fallback_ativado = false`** — `experiment.m:3093`, **1 única ocorrência no arquivo inteiro**. Constante.
- **`env.pymoo = "0.6.2"`** — `experiment.m:3083`, string literal, não lida do ambiente. O `run_piso` **usa** a ponte pymoo (`py.gc.collect()` no passo 12); se a versão do venv mudar, o ⑤ mente em silêncio.

Nenhum dos três afeta o mecanismo; os três afetam quem os ler como medição.

---

## 5. Comparação canônica (Entrega 2) — **N/A**, substituída pelo papel de régua

O nsga2 é **controle sem artigo**: não há setup de paper com que interseccionar nem indicador publicado a comparar; a Etapa 6 e o Botão 4 só se aplicam aos 17 com artigo. A pergunta que a substitui — *o piso cumpre a função de régua?* — foi re-medida integralmente em `f5/metricas_finais_f52c.csv`:

| pergunta | medida (re-computada hoje) |
|---|---|
| **(i) A régua é limpa de ruído de máquina?** | Sim para a comparação **intra-piso**: os 4 pisos rodaram integralmente na `vm3` (25/25 cada, `tempo_f52d.csv` pós-O-19) ⇒ o piso O-18 (HV ≤1,55%; IGD+ ≤58,98%) não se aplica entre eles |
| **(ii) A régua discrimina?** | Sim. IGD+ final: nsga2 é o **melhor dos 4 pisos em 11/25** problemas, **rank médio 1,80/4**, pior em 2. nsga3 vence 6, smsemoa 8. **A banda min–máx é não-degenerada em 25/25** (razão máx/mín de **1,180** no ZDT6 a **5,100** no BBOB_F22) |
| **(iii) O surrogate compra alguma coisa?** | Sim, qualificado. Em **305 comparações** (25 problemas × 13 SA-MOEA): o SA bate o piso em **178 (58,4%)**; descontando o piso de ruído (só conta se `IGD+_SA < IGD+_nsga2 / 1,5898`), **99 (32,5%)** inequívocas. O SA é **pior** em **127 (41,6%)**, e pior **além do ruído** em **53 (17,4%)** |
| **(iv) Onde a régua humilha o corpo** | **DTLZ1** (D=7, M=3): o nsga2 é o **melhor de TODOS os 17 algoritmos** — é o único problema em que um piso ganha global. Rank global médio do nsga2: **8,92** entre 14–17 algoritmos por problema |
| **Custo** | 25 células = **128,65 s** (mediana 4,63 s; mín 1,02 s ZDT3; máx 19,36 s MMF1) = **0,0357 h-core**. O piso é gratuito, como o §3.2 prometia, e **87,8%** disso é startup do MATLAB |

Nenhuma leitura comparativa é fixada aqui — **DI-39** é explícita: *"a rodada-42 é validação de fidelidade e piloto de timing, não a bateria de comparação; a banda dos pisos é construída no M8/M9"*.

---

## 6. Saúde (s42 = corpus principal)

**Trajetórias** (20 checkpoints × 25 células = **475 transições**, `f5/trajetorias/main_nsga2_*_42.json`): **IGD+ monotônico não-crescente em 475/475 — 0 violações**; **HV monotônico não-decrescente em 475/475 — 0 violações**. Ganho `IGD+(fe=0)/IGD+(final)` mediano **6,46×**, de **1,389×** (ZDT6) a **117,24×** (BBOB_F17).

**Monotonia interna** (o que a trajetória não vê): **787** transições `geração×objetivo` do ponto ideal, **0 violações** (§P5). Elitismo: **351/351** sem violação (§P4). Somadas: **1.613 transições verificadas, zero violações de monotonia ou elitismo**.

**`HV = 0` e `n_nd`**: 5 células terminam com HV = 0 (DTLZ1, DTLZ3, DTLZ7, ZDT4, ZDT6) — front mal-convergido para além do ponto de referência normalizado, que é **dado, não NaN** (§12.2/D69), coerente com IGD+ final alto (DTLZ3 305,7; DTLZ1 45,3; ZDT4 43,9) e com a estagnação máxima (DTLZ7 78,8%). `n_nd` final de **4** (BBOB_F55) a **149** (MMF16_20).

**Onde o mecanismo perde pressão**: as 6 células que saturam `n_front1 = 20` já na geração 1 rodam **todas as D gerações com a população inteira na frente 1** — a partir daí só a crowding distance seleciona. É o modo de falha canônico do NSGA-II em M=3/front largo e é *por isso* que o estudo tem NSGA-III casado ao eixo indicador. **Saúde do mecanismo, não infidelidade.**

**Sonda — regra 12 vacuosa neste config**: não há bloco `regime='sonda'` (a régua Sobol) **nem** `regime='sonda_estratificada'` — a ③ tem 0 linhas em 25/25 e no smoke, e `sonda_f52e.csv` traz 0 linhas nsga2. Não há, portanto, o que separar. Também não há endpoint ⑦ (regime online ⇒ o endpoint é a própria ①), logo o config não sofre o empate-por-desenho da família offline.

**Contrato (Entrega 3)**: `integridade_f52a.csv` → 25 linhas, todas `VAZIO` da ③ = **por desenho com regra citada** (DI-13.7/DI-13.2), 0 parquets ilegíveis, 0 manifestos ilegíveis, 0 linhas rasgadas; `contrato_f52b.csv` → **0 linhas nsga2** (não está entre os 7 da não-conformidade `⑤ sem params`); F5.1 → dentro de *"os 13 MATLAB + os pisos: zero vermelhos"*. **Zero defeitos de contrato atribuíveis ao nsga2**; o único ⚠ remanescente é o header do ⑥ (§N6), transversal aos 13 configs MATLAB.

---

## 7. SCORE e VEREDITO

### **SCORE: 10/10 — ACEITAR** · **VEREDITO: MANTEVE**

**Por que MANTEVE, com a causa nomeada.** O score não muda porque **o mecanismo não mudou — e desta vez isso é medido, não inferido**: o smoke T11 e a célula da s42 de `main/nsga2/DTLZ2` são **bit-idênticos** na ①, na ② e, campo a campo, no ⑥ (0 de 42 registros divergentes fora de `ts`/wall). As quatro correções da T11 que tocam o config são **todas de instrumentação do ⑤**, e a inspeção dos três commits que alteram `src/experiment.m` na região do piso confirma que nenhum toca uma linha de algoritmo; `src/piso_instrument.m` não foi tocado pela campanha. **Não havia correção de mecanismo a validar porque a F5 não achou defeito de mecanismo** (0 classe (3) em 25 aspectos).

O que a T11 **melhorou** de fato, medido: **3 das 4 lacunas declarativas que a F5 apontou fecharam** — `repo_hash` (I-09), `sigma_dict` (G-7) e a string errada de `geracoes_derivadas` (I-13) — e a **armadilha nº5 da F5** (recomputar dominância na ① é recomputá-la em float32) virou a **regra normativa 11 do CONTRATO**. O que **não fechou**: o header do ⑥ (`run_id`/`ambiente`/`params`/`sigma_dict`, CONTRATO §6 l.296) segue vazio na s42 **e no smoke**; e o gate novo G-7 cobre 2 dos 4 campos da linha "pisos".

**Por que segue 10 e não 9.** A base do 10 é o mecanismo, e ele está intacto e mais provado que antes: **zero aspectos (3)**; a query-joia D88 fecha 25/25 (24 diretas + DTLZ4 restaurado em float64, fórmula validada 393/393); o ledger da D89 fecha 25/25 **por dois métodos independentes**; `n_geracoes = D+1` deixou de ser regularidade observada e passou a ter **mecanismo provado** (`Σ(20−Δfe_g) ≡ resto`, 25/25); 1.613 transições sem uma violação. A lacuna do G-7 é de **cobertura de gate**, não de dado nem de algoritmo — e na F5 essa cobertura era **zero**: rebaixar o score por uma melhoria parcial de algo que antes não existia seria perverso. Ela vai à mesa como escalação (§8), não como (3).

**Recomendação: ACEITAR.** Sem caveat de fidelidade. Dois caveats de leitura, ambos transversais e nenhum deste config (§9), e uma pendência de pré-registro já declarada **antes** do disparo: **DI-39** (N=20 provisório até o `SUB-varN`), que a T11 não moveu e que é decisão do autor, não achado meu.

---

## 8. Aspectos classe (3) 🎯 — com evidência completa

**Nenhum no mecanismo.** A verificação adversarial não recebe item de mecanismo do nsga2.

**Uma escalação, de INSTRUMENTAÇÃO** (não entra no denominador da §3; declarado aqui por transparência):

> **G-7/`contrato_61.json` não cobre a linha "pisos" do CONTRATO §6.1.** `configs.nsga2.di10 = ["f_best","n_front1"]`; o CONTRATO §6.1 l.335 exige também **ideal/nadir da pop por geração**. **Prova por mutante** (`probe_g7_piso.py`, tempdir, produção intocada): removendo `ideal`, `nadir_pop` e `nadir_front1` de **13/13** eventos do smoke, os **6 gates seguem verdes**, com o G-7 relatando "⑥ 3/3 campos". **Causa raiz**: o `_meta` do artefato declara construção por *"extração automática dos identificadores em crase da tabela §6.1"*, e na l.335 só `n_front1` e `f_best[]` estão em crase. **Impacto**: nenhum na s42 nem no smoke (376/376 e 13/13 têm os campos); na campanha de 30 sementes, a perda silenciosa desses dois campos apagaria as bases de §P5 (787 medições) e §P6 (376) sem acender gate. **Conserto**: uma linha no artefato, replicada nos 4 pisos.

**Registro dos dois itens que *pareciam* candidatos e estão fechados, ambos classe (2), ambos re-medidos hoje:** as 50/376 divergências de `n_front1` (export float32, D53 — direção 49-a-1, DTLZ4 restaurado 13/13 em float64) e o 1 par de X duplicado do MMF4 (quase-clone de SBX que passa o dedup float64 e colide no cast). **Item transversal para a F5.5/F5.7 (não é deste config):** `src/metrics.py` filtra não-dominado sobre a ① float32 e **não foi alterado pela T11**; `n_nd` (+108%), `spacing` (−41,8%) e `GD` (+18,9%) do DTLZ4 são artefato do export em **todos** os configs, enquanto **IGD+ e HV são provadamente invariantes ao filtro** (re-provado bit-a-bit hoje).

---

## 9. Teto de verificabilidade (T), armadilhas, e o que este corpus NÃO permite verificar

**T — exige código ou re-run (2 itens, fora do denominador):**
1. **Operadores do Balde C** — `SBX proC=1 dis_c=20` + `PM proM=1 dis_m=20`: eco de string; a grandeza não é exportada. Único vestígio indireto: a taxa de clone (2,607%), compatível com `dis_c=20`, não discriminante.
2. **Miolo do stock do PlatEMO** — torneio binário por crowded-comparison, `randperm`/RNG, truncagem interna da última fronteira. O **resultado** fecha (P1/P4/P5/P8); a **mecânica**, não.
*(Sub-item coberto por `repos.lock`+`anchors.json` e não por dado: o diff textual do stock — mitigado por `patches='NENHUM'` e agora pelo `repo_hash` real no ⑤. Não entra em T a não-perturbação da sonda: **não há sonda**, e o par cross-campanha do §T1 é mais forte que o par g6.)*

**O que este corpus NÃO permite verificar:** (a) que os operadores e o miolo são os do stock — só que o resultado é consistente com eles; (b) que o `repo_hash` corresponde a uma **árvore limpa** (o `repo_hash_corrente()` não checa `git status --porcelain`); (c) que `n_retries`/`fallback_ativado`/`env.pymoo` do ⑤ MATLAB refletem a execução (são constantes de fonte — §4.6); (d) qualquer coisa sobre modelo/incerteza/sonda — **não existem** neste config; (e) o comportamento sob **N ≠ 20** (DI-39, `SUB-varN` não executado) e sob **concorrência de campanha** (o corpus é 1 célula por vez).

**Armadilhas confirmadas na escala (25/25 salvo indicação):**
1. **③ com 0 linhas ≠ surrogate morto** — DI-13.7/13.2. Na s42 vinha junto `sigma_dict` **ausente**; **no código de hoje ele está DECLARADO** como `nao_se_aplica`. Uma bateria que exija `sigma_dict` reprova 25/25 da s42 e passa no smoke.
2. **A ② é MULTICONJUNTO** — 107 clones em 69 das 376 gerações (máx 5). Tratar `②(g)` como `set` faz `len < 20` em 69 gerações.
3. **`n_geracoes = D + 1`**, não `20D/N = D` — e **também não** `floor((20D+cache_hits)/20)`, que erra o ZDT1. **O manifesto de hoje diz o certo: leia `n_geracoes`.**
4. **O bloco de seeding são `N_efetivo + 1` cache-hits em `fe = 11D−1`** (21 no nsga2/smsemoa; **16** no nsga3/moead em M=3), com o primeiro **logado duas vezes** (`c0` do wrapper). Sem descontá-lo o ledger erra por 21.
5. **Recomputar dominância na ① é recomputar em float32** — 13,3% das gerações divergem; no DTLZ4 a estrutura inverte (7/23 → 23/7). **Agora é a regra 11 do CONTRATO.** Para fronteiras, recompute f em float64 do X; para ideal/nadir, compare **RELATIVO** (2,98e-02 absoluto no BBOB_F17 é 6e-08 relativo).
6. **`f_best[]` é o ponto IDEAL** (melhor por objetivo), não o vetor de uma solução — idêntico a `ideal` em 25/25.
7. **`nadir_front1` NÃO é monotônico**; só o `ideal` é.
8. **A ② NÃO tem off-by-one** (≠ c217) — provado pelo recomputo de ideal/nadir a ULP. Aplicar aqui o teste do c217 reprova 25/25.
9. **`N_efetivo = 20` só vale para nsga2 e smsemoa** — nsga3 e moead caem a **15** em M=3 (re-medido nas 100 células).
10. **`~1 s/célula` é startup do MATLAB** — 87,8% do wall; MMF1 custou 19,36 s por ser a primeira célula da campanha, ZDT3 1,02 s numa sessão quente.
11. **`hard_stop` ⟺ `resto > 0`** — MMF4 é a única sem o guard, e está certo.
12. **`repo_hash=''` é da s42, não do código** — o código de hoje o preenche. Ler a s42 como "o repo não carimba" é o erro que a própria T11 cometeu (ERRATA 11).
13. **`n_retries`, `fallback_ativado` e `env.pymoo` do ⑤ MATLAB são CONSTANTES DE FONTE** — não use como evidência de saúde (§4.6).
14. **`teto_wall`/`motivo_parada` não existem no stack MATLAB** — o campo de término deste config é `footer.termino`, e o `mapa_termino.json` agora o declara. Buscar `motivo_parada` reprova 25/25.

---

## 10. Erratas — contra a minha própria F5 e contra a T11

**Contra a F5 `f5/relatorios_config/nsga2.md` (4, todas re-medidas hoje):**
1. **§U9/§ficha — "708 guards (`cache_hit` 683 + `hard_stop` 24 + 1 duplicata)"**: são **732 registros de guard = 708 `cache_hit` + 24 `hard_stop`**. O "683" era 708 − 25 (as duplicatas de arranque) rotulado errado.
2. **§4(ii) — "razão máx/mín entre 1,006 no WFG1 e 3,80 no ZDT1"**: a razão IGD+ mín é **1,180 (ZDT6)** e a máx é **5,100 (BBOB_F22)**; WFG1 é 1,434 e ZDT1 é 3,799 (o valor do ZDT1 estava certo; o rótulo "máximo", não). **Conclusão inalterada**: banda não-degenerada em 25/25.
3. **§N2 — "IGD+ e HV BIT-IDÊNTICOS"**: verdadeiro para o contrafactual que a frase pretendia (trocar o **filtro** de dominância mantendo os valores) — re-provado bit-a-bit hoje. **Falso** se o contrafactual recomputa f em float64: aí IGD+ move 2,42e-09 e HV 5,33e-08 — 6 ordens abaixo do piso O-18, mas não zero.
4. **§P7 — "de 0,00% (MMF4) a 7,83% (BBOB_F1)"**: a taxa máxima por célula é **8,50%** (BBOB_F1, 17 duplicatas de prole em 200 slots). O agregado 2,61% está certo (183/7.020 = 2,607%).

**Contra a T11 (2):**
5. **`geracoes_derivadas` — "acerta 103/112 células da s42"** (`experiment.m:2552`): a fórmula acerta **93/100** no corpus oficial dos 4 pisos (nsga2 **24/25**, nsga3 25/25, smsemoa 25/25, moead 19/25) e **24/25** no nsga2. O denominador **112 não é reproduzível**: `resultados_experimentos` tem **100** células de piso (25×4, como a DI-39 diz) e `data/experiments` tem **124** (31×4: 25 da s42 + 6 da semente 0). Também: *"a string antiga acerta 2/112"* — no corpus oficial ela acerta **0/100**.
6. **`mapa_termino.json` — `n_footers_observado {"1": 28}`** para os 4 pisos: medi **31 células com footer e 0 sem**, por piso, em `data/experiments`. O campo (`termino`) e o esperado (`[1]`) estão corretos e batem com 25/25 + smoke; a contagem observada, não.

---

*Artefatos preservados em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/nsga2/`: scripts `bateria_t11_nsga2.py` (U1–U12, P1–P8, N1–N6 + presença da instrumentação nova nas 25 células) · `bateria2_t11_nsga2.py` (P1 via ① float32 e via float64 do DTLZ4; P7 rounds completos × truncado) · `smoke_vs_s42_nsga2.py` (o par cross-campanha, §T1) · `probe_g7_piso.py` (a prova por mutante da lacuna do G-7, §4.5). Evidência: `aspectos_t11_nsga2.csv` (25 linhas × 96 colunas), `p1_p7_t11_nsga2.csv`, `formula_ger_4pisos.csv` (100 células dos 4 pisos). Insumos pré-computados usados sem recomputo: `f5/metricas_finais_f52c.csv`, `f5/trajetorias/main_nsga2_*_42.json`, `f5/tempo_f52d.csv`, `f5/contrato_f52b.csv`, `f5/integridade_f52a.csv`, `f5/sonda_f52e.csv`. Interpretador `/Users/gmello/Documents/python_venvs/mestrado_experimentos_dissertacao/bin/python`. **READ-ONLY absoluto**: nenhuma célula rodada, `experiments.py` nunca invocado, `data/experiments` nunca escrito (o probe do §4.5 usou tempdir, removido ao fim).*