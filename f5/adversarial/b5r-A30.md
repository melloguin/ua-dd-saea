## b5r-A30 — VEREDITO: CONFIRMADO(d) — defeito de INSTRUMENTAÇÃO/DOC (e, no que importa para a régua, **classe (3) REFUTADA → reclassificar como (2)**)

**Resumo em 3 linhas**
O fato é verdadeiro e sobreviveu a quatro ataques independentes: a chave `"1"` do archive é a população inicial LHS (pré-seleção), escrita por `Population.__init__` e nunca re-carimbada pelo patch DI-16.16 — reproduzi as 3 provas do analista com código próprio e acrescentei uma 4ª (retenção de pais) e um controle negativo (LHS na geração 2 falha em 45/45).
O que **cai** é o enquadramento: o comportamento está sancionado em substância pelo bundle (`contador 40k: **init+pop**`, SPEC v2.2 / `alg_b5_prob.md:26`) e pelo CONTRATO DEF-C2 (offline ⇒ ③ = "a pop-surrogate do MOEA interno, **todas as gerações**"), é **família-wide** (b5r + b5m + moead_media, 135/135 células) e **já foi fechado como classe (2) no relatório-irmão b5m** (A19 + armadilha nº 1) na mesma bateria F5.3b.
Sobra um item real, pequeno: a frase do runner/handoff ("a ③ de b5r … é 'a população mantida por geração'") generaliza para a chave 1 o que só vale para as chaves ≥2 — doc, não mecanismo. **Zero células excluídas, zero números da dissertação alterados** (só uma glosa de contagem).

**O que tentei para derrubar (os 4 passos, com os números que obtive)**

1. **Por DECISÃO** — três âncoras encontradas, nenhuma citada pelo analista: (i) bundle/SPEC v2.2, `claude_code_context/30_rodada3_standalone/alg_b5_prob.md:26` e `SPEC_experimentos_v5.2.md:815`: *"contador 40k: **init+pop**; RVEA soma |offspring|/gen"* — a população inicial é evento contado **antes** da 1ª seleção, exatamente o modelo aritmético da leitura do achado; (ii) `CONTRATO_DE_DADOS.md:131` (DEF-C2): *"offline → a pop-surrogate do MOEA interno, **todas as gerações**"* — um snapshot inicial está **dentro** do contrato, não contra ele; (iii) precedente de protocolo U10 (`f5/PROTOCOLO_ANALISE_FIDELIDADE.md`): *"aritmética entre camadas com o **off-by-one documentado por config**"* (②=N+1 no b1; ④ ger-1 duplicada no e7) + DI-19.3 (n_geracoes definido por query por-config no e103). O `sigma_dict` das 45 células (18 chaves) de fato **não** contém a palavra "sobreviv*" nem "geração 1" — a declaração conflitante vive em `src/b5_prob.py:397` e em `handoff/R3-b5_REPASSE-A-TORRE.md:72` (A.6). Derrubada **parcial**: sanciona o fato, não cobre a redação.
2. **Por QUERY** — reprodução independente, sem reusar nenhum artefato da bateria b5r (`a30_verifica.py`, `a30_impacto.py`): `|pop| ger 1 == N_RV` (50/105) em **45/45**; LHS-perfeito (1 ponto/estrato 1/n) em **727/727** dimensões na ger 1 e **0/45 células** na ger 2 (só **9/727** dims passam) e ger 3 (**6/727**) — o controle negativo que faltava; chaves densas 1..K em 45/45, `K == n_geracoes` do manifesto em 45/45, `n_eventos decision == K` em 45/45; contabilidade 40k: leitura "chave 1 = init" fecha **45/45** (`FE_antes ∈ [39.917; 40.000]`, `FE_final ∈ [40.002; 40.087]`), leitura "todas as chaves são seleção" falha **45/45** (`FE_antes ∈ [40.002; 40.087] > 40.000`) — off-by-one exato, sem zona cinzenta. Robustez: teste em float64 sobre a vista float32 persistida, com bounds reais (`ZDT4` em [−5,5] etc.); P(amostra uniforme ser LHS-perfeita) = 10⁻²⁰·⁵ por dimensão em n=50 e 10⁻⁴⁴·² em n=105 — acaso descartado. **Prova nova (4ª, minha)**: as gerações ≥2 **retêm pais** (fração de linhas de g presentes em g−1 = 0,45–0,78, mediana 0,63 em g∈[3,40]), ou seja são de fato pós-`keep()` — prole pré-seleção daria interseção ~0 (SBX/PM contínuos). Contra-exemplos que matam a hipótese "ger 1 é só uma seleção que manteve tudo": `swap_medium-lhs_DTLZ2` nunca passa de 100 depois da ger 1 (mas tem 105 nela), `DTLZ3` máx 59 (mediana 37), `DTLZ4` máx 50 (mediana 14), `swap_medium-lhs_ZDT4` máx 16 (mediana 7); só **2,2%** das gerações g≥2 atingem N_RV.
3. **Por CÓDIGO** — é o que o código faz **por construção**, e a origem é do autor original, não nossa: `algorithms/b5_Prob-RVEA/desdeo_emo/population/Population.py:111` (`gen_count = 1`), `:126` (`self.add(individuals, use_surrogates)` dentro do `__init__`), `:153` (`individuals_archive[str(gen_count)] = offsprings`), `:156` (`gen_count += 1`). O nosso patch está em `desdeo_emo/EAs/BaseEA.py:212-213` e re-carimba `str(gen_count − 1)`, que já vale ≥2 na primeira `_next_gen` ⇒ a chave `"1"` é estruturalmente inalcançável pelo patch. O invariante que o patch **prometeu** (⑦ ≡ última geração da ③, bit-a-bit float32) eu recomputei: **45/45 VERDE**. Não é anomalia: é comportamento declarado do motor.
4. **Controles** — **positivos**: `b5m` (mode 72) e `moead_media` (mode 12) exibem o **mesmo** padrão em 45/45 cada (ger 1 == N_RV, LHS 727/727 dims cada, ΣK=30.165 ⇒ 30.120 passos reais + 45 snapshots) — logo é **convenção do motor DESDEO vendorizado**, não defeito do b5r; **negativo**: `treed_media` (mesma família BaseDecompositionEA, mas instrumentado por *hook* no `_next_gen`, `src/treed_media.py:174-183`) **não** tem o fenômeno — ger 1 tem 35–78 indivíduos (< N_RV) e **0/188** dimensões LHS-perfeitas. O teste discrimina, e localiza a causa no caminho `Population.add` do archive stock.

**Por que não caiu / por que caiu**
Não caiu como **fato**: código e dados convergem, com margem enorme (10⁻²⁰ de acaso no LHS, off-by-one aritmético exato no 40k, controle negativo limpo). Caiu como **classe (3)**: (a) o artigo (Mazumdar et al., TEVC 2022) não prescreve nada sobre chaves de archive — a fidelidade envolvida (pop inicial = LHS ≠ dataset, Alg. 1 l. 2) já é o aspecto **A13, classe (2) sob B18.9**, e A30 é apenas o *rastro de log* dessa mesma decisão; (b) o bundle sanciona o modelo de contagem `init+pop`; (c) o CONTRATO manda gravar "todas as gerações" no offline; (d) o **mesmo fato foi classificado (2) no b5m** pela mesma bateria — a divergência é de classificação entre relatórios-irmãos, não de mecanismo. Causa do falso-positivo: **leitura literal de uma declaração de escopo estreito** (a frase A.6/`b5_prob.py:397` descreve o que a `_next_gen` grava, e a chave 1 não é escrita pela `_next_gen`) **sem cruzar com o bundle** — exatamente o modo de falha que a cadeia de precedência do protocolo (§0) existe para evitar.

**Enquadramento e bússola D29**
Bucket **(d) defeito de instrumentação/doc**: o mecanismo está certo (o patch cumpre seu invariante 45/45), o que engana é a redação. Bússola: o arquivamento da pop inicial em `Population.__init__` é **🟠 laranja — detalhe de implementação do código original ⇒ segue o CÓDIGO** (não há prescrição no artigo a contrariar). A camada de export que o consome é **🟢 verde — extensão nossa declarada**, e é aí que está o furo: a extensão foi declarada de forma incompleta (o `sigma_dict` cala sobre a semântica da chave 1). Nada **🔴/🔵/🟣**. Não é (a) bug — não há saída errada nem invariante violado; não é (b) — não vira resultado de dissertação; não é (c) — nenhum dado corrompido.

**Impacto**
Nulo em ciência, cosmético em contagem. (i) 3.020 de **1.772.336** linhas de ③-busca (**0,170%**) são o snapshot inicial — e são linhas **legítimas** (predição real do GP sobre a pop LHS, FE real = 0). (ii) `n_geracoes` conta **+1**: 57.836 snapshots = 45 populações iniciais + **57.791** passos de seleção (erro relativo global 0,078%; máx **0,245%** em `swap_medium-lhs_DTLZ2`, K=408; mín 0,018% em `swap_small-mvns_ZDT4`, K=5548). Mesma glosa vale para b5m e moead_media (30.165 → **30.120** cada). (iii) **Nenhuma célula sai da análise**; nenhum gate afetado (`gates_f51.csv` 45/45 VERDE; `final_eval --check` compara a **última** geração — invariante recomputado 45/45); `metricas_finais_f52c.csv` lê a ① (dataset) e `tempo_f52d.csv` só tem `wall_s` — nenhum número publicado muda. (iv) **Aviso anti-overcorreção**: as 56.081 transições pai→filho da query-joia A11 **estão certas como estão** — a transição ger 1 → ger 2 *é* um passo de seleção real (sobreviventes escolhidos de init ∪ prole); quem "corrigir" descartando-a perde 45 transições válidas. (v) Correção editorial no relatório b5r: rebaixar A30 de (3) para (2)/doc ⇒ o denominador vira **19/26 (2) = 73,1%** e **0/26 (3)**; pela régua do FRAMEWORK-v2 o score **9,0 sobe para 10** apenas se o autor aceitar que o item vira nota de doc (recomendo 9,5–10 com o caveat científico do GP degenerado permanecendo — esse é o que realmente rebaixa).

**Ação p/ F5.7 + custo**
1. **Doc (obrigatório, custo ~0 h, sem re-run)**: acrescentar ao `sigma_dict` dos **três** configs DESDEO (`src/b5_prob.py` ~l.196, `src/piso_offline.py` ~l.230) a chave `granularidade_③`: *"③ geração 1 = população INICIAL (LHS, pré-seleção, snapshot do `Population.__init__`); gerações 2..n = população pós-seleção/pós-substituição; passos de seleção = n_geracoes − 1; FE interno conta init+pop (bundle v2.2)"*. Como a rodada-42 está **congelada**, o texto vale para runs futuros; para os dados atuais o registro vai à torre + à lista de armadilhas do F5.6 (a armadilha nº 1 do b5r já cobre; falta espelhá-la em b5m — já lá — e em **moead_media**, que não a registra).
2. **Instrumentação (opcional, alto retorno)**: duplo carimbo na `_next_gen` — `str(g)` = prole pré-seleção + `str(g)_sel` = sobreviventes. Retira do teto o **T A12** (identidade do `argmin` da média-MC) e daria ao b5r a query-joia fechada que b1 (7.073/7.073) e e81 têm; é RNG-neutro (cópia densa), logo a busca sai bit-idêntica. **Custo**: 3 linhas em `BaseEA.py:212-213` + re-lacrar a âncora `b5-mode7-archive` no `repos.lock` + re-run das 45 células (**3,64 h-core** medidas) + ③ ~2× (1,77 M → ~3,5 M linhas; ~330 MB → ~660 MB no bucket). Só vale se a rodada perfeita já for re-rodar o b5r.
3. **Padronização (recomendado se (2) acontecer)**: carimbar o snapshot inicial como `geracao = 0` (ou `fase='init_pop'`) nos 3 configs DESDEO, alinhando com o `treed_media`, que não arquiva a inicial — assim "nº de gerações" passa a ser não-ambíguo entre os 6 configs offline. Custo marginal dentro do item 2; sozinho, +0 h de código mas exige o mesmo re-run.

**Evidências** (todas em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/baterias/f54/b5r-A30/`): `a30_verifica.py` (T1–T5, independente) → `b5r_A30_celulas_b5r.csv` (45), `b5r_A30_celulas_b5m_moead_media.csv` (90, controle positivo), `b5r_A30_celulas_treed_media.csv` (10, controle negativo); `a30_impacto.py` → `b5r_A30_impacto.csv` (volumetria, invariante DI-16.16 45/45, ⑥ ger 1). Nota lateral achada de brinde: o `f_best` do ⑥ na ger 1 bate o `min μ` da ③ em **44/45** — a exceção é `DTLZ1/f2`, onde μ_min = 3,98e−62 (float64 do ⑥) **underflow para 0,0** no float32 da ③; é a assimetria já documentada em A25, não defeito.