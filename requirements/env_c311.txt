# =====================================================================
# env_c311  —  Python 3.8 EXATO (⚠ NÃO 3.9: o C do GPy 1.9.9 usa tp_print, removido no py3.9 —
# vale na VM também). Mac via ROSETTA ✅ provisionado+validado 2026-07-22. Extra: Pillow<10.
# Venv: /Users/gmello/Documents/python_venvs/env_c311   (na VM)
# Algoritmo: c311 (TGPR-MO, offline)
# REGRAS: NUNCA co-importar com env_b5 (D79/N.1.2); overlay desdeo_* root-first;
#         NUNCA importar evaluate_population (dispara matlab.engine). Rodar com MPLBACKEND=Agg.
# =====================================================================
GPy~=1.9.9
graphviz
scikit-learn==1.1.2
numpy==1.20.2
scipy
pyDOE
plotly==4.14.3
plotly-express==0.4.1
matplotlib
statsmodels
pandas
Pillow<10             # Pillow>=10 exige numpy>=1.21 (numpy.typing.NDArray); o pin é 1.20.2
pyarrow               # ler DoE/dataset parquet + export
pymoo==0.6.1.2        # [R3-c311 Fase B] finais ⑦/ND via src/problems.py DENTRO do runner
                      # offline; py3.8 tem typing.Literal nativo (SEM o shim do env_b5); puro-python.
# optproblems: NÃO instalar — dep import-time do desdeo_problem via testproblems, que o caminho
#   DataProblem do c311 nunca usa; o runner src/c311_tgprmo.py o STUBA em sys.modules (respeita D80).
# pygmo: idem AUSENTE do caminho framework/ do c311 (DI-16.14) — nenhum shim (≠ b5).
