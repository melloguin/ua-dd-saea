# RELATÓRIO DE FIDELIDADE — c141 (MMRAEA, Shen et al. 2025 · MATLAB/PlatEMO 4.15) · **VALIDAÇÃO T11** · comparação contra a F5.3b

**Corpora auditados.** **(A) s42 — mecanismo:** as **25/25 células main** de `resultados_experimentos/c141/{problema}/42/` — 1.967 ciclos · 10.856 FEs (①) · 386.382 linhas ③-online · 2.048.000 linhas de sonda (1.024 blocos × 2.000) · 3.131 eventos ⑥ · 68 cache-hits · 22 hard-stops. Máquina real `vm3` nas 25 (`tempo_f52d.csv`). **(B) smoke T11 — código de hoje:** `evidencia_T11/smoke_matlab/.../main/c141/MMF1` (D=2, 61 FE, 10 ciclos, 7 blocos de sonda, 21 eventos ⑥), rodado no **Mac Apple M1 Pro** (`uname -m` = arm64). **(C) par G-6:** `g6_com` × `g6_sem`, mesma célula. **Escritas só em** `f5/t11/baterias/c141/`; dados/artigo/bundle READ-ONLY; nenhuma célula rodada.

---

## 1. Ficha do mecanismo (condensada)

Shen et al. (2025), *Complex & Intelligent Systems* 11:147. DoE LHS 11d−1 → por ciclo: (i) **Fit**=SDE (Eq. 7) e **FN**=front number sobre o arquivo A; (ii) **3 modos de RBF multiquadrática** — ARBFs (1/objetivo), FitRBF (SDE), FNRBF (front number); (iii) **bi-população** por wmax=20 gerações (P1=CSO com fitness SDE-predito, P2=GA SBX/PM, seleção NSGA-II em cada), funde em Ptot=2N; (iv) **cascata tri-nível** (Alg. 3): nível 1 = `F1(Ptot∪A)∩Ptot`; nível 2 = ND de [−Fit1, Fit2, −Fit3]; nível 3 = ND(Q,U) ∪ argmax U (Eq. 8); (v) avalia o lote e repete.

**Divergências sancionadas, nomeadas** (`SPEC:809` + bundle `alg_c141_mmraea.md` + DIs): maxFE = **31D−1** e não 11d+119 (D21/D61) · DoE **injetado** do artefato (D63/D88/D94) · **N = min(100, 11D−1)** contra N=50 do paper, com `Problem.N` **POR subpopulação** (B12.6/DEF-A5/D20) e o piso anti-crash da faixa **D≤4** (`ES_PDR.m:20`) · **`u=5` REVOGADO** (v2.2/DI-18: o gatilho real é "resta >1 candidato") · nível 1 mistura **POP predita + ARQUIVO verdadeiro** (🟠 impl→código) · vetor do nível 2 **[−Fit1, Fit2, −Fit3]** (DI-18: troca de NOMES Fit2↔Fit3, não de semântica) · `rbf_build` MQ c=1, poly=0, **sem scaling de X**, `mldivide` denso **sem regularização** (B12.7, D30 🟠 — "risco de mau condicionamento, mantido") · σ exportado em 2 colunas (D45 🟢) · guard `+eps` do SDE (DEF-L4) e guard batch-vazio [IMPL] · dsmerge ds=1e-14. `sigma_dict` do ⑤: `sigma_0` = U de ranks na população completa; `sigma_1` = std bruto dos 3 modos.

**O que mudou no código entre a s42 e hoje:** para o c141, **nada no mecanismo**. `git log` de `src/c141_instrument.m`, `src/c141_sonda.m` e `algorithms/c141_MMRAEA` para em **2026-07-19** (commits `76fbbd7`/`47daa76`/`ff38f2b`), **7 dias antes** da célula da s42 (`created_at` = 2026-07-26T10:37:26Z). Os arquivos compartilhados que mudaram — `src/FEBudget.m` (`arquivoX`, acessor read-only), `src/SondaState.m` (flag `desligada` + `probeEstratificada`, fiada só em b4/c217/e74) e `src/experiment.m` (campos do ⑤) — **não tocam nenhum caminho de RNG nem de busca do c141**.

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.0 Tabela-resumo (25 aspectos · 2 novos em 🆕 · 1 mudança de classe em ⬆)

| # | aspecto | classe | verificab. | resultado-síntese |
|---|---|---|---|---|
| U1 | orçamento FE = 31D−1 | (2) D21/D61 | direta | 25/25 exato; `fe_index` denso 0-based 25/25; `termino='normal'` 25/25; smoke 61/61 |
| U2 | DoE LHS 11D−1 do artefato | (1) | direta+eco | 25/25 == 11D−1; ΔX **bit-exato em float32**; `doe_hash` ≡ sidecar; **smoke: init 21/21 BIT-IDÊNTICO à s42** |
| U3 | 1 retreino dos 3 modos/ciclo | (1) | direta | \|④\|=\|`c141_gen`\|=\|`fit_series`\| em 25/25 (Σ 1.967) e no smoke (10) |
| U4 | sonda: cadência + finalProbe + join posicional | (2) DI-09/DI-12.5 | direta | 1.024 blocos × 2.000 exatos; 0 falhas; **smoke: g=[1,2,4,6,8,10,**11**]** com `motivo='final'` — finalProbe exercitado no código de hoje |
| U5 | WAPE por bloco · cobertura N/A | (2) §5 | direta/N/A | ΔWAPE mediano **−0,5%** (25 células); `cobertura95` vazia em 2.279/2.279 blocos-objetivo |
| U6 | `fit+busca ≤ tempo_geracao_s` | (2) DI-13.10 | direta | **0 violações em 1.967** linhas de ④ |
| U7 | `fe_treino_max` monotônico | (2) DI-13.15 | direta | **25/25 monotônico** (≠ b1/b4/c217, regra 9 do CONTRATO) |
| U9 | ledger de FE entre camadas | (2) D57/D89 | direta | 3.836+7.030−68+58 = **10.856** = Σ`fe_final`, 25/25; smoke 21+37−1+4=61 |
| U10 | erro de fantasia dos infills | (2) | direta | 9.084 in-sample × 7.491 fantasias; otimismo mediano **66,8%**; smoke: 37/37 otimistas |
| **U11** ⬆ | **não-perturbação da sonda (§3.1)** | **(1) MEDIDO** *(era **T** na F5)* | **direta** | ① `sha256`=**`d7f20cd0c4648c3f`** IDÊNTICA com e sem sonda; ② também idêntica; ③ 14.420→420 |
| F1 | kernel MQ c=1, poly=0, **sem scaling** | (1) | direta | s42: vence 8 alternativas em 14/14 pontos-teste; **smoke: reconstrução do zero, erro rel. mediano 5,9e-05** |
| F2 | `mldivide` SEM regularização | (2) D30 🟠 | direta | ⚠ **ERRATA re-medida**: μ−f **EXATAMENTE 0** em **18.480/18.748 pares (98,571%)**, não em 25/25 células; rcond até **4,5e-25** |
| F3 | nível 1 = pool predito ∪ arquivo **VERDADEIRO** | (2) 🟠 bundle | direta | s42 **1.649 × 1.575** (teste discriminativo); 100% em 6 células/246 ciclos; **smoke 10/10** |
| F4 | Fit1 = SDE Eq. 7 min-max | (1) | direta | s42: 1.647/1.649 a rel<1e-5; **smoke: 10/10 a rel<1e-5, mediana 5,39e-07** |
| F5 | nível 2 = ND de [−Fit1, Fit2, −Fit3] | (1) DI-18 | direta | \|fit3\|/\|fit1\|=**1,41** (SDE) × \|fit2\|/\|fit1\|=**230** (front number) |
| F6 | Q, U (Eq. 8) e nível 3 | (1) | direta | Q/U exatos em 1.490/1.967; lote reconstruído em 1.706/1.967 |
| F7 | σ do c141 (σ_0 U-ranks · σ_1 std-3-modos) | (2) 🟢 D45 | direta | teto **2(2N−1) EXATO em 25/25**; **smoke: max σ_0 = 82 = 2(2·21−1) e `U_pool_max`≡max(σ_0) em 10/10** |
| F9 | papel da incerteza (`Pb`=argmax U) | (1) Alg.3 l.15 | direta | Pb é ponto **EXTRA** em **1.727/1.967 = 87,80%** |
| C1 | N = min(100, 11D−1) · crash D≤4 | (2) D20/DEF-A5 | direta | 25/25 exato; **smoke MMF1 D=2 → N=21, 61/61 FE, `status=ok`** — o fix anti-crash confirmado no código de hoje |
| C2 | bi-pop: N/subpop, pool 2N, wmax=20 | (1) B12.6 | direta+eco | `n_pool==2N` em 1.967/1.967 e 10/10; ③-online = 2N linhas/ciclo em 25/25 |
| C3 | u=5 revogado · batch variável | (2) v2.2/DI-18 | direta | **140 ciclos (7,1%) com lote>5**, máx **11**; **18 saídas no nível 2, TODAS com `n_front2==1`**; ramo (Q,U) em **99,08%** |
| C4 | hard-stop, dsmerge, guards, cache-hit | (2) D21/D61/L4/D89 | direta | `hard_stop` com `fe==maxfe` em 22/22 e no smoke (61); **`c0=1` em 25/25**; DEF-L4 e batch-vazio: **0 disparos** |
| C5 | miolo do código (CSO r1/r2, SBX/PM, ES_PDR, porte) | **T** | não-verif. | sem rastro nos dados |
| **T1** 🆕 | **instrumentação: DI-10 + os campos NOVOS da T11** | (2) DI-10/B-03/I-09/G-6b | direta | DI-10 completo em 1.967/1.967 e 10/10; `schema_version` 1→**2**, `campanha_id` 0/25→**presente**, `repo_hash` ''→**`195f64ed…`**, `sonda.desligada` ausente→**declarado** |
| **T2** 🆕 | **reprodutibilidade do mecanismo entre MÁQUINAS** | **(3)** 🎯 | direta | ① da s42 (vm3) **≠** ① do smoke (M1) na MESMA célula/semente; divergência no **1º infill**; HV difere **2,31%** (> piso O-18 de 1,55%) |

---

### 2.1 U1 — orçamento de FE: 31D−1, não 11d+119

**(a) Artigo.** §*Parameter settings*: "*FEmax* is set to 11*d*+119". **(b) SPEC.** D21/D61 impõem `maxFE = 31D−1` com hard-stop exato em todos os 21 configs — a régua transversal que torna 25 problemas × 16 algoritmos comparáveis; o bundle registra "FEmax nativo do paper NÃO adotado". **(c) Observado** (`t02_universal_s42.csv`): `maxfe == fe_final == len(①) == 31D−1` em **25/25**, com `(fe_index == arange(n)).all()` verdadeiro em 25/25 e `footer.termino='normal'` + `status='ok'` em 25/25. Exemplos: MMF1 D=2→61; DTLZ1 D=7→216; BBOB D=10→309; DTLZ2 D=12→371; MMF16_20 D=20→619; WFG/DTLZ7 D=22→681; ZDT1/3 D=30→929. **Smoke:** 61/61 no MMF1, `termino='normal'`. **(d) Mecanismo.** O hard-stop corta o lote no meio via exceção no wrapper de FE; como o lote é variável (1..11), o corte é a regra: 22 das 25 células disparam o guard. **(e) (2) — sancionado (D21/D61); direta.**

### 2.2 U2 — DoE 11D−1 LHS injetado do artefato

**(a) Artigo.** Alg. 1 l.1: LHS com 11*d*−1 indivíduos. **(b) SPEC.** A CONTAGEM é do paper; a PROVENIÊNCIA é nossa — D63/D88 carregam `data/doe/{pb}/doe_{pb}_42.parquet` e D94 troca `UniformPoint-Latin` por `A2 = Problem.Evaluation(Problem.data.X0)` em `MMRAEA.m:24`. **(c) Observado.** `(fase=='init').sum() == 11D−1` em 25/25 (21, 76, 109, 131, 219, 241, 329); ΔX contra o artefato **bit-exato em float32** nas 25; `doe_hash` ⑤ ≡ sidecar ≡ header do ⑥ em 25/25; `footer.cp_init=true` 25/25. **Prova nova, e ela é forte:** as **21 linhas de `init` do smoke T11 são BIT-IDÊNTICAS às da s42** (`max|ΔX| = 0,0` nas 21 primeiras linhas da ①, `t05_divergencia_maquina.py` §1) — a injeção do DoE atravessa 5 dias de commits, dois sistemas operacionais e duas arquiteturas de CPU sem mover um bit. **(d) Mecanismo.** O parquet é float64, o export float32 sem arredondamento (D53); o único desvio é 1 ULP relativo ao bound. **(e) (1) — conforme o artigo (proveniência declarativa D63/D94); direta.**

### 2.3 U3 — um retreino dos 3 modos por ciclo externo

**(a) Artigo.** Alg. 1 l.6–8: ARBFs, FitRBF e FNRBF reconstruídos a cada iteração externa. **(b) SPEC.** Sem mudança; ④ exige 1 linha por retreino, `sigma_dict` declara regime online. **(c) Observado.** `|④| = |c141_gen| = |fit_series|` em **25/25** (Σ 1.967) e no smoke (10 = 10 = 10), `geracao` sequencial 1..n, `tempo_fit_s` > 0 em 1.967/1.967. `modelo_hp.n` cresce monotonicamente com o arquivo (smoke: 21→25→26→29→36→41→43→49→51→54). **(d) Mecanismo.** Ledger fechado por construção; a última geração não gera evento porque o hard-stop a interrompe — daí `n_geracoes = |c141_gen| + 1` em **25/25** e no smoke (11 = 10+1). **(e) (1); direta.**

### 2.4 U4 — sonda: cadência, finalProbe e o join posicional

**(a) Artigo.** Nada — instrumentação nossa. **(b) SPEC.** DI-09/DI-12.5 §17.2.2: bloco S=2.000 Sobol aninhado em `g==1 ∨ g%2==0` + **finalProbe** se a última geração for ímpar; join **POSICIONAL** (regra 5 do CONTRATO §10), hash `x_hash_online` conferido. **(c) Observado.** s42: **1.024 blocos, 2.000 linhas exatas em 1.024/1.024**, `n_falhas=0` em 25/25, `n_blocos` ≡ nº de eventos `sonda` ≡ `③.geracao.nunique()` em 25/25; join contra `sonda_{pb}.parquet[0:2000]` com max|ΔX| ≤ 1 ULP-float32 da amplitude do bound. **No smoke o teste é mais nítido do que na s42**: `n_geracoes=11` é ÍMPAR, e as gerações sondadas são **[1, 2, 4, 6, 8, 10, 11]** com `motivo` = 6× `cadencia` + **1× `final`** — o finalProbe **rodou no código de hoje** e cobriu a geração ímpar terminal. **(d) Mecanismo.** O Sobol é ANINHADO (DI-13.5): as 2.000 primeiras linhas do gabarito de 20.000 são o bloco online. Ler as 2.000 ÚLTIMAS dá max|ΔX| = 0,967 (armadilha 5 do §9, re-medida). **(e) (2) — instrumentação sancionada; direta.**

### 2.5 U5 — WAPE por bloco e a cobertura que não existe aqui

**(a) Artigo.** Nada (o paper mede só IGD final). **(b) SPEC.** §5 congela WAPE por objetivo no espaço CRU e cobertura ±1,96σ. Aqui `transf_tipo` é NULL e `espaco_modelo='cru'` em todas as linhas de sonda — não há des-transformação. **(c) Observado** (`t10_saude.py` sobre `sonda_f52e.csv`, 2.279 blocos-objetivo): ΔWAPE 1º→último bloco tem **mediana −0,5%**, reproduzindo o "−0%" da régua oficial, com distribuição bimodal — 13 células melhoram (BBOB_F1 −13,6%, BBOB_F17 −11,9%, MMF4 −11,1%, DTLZ2 −10,6%) e 6 explodem (MMF1 **+610,9%**, ZDT6 +589,7%, MMF11_L +349,2%, DTLZ1 +277,2%, DTLZ4 +224,9%, WFG1 +222,7%). **`cobertura95` é NULL em 2.279/2.279** blocos-objetivo — N/A por desenho, citando a regra §5: `sigma_0` (inteiro em [0, 2(2N−1)]) e `sigma_1` (std entre 3 alvos incomensuráveis) não são desvios preditivos por objetivo. **(d) Mecanismo.** A deriva é LOCAL: a RBF por `mldivide` interpola no suporte e **explode fora dele**; onde o arquivo colapsa numa região (MMF1, ZDT6, MMF11_L — D=2 com 21 pontos iniciais), a extrapolação sobre a grade Sobol uniforme diverge. **(e) (2) — definição congelada; direta.**

### 2.6 U6 — invariante dupla do timing (DI-13.10)

**(a)** Nada no artigo. **(b) SPEC.** §17.6/DI-13.10: `tempo_geracao_s` mede o ciclo do ALGORITMO e não inclui a sonda; logo `fit+busca ≤ geracao` sempre, e a soma COM a sonda viola exatamente nas gerações com bloco. **(c) Observado.** `tempo_fit_s + tempo_busca_s − tempo_geracao_s > 1e-6`: **0 violações em 1.967 linhas de ④** (25/25 células). No manifesto do smoke o desconto aparece explícito: `tempo_pred_sonda_s = 0,250 s` de `tempo_total_s = 2,263 s` (11,0%) fora do relógio do algoritmo. **(d) Mecanismo.** O cronômetro do ciclo é parado antes da chamada da sonda e retomado depois — é o que preserva a comparabilidade de custo entre configs com e sem sonda. **(e) (2) — DI-13.10; direta.**

### 2.7 U7 — `fe_treino_max` MONOTÔNICO (ao contrário de b1/b4/c217)

**(a) Artigo.** Alg. 1 l.18 `A ← A ∪ Pnew`: o arquivo só cresce, sem subamostragem. **(b) SPEC.** A regra 9 do CONTRATO §10 (DI-13.15) declara `fe_treino_max` NÃO-monotônico **em b1/b4/c217**; para c141 nada foi mudado. **(c) Observado.** `diff(fe_treino_max) ≥ 0` em **25/25 células** — sem exceção; e no smoke (20→24→25→28→35→40→42→48→50→53). **(d) Mecanismo.** Sem cap de treino, o `mldivide` cresce O(n³) — 924³ ≈ 7,9e8 no ZDT1 — e é o que sustenta 269 s no ZDT1 contra 7 s no MMF1. **(e) (2) — conforme o declarado; direta.**

### 2.8 U9 — o ledger de FE entre camadas

**(a) Artigo.** Alg. 1 l.17: `FE ← FE + |Pnew|`. **(b) SPEC.** D57 (`solution_id`=dedup por X) + D89 (cache-hit não consome FE) ⇒ `11D−1 + Σlote − cache + lote_final = fe_final`. **(c) Observado.** Fecha em **25/25**, com `lote_final ∈ [1,4]`. Global: **3.836 + 7.030 − 68 + 58 = 10.856** = Σ`fe_final`. No smoke: **21 + 37 − 1 + 4 = 61** = `fe_final`. **(d) Mecanismo.** O `lote` conta os SELECIONADOS; quando um é clone de ponto já avaliado, o wrapper serve do cache e a ① não ganha linha — daí o termo `− cache_hits`. **(e) (2) — D57/D89; direta.**

### 2.9 U10 — erro de fantasia: 9.084 interpolações × 7.491 fantasias

**(a) Artigo.** Implícito: a cascata escolhe pelos valores PREDITOS. **(b) SPEC.** DI-13/D57: `real_solution_id` liga a ③-online à ①. **(c) Observado.** 16.575 linhas com `real_solution_id`, divididas em duas naturezas: **(i) 9.084** cujo ponto já estava no treino (ver F2) e **(ii) 7.491** que só viraram FE depois — a fantasia genuína, com **otimismo (μ < f real) em 66,8% (mediana das 25 células)**, chegando a 95,2% no MMF1, 82,5% no MMF16_20 e 80,5% no DTLZ1. **No smoke, 37/37 das linhas de fantasia são otimistas (100%)**, com |erro| mediano 2,42 — o mesmo viés, na célula em que ele é mais forte. **(d) Mecanismo.** A cascata seleciona onde o modelo prevê BEM e o modelo prevê otimisticamente fora do suporte — o mesmo padrão do b1 (75–100%). **(e) (2) — análise nossa sobre D57; direta.**

### 2.10 ⬆ U11 — NÃO-PERTURBAÇÃO DA SONDA: de **T** para **MEDIDO E APROVADO**

**(a) Artigo.** Nada — é invariante 🔴 do CONTRATO §3.1: a sonda não pode alterar o estado do RNG nem o curso da busca. **(b) SPEC/T11.** Na F5 este aspecto foi classificado **T (não-verificável — exige run pareado)**; o `L.7` do bundle declarava ZERO RNG em infill/RBF/dsmerge/ES_PDR, mas isso era evidência de CÓDIGO, não de DADO. A campanha T11 entregou o gêmeo MATLAB da flag `UA_DD_SAEA_SONDA_OFF` (`SondaState.m`, commit `4126b78`) — a doutrina explícita é **"desarmar, não sumir"**: o objeto da sonda continua existindo, os contadores vão a 0 e o manifesto DECLARA `desligada=true`, para o `build_manifest` não quebrar em `[].n_blocos`. **(c) Observado — medido por mim, não lido** (`t04_smoke_g6.py`):

| camada | `g6_com` | `g6_sem` | veredito |
|---|---|---|---|
| ① `__real.parquet` sha256 | `d7f20cd0c4648c3f` | `d7f20cd0c4648c3f` | **BIT-IDÊNTICA** ✅ |
| ② `__pop.parquet` sha256 | `885d26ccd9eea400` | `885d26ccd9eea400` | **BIT-IDÊNTICA** (sinal EXTRA, não reivindicado pela T11) ✅ |
| ③ `__surrogate.parquet` | 14.420 linhas (420 online + 14.000 sonda) | **420 linhas** (só online) | encolheu ✅ |
| ⑤ `sonda.desligada` / `n_blocos` | `false` / **7** | **`true`** / **0** | declarado ✅ |
| ⑥ eventos `rec='sonda'` | 7 | **0** | ✅ |
| `fe_final` / `n_geracoes` / `cache_hits` | 61 / 11 / 1 | 61 / 11 / 1 | idênticos ✅ |

O hash `d7f20cd0c4648c3f` é **exatamente** o publicado em T11 §5 para o c141 — **confirmado por medida independente**, não aceito por leitura. Os **três sinais** exigidos pela lição 4.2.5 da T11 estão presentes simultaneamente (`desligada=true` + `n_blocos→0` + ③ ENCOLHENDO), o que descarta a hipótese de "desarme que nunca disparou". **Bônus:** o smoke (campanha `t11-smoke-195f64e`) e o `g6_com` (campanha `195f64ed0a69_2026-07-30`) — dois runs independentes, em momentos diferentes — têm ①, ② **e ③** com os mesmos sha256, provando também a **reprodutibilidade intra-máquina** do config, incluindo as 14.000 predições de sonda. **(d) Mecanismo.** A sonda do c141 é predição pura (`c141_sonda.m` sem RNG); a flag intercepta em `due(g)`, `probeOffline`, `finalProbe` e `probeEstratificada`. **(e) ⬆ (1) — invariante do contrato conforme, agora com verificabilidade DIRETA. Sai do balde T.** *Este é o único aspecto do c141 que MUDOU DE CLASSE na T11, e mudou para melhor.*

### 2.11 F1 — o kernel: multiquadrática c=1, poly=0, SEM scaling de X

**(a) Artigo.** §*Radial basis functions*: a multiquadrática com "*σ* … set as 1 as default". Nada sobre normalizar X ou cauda polinomial. **(b) SPEC.** B12.7 (fechada na v2.2): `rbf_build(S,Y)` com defaults idênticos ×3 — MQ c=1, poly=0, **nenhum scaling**, código herdado do paper (D30 🟠). **(c) Observado.** `modelo_hp = {bf_type:'MQ', bf_c:1, poly:0}` em 1.967/1.967 (s42) e 10/10 (smoke) — eco declarativo. O teste **discriminativo** da F5 sobre a s42 (MQ c=1 vence 8 kernels alternativos + cauda linear em **14/14** pontos-teste, por 2×–10.000×) segue de pé. **Re-executado no dado PÓS-T11** (`t09_cascata_smoke.py`, coluna `erro_mu_rel`): reconstruindo μ do zero, com X **CRU**, a partir da ① e comparando com a ③ do smoke, o erro relativo mediano por ciclo vai de **3,6e-07** (g=8) a **5,3e-04** (g=1), mediana **5,9e-05** — inteiramente atribuível ao armazenamento float32 (D53) amplificado por cond(Φ). **(d) Mecanismo.** Se houvesse scaling, Φ = √(‖xᵢ−xⱼ‖²+1) mudaria de escala e a identificação falharia — é o que se vê nas variantes c=0,5 e c=2, que são exatamente re-escalas do argumento. O sucesso com X cru **prova** o "sem scaling" por dado, em ambos os corpora. **(e) (1) — conforme o artigo; direta.**

### 2.12 F2 — `mldivide` denso SEM regularização · **⚠ ERRATA CONTRA A PRÓPRIA F5**

**(a) Artigo.** Não especifica o solver; a Eq. do RBF assume o sistema interpolante Φw = F. **(b) SPEC.** Bundle: "solve por `mldivide` denso SEM regularização", mantido como código oficial (🟠 impl→código), com "risco de mau condicionamento, mantido"; dsmerge ds=1e-14 tão frouxo que quase-duplicatas passam. **(c) Observado — e aqui eu corrijo o meu próprio relatório da F5.** A F5 afirmou: *"nas 9.084 linhas … `μ − f_real = 0,0` com máximo 0,0 em **25/25 células**"*. **Isso é falso na leitura mais estrita.** Re-medindo (`t07_interpolacao.py`, `t07b_residuo_relativo.py`):

- por LINHA: erro exatamente 0 em **15/25 células**, não 25/25;
- por PAR (linha, objetivo) — a unidade correta: **18.480 de 18.748 = 98,571% com μ − f EXATAMENTE 0,0**;
- os 268 pares não-nulos: em **196 deles (73,1%) o f verdadeiro é EXATAMENTE 0,0** (pontos de eixo de DTLZ/ZDT), onde "zero exato" é um teste infinitamente severo; o resíduo/escala-da-célula tem **mediana 7,6e-15, p95 3,1e-13** e **99,63% deles abaixo do eps-float32 (1,19e-7)**;
- o **único** resíduo materialmente acima é MMF1, **7,2e-07 relativo** — e ele cai exatamente no ciclo cujo **1/cond(Φ) = 6,55e-17**, o pior condicionamento medido no config inteiro (`t08_rcond_residuo.csv`).

**A conclusão da F5 não só sobrevive como fica MAIS FORTE.** Um ridge produziria resíduo sistemático **proporcional a λ em TODAS as linhas**; o que se mede é zero-bit em 98,6% e resíduo ~1e-14 da escala concentrado onde Φ degenera — a assinatura exata de um solve interpolante backward-estável sem regularização. O mau condicionamento continua medido: `1/cond(Φ)` até **4,48e-25** (BBOB_F5), **275 de 1.967 ciclos (14,0%) abaixo de 1e-12**, com **ZDT1 em 156 dos 173**. No smoke o padrão se repete em escala menor: `rcond` mínimo **5,36e-14**, **3 dos 10 ciclos abaixo de 1e-12**. **(d) Mecanismo.** Φ é densa sobre o arquivo inteiro; conforme ele adensa (o dsmerge de 1e-14 não funde quase-duplicatas), as linhas ficam quase colineares e cond(Φ) explode. O `mldivide` devolve LU com warning `RCOND` sem truncar — daí a interpolação continuar exata nos nós e a extrapolação ficar selvagem. **(e) (2) — sancionado (D30/D29 🟠); direta. Com a errata numérica registrada.**

### 2.13 F3 — nível 1: pool PREDITO ∪ arquivo VERDADEIRO (o código contra a letra do paper)

**(a) Artigo.** §*Sampling strategy*: "…the nondominated sorting is based on the predicted objective values of the approximation models (ARBFs)" — a letra diz PREDITO para os dois conjuntos. **(b) SPEC.** A linha de fidelidade v2.2 registra o oposto: "nível 1 mistura POP predita + **ARQUIVO verdadeiro**", divergência 🟠 impl→código sancionada porque é o que o código oficial faz. **(c) Observado.** s42 (`b08_arquivo.csv`, teste discriminativo ciclo a ciclo nas 25 células): **arquivo VERDADEIRO 1.649/1.967 (83,8%) × arquivo PREDITO 1.575 (80,1%)**, com separação célula a célula onde há sinal (DTLZ2 65/65 × 62/65; WFG4 90/125 × 67/125; MMF16_20 72/119 × 57/119; BBOB_F1 13/59 × 6/59). Seis células fecham em **100%** com o arquivo verdadeiro (DTLZ1, DTLZ2, DTLZ3, DTLZ4, MMF1, MMF4 — **246 ciclos consecutivos**). Quatro regras alternativas de dedup foram testadas e **todas são piores** (`b08_regras.csv`: bruto 1.649 × unique-μ 178 × sem-empate 1.371 × unique-global 178). **No dado PÓS-T11 a identidade fecha em 10/10 ciclos** (`t09_cascata_smoke.csv`, coluna `ok_v`) — o MMF1 D=2 não é discriminativo (as duas hipóteses acertam 10/10, como já acertavam 12/12 na s42), então o smoke **confirma que o nível 1 continua sendo `F1(Ptot∪A)∩Ptot`**, sem separar qual arquivo. A discriminação continua ancorada na s42. **(d) Mecanismo do resíduo de 318 ciclos.** 317 das 318 falhas (99,7%) têm |Δ| ≤ nº de membros do 1º front com margem de dominação EXATAMENTE 0,0: o float32 do export (D53) **cria** empates que o double do MATLAB não tem — exatamente o que a **regra 11 do CONTRATO §10**, escrita nesta campanha, agora formaliza ("dominância sobre ① ou ⑦ é LOSSY"). **A regra 11 é a formalização normativa do achado que este config produziu na F5.** **(e) (2) — sancionado no bundle e confirmado por teste discriminativo; direta, com teto de precisão declarado.**

### 2.14 F4 — Fit1 = fitness SDE (Eq. 7) com normalização min-max

**(a) Artigo.** Eq. 7: `Fit(x) = min_{q∈P\{x}} ‖max(0, f(q) − f(x))‖₂`; Alg. 3 l.4 calcula sobre os objetivos APROXIMADOS. **(b) SPEC.** Sem mudança; o bundle acrescenta o hazard "SDE via min-max → NaN se fmax=fmin" e o guard `+eps` (DEF-L4). **(c) Observado.** s42: condicionado ao conjunto P estar certo (os 1.649 de F3), a identidade fecha a rel<1e-5 em **1.647/1.649 = 99,9%**, com erro relativo mediano **1,22e-7 = eps-float32**. **Reconstruído do zero no smoke T11** — apenas com os μ logados na ③ e a min-max sobre o conjunto do nível 1 — o `fit1.max` do ⑥ fecha a **rel < 1e-5 em 10/10 ciclos**, com erro relativo **mediano 5,39e-07** e máximo 5,25e-06 (`t09_cascata_smoke.csv`). Casos concretos: g=1 logado 0,0630670 × reconstruído 0,0630670; g=8 0,0973390 × 0,0973380. **(d) Mecanismo.** O `min` sobre distâncias euclidianas de objetivos min-max-normalizados é contínuo e não-expansivo nos μ; o erro do float32 se propaga sem amplificação. Nenhuma célula produziu NaN de SDE — o guard DEF-L4 tem **0 disparos** nos dois corpora. **(e) (1) — conforme o artigo (Eq. 7); direta.**

### 2.15 F5 — nível 2 e o vetor de sinais [−Fit1, Fit2, −Fit3]

**(a) Artigo.** Alg. 3 l.7: "Do nondominated sorting (−**Fit**1, −**Fit**2, **Fit**3)", com Fit2 = FitRBF (SDE) e Fit3 = FNRBF (front number). **(b) SPEC.** DI-18 fixou **[−Fit1, Fit2, −Fit3]** porque no CÓDIGO os índices 2 e 3 estão trocados em relação à nomenclatura do paper. O agente-4 reporta a versão do paper (`(−Fit1,−Fit2,Fit3)`) e o gatilho `u=5`, sem conhecer nem a troca nem a revogação — **caso-livro da cadeia de precedência §0**. **(c) Observado — a semântica provada por ESCALA.** Nos 1.967 eventos: razão mediana |fit3|/|fit1| = **1,414** (mesma ordem — ambos SDE) contra |fit2|/|fit1| = **230,5** (ordem de front number). Medianas globais: fit1 = 0,0077, fit3 = 0,0104, fit2 = 0,740; fit2 percorre a escala inteira de níveis ND (DTLZ2 g64: [0,277; 9,56]) e vai a negativo por extrapolação (ZDT6 mediana −192,2). **Logo, no LOG, fit2 = FN-predito e fit3 = SDE-predito, e o vetor de DI-18 é literalmente o mesmo objetivo do paper.** `n_front2` fecha em 1.587/1.967 (80,7%; 85,1% condicionado a F3+F4). **(d) Mecanismo.** Fit2/Fit3 herdam o erro float32 amplificado por cond(Φ); comparações ND próximas do empate flipam sobre ~13 candidatos medianos. Nada indica divergência de regra. **(e) (1) — conforme o artigo; DI-18 é reconciliação de NOMES, não desvio. Direta.**

### 2.16 F6 — Q, U (Eq. 8) e o nível 3

**(a) Artigo.** Eq. 8: `Q_j = Σ_i R_i,j` e `U_j = Σ_{i<k}|R_i,j − R_k,j|`; Alg. 3 l.13–16: `Pa ← F1(Q,U)`, `Pb ← argmax U`, `Ps ← Pa ∪ Pb`. **(b) SPEC.** Sem mudança na fórmula; DI-10 acrescenta a telemetria. **(c) Observado.** Q e U reconstruídos por ranks ascendentes dos três vetores assinados batem **exatamente** (min E máx) em **1.490/1.967**; os limites teóricos Q ∈ [3, 3|P|] e U ∈ [0, 2(|P|−1)] são respeitados em **1.967/1.967**; o lote reconstruído == logado em **1.706/1.967 (86,7%; 89,9% condicional)**. **(d) Mecanismo.** Q e U são funções de RANKS — degraus discretos, estáveis a ruído numérico exceto quando duas fitness estão a menos de 1 ULP. O resíduo é sempre pequeno e concentrado nas mesmas células em que F3/F4 já derraparam, e nunca ocorre nas limpas (DTLZ2 60/65, ZDT1 168/173, WFG9 120/124). **(e) (1) — conforme o artigo; direta.**

### 2.17 F7 — σ do c141: `σ_0` = U de ranks do POOL COMPLETO · `σ_1` = std dos 3 modos (D45 🟢)

**(a) Artigo.** U só existe DENTRO da cascata, sobre o punhado de candidatos do nível 2; o paper nunca calcula U para a população inteira, nem a dispersão bruta entre os três alvos (que é literalmente incomensurável: SDE ~1e-2 contra front number ~1e0). **(b) SPEC.** D45 (extensão 🟢 declarada) exporta a incerteza NATIVA — U de ranks sobre os 2N do pool — para o c141 ter coluna comparável na ③, e TAMBÉM o proxy tipo-ensemble `std([Fit1,Fit2,Fit3])` para permitir comparação com o c149, declarando a incomensurabilidade. O `sigma_dict` do ⑤ diz isso literalmente em 25/25 células e no smoke (regra 3 do CONTRATO §10 respeitada antes de qualquer leitura da ③). **(c) Observado — três identidades.** **(i)** σ_0 é inteiro em **100,0% das 386.382 linhas**, 0 NaN; σ_1 nunca é NaN (0 em 386.382). **(ii) Teto teórico EXATO**: com pool 2N e ranks 1..2N, U ≤ 2(2N−1); o máximo observado por célula é **exatamente esse teto em 25/25** — 398 nas 21 células com N=100, **302** no DTLZ1 (N=76) e **82** nas 3 células D=2 (N=21). Nenhuma excede nem fica aquém. **No smoke o teste passa também: max(σ_0) = 82 = 2(2·21−1) EXATO, e `U_pool_max` do ⑥ ≡ max(σ_0) do ③ em 10/10 ciclos** (na s42 essa segunda identidade fechava em 1.771/1.967 = 90,0%). **(iii)** σ_1 discrimina a convenção: **ddof=1** (MATLAB) dá erro relativo mediano **9,04e-04**; ddof=0 (numpy) TRAVA em **0,1836 = 1 − 1/√1,5** — separação de 200×. **(d) Mecanismo.** U(pool) é inteiro derivado de três permutações de 2N elementos; uma inversão de rank por float32 muda U em ±2 nos dois pontos trocados. O teto exato em 25/25 é o teste mais duro e passa sem exceção: **prova que o U é calculado sobre o pool INTEIRO, não sobre o subconjunto do nível 2** (onde o teto seria 2(|P|−1) ≪ 398). **(e) (2) — extensão nossa sancionada (D45 🟢); direta.**

### 2.18 F9 — o PAPEL da incerteza: 87,80% dos ciclos gastam uma FE só por causa de U

**(a) Artigo.** Alg. 3 l.15: `Pb ← Maximum uncertainty of P`; §*Sampling strategy*: "the solution with the maximum values of uncertainty is **also** selected" — uso bivalente (minimizar U no sorting E forçar o máximo de U). O agente-4 classifica `aceita-explora` (G4=9, G5=8) e nota que "o papel de U não é isolado de Q". **(b) SPEC.** Sem mudança; a instrumentação DI-10 permite medir, pela primeira vez, se `Pb` é redundante. **(c) Observado** (`b06_incerteza.csv`, re-agregado): reconstruindo a frente ND de (Q,U) (=Pa) e o argmax U (=Pb) ciclo a ciclo, **Pb NÃO está em Pa em 1.727 dos 1.967 ciclos = 87,80%**. Como |Pa| tem mediana 2–3 e o lote mediano é 3–4, isso equivale a **1.727 de 7.030 FEs de infill = 24,6% do orçamento de amostragem gasto puramente por discordância do ensemble**. Por célula: 100% no MMF1, 96,9% no DTLZ2, 96,4% no BBOB_F5, mínimo 72,7% no MMF4. E os pontos são de fato distantes: `dist_min_arquivo` mediano por célula de 0,106 a 1,514, com máximos de 45,9 (WFG5) — exploração real. **(d) Mecanismo.** Pa tende a conter os pontos de menor Q com U pequeno; o ponto de MAIOR U quase nunca é ND ali porque U alto vem junto de Q ruim (os três modos discordam onde nenhum é confiável). `Pa ∪ Pb` é quase sempre união estrita. **É a medida que o paper nunca fez, e ela sustenta o G5 do agente-4 com evidência empírica.** **(e) (1) — conforme o artigo, com quantificação nova; direta.**

### 2.19 C1 — N = min(100, 11D−1), o crash D≤4 e o fix

**(a) Artigo.** §*Parameter settings*: "The population size *N* is set to 50, *wmax* is set to 20". **(b) SPEC** (`SPEC:809`): D20/DEF-A5 fixam `N = min(100, 11D−1)` — o teto 100 vem do `Problem.N` default do PlatEMO e o piso 11D−1 é anti-crash: `ES_PDR.m:20` estoura quando o pool da 1ª geração (`NI + 2⌊NI/2⌋`) é menor que N, e a v5.2.1 corrigiu a faixa real para **D≤4** (a v5.2 dizia D≤9). B12.6 esclarece que `Problem.N` é **por subpopulação** — fidelidade ao paper seria N=50; ficamos em 100. **(c) Observado.** `params.N_subpop == min(100, 11D−1)` em **25/25**: **21** nas três células D=2 (MMF1, MMF4, MMF11_L — exatamente a faixa de crash), **76** no DTLZ1 (D=7) e 100 nas 21 restantes. **Nenhuma célula com D≤4 caiu, nem na s42 nem no smoke.** O smoke é a re-verificação do fix no código de HOJE: MMF1, **D=2**, `N_subpop=21`, `status='ok'`, `fe_final=61=maxfe`, `n_pool=42` em 10/10 ciclos, zero exceção — **o fix anti-crash está vivo e funcionando após a T11**. **(d) Mecanismo.** Em D=2 o DoE tem 21 pontos; com N=100 o `Rank(1:N)` de `ES_PDR.m:20` indexaria além do pool. O piso 11D−1 iguala N ao DoE e neutraliza o bug sem tocar no fluxo; o efeito colateral é o pool de infill ser 42 e não 200 nessas três células. **(e) (2) — sancionado (D20/DEF-A5); direta.**

### 2.20 C2 — bi-população: N por subpop, pool 2N, wmax = 20

**(a) Artigo.** Alg. 1 l.9–14 e §*Effectiveness of bi-population*: "|P1| = |P2| = |Ptot| = 100" na ablação com N=50 por subpop. **(b) SPEC.** B12.6 confirma `Problem.N` POR subpopulação e pool 2N; wmax=20 sem alteração; subpopulações sequenciais sem migração. **(c) Observado.** `n_sub1 == n_sub2 == N` e `n_pool == 2N` em **1.967/1.967** ciclos (s42) e **10/10** (smoke), e a ③-online tem **exatamente 2N linhas por geração** em 25/25 células — 386.382 linhas, sem uma única geração fora do padrão; no smoke, 420 = 10 × 42. `wmax = 20` no `params` e no header do ⑥ em 25/25 e no smoke (eco declarativo — o loop interno não é logado). **(d) Mecanismo.** A ③ grava a população final do bi-pop (DEF-C2); o contador ser 2N invariante em 1.977 ciclos prova que a seleção ambiental NSGA-II corta as duas subpopulações de volta a N em todas as 20 gerações internas. **(e) (1) — conforme o artigo; direta (wmax por eco).**

### 2.21 C3 — u=5 REVOGADO, batch variável, e o batch-zero que nunca veio

**(a) Artigo.** `u` é "maximum number of new samples", fixado em 5, e aparece em Alg. 3 l.3 e l.9 (`if |P| > u`). O agente-4 repete "escolhe até `u` (=5) pontos por iteração". **(b) SPEC.** A v2.2 estabeleceu que **a cascata do código não usa u=5 em lugar nenhum**; DI-18 o revogou formalmente. O bundle ainda prevê "batch VARIÁVEL, pode ser 0". **(c) Observado** (`t03_cascata.csv`, re-medido nos 1.967): **(i) u=5 não existe** — a distribuição do lote é **1:33 · 2:343 · 3:670 · 4:517 · 5:264 · 6:85 · 7:41 · 8:12 · 9:1 · 11:1**, com **140 ciclos (7,1%) acima de 5** e máximo **11** (BBOB_F5). Sob u=5 nenhum lote passaria de 6 e a cauda 7–11 seria impossível. **(ii) O gatilho é ">1 candidato"** — **0 dos 1.967 ciclos sai no nível 1**, e os **18 que saem no nível 2 têm TODOS `n_front2 == 1`** (BBOB_F55 1, MMF4 1, WFG2 7, WFG4 1, ZDT1 2, ZDT3 1, ZDT4 1, ZDT6 4). **(iii)** o ramo (Q,U) roda em **1.949/1.967 = 99,08%**. **(iv) o batch nunca foi 0**: `lote ≥ 1` em 1.967/1.967. No smoke o lote vai de 1 a 7 em 10 ciclos (2 acima de 5) e a saída é nível 3 em 10/10. **(d) Mecanismo.** O nível 1 devolve mediana de 113 candidatos (56,5% do pool) — longe de esvaziar; o nível 2 reduz à mediana 13. Como |Pa| ≥ 1 e Pb sempre existe, `lote ≥ 1` é estrutural: o batch-zero exigiria `P = ∅` no nível 1. **(e) (2) — sancionado (v2.2/DI-18), confirmado por dado nos dois corpora; direta.**

### 2.22 C4 — hard-stop, overshoot, dsmerge, guards e o `c0=1`

**(a) Artigo.** Alg. 1 l.3 `while FE ≤ FEmax` só testa no topo — o último lote pode ultrapassar, e o paper não trata disso; nada sobre fusão de quase-duplicatas. **(b) SPEC.** D21/D61 exigem hard-stop EXATO (overshoot ≤ |PopNew|−1 cortado no meio do lote); DEF-L4 torna 🟠-obrigatório o `+eps` do SDE; o guard batch-vazio [IMPL] protege `Problem.Evaluation([])`; D89 zera o custo do cache-hit. **(c) Observado.** **Hard-stop:** 22 das 25 células disparam, e em **22/22 o `fe` do guard == `maxfe`** — corte exato, zero overshoot; as 3 sem guard (BBOB_F17, DTLZ3, WFG1) aterrissaram exatas com `lote_final=1`. No smoke: guard `hard_stop` com `fe=61 == maxfe`. **Reconciliação:** `Counter(guard.name)` do ⑥ ≡ `manifest.cache_hits` ≡ `footer.cache_hits` em **25/25** (Σ = 68), e **só DOIS nomes de guard aparecem em toda a rodada**: `cache_hit` (68) e `hard_stop` (22). **DEF-L4 e batch-vazio: 0 disparos** nos dois corpora — instalados e nunca necessários. **`c0 = 1` é lei aqui também: exatamente 1 cache-hit cai no init em 25/25 células** e no smoke — a mesma assinatura estrutural do harness que o piloto b1 encontrou, agora confirmada em 26 células e duas máquinas. **(d) Mecanismo.** O primeiro `Problem.Evaluation` do init reencontra a chave-X do ponto 0 no cache (registro duplo do wrapper), gastando 0 FE; os demais cache-hits são clones da cascata. O dsmerge de 1e-14 é tão apertado que quase-duplicatas engordam Φ — é o que produz os rcond de F2 (`modelo_hp.n` difere de `|① ≤ ftm|` em 59 de 1.967 ciclos). **(e) (2) — sancionado (D21/D61/DEF-L4/D89/[IMPL]); direta.**

### 2.23 C5 — o miolo do código (T)

Invisíveis nos dados, nos DOIS corpora: (i) `r1, r2` **escalares** do CSO (divergência do CSO canônico mantida como código); (ii) SBX proC=1,0/disC=20 e PM proM=1/d (só eco do bundle); (iii) `ES_PDR` = NSGA-II literal nas duas subpopulações com SDE só no torneio do CSO; (iv) as 20 gerações internas do bi-pop (só a população final chega à ③); (v) o porte de 3 linhas (`MMRAEA.m:21/:50`, `EAOptimization.m:33`) e o init STOCK byte-igual ao K-RVEA (S.2#18); (vi) "zero toolbox"; (vii) a contabilidade de RNG; (viii) a arbitragem de empates dentro do `NDSort` do PlatEMO. Todos cobertos por `anchors.json` + `repos.lock` + o `repo_hash` **novo** (I-09) — mas por declaração, não por dado. **T — não-verificável; fora do denominador.**

### 2.24 🆕 T1 — a instrumentação: DI-10 (velha) + os campos NOVOS da T11

**(a) Artigo.** Nada. **(b) SPEC/T11.** DI-10 exigiu o enriquecimento do ⑥ (`n_por_nivel`, `modelo_hp`, `U_pool_max`, `f_best`, `dist_min_arquivo`, tríades fit1/2/3 e Q/U, `ramo_QU`, `motivo`, `nivel`). A T11 acrescentou, **transversalmente e não por config**: `campanha_id` + `schema_version=2` (B-03/G3), `repo_hash` (I-09/G7 — "o elo D80 run↔código, era `''` em 666/666 células"), `sonda.desligada` (G-6b) e o writer do ⑥ em append com `O_APPEND`+`flock` (B-11/G1). **(c) Observado — o antes-e-depois medido** (`t02_universal_s42.csv` × `t04_smoke_g6.py`):

| campo | s42 (25 células) | smoke T11 | veredito |
|---|---|---|---|
| `schema_version` | **1** em 25/25 | **2** | novo ✅ |
| `campanha_id` | **ausente em 25/25** | `t11-smoke-195f64e` | novo ✅ |
| `repo_hash` | **`''` em 25/25** | `195f64ed0a69dc3f6c64c66912076e243be90c49` | novo ✅ |
| `sonda.desligada` | **ausente em 25/25** | `false` (e `true` no `g6_sem`) | novo ✅ |
| `timing.tempo_aval_real_s` | **não-nulo em 25/25** (0,0225 a 1,347 s) | 0,0173 s | ⚠ **já existia** |
| `params` (N_subpop/wmax/kernel/ds_dsmerge) | presente em 25/25 | idêntico | já existia |
| `sigma_dict` | presente em 25/25 | idêntico | já existia |
| `REGRA_DO_ROTULO` | 0/25 | ausente | **N/A por desenho** |
| linhas `regime='sonda_estratificada'` | 0 | 0 | **N/A por desenho** |
| telemetria DI-10 completa | 1.967/1.967 ciclos | **10/10** | preservada ✅ |
| linhas malformadas no ⑥ | **0 de 3.131** | 0 de 21 | o splice B-11 não tocou c141 |
| header=1 ∧ footer=1 | **25/25** | 1/1 | ✅ |

**⚠ Errata que eu levanto contra o briefing da torre:** o item "I-3 `tempo_aval_real_s`: não-nulo nos 15 smokes MATLAB ✅" sugere instrumentação nova. Para o c141 **ela não é nova** — o campo já está preenchido em **25/25 células da s42** (mín. 0,0225 s no MMF1, máx. 1,347 s), porque a **DI-12.4 já havia instrumentado `src/FEBudget.m`**; a própria mensagem do commit `83dbdac` diz isso ("a DI-12.4 instrumentou só o `src/FEBudget.m`"). O I-02 da T11 é ganho **do stack Python**, não do MATLAB. **(d) Mecanismo.** DI-10 é o que converte o c141 de "config declarativo" em "config provado por dado": sem `U_pool_max` não haveria como provar F7; sem `modelo_hp.n`/`fe_treino_max` não haveria como reconstruir o treino e nenhuma das identidades F1–F6 existiria. Os campos da T11 não tocam o mecanismo — fecham o elo de **proveniência** (D80). **(e) (2) — instrumentação sancionada (DI-10 + B-03/I-09/G-6b); direta.**

### 2.25 🆕 T2 — REPRODUTIBILIDADE DO MECANISMO ENTRE MÁQUINAS 🎯 **(3)**

**(a) Artigo.** Nada — mas todo o desenho experimental (30 sementes × 695 células × 3 máquinas) pressupõe que a máquina não é uma variável. **(b) SPEC.** Não há decisão que sancione não-reprodutibilidade cross-máquina no stack MATLAB. Existe a simétrica no stack Python: **DI-05/DEF-L2 desliga o kernel C++ fusionado do BoTorch justamente para obter "caminho Python puro, determinístico Mac×Linux"** (`SPEC:434`, verbete c262) — ou seja, o projeto **já decidiu que determinismo Mac×Linux importa**, e legislou sobre ele **num stack só**. O piso O-18 (HV ≤1,55%; IGD+ ≤58,98%) é o número em vigor para "ruído entre máquinas".

**(c) Observado — a medida.** A célula `main/c141/MMF1/42` existe nos dois corpora: s42 em **vm3** (`tempo_f52d.csv`, x86-Linux) e smoke T11 no **Mac Apple M1 Pro** (arm64). O `doe_hash` é o mesmo, o `maxfe` é o mesmo, os arquivos de código do c141 não mudaram desde 2026-07-19. E mesmo assim:

| | s42 (vm3) | smoke T11 (M1) |
|---|---|---|
| ① `__real.parquet` X-sha256 | `6f7838baf7529cd3` | `75c9d6a7b21a4d17` |
| linhas de `init` idênticas | **21/21** | ✅ |
| **1ª linha divergente** | — | **`fe_index = 21` = o PRIMEIRO infill** |
| `n_geracoes` / blocos de sonda | 13 / 8 | **11 / 7** |
| `n_acumulado` do treino | 21,24,27,29,32,36,39,44,46,50,52,56 | 21,25,26,29,36,41,43,49,51,54 |
| IGD+ / HV / \|ND\| | 0,043498 / 0,778452 / 14 | 0,042825 / **0,760446** / 15 |

**O que EXCLUI a hipótese "o código mudou":** (i) `git log` de `c141_instrument.m`, `c141_sonda.m` e `algorithms/c141_MMRAEA` para em 2026-07-19, **antes** da s42; (ii) as três mudanças em arquivos compartilhados (`FEBudget.arquivoX` read-only, `SondaState.desligada`, campos do ⑤) não tocam RNG nem busca, e `probeEstratificada` só está fiada em b4/c217/e74; (iii) `git log -p` de `src/experiment.m` desde 2026-07-25 **não tem uma única linha com `rng(`, `RandStream`, `rand(`, `randn`, `randperm` ou `shuffle`**.

**O que EXCLUI a hipótese "o fluxo de RNG mudou"** (`t05_divergencia_maquina.py` §4, o controle mais forte deste relatório): dos **15 smokes MATLAB**, **7 têm a ① BIT-IDÊNTICA à s42** — `b4`/MMF1 (40 infills), `c217`/MMF1 (40), `moead`, `nsga2`, `nsga3`, `smsemoa`/DTLZ2 (240 infills cada, 13–18 gerações de SBX/PM+NDSort) e `e103`/MMF1. E entre os que divergem, **`b1` só diverge no 38º infill (fe=58 de 61) e `b3` no 40º (fe=60)** — 37 e 39 infills consecutivos bit-idênticos antes de separar. Um deslocamento no fluxo de RNG divergiria no primeiro infill em todos; **o que se vê é um evento de cauda raro que leva dezenas de ciclos para aparecer**.

**O que RESTA e o que eu NÃO consigo resolver.** Testei o modelo diretamente: no **bloco de sonda da geração 1** — mesmos 2.000 pontos Sobol fixos, treino de 21 pontos bit-idênticos — **`mu_0` e `mu_1` são idênticos em 2.000/2.000 nas duas máquinas, com max|Δ| = 0**. Mas o schema do parquet é `float` (**float32**, D53): o teste prova apenas que o modelo concorda **até ~1,2e-7 relativo**; abaixo disso o artefato não resolve. Já a população da geração 1 diverge macroscopicamente (0 pares (x0,x1) em comum entre as 42 de cada lado; distância mediana ao vizinho mais próximo **0,046**). **Conclusão medida:** a divergência é compatível com uma diferença de último bit em `mldivide`/BLAS entre **x86-64+MKL** e **ARM64+Accelerate**, amplificada por ~840 comparações de fitness surrogate por ciclo (20 gerações internas × 21 pares de CSO × 2 subpopulações) até um flip de comparação; e é **incompatível** com mudança de código ou de RNG. O que **não posso** provar com estes artefatos é o mecanismo abaixo do float32 — isso exige um re-run instrumentado com dump em double nas duas máquinas.

**A consequência que importa, e ela extrapola o c141** (`t12_piso_maquina.csv`, 15 células, mesma semente, mesmo código de mecanismo):

| config/problema | ΔIGD+ | **ΔHV** | veredito |
|---|---:|---:|---|
| e74/DTLZ2 | 14,66% | **10,16%** | *(confundido com o fix DI-45)* |
| **c238/MMF1** | **80,03%** | **9,69%** | puro efeito de máquina |
| e74/ZDT1 · e74/MMF1 | 24,47% · 13,85% | 3,54% · 2,93% | *(DI-45)* |
| **c141/MMF1** | 1,55% | **2,31%** | puro efeito de máquina |
| **b1/MMF1** | **12,50%** | 2,10% | puro efeito de máquina |
| b3/MMF1 · e7/MMF11_L | 3,55% · 14,59% | 0,65% · 0,62% | dentro do piso |
| b4, c217, e103, 4 pisos | **0,00%** | **0,00%** | bit-idênticos |

**O piso O-18 de HV (≤1,55%) é EXCEDIDO em 6 das 15 células**, chegando a 10,16%; o de IGD+ (58,98%) é excedido em 1 (c238, 80,03%). Para o c141 especificamente: ΔHV = **2,31% > 1,55%**, ΔIGD+ = 1,55% ≪ 58,98%.

**(d) Por que aconteceu.** Aritmética de ponto flutuante não é associativa; BLAS/LAPACK escolhem ordem de redução por CPU/threading. Os 7 configs bit-idênticos são exatamente os que **não decidem por valor contínuo de surrogate** — 4 pisos (sem surrogate), b4/c217 (decidem por CLASSE ternária, robusta a último bit) e e103 (offline, uma única fatoração). Os que divergem — b1 (EI), b3 (Kriging), c141 (RBF), c238 (EIM), e7 (NN) — todos ordenam candidatos por valor real predito. **É a mesma assinatura numérica que a F5 já havia documentado no c141 sob o rótulo "empates criados pelo float32", agora vista de fora, entre máquinas.**

**(e) (3) 🎯 — desvio sem decisão sancionadora; verificabilidade direta para o FATO, não-verificável (T) para o mecanismo abaixo do float32.** **Escopo honesto:** é achado **TRANSVERSAL do harness**, não defeito do c141, e **o impacto sobre a fidelidade do c141 é medido como NULO** — a cascata reconstrói igual nas duas máquinas (s42/vm3: nível 1 em 1.649/1.967 e Fit1 em 99,9%; smoke/M1: nível 1 em **10/10** e Fit1 em **10/10**). O que ele ameaça é **comparabilidade entre células rodadas em máquinas diferentes**, e a campanha M8/M9 vai rodar 695 células em **3 máquinas**.

---

## 3. % por classe (Botão 2)

**Total = 25 aspectos · T = 1 (C5) · denominador = 25 − 1 = 24.**

| classe | aspectos | n | % |
|---|---|---:|---:|
| **(1)** conforme o artigo | U2, U3, **U11**, F1, F4, F5, F6, F9, C2 | **9** | **37,5%** |
| **(2)** desvio/extensão sancionado | U1, U4, U5, U6, U7, U9, U10, F2, F3, F7, C1, C3, C4, T1 | **14** | **58,3%** |
| **(3)** desvio inexplicado 🎯 | **T2** (transversal — reprodutibilidade cross-máquina) | **1** | **4,2%** |
| **T** (fora do denominador) | C5 (miolo do código sem rastro nos dados) | 1 | — |

**Contra a F5** (26 aspectos, T=2, denominador 24): (1) 33,3%→**37,5%**, (2) 66,7%→58,3%, (3) 0,0%→**4,2%**, **T 2→1**. A verificabilidade subiu de **24/26 = 92,3%** para **24/25 = 96,0%** — o único T restante é o miolo de código, que nenhum artefato pode alcançar.

Marca declarativa visível (Botão 3): U2 (proveniência do DoE), C2 (wmax=20), F1 (eco `modelo_hp`) e T1 (`params`/`sigma_dict`/`repo_hash`) contam com eco declarativo; em U2 e F1 o eco é **redundante** — o ΔX bit-exato e a identificação discriminativa do kernel provam o mesmo por dado.

---

## 4. 🆕 AS CORREÇÕES DA T11 — o que tocou o c141, e o que de fato funciona

**O c141 não aparece em NENHUM item da FASE A** (A1 c154 · A2 c122 · A3 b5 · A4 c262 · A5 e103 · A6 sobol_batch/nsga3 · A7 b4 · A8 e74 · A9 c217 · A10 c311 · A11 os 4 classificadores). Tudo o que chegou nele veio da FASE G, transversal. Item a item, com evidência **medida** ou **lida**:

| # | melhoria | implementada? | **funciona de fato?** | evidência | o que falta |
|---|---|---|---|---|---|
| **1** | **G-6b · par de não-perturbação (§3.1) no stack MATLAB** — a flag `UA_DD_SAEA_SONDA_OFF` | ✅ `src/SondaState.m` commit `4126b78`: `desligada` em `due()`, `probeOffline`, `finalProbe`, `probeEstratificada` | ✅ **SIM, e é a única correção que mudou a CLASSE de um aspecto** | MEDIDO: ① sha256 `d7f20cd0c4648c3f` idêntica com/sem sonda; ② também idêntica; ③ 14.420→420; ⑤ `desligada=true`, `n_blocos=0`; ⑥ 7→0 eventos `sonda`; `fe_final`/`n_geracoes`/`cache_hits` intactos | 1 célula (MMF1 D=2, 61 FE). Não cobre problemas em que a extrapolação da RBF é violenta (DTLZ4, ZDT6) nem D alto |
| **2** | **B-03/G3 · `campanha_id` + `schema_version=2`** | ✅ | ✅ SIM | MEDIDO: ausente em **25/25** da s42; presente no smoke (`t11-smoke-195f64e`) e nos dois lados do par G-6 | — |
| **3** | **I-09/G7 · `repo_hash` no ⑤ MATLAB** (`experiment.m:repo_hash_corrente`) | ✅ commit `83dbdac` | ✅ SIM | MEDIDO: `''` em **25/25** da s42 → `195f64ed0a69dc3f6c64c66912076e243be90c49` no smoke. **Fecha o elo D80** que sustenta todo o aspecto declarativo do C5 (o balde T) | O elo passa a existir, mas o C5 continua T: o hash diz QUAL código, não O QUE ele faz |
| **4** | **B-11/G1 · writer do ⑥ em append (`O_APPEND`+`flock`)** | ✅ | ✅ SIM, **e o c141 nunca precisou dele** | MEDIDO: **0 linhas malformadas em 3.131 eventos** nas 25 células da s42, header=1 e footer=1 em 25/25. As 49 malformadas da ERRATA 1 eram `b1_gen`/`sonda`, não c141 | — |
| **5** | **I-02/G7 · `tempo_aval_real_s`** | ✅ no Python | ⚠ **NÃO É NOVIDADE NO MATLAB** | MEDIDO: **não-nulo em 25/25 células da s42** (0,0225 a 1,347 s), porque a DI-12.4 já instrumentara `src/FEBudget.m`. **Errata contra o briefing da torre (item I-3)** | nada a fazer — o ganho foi do stack Python |
| **6** | **G-1/G-2/G-3/G-4/G-7/B-15 · os 6 portões em modo `campanha`** | ✅ | ✅ SIM | MEDIDO por mim (não lido): `gates_de_proveniencia('main','c141','MMF1',42,…,modo='campanha')` dá **6/6 OK** nas TRÊS pastas (smoke, g6_com, g6_sem). G-1: "37/37 bit-idênticos, max\|ΔX\|=0, 0 id órfão na ①" | — |
| **7** | **A11 · sonda estratificada · I-1 `*_ids` · I-2 `REGRA_DO_ROTULO`** | ➖ **N/A por desenho** | — | MEDIDO: 0 linhas `sonda_estratificada` e 0 `REGRA_DO_ROTULO` nos DOIS corpora. Correto: o A11 fia o bloco só em b4/c217/e74/c122, e c141 não é classificador (não tem rótulo a recuperar) | nada — e a **regra 12** do CONTRATO fica vacuamente satisfeita: c141 só tem a régua Sobol |

**🔎 Procurei o padrão do c217 ("campo presente, dado sentinela") e NÃO o encontrei no c141.** Todo campo novo que existe no smoke carrega valor real e verificável: `repo_hash` é um SHA-1 de 40 caracteres que resolve para um commit real; `campanha_id` é único e ausente da produção; `desligada` alterna corretamente entre os dois lados do par; `params`/`sigma_dict` são idênticos entre s42 e smoke. **Nenhum sentinela, nenhum carimbo falso, nenhum campo decorativo.** A razão estrutural é simples e vale registrar: **o c141 não recebeu campo NOVO de conteúdo semântico** — recebeu proveniência (`campanha_id`, `repo_hash`) e um interruptor (`desligada`). A instrumentação de conteúdo dele é a DI-10, de 2026-07-19, e ela já estava completa em 1.967/1.967 ciclos.

**O que a T11 NÃO resolveu para o c141:** (i) o `C5` continua T — nenhum artefato alcança `r1/r2` escalares do CSO, SBX/PM, `ES_PDR` ou as 20 gerações internas, e nenhuma correção da campanha tentou; (ii) o **ciclo WFG5 g=76** que a F5 deixou como confirmação opcional continua sem re-run; (iii) e a campanha **introduziu** a questão T2 ao rodar os smokes numa arquitetura de CPU diferente da que produziu a s42 — o que é, ironicamente, o achado mais útil que ela gerou para este config.

---

## 5. Comparação canônica (Entrega 2)

**Setup do paper** (§*Experimental studies* + agente-4 `_full`): 3-obj DTLZ1–7, 2-obj ZDT1–4,6, **3-obj** WFG1–9, d ∈ {20,40,60,100} (84 instâncias), FEmax = 11d+119, 20 runs, IGD com |R|=10.000, Wilcoxon 0,05 + Friedman. Baselines: CPS-MOEA, MOEA/D-RBF, EDN-ARMOEA, MCEA/D. Métricas nossas re-computadas por mim e **idênticas** às da F5.2c (c141/MMF1: IGD+ 0,043498, HV 0,778452, |ND| 14 — bate `metricas_finais_f52c.csv` linha 125).

| nossos problemas | veredito | sentido executado (mesma máquina vm3 que os 4 pisos) |
|---|---|---|
| **DTLZ2** (D=12, M=3, 371 FE) × d=20/339 FE | **âncora direcional** | paper: "significantly outperforms … on all the test instances". Aqui IGD+ **0,040861**, rank **2/16**, razão vs smsemoa **0,240**, HV 0,710 × 0,377. **CONFIRMA ✓** |
| **DTLZ7** (D=22, M=3, 681 FE) × d=20 | **âncora direcional** | paper: idem. Aqui **rank 1/15**, IGD+ 0,034897 × nsga3 1,1247 → razão **0,031 (32×)**; HV 0,509 × 0,000. **O mais forte do config ✓** |
| **DTLZ3** (D=12, M=3) × d=20 | **âncora direcional** | paper: "All the algorithms show poor performance"; MCEA/D melhor, MMRAEA 2º. Aqui IGD+ 226,77, rank 8/16, razão **1,748** (perde). **A fraqueza declarada CONFIRMA ✓** |
| **DTLZ4** (D=12, M=3) × d=20 | **âncora direcional** | paper: EDN-ARMOEA melhor em DTLZ4 d=20 — MMRAEA não vence. Aqui rank 10/15, razão **1,566**. **CONFIRMA ✓** |
| **ZDT1** (D=30, M=2, 929 FE) × d=20/40 | **âncora direcional** | paper: melhor em quase todo o ZDT. Aqui razão vs nsga2 **0,096 (10×)**, rank 5/16. **CONFIRMA ✓** |
| **ZDT3** (D=30) × d=20/40 | **âncora direcional** | paper: ZDT3 d=20 é **exceção declarada**. Aqui razão 0,646 (ainda bate o piso), rank 5/15 — "exceção sem colapso" ✓ |
| **ZDT4** (D=10, M=2) × d=20 | **âncora direcional fraca** | paper: ZDT4 multimodal é a **outra exceção**. Aqui rank 3/17, razão 0,598, mas **HV=0 e \|ND\|=1** — colapsou num ponto. **CONFIRMA ✓** |
| **ZDT6** (D=10) × d=20 | **âncora direcional fraca** | paper: melhor. Aqui razão **0,016 (62×)**, rank 3/16 ✓ |
| **DTLZ1** (D=7, M=3) | **incomensurável (eixo X)** | d=7 é 2,9× MENOR que o menor d do paper (20). Observado rank 14/17, razão 1,927; registrado, não julgável |
| **WFG1/2/4/5/9** (D=22, **M=2**) | **incomensurável (eixo X — M difere)** | o paper usa WFG **3-objetivo**. Nenhum indicador compara. **MAS o diagnóstico do surrogate compara**: o paper confessa "*RBFs are not suitable for approximating the objective functions of the WFG problems*". Nossa régua Sobol mede exatamente isso: corr mediana **0,074 (WFG5) · 0,135 (WFG4) · 0,242 (WFG9) · 0,230 (WFG1) · 0,325 (WFG2)** — o pior grupo do config, com WAPE PLANO (0,21→0,21 em WFG5). **A confissão do paper está CONFIRMADA POR DADO**, como diagnóstico, não como comparação |
| **MMF1/4/11_L/16_20 · BBOB_F{1,5,17,22,37,49,55}** | **incomensurável (eixo X)** | ausentes do paper |

**Placar: comparável 0 · âncora direcional 8/8 com sentido CONFIRMADO (4 forças + 4 fraquezas declaradas) · incomensuráveis 17.** Botão 4 respeitado: nenhum "artigo melhor" declarado sob incomensurabilidade. **Âncora J com caveat metrológico forte** (paper: DTLZ2 M=3 d=20 IGD = 1,2010e-1; ZDT1 d=20 = 3,3486e-2, IGD CRU, |R|=10.000, média de 20 runs; nós: objetivos e referência NORMALIZADOS (D69), 1 semente, dimensões diferentes): DTLZ2 D=12 dá 0,57× do valor do paper (mais fácil, orçamento 1,09× — coerente) e ZDT1 D=30 cai **entre** o d=20 e o d=40 do paper (mais difícil, orçamento 2,7× — coerente). **Ordem de grandeza compatível; NÃO é teste de âncora ±3σ.**

---

## 6. Saúde (s42 = corpus principal)

**Contrato e integridade — re-verificados:** `contrato_f52b.csv` e `integridade_f52a.csv` têm **ZERO linhas para c141**; `params` presente em 25/25 (c141 nunca esteve entre os 7 configs sem `params` — ERRATA 11 da T11); 3.131 eventos ⑥ sem uma linha malformada; header=1 e footer=1 em 25/25; `n_retries=0` em 25/25.

| célula | ΔWAPE 1º→últ | corr mediana | IGD+ | melhor piso | razão | rank | \|ND\| |
|---|---:|---:|---:|---|---:|---:|---:|
| BBOB_F1 | −13,6% | 0,992 | 0,004107 | 0,1360 nsga2 | **0,030** | 3/17 | 144 |
| BBOB_F5 | −9,6% | 0,960 | 0,008650 | 0,1193 smsemoa | **0,073** | 2/17 | 107 |
| BBOB_F17 | −11,9% | 0,892 | 0,018792 | 0,0206 nsga2 | 0,912 | 2/17 | 13 |
| BBOB_F22 | −5,6% | 0,953 | 0,034329 | 0,1591 smsemoa | **0,216** | **1**/17 | 31 |
| BBOB_F37 | −2,0% | 0,892 | 0,102438 | 0,2539 smsemoa | 0,403 | **1**/17 | 18 |
| BBOB_F49 | +0,6% | 0,565 | 0,080852 | 0,2071 nsga2 | 0,390 | **1**/17 | 24 |
| BBOB_F55 | +21,9% | 0,364 | 0,013118 | 0,6061 nsga2 | **0,022** | **1**/16 | 47 |
| DTLZ1 | +277,2% | 0,737 | 87,394 | 45,346 nsga2 | 1,927 | 14/17 | 12 |
| DTLZ2 | −10,6% | 0,970 | 0,040861 | 0,1701 smsemoa | **0,240** | 2/16 | 173 |
| DTLZ3 | +40,8% | 0,821 | 226,77 | 129,77 smsemoa | 1,748 | 8/16 | 9 |
| DTLZ4 | +224,9% | **0,088** | 0,442326 | 0,2824 nsga3 | 1,566 | 10/15 | 17 |
| DTLZ7 | −7,3% | 0,999 | 0,034897 | 1,1247 nsga3 | **0,031** | **1**/15 | 195 |
| MMF1 | **+610,9%** | 0,348 | 0,043498 | 0,0570 nsga3 | 0,763 | 7/17 | 14 |
| MMF11_L | +349,2% | 0,977 | 0,018393 | 0,0232 nsga3 | 0,794 | 5/17 | 15 |
| MMF16_20 | −8,6% | 0,948 | 0,027116 | 0,0316 smsemoa | 0,858 | 4/15 | 186 |
| MMF4 | −11,1% | 0,709 | 0,041392 | 0,0377 nsga3 | 1,098 | 14/17 | 21 |
| WFG1 | +222,7% | 0,230 | 0,540107 | 0,4954 smsemoa | 1,090 | 9/14 | 87 |
| WFG2 | −7,8% | 0,325 | 0,169463 | 0,1228 nsga2 | 1,380 | 9/16 | 68 |
| WFG4 | −1,7% | 0,135 | 0,076473 | 0,0444 smsemoa | 1,721 | 7/16 | 65 |
| WFG5 | −0,5% | **0,074** | 0,050556 | 0,0931 nsga2 | 0,543 | 3/16 | 120 |
| WFG9 | +0,3% | 0,242 | 0,075082 | 0,0955 nsga3 | 0,786 | **1**/16 | 73 |
| ZDT1 | +139,2% | 0,993 | 0,042654 | 0,4441 nsga2 | **0,096** | 5/16 | 31 |
| ZDT3 | +48,0% | 0,896 | 0,222530 | 0,3443 nsga2 | 0,646 | 5/15 | 24 |
| ZDT4 | −2,7% | 0,489 | 26,245 | 43,898 nsga2 | 0,598 | 3/17 | **1** |
| ZDT6 | +589,7% | 0,318 | 0,104249 | 6,4457 nsga2 | **0,016** | 3/16 | 7 |

- **Posição vs pisos:** bate o melhor dos 4 pisos em **18/25**, com **6 primeiros lugares absolutos** (BBOB_F22, F37, F49, F55, DTLZ7, WFG9) e **rank IGD+ médio 4,84** de ~16,4 algoritmos — reproduzindo o "c141 4,8" da prévia F5.2c. c141 e os 4 pisos rodaram TODOS na vm3: o piso de ruído entre máquinas **não contamina esta leitura** (e, à luz de T2, isso deixou de ser detalhe e passou a ser condição).
- **Trajetórias:** **0 violações de monotonicidade do IGD+ e 0 do HV em 475 transições** (20 checkpoints × 25 células) — re-medido.
- **Régua Sobol (regra 12 — c141 SÓ tem a régua; 0 linhas estratificadas, nenhum bloco a separar):** 2.279 blocos-objetivo, `cobertura95` NULL em 2.279/2.279 (N/A por desenho). Quatro regimes nítidos: **(A) o RBF domina** — DTLZ7 (corr 0,999), ZDT1 (0,993), BBOB_F1 (0,992), DTLZ2 (0,970), MMF16_20 (0,948), que são exatamente as células em que o config bate o piso com folga; **(B) o RBF é cego** — WFG5 (0,074), WFG4 (0,135), WFG1 (0,230), WFG9 (0,242), WFG2 (0,325), com WAPE PLANO do começo ao fim; **(C) o RBF diverge numericamente** — MMF1, ZDT6, MMF11_L, DTLZ1, DTLZ4; **(D) ruído de ranking** — BBOB_F55 (0,364), BBOB_F49 (0,565), onde a métrica final é ótima mesmo assim.
- **PRIOR 9,5 — a divergência do RBF no MMF1: CONFIRMADA E REPRODUZIDA NUMA SEGUNDA MÁQUINA.** s42: μ_1 ∈ **[−120,91; 384,36]** contra f ∈ [3,4e-5; 8,62]; último bloco do objetivo 1 com WAPE **7,74**, `MAE/MAE_ref` **10,20**, spearman **0,042** (o prior anotava μ ∈ [−145, 210]). No smoke T11 o mesmo fenômeno é **PIOR**: μ_1 ∈ **[−210,75; 674,82]**, WAPE **18,51**, `MAE/MAE_ref` **24,41**, spearman **0,015**. O objetivo 0 se mantém saudável nos dois (spearman 0,608 e 0,747). **E o efeito na métrica final continua não se materializando:** IGD+ 0,043498 (vm3) e 0,042825 (M1) contra 0,0570 do melhor piso — **c141 bate os 4 pisos no MMF1 nas duas máquinas**. Leitura: o mecanismo é fiel, o surrogate é ruim, e a cascata — que no MMF1 gasta **100% dos ciclos** forçando o argmax-U — compensa.
- **DTLZ4 é o pior surrogate do config** (corr mediana **0,088**, mínimo −0,073; `MAE/MAE_ref` mediano 6,70) — pior que as WFG que o próprio paper declara indomáveis pela RBF. Causa: α=100 ⇒ x^100 vira quase função-degrau, que a multiquadrática c=1 sem scaling não representa. Explica mecanicamente o rank 10/15 e casa com a derrota declarada do paper em DTLZ4.

---

## 7. SCORE, recomendação e VEREDITO vs F5

## **SCORE: 9,5/10 · ACEITAR** · **VEREDITO: MANTEVE**

**Por que MANTEVE, com a causa nomeada.** O mecanismo **não mudou** — e isso é medido, não suposto: os arquivos do c141 (`c141_instrument.m`, `c141_sonda.m`, `algorithms/c141_MMRAEA`) não têm commit desde **2026-07-19**, sete dias antes da célula da s42, e nenhum dos arquivos compartilhados alterados pela T11 toca um caminho que o c141 execute. Nenhuma das 16 erratas da T11, nenhum dos 4 CRÍTICOS, nenhum dos 18 ALTOS e nenhum dos 11 itens da FASE A endereça este config. **"Manteve" é o veredito esperado e é o correto.**

O que **melhorou de fato**, e é pouco mas é real:
1. **U11 saiu do balde T.** O par G-6 (`d7f20cd0c4648c3f`, medido por mim nas duas pastas) provou por DADO o invariante 🔴 do CONTRATO §3.1 que a F5 só podia declarar por CÓDIGO. **A verificabilidade do config foi de 24/26 (92,3%) para 24/25 (96,0%)**, e o T restante é o único inalcançável por artefato.
2. **A cascata foi re-provada no código de HOJE.** No smoke pós-T11 reconstruí do zero: kernel MQ c=1/poly=0/sem-scaling (erro rel. mediano 5,9e-05), nível 1 em **10/10**, Fit1/SDE em **10/10** a rel<1e-5, teto de σ_0 = 82 EXATO com `U_pool_max`≡max(σ_0) em **10/10**, ledger 21+37−1+4=61, finalProbe exercitado numa geração ímpar. **A fidelidade não é uma afirmação sobre um dado velho — ela vale para o binário que vai rodar a campanha.**
3. **O elo D80 finalmente existe** (`repo_hash` de `''` em 25/25 para um SHA-1 real), o que dá base ao aspecto declarativo do C5 sem tirá-lo do balde T.

O que **piorou / o que eu corrijo**:
4. **Apareceu um (3) que a F5 não tinha** — mas ele é **transversal do harness, não do c141**, e seu impacto sobre a fidelidade do c141 é **medido como nulo** (a cascata reconstrói igual nas duas máquinas). Por isso não desconto o score de fidelidade por ele. **Se o autor preferir contá-lo contra o config, o score é 9,0** — é decisão dele (D97), e eu deixo o botão exposto.
5. **Uma errata contra o meu próprio relatório da F5** (F2: "μ−f = 0,0 em 25/25 células" → o correto é **98,571% dos pares e 15/25 células**). A errata **tensiona o número e reforça a conclusão** — nenhuma reclassificação.

**Por que não 10:** as identidades de CONTAGEM da cascata fecham em 81–90% na s42, não em 100%; o resíduo está explicado (99,7% coberto por empates criados pelo float32 do export, D53 — hoje normatizado na regra 11 do CONTRATO), mas **o ciclo `main/c141/WFG5` g=76 segue sem causa provada** e a T11 não o re-rodou. Some-se o T de C5 e o (3) transversal.

**Recomendação: ACEITAR.** Com dois pedidos ao autor, ambos baratos e nenhum bloqueante: (i) **decidir sobre T2 antes do disparo M8/M9** — o piso O-18 de HV (1,55%) está desatualizado, e as 695 células × 30 sementes vão sair de 3 máquinas; (ii) **estender o par G-6 do c141 a UMA célula de D alto** (DTLZ2 D=12 ou ZDT1 D=30), porque o par existente é MMF1 D=2 com 61 FE — o regime em que a RBF é mais bem-condicionada.

---

## 8. Aspectos classe (3) 🎯 — evidência completa

**Um: T2 — a ① da mesma célula/semente difere entre vm3 (x86-64/Linux) e Mac (Apple M1/arm64), e o desvio de HV excede o piso O-18.**

- **Célula:** `main/c141/MMF1/42`. **s42:** `resultados_experimentos/c141/MMF1/42/exp_main_c141_MMF1_42__real.parquet`, X-sha256 `6f7838baf7529cd3`, `n_geracoes=13`, 8 blocos de sonda. **T11:** `evidencia_T11/smoke_matlab/experiments/main/c141/exp_main_c141_MMF1_42__real.parquet`, X-sha256 `75c9d6a7b21a4d17`, `n_geracoes=11`, 7 blocos.
- **DoE idêntico:** `doe_hash` igual; **as 21 linhas de `init` são bit-idênticas** (max|ΔX| = 0,0). A divergência começa em `fe_index = 21` — **o primeiro infill**, |ΔX| = 1,089.
- **Código idêntico:** `git log` de `src/c141_instrument.m` / `src/c141_sonda.m` / `algorithms/c141_MMRAEA` para em 2026-07-19 (`76fbbd7`, `47daa76`, `ff38f2b`); a s42 é de 2026-07-26. `git log -p -- src/experiment.m` desde 2026-07-25 não contém nenhuma linha com `rng(`/`RandStream`/`rand(`/`randn`/`randperm`/`shuffle`. `FEBudget.arquivoX` é read-only e `SondaState.probeEstratificada` só é chamada de `b4_sonda.m`/`c217_sonda.m`/`e74_sonda.m`.
- **RNG idêntico (controle):** 7 de 15 smokes MATLAB têm a ① **bit-idêntica** à s42 — `b4`/MMF1, `c217`/MMF1, `moead`/`nsga2`/`nsga3`/`smsemoa`/DTLZ2 (240 infills cada) e `e103`/MMF1. E `b1` diverge só no 38º infill (fe=58/61), `b3` no 40º (fe=60/61) — **37 e 39 infills consecutivos idênticos**, incompatível com deslocamento de fluxo de RNG.
- **Modelo idêntico até a resolução do artefato:** no bloco de sonda da geração 1 (mesmos 2.000 pontos Sobol, mesmo treino de 21 pontos), `mu_0` e `mu_1` são **idênticos em 2.000/2.000, max|Δ| = 0**. Mas `pq.read_schema(...).field('mu_0').type` = `float` (**float32**, D53): o teste limita a diferença do modelo a **< ~1,2e-7 relativo** e não resolve abaixo disso.
- **Efeito na população:** já na geração 1 as 42 posições divergem — **0 pares (x0,x1) em comum**, distância mediana ao vizinho mais próximo **0,0462**, máxima 0,177.
- **Efeito nos indicadores (o que importa para a campanha):** c141/MMF1 ΔIGD+ = **1,55%**, ΔHV = **2,31%**. Transversalmente, nas 15 células com par: ΔHV **acima do piso O-18 de 1,55% em 6/15**, máximo **10,16%** (e74/DTLZ2); ΔIGD+ máximo **80,03%** (c238/MMF1). Os configs que decidem por CLASSE (b4, c217) ou não têm surrogate (4 pisos, e103) dão **0,00% em ambos**.
- **Impacto sobre a fidelidade do c141: NULO, medido.** Cascata reconstruída nas duas máquinas: vm3 → nível 1 em 1.649/1.967, Fit1 em 1.647/1.649; M1 → nível 1 em 10/10, Fit1 em 10/10, teto de σ_0 exato, ledger fechado.
- **Custo de fechar:** um par de re-runs instrumentados da mesma célula nas duas máquinas com dump em **double** de Φ, w e dos μ do primeiro ciclo. **Prioridade: MÉDIA-ALTA no eixo transversal, BAIXA no eixo de fidelidade.**
- **Recomendação:** re-medir o piso O-18 com estes 15 pares (ou fixar arquitetura por config na campanha) **antes** do disparo M8/M9. `evidencia/f5/t11/baterias/c141/t12_piso_maquina.csv` já traz a tabela pronta.

*Confirmação OPCIONAL herdada da F5 (não é classe 3, e a T11 não a executou):* `main/c141/WFG5` geração 76 — o único ciclo dos 1.967 em que o nível 1 reconstruído (101) fica abaixo do logado (103) **sem nenhum empate exato** que explique. 0,05% dos ciclos, sem efeito em indicador.

---

## 9. Teto de verificabilidade (T) + armadilhas + o que este corpus NÃO permite

**T (1 aspecto, C5):** `r1/r2` escalares do CSO e o passo do perdedor · SBX proC=1,0/disC=20 e PM proM=1/d · as 20 gerações internas do bi-pop e a seleção `ES_PDR`/crowding · o porte de 3 linhas e o init STOCK byte-igual ao K-RVEA · a contabilidade de RNG e a ausência de migração · a arbitragem de empates dentro do `NDSort` do PlatEMO. Cobertos por `anchors.json` + `repos.lock` + `repo_hash` (I-09, novo) — **declaração, nunca dado**.

**O que este corpus NÃO permite verificar** (mesmo com a T11): **(1)** o mecanismo abaixo do float32 — o export D53 é o teto duro de toda reconstrução; **(2)** se o c141 é fiel em D≥12 no CÓDIGO DE HOJE — o único smoke é MMF1 **D=2, 61 FE, 10 ciclos**, o regime mais benigno do config, e as células ricas (ZDT1 D=30 com 173 ciclos, DTLZ7 com 102) só existem no dado PRÉ-T11; **(3)** o comportamento sob 30 sementes — tudo aqui é semente 42; **(4)** se `mldivide` degrada diferente em D alto entre as arquiteturas — o par de máquinas só existe em MMF1; **(5)** qualquer coisa sobre a sonda estratificada, `REGRA_DO_ROTULO` ou `*_ids`, que **não se aplicam ao c141 por desenho** (não é classificador) — o que também significa que a regra 12 do CONTRATO é vacuamente satisfeita aqui e que o c141 não tem o risco "campo presente, dado sentinela" que derrubou o c217.

**Armadilhas confirmadas na escala (para a F5.4 e para a R4):**
1. **O float32 do export (D53) é o teto real da reconstrução da cascata, não a álgebra.** 317 de 318 falhas do nível 1 (99,7%) têm |Δ| ≤ nº de membros do front com margem de dominação EXATAMENTE 0,0. **Hoje isto é norma: regra 11 do CONTRATO §10.**
2. **`fit1/fit2/fit3` do ⑥ são estatísticas do conjunto do NÍVEL 2, não do nível 1.** Só `fit1.max` coincide nos dois conjuntos; usar o máximo como âncora.
3. **A troca Fit2↔Fit3 entre paper e código é real e é só de NOME.** Validar contra a letra (`−Fit1,−Fit2,Fit3`) reprova run fiel; a ESCALA prova a semântica (|fit3|/|fit1| = 1,41; |fit2|/|fit1| = 230).
4. **Procurar `u=5` reprova run fiel** (DI-18): 140 ciclos com lote > 5, máximo 11; o gatilho real é "resta >1 candidato", provado pelas 18 saídas de nível 2 todas com `n_front2 == 1`.
5. **A sonda online lê as 2.000 PRIMEIRAS linhas do gabarito de 20.000** (Sobol aninhado, DI-13.5): comparar com as 2.000 ÚLTIMAS dá max|ΔX| = **0,967** e falso-alarme catastrófico. Conferir sempre `x_hash_online`.
6. **`n_geracoes` do manifesto = nº de eventos `c141_gen` + 1** em 25/25 e no smoke — a geração final é iniciada (grava sonda) e cortada pelo hard-stop antes do log.
7. **`c0 = 1` também aqui:** exatamente 1 cache-hit no init em 25/25 e no smoke. Invariante do harness, confirmado agora em duas arquiteturas.
8. **`rcond` do `mldivide` desaba por desenho:** 275 de 1.967 ciclos (14,0%) abaixo de 1e-12, mínimo **4,48e-25**; ZDT1 com 156 de 173. O warning `RCOND` é comportamento FIEL, e convive com interpolação exata nos nós.
9. **σ_2 é NaN em 100% das linhas nas 6 células M=3 e simplesmente não existe nas 19 células M=2** (D45 usa 2 dos M slots). Ler como "cobertura perdida" é erro.
10. **Cobertura 2σ é N/A neste config** — `cobertura95` NULL em 2.279/2.279 blocos-objetivo, por desenho.
11. **A "iteração 0-FE" prevista pelo bundle não ocorreu:** `lote ≥ 1` em 1.967/1.967 e 10/10.
12. **DTLZ4 é o pior surrogate do config** (corr mediana 0,088), pior que as WFG que o paper declara indomáveis. Efeito de α=100 sobre multiquadrática sem scaling.
13. 🆕 **`tempo_aval_real_s` não é novidade da T11 no MATLAB** — já vem preenchido em 25/25 células da s42 (DI-12.4). Tratá-lo como campo novo produz falso-positivo de melhoria.
14. 🆕 **Comparar ① entre a s42 e um smoke T11 do MESMO config é comparação CROSS-MÁQUINA disfarçada.** Em 6 dos 15 configs MATLAB o dado difere, e a diferença **não** é código. Antes de acusar regressão, rodar o controle: `b4`, `c217`, `e103` e os 4 pisos são bit-idênticos e servem de baliza.

---

**Artefatos desta análise** — todos em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/c141/` (único local de escrita; dados, artigo e bundle READ-ONLY; nenhuma célula executada; nenhuma invocação de `experiments.py`):
`t01_s42_vs_smoke.py` (censo s42×smoke nos 15 MATLAB) · `t02_universal_s42.py` → `t02_universal_s42.csv` (U1–U10, C1–C4, F7 nas 25 células + o antes/depois dos campos T11) · `t03_cascata_niveis.py` → `t03_cascata.csv` (nível de saída, lote, ramo_QU nos 1.967 ciclos) · `t04_smoke_g6.py` (sha256 por camada, os 3 sinais do desarme G-6, campos novos, ⑥ do smoke) · `t05_divergencia_maquina.py` (onde a divergência começa, modelo da g=1, controle dos 15 configs) · `t06_identidades.py` → `t06_identidades_s42.csv` · `t07_interpolacao.py` → `t07_interpolacao.csv` e `t07b_residuo_relativo.py` → `t07b_residuo_relativo.csv` (**a errata do F2**) · `t08_rcond_residuo.py` → `t08_rcond_residuo.csv` · `t09_cascata_smoke.py` → `t09_cascata_smoke.csv` (**a cascata reconstruída no código pós-T11**) · `t10_saude.py` (régua Sobol, métricas, trajetórias) · `t11_jsonl_integridade.py` → `t11_jsonl.csv` · `t12_piso_maquina.py` → **`t12_piso_maquina.csv` (o piso O-18 re-medido nas 15 células)**. Insumos pré-computados reusados sem recomputar: `f5/metricas_finais_f52c.csv`, `f5/sonda_f52e.csv`, `f5/trajetorias/`, `f5/contrato_f52b.csv`, `f5/integridade_f52a.csv`, `f5/tempo_f52d.csv`, e as baterias da F5 em `f5/baterias/c141/` (`b06_incerteza.csv`, `b08_arquivo.csv`, `b08_regras.csv`, `lib_c141.py`). Interpretador: `/Users/gmello/Documents/python_venvs/mestrado_experimentos_dissertacao/bin/python`.