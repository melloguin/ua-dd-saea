# RELATÓRIO DE FIDELIDADE — b1 (ParEGO, PlatEMO 4.15) · **validação T11** · re-execução sobre a s42 inteira + smoke/par G-6

**Escopo desta rodada.** **24/25 células** de `resultados_experimentos/b1/{problema}/42/` — **a WFG1 VOLTA ao denominador do mecanismo** (§4.3 explica: o dano é confinado ao ⑥ e as camadas ①②③④⑤ estão íntegras e fecham todas as identidades). Continua fora só a **DTLZ4**, que nunca foi produzida (falha determinística do `dacefit` em duas arquiteturas — inventário categoria D, item 29). Mais **3 células do corpus T11**: `smoke_matlab/main/b1/MMF1`, `g6_com/b1/MMF1` e `g6_sem/b1/MMF1`.

**Volume auditado (24 células, s42):** **7.516 iterações de BO** (④) · **7.447 eventos `b1_gen`** sobreviventes no ⑥ · 6.780 infills reais · 760 cache-hits · **10.485 linhas de ①** (3.705 de DoE) · **2.994.889 linhas de ② em 7.540 gerações** · **13.529.816 linhas de ③** (5.959.816 online + 7.570.000 de sonda em **3.785 blocos × 2.000**) · **77,36 M candidatos scorados** no GA interno (derivado de `ga_pop×ga_iters`). Corpus T11: 3 células × 54 iterações, ⑥ com **0 linhas malformadas** nas três.

**Fato de proveniência que sustenta tudo (medido, não assumido):** `git log` mostra que **nenhum arquivo do b1 foi tocado pela T11** — `src/b1_instrument.m` (2026-07-19), `src/b1_sonda.m` (2026-07-19), `ParEGO.m` (2026-07-21), `EvolALG.m` (2026-07-17); a célula s42/MMF1 é de 2026-07-26. O que a T11 mudou no caminho do b1 está **inteiramente na camada MATLAB compartilhada** (`src/experiment.m` +160 linhas, `src/SondaState.m` +283). **O mecanismo do b1 hoje é bit-a-bit o mesmo que produziu a rodada-42.**

---

## 1. Ficha do mecanismo (condensada)

Knowles, *"ParEGO: A Hybrid Algorithm With On-Line Landscape Approximation…"*, IEEE TEVC 10(1):50–66, 2006. Laço canônico: LHS de 11d−1 → por iteração sorteia 1 λ, re-escalariza TODO o arquivo pela Tchebycheff aumentada (ρ=0,05) sobre objetivos normalizados, treina **um** DACE mono-output do escalar, um GA interno maximiza o **EI** com 10.000 avaliações do modelo, avalia 1× o argmax-EI. Sem população externa; diversidade emerge da varredura de λ.

Nossa instância (`algo_version=b1-ParEGO-PlatEMO4.15`, R2025a Update 1): patches **P1** (DoE do artefato — D94, par `ParEGO.m:29–:30` substituído JUNTO; **verificado no código: `ParEGO.m:34` é `Population = Problem.Evaluation(Problem.data.X0)`** e a re-escala não existe mais no arquivo), **P2** (`sqrt(max(mse,0))` — hoje `EvolALG.m:43`, DEF-L8/D76), **P4** (NaN-guard — hoje `ParEGO.m:50-54`, DEF-A8). P3 (fix do torneio) **não aplicado**.

**Divergências paper×código sancionadas e MANTIDAS** (D29 🟠 impl→código; D20/D30/D47; SPEC:500): λ = 100 vetores (M=2) / NBI 91 (M=3) contra 11/15 da Tab. V · normalização pelo **arquivo** por iteração (B1.6 fechada) contra os "limites conhecidos" do §IV · subset **top-(11D−1+25) por PCheby determinístico + dedup 1e-6** contra ½ melhores + ½ aleatórias · **GA geracional com truncamento elitista e lote ≈2·|arquivo|** contra steady-state pop 20 · IFEs = 10.000 (texto do §IV; a Tab. V diz 200.000 — inconsistência interna do paper) · **MLE por boxmin sem restarts com warm-θ, bounds [1e-5,20]** contra Nelder–Mead 20 restarts · **torneio bugado `EvolALG.m:23`** (K.3 — fica). Modelo `regpoly1+corrgauss`. **D47** ratificou mono-output e rejeitou M GPs.

---

## 2. DISSECAÇÃO DOS ASPECTOS

### 2.1 Tabela-resumo (34 aspectos · Δ = mudança vs F5)

| # | aspecto | classe | verificab. | Δ | resultado-síntese (24 células · 7.447 eventos ⑥ · 13,53 M linhas de ③) |
|---|---|:---:|---|:--:|---|
| U1 | Orçamento FE e término | (2) | direta | +WFG1 | 31D−1 exato **24/24**; `fe_index` denso, `solution_id` único, `termino='normal'`, `cp_init=true` 24/24 |
| U2 | DoE 11D−1 do artefato (P1/D94) | **(1)** | direta | +WFG1 | `max\|ΔX\|=0,0 EXATO` **24/24**; `doe_hash`≡sidecar 24/24; **+ código lido**: `ParEGO.m:34` |
| U3 | 1 retreino por iteração | **(1)** | direta | +WFG1 | ④≡`fit_series`≡③-online-gens = **7.516** em 24/24 |
| U4 | Cadência da sonda + finalProbe | (2) | direta | +WFG1 | fórmula exata **24/24**; 3.785 blocos × 2.000; **G ímpar em 6 células** (WFG1 é a 6ª: 443) |
| **U5** | **Não-perturbação da sonda** | **(2)** | **direta** | **T→(2)** | **① BIT-IDÊNTICA com/sem sonda** (61×10); ③-online 5.076 linhas idênticas; ⑥ 54 eventos idênticos em 16 campos |
| U6 | WAPE/cobertura — régua ESCALAR | (2) | direta | = | 3.562 blocos; ΔWAPE mediano **+48,9%**, piora em 18/23 |
| U7 | Invariante dupla de timing | (2) | direta | +WFG1 | 1ª perna **7.516/7.516**; 2ª perna fecha 24/24 (WFG1: 222 violações ⊆ sonda, falta só g=443) |
| U8 | `fe_treino_max` não-monotônico | (2) | direta | +WFG1 | não-monot. em **20/24** (WFG1: 51 quedas em 374 pts) |
| U9 | Reconciliação de guards + ledger | (2) | direta | +WFG1 | **7.516 = 6.780 + 760 − 24**; WFG1: 443 = 440+4−1 exato |
| U10 | Off-by-one estrutural da ② | (2) | direta | +WFG1 | ② = n_iter+1 em **24/24** (7.540); `\|②_g\|=11D−1+(g−1)` **444/444** na WFG1; dup final ≡ cache−1 24/24 |
| U11 | Erro de fantasia escalar | (2) | direta | = | \|err\| mediano 0,0575; otimista em **95,0%**; dentro de 2σ **38,1%** |
| F1 | **QUERY-JOIA: identidade do EI** | **(1)** | direta | +WFG1 | **7.447/7.447 @ rel<1e-6** (WFG1: 374/374, máx 1,55e-14) |
| F2 | Cadeia `best_sid` ⇄ ① | **(1)** | direta | +WFG1 | fecha nos 2 ramos; WFG1: 372 não-cache + 2 cache = 374/374 |
| F3 | σ pesa na decisão | **(1)** | direta | = | 2.099/5.462 não-greedy; **σ_sel > σ_greedy em 100%**; razão mediana 1,074 |
| F4 | ③ = pop FINAL do GA + EI elitista | (2) | direta | = | `ei_best≡min(e0_trace)` 7.073/7.073; concordância `last_eq`⇔`match` **96,6%** |
| F5 | Calibração do σ escalar | (2) | direta | = | cobertura 2σ **0,9415 → 0,4275**; <0,95 no último bloco em 21/23 |
| F6 | Curva de aprendizado dos hp | (2) | direta | = | θ completo em 7.073/7.073; 1º fit **2,13×** a mediana |
| B1 | λ: N=100 / NBI 91 | (2) | direta | +WFG1 | grid exato 24/24 (WFG1 desvio 9,9e-5, 99 λ distintos, máx 8 repet.) |
| B2 | PCheby ρ=0,05 | **(1)** | direta | +WFG1 | `gbest`≡min PCheby **7.447/7.447 @ abs<1e-6** (WFG1 máx 5,66e-8) |
| B3 | Normalização min/max do ARQUIVO | (2) | direta | +WFG1 | ≡ min/max acumulado pré-fit **7.447/7.447** (WFG1 máx 4,44e-8) |
| B4 | Mono-output (D47) | **(1)** | direta+eco | +WFG1 | `mu_1..`/`sigma_1..` **100% NULL** em 13,53 M linhas, 24/24 |
| B5 | Subset top-cap + dedup 1e-6 | (2) | direta | +WFG1 | 2 identidades **7.447/7.447**; **+ código**: o dedup é `unique(round(X·1e6))` ∩ `unique(round(PCheby·1e6))` |
| B6 | Guardas P2 (mse<0) e P4 (NaN) | (2) | direta | +WFG1 | **0 σ<0, 0 σ NaN, 0 μ NaN** em 13,53 M linhas; contadores = 0 em 7.447 |
| B7 | GA interno: IFEs, lote 2·\|A\| | (2) | direta | +WFG1 | `ga_iters=⌈10000/ga_pop⌉` **7.447/7.447**; `ga_pop−2·n_arquivo ∈ {0,−2}` |
| B8 | θ-bounds [1e-5, 20] | (2) | direta | +WFG1 | **0 violações** em 132.915 componentes |
| B9 | MLE boxmin × NM-20-restarts | **T** | não-verif. (lido) | T→T-lido | só θ FINAL logado; **código confirma a chamada única** `ParEGO.m:78` |
| **B10a** | **Torneio bugado K.3** | **(2)** | **indireta+código** | **T→(2)** | ativo em **6.475/7.073 (91,5%)**; **P(pai=DoE) mediana 0,991**; peso **3,78%** dos candidatos |
| B10b | Operadores do GA interno | **T** | não-verif. (lido) | T→T-lido | ③ grava o pool, não os operadores; código mostra `OperatorGA(…,{0,0,1,20})` (mutação-only) |
| B11 | Hazard near-dup / cache-hit D89 | (2) | direta | +WFG1 | `dist_min_arquivo==0` ⟺ cache-hit em **24/24** (WFG1: g329, g413) |
| B12 | `n_arquivo` = contador do PlatEMO | (2) | direta | +WFG1 | ≡11D−1+(g−1) em **7.447/7.447** |
| B13 | Duplicatas float32 na ① | (2) | direta | +WFG1 | **176 X duplicados** em **15/24** células (WFG1 +15) |
| N1 | c0 = 1 cache-hit no init é LEI | (2) | direta | +WFG1 | exatamente 1 guard `cache_hit` com `fe=1` em **24/24** |
| **N2** | **Identidade de campanha (T11-G3/G7)** | **(2)** | direta | **NOVO** | s42: schema **v1**, sem `campanha_id`, `repo_hash` **vazio** 24/24 · T11: schema **v2**, `campanha_id` + `repo_hash=195f64ed` (= commit real) 3/3 |
| **N3** | **Writer do ⑥ em append (T11-G1)** | **(2)** | direta-decl.+medida | **NOVO** | `experiment.m:jsonl_open` trunca 'w'→fecha→reabre 'a'; **gêmeo em tempdir: `a+a` perde 0/10.000 em 2/2; `w+w` perde exatamente 5.000** |

### 2.2 Blocos narrativos

**U1 — Orçamento de FE e término.** *(a)* O paper usa orçamentos absolutos de 100 e 250 FEs para d=2–8 (§VII). *(b)* A SPEC troca por `maxFE=31D−1` com hard-stop exato (D21/D61), fixando a fração DoE/orçamento em **35,4%** contra os 87% do pior caso do paper. *(c)* `len(①)==31D−1` em **24/24** (10.485 linhas; WFG1 = 681 = 31·22−1 exato); `fe_index` denso 0-based, `solution_id` único, `fe_final==maxfe`, `footer.termino='normal'` e `cp_init=true` nas 24. `n_retries=0` e `fallback_ativado=false` em 24/24. *(d)* O b1 consome exatamente 1 FE por iteração (`ParEGO.m:95`) e 11D−1 no init — não há overshoot nativo a cortar. *(e)* **(2)** — direta.

**U2 — DoE de 11D−1 do artefato (P1/D94).** *(a)* Alg. 1, l.2-5: `LATINHYPERCUBE(d)` com 11d−1 pontos, fórmula idêntica à nossa. *(b)* Divergência só de proveniência (D63/D88), com a exigência D94 de substituir o **par** `:29–:30` — patchar só a `:29` deixaria a re-escala rodando e causaria dupla-escala silenciosa em WFG/BBOB. *(c)* `max|ΔX| = 0,0 EXATO` (float32) em **24/24** (WFG1 inclusive) e `doe_hash`≡sidecar 24/24. **Novidade desta rodada: a prova migrou de indireta para direta.** A F5 listou "provar que a `:30` nunca roda" no teto §9; a leitura do arquivo mostra que a linha **não existe**: `ParEGO.m:28-34` mantém `N = 11*Problem.D-1` e chama `Population = Problem.Evaluation(Problem.data.X0)`, sem nenhuma re-escala. *(d)* O zero exato em WFG (bounds [0,2i]) e BBOB ([−5,5]) só é possível se nenhuma re-escala tocou o X. *(e)* **(1)** — direta.

**U3 — Um retreino do DACE por iteração.** *(a)* O `DACE(...)` do Alg. 1 está dentro do while — o modelo é reconstruído a cada iteração sob λ novo. *(b)* Intocado. *(c)* `len(④) == len(manifest.fit_series) == nº de gerações da ③ online` = **7.516** em 24/24 (WFG1: 443 = 443 = 443, mesmo com o ⑥ truncado em 374 — **as três camadas independentes concordam onde a quarta se perdeu**). *(d)* Como λ muda a cada iteração, o alvo do GP muda: não existe reaproveitar modelo. *(e)* **(1)** — direta.

**U4 — Cadência da sonda e o finalProbe.** *(a)* Instrumentação nossa. *(b)* DI-12.5: `g==1` ∪ pares ∪ finalProbe da última geração. *(c)* A fórmula `{1}∪{pares≤G}∪{G}` fecha **exata em 24/24**; 3.785 blocos, todos com 2.000 pontos. **G é ímpar em 6 células** — as 5 da F5 mais a **WFG1 (G=443 → 223 blocos, batendo o `|esperado|=223` do manifesto)**. No smoke T11 (G=54, par) a cadência dá 28 blocos e também fecha. *(d)* O finalProbe dispara fora do laço de contabilidade da iteração — o que produz a assinatura do §U7. *(e)* **(2)** — direta.

**U5 — Não-perturbação da sonda. 🔓 O TETO QUE CAIU.** *(a)* Não existe no paper. *(b)* Contrato §3.1 (🔴): a sonda consome tempo e RNG mas **não pode** alterar a trajetória. A F5 declarou T porque "exige um run pareado com a sonda desligada" — e ele não existia. A T11 (commit `4126b78`, G6, "o gêmeo MATLAB do `sonda_on`") criou o interruptor. *(c)* **Medido no par `g6_com` × `g6_sem` (b1/MMF1), por mim, do zero:** a **① é BIT-IDÊNTICA** — 61 linhas × 10 colunas, X, f, `fase`, `solution_id` e `fe_index` todos iguais elemento a elemento; a **③ online é BIT-IDÊNTICA** — 5.076 linhas, **zero** colunas numéricas com qualquer diferença; e os **54 eventos `b1_gen` do ⑥ são idênticos em 16 campos auditados** (`gbest`, `mu_best`, `sigma_best`, `ei_best`, `norm_min/max`, o **vetor λ**, `n_treino`, `n_subset`, `ga_pop`, `ga_iters`, `dist_min_arquivo`, `theta_media`, `best_sid`, `fe`, `geracao`) — **0 divergências**. E os três sinais de que a sonda **rodou** num lado e **não** no outro: `sonda.desligada` `false`→`true`; `n_blocos` 28→0; a ③ **encolhe** de 61.076 para 5.076 linhas e o regime `sonda` desaparece; o ⑥ cai de 105 para 77 linhas. *(d)* O código explica: `ParEGO.m:87` chama `b1_sonda` **depois do fit e antes do `EvolALG`**, e `grep` por `rand|randi|randn|rng(` em `src/b1_sonda.m` e `src/b1_instrument.m` devolve **zero** chamadas (só o comentário `b1_sonda.m:34`: "predictor/corrgauss/regpoly1 são aritmética pura"). Sem consumo de RNG, o fluxo do `EvolALG` é o mesmo. *(e)* **(2)** — **direta**. ⚠ A prova é **por máquina e por célula**: vale para MMF1 no Mac; a generalização às 24 células é inferência do código, não medição.

**U6 — WAPE/cobertura na régua ESCALAR (§5, regra 4 do R4).** *(a)/(b)* Régua nossa; o b1 é o único config em que `sonda_f52e.csv` **não tem uma única linha** (re-confirmado: 0 linhas de b1), porque `mu_0` é o PCheby, não `f0`. *(c)* Nos 3.562 blocos das 23 células reconstruídos com o `transf_params` do próprio bloco: WAPE mediano 0,191→0,288, **variação +48,9%**, piora em **18/23**; extremos BBOB_F49 +478%, DTLZ7 +390%, WFG4 −57,5%. Join posicional contra o gabarito com `max|ΔX| = 0,0` em 7,12 M linhas. *(d)* Não é o modelo desaprendendo — é a **régua se movendo**: o intervalo de normalização encolhe conforme o arquivo se concentra, e o mesmo erro absoluto vira erro relativo maior sobre um gabarito Sobol global. *(e)* **(2)** — direta. *(Não recomputei os 223 blocos da WFG1: a reconstrução exige o `transf_params` bloco a bloco e a conclusão já é sólida em 3.562.)*

**U7 — A invariante dupla de timing.** *(a)/(b)* DI-13.10. *(c)* 1ª perna: **7.516/7.516 linhas do ④, zero violações** (WFG1: 443/443). 2ª perna na versão b1: as gerações em que `fit+busca+sonda > tempo_geracao` são **⊆ as gerações de sonda em 24/24**, e nas de G ímpar falta **exatamente uma** — a do finalProbe. Na WFG1: 222 violações, todas dentro das 223 gerações de sonda, faltando **só a 443**. Reprodução perfeita da assinatura em uma 6ª célula. *(d)* Na geração do finalProbe o `tempo_pred_sonda_s` do ④ é 0,0 e o custo vive só no evento `rec='sonda'` do ⑥: o probe roda depois de a iteração ter emitido seu registro. *(e)* **(2)** — direta.

**U8 — `fe_treino_max` não-monotônico.** *(a)* Inexistente no paper. *(b)* DI-13.15 / regra 9 do CONTRATO admitem a não-monotonicidade quando há subamostragem. *(c)* **20/24** células não-monotônicas; a WFG1 entra com **51 quedas** nos 374 pontos sobreviventes. Monotônicas só MMF1/MMF4/MMF11_L (D=2) e BBOB_F55. *(d)* O subset é o **top por PCheby** sob um λ que muda: um ponto recente pode não estar entre os melhores sob o λ da vez e o índice máximo recua. *(e)* **(2)** — direta.

**U9 — Reconciliação de guards e ledger de FE.** *(a)/(b)* D89 (cache-hit = 0 FE, sem linha na ①), D57, DEF-C2. *(c)* `cache_hit` do ⑥ ≡ manifesto ≡ footer nas células com footer; o **ledger `n_iter = n_infills + cache_hits − c0` fecha em 24/24** — agregado **7.516 = 6.780 + 760 − 24**; a WFG1 fecha **443 = 440 + 4 − 1** usando o ④ (o ⑥ dela só preservou 2 dos 4 guards de cache — os outros 2 estão na cauda perdida, e a aritmética **prova** que existiram). No smoke T11: **54 = 40 + 15 − 1**. *(d)* O cache-hit não gasta FE mas gasta uma iteração; é o que faz BBOB_F37 rodar 515 iterações onde a aritmética ingênua previa 200. *(e)* **(2)** — direta.

**U10 — Off-by-one estrutural da ②.** *(a)/(b)* DEF-C2: a ② registra o arquivo **antes** de cada iteração. *(c)* ② com n_iter+1 gerações em **24/24** (7.540 = 7.516+24); `|②_g| = 11D−1+(g−1)` em **444/444** na WFG1 (e nas 23 anteriores); dup na última ② ≡ `cache_hits−1` em 24/24 (WFG1: 3 = 4−1). *(d)* O `−1` é o c0: o cache-hit do init não re-adiciona membro. *(e)* **(2)** — direta.

**U11 — Erro de fantasia escalar do infill.** *(a)/(b)* Adaptado à limitação irremediável do D47: sem μ por objetivo, o erro de fantasia só existe na régua escalar da própria iteração. *(c)* Via `best_sid`→①: erro absoluto mediano por célula 0,0104–0,6245 (mediana das 23 = **0,0575**); fração otimista mediana **0,950** (1,000 em BBOB_F17); o infill cai dentro do próprio ±1,96σ em só **38,1%**; melhora o incumbente em **8,6%** das iterações. *(d)* Maximizar EI herda a cauda negativa do erro do modelo; e melhorar `gbest` não é o objetivo — cobrir o front com uma sequência de mínimos escalarizados é. *(e)* **(2)** — direta.

**F1 — QUERY-JOIA: a identidade fechada do EI.** *(a)* §III/Fig. 1: EI na forma fechada de Jones sob `N(μ,σ²)`. *(b)* Intocado. *(c)* `(gbest−μ)·Φ(z) + σ·φ(z) ≡ −ei_best` fecha em **7.447/7.447 eventos @ rel<1e-6**: as 7.073 da F5 (máx 1,37e-11) **mais as 374 da WFG1** (máx **1,55e-14**) **mais 54/54 no smoke T11** e 54/54 nos dois lados do par G-6. **A leitura do código fecha o círculo:** `EvolALG.m:44` é literalmente `EI(i) = -(Gbest-y)*normcdf((Gbest-y)/s)-s*normpdf((Gbest-y)/s)` — o sinal negativo (PlatEMO minimiza −EI) e o uso de `normcdf` (que no MATLAB é `erfc`, não `erf`) estão no fonte. *(d)* A armadilha #1 da F5 (Φ com `erf` produz 13 falsos negativos por cancelamento catastrófico) fica confirmada pela via do código. *(e)* **(1)** — direta. **O uso da incerteza está PROVADO pelos dados em 4 corpora independentes.**

**F2 — Cadeia seleção → avaliação real.** *(a)* Alg. 1, l.11-12. *(b)* D57 + D89. *(c)* `best_sid ∈ ①` em 7.447/7.447; ≡ linha aberta pelo infill nos ramos não-cache e ≡ `solution_id` do guard nos ramos de cache. Na WFG1 os **únicos 2 desencontros** contra `SID[fe−1]` são **exatamente** as 2 iterações de cache-hit (g329 e g413) — o discriminante `dist_min_arquivo==0` acerta os dois. *(d)* A prova não depende do `x_key`: casa por `solution_id` nos dois ramos. *(e)* **(1)** — direta.

**F3 — σ pesa na escolha.** *(a)* §III: *"EGO does NOT just choose the solution that the model predicts would minimize the cost"*. *(b)* Intocado (contrafactual G5 do agente-4). *(c)* Nas 5.462 iterações pool-consistentes, **2.099 (38,4%)** são não-greedy, e em **100% delas o σ do escolhido é maior que o do argmin-μ**; razão mediana 1,074 (até 6,06 em ZDT1); percentil-σ 0,454 × 0,025. *(d)* A F5 corrigiu aqui um artefato de float32 da v1.0 (57,4%→38,4%) e o enunciado ficou **categórico**, não ruidoso. *(e)* **(1)** — direta.

**F4 — ③ = população FINAL do GA interno, EI rastreado de forma elitista.** *(a)* O paper não especifica o que se persiste. *(b)* DEF-C2/⟦v5.2.1⟧: grava-se a população final do otimizador de aquisição por iteração de BO — e o **código confirma** (`EvolALG.m:55-62`: `last` é sobrescrito a cada geração interna, então ao sair do while é a final). *(c)* `linhas/geração ≡ ga_pop ≡ n_pool_ga` 7.073/7.073; `ei_best ≡ min(e0_trace)` 7.073/7.073; `e0_trace` monotônico em só **16,9%**; `ei_best == último(e0_trace)` em 74,0%, com concordância `last_eq`×`match` de **96,6%** e `P(match|last_eq)=99,87%`. *(d)* `e0_trace` é o melhor da população **corrente**, não o best-so-far; como ele sobe, o vencedor pode já não estar na população final gravada. *(e)* **(2)** — direta.

**F5 — Calibração do σ escalar: sobreconfiança tardia.** *(a)/(b)* Análise nossa na régua reconstruída. *(c)* Cobertura ±1,96σ por bloco: mediana **0,9415 → 0,4275**, com **21/23** abaixo de 0,95 no bloco final; colapsos em ZDT4 (0,9285→0,0875 com correlação final 0,9999), DTLZ3 (→0,1455), BBOB_F22 (→0,1270). Só BBOB_F55 e ZDT6 terminam calibrados. *(d)* Kriging interpolador sobre pontos cada vez mais agrupados colapsa lengthscales (os 10.578 componentes de θ em 1e-5 do §B8) e o MSE despenca inclusive onde o modelo erra. *(e)* **(2)** — direta.

**F6 — Curva de aprendizado dos hiperparâmetros e o warm-θ.** *(a)* Alg. 1, l.25: NM com 20 restarts. *(b)* D30: boxmin sem restarts, θ0=10 só na 1ª iteração, warm-start depois — **confirmado no código**: `ParEGO.m:35` (`theta = 10.*ones(1,D)`) e `ParEGO.m:80` (`theta = dmodel.theta`). *(c)* `modelo_hp` traz θ completo em 7.447/7.447 (**132.915 componentes** com a WFG1); o 1º fit custa **2,13×** a mediana; pós-cap a curva de custo é plana ou decrescente (drift negativo em 15/21 células). **Achado novo:** no par smoke×s42 (arquiteturas diferentes) **θ é BIT-IDÊNTICO nas 45 gerações comparáveis** — `theta_media/min/max` com drift relativo **exatamente 0,0** —, o que prova que o boxmin é numericamente estável e que a MLE não é a fonte da deriva (§4.2). *(e)* **(2)** — direta.

**B1 — λ por iteração: 100/91 contra os 11/15 do paper.** *(a)* §IV + Tab. V: 11 (k=2) e 15 (k=3), com racional explícito no §VII-B de permitir "várias passadas" por vetor. *(b)* D20/D30: N=100 (default PlatEMO) e NBI 91 para M=3; o bundle anota que isso **quebra** o racional. *(c)* λ no grid em 7.447/7.447 (desvio máx 9,9e-5 em M=2, 1,2e-5 em M=3); distintos 38–100 (M=2) e 68–91 (M=3); máxima repetição 13 ⇒ sorteio **com reposição**, como o paper prescreve; a WFG1 entra com 99 de 100 e máx 8 repetições. `N_lambda` no header e em `params` em 24/24. *(d)* Efeito material: MMF1 faz **0,48 passada** por vetor onde o paper faria 4,4; ZDT1 faz 6,0. A divergência penaliza o regime de orçamento curto e é neutra/favorável no longo. *(e)* **(2)** — direta.

**B2 — Tchebycheff aumentada com ρ=0,05.** *(a)* Eq. (2), §IV. *(b)* Intocado — e o `0,05` está **literal** em `ParEGO.m:56`. *(c)* `gbest` reconstruído da ① fecha em **7.447/7.447 @ |Δ|<1e-6** (máx 1,91e-7 na s42; 5,66e-8 na WFG1). *(d)* A armadilha da tolerância permanece: 125 iterações "falham" no critério **relativo** porque `gbest` vale 1e-9–5e-3 (o incumbente coincide com o canto da normalização). O b3 exige relativo; o b1 exige **absoluto**. *(e)* **(1)** — direta.

**B3 — Normalização pelo min/max do ARQUIVO.** *(a)* §IV: normalizar "com respeito aos limites conhecidos (ou estimados) do espaço de custo" — estático e exógeno. *(b)* **B1.6 fechada**: o código normaliza pelo arquivo, recalculado a cada iteração (`ParEGO.m:50-55`, com o P4 no `den(den==0)=eps`). 🟠 impl→código. *(c)* `norm_min`/`norm_max` ≡ min/max do arquivo pré-fit em **7.447/7.447 @ rel<1e-5** (máx 5,71e-8); a WFG1 fecha 374/374 sem precisar da janela de cache (`fe` em vez de `fe−1`) porque suas 2 iterações de cache não caem nas gerações críticas. *(d)* É a divergência com maior efeito sobre a **interpretação das métricas de surrogate** (§U6) e nenhum sobre a fidelidade do mecanismo. *(e)* **(2)** — direta.

**B4 — Mono-output (D47).** *(a)* §III/§IV: um DACE sobre o custo escalarizado. *(b)* D47 ratificou e rejeitou M GPs. *(c)* `mu_1..`/`sigma_1..` **100% NULL** em **13.529.816 linhas nas 24 células**; literais únicos e idênticos (`pred_tipo='valor'`, `modelo_flag='GP-DACE'`, `espaco_modelo='transformado'`, `transf_tipo='escalar-tcheby'`) — inclusive na WFG1 e no smoke T11; `sigma_dict` presente em 24/24 s42 e 3/3 T11, com o mesmo texto. *(d)* É a decisão que obriga a régua à parte (regra 4 do R4). *(e)* **(1)** — direta + eco declarativo.

**B5 — Subset top-(11D−1+25) + dedup 1e-6.** *(a)* §IV: ½ melhores sob λ + ½ aleatórias sem reposição; e o autor é explícito que o subset é concessão aos 21 runs, não o método. *(b)* Código: top determinístico + dedup. *(c)* `n_treino == n_subset − n_dedup` e `n_subset == min(n_arquivo, cap)` em **7.447/7.447**; satura no cap em 23/24. **Refinamento de causa desta rodada:** a leitura de `ParEGO.m:68-72` mostra que o dedup é a **interseção de dois filtros** — `unique(round(PDec*1e6)/1e6,'rows')` **e** `unique(round(PCheby*1e6)/1e6)`. A F5 atribuiu o caso DTLZ1 (dedup remove até 74 de 101) a near-duplicatas em X; o código mostra que num **plateau de f** (DTLZ1 não-amaciado, g com 20π) quem corta é o **segundo** filtro, sobre o escalar — pontos distintos em X colapsam no mesmo PCheby arredondado. *(d)* O dedup existe para impedir o crash "Bad parameter region" do `dacefit`; é o que salva a DTLZ1 e é a família de fenômeno que provavelmente matou a DTLZ4. *(e)* **(2)** — direta (o "top" em si: indireta).

**B6 — Guardas numéricas P2 e P4.** *(a)* Inexistentes no paper. *(b)* P2 (`EvolALG.m:43`, `s = sqrt(max(mse,0))`, com o contador `n_mse_neg` em `:40-42`) e P4 (`ParEGO.m:53-54`). *(c)* Em **13.529.816 linhas**: **0 σ negativos, 0 σ NaN, 0 μ NaN**; `Σn_mse_neg = Σn_ei_nan = Σnan_guard = 0` nas 7.447 iterações da s42 **e** nas 54 do smoke T11. Mínimo global de σ na WFG1: 1,415e-4. *(d)* Confirma a correção de fato da F5: `σ=0` **exato** não ocorre; o clone-hazard leva o MSE ao limiar do zero de máquina sem cruzá-lo, e o P2 nunca precisou clampar. *(e)* **(2)** — direta.

**B7 — GA interno: 10.000 avaliações, lote 2·|A|, geracional.** *(a)* §IV: steady-state pop 20, torneio binário sem reposição, SBX 0,2, mutação-shift, 10.000 avaliações (Tab. V diz 200.000 — inconsistência do paper). *(b)* D30 mantém 10.000 mas troca o motor. *(c)* `ga_iters == ⌈10000/ga_pop⌉` e `ga_pop − 2·n_arquivo ∈ {0,−2}` em **7.447/7.447**; gerações internas de **250 (D=2)** a **6–8 (D=30)**. **Novo:** o código explica o `{0,−2}` — `N = size(Off,1)` é fixado **antes** do while (`EvolALG.m:24`) e nunca atualizado; da 2ª geração em diante `Off` tem `2·ceil(N/2)`, que excede N em 1 quando N é ímpar, e `EvolALG.m:61` grava só `Off(1:min(N,...))` — a linha excedente nunca ganha y/s/EI. *(d)* Em D alto o GA interno faz **6–11 gerações** sobre populações de ~900–1.700: amostragem larga com pouca pressão evolutiva, o oposto do steady-state profundo do paper. *(e)* **(2)** — direta.

**B8 — Bounds de θ.** *(a)* Sem caixa declarada no texto. *(b)* [1e-5, 20], literal em `ParEGO.m:78`. *(c)* **0 violações em 132.915 componentes** (24 células); 1.648 saturados em 20 e 10.578 em 1e-5 na s42-23, concentrados em MMF16_20 (4.181) e no conjunto WFG (4.001); a WFG1 satura em ambos os extremos (min 1,00e-5, máx 20,0000). *(d)* Informação, não defeito: é o elo mecânico com o §F5 — θ nos cantos ⇒ GP que interpola bem e subestima grosseiramente o próprio erro fora dos sites. *(e)* **(2)** — direta.

**B9 — MLE: boxmin sem restarts × NM com 20 restarts.** *(a)* Alg. 1, l.25, explícito: "com 20 restarts". *(b)* D30 mantém o boxmin do PlatEMO. *(c)* **Continua sem rastro nos dados**: `modelo_hp` loga θ final, `sigma2` e `tempo_fit_s`; não a trajetória, o nº de avaliações da verossimilhança nem o critério de parada. **O que esta rodada acrescenta é leitura de código**: `ParEGO.m:78` é uma chamada única a `dacefit(...)` dentro do laço, sem envelope de restart — o que **confirma a declaração do bundle**, mas por uma via que o protocolo classifica como teto. Evidência indireta adicional: θ bit-idêntico entre duas arquiteturas (§F6) é o que se espera de um otimizador determinístico de corrida única. *(d)/(e)* **T** — permanece, agora com a etiqueta *lido*.

**B10a — O torneio bugado K.3. 🔬 O ITEM QUE SAIU DA SOMBRA.** *(a)* O paper usa outro GA (torneio binário sem reposição sobre a população interna); não há contraparte. *(b)* SPEC:434/500 classificam 🟠 IMPL→CÓDIGO e `EvolALG.m:9-10` registra "o bug do torneio (:16) fica (CODIGO K.3)". *(c)* **A leitura do código dá o mecanismo exato, e ele é mais forte do que "favorece os mais antigos".** `ParEGO.m:92` passa `Population.decs` (o arquivo INTEIRO, `n_arquivo` linhas) como `Dec` e `PCheby` (o subset **pós-cap e pós-dedup**, `n_treino` linhas) como fitness; `EvolALG.m:23` faz `Dec(TournamentSelection(2,size(Dec,1),PCheby),:)`; e `TournamentSelection` sorteia `randi(length(PCheby),K,N)` — **índices em 1..n_treino usados para endereçar Dec**. Dois danos: (i) as linhas `n_treino+1..n_arquivo` **nunca podem ser pais**; (ii) a aptidão que ganha pertence a outro indivíduo. E há um terceiro, que o bundle não nomeia: quando o cap dispara (`ParEGO.m:57-64`), `PCheby = PCheby(Next)` com `Next = sort(PCheby)` ⇒ **o vetor de aptidão está ORDENADO ASCENDENTE**, logo `rank(i)=i` e o torneio degenera em **`min(U1,U2)`** — uma seleção **posicional pura, com zero conteúdo de aptidão** — sobre um `Dec` que está em ordem cronológica de avaliação. O que deveria ser seleção elitista vira **seleção por idade**. **Números medidos sobre as 7.073 iterações:** o cap está ativo em **6.475/7.073 = 91,5%** (nas 598 pré-cap, 93 já têm `n_dedup>0` e portanto também desalinham); a fração do arquivo **inalcançável** como pai-crossover tem mediana **0,435** e máximo **0,863**; e como `n_treino ≤ 11D−1+25` enquanto o DoE ocupa as linhas 1..11D−1, a probabilidade de um pai da 1ª geração interna ser **um ponto do DoE** é `1−((n_treino−11D+1)/n_treino)²` = **mediana 0,9912**, mínimo 0,7046, e **1,0000 em 344 iterações** (onde o dedup derruba `n_treino` abaixo de 11D−1). Por D: 0,801 (D=2), 0,997 (D=7), 0,968 (D=10), 0,996 (D=20), 0,992 (D=22), 0,997 (D=30). **Peso do ramo:** o galho afetado é a metade-crossover da 1ª geração interna, i.e. `ga_pop/2` de `ga_pop×ga_iters` candidatos scorados ⇒ **2,78 M de 73,47 M = 3,78%** (24 células: 2,94 M de 77,36 M = 3,79%) — e cresce com D: 0,41% (D=2) → 4,39% (D=22) → **5,91% (D=30)**. *(d)* **ERRATA 10 do handoff CONFIRMADA por leitura:** `EvolALG.m:64` (o handoff cita `:65`, que é o decremento de IFEs) usa `Parent(TournamentSelection(2,size(Parent,1),EI(index(1:ceil(N/2)))),:)` — domínios do mesmo tamanho, aptidão alinhada. Da 2ª geração em diante **não há bug**. E a segunda metade da 1ª geração (`OperatorGA(Problem,Dec,{0,0,1,20})`, mutação-only) varre o arquivo **inteiro**, sem viés — o que confirma a observação do handoff de que o conserto de um token estreitaria esse ramo. *(e)* **(2)** — verificabilidade **indireta**: a derivação vem do código (`ParEGO.m:92`, `EvolALG.m:23`, `TournamentSelection.m:27`) e a magnitude vem de campos logados (`n_treino`, `n_arquivo`, `ga_pop`, `ga_iters`). **A classificação 🟠 CONTINUA DEFENSÁVEL** — e o argumento decisivo não é a inércia, é o §III-A do próprio paper: o autor declara o **maximizador de aquisição intercambiável** ("substitui o branch-and-bound do EGO por facilidade de implementação"), de modo que um defeito dentro do GA interno não toca o mecanismo que a tese mede (Kriging do escalarizado + EI).

**B10b — Os operadores internos do GA.** *(a)* SBX p=0,2 gerando um filho, mutação por shift ±(1/100)·range com p_gene=1/d, init 5 mutantes + 15 LHS. *(b)* O código usa `OperatorGA` do PlatEMO com defaults no ramo de crossover e `{0,0,1,20}` (proC=0, disC=0, proM=1, disM=20 — mutação polinomial pura) no ramo de mutação: outro operador, outra parametrização, sancionado dentro do "GA geracional × steady-state" do D30. *(c)* **Sem rastro recuperável nos dados**: a ③ grava o resultado (pool final), não os operadores; a genealogia foi rejeitada no DI-10. O único indício circunstancial continua sendo `real_solution_id` no pool online (mediana 0,11%, 38,7% em BBOB_F49), que o clone-hazard explica igualmente. *(d)/(e)* **T** — permanece, agora com os parâmetros efetivos *lidos* no código.

**B11 — Hazard near-dup, cache-hit D89 e `dist_min_arquivo`.** *(a)* O paper reconhece o risco de revisitar pontos; não instrumenta. *(b)* D89 + DI-10; `ParEGO.m:99` (`Problem.FE = Problem.data.bud.fe`) é a re-sincronização que implementa "cache-hit não consome FE". *(c)* **`dist_min_arquivo == 0` ⟺ iteração de cache-hit em 24/24** — 733 casos na s42-23 e 2 na WFG1 (g329, g413), e 14 no smoke T11 (= 15 cache − 1 c0). Hazard industrial no BBOB: F37 316 cache-hits (61,4%) e F49 305 (60,5%), streaks de 37. *(d)* Quando o GA cai numa bacia estreita e a mutação copia o pai, o argmax-EI já foi avaliado; o guard intercepta, não gasta FE, e a iteração se repete. Fiel ao D89 — mas reduz os infills efetivos de F37/F49 a 200 de 309. *(e)* **(2)** — direta.

**B12 — `n_arquivo` é o contador do PlatEMO, não o arquivo único.** *(a)* Inexistente no paper. *(b)* DI-10; `ParEGO.m:43-44` (`[N,D]=size(Population.decs); n_arquivo=N`). *(c)* ≡ `11D−1 + (g−1)` em **7.447/7.447** (WFG1 374/374; smoke 54/54) — cresce +1 por iteração inclusive nas de cache-hit. *(d)* É load-bearing para `ga_pop=2·n_arquivo`, `n_subset=min(n_arquivo,cap)` e `|②_g|`; quem assumir `n_arquivo==|①|` falha exatamente nas células de cache alto. *(e)* **(2)** — direta.

**B13 — Duplicatas de X em float32 na ①.** *(a)/(b)* D57 (dedup bit-a-bit em float64) × D53 (export float32). *(c)* **176 duplicatas em 15 das 24 células** (as 161 da F5 + **15 na WFG1**); o `|Δf|` entre os pares chega a 1,22e-4 ⇒ são pontos genuinamente distintos em float64. *(d)* Não é falha de dedup: é o colapso do export. Casa com a **regra 11 nova do CONTRATO** (dominância sobre ①/⑦ é LOSSY): a WFG1 tem n_nd=118 — o maior de todos os 15 algoritmos naquele problema — e parte dessa contagem vive na resolução do float32. *(e)* **(2)** — direta.

**N1 — `c0 = 1` é lei.** *(a)/(b)* Artefato do wrapper de FE. *(c)* Exatamente **um** guard `cache_hit` com `fe=1` em **24/24** da s42 e em 3/3 do corpus T11. *(d)* Propriedade transversal do wrapper MATLAB (25/25 no b3); sem descontá-lo o ledger erra por 1 em todas. *(e)* **(2)** — direta.

**N2 — Identidade de campanha (T11-G3/G7). 🆕** *(a)* Inexistente no paper. *(b)* B-03/I-09: o manifesto passa a **schema v2** com `campanha_id` (env `UA_DD_SAEA_CAMPANHA_ID` ou `{commit12}_{data UTC}`) e `repo_hash`, porque sem carimbo de campanha o `is_run_done` absorvia célula de smoke/pré-retrofit como pronta — e o commit `4253fa4` nomeia **`b1 DTLZ2` e `b1 ZDT1` de semente 0** entre as 5 stale que o resume estava engolindo. *(c)* **Censo das 24 células s42:** `schema_version=1`, `campanha_id` **ausente**, `repo_hash` **vazio** em 24/24. **Corpus T11:** `schema_version=2` em 3/3; `campanha_id='t11-smoke-195f64e'` (smoke) e `'195f64ed0a69_2026-07-30'` (par G-6); `repo_hash=195f64ed0a69dc3f6c64c66912076e243be90c49`, que `git cat-file -t` confirma ser um **commit real** do repo (`195f64e [T11-B3/T5] relatório de conformidade dos 24 configs`). *(d)* A lacuna que isso fecha é operacional e é do b1: duas células de semente 0 do b1 existiam e eram confundidas com prontas; com o carimbo, o disparo das 30 sementes distingue. *(e)* **(2)** — direta. ⚠ **Armadilha:** um gate que exija `campanha_id` reprova as 24 células fiéis da s42.

**N3 — Writer do ⑥ em append (T11-G1). 🆕 A correção que é do MEU config.** *(a)* Inexistente no paper. *(b)* B-11: `src/experiment.m:jsonl_open` passou a **truncar 1× em `'w'`, fechar e reabrir em `'a'`**, e o comentário do commit `945b6be` nomeia explicitamente **"a família do `main/b1/WFG1` (49 de 931 spliced, 0 footer)"** como o alvo. *(c)* **Três medições minhas.** (i) **Código em HEAD**: `experiment.m:3347-3353` tem a sequência trunca→`fclose`→`fopen(path,'a')` com asserts nos dois. (ii) **Lado Python**: `src/audit_log.py:130-135` já usa `O_APPEND` em **toda** abertura, com `O_TRUNC` só quando `append=False`. (iii) **Gêmeo em tempdir** (`writer_twin.py`, 2 processos × 5.000 linhas, 2 repetições cada): `w+w` → **sobram exatamente 5.000 de 10.000** (reprodução exata do número do commit); `w+a` → perde 4.913 e 3.987; **`a+a` → 10.000 de 10.000, 0 malformadas, nas 2 repetições**. (iv) No corpus T11 o ⑥ do b1 tem **0 linhas malformadas e footer presente** em 3/3. *(d)* A correção **funciona e é completa para o modo de PERDA**, porque os dois escritores do ⑥ do b1 (o dono MATLAB e o despachante Python) estão agora ambos em append — exatamente o caso `a+a` que mede zero perda. *(e)* **(2)** — direta-declarativa + medida. ⚠ **O que ainda falta (§4.4):** `jsonl_line` (`experiment.m:3355-3361`) faz `fprintf` **sem `fflush`**, e o smoke é single-writer — ou seja, o modo de **splice** (as 50 linhas rasgadas) fica muito improvável mas **não provadamente eliminado**, e o smoke não exercita concorrência.

---

## 3. Percentual por classe (Botão 2)

**Total = 34 aspectos · T = 2 (B9, B10b) · denominador = 34 − 2 = 32.**

| classe | aspectos | n | % |
|---|---|---:|---:|
| **(1)** conforme o artigo | U2, U3, F1, F2, F3, B2, B4 | **7** | **21,9%** |
| **(2)** desvio/extensão sancionado | U1, U4, **U5**, U6, U7, U8, U9, U10, U11, F4, F5, F6, B1, B3, B5, B6, B7, B8, **B10a**, B11, B12, B13, N1, **N2**, **N3** | **25** | **78,1%** |
| **(3)** desvio inexplicado 🎯 | — | **0** | **0,0%** |
| **T** (fora do denominador) | B9 (miolo da MLE — só o θ final é logado) · B10b (operadores do GA interno — genealogia rejeitada no DI-10) | 2 | — |

**Comparação com a F5** (31 aspectos, T=3, denominador 28, 25,0%/75,0%/0,0%): o T caiu de 3 para 2 (U5 fechado por dado, B10a por derivação); entraram 3 aspectos novos (B10a desdobrado, N2, N3), todos (2). A queda de (1) de 25,0% para 21,9% é **puramente de composição** — os 3 aspectos novos são instrumentação/infra, que por construção não são "conformes ao artigo". **Nenhum aspecto mudou de classe no sentido de piora, e os 7 (1) são os mesmos 7.**

*U12 do protocolo (⑦/`nd_pos_real`) não se aplica: b1 é online e não tem camada ⑦.*

---

## 4. 🆕 AS CORREÇÕES DA T11 — o que tocou o b1

### 4.1 O que a T11 mudou no b1: **nada no algoritmo, quatro coisas na camada compartilhada**

| melhoria | implementada? | funciona de fato? | evidência | o que falta |
|---|---|---|---|---|
| **G-6 · interruptor `sonda_off` (MATLAB)** | ✅ `4126b78` | ✅ **SIM, e é a correção mais valiosa** | ① bit-idêntica com/sem (61×10), ③-online 5.076 linhas idênticas, ⑥ 54 eventos idênticos em 16 campos; e a sonda comprovadamente **rodou** num lado (`desligada` false→true, n_blocos 28→0, ③ 61.076→5.076) | a prova é de **1 célula em 1 máquina**; nada garante a mesma coisa em D=30 |
| **G-1 · writer do ⑥ em append** | ✅ `945b6be` (`experiment.m:3347`) | ✅ para o modo PERDA (gêmeo: `a+a` 0/10.000 perdidas ×2; `w+w` 5.000 perdidas) | código dos DOIS escritores lido (`experiment.m:3347`, `audit_log.py:130`) + gêmeo em tempdir | **sem `fflush` por linha** em `jsonl_line` ⇒ o modo SPLICE não está provado eliminado; e **o smoke é single-writer, não testa concorrência** |
| **G-3/G-7 · `campanha_id` + `repo_hash` + schema v2** | ✅ `4253fa4`/`83dbdac` | ✅ 3/3 no T11, `repo_hash` = commit real | censo: 24/24 s42 sem; 3/3 T11 com | as 24 células da s42 **continuam schema v1** — qualquer gate novo tem de tolerar isso |
| **campanha vs semente 0** | ✅ | ✅ resolve um problema **do b1** | o commit nomeia `b1 DTLZ2` e `b1 ZDT1` de semente 0 entre as 5 stale | nada a fazer no b1 |

### 4.2 A verificação que ninguém pediu: **o código de hoje ainda produz um run fiel?**

Rodei a bateria de identidades da F5 sobre o smoke T11 (código de hoje, 54 iterações): **F1 EI 54/54 · B2 gbest 54/54 · B3 normalização 54/54 · B5 subset 54/54 · B7 GA 54/54 · B12 n_arquivo 54/54 · U7 timing 54/54 · U4 cadência exata (28 blocos) · ledger 54 = 40 + 15 − 1 · B11 `dist_min=0` em 14 = cache−c0 · B6 guards todos zero · B4 nulos do D47 · θ ∈ [1e-5, 20]**. **Doze identidades, todas fechando.** O mesmo nos dois lados do par G-6. **A instrumentação nova não quebrou o mecanismo.**

### 4.3 WFG1 — o dano REAL, re-medido (e o handoff subestima)

| | handoff D-F | **medido por mim** |
|---|---|---|
| linhas do ⑥ | 930 | **931** |
| malformadas | 49 | **50** |
| eventos `b1_gen` perdidos | "39 de 413" (9,4%) | **69 de 443 (15,6%)** — o ④ tem **443** linhas e o `fit_series` **443** entradas; o ⑥ para na geração 413 |
| bloco perdido | "contíguo a partir de 358" | **NÃO é contíguo** (o conjunto ausente vai de 358 a 412 com furos, mais tudo de 414 a 443) |
| footer | ausente | ausente ✅ |

**E a conclusão que muda a decisão do autor:** o dano é **exclusivo do ⑥**. ① = 681 = 31D−1 exato, init = 241 = 11D−1 exato, DoE `max|ΔX|=0,0` e `doe_hash` ✅; ② = 444 gerações com `|②_g|` correto em 444/444; ③ = 854.888 linhas (223 blocos de sonda, todos 2.000); ④ = 443 linhas, 100% da invariante de timing. **Sobre os 374 eventos sobreviventes, TODAS as identidades fecham**: F1 374/374 (máx 1,55e-14), B2 374/374, B3 374/374, B5 374/374, B7 374/374, B12 374/374, B8 0 violações, B6 guards zero, F2 com os 2 únicos desencontros = as 2 iterações de cache. **O ledger fecha exato (443 = 440 + 4 − 1) e a cadência fecha exata (223 blocos).**
**Recomendação concreta ao autor (muda o item 7/8 do §7 do handoff):** eu computei a métrica que faltava — **b1/WFG1 IGD+ = 0,518932, HV = 0,180537, n_nd = 118** (pipeline validado: recomputei MMF1/WFG2/ZDT3 e bati o `metricas_finais_f52c.csv` com Δ ≤ 4e-7). O b1 era o **único** dos 15 algoritmos sem linha de WFG1. **A célula NÃO precisa de re-run para a análise R4** — perdeu-se 15,6% do filme de auditoria, não o dado experimental. Se o bucket tiver o ⑥ íntegro (D-F), é ganho puro; se não tiver, o re-run é opcional e de prioridade baixa.

### 4.4 O padrão "campo presente, dado sentinela" (o padrão do c217) — **não existe no b1**

Procurei-o e **não achei — porque o b1 não recebeu campo novo nenhum**. Diff de schema smoke × s42 nas quatro camadas: **①②③④ com colunas idênticas, zero novas, zero sumidas**; e as chaves dos 5 tipos de registro do ⑥ (`header`/`guard`/`sonda`/`b1_gen`/`footer`) são **exatamente as mesmas**. As melhorias I-1 (`ref_ids`/`pmid_ids`), I-2 (`REGRA_DO_ROTULO`), I-5 (`y_treino_dist`) e I-6 (sonda estratificada) são dos **classificadores** (b4/c217/c122/e74) — o b1 não é classificador e não as recebe; a I-4 (`NO_RETRY`, `experiments.py:84`) é do lado Python. A **I-3** (`tempo_aval_real_s`) o handoff lista como conquista do smoke: **medido, ela já existia na s42** — presente e não-nulo em **24/24** células pré-T11 (MMF1: 0,0323 s). Para o b1 é um no-op.
O que **sim** merece a etiqueta "sentinela por ausência": `sonda.desligada` e `campanha_id` **não existem** nas 24 células da s42 — quem escrever gate sobre eles reprova o corpus oficial inteiro.

### 4.5 Desconfiando dos números do handoff — **as re-medições, uma a uma**

| afirmação do T11 | minha medição (s42 inteira) | veredito |
|---|---|---|
| VD-b1 "o ramo é **3,87%** dos candidatos scorados" | **3,78%** (2,78 M de **73,47 M**; com WFG1: 3,79% de 77,36 M) | ✅ **confirmado** (o denominador deles, 87,77 M, é 19% maior que o meu, que é exato: `Σ ga_pop×ga_iters`) |
| ERRATA 16: "e0_trace CONSTANTE em **597** dos 1.029" | **597** ciclos com `e0_trace` constante | ✅ **exato, ao número** |
| "1.029 ciclos onde a 1ª geração venceu" | **924** (23 células) / **977** (24 células) por `argmin(e0_trace)==0`; testei 3 critérios alternativos e nenhum dá 1.029 | ⚠ **não reproduzo** — diferença de 5–11% |
| §15.5: "`e0_trace` **constante em 58% dos ciclos**" | **597/7.073 = 8,4%**. Os 58% são `597/1.029`, uma fração **condicional** a "a 1ª geração venceu" | ❌ **ERRATA da ERRATA** (a 17ª) — o handoff generaliza um número condicional |
| "INERTE em ~93,9% dos ciclos" | **95,4%** pelo mesmo critério (7.073−327 = 924−597 melhoras reais). Mas **nenhum dos dois número limita a influência**: por truncamento elitista (`EvolALG.m:63`) a população da 1ª geração é o **pool de pais da 2ª**, então o galho propaga por descendência | ⚠ **direção certa, cota inválida** |
| ERRATA 10: "afeta só a 1ª geração; da 2ª em diante os domínios casam (`EvolALG.m:65`)" | ✅ confirmado por leitura — mas a linha é **`:64`**; a `:65` é `IFEs = IFEs - size(Off,1)` | ✅ com correção de linha |
| "o conserto seria um token (`ParEGO.m:92`)" | ✅ `ParEGO.m:92` é `[PopDec,gainfo]=EvolALG(Problem,PCheby,Population.decs,dmodel,IFEs)`; a L.1 do bundle cita `:60` — é a **mesma chamada** na numeração pré-instrumentação | ✅ (a divergência de linha é aparente) |
| ERRATA 11: "7 configs sem `params` no ⑤ — era leitura da s42 pré-T11" | `params` presente e completo em **24/24** células s42 do b1 (11 chaves) | ✅ b1 nunca esteve entre eles |
| D-F: "b1/WFG1 perdeu 39 de 413" | **69 de 443**, ausências **não contíguas**, 50 malformadas | ❌ **subestima** (§4.3) |

---

## 5. Comparação canônica (Entrega 2) — Botão 4 respeitado

**Setup do paper:** 9 funções (KNO1, OKA1/2, VLMOP2/3, DTLZ1a/2a/4a/7a), d=2–8, M=2–3, init 11d−1, **100 e 250 FEs**, 21 runs, S-measure/HV cru + ε-binário, Mann–Whitney 99%, baselines NSGA-II e busca aleatória. Envelope declarado: "até oito dimensões". Inalterado nesta rodada.

| nosso × paper | veredito | sentido do paper | executado |
|---|---|---|---|
| **DTLZ2** D=12,M=3,371 FE × **DTLZ2a** d=8,M=3,250 FE | **âncora direcional** | ParEGO ≫ NSGA-II (S 4,4620 × 4,07554, MW 99%) | IGD+ **0,0901** × nsga2 0,2315 (2,57×) e × melhor piso 0,1701 (1,89×); HV 0,5388 × 0,2712. **SENTIDO CONFIRMA ✓✓** |
| **DTLZ7** D=22,M=3,681 FE × **DTLZ7a** d=8,M=3 | **âncora direcional** | inversão de regime: NSGA-II vence a 100 FEs, ParEGO a 250 | IGD+ **0,0488** × nsga3 1,1247 (23,0×); HV 0,5032 × **0,000 nos 4 pisos**. **SENTIDO CONFIRMA ✓✓** |
| **DTLZ1** D=7,M=3, g com 20π × **DTLZ1a** d=6,**M=2**, g com **2π** | **incomensurável (eixo X)** | quase empate | registrado, não julgável |
| **DTLZ4** × **DTLZ4a** | **N/A com causa** | sem vencedor no paper | célula nunca produzida — a única âncora perdida |
| **WFG1** (agora com métrica) + 20 outras | **incomensuráveis (eixo X)** | ausentes do paper ou fora do envelope | WFG1: IGD+ 0,5189 × melhor piso smsemoa 0,4954 ⇒ razão **1,047**, dentro do piso de ruído ⇒ **não é sinal** |

**Placar: comparável 0 · âncora direcional 2/2 executadas e CONFIRMADAS · 1 perdida por falha real · incomensuráveis 21.** Os níveis do paper são HV **cru** com referência do superset dos 21 runs em objetivos não-normalizados e d ≤ 8; a nossa primária é IGD+ normalizado com ref HV=(1,…,1) (D69/D70): **grandezas incomensuráveis em nível — em nenhum ponto se declara "o artigo foi melhor"**.

---

## 6. Saúde em escala (Classe B — comportamento, não fidelidade)

- **Corpus:** 24 células. A tabela célula-a-célula da F5 (23 linhas) vale sem alteração — nenhuma métrica foi recomputada, e não havia por quê. **A linha nova é a WFG1: IGD+ 0,5189 · HV 0,1806 · n_nd 118 · razão vs melhor piso 1,047 · 8º de 15 algoritmos.**
- **Posição vs pisos:** bate o melhor dos 4 pisos em **14/24** (era 14/23; a WFG1 não bate, mas a razão 1,047 está muito dentro do piso de ruído O-18 e **não é lida como sinal**). Vitórias decisivas (razão <0,41): ZDT6 0,022 · ZDT3 0,037 · DTLZ7 0,043 · ZDT1 0,059 · BBOB_F1 0,240 · BBOB_F5 0,295. Derrotas decisivas (>1,59): WFG4 2,58 · WFG9 1,93 · DTLZ1 1,91 · WFG2 1,81 · DTLZ3 1,74 · ZDT4 1,63.
- **Trajetórias:** IGD+ monotônico em 23/23, **0 violações em 437 transições**; HV nunca cai; ganho mediano 18,1×, máximo 198,6× (ZDT3).
- **Sonda — régua Sobol (bloco `regime='sonda'`, o único comparável entre configs):** ΔWAPE mediano **+48,9%**, piora em 18/23; cobertura 2σ 0,9415→0,4275. Por família: ZDT com correlação subindo a 0,96–1,00 e cobertura colapsando (ZDT4: corr 1,00 × cob 0,0875); BBOB com correlação degradando a ~0; WFG travado em 0,05–0,35 em D=22.
- **Bloco `sonda_estratificada` (regra 12 do CONTRATO): NÃO EXISTE no b1** — nem na s42 nem no smoke T11 (regimes medidos: apenas `online` e `sonda`). Não há bloco separado a reportar, e nada a misturar. **Cumprida por vacuidade.**
- **Regra 11 nova (dominância LOSSY):** aplica-se com força ao b1 — 176 X duplicados em float32 na ① em 15/24 células, com `|Δf|` até 1,22e-4 entre pares que a busca distinguiu em float64. Todo `n_nd` do config (inclusive os 118 da WFG1) carrega a ressalva.
- **Custo:** 6,11 h-core nas 23 células (~2,4× o b3 por célula) — 1 fit de DACE por iteração contra 1 por ciclo, com fração de tempo em fit chegando a 71,4% em ZDT1.

---

## 7. SCORE, recomendação e VEREDITO

### **SCORE: 9,6/10 · ACEITAR + caveat**  ·  **VEREDITO: MELHOROU (9,4 → 9,6)**

**As quatro causas, nomeadas:**

1. **INSTRUMENTAÇÃO NOVA fechou um teto — a causa principal.** O par `g6_com`/`g6_sem` não existia na F5; agora existe e eu o medi: **① bit-idêntica, ③-online bit-idêntica, 54 eventos do ⑥ idênticos em 16 campos**, com a sonda comprovadamente ligada de um lado. O **U5** era um dos 3 itens do teto e um dos "o que impede o 10" da F5. Está fechado, com evidência de classe `direta`.
2. **MAIS EVIDÊNCIA — a WFG1 voltou.** 23 → **24 células**; +443 iterações, +681 linhas de ①, +205.350 de ②, +854.888 de ③, +223 blocos de sonda. E não voltou de graça: **todas as identidades fecham nos 374 eventos sobreviventes**, o ledger fecha exato, a cadência fecha exata, e ela reproduz a assinatura do finalProbe numa 6ª célula. Além disso **eu preenchi a métrica que faltava** (IGD+ 0,5189), que era o único buraco do b1 na matriz de 15 algoritmos × WFG1.
3. **CORREÇÃO REAL, verificada — e ela é do meu config.** O `jsonl_open` em append (T11-G1) ataca nominalmente a família `main/b1/WFG1`. Verifiquei nos dois escritores (`experiment.m:3347`, `audit_log.py:130`) e no gêmeo em tempdir: **`a+a` perde 0 de 10.000 em 2/2; `w+w` perde exatamente 5.000**. O modo de perda que destruiu 15,6% do filme da WFG1 está eliminado.
4. **MÉTODO — o torneio K.3 saiu da sombra.** Era T ("invisível nos dados") e vira **(2) com efeito quantificado**: ativo em 91,5% das iterações, `P(pai=DoE)` mediana **0,9912**, peso **3,78%** dos 73,47 M candidatos, com a derivação exata (o vetor de aptidão está **ordenado** pós-cap ⇒ o torneio degenera em `min(U1,U2)` ⇒ seleção por **idade**, não por aptidão). E isso **REFUTA uma recomendação minha da F5**: eu havia nomeado o torneio "candidato de primeira ordem para explicar o desempenho fraco em WFG/D=22"; medido, o galho da 1ª geração produz o vencedor com melhora real em **0,0% (WFG2, WFG4), 1,4% (WFG5, WFG9)** dessas células. **A hipótese está morta**, e o re-run instrumentado de MMF4 que eu recomendei **não é mais necessário** — a derivação sobre campos já logados resolve o item.

**O que NÃO mudou, e é o esperado:** os 31 aspectos da F5 mantêm classe e valor; nenhuma identidade se moveu; **zero aspectos classe (3)**, agora sobre 24 células em vez de 23. O mecanismo não mudou porque **nenhuma linha do b1 foi tocada** — e isso é uma conclusão, não uma omissão.

**O que impede o 10:** (i) a **DTLZ4** continua nunca produzida — fragilidade real do `dacefit` ante subset degenerado, determinística em duas arquiteturas, e ela custa a única âncora direcional perdida; (ii) o teto **T de 2 itens** (miolo da MLE, operadores do GA), agora ambos *lidos* mas não mensuráveis; (iii) a **cauda de sobreconfiança** (cobertura 2σ 0,94→0,43, 21/23 abaixo de 0,95 no fim) — comportamento fiel, mas limita o quanto se pode confiar no σ do b1; (iv) o modo **splice** do writer não está provadamente eliminado (sem `fflush` por linha) e o smoke não testa concorrência; (v) **novo:** a trajetória do b1 é **sensível à arquitetura** (§9, armadilha 14) e a campanha M8/M9 roda em 3 máquinas.

---

## 8. Aspectos classe (3) 🎯

**Nenhum.** Nada deste config segue à verificação adversarial como desvio inexplicado — agora sobre **24 células, 7.447 eventos de ⑥, 13,53 M linhas de ③ e 3 células do corpus T11**. Os cinco "não-fechamentos" da F5 continuam resolvidos (Φ com `erfc`; `gbest` no critério absoluto; janela `fe` em gerações de cache; rastreamento elitista do EI; duplicatas float32 do export), e os três achados novos desta rodada foram todos dissolvidos com causa provada:

1. **A WFG1 "reprovada" não estava corrompida** — o dano é do writer e é exclusivo do ⑥; ①②③④⑤ estão íntegras e fecham tudo.
2. **O smoke T11 diverge da s42 na mesma célula** (48 → 54 iterações, 9 → 15 cache-hits, 3 de 61 linhas da ① diferentes) — **não é perturbação da instrumentação**: o DoE é bit-idêntico, a **sequência de λ é idêntica em 48/48 gerações** (o RNG não foi tocado), o **θ é bit-idêntico em 45/45** (o fit é o mesmo), e a deriva vive só no preditor (`mu_best` máx 3,2e-11, `ei_best` máx 3,4e-12) até um empate virar de lado na g46. A s42/MMF1 rodou na **vm3** (`tempo_f52d.csv`) e o smoke no **Mac** — é ULP de BLAS. ⚠ **Confonde código×máquina e não é separável com este corpus** (não há smoke de ZDT1/ZDT3, as únicas células b1 do Mac na s42) — vai ao teto §9.
3. **O torneio K.3** — classificado (2), com o efeito medido e a decisão 🟠 **ratificada**, pelo argumento do próprio paper (§III-A: maximizador de aquisição intercambiável).

**Um item vai à F5.7 como recomendação de código, não como (3):** `experiment.m:jsonl_line` sem `fflush` por linha. Custo: 1 linha. Fecha o modo splice que ainda não está provado eliminado, no writer que já perdeu 15,6% de uma célula do b1.

---

## 9. Teto de verificabilidade (T) + armadilhas + o que este corpus NÃO permite

**T — exige código ou re-execução (2 itens, ambos agora com o código LIDO):** o **miolo da MLE** (`ParEGO.m:78` — chamada única de `dacefit`, sem envelope de restart: confirma a declaração, mas a trajetória do boxmin, o nº de avaliações da verossimilhança e o critério de parada seguem invisíveis) · os **operadores internos do GA** (`OperatorGA` default no crossover e `{0,0,1,20}` na mutação — a ③ grava o pool final, não quem o gerou). **Saíram do teto:** U5 (par G-6), B10a (derivação sobre campos logados), e a prova de que a re-escala do DoE não roda (a linha não existe mais no arquivo).
**Fora do alcance deste corpus:** a contabilidade de RNG por chamada · as ~10.000 predições intermediárias do GA por iteração · o erro de fantasia **por objetivo** (limitação irremediável do D47) · **a não-perturbação da sonda em qualquer célula que não seja MMF1/Mac** · **a separação código-T11 × máquina na deriva do §8.2**.

**Armadilhas — as 12 da F5 seguem TODAS válidas em 24 células** (Φ com `erfc`; `gbest` absoluto; janela `fe` em cache; `n_arquivo ≠ |①|`; ③ = pop final com EI elitista; `dist_min=0` ⟺ cache; clone-hazard industrial no BBOB; X duplicado em float32 — **agora 176 em 15/24**; `c0=1` é lei; finalProbe fora do relógio; dedup da DTLZ1; régua escalar e móvel). **Seis novas:**

13. **A prova de não-perturbação é POR CÉLULA E POR MÁQUINA.** O par G-6 do b1 é MMF1 no Mac. Estender "19/19 configs" a "todas as células" é inferência, não medição.
14. **A trajetória do b1 é sensível à ARQUITETURA.** A mesma célula, mesmo código, mesma semente: DoE bit-idêntico e λ idêntico em 48/48, mas 3 de 61 linhas da ① diferentes, cache-hits 9→15, iterações 48→54. Numa campanha de 30 sementes em 3 máquinas, **uma célula re-rodada em outra máquina não é reproduzível bit-a-bit** — a atribuição de máquina (`_logs_lotes`) tem de acompanhar o dado até a R4, e o piso de ruído O-18 tem de ser aplicado **dentro** do config.
15. **`e0_trace` constante é 8,4% dos ciclos, não 58%** (§15.5 do handoff generaliza uma fração condicional). E "a 1ª geração venceu" **não** é medida de inércia do torneio: por truncamento elitista, a população da 1ª geração é o pool de pais da 2ª.
16. **A WFG1 perdeu 69 de 443 gerações (15,6%), não 39 de 413**, e as ausências **não são contíguas** — quem confiar no `n_geracoes` do ⑥ dela erra por 16%; o número certo está no ④ e no `fit_series`.
17. **`campanha_id`, `repo_hash` e `sonda.desligada` não existem na s42** (24/24 em schema v1). Gate que os exija reprova o corpus oficial inteiro — é a armadilha da ERRATA 11 repetindo-se.
18. **O append só fecha se AMBOS os escritores estiverem em append** (medido: `w+a` ainda perde 3.987–4.913 de 10.000). O fix do b1 está completo porque o lado Python já usava `O_APPEND`; qualquer escritor futuro em `'w'` reabre o buraco.

---

**Artefatos preservados** — em `/Users/gmello/Documents/python_repos/mestrado/ua-dd-saea/f5/t11/baterias/b1/` (único local de escrita; dados, artigos, bundles e código em READ-ONLY; nenhuma célula rodada, `experiments.py` nunca invocado; interpretador `/Users/gmello/Documents/python_venvs/mestrado_experimentos_dissertacao/bin/python`):

`inspect_smoke.py` (diff de schema do ⑥ smoke × s42 — 5 tipos de registro, chaves idênticas) · `cmp_manifest.py` (⑤ nos 4 runs) · **`u5_par.py`** (a prova de não-perturbação: ①/③-online/⑥ do par G-6) · `smoke_vs_s42.py` + `drift.py` (deriva cross-arquitetura: λ 48/48, θ 45/45, ULP por campo e por geração) · **`torneio_e0.py`** → **`b1_torneio_iters.csv`** (7.447 linhas: exposição do torneio, `p_doe`, e0_trace por iteração) + `torneio2.py` + `crit.py` (os 3 critérios testados contra o 1.029) · `wfg1.py` + **`wfg1_bateria.py`** → `wfg1_iters.csv` + `wfg1_extra.py` (a recuperação da WFG1) · `wfg1_metrica.csv` (IGD+/HV inéditos) · **`smoke_bateria.py`** (as 12 identidades nos 4 runs) · **`writer_twin.py`** (o gêmeo do writer, em tempdir). *A pasta também contém artefatos `00_*`–`06_*` de uma passagem anterior que não são meus e que não cito como evidência.*

Insumos da F5 consumidos sem recomputar: `f5/baterias/b1/{b1_celulas,b1_iters,b1_extras,b1_joia_celula,b1_joia_diag,b1_surrogate,b1_fantasia,b1_saude,b1_p2}.csv` (todos os totais re-conferidos por agregação) · `f5/metricas_finais_f52c.csv` · `f5/tempo_f52d.csv` · `f5/sonda_f52e.csv` (0 linhas de b1, confirmado).