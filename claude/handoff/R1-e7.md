# handoff/R1-e7.md — Rodada 1, fan-out: e7 EDN-ARMOEA (built-in PlatEMO 4.15 — N.2.1)

**Data:** 2026-07-17 · **Env:** MATLAB **R2025a** (Mac arm64) + PONTE `pyenv` InProcess
(`~/ponte_teste/bin/python`; numpy 2.4.6 · pymoo 0.6.2 — verificada ANTES, D80/D81; Deep
Learning Toolbox — assert nativo do EDNARMOEA + mapminmax; Statistics — kmeans/pdist2).
Leitor Python independente no **env-main** (`.../mestrado_experimentos_dissertacao/bin/python`).

**Gate objetivo (encanamento, D97):** `accept.py R1-e7 --alg e7 --problema {MMF1,ZDT1,DTLZ2}
--semente 0` → **exit 0 (VERDE)** nos três: 4 saídas, **FE=31D−1 EXATO**, CP-init bit-a-bit. ✅
**Fidelidade:** instrumentada e entregue para a validação MANUAL do autor (D97) — **não é gate meu**.
**Regressão:** F0-01/02/03/04, R1-00 (stub MATLAB PASS 7/0), R1-c217 (×3), R1-c141, R1-b3 (×3),
R1-b4 (×3) e **R1-b1 (×3)** seguem VERDES; `preflight.py` exit 0 (e7-doe-D94 = APLICADO).
**Verificação adversarial extra:** 3 revisores independentes sobre os diffs — 1 achado real
(higiene: CRLF→LF no patch do trainmodel) **corrigido** (diff final cirúrgico, 1 linha + comentário).

**Escopo:** 5º fan-out do padrão c217, na MESMA sessão do R1-b1. Infra transversal INTOCADA
nesta fase (o fix O(n) do write_surrogate é da fase b1, já committado). `run_e7` = cópia do
`run_b3`/`run_b1` com os deltas do e7. Eu NÃO julguei fidelidade (D97).

---

## 1. Resultado do gate (por problema, semente 0)

| Problema | D | M | maxFE | FE_final | EXATO | CP-init | ciclos | ramos (conv/incert) | n_treino (cap 11D−1) | cache_hits | RSS max | wall | accept.py |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **MMF1** | 2 | 2 | 61 | **61** | ✅ | ✅ | 13+corte | 8 / 5 | 21 (const) | 1 | 0,9 GB | 302 s | **VERDE** |
| **DTLZ2** | 12 | 3 | 371 | **371** | ✅ | ✅ | 80 (exato) | 69 / 11 | 131 (const) | 1 | 1,8 GB | 1.691 s | **VERDE** |
| **ZDT1** | 30 | 2 | 929 | **929** | ✅ | ✅ | 200 (exato) | 152 / 48 | 329 (const) | 1 | **3,25 GB** | **4.343 s** | **VERDE** |

`cache_hits=1` = o probe absorvido (receita N.0) nos 3. **Fechamento do FE**: D=12/D=30 fecham
EXATO em ciclos inteiros (240/3=80; 600/3=200); **MMF1 (40 infills, não divisível por Ke=3)
provou o corte D61**: o 14º lote foi cortado no meio pelo hard-stop (guard `hard_stop` no jsonl)
→ FE=61 cravado. **e7 é o run mais CARO da R1 até aqui** (ZDT1 72 min — os ~1,68M passos SGD
da B6.4, medidos, nunca reduzidos) e o mais faminto de RAM (**3,25 GB** — D86, ver §4).

## 2. Patches aplicados (por arquivo:linha, numeração STOCK)

**Em `algorithms/_PlatEMO/.../Multi-objective optimization/EDN-ARMOEA/` (built-in 4.15):**
- `EDNARMOEA.m:31–:32` — **P1 injeção do DoE (D94): as DUAS linhas JUNTAS** →
  `A = Problem.Evaluation(Problem.data.X0);` (X0 NATIVO direto; a re-escala da :32 nunca roda).
  `:30 NI=11D−1` fica stock.
- `Dropout/trainmodel.m:6` — **dropout 0,1 do PAPER (fidelidade 🟠→ARTIGO, D30)**:
  `Params.dropP=[0.2,0.5]` → **`[0.1,0.1]`** (entrada+oculta; invertido; ATIVO na inferência —
  altera o σ que a tese mede; diff cirúrgico com CRLF preservado).
- `Dropout/Estimate.m:26` — **guard (N.3)**: `std=sqrt(s2-mu.^2)` →
  `std=sqrt(max(var,0))` + **3º output `nneg`** (contagem de var<0 clampada → evento `std_neg`;
  sem o guard, PopStd viraria COMPLEXO silenciosamente).
- `EDNARMOEA.m` (instrumentação só-leitura): tic/toc `trainmodel` (init 8e4) e `updatemodel`
  (8e3)/ciclo; snaps da pop SELECIONADA de cada geração interna (19/ciclo, pós-
  `EnvironmentalSelection` — DEF-C2); `ymin_vig`/`espaco_vig` (C3, ver §4); pós-infill:
  **sync D89** `Problem.FE = Problem.data.bud.fe` + **guarda (c) do D60** (`stall_ciclos` de
  ciclos consecutivos com 0 FE — só LOGA, nunca conserta) + chamada `e7_instrument`.
  `RatioVelho/RatioNew` = cópias p/ o jsonl; `flag=RatioOld-Ratio<delta` INTACTO.

**O que FICA stock (CÓDIGO — B6.6, registradas no manifesto):** δ=0.05 `<` estrito (paper
0.08); Ke=3 (paper k=5); N=100 (paper P=50 — D20); init de pesos N(0,1/fan_in) Marsaglia
(paper U[−0.5,0.5]); hardcodes que ignoram Params — decay (`trainNet.m`) e run/batch
(`updatemodel.m:4–5`); SGD 8e4/8e3, lr=0.01, batch=D, sem momentum; T=100; rede 40/40
ReLU+tanh única multi-M; `iniA` printf morto; **SEM dedup de infill** (duplicata = cache-hit
0 FE — D89; slot perdido, logado como `dup_infill`).

**⚠ ACHADO DESTA SESSÃO — sombra de path do DRLOS-EMCMO (S.8):** o built-in **DRLOS-EMCMO**
(fora do estudo) vendoriza um `Dropout/` **byte-idêntico** ao do EDN-ARMOEA, e `D<E` no
genpath → `Estimate/trainmodel/trainNet/testNet/updatemodel/dropout/iniA/MgaussRandom`
resolveriam p/ as cópias STOCK do DRLOS (as chamadas saem de `EDNARMOEA.m`, que NÃO é
same-folder do `Dropout/`) — **os patches do e7 nunca rodariam, silenciosamente** (o dropout
0.1 e o guard seriam ignorados). Só foi pego porque o 3º output do Estimate quebrou rápido.
**Fix:** `ensure_paths_e7` faz **prepend** de `genpath(EDN-ARMOEA/)` + **asserts `which`** dos
8 nomes (mesmo padrão do `ensure_paths_c141`/S.8). Vale a lição p/ TODO built-in com
subpasta: **same-folder NÃO cobre subpastas** — sempre asserte a resolução.

**Integração [IMPL] (repo raiz):**
- `src/experiment.m` — `case 'e7' → run_e7` (corpo = cópia do run_b3/b1): **`'N',100`** (D20;
  popsize interna — o e7 NÃO reescreve Problem.N; NW = |UniformPoint(100,M)| = **100 (M=2) /
  91 NBI (M=3)** = denominador do Ratio, logado); EDNARMOEA **sem `'parameter'`**
  (δ/wmax/Ke = defaults do código); ponte → `load_doe` → FEBudget ANTES do Problem → probe
  absorvido → `c217_batch_eval` (D61) → `rng` depois → export 4 camadas → CP-init → manifesto
  (params/divergências/sigma_dict) → falha honesta. + `ensure_paths_e7` (com a precedência).
- `src/e7_instrument.m` (**novo**, molde do b3_instrument) — ver §4.
- `claude_code_context/artifacts/anchors.json` — âncora **`e7-doe-D94` concretizada**
  (placeholders → literais); preflight **APLICADO**, exit 0. ⚠ **Sinalizada p/ seu veto**
  (precedente c141/b3/b1).

## 3. O que este 5º fan-out re-confirmou / acrescentou ao padrão (p/ c238, e103, pisos)

1. Padrão `run_<alg>` por cópia: 6º algoritmo seguido com a infra transversal intocada.
2. **O corte D61 no meio do lote de infill Ke=3** funcionou na 1ª ocasião real (MMF1, 40 não
   divisível por 3) — FE exato sem overshoot.
3. **Nova regra de prontidão**: built-in com SUBPASTA própria exige checagem de sombra de
   path (o caso DRLOS-EMCMO×EDN-ARMOEA) — `which`-asserts no `ensure_paths_<alg>`.
4. O writer O(n) da fase b1 digeriu as ③ do e7 (24,7k/152k/380k linhas) sem fricção.
5. Guardas armadas ≠ disparadas: `dup_infill`/`saldo_congelado`/`std_neg` NÃO dispararam na
   semente 0 (sem dedup, o e7 não propôs duplicata bit-exata aqui); vigiar na bateria.

## 4. Instrumentação produzida (para a SUA validação de fidelidade — D97)

Por run: **① real** (31D−1) · **② pop** (membership do arquivo A por ciclo — cresce +3) ·
**③ surrogate (DEF-C2 — EA, regressor POR OBJETIVO)**: a pop SELECIONADA de **CADA geração
interna** do ARMOEA-sobre-o-surrogate (19/ciclo — TODAS; MMF1 24.700, DTLZ2 152.000, ZDT1
380.000 linhas): `pred_tipo=valor`, **`mu_j` = média e `sigma_j` = desvio POPULACIONAL das
T=100 passagens MC-dropout, POR OBJETIVO**, `modelo=EDN-MCdropout`, no **ESPAÇO DO MODELO**:
`espaco_modelo=cru` no ciclo 1 (treino inicial em y cru) e `transformado` depois — o
`SelectTrainData` translada o y por **ymin=min(A.objs)** do fim do ciclo anterior (o mapminmax
é interno/reversado no Estimate); C3: `transf_tipo=translacao`, **`transf_params={ymin}`** →
**cru = μ + ymin** (invertível; a ① dá o cru real dos avaliados — leitura D47 da DEF-C3).
`geracao=CICLO` (datada pelo retreino — precedente b3); blocos por geração interna em ordem,
contagens em `pop_por_w`. `real_solution_id` nullable (double+NaN — fallback R1-00).

**`.jsonl` §17.5/S.7** (linha `e7_gen` por ciclo): `RatioOld`/`Ratio`/`delta`, **`flag` +
`ramo` (convergência|incerteza) + `motivo`** (a desigualdade exata), `lote`, `fe_ciclo`,
**`n_dup_infill`** (slots perdidos — D89), **`stall_ciclos`** (guarda (c) D60), `n_treino`,
`NW`, **`ymin`/`espaco`** (C3), `pop_por_w`, `n_std_neg`, **`rss_mb`** (D86 — RAM observável
por ciclo), tempos; eventos `guard`: `std_neg`, `dup_infill`, `saldo_congelado`, `cache_hit`,
`hard_stop`. **Timing §17.6**: 1 retreino (8e3)/ciclo + **linha extra do treino INICIAL
(8e4)** no 1º ciclo; `n_acumulado` **constante = 11D−1** (cap do SelectTrainData) → curva plana.

**Números por problema (semente 0) — GUIA, não gate (D97):**

| Problema | FE | IGD_raw | \|ND\| | gatilho dual observado | âncora do paper (Anexo J) |
|---|---|---|---|---|---|
| MMF1 (d=2) | 61 | — (MMF: IGDX pós-hoc D99) | 10 | conv 8 / incert 5 | — |
| DTLZ2 (d=12, M=3) | 371 | **2,1942e-1** (das-dennis 5050→esfera) | 88 | conv 69 / incert 11 | DTLZ2 **d=20**: 0,647 (11d+119 FEs) |
| ZDT1 (d=30) | 929 | **4,5967e-1** (front analítico, 10k) | 23 | conv 152 / incert 48 | — (paper não roda ZDT) |

> ⚠ **CAVEATS para a sua leitura (D97 — eu não julgo):**
> 1. **d/orçamento divergem da âncora J** (paper d∈{20,40,60,100}, 11d+119 FEs) — a comparação
>    justa é do **MECANISMO**: o gatilho dual (RatioOld−Ratio≷δ) alternando incerteza(max σ̄)×
>    convergência(min ‖obj transladado‖) — tudo no `e7_gen` (`ramo`/`motivo`); o σ agora vem do
>    **dropout 0,1 do paper** (patch D30 — o σ é o objeto da tese).
> 2. **O ramo incerteza DISPAROU** nos 3 problemas (5/11/48×) — o switch dual está vivo (cf.
>    ablação Tabela I do paper, M.5).
> 3. **`dup_infill`/`saldo_congelado` NUNCA dispararam na semente 0** — o e7 não propôs
>    duplicata bit-exata aqui; se a bateria congelar saldo (ciclos 0-FE), o evento acusa e o
>    watchdog D60-b (timeout) é a rede — comportamento do algoritmo, não conserto (D89).
> 4. **RAM (D86): pico 3,25 GB no ZDT1** (rss por ciclo no jsonl) — com 8 workers PCT ≈ 26 GB
>    agregados no pior caso; **dimensionar no piloto** antes das 30 sementes.
> 5. **Custo (B6.4): ZDT1 72 min/run** (fit inicial 11 s; update mediano 1,17 s ×200; o grosso
>    é o Estimate — 100 passes × 20 chamadas × 200 ciclos); extrapolação 30 sementes ≈ 36 h/core
>    — o e7 é o novo pior-caso de wall-clock da R1 (§22.2: pior que b3 1.570 s e b1 1.719 s).
> 6. **IGD é raw** — compute do SEU jeito a partir da ① crua.

## 5. Como reproduzir

```
# MATLAB (raiz do repo):
addpath('src'); addpath(genpath('algorithms/_PlatEMO/PlatEMO'));
experiment('e7','MMF1',0,'main','data')     % idem ZDT1, DTLZ2
% ou pelo despachante (skip idempotente; e7 ja esta no roster default):
experiments('algorithms',{'e7'},'problems',{'MMF1','ZDT1','DTLZ2'},'seeds',0,'parallel',false)

# Python (env-main), gate objetivo:
python3 scripts/accept.py R1-e7 --alg e7 --problema MMF1 --semente 0   # idem ZDT1/DTLZ2
```
Outputs em `data/experiments/main/e7/` (gitignored — regeneráveis).

## 6. Pendências / notas

1. **Commits `[R1-e7]`** (3): patches EDN-ARMOEA (3 arquivos `_PlatEMO`) · adapter `run_e7` +
   `e7_instrument` + âncora concretizada · handoff + cartão. Nenhum blob.
2. **Para seu VETO (sinalizações desta sessão):** (a) âncora `e7-doe-D94` concretizada (§2);
   (b) carimbo `geracao=ciclo` na ③ + ③ em ESPAÇO DO MODELO com `transf_params={ymin}`
   (leitura D47 da DEF-C3 — uma linha por candidato, cru derivável por μ+ymin; NÃO
   materializei as duas linhas por espaço); (c) a **precedência de path do e7**
   (prepend+asserts — mexe na ordem de resolução do processo MATLAB; c141 tem o mesmo padrão).
3. **Toolboxes em runtime**: Deep Learning (mapminmax + assert nativo) + Statistics (kmeans/
   pdist2); sem trainNetwork (a rede é feita à mão — a RAM D86 é do processo, medida por ps).
4. **A validação de fidelidade é SUA (D97)**: números da §4 são guia; o veredito (gatilho dual
   fiel? σ com cara de MC-dropout 0,1? translação coerente?) é seu, lendo os `.jsonl`
   (`ramo`/`motivo`/`ymin`) + a ① + a ③ com `transf_params`.
