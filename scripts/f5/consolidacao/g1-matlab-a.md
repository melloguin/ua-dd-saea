[{"config":"b1","familia":"GP-BO","stack":"MATLAB","n_celulas":23,"exclusoes":"2 exclusões das 25 do grid: DTLZ4 — célula NUNCA produzida, falha REAL e determinística do dacefit (`least squares problem is underdetermined`, status=failed), tentada 2× em 2 arquiteturas (vm3 Linux/Intel e Mac arm64), categoria D do INVENTARIO item 29; WFG1 — REPROVADA na F5.2a (`JSONL_RUIM linhas=930 ruins=49 footer=False`, 49 eventos truncados sem footer, madrugada de 2026-07-27 na vm3, janela O-19 com assinatura de escrita concorrente): ①–⑤ íntegras e gates F5.1 verdes (não parseiam o ⑥), mas o filme do mecanismo (Classe A) está contaminado ⇒ fora do denominador.","score":9.4,"recomendacao":"aceitar+caveat","pct_classe1":25.0,"pct_classe2":75.0,"pct_classe3":0.0,"denominador":28,"n_teto_T":3,"P1_protocolo":"✅ — as 24 células então existentes ficaram verdes em TODOS os gates F5.1 (FE 31D−1 exato, CP-init bit-a-bit, camadas, cadência); b1 não aparece entre os 5 vermelhos da 2ª passada. Re-verificado por conta própria: `len(①)==31D−1` 23/23 (9.804 linhas), `fase=='init'`==11D−1 23/23 (3.464), CP-init com max|ΔX| = 0,0 EXATO em float32 nas 23, `doe_hash` ≡ sidecar 23/23, `cp_init=true` 23/23, cadência {1}∪{pares≤G}∪{G} exata 23/23 com 3.562 blocos × 2.000 pts. Nota: os gates NÃO parseiam o ⑥ — por isso WFG1 passou na F5.1 e só caiu na F5.2a.","P2_contrato":"⚠ — `contrato_f52b.csv`: ZERO linhas para b1 (contagem da ①, fase init, schema ①③④, blocos de sonda, `fe_final==maxfe`, `sigma_dict` presente); b1 NÃO está entre os 7 configs da não-conformidade transversal do ⑤ sem `params` (verificado: `manifest.params` completo — N_lambda, IFEs, rho, dace, theta0, theta_bounds, warm_theta, mle, subset, normalizacao, ga_interno — em 23/23). `integridade_f52a.csv`: 1 achado, e é INFRA de escrita, não do mecanismo — `main/b1/WFG1` JSONL_RUIM, corretamente reprovada e excluída (pede `'parallel',false` explícito no RUNBOOK, item 8 do RELATORIO_F5 §5.6).","P3_mecanismo":"✅ — 31 aspectos, T=3, denominador 28: classe (1) 7 aspectos = 25,0% (U2, U3, F1, F2, F3, B2, B4) · classe (2) 21 = 75,0% · classe (3) 0 = 0,0%. Os 28 verificáveis fecham TODOS em contagem exata sobre 7.073 iterações, 12,67 M linhas de ③, 3.562 blocos de sonda e 9.804 linhas de ①, com decisão sancionadora citável para cada desvio (D20, D21/D61, D30, D47, D53, D57, D63/D88, D76, D89, D94, DI-10, DI-12.5, DI-13.10, DI-13.15, DEF-A8, DEF-C2, DEF-L8, B1.6, K.3).","P4_saude":"⚠ — trajetórias impecáveis (IGD+ monotônico em 23/23 células, 0 violações em 437 transições; HV nunca cai; ganho mediano 18,1×, máx 198,6× em ZDT3), mas a SONDA piora e a calibração colapsa: ΔWAPE mediano +48,9% com piora em 18/23 (régua ESCALAR e MÓVEL reconstruída bloco a bloco — o `sonda_f52e.csv` não cobre b1), e cobertura ±1,96σ de 0,9415 (1º bloco) → 0,4275 (último), com 21/23 abaixo de 0,95 no fim (ZDT4 0,9285→0,0875 com correlação final 0,9999 — sabe a ordem, não a escala). Corroboração in-sample independente: só 38,1% dos infills caem no próprio ±1,96σ; elo mecânico com os 10.578 componentes de θ saturados em 1e-5. Prior 8,5 v2 (sobreconfiança tardia) CONFIRMADO em escala.","P5_canonica":"✅ — comparável 0 · âncora direcional 2/2 executadas e CONFIRMADAS · 1 perdida por falha real (DTLZ4) · incomensuráveis 20. DTLZ2 D=12 M=3 (paper: ParEGO ≫ NSGA-II, S 4,4620 × 4,07554): b1 IGD+ 0,0901 × nsga2 0,2315 (2,57×) e × melhor piso smsemoa 0,1701 (1,89×), bate os 4 pisos — SENTIDO CONFIRMA ✓✓. DTLZ7 D=22 M=3 (paper: inversão de regime, ParEGO vence a 250 FEs): b1 IGD+ 0,0488 × nsga3 1,1247 (23,0×), HV 0,5032 contra 0,000 nos 4 pisos — CONFIRMA ✓✓. DTLZ1 registrado como incomensurável (M 3×2 e versão amaciada no paper). Botão 4 respeitado: níveis do paper são HV cru em d≤8, incomensuráveis com IGD+ normalizado (D69/D70); nossa fração DoE/orçamento é 35,4% contra 87% do pior caso do paper ⇒ as confirmações são conservadoras.","P6_vs_pisos":"✅ — bate o melhor dos 4 pisos em 14/23 (denominador 23); rank IGD+ médio 7,65 de 17 algoritmos do main. Vitórias decisivas (razão < 0,41, fora do piso de ruído O-18): ZDT6 0,022 · ZDT3 0,037 · DTLZ7 0,043 · ZDT1 0,059 · BBOB_F1 0,240 · BBOB_F5 0,295. Derrotas decisivas (> 1,59): WFG4 2,58 · WFG9 1,93 · DTLZ1 1,91 · WFG2 1,81 · DTLZ3 1,74 · ZDT4 1,63. As 11 restantes (0,52–1,29) NÃO são sinal (piso de ruído IGD+ ≤58,98%, 1 semente). ZDT1/ZDT3 rodaram no Mac ⇒ carregam caveat de máquina, mas as razões são ~20× o piso e o sinal sobrevive.","P7_adversarial":"⚠ — ZERO aspectos classe (3): os cinco não-fechamentos brutos foram todos resolvidos dentro do relatório com causa numérica provada (13 falhas do EI = erf × erfc do gabarito; 125 falhas relativas do `gbest` = divisão por ≈0; 11 furos do `norm_min` = janela de cache-hit; 1.611 não-correspondências do argmax-EI = rastreamento elitista; 161 duplicatas de X = colapso float64→float32 do D53). Um item vai à F5.4/F5.7 como verificação DIRIGIDA (não classe 3, porque é declarado): o torneio bugado de `EvolALG.m:16` (K.3), estruturalmente ativo em 23/23 células a partir do 27º treino, que o bundle diz favorecer sistematicamente os pontos mais antigos. Não há veredito F5.4 nesta rodada (não existe arquivo b1 em f5/adversarial/).","query_joia":"Identidade fechada do Expected Improvement, o coração da família BO: `(gbest − μ_best)·Φ(z) + σ_best·φ(z) ≡ −ei_best` com `z = (gbest − μ_best)/σ_best`, testada em TODAS as 7.073 iterações — fecha 7.073/7.073 no critério relativo, com z chegando a −365,2. Reforçada por duas provas irmãs: a cadeia `best_sid`→① fecha 7.073/7.073 nos dois ramos (6.340 infills novos + 733 cache-hits) e o σ do escolhido é maior que o do argmin-μ em 100% das 2.099 decisões não-gulosas.","prova_numerica_central":"7.073/7.073 @ erro relativo < 1e-6 na identidade do EI — erro relativo MEDIANO exatamente 0,0, P99 3,42e-14, máximo 1,37e-11; 5.957/7.073 fecham já no critério absoluto |Δ| < 1e-18. As 13 exceções da v1.0 foram REPRODUZIDAS com `0,5·(1+erf(z/√2))` e DISSOLVIDAS com `ndtr`/`0,5·erfc(−z/√2)`: o log do MATLAB estava certo o tempo todo.","achados_classe3":[],"caveats_d97":["b1 é o ÚNICO config cuja régua de sonda é ESCALAR e MÓVEL (D47, mono-output sobre o PCheby): comparar o ΔWAPE +48,9% do b1 com o +4% do b3 ou o −38% do e81 é estruturalmente inválido (regra 4 do R4).","A cobertura ±1,96σ cai de 0,94 para 0,43 (21/23 abaixo de 0,95 no fim): o σ do b1 é fiel ao mecanismo mas NÃO é confiável para análise de incerteza — ZDT4 termina com correlação 0,9999 e cobertura 0,0875.","O grid de λ com 100/91 vetores (D20) contra os 11/15 da Tabela V quebra o racional declarado das 'várias passadas' do §VII-B e penaliza EXATAMENTE o regime de orçamento curto (D=2 faz 0,48 passadas onde o paper faria 4,4); em runs longos é neutro ou favorável (ZDT1 faz 6,0).","O GA interno tem 6 gerações em D=30 (lote ≈2·|arquivo|, IFEs=10.000): o orçamento de aquisição é o do paper, a TOPOLOGIA da busca não é — hipótese natural para a fraqueza em WFG/D=22.","Clone-hazard industrial no BBOB: F37 com 316 cache-hits (61,4% das 515 iterações) e F49 com 305 (60,5%), streaks de até 37 — fiel ao D89 (0 FE), mas reduz os infills efetivos a 200 de 309; é comportamento, não bug.","2 das 25 células do grid estão perdidas (DTLZ4 e WFG1) — o denominador do b1 é 23, e a única âncora direcional perdida do config é justamente a DTLZ4.","Correções de prior da v1.1 que o D97 precisa herdar: a fração não-greedy é 38,4% (não 57,4% — o número antigo tinha ruído de float32) e 'σ = 0 exato ocorre' é FALSO (mínimo real 5,15e-9; o guard P2 nunca precisou clampar).","Um gate transversal que exija `dupX == 0` na ① reprovaria 14 células FIÉIS do b1 (161 X duplicados em float32, com |Δf| até 1,22e-4 provando que são pontos distintos em float64)."],"itens_torre_central":[{"item":"Fechar o torneio bugado K.3 (viés pró-antigos pós-cap, ativo em 23/23 células a partir do 27º treino) — leitura de código + re-run instrumentado logando o índice do pai escolhido; se confirmado, é candidato de primeira ordem para explicar a fraqueza em WFG/D=22","onde":"EvolALG.m:14-20 (b1/ParEGO, PlatEMO 4.15); re-run de MMF4","severidade":"alta","custo":"1 leitura de código + 1 re-run de MMF4 (40 iterações, ~35 s) — desprezível"},{"item":"Guard contra o modo de falha do dacefit sobre subset degenerado antes das 30 sementes (DTLZ4 morreu com `least squares underdetermined`, determinístico em 2 arquiteturas; mesma família de fenômeno do dedup que remove até 74 de 101 pontos no DTLZ1)","onde":"ParEGO.m:51-55 (dedup 1e-6) / dacefit; célula main/b1/DTLZ4","severidade":"alta","custo":"1 guard + 1 re-run da célula (~0,3 h-core)"},{"item":"Re-run de infra da WFG1 e `'parallel',false` explícito nos comandos MATLAB do RUNBOOK (a assinatura do JSONL_RUIM é de writer concorrente na janela O-19)","onde":"RUNBOOK (comandos MATLAB) + célula main/b1/WFG1","severidade":"media","custo":"1 edição do RUNBOOK + 1 re-run de célula (~0,27 h-core)"},{"item":"Não incluir `dupX == 0` como gate transversal da ① — reprovaria 14 células fiéis do b1 (colapso float64→float32 do export D53, provado por |Δf| ≠ 0 entre os pares)","onde":"gates F5.1 / protocolo de contrato","severidade":"media","custo":"edição do gate, 0 h-core"},{"item":"Congelar no protocolo que Φ deve ser calculado com `erfc` (nunca `0,5·(1+erf(z/√2))`) e que `gbest` exige tolerância ABSOLUTA — vale para todos os configs BO (b3, c238, c262, c154, e81)","onde":"PROTOCOLO_ANALISE_FIDELIDADE.md §5 / queries da F5.3b","severidade":"media","custo":"edição de protocolo, 0 h-core"}],"celulas_para_rerun":["main/b1/WFG1 — re-run de INFRA (JSONL_RUIM: 930 linhas, 49 ruins, footer=False, vm3 na janela O-19); exige `'parallel',false` explícito no RUNBOOK antes de re-disparar","main/b1/DTLZ4 — nunca produzida; só re-tentar DEPOIS de instalar guard contra o dacefit subdeterminado (falha determinística em vm3 Linux/Intel e Mac arm64)","main/b1/MMF4 — re-run instrumentado de 40 iterações (~35 s) logando o índice do pai do torneio, para fechar o item K.3 antes das 30 sementes"],"teto_T":["U5 — não-perturbação da sonda sobre o RNG e a trajetória: exige run pareado com a sonda desligada, comparando a ① linha a linha.","B9 — miolo da MLE (boxmin SEM restarts × Nelder–Mead com 20 restarts do paper, warm-θ, θ0=10 na 1ª iteração): só o θ FINAL é logado; a trajetória, o nº de avaliações da verossimilhança e o critério de parada são invisíveis (a única evidência é o degrau de custo de 2,13× no 1º fit).","B10 — operadores internos do GA (SBX 0,2, mutação-shift ±(1/100)·range, torneio binário, init 5 mutantes + 15 LHS) e o torneio bugado K.3: a ③ grava o pool FINAL, não quem o gerou; a genealogia de pais foi rejeitada no DI-10 como patch invasivo.","Prova de que a linha `ParEGO.m:30` (re-escala do DoE) literalmente nunca executa: o ΔX = 0,0 EXATO contra o artefato é evidência forte mas indireta (D94).","As ~10.000 predições intermediárias do GA por iteração: o contrafactual guloso só é computável sobre o pool final, e mesmo assim só nas 5.462 iterações pool-consistentes.","Limitação IRREMEDIÁVEL do D47: um candidato nunca avaliado não tem μ/σ por objetivo, logo o erro de fantasia POR OBJETIVO simplesmente não existe para o b1.","Contabilidade de RNG (1 `randi` para λ + 2 `randi` de torneio + GAreal + PM por geração interna)."],"capitulo_sintese":"O b1 executa o ParEGO canônico de Knowles (2006) sobre PlatEMO 4.15: DoE LHS de 11D−1 pontos e, por iteração, sorteio de 1 vetor λ, re-escalarização de TODO o arquivo pela Tchebycheff aumentada com ρ=0,05, treino de UM único Kriging DACE mono-output sobre o escalar (D47) e maximização do Expected Improvement por um GA interno de 10.000 avaliações do modelo, gastando exatamente 1 FE por iteração. As 23 células auditadas somam 7.073 iterações de BO, 6.340 infills, 756 cache-hits, 9.804 linhas de ①, 12,67 M linhas de ③ e 3.562 blocos de sonda, a 6,11 h-core. O mecanismo está provado três vezes por dado: a identidade fechada do EI fecha 7.073/7.073 no critério relativo com erro mediano exatamente 0 e máximo 1,37e-11 — e as 13 exceções da v1.0 eram cancelamento catastrófico do GABARITO do analista (erf contra erfc), não do MATLAB; a cadeia `best_sid`→① fecha 7.073/7.073 nos dois ramos; e nas 2.099 decisões não-gulosas (38,4% das 5.462 pool-consistentes) o σ do escolhido é MAIOR que o do argmin-μ em 100% dos casos, razão mediana 1,074 (até 6,06) e percentil-σ 0,454 contra 0,025 — o uso da incerteza é categórico, não estatístico. Divergem, todas com decisão sancionadora citável: λ em grid de 100 (M=2) / NBI 91 (M=3) contra os 11/15 da Tabela V (D20, que quebra o racional das várias passadas e penaliza justamente o regime de orçamento curto), normalização pelo min/max do ARQUIVO por iteração contra os limites exógenos do §IV (B1.6 — é a divergência de maior efeito sobre a INTERPRETAÇÃO das métricas de surrogate e nenhum sobre a fidelidade), subset top-(11D−1+25) determinístico com dedup 1e-6 contra o ½ melhores + ½ aleatórias, GA geracional com lote ≈2·|arquivo| contra o steady-state de população 20 (em D=30 sobram 6 gerações internas), e boxmin sem restarts contra Nelder–Mead com 20 restarts (teto T). Resultado: ZERO aspectos classe (3) e 28/28 verificáveis fechando em contagem exata — 25,0% classe (1), 75,0% classe (2). O comportamento em escala é saudável (IGD+ monotônico em 437/437 transições, HV nunca cai, ganho mediano 18,1×, 14/23 batendo o melhor piso, rank 7,65/17, domínio nítido em ZDT1/ZDT3/ZDT6/DTLZ7 com razões 0,02–0,06), mas a calibração colapsa: cobertura ±1,96σ de 0,9415 para 0,4275, com 21/23 células abaixo de 0,95 no fim e ZDT4 terminando com correlação 0,9999 e cobertura 0,0875 — o GP sabe a ORDEM e não sabe a ESCALA, e a medida in-sample independente (só 38,1% dos infills dentro do próprio 2σ) o corrobora. Veredito 9,4 · ACEITAR + caveat: o que impede o 10 são as 2 células perdidas (DTLZ4 por fragilidade REAL do dacefit, WFG1 por infra de escrita), o teto T de 3 itens — com o torneio bugado K.3 ativo em 23/23 células e invisível nos dados — e a cauda de sobreconfiança, que é comportamento fiel mas limita o quanto se pode confiar no σ do b1."},{"config":"b3","familia":"GP-BO","stack":"MATLAB","n_celulas":25,"exclusoes":"","score":9.6,"recomendacao":"aceitar","pct_classe1":29.6,"pct_classe2":70.4,"pct_classe3":0.0,"denominador":27,"n_teto_T":4,"P1_protocolo":"✅ — b3 não aparece entre os 5 vermelhos da 2ª passada: 25/25 VERDES em todos os gates F5.1 (FE exato, CP-init bit-a-bit, camadas, cadência). Re-verificado aqui: `len(①)==31D−1` 25/25 (10.856 linhas, `fe_index` denso 0-based, 10.856 `solution_id` únicos e ZERO duplicatas de X), `fase=='init'`==11D−1 25/25 com max|ΔX| = 0,0 EXATO contra o artefato e `doe_hash` ≡ sidecar 25/25, e a cadência fecha com a fórmula {1}∪{pares≤G}∪{G} com G = n_ciclos + 1{hard_stop} em 25/25 (761 blocos × 2.000 pts, G ímpar em 12 células).","P2_contrato":"✅ — `contrato_f52b.csv`: ZERO linhas para b3; `integridade_f52a.csv`: ZERO linhas para b3 (nenhum parquet ilegível, nenhum manifesto quebrado, nenhum `.jsonl` truncado, footer presente em 25/25). b3 NÃO está entre os 7 configs da não-conformidade transversal do ⑤ sem `params` — verificado diretamente: `manifest.params` completo (N_vetores, alpha, wmax, mu, delta, dace, theta0, theta_bounds, warm_theta) em 25/25. Nada a arbitrar como 'por desenho × defeito': os únicos comportamentos que pareceriam anomalia têm regra citada (② com N+1 gerações por DEF-C2, `fe(último ciclo) < maxfe` nas 16 células com hard_stop por DEF-A2+D21, `u_efetivo=5` com Δfe<5 por D89, bloco de sonda extra em geração ímpar por DI-12.5).","P3_mecanismo":"✅ — 31 aspectos, T=4, denominador 27: classe (1) 8 = 29,6% (U2, U3, F1, F4, B2, B3, B4, B7) · classe (2) 19 = 70,4% · classe (3) 0 = 0,0%. As 27 checagens verificáveis fecham em contagem EXATA sobre 1.443 ciclos, 7.215 seleções, 3,78 M linhas de ③ e 761 blocos de sonda, com decisão citável para cada desvio (D20, D21/D61, D30, D57, D63/D88, D89, D94, DI-10, DI-12.5, DI-13.10, DI-13.15, DI-18, DEF-A2, DEF-L8, B3.2, B3.5).","P4_saude":"✅ — a sonda é 'chata' e a calibração é ESTÁVEL, ao contrário do b1: ΔWAPE mediano +4,4% por (célula,objetivo) — exatamente o +4% da régua oficial F5.2e — com piora em 13/25 e melhora em 12/25; cobertura ±1,96σ mediana 0,932 (1º bloco) → 0,924 (último), 14/25 terminando ≥0,90 e 4 ≥0,99, contra 0,94→0,43 do b1; ZERO σ-NaN em 1.522.000 medições. A estabilidade tem causa estrutural provada: |A1| é FIXO em 11D−1 (1.443/1.443), então o Kriging nunca colapsa o lengthscale. 6 células terminam <0,60 (DTLZ7 0,374, MMF11_L 0,464, ZDT3 0,497, MMF4 0,489, ZDT4 0,508, ZDT1 0,520) — diagnóstico, não patologia: onde o WAPE final é 0,000–0,004 e a correlação 0,999–1,000, o σ encolhe rápido DEMAIS (sub-confiança do MSE sob interpolação quase-exata). Trajetórias: IGD+ monotônico em 25/25 (0 violações em 475 transições), HV nunca cai, ganho mediano 11,5× (máx 196× em ZDT1).","P5_canonica":"⚠ — comparável 0 · âncora direcional 5/5 executadas · incomensuráveis 20. Das 5 direcionais o sentido CONFIRMA em 4 (DTLZ7 22,2× melhor que nsga3 e DTLZ2 batendo os 4 pisos; DTLZ1 e DTLZ3 contra ParEGO; DTLZ4 ≈ dentro do piso de ruído) e FALHA em 3 legs (DTLZ1 e DTLZ3 contra os pisos — o '≈ RVEA' do paper não se sustenta, b3 fica 2,38× e 3,29× pior; e DTLZ2 contra ParEGO, onde b1 é +58% melhor, mas isso fica na ordem do piso de ruído medido, 58,98%, com 1 semente). A regularidade qualitativa central do paper reproduz-se com precisão (K-RVEA domina em fronts bem-comportados/disjuntos e perde nos multimodais, pela razão que o próprio paper dá). Botão 4 respeitado: os níveis do paper são IGD CRU em n=10, incomensuráveis com IGD+ normalizado (D69/D70). BÔNUS: a âncora EMPÍRICA do próprio artigo (Fig. 9, tempo de treino constante) CONFIRMA-SE — `n_treino` travado em 11D−1 nas 1.443 fits e drift do tempo de fit NEGATIVO em 21/25 células.","P6_vs_pisos":"✅ — bate o melhor dos 4 pisos em 18/25 (denominador 25); rank IGD+ médio 6,36 de 17 algoritmos do main, reproduzindo a prévia da F5.2c. Vitórias decisivas (razão < 0,63): ZDT1/ZDT3/DTLZ7 0,04–0,05 · BBOB_F55 0,09 · ZDT6 0,24 · BBOB_F5 0,25 · BBOB_F1 0,30 · BBOB_F22 0,38 · WFG5 0,58 · MMF4/MMF11_L 0,59 · BBOB_F37 0,62. Derrotas decisivas (> 1,59): DTLZ3 3,29 · DTLZ1 2,38 · WFG4 2,34 · WFG9 2,08 — os multimodais e as WFG com transformações íngremes, exatamente o modo de falha que o paper antecipa. As razões entre 0,63 e 1,59 NÃO são lidas como sinal (piso de ruído O-18, 1 semente). Cabeça-a-cabeça com o b1: b3 vence em 13/23 células, razão mediana 0,91 — EA+GP × MOBO termina empatado com vantagem marginal para o b3 nesta semente.","P7_adversarial":"⚠ — ZERO aspectos classe (3): os quatro não-fechamentos brutos foram todos resolvidos dentro do relatório com causa provada (identidade de σ falhando em 5 células = tolerância ABSOLUTA contra σ ~1e6, fecha 25/25 no relativo; 104 furos do `dist_min_arquivo` = janela de ciclo errada `fe−5` em vez de `fe_anterior`; ΔWAPE de sinal invertido = ordenação lexicográfica da coluna `bloco` como string; quebra do ledger em 8 células = c0 e ciclo truncado pelo hard-stop). Um item vai à F5.4/F5.7 como verificação DIRIGIDA (não classe 3, porque é declarado): o bug de índice do ramo (a) do `UpdataArchive` (vetores→soluções), com alcance agora QUANTIFICADO — atingível só quando NI<N, i.e. D≤9, o que ocorre em 4/25 células (MMF1, MMF4, MMF11_L com D=2 e NI=21<100; DTLZ1 com D=7 e NI=76<91). Evidência de que não corrompe o observável: nessas 4 células `arquivo`≡`n_treino`≡NI em 52/52 ciclos e `nzero_updata`=0 em 52/52. Não há veredito F5.4 nesta rodada (não existe arquivo b3 em f5/adversarial/).","query_joia":"Query-joia da família GP-BO: `index` (patch de 1 linha em `KrigingSelect.m:64`) → última sub-população da ③ do ciclo → σ do selecionado, comparado com `sigma_sel.sqrtmse_por_objetivo` — fecha 7.215/7.215 seleções. Irmã e ainda mais forte: `criterio_meanMSE ≡ média_j(σ_j²)` — o critério do código é a média das VARIÂNCIAS, SEM raiz, contra a 'average of the standard deviations' impressa no paper (fator ~20× de diferença), o que transforma a DI-18 de decisão documental em FATO MEDIDO.","prova_numerica_central":"`criterio_meanMSE ≡ média_j(σ_j²)` em 7.215/7.215 seleções com erro RELATIVO máximo 4,24e-16 — identidade aritmética, não aproximação (célula exemplar DTLZ2 ciclo 48: σ = [0,053828; 0,035098; 0,055978] → média das variâncias = 0,00242093 EXATO, enquanto a média dos σ daria 0,04830). A cadeia `index`→③→σ fecha 7.215/7.215 a erro relativo máximo 5,92e-8 = exatamente o ULP do float32 do export D53.","achados_classe3":[],"caveats_d97":["O critério de incerteza do b3 é a média das VARIÂNCIAS sem raiz (`KrigingSelect.m:61`), não a média dos desvios-padrão que o paper imprime — a diferença é de fator ~20× e pondera quadraticamente o objetivo mais incerto, dominando o critério em problemas com escalas heterogêneas (WFG, BBOB). É desvio material do TEXTO, mas é o código oficial, sancionado (DI-18) e agora provado por dado.","O operando do switch é arquivo REAL × população PREDITA (não |Vf^ia|_tu−1 do paper): NumV1 ≠ NumV2 do ciclo anterior em 1.334/1.418 transições (94,1%). Consequência: o ramo TRAVA — 16/25 células rodam 100% APD, 2/25 rodam 100% incerteza (DTLZ3 48/48 e MMF16_20 80/80) e só 7/25 alternam.","O travamento em incerteza NÃO é intrinsecamente ruim: em DTLZ3 convive com WAPE plano e IGD+ 3,29× o piso (ciclo vicioso confirmado), mas em MMF16_20 o MESMO travamento total produz o 2º melhor rank do config (0,025 × 0,032 do piso).","REFUTADO pela escala (registrar): 'APD 100% em M=2' é falso — M=2 usa incerteza em 167/1.093 ciclos (15,3%) e em 5/19 células (WFG9 73,3%, WFG5 65,9%, WFG4 43,5%); a variável que trava o switch é a GEOMETRIA (sinal de NumV2−NumV1), não M.","REFUTADO pela escala: 'DTLZ2 abaixo dos 4 pisos' é falso pela métrica oficial — b3 IGD+ 0,142 bate os 4 pisos (melhor 0,170) e o HV 0,443 bate 0,377; DTLZ2 é célula MEDIANA do config, não fraca. E o 'switch preso 44/48' é, medido, 34/48 (70,8%).","O código não tem a guarda '1º update sempre APD' do §III-C: 5/25 células iniciam o ciclo 1 no ramo INCERTEZA (DTLZ1 Flag=+5, DTLZ3 +32, MMF16_20 +41, WFG5 +7, ZDT1 +10) — é o desvio paper×implementação de maior consequência comportamental do config.","6 células terminam com cobertura 2σ <0,60 porque o σ encolhe rápido demais onde o modelo é quase exato (DTLZ7/ZDT1/ZDT3 com WAPE final 0,000–0,004 e correlação 0,999–1,000) — sub-confiança, o oposto do b1.","θ NUNCA é logado no b3 (o `modelo_hp` traz regr/corr/bounds/n/tempo): bounds [1e-5,100], θ0=5 e a trajetória do warm-θ são invisíveis nos dados — diferentemente do b1, aqui nem o θ final existe.","b3 é o config GP mais barato do main (2,56 h-core nas 25 células, contra 6,11 h do b1 nas 23) — o payoff direto do |A1| fixo."],"itens_torre_central":[{"item":"Fechar o bug de índice do ramo (a) do `UpdataArchive` (vetores→soluções), com alcance quantificado em 4/25 células (MMF1, MMF4, MMF11_L, DTLZ1); enquanto não fechar, essas 4 carregam incerteza residual sobre a gestão do A1","onde":"UpdataArchive.m:37-61 (b3/K-RVEA, PlatEMO 4.15); re-run de MMF1","severidade":"media","custo":"1 leitura de código + 1 re-run instrumentado de MMF1 (8 ciclos, ~15 s)"},{"item":"A coluna `bloco` do `sonda_f52e.csv` é STRING — ordenar sem `astype(int)` inverte o sinal do ΔWAPE (+4,4% vira −5,7%). Armadilha TRANSVERSAL, vale para os 24 configs","onde":"f5/sonda_f52e.csv (consumo) / queries da F5.3b e F5.5","severidade":"alta","custo":"1 linha por query, 0 h-core"},{"item":"Congelar no protocolo que a identidade de σ do b3 exige tolerância RELATIVA (em BBOB_F17 σ chega a 3,5e6 e o erro absoluto de armazenamento float32 sobe a 0,44): o critério absoluto reprova 5 células fiéis — espelho exato da regra do b1, que precisa da ABSOLUTA","onde":"PROTOCOLO_ANALISE_FIDELIDADE.md §5 / queries de identidade da família BO","severidade":"media","custo":"edição de protocolo, 0 h-core"},{"item":"Avaliar logar θ no `modelo_hp` do b3 (hoje ausente) antes das 30 sementes — sem ele, bounds [1e-5,100], θ0=5 e a curva do warm-θ ficam permanentemente no teto T","onde":"instrumentação b3 (modelo_hp) — bump de repo_hash, tem de viajar no lote F5.7","severidade":"baixa","custo":"1 campo + suíte (394 testes); bump de repo_hash ⇒ só no mesmo lote de refinamento"},{"item":"Manter os sentinelas `n_vetores_vazios` e `nzero_updata` (0 em 1.443/1.443) para as 30 sementes: sua utilidade é como sentinela de modo de falha latente, não como sinal desta rodada","onde":"instrumentação DI-10/L.2 do b3","severidade":"baixa","custo":"0 (já instalados)"}],"celulas_para_rerun":["main/b3/MMF1 — re-run instrumentado (8 ciclos, ~15 s) mais 1 leitura de `UpdataArchive.m:37-61` para fechar em definitivo o bug de índice do ramo (a) antes das 30 sementes"],"teto_T":["U5 — não-perturbação da sonda: exige run pareado com a sonda desligada.","F6 — valores de θ e trajetória da MLE: o `modelo_hp` do b3 loga regr/corr/theta_bounds/n/tempo mas NUNCA θ; bounds [1e-5,100], θ0=5 e o warm-θ só têm evidência indireta (degrau de custo de 1,9–2,2× no 1º fit).","B13 — bug de índice do ramo (a) do `UpdataArchive`: sem rastro nos dados (|A1| permanece 11D−1, `nzero_updata`=0, nada no ⑥ distingue os dois ramos); alcance quantificado em 4/25 células (D≤9).","B14 — miolo do RVEA interno: θ_APD do infill = (21/20)^α, SBX ηc=20 pc=1, PM ηm=20 pm=1/D, k-means++ (`KrigingSelect:19`, `UpdataArchive:37/:50`) e `randi` por cluster (`:41/:54`) — a ③ traz o resultado das 20 gerações, não os operadores.","A ordem lexicográfica do `unique` no reinício da população (B3.5), que define o pareamento posicional do `OperatorGA`.","As ~28.860 populações intermediárias existem na ③, mas os operadores que as geraram, não; e a contabilidade de RNG é invisível."],"capitulo_sintese":"O b3 executa o K-RVEA de Chugh et al. (TEVC 2018) sobre PlatEMO 4.15: DoE LHS de NI=11n−1, dois arquivos (A1 de treino com tamanho FIXO NI, A2 de não-dominados), um Kriging DACE por objetivo, e um ciclo de gestão de modelo que roda wmax=20 gerações do RVEA sobre o surrogate, escolhe u=5 indivíduos para reavaliação real por clusters — com critério de mínimo APD (convergência) ou máxima incerteza (diversidade) conforme o diagnóstico ΔVf ≶ δ — e repõe |A1|=NI descartando por clustering. As 25 células, sem uma única exclusão, somam 1.443 ciclos, 7.215 seleções, 10.856 linhas de ①, 3,78 M linhas de ③ e 761 blocos de sonda, a 2,56 h-core, todas na vm3. A query-joia prova o mecanismo duas vezes: a cadeia `index`→③→σ do selecionado fecha 7.215/7.215 a erro relativo máximo 5,92e-8 (o ULP do float32 do export) e a identidade `criterio_meanMSE ≡ média_j(σ_j²)` fecha 7.215/7.215 a 4,24e-16 — provando que o critério é a média das VARIÂNCIAS, sem raiz, contra a 'average of the standard deviations' impressa no artigo, um fator de ~20×, o que converte a DI-18 de decisão documental em fato medido. Que o switch de fato TROCA o critério, e não só o rótulo, está na separação de percentis do meanMSE do selecionado — 0,916 no ramo incerteza (n=1.715) contra 0,355 no ramo APD (n=5.500) — e na inversão espelhada do APD nas 7 células que exercitam os dois ramos. Os dois desvios paper×código de maior consequência também só a escala poderia demonstrar: o OPERANDO do switch é arquivo real × população predita (NumV1 ≠ NumV2 do ciclo anterior em 1.334/1.418 transições, 94,1%), o que TRAVA o ramo — 16/25 células rodam 100% APD, 2/25 rodam 100% incerteza (DTLZ3 48/48, MMF16_20 80/80) e só 7/25 alternam; e a ausência da guarda '1º update sempre APD' faz 5/25 células iniciarem no ramo de incerteza. A âncora empírica do próprio artigo (Fig. 9, tempo de treino constante) confirma-se: `n_treino` ≡ 11D−1 em 1.443/1.443 e o drift do tempo de fit é NEGATIVO em 21/25 células. Zero aspectos classe (3), 27/27 verificáveis fechando (29,6% classe 1 / 70,4% classe 2), calibração estável (0,932→0,924, contra 0,94→0,43 do b1, sustentada pelo |A1| fixo), IGD+ monotônico em 475/475 transições, 18/25 batendo o melhor piso e rank 6,36/17. Veredito 9,6 · ACEITAR — o melhor score do grupo e um dos poucos configs em que a fase inteira fecha sem exclusão; o que impede o 10 é o teto T de 4 itens, com o bug de índice do ramo (a) do `UpdataArchive` DECLARADO, invisível nos dados e alcançável em 4/25 células, além da cauda de sub-cobertura em 6 células e da leitura de 1 semente."},{"config":"b4","familia":"classificador","stack":"MATLAB","n_celulas":25,"exclusoes":"","score":9.0,"recomendacao":"aceitar+caveat","pct_classe1":36.0,"pct_classe2":60.0,"pct_classe3":4.0,"denominador":25,"n_teto_T":4,"P1_protocolo":"✅ — b4 está dentro dos 13 configs MATLAB com ZERO vermelhos na F5.1 (citado, não re-verificado). Re-provado por conta própria em escala: `len(①)==31D−1 ∧ fe_final==maxfe ∧ fe_index denso` 25/25 (61 a 929), `solution_id` denso 0-based 25/25; DoE `fase=='init'`==11D−1 em 25/25 com ΔX artefato×① BIT-A-BIT (max|ΔX| = 0,0) em 25/25, `doe_hash` ≡ sidecar 25/25 e `cp_init=true` 25/25 — só o patch `CSEA.m:29` (que remove o cap nativo `min(11D−1,109)`) permite D=30 ter 329 pontos e não 109; camadas e cadência: 3.166/3.166 blocos com 2.000 linhas exatas e join posicional bit-a-bit, 0 blocos faltando em 25/25.","P2_contrato":"✅ — `integridade_f52a.csv`: 0 linhas b4 (25/25 íntegras); `contrato_f52b.csv`: 0 linhas b4 (contrato estrutural 100% conforme). Verificado em escala: o ⑤ TEM a chave `params` (9 campos: N, K_refs, gmax, rede, treinador, operadores, treino, gate, exec_env) em 25/25 com 1 único valor distinto por campo — b4 NÃO está entre os 7 configs da não-conformidade §5; `sigma_dict` presente, íntegro e idêntico em 25/25. Mapa de término: `manifest.status='ok'` E `footer.termino='normal'` E `fe_final==maxfe` em 25/25 (bug B1 não aplicável). Única observação: o header do ⑥ não traz `run_id`/`ambiente`/`sigma_dict` (§6 do CONTRATO) — doc-sync já escalado, mesmo padrão do c217, SEM perda analítica.","P3_mecanismo":"⚠ — 29 aspectos, T=4, denominador 25: classe (1) 9 = 36,0% · classe (2) 15 = 60,0% · classe (3) 1 = 4,0% (A12, a nomenclatura `p0`/`p1` do ⑥ contra a classe medida). Com o veredito REFUTADO da F5.4, A12 se funde a A11 como classe (1) e a tabela passa a (1) 10 = 40,0% · (2) 15 = 60,0% · (3) 0 = 0,0% — b4 fica SEM nenhum item classe (3), e o §7 recomenda à torre reavaliar 9,0 → 9,5.","P4_saude":"⚠ — o b4 não tem cabeça de valor (μ/σ/pred_score 100% NULL em 6.339.026 linhas), logo WAPE e erro de fantasia são N/A por desenho e o `sonda_f52e.csv` tem 0 linhas b4 (correto, não buraco). A régua é AUC contra o rótulo por construção: mediana global 0,6411 em 2.758 blocos computáveis, AUC>0,5 em 90,6% dos blocos e >0,7 em 39,7%; por célula, 17/25 com mediana >0,6 e 13/25 >0,7 (BBOB_F22 0,928 · BBOB_F1 0,900 · WFG1 0,877), só 6/25 em ≈0,5. Tendência 1º→4º quartil de blocos POSITIVA em 14/25 (DTLZ3 +0,260, ZDT1 +0,197, BBOB_F55 +0,181) — o prior 'não discrimina' está refutado e ampliado. O L mediano da sonda cai de 0,127 para 0,037: a rede aprende a REJEITAR o espaço aleatório conforme o arquivo converge, comportamento correto e não colapso. Trajetórias: IGD+ monotônico não-crescente em 475/475 transições, 0 violações. Custo: 6,075 h-core com 90,96% do wall-clock em `fit` (rede nova por geração).","P5_canonica":"✅ — nenhuma célula é numericamente comparável (Botão 4). A única interseção de SETUP de todo o config é ZDT1 D=30 M=2 com 929 FEs contra os 900 do paper (1,03×, mesma família, mesma dimensão, mesmo M) — mas o paper não publica número, só a Fig. 10, então nenhuma comparação numérica é possível. O que a norma permite comparar é a ORDEM intra-família DTLZ, e ela reproduz o artigo por inteiro: paper 1,89e-1 / 3,89e-1 / 1,39e+0 / 4,36e+1 / 1,09e+2 contra nosso IGD+ 0,2208 / 0,2799 / 1,5632 / 81,21 / 278,83 em DTLZ2/DTLZ4/DTLZ7/DTLZ1/DTLZ3 — 5/5 POSIÇÕES IDÊNTICAS, com ordem de grandeza compatível (DTLZ2 0,22 vs 0,19; DTLZ7 1,56 vs 1,39). O modo de falha DECLARADO confirma-se (DTLZ7, front descontínuo, é o nosso pior DTLZ em rank, 14/17); a alegada força em DTLZ1/DTLZ3 não se reproduz sob o nosso orçamento, mas o próprio paper reporta 43,6 e 109 nessas instâncias, i.e. também não convergidas.","P6_vs_pisos":"⚠ — bate o melhor piso em 6/25 (BBOB_F49 0,79× · BBOB_F17 0,82× · BBOB_F55 0,87× · ZDT3 0,93× · WFG1 0,99× · DTLZ4 0,99×); mais 15/25 acima porém DENTRO do piso de ruído entre máquinas (IGD+ ≤58,98%, O-18); e 4/25 claramente abaixo (BBOB_F22 2,48× · DTLZ3 2,15× · MMF11_L 1,90× · DTLZ1 1,79×). Rank médio 9,84/17 no main. Melhores posições absolutas: BBOB_F17 1º, WFG1 1º, WFG4 3º, DTLZ4 4º, ZDT3 6º, WFG2 7º. Roster homogêneo: 25/25 na vm3, então o piso de ruído só entra na comparação com pisos de outras máquinas (já aplicado).","P7_adversarial":"✅ — o único item classe (3), A12, foi à F5.4 e voltou REFUTADO. O mapeamento empírico que o analista mediu está CERTO (`p0` = MAE da categoria II / rótulo 1; `p1` = MAE da categoria I / rótulo 0) e foi reproduzido e ampliado em 4 rotas independentes, mas o ACHADO ('campos invertidos' = desvio de fidelidade não explicado) é falso: `p0`/`p1` não são índices de classe, são nomes de variável NATIVOS do PlatEMO (`CSEA.m:78–79`, bloco byte-idêntico ao import pristino, md5 `fedbabcd0385cdc11351c6b2e38c1485`, intocado pelos nossos 2 commits de patch), repassados posicionalmente e sem troca ao jsonl, e a adoção desses nomes é decisão RATIFICADA (DI-18.2:857 + ⟦v5.2.1⟧, que abandonou os nomes do paper p1/p2 e adotou os do código/log). Prova que mata o verbo 'invertido': reconstruindo o ramo literalmente de `SurrogateAssistedSelection.m:27–57` com `p0`/`p1` COMO LOGADOS fecha 6.268/6.268; com os dois valores TROCADOS cai a 1.252/6.268 (19,97%). Resíduo: (d) defeito de DOC, severidade mínima. Zero células excluídas, zero números publicados mudam.","query_joia":"A regra de RÓTULO do CSEA (o que o classificador de fato aprende): categoria II ⟺ `∀j ∃k: f_k ≤ R_jk` sobre as K=6 referências — AND sobre refs, OR sobre objetivos, o oposto lógico da Alg. 4 impressa (que exige DOMINÂNCIA). Reconstruindo o arquivo MATLAB e recomputando `rr` das 6.268 gerações contra 5 regras candidatas, a regra DI-18/código bate 5.192/6.268 no bruto e 3.589/3.589 (100,000%) nas gerações decididas, enquanto a regra do paper bate 0/6.268. Irmã: o gate de confiabilidade `p0<0,4 ∨ (p1<tr ∧ p0<1−tr)` separa ramo 1 de ramo 4 em 6.268/6.268 (100,000%), isolando o `0,4` hardcoded.","prova_numerica_central":"Sanduíche float32 `n1_lo ≤ n1_log ≤ n1_hi` em 6.268/6.268 gerações (100,00%) e regra DI-18 exata em 3.589/3.589 gerações DECIDIDAS (100,000%, presentes em 24/25 células), com a regra impressa do paper refutada em 0/6.268; e o gate separando os ramos em 6.268/6.268 (100,000%) contra 87,33% da regra pura do artigo. Complemento: a partição 75/25 fecha com identidade exata `⌈¾|D0|⌉+⌈¾|D1|⌉` em 6.268/6.268 e o gate L>0,9 em 1.811/1.811 infills (mínimo global 0,9000033).","achados_classe3":[{"id":"A12","titulo":"Nomenclatura `p0`/`p1` do ⑥ invertida em relação à classe que medem (`p0` carrega o MAE da categoria II e `p1` o da categoria I, provado 6.268/6.268 pela predição fechada `p0_NaN ⟺ ⌊¼·|D1|⌋ == 0`)","veredito_f54":"REFUTADO"}],"caveats_d97":["CAVEAT DE REGIME, NÃO DE FIDELIDADE — o número 'b4 usou o surrogate' deve ser lido como 25,8%, não 100%: dos 7.026 FEs, apenas 1.811 (25,8%) foram gateados pelo classificador e 5.215 (74,2%) sorteados no ramo 4; o ramo 4 responde por 83,2% das gerações e em 8/25 células (as 7 BBOB de D=10 menos F22, mais DTLZ1 e ZDT4) o classificador NUNCA gateou um único FE.","Quando gateou, porém, acertou: 84,1% dos FEs de ramo 1 caem de fato na categoria 'boa' contra 47,2% do ramo aleatório — +37 pontos percentuais sobre o acaso, em escala. É o resultado mais forte do config para o eixo 'papel da incerteza'.","O `0,4` hardcoded do gate substituiu um limiar ADAPTATIVO por um absoluto e é ESTRUTURALMENTE mais permissivo (como `tr ≤ 0,25` sempre, nunca é mais restritivo): abre o ramo confiável 1.053 vezes contra 259 sob a regra do paper (4,07×), e 1.405 dos 1.811 FEs de ramo 1 (77,6%) existem só por causa dele.","54,8% das gerações de ramo 1 são ESTÉREIS (577 stalls 0-FE, 100% em ramo 1): o gate abre, gera 12 candidatos e nenhum chega a L>0,9 — 418 desses 577 ocorrem em gerações que só abriram por causa do 0,4. Ler o stall como 'R2/descartar tudo' inverte a causa.","Os ramos 2 e 3 NUNCA disparam (0/6.268) e o R3 é MECANICAMENTE inalcançável: exige `p_catI > 1−tr`, e `p_catI` tem máximo 0,756 contra `1−tr ≥ 0,75` — a janela é uma lasca.","A rede é reconstruída do zero 6.268 vezes (adam/100 épocas/Glorot por geração, contra LM/T=500 do paper): 90,96% do wall-clock mediano está em `tempo_fit_s` e a projeção M8/M9 do b4 é dominada pelo retreino, não pela busca.","A projeção radial da Alg. 2 DEGENERA em M=2 (W₂ = (−1; 1,22e−16) ⇒ espaço radial 1-D, grade de 2 células), e 17 das 25 células são M=2 — isso enfraquece estruturalmente o componente de diversidade do config (flag §15.1), mas não é verificável (nem Y nem G são exportados).","F5.4 recomenda à torre reavaliar o score 9,0 → 9,5, porque o item que separava o config de 9,5 (o único classe (3)) foi refutado e b4 fica com (1) 40,0% / (2) 60,0% / (3) 0,0%.","DTLZ4 é o caso extremo de underflow float32: `x^100` faz 80,3% das 371 linhas terem ao menos um objetivo exatamente 0,0 (menor não-zero = 1,4e-45, o denormal mínimo) ⇒ 0 gerações 'decididas', embora o sanduíche feche 338/338. Comparar `rr` com igualdade ingênua reprova 7 células falsamente."],"itens_torre_central":[{"item":"CONTRA-RECOMENDAÇÃO URGENTE da F5.4: NUNCA trocar os valores de `p0`/`p1`. Inverter os campos faz o log DEIXAR de reproduzir o gate executado em 5.016/6.268 gerações (cai de 6.268/6.268 para 1.252/6.268) e desincroniza a string `motivo`, que imprime 'p0=…<0.4'","onde":"src/b4_instrument.m:98 / qualquer pós-processamento de p0/p1","severidade":"alta","custo":"0 — é uma proibição, não uma ação"},{"item":"Ação DOC-ONLY recomendada (risco zero, sem re-run): glosar que `p0` = MAE da categoria II (rótulo 1, `CSEA.m:78`) e `p1` = MAE da categoria I (rótulo 0, `CSEA.m:79`), nomes nativos do PlatEMO e NÃO índices de classe","onde":"src/experiment.m:1206 (sigma_dict.cobertura_3) · CONTRATO_DE_DADOS.md:297 (§5, linha b4) · SPEC_experimentos_v5.2.md:803 e :1875 + regerar bundles por gen_bundles.py (nunca editar bundle à mão)","severidade":"media","custo":"~20 min, 0 h-core, 0 re-execução — a rodada-42 NÃO precisa ser refeita"},{"item":"OPCIONAL (só se a torre quiser blindar as 30 sementes): ACRESCENTAR os apelidos `'p_catII', p0, 'p_catI', p1` AO LADO de `p0`/`p1` — nunca no lugar. É logging puro, pós-decisão, consome zero RNG ⇒ trajetórias bit-idênticas","onde":"src/b4_instrument.m:98","severidade":"baixa","custo":"1 linha + suíte (394 testes) ≈ 30 min, mas BUMP de repo_hash ⇒ tem de viajar no mesmo lote de refinamento F5.7, antes do disparo M8/M9, nunca como patch avulso sobre a árvore congelada"},{"item":"Corrigir a prosa do relatório b4 nas linhas 201 (a premissa 'leitura natural'), 207 ('nenhuma decisão cobre' → DI-18.2:857 cobre), 210 (recomendação) e 220 (armadilha #1 deve ser reescrita como convenção de nomes herdada do PlatEMO, não inversão)","onde":"f5/relatorios_config/b4.md","severidade":"baixa","custo":"4 edições de prosa"},{"item":"Reavaliar o score do b4 de 9,0 para 9,5, já que o único item classe (3) foi refutado e o §7 item 2 dissolve","onde":"f5/scores_f53.csv e MATRIZ-MESTRA do relatório final","severidade":"media","custo":"atualização de 1 linha"}],"celulas_para_rerun":[],"teto_T":["A5 — critério `Con`/`Crowd` do `RadialSelection` (Alg. 1/2) e a COMPOSIÇÃO de P: a ② é o ARQUIVO e P (|P|=N=50) nunca é exportada; só `|PR|=6` e `refs ⊆ arquivo` (6.268/6.268) são verificáveis.","A15 — ramos 2/3 do gate: 0/6.268 disparos, com razão mecânica MEDIDA (R3 exige `p_catI > 1−tr` e `p_catI` tem máximo 0,756 contra `1−tr ≥ 0,75`).","A28 — projeção radial degenerada em M=2 (W₂ ≈ 0 ⇒ espaço radial 1-D, grade ⌊√6⌋=2), em 17/25 células: nem `Y` nem `G` são exportados.","A29 — `guard_randperm` (bug latente `SAS.m:30`, dispara se D > |Pop|+6): 0/6.268 disparos, coerente com D≤30 < 56 — latente, não exercitado.","Laço interno do `SASelection` (gmax=3000 = 250 × `OperatorGA(12)`) e `div = ⌈√6⌉ = 3` subgrupos: só o teto de lote 12 é observável.","η SBX/PM por geração (o Balde C {1,20,1,20} é declarativo no header; a grandeza não é exportada) e fitness/pesos internos da FNN (Glorot, z-score reajustado por geração).","Texto dos patches `CSEA.m:29/30/31/36/47` (o comportamento está provado; o diff exige `anchors.json`+`repos.lock`) e a não-perturbação da sonda sobre o RNG."],"capitulo_sintese":"O b4 executa o CSEA de Pan et al. (TEVC 2019), o exemplar-raiz da família classification/relation-based: em vez de aproximar objetivos, um ÚNICO classificador FNN prevê a relação candidato-vs-referências. Por geração o algoritmo escolhe K=6 referências por seleção radial, rotula TODO o arquivo em categoria I/II, calcula `rr` e `tr = 0,5·min{rr,1−rr}`, particiona 75/25 estratificado por classe, treina a rede do zero, mede os MAE por classe na validação — a incerteza do config — e usa a reliability configuration para decidir se confia no modelo. As 25 células somam 6.268 gerações, 7.026 infills, 3.166 blocos de sonda e 6.339.026 linhas de ③, a 6,075 h-core com 90,96% do tempo em fit. As duas peças que DEFINEM o CSEA foram provadas por dado, não por leitura de código: a regra de rótulo é `∀j ∃k: f_k ≤ R_jk` (AND sobre refs, OR sobre objetivos — 3.589/3.589 gerações decididas, sanduíche float32 100% em 6.268/6.268), com a regra impressa na Alg. 4 do paper (dominância estrita) refutada em 0/6.268; e o gate `p0<0,4 ∨ (p1<tr ∧ p0<1−tr)` separa os ramos em 6.268/6.268 (100,000%), isolando e quantificando o `0,4` hardcoded, que é literalmente a R1 do artigo com o primeiro `tr` trocado por um limiar absoluto — a regra pura do paper acerta só 87,33%, e o 0,4 abre o ramo confiável 4,07× mais, produzindo 1.405 dos 1.811 FEs gateados. A partição fecha com identidade exata `⌈¾|D0|⌉+⌈¾|D1|⌉` em 6.268/6.268 e o gate L>0,9 em 1.811/1.811 (mínimo 0,9000033). O único item classe (3), a suposta inversão de `p0`/`p1` (A12), foi à F5.4 e voltou REFUTADO: são nomes de variável nativos do PlatEMO (`CSEA.m:78–79`, byte-idêntico ao upstream, jamais tocado pelos nossos patches), transportados posicionalmente, com adoção ratificada em DI-18.2 — e a reconstrução literal do ramo fecha 6.268/6.268 com os valores COMO LOGADOS e cai a 1.252/6.268 se trocados, de modo que inverter os campos seria ativamente destrutivo; resta um defeito (d) de DOC de severidade mínima, e o config fica com (1) 40,0% / (2) 60,0% / (3) 0,0%. O sentido canônico reproduz o paper 5/5 nas posições intra-família DTLZ, incluindo o modo de falha declarado (DTLZ7 é o pior DTLZ em rank, 14/17), e o contrato é impecável (0 linhas em integridade e em contrato, `params` completo em 25/25). O caveat que sustenta o 9,0 é de REGIME e não de fidelidade: 74,2% dos FEs vieram do ramo aleatório e 8/25 células nunca gatearam um FE, com 54,8% das gerações de ramo 1 estéreis — mas quando o gate confiou, 84,1% dos FEs caíram de fato na categoria boa contra 47,2% do sorteio, +37 pontos percentuais de acerto sobre o acaso. Veredito 9,0 · ACEITAR + CAVEAT, com recomendação explícita da F5.4 de reavaliar para 9,5."},{"config":"c141","familia":"NN-ensemble","stack":"MATLAB","n_celulas":25,"exclusoes":"","score":9.5,"recomendacao":"aceitar","pct_classe1":33.3,"pct_classe2":66.7,"pct_classe3":0.0,"denominador":24,"n_teto_T":2,"P1_protocolo":"✅ — 25/25 células verdes nos gates F5.1 (citado, não refeito) e re-verificado por conta própria: `maxfe == fe_final == len(①) == 31D−1` em 25/25 com `fe_index` e `solution_id` densos 0-based e `footer.termino='normal'`/`status='ok'` 25/25; DoE `fase=='init'`==11D−1 em 25/25 com ΔX BIT-EXATO em float32 contra o artefato (em float64 o desvio é puro ULP-float32 escalado pelos bounds: 2,97e-8 em [0,1], 2,37e-7 no BBOB, 1,90e-6 no WFG), `doe_hash` ≡ sidecar 25/25, `cp_init=true` 25/25; camadas e cadência: 1.024 blocos × 2.000 linhas exatas, `n_falhas=0` em 25/25, regra `g==1 ∨ g%2==0` + finalProbe fechando 25/25 quando lida como 'última geração INICIADA', com `x_hash` ≡ `x_hash_online` em 25/25.","P2_contrato":"✅ — `contrato_f52b.csv`: ZERO linhas para c141 (contagem da ①, fase init, schema de ①③④, ② na família sancionada, blocos de sonda 2.000×k, `fe_final==maxfe`, `sigma_dict` presente); c141 NÃO está entre os 7 configs da não-conformidade transversal do ⑤ sem `params` (verificado: `manifest.params = {N_subpop, wmax, kernel, ds_dsmerge}` presente em 25/25). `integridade_f52a.csv`: ZERO linhas (nenhum parquet ilegível, nenhum manifesto quebrado, nenhum ⑥ truncado, footer em 25/25). Dois achados que um leitor desavisado levantaria como defeito e são POR DESENHO com regra citável: `n_geracoes = |eventos c141_gen| + 1` em 25/25 (a última geração é iniciada, grava sonda, e é cortada pelo hard-stop antes do log — D21/D61) e `sigma_2` 100% NaN nas 6 células M=3 e ausente nas 19 M=2 (D45 usa só 2 dos M slots de σ).","P3_mecanismo":"✅ — 26 aspectos, T=2, denominador 24: classe (1) 8 = 33,3% (U2, U3, F1, F4, F5, F6, F9, C2) · classe (2) 16 = 66,7% · classe (3) 0 = 0,0%. Todo desvio tem decisão sancionadora citável e assinatura MEDIDA conforme o declarado (D20/D21/D45/D61/D63/D89/D94, DEF-A5/L4, DI-10/DI-13.10/DI-18, B12.6/B12.7), incluindo os três que a letra do paper e o agente-4 contradizem — u=5 REVOGADO, arquivo VERDADEIRO no nível 1 e N=100 por subpopulação — todos resolvidos a favor do bundle POR DADO. Marca declarativa (Botão 3): U2, C2 e F1 contam como (1) com eco declarativo, mas em F1 e U2 o eco é REDUNDANTE (a identificação discriminativa do kernel e o ΔX bit-exato provam o mesmo por dado).","P4_saude":"⚠ — a sonda é globalmente plana mas BIMODAL: ΔWAPE mediano −0,5% (reproduzindo o −0% da régua oficial), com 11 células melhorando (BBOB_F1 −13,6%, BBOB_F17 −11,9%, MMF4 −11,1%, DTLZ2 −10,6%) e 7 piorando forte (MMF1 +611%, ZDT6 +590%, MMF11_L +349%, DTLZ1 +277%, DTLZ4 +225%, WFG1 +223%, ZDT1 +139%) — a causa é mecânica e provada: o RBF por `mldivide` sem regularização INTERPOLA dentro do suporte e EXPLODE fora dele. A cobertura 2σ é N/A POR DESENHO (σ_0 é U de ranks, inteiro em [0, 2(2N−1)]; σ_1 é std entre 3 alvos de escalas incomensuráveis — nenhum é desvio preditivo por objetivo; `sonda_f52e.csv` traz `cobertura95` vazia). Quatro regimes nítidos na régua própria (1.024 blocos × 2.000 × M = 62.464 medições): (A) o RBF DOMINA em ZDT1 (spearman 0,996), DTLZ7 (0,999), BBOB_F1 (0,993), DTLZ2 (0,970), MMF16_20 (0,949); (B) é CEGO em toda a família WFG (spearman 0,076–0,308, MAE/MAE_ref ≈1,0 — não melhor que prever a média); (C) DIVERGE numericamente em MMF1/ZDT6/DTLZ4/DTLZ1; (D) ruído de ranking em BBOB_F55/F49. Trajetórias impecáveis: 0 violações de monotonicidade do IGD+ e 0 do HV em 475 transições; ganho de 2,07× a 405×, mediana 16,6×.","P5_canonica":"✅ — comparável 0 · âncora direcional 8/8 com sentido CONFIRMADO (4 forças declaradas + 4 fraquezas declaradas) · incomensuráveis 17. Confirmam as FORÇAS: DTLZ2 (rank 2/16, razão 0,240 vs smsemoa, HV 0,710×0,377), DTLZ7 (rank 1/15, razão 0,031 = 32× melhor, HV 0,509 contra 0,000), ZDT1 (razão 0,096 = 10×), ZDT6 (razão 0,016 = 62×). Confirmam as FRAQUEZAS que o próprio paper declara: DTLZ3 (razão 1,75), DTLZ4 (rank 10/15, razão 1,57), ZDT4 (rank 3/17 mas HV=0 e |ND|=1 — convergiu para um ÚNICO ponto) e ZDT3 (a 'exceção declarada', razão 0,646, ainda batendo o piso). Achado adicional forte: a CONFISSÃO do paper sobre as WFG ('RBFs are not suitable for approximating the objective functions of the WFG problems') está CONFIRMADA POR DADO como diagnóstico (spearman mediano 0,076 em WFG5 a 0,308 em WFG2; MAE/MAE_ref ≈1,03–1,06 em WFG4/WFG5). Âncora J com caveat metrológico forte: DTLZ2 IGD 6,839e-2 contra 1,2010e-1 do paper e ZDT1 4,582e-2 caindo entre o d=20 (3,3486e-2) e o d=40 (4,5148e-2) — ordem de grandeza compatível, mas NÃO é teste de âncora ±3σ (métricas em espaços diferentes, 1 semente contra 20). Botão 4 respeitado: nenhum 'artigo melhor' declarado.","P6_vs_pisos":"✅ — bate o melhor dos 4 pisos em 18/25 (denominador 25), com SEIS primeiros lugares absolutos (BBOB_F22, BBOB_F37, BBOB_F49, BBOB_F55, DTLZ7, WFG9) e rank IGD+ médio 4,84 — o 3º melhor config de todo o main, reproduzindo exatamente a prévia da F5.2c. Leitura LIMPA de máquina: c141 e os 4 pisos rodaram TODOS na vm3, então o piso de ruído entre máquinas (O-18) NÃO contamina esta comparação (aplica-se só às comparações contra c262/c122 e c238). Vitórias mais fortes: ZDT6 0,016 · BBOB_F55 0,022 · BBOB_F1 0,030 · DTLZ7 0,031 · BBOB_F5 0,073 · ZDT1 0,096 · BBOB_F22 0,216 · DTLZ2 0,240. Derrotas: DTLZ1 1,927 · DTLZ3 1,748 · WFG4 1,721 · DTLZ4 1,566 · WFG2 1,381 · MMF4 1,098 · WFG1 1,090.","P7_adversarial":"N/A — ZERO aspectos classe (3); nada segue à verificação adversarial F5.4 como desvio inexplicado. Todos os não-fechamentos brutos (318 no nível 1, 380 no nível 2, 261 no lote, 196 no `U_pool_max`) foram resolvidos DENTRO da análise com causa numérica demonstrada — quantização float32 do export (D53) criando empates de dominação e inversões de rank — e viraram armadilhas de protocolo. Uma única confirmação OPCIONAL foi sugerida (explicitamente NÃO é classe 3): `main/c141/WFG5` geração 76, o único ciclo dos 1.967 em que o nível 1 reconstruído (101) fica abaixo do logado (103) SEM nenhum empate exato que explique a diferença; hipótese mais provável é um par separado por <1 ULP-float32 em UM objetivo. Prioridade BAIXA: 0,05% dos ciclos, sem efeito sobre nenhum indicador.","query_joia":"Duas identidades BIT-EXATAS e discriminativas provam o mecanismo. (i) A INTERPOLAÇÃO: nas 9.084 linhas de ③-online cujo ponto pertence ao treino do próprio ciclo, `μ − f_real = 0,0` EXATAMENTE (mediana E máximo) em 25/25 células — impossível sob qualquer ridge, logo é prova de que o `mldivide` é DENSO e SEM regularização. (ii) O TETO de σ_0: com pool de 2N e ranks 1..2N, o U de ranks tem teto 2(2N−1), e o máximo observado por célula é EXATAMENTE esse teto em 25/25 (398 nas 21 células com N=100, 302 no DTLZ1 com N=76, 82 nas 3 células D=2 com N=21) — prova que a incerteza D45 é o U calculado sobre o POOL INTEIRO, não sobre o subconjunto do nível 2.","prova_numerica_central":"μ − f_real = 0,0 EXATO (bit-a-bit, mesmo em float32) em 9.084 linhas de ③-online, com máximo 0,0 em 25/25 células — a prova de ausência de regularização no `mldivide`, que convive com rcond desabando a 4,48e-25 (275 de 1.967 ciclos abaixo de 1e-12; ZDT1 com 156 dos 173). Complementos discriminativos: o teto de σ_0 == 2(2N−1) EXATO em 25/25; o kernel MQ c=1/poly=0/sem-scaling vencendo 8 alternativas + cauda linear em 14/14 pontos-teste por 2×–10.000×; e σ_1 separando ddof=1 (erro relativo mediano 9,04e-4) de ddof=0 (travado em 0,1836 = 1−1/√1,5) por 200×.","achados_classe3":[],"caveats_d97":["u=5 NÃO EXISTE no código (DI-18/v2.2 revogou): 140 dos 1.967 ciclos (7,1%) têm lote > 5, com máximo 11 — sob u=5 nenhum lote poderia passar de 6. O gatilho real é 'resta >1 candidato', provado pelas 18 saídas de nível 2, TODAS com `n_front2 == 1`. Procurar u=5 reprova um run fiel.","O nível 1 mistura pool PREDITO com arquivo VERDADEIRO, contra a letra do paper (que manda predito nos dois): teste discriminativo 1.649/1.967 (83,8%) contra 1.575 (80,1%), com 6 células fechando em 100% (246 ciclos consecutivos sem discrepância). O bundle vence o paper por dado.","A troca de índices Fit2↔Fit3 entre paper e código é REAL e é só de NOME: a escala prova a semântica (|fit3|/|fit1| = 1,414 ⇒ ambos SDE; |fit2|/|fit1| = 230,5 ⇒ front number). Validar o vetor de sinais contra a letra do artigo reprova um run fiel.","A incerteza é LOAD-BEARING e agora quantificada: o `argmax U` é ponto EXTRA (fora da frente ND de (Q,U)) em 1.727 dos 1.967 ciclos = 87,8%, o que equivale a 24,6% dos 7.030 FEs de infill gastos PURAMENTE por discordância do ensemble — e os pontos escolhidos são de fato distantes (dist_min_arquivo com máximos de 45,9 em WFG5).","A cobertura ±1,96σ é N/A NESTE CONFIG por desenho: σ_0 é inteiro (U de ranks) e σ_1 é std entre alvos de escalas incomensuráveis — σ_1 serve como DETECTOR DE EXTRAPOLAÇÃO, não como barra de erro. Ler `cobertura95` vazia como buraco de dado é erro.","O `rcond` do `mldivide` desaba POR DESENHO (mínimo 4,48e-25; ZDT1 com 156 dos 173 ciclos abaixo de 1e-12): o warning RCOND do MATLAB é comportamento FIEL, não sintoma, e convive com interpolação exata nos nós e extrapolação selvagem (μ ∈ [−120,9; 384,4] no MMF1 contra f ∈ [4e-4; 8,34]).","N = min(100, 11D−1) contra o N=50 do paper (D20/DEF-A5): o piso 11D−1 é ANTI-CRASH (`ES_PDR.m:20` estoura quando o pool da 1ª geração é menor que N, faixa real D≤4) e o efeito colateral é que em D=2 o pool de infill é 42 e não 200.","PRIOR 5,0 v2 (divergência numérica do MMF1) CONFIRMADO e AMPLIADO: μ ∈ [−120,91; 384,36] contra f ∈ [4,0e-4; 8,343], WAPE do objetivo 1 subindo 1,06 → 7,74 e MAE/MAE_ref chegando a 13,50 no pico — MAS o efeito na métrica final NÃO se materializou (IGD+ 0,0435 contra 0,0570 do melhor piso: c141 BATE os 4 pisos no MMF1, contrariando a leitura pessimista do prior).","ACHADO NOVO só visível na escala: DTLZ4 é o PIOR surrogate do config (spearman mediano 0,084, mínimo −0,073; MAE/MAE_ref mediano 6,70), pior até que as WFG que o próprio paper declara indomáveis pelo RBF — efeito de α=100 (x^100 vira quase função-degrau) sobre uma multiquadrática sem scaling.","A 'iteração 0-FE' prevista pelo bundle NÃO ocorreu: `lote ≥ 1` em 1.967/1.967. É estruturalmente improvável nesta cascata (|Pa| ≥ 1 e Pb sempre existe) — registrar como expectativa não realizada, não como falha."],"itens_torre_central":[{"item":"Congelar no protocolo que o float32 do export (D53) é o TETO REAL da reconstrução de cascatas ND, não a álgebra: 317 de 318 falhas do nível 1 (99,7%) têm |Δ| ≤ nº de membros do front com margem de dominação EXATAMENTE 0,0 — qualquer auditoria futura de cascata ND deve usar critério de EMPATE, não de igualdade","onde":"PROTOCOLO_ANALISE_FIDELIDADE.md §5 / queries de reconstrução ND da F5.3b","severidade":"media","custo":"edição de protocolo, 0 h-core"},{"item":"Documentar que `fit1/fit2/fit3` do ⑥ são estatísticas do conjunto do NÍVEL 2, não do nível 1 (só o `fit1.max` coincide nos dois conjuntos, porque o ponto de máximo Fit1 é sempre ND em [−Fit1,·,·]) — descoberto por dado, hoje não está em nenhum artefato","onde":"CONTRATO_DE_DADOS.md (linha c141) / sigma_dict do c141","severidade":"media","custo":"edição de doc, 0 h-core"},{"item":"Confirmação OPCIONAL do único ciclo sem causa provada (WFG5 g76: nível 1 reconstruído 101 contra 103 logado, sem nenhum empate exato) — hipótese: par separado por <1 ULP-float32 em um objetivo, o inverso do padrão dominante","onde":"main/c141/WFG5 ciclo 76; evidência em f5/baterias/c141/b03_nivel1_empates.csv linha 1322 e b03_mecanismo.csv","severidade":"baixa","custo":"1 re-run instrumentado de um único ciclo com dump em double; prioridade baixa (0,05% dos ciclos, sem efeito em indicador)"},{"item":"Registrar que a sonda online lê as 2.000 PRIMEIRAS linhas do gabarito de 20.000 (Sobol ANINHADO, DI-13.5): comparar com as 2.000 ÚLTIMAS dá max|ΔX| = 0,989 e um falso-alarme catastrófico — conferir sempre `x_hash_online`","onde":"protocolo de join da sonda (transversal aos configs)","severidade":"media","custo":"edição de protocolo, 0 h-core"}],"celulas_para_rerun":["main/c141/WFG5 ciclo 76 — re-run instrumentado com dump em DOUBLE (OPCIONAL, prioridade baixa): é o único ciclo dos 1.967 cujo desvio de contagem do nível 1 não tem causa provada; 0,05% dos ciclos, sem efeito sobre nenhum indicador"],"teto_T":["U11 — não-perturbação da sonda: exige run pareado com e sem sonda; o L.7 do bundle declara ZERO RNG em infill/RBF/dsmerge/ES_PDR, o que torna a perturbação implausível, mas isso é evidência de CÓDIGO, não de dado.","C5 — coeficientes `r1, r2` ESCALARES do CSO (1 valor por par em vez de vetores) e o passo de atualização do perdedor: sem rastro nos dados.","C5 — SBX proC=1,0/disC=20 e PM proM=1/d: só o eco do bundle; os operadores não são logados.","C5 — as 20 gerações internas do bi-pop e a seleção ambiental `ES_PDR`/crowding: só a população FINAL chega à ③.","C5 — o porte de 3 linhas (`MMRAEA.m:21/:50`, `EAOptimization.m:33`), o init STOCK byte-igual ao K-RVEA (S.2#18) e o 'zero toolbox': cobertos por `anchors.json`+`repos.lock`, nenhum por dado.","C5 — contabilidade de RNG (LHS + 20×[randperm + 2 rand/par + PM 2 rand] + 20×OperatorGA por ciclo) e a ausência de migração entre subpopulações.","A arbitragem de EMPATES dentro do `NDSort` do PlatEMO — é o que fecharia o resíduo de 16% do nível 1."],"capitulo_sintese":"O c141 executa o MMRAEA de Shen et al. (Complex & Intelligent Systems, 2025) — um SAEA de RBF para MOPs caros de alta dimensão. Por ciclo externo ele calcula o fitness SDE e o front number sobre o arquivo, treina TRÊS modos de RBF multiquadrática sobre alvos heterogêneos (ARBFs por objetivo, FitRBF sobre o escalar SDE, FNRBF sobre o front number), evolui uma BI-POPULAÇÃO paralela por wmax=20 gerações (P1 por CSO, P2 por GA, seleção ambiental NSGA-II em cada uma sobre objetivos preditos), funde num pool de 2N e aplica uma amostragem TRI-NÍVEL cuja última etapa é a frente ND de (Q,U) unida ao argmax de U. As 25 células somam 1.967 ciclos, 10.856 FEs reais, 386.382 linhas de ③-online, 1.024 blocos de sonda (2.048.000 linhas) e 68 cache-hits, todas na vm3 — a mesma máquina dos 4 pisos, o que torna a comparação com pisos limpa de ruído O-18. A cascata INTEIRA foi reconstruída dos dados e fecha: nível 1 em 1.649/1.967 (100% em 6 células, 246 ciclos consecutivos), Fit1/SDE a erro relativo mediano 1,22e-7 = eps-float32 em 99,9% dos ciclos válidos, Q e U exatos em 1.490, lote reconstruído em 1.706 (86,7%) e `U_pool_max` em 1.771. Duas identidades são BIT-EXATAS e discriminativas: a interpolação (μ − f = 0,0 em 9.084 linhas, 25/25) prova `mldivide` SEM regularização — impossível sob qualquer ridge — e o teto de σ_0 == 2(2N−1) EXATO em 25/25 prova que a incerteza D45 é o U de ranks do POOL COMPLETO; o kernel MQ c=1, poly=0 e sem scaling de X vence 8 alternativas mais a cauda linear em 14/14 pontos-teste por 2×–10.000×, e σ_1 discrimina ddof=1 de ddof=0 por 200×. Os três pontos em que a letra do paper e o agente-4 divergem do bundle foram resolvidos A FAVOR DO BUNDLE por dado: u=5 revogado (140 ciclos com lote>5, máximo 11), arquivo VERDADEIRO no nível 1 (1.649 contra 1.575 no teste discriminativo) e N=100 por subpopulação. O papel da incerteza está quantificado como o paper nunca fez: o argmax-U é ponto EXTRA em 1.727/1.967 ciclos (87,8%), i.e. 24,6% dos 7.030 FEs de infill são gastos puramente por discordância do ensemble, em pontos genuinamente distantes do arquivo. Zero classe (3) — o resíduo de contagem (318 ciclos) está explicado em 99,7% por empates de dominação que o float32 do export CRIA e o double do MATLAB não tem. Contrato e integridade impecáveis (0 linhas em ambos), trajetórias sem uma única violação em 475 transições, 18/25 batendo o melhor piso com 6 primeiros lugares absolutos e rank médio 4,84 — o 3º melhor config do main. Veredito 9,5 · ACEITAR; o que impede o 10 é que as identidades de CONTAGEM da cascata fecham em 81–90% e não em 100% (resíduo explicado, mas 1 ciclo em 1.967 — WFG5 g76 — segue sem causa provada), além do teto T de 2 itens."},{"config":"c217","familia":"classificador","stack":"MATLAB","n_celulas":25,"exclusoes":"","score":9.5,"recomendacao":"aceitar","pct_classe1":38.5,"pct_classe2":61.5,"pct_classe3":0.0,"denominador":26,"n_teto_T":3,"P1_protocolo":"✅ — F5.1 com 25/25 `accept[R1-c217]=VERDE · auditar=VERDE · 0 vermelhos` (dentro dos 13 configs MATLAB sem vermelhos), citado e não re-verificado; mas os três eixos foram REFEITOS por conta própria em escala: FE — `len(①)==31D−1 ∧ fe_final==maxfe ∧ manifest.maxfe==31D−1` em 25/25 (61 a 929), com `fe_index` E `solution_id` ambos densos 0-based (descarta buracos de escrita); CP-init — `fase=='init'`==11D−1 em 25/25 e `max|X_artefato − X_①|` sob cast float32 EXATAMENTE 0,0 em 25/25 (a v1.0 só citava a F5.1), `doe_hash` ≡ sidecar 25/25, `cp_init=true` 25/25 — é o patch `PCSAEA.m:30-31` que desfaz o `max(11D−1,N)` do código oficial e garante 21 pontos e não 50 nas 3 células D=2; camadas/cadência — 3.410 blocos com 2.000 linhas exatas e `ok=true` em 25/25, join posicional com `max|ΔX| = 0,0` em 3.410/3.410 blocos (a v1.0 provara só a geração 1).","P2_contrato":"⚠ — 1 defeito de contrato REAL e 1 doc-sync, ambos já escalados, ambos com fallback verificado e ZERO perda analítica. `contrato_f52b.csv`: 25/25 células com `camada 5 · CHAVES · faltam ['params']` — c217 é 1 dos 7 configs da não-conformidade transversal (item 6 do §0 do RELATORIO_F5); fallback verificado em escala: o header do ⑥ traz os params ACHATADOS em 25/25 (`{D, M, N, alg, algo, delta, doe_hash, gmax, maxfe, problema, regime, semente, ts}`, com N=50, delta=0,8, gmax=3000 ≡ D20/D17 e `doe_hash` ≡ sidecar). CONTRATO §6: header do ⑥ sem `run_id`/`ambiente`/`params`-dict/`sigma_dict` — doc-sync (item 5 do §0), tudo presente no ⑤. `integridade_f52a.csv`: 0 linhas c217 (25/25 íntegras). Mapa de término: `manifest.status='ok'` E `footer.termino='normal'` E `fe_final==maxfe` em 25/25 (não existe `motivo_parada`; a triagem por `status` sozinho é insuficiente — bug B1). É o ÚNICO item que impede a nota 10 pela régua do FRAMEWORK-v2, e não é defeito de mecanismo.","P3_mecanismo":"✅ — 29 aspectos, T=3, denominador 26: classe (1) 10 = 38,5% (A3, A6, A8, A9, A10, A11, A15, A16, A18, A19) · classe (2) 16 = 61,5% · classe (3) 0 = 0,0%. Movimento vs a v1.0 (25 aspectos, denominador 23, 39,1%/60,9%/0%): +4 aspectos novos (A6 tricotomia, A7 |Dvalid|, A17 n_Pmid, A26 elo ③↔①) e 1 promoção de T → (1) (A16, o laço interno Gmax, via assinatura de tempo). Os percentuais quase não se movem; o que muda é a FORÇA da evidência por trás de cada linha.","P4_saude":"⚠ — sem cabeça de valor (μ/σ/pred_classe 100% NULL em 6.827.034 linhas), logo WAPE e erro de fantasia são N/A POR DESENHO e o `sonda_f52e.csv` tem 0 linhas c217 (correto, não buraco). A régua é a ③ direta: AUC de Mann-Whitney do `pred_score` ternário contra rótulos de dominância no gabarito Sobol — 2.085 de 3.410 blocos são INFORMATIVOS (61,1%, em 20/25 células), com `AUC_bom` mediana 0,5195 (>0,5 em 77,6%) e `AUC_ND` mediana 0,5645 (>0,5 em 80,8%); os 1.325 degenerados são os 1.106 blocos das 5 WFG (frac(score=0)=1,000 em TODOS), 200 de 201 do MMF16_20 e 19 de 121 do DTLZ2 — degeneração COERENTE com o Error1 interno (`p⁺ ≈ 0` e contradição ~100%), i.e. saúde e não infidelidade. A régua que SOBE é o Error1: `p⁺` cresce do 1º ao 4º quartil de gerações em 17/25 células (BBOB 7/7 — F37 0,101 → 0,420). Trajetórias: IGD+ monotônico não-crescente em 475/475 transições, 0 violações, redução mediana 0,206× (melhor BBOB_F17 0,0103×). Custo: 0,547 h-core — o config MAIS BARATO da rodada por larga margem.","P5_canonica":"⚠ — nenhuma célula numericamente comparável (Botão 4), mas o SENTIDO executa em duas famílias e uma sobre-afirmação da v1.0 foi CORRIGIDA. DTLZ (paper m=3, d=15, 2.000 FEs, 30 runs): paper 6,9212e-2 / 7,8816e-2 / 1,9200e-1 / 2,9867e+1 / 6,7647e+1 contra nosso IGD+ 0,2045 / 0,6226 / 1,0540 / 85,75 / 389,43 em DTLZ2/4/7/1/3 — 5/5 POSIÇÕES IDÊNTICAS, Spearman ρ = 1,000, sob 2,9–9,3× MENOS orçamento, com a razão nosso/paper crescendo monotonicamente com a dificuldade (2,95× em DTLZ2 → 5,76× em DTLZ3), efeito esperado do corte de orçamento e não de mecanismo. WFG (a família mais próxima: d=22 contra 21, M=2 idêntico): com o valor do WFG5 corrigido para 1,1186e-1, dá 3/5 posições idênticas e ρ = 0,900 — a v1.0 afirmou 5/5 e a afirmação está ERRADA; a única inversão é o par adjacente WFG5↔WFG9, que no paper dista 19% e em nós 4%. A âncora `DTLZ2_d15` (m=3, semente 0, 464 FEs) foi auditada como CONTROLE INDEPENDENTE: 300 gerações, 100% estado 3, e TODAS as identidades fechadas do config batem 300/300 — prova de que as formas fechadas não são artefato da semente 42 nem dos nossos 25 problemas; o IGD cru 3,077e-1 fica 4,45× acima da média do paper com 4,31× menos orçamento, exatamente como o bundle antecipa. E há uma grandeza QUASE-comparável e favorável: a acurácia por amostra do surrogate, 0,5485 nosso contra 0,60417 do paper (91%; 97% no DTLZ3).","P6_vs_pisos":"⚠ — bate o melhor piso em 7/25 (MMF4 0,690× · MMF1 0,699× · BBOB_F55 0,788× · MMF16_20 0,842× · BBOB_F37 0,848× · DTLZ7 0,937× · ZDT6 0,972×); mais 10/25 acima porém DENTRO do piso de ruído entre máquinas (BBOB_F17 1,21× · DTLZ2 1,20× · WFG1 1,04× · WFG2 1,31× · WFG9 1,34× · ZDT4 1,32× · ZDT3 1,41× · BBOB_F1 1,41× · WFG5 1,44× · MMF11_L 1,55×); e 8/25 claramente abaixo (DTLZ3 3,00× · BBOB_F22 2,88× · DTLZ4 2,20× · BBOB_F49 2,16× · DTLZ1 1,89× · BBOB_F5 1,87× · ZDT1 1,77× · WFG4 1,72×). Rank médio 10,44/17 no main; melhores posições MMF1 3º, MMF16_20 3º, MMF4 5º, WFG1 6º. Roster homogêneo (25/25 vm3).","P7_adversarial":"N/A — 'Nada a enviar à F5.4'. ZERO aspectos classe (3), agora com 29 aspectos (contra 25 na v1.0) e com quatro queries novas que eram candidatas NATURAIS a achado (a tricotomia do Alg. 4, o `|Dvalid|`, o `n_Pmid` e o elo ③↔①): as quatro fecharam 100%. Restam apenas dois itens de DOC-SYNC para a F5.7, que não são classe 3 porque nada no log nem no comportamento está errado: a ambiguidade do símbolo `lnum` no bundle (identifica `lnum ≡ |Pmid|` mas o dado mostra `batch = ⌊|Pbest|/2⌋`, e as duas grandezas coincidem em N=50 e divergem na rampa) e o par ⑤-sem-`params` / header do ⑥ incompleto.","query_joia":"DUAS identidades fechadas a 6.762/6.762, que juntas provam o desenho amostral e a contabilidade de incerteza do PC-SAEA por DADO e não por leitura de código. (i) O SPLIT: `n_best == ⌈¾·⌈|P|/4⌉⌉` e `n_worst == ⌈¾·(⌈|P|/2⌉ − ⌈|P|/4⌉)⌉` — uma única query que prova o ⌈·⌉ do Alg. 2, a ASSIMETRIA do código (13/12 quando N=50) e o ¾ do Alg. 3 aplicado com TETO e não piso (com ⌊·⌋ reprovaria 6.762/6.762). (ii) A TRICOTOMIA da `Validation` (Alg. 4, letra por letra): `p⁺·|Dv| + p⁻·|Dv| + contradições ≡ |Dv|`, com `|Dv|` derivado do complemento exato do treino — valida SIMULTANEAMENTE o conjunto de validação, a contagem de contradições e a semântica dos dois p, e REFUTA a leitura ingênua `p⁻ = 1 − p⁺` (só vale em 198/6.762 = 2,9%).","prova_numerica_central":"6.762/6.762 (100,000%) nas duas identidades fechadas — split e tricotomia — MAIS a regra tripla recomputada da desigualdade (`p⁺>δ → 1`, `p⁻>δ → 2`, senão 3) em 6.762/6.762 e o lote em forma fechada `⌊⌈|P|/4⌉/2⌋` em 55/55 disparos; e as MESMAS formas fecham 300/300 na célula-âncora independente `DTLZ2_d15` (d=15, semente 0), provando que não são artefato da semente 42. O contrafactual da D17 é o número que sustenta a decisão: sob a fiação as-shipped os mesmos 6.762 pares (p⁺,p⁻) dariam estado 1 em 3.956 gerações (58,5%) contra as 38 (0,56%) observadas, divergindo em 5.932/6.762 = 87,7%.","achados_classe3":[],"caveats_d97":["A frase correta para o D97 é: 'o surrogate gateou 327 dos 7.034 FEs (4,6%); quando gateou pelo ramo DIRETO, o infill dominou 11% mais do arquivo que um sorteio contemporâneo' — NÃO 'o classificador guiou a busca'.","Declarar 'surrogate morto' ao ver 99,187% de estado 3 é erro: é ARITMÉTICA. Com `|Dvalid| = 6` e δ=0,8 o estado 1 exige k ≥ 5 acertos consistentes em 6, e a frequência empírica de k≥5 (0,57%) prevê a taxa observada (0,56%) com 1 casa decimal — e a acurácia do modelo é 91% da publicada pelo paper.","O teste PAREADO (327 gated contra vizinhança aleatória de ±25 gerações na mesma célula) dá 1,06× no agregado com Wilcoxon p = 0,0050; decomposto: estado 1 (usa) 1,11× e 153/228 vitórias, estado 2 (INVERTE) 0,92× e 39/99 — o 'uso reverso', que o próprio paper chama de elemento mais original, NÃO se reproduz como vantagem nos nossos runs (n=17 eventos; é observação de comportamento sob 31D−1, não veredito de fidelidade).","A D17 (2 guardas corrigindo a fiação condição↔ação AO PAPER) deixou de ser decisão declarada e virou número: sem ela, o estado 1 ANTI-GREEDY dispararia em 100,0% das gerações de WFG1 e WFG4, 99,8% de WFG2 e WFG5 e 95,0% de WFG9 — a família WFG inteira teria rodado selecionando deliberadamente os candidatos previstos como PIORES, com o lote saltando de 7.034 para 36.513 FEs (5,2×).","O classificador é NÃO-INFORMATIVO em toda a família WFG e em MMF16_20 (`frac(score=0) = 1,000` em 1.306 de 3.410 blocos; contradição ~100% da validação em 2.565 das 6.762 gerações) — e isso é COERENTE com a própria medida de confiabilidade do mecanismo, não infidelidade.","`pred_score == 0` no estado 3 é SENTINELA de modelo-não-consultado (o Alg. 5 L20 nem chama `Prediction`), não predição de empate: vale para 6.707 dos 7.034 infills (95,4%). Tratá-lo como 'o modelo previu indiferença' superestima a atividade do surrogate em 95,4%.","`pred_confianca` é SEMPRE o Error1 do ciclo (constante por bloco em 3.410/3.410), inclusive nas 17 gerações de estado 2 que dispararam por Error2 — não é confiança por-ponto nem 'a confiança do estado corrente'.","Divergências mantidas no CÓDIGO (K.3/DEF-B5.6) com efeito medido: pareamento TODOS×TODOS em vez de best×worst (`n_pares = n_tr(n_tr−1)` = 342 contra 180 do paper, 1,90× mais amostras — e pares best×best/worst×worst borram justamente a fronteira que o §2.2 diz querer nítida, hipótese mecânica mais provável para a acurácia abaixo da do paper), split assimétrico, spread da PNN 0,1925 contra 0,2 e SBX/PM η=15/5 contra 20.","`|Dvalid|` NÃO é `|Pbest^valid|` (o Alg. 3 impresso) — é `vb + vw`, daí a granularidade 1/6 e não 1/3; recompor `p⁺` com o denominador do paper reprova 6.762/6.762.","Contrato: ⑤ sem `params` em 25/25 é defeito REAL (1 dos 7 configs escalados), mas o fallback do header do ⑥ dá zero perda analítica — se o autor preferir pesar mais esse item, 9,0 permanece defensável e o veredito (aceitar) não muda."],"itens_torre_central":[{"item":"⑤ sem a chave `params` em 25/25 células — defeito de CONTRATO real (c217 é 1 dos 7 configs da não-conformidade transversal já escalada, item 6 do §0); fallback verificado no header achatado do ⑥, zero perda analítica, mas é o item que impede a nota 10","onde":"camada ⑤ (manifest) do c217 / CONTRATO §5 — item 6 do §0 do RELATORIO_F5","severidade":"media","custo":"acrescentar `params` ao manifesto; bump de repo_hash ⇒ tem de viajar no lote F5.7 antes do disparo M8/M9"},{"item":"Header do ⑥ sem `run_id`/`ambiente`/`params`-dict/`sigma_dict` (os 4 exigidos pelo CONTRATO §6) — doc-sync, mesmo padrão já escalado para o b4 (item 5 do §0); tudo está no ⑤, sem perda","onde":"header do ⑥ do c217 (e do b4) / CONTRATO §6","severidade":"baixa","custo":"doc-sync ou 1 campo no writer; se tocar o writer, bump de repo_hash"},{"item":"DESAMBIGUAR o símbolo `lnum` no bundle: ele identifica `lnum ≡ |Pmid|` (=13 para N=50) e usa `batch = ⌊lnum/2⌋`, mas o dado mostra `batch = ⌊|Pbest|/2⌋` e `|Pmid| = 2⌊|P|/8⌋+1` — coincidem em N=50 e DIVERGEM na rampa (|P|=22 ⇒ |Pmid|=5, |Pbest|=6, batch 3 medido). Com 30 sementes × D=2 as células de rampa se multiplicam por 30","onde":"bundle alg_c217 / SPEC_experimentos_v5.2.md + gen_bundles.py (nunca editar bundle à mão)","severidade":"media","custo":"edição de SPEC + regeneração dos bundles, 0 h-core"},{"item":"Registrar no protocolo que `dist_min_arquivo` é LISTA quando `lote>1` e ESCALAR quando `lote==1` (correspondência com `estado ∈ {1,2}` em 6.762/6.762) e que `real_solution_id` é `double` + NaN — `astype(float)`/`astype(int32)` diretos quebram nas 6,82 M linhas de sonda","onde":"CONTRATO_DE_DADOS (regra 2, dicotomia MATLAB) / queries da F5.3b","severidade":"baixa","custo":"edição de doc, 0 h-core"},{"item":"Sempre imprimir o DENOMINADOR de blocos informativos ao reportar AUC da sonda do c217: a AUC é 0,5 exato em 1.325 blocos por empate total (score≡0 em toda a WFG e em MMF16_20) e incluí-los derruba a mediana global de 0,5195 para 0,5000 sem que nada tenha mudado","onde":"protocolo de leitura da sonda para configs sem cabeça de valor","severidade":"baixa","custo":"edição de protocolo, 0 h-core"}],"celulas_para_rerun":[],"teto_T":["A27 — η SBX/PM = 15/5 (o paper fixa 20; K.3 mantém o código): nenhuma grandeza de operador é exportada — nem no header, nem no `modelo_hp`, nem na ③ — e o efeito não é separável do laço interno não gravado.","A28 — regime-NaN → ramo aleatório (DEF-L6): 0/6.762 ocorrências (busca por 'NaN' nas 6.762 strings de `motivo`); o ramo existe, está instrumentado e NÃO foi exercitado — só um problema de objetivo constante o acordaria.","A29 — fitness da Eq. 4 (SPEA2-raw + SDE com annealing r=FE/FEmax), os rótulos de cada par, a diversidade degenerada `D≡0,5` e o SDE inerte: nada é exportado; só `|P|` é inferível (via A4/A17). O proxy `dist_min_arquivo` cai em 23/25 células e `n_front1` sobe em 21/25 — compatível com convergência, mas não separa SDE ativo de inerte.","O gate ±0,95 como mecanismo DISTINTO do argmax: o score exportado é ternário {−1,0,+1} agregado, então `score > 0,95 ⟺ score == +1` e a verificação é necessária mas não suficiente; e dentro dele o ramo 'contagem do gate' contra o fallback ⌊lnum/2⌋ é indistinguível porque 0/55 lotes foram parciais.","O TEXTO das 2 guardas D17 (`SAS.m:21` → `error1>delta`; `:39` → `error2>delta`): o comportamento condição⇔ação está provado em 6.762/6.762, mas o diff exige `anchors.json`/`repos.lock`.","O CONTEÚDO do pool interno de `wmax` iterações (DEF-C2 grava só o lote final): Kendall-τ e contrafactual do pool são impossíveis — embora o LAÇO em si tenha saído do teto T (A16, promovido a classe (1) pela assinatura de tempo).","Não-perturbação da sonda sobre o RNG (plausível pelo determinismo do `newpnn`, não provada) e o spread EFETIVO do kernel da PNN (o `modelo_hp` é declaração do instrumentador)."],"capitulo_sintese":"O c217 executa o PC-SAEA de Tian et al. (SWEVO 2023), o exemplar-raiz da sub-família COMPARAÇÃO PAR-A-PAR com gestão de modelo por confiabilidade. Por geração ele ordena a população pelo fitness da Eq. 4, extrai ⌈|P|/4⌉ melhores e piores, pareia-os em amostras ⟨x,y,1⟩/⟨y,x,0⟩, treina do zero uma PNN de Specht (spread 0,1925, sem treino iterativo), valida cada par nas DUAS direções contando p⁺ (consistente-e-certo), p⁻ (consistente-e-invertido) e CONTRADIÇÕES, e aplica a regra tripla com δ=0,8: usar o modelo, INVERTÊ-LO (erro sistemático é recuperável) ou ignorá-lo — devolvendo ≤6 candidatos com margem |score|>0,95 nos dois primeiros casos e 1 aleatório no terceiro. As 25 células somam 6.762 gerações, 7.034 infills, 3.410 blocos de sonda e 6.827.034 linhas de ③, a apenas 0,547 h-core — o config mais barato da rodada —, mais 1 célula-âncora fora da rodada (`DTLZ2_d15`, semente 0, 300 gerações). O mecanismo está provado por DADO em duas identidades fechadas a 6.762/6.762: o desenho amostral (`n_best = ⌈¾⌈|P|/4⌉⌉`, `n_worst = ⌈¾(⌈|P|/2⌉−⌈|P|/4⌉)⌉`, que numa query só prova o teto do Alg. 2, a assimetria do código e o ¾ com ⌈·⌉) e a tricotomia do Alg. 4 (`p⁺|Dv| + p⁻|Dv| + contradições ≡ |Dv|`, que valida simultaneamente o conjunto de validação, as contradições e a semântica dos dois p, refutando `p⁻ = 1 − p⁺`, verdadeiro em só 2,9% das gerações) — e as MESMAS formas fecham 300/300 na célula-âncora independente. A D17, a decisão mais importante do config (2 guardas que corrigem AO ARTIGO a fiação condição↔ação invertida do PlatEMO 4.15), virou número: sob a fiação as-shipped os mesmos pares dariam estado 1 em 58,5% das gerações contra os 0,56% observados, divergindo em 87,7%, com WFG1/WFG4 rodando 100% ANTI-GREEDY. A dominância do estado 3 (99,187%) é o mecanismo ACERTANDO e não morrendo: com |Dv|=6 e δ=0,8 o estado 1 exige 5/6, e a frequência empírica de k≥5 (0,57%) prevê a taxa observada (0,56%) com uma casa decimal, enquanto a acurácia por amostra (0,5485) é 91% da publicada (97% no DTLZ3) sob 2,9–9,3× menos orçamento. O teste pareado mostra que a incerteza é load-bearing mas modesta: gated pelo ramo DIRETO domina 11% mais do arquivo que um sorteio contemporâneo (0,5870 × 0,5296, 153/228, Wilcoxon p=0,0050), enquanto o uso INVERTIDO — o elemento que o paper chama de mais original — não entrega ganho (0,92×, 39/99). Zero classe (3), 26/26 verificáveis fechando (38,5% classe 1 / 61,5% classe 2), canônica com DTLZ 5/5 (ρ=1,000) e WFG 3/5 (ρ=0,900, corrigindo uma sobre-afirmação da v1.0). Veredito 9,5 · ACEITAR sem caveat de fidelidade; o que impede o 10 é o teto T de 3 itens e o único defeito de contrato real do grupo — ⑤ sem `params` em 25/25, já escalado, com fallback verificado e zero perda analítica."},{"config":"c238","familia":"GP-BO","stack":"MATLAB","n_celulas":25,"exclusoes":"","score":9.5,"recomendacao":"aceitar+caveat","pct_classe1":40.0,"pct_classe2":60.0,"pct_classe3":0.0,"denominador":25,"n_teto_T":2,"P1_protocolo":"✅ — 25/25 verdes nos gates F5.1 (FE exato, CP-init bit-a-bit, camadas, cadência da sonda), citado; e o CP-init foi RE-EXECUTADO aqui por independência. FE: `len(①)==31D−1` em 25/25 (61 a 929, Σ=10.856), `fe_index` denso 0-based e `solution_id` único em 25/25, `fe_final==maxfe` e `footer.termino='normal'` em 25/25, `lote` sempre 1 ⇒ overshoot ZERO (q=1 é nativo). CP-init: `fase=='init'`==11D−1 em 25/25 e, arredondando o artefato float64 para float32 (D53), `max|ΔX| = 0,0 EXATO` em 25/25; `doe_hash` do manifesto ≡ recalculado do artefato ≡ sidecar em 25/25. Cadência: `g==1 ∨ g%2==0` + finalProbe em 25/25, 3.535 blocos com 2.000 pts exatos e `n_falhas=0`; join posicional provado bloco a bloco em 7.070.000 linhas (desvio máximo 1,907e-06 nas WFG = ULP-float32 × escala dos bounds).","P2_contrato":"✅ — `contrato_f52b.csv`: ZERO linhas para c238 (25/25 sem qualquer desvio estrutural: contagem da ①, fase init, schema de ①③④, ② na família sancionada, blocos de sonda 2.000×2, `fe_final==maxfe`, `sigma_dict` presente); c238 NÃO está entre os 7 configs da não-conformidade transversal do ⑤ sem `params` (verificado no dado: `manifest.params` traz as 12 chaves do bundle). `integridade_f52a.csv`: ZERO linhas (todos os ~175 arquivos abertos e parseados sem erro; ⑥ com footer em 25/25, `footer_n=1`, `status='ok'`, `cp_init=true`, `termino='normal'`). Não há nada 'por desenho × defeito' a arbitrar porque não houve achado; as duas anomalias reais do config são METROLÓGICAS e não de contrato — o split de máquinas (O-19) e o teto float32 da ① nas 3 células de objetivo saturante.","P3_mecanismo":"✅ — 27 aspectos, T=2, denominador 25: classe (1) 10 = 40,0% (U2, U3, U8, E1, E2, E3, E4, E5, E6, E7 — a maior fração classe (1) do grupo) · classe (2) 15 = 60,0% · classe (3) 0 = 0,0%. Nota de contagem: A1/A2/A3 são achados DERIVADOS de aspectos já contados (U10/E10/E7) e entram como sub-itens do mesmo aspecto para não inflar o denominador. Decisões sancionadoras citadas: D21/D61, D63/D88, D53, D57/D89, D30 + DEF-B10.2, B10.7, DEF-C2, DEF-C3, DI-10, DI-12.5, DI-13.10, DI-18, N.5/L.9/I.9/E.5, D81.","P4_saude":"✅ — a sonda APRENDE e a calibração é boa: ΔWAPE mediano −2,7% com melhora em 13/25 células (coerente com o 'quase-plano −2%' da régua oficial), cobertura ±1,96σ mediana 0,911 → 0,916 (9 células terminam <0,90) e ZERO σ-NaN em 100% dos blocos (`n_validas`=2.000 sempre) — o recomputo independente das 7.926 medições reproduz o `sonda_f52e.csv` com max|ΔWAPE| = 5,0e-07 e Δcobertura = 0,0 EXATO. Prior 7,5 v2 CONFIRMADO em escala: DTLZ2 obj₂ vai de 9,79% para 0,98% com cobertura 0,837 → 0,999; e a degeneração numérica do MMF1 CONFIRMA-SE e é 5,5× MAIOR do que a sonda-piloto viu (obj₁ WAPE 0,722 → 1,718, |μ|máx 1.882,3 contra os 339 do prior, cobertura 0,940 → 0,594, com θ no bound). Trajetórias impecáveis: IGD+ monotônico não-crescente em 25/25 células, 0 violações em 475 transições, ganho mediano 18,0× (máximo 217,9× em ZDT6). Leitura cruzada valiosa: as duas células de correlação terminal quase perfeita e WAPE ~1e-4 (BBOB_F1 e DTLZ7) são exatamente as que exibem o extremo do EIM saturado — modelo perfeito demais ⇒ critério morto.","P5_canonica":"⚠ — comparável 0 · direcional-QUALITATIVA 3/3 confirmadas · incomensuráveis 25 · nenhuma declaração de 'artigo melhor' (Botão 4 respeitado). As comparações do paper são todas INTERNAS (EIMe/EIMm/EIMh × EIe/EIm/EIh) e não há MOEA na bateria dele, e como o driver hardcoda EIMe temos só 1 dos 6 braços — o que ELIMINA todos os vereditos direcionais internos do artigo. Restam três âncoras qualitativas, e as três confirmam: (i) §VI-D recomenda EIMe/EIMm para FRENTES DISCRETAS — DTLZ7 dá IGD+ 0,1095 contra 1,1247 do melhor piso (razão 0,097, 10,3× melhor) e ZDT3 dá 0,1048 contra 0,3443 (razão 0,305); (ii) §V-A confessa que o EGO tem desempenho ruim onde o Kriging não aproxima bem, e por isso o paper escolheu deliberadamente só suítes fáceis — nosso grid tem os três casos que ele EVITOU (DTLZ1, DTLZ3, ZDT4) e eles são EXATAMENTE os 3 piores resultados absolutos do config (IGD+ 71,5 · 226,4 · 51,5, HV=0 nos três) e 3 das 10 células em que o c238 perde do melhor piso. Faixas-guia da âncora J registradas SEM veredito (HV/IGD do paper são não-normalizados em n=6).","P6_vs_pisos":"✅ — bate o melhor dos 4 pisos em 15/25 (denominador 25); rank IGD+ médio 6,88 de 17 — o 6º melhor config da bateria (atrás de c262 3,71, c122 4,72, c141 4,84, e74 6,08 e b3 6,36; à frente de b1 7,65, c154 8,91 e dos 4 pisos) — com TRÊS primeiros lugares absolutos (MMF11_L, MMF16_20, WFG5). Vitórias decisivas (razão ≤0,33): ZDT6 0,006 · BBOB_F5 0,085 · DTLZ7 0,097 · BBOB_F55 0,112 · BBOB_F1 0,203 · ZDT1 0,213 · ZDT3 0,305 · WFG5 0,324 — o regime suave / frente descontínua / DoE informativo, exatamente onde o paper posiciona o método. Derrotas decisivas (≥1,58): DTLZ4 2,72 · WFG4 2,42 · BBOB_F49 2,04 · BBOB_F17 1,82 · DTLZ3 1,74 · DTLZ1 1,58 — multimodais e WFG deceptivas. CAVEAT DE MÁQUINA: as 7 células Mac (DTLZ7, MMF16_20, WFG1/2/4/5/9) incluem 3 das vitórias decisivas e 2 derrotas; razões dentro do piso O-18 não são lidas como sinal — só sobram as ≤0,33 e ≥1,7.","P7_adversarial":"N/A — 'Nada segue à verificação adversarial F5.4 por este config'. ZERO aspectos classe (3). Os quatro não-fechamentos brutos da bateria foram TODOS resolvidos dentro da análise com causa provada: (1) 463 gerações com `n_front` discordante = quantização float32 da ① em objetivos saturantes (DTLZ4 com 228 componentes exatamente 0 e min|f|≠0 = 1,401e-45, o menor subnormal float32; ZDT6 com 23 linhas de mesmo f₀; MMF11_L com 1 ponto de fronteira por geração) — e a identidade EIM fecha MESMO ASSIM porque `min_j‖EI^j‖` é invariante a réplicas na frente, com bracket de largura numérica ZERO; (2) 131 gerações fora do critério RELATIVO = `eim_best` minúsculo (ZDT6 mediano 3,64e-05) contra piso absoluto float32 constante; (3) 32% de argmax≠infill = pool final ACHATADO (razão mediana/máximo 0,999999) com desempate abaixo da precisão de export; (4) `eim_best` não-monotônico em 47% das transições = REMODELO do GP (80,7%), não a re-escala (4,0%).","query_joia":"A identidade fechada do EIM Euclidiano, verbatim do Apêndice A do paper: `lamda = (F − 1*u)./(1*s)`, `EIM = (F − 1*u).*gausscdf(lamda) + 1*s.*gausspdf(lamda)`, `y = min(sqrt(sum(EIM.^2,2)))` — reimplementada em float64 e aplicada geração a geração nas 25 células, sobre o pool final da ③ contra a frente ND reconstruída da ① e re-escalada com o `{min,range}` da PRÓPRIA iteração. Fecha 7.020/7.020, i.e. a fórmula que gerou cada uma das 7.020 decisões é, bit a bit dentro do float32, a equação (22)+(23) do artigo. Complementada pelos dois contrafactuais que provam que a incerteza é load-bearing: com σ→0⁺ o argmax MUDA em 5.316/7.020 (75,7%) das iterações, e a monotonicidade N2 (σ↑ ⇒ EIM↑) vale para TODOS os pontos do pool em 7.020/7.020 gerações.","prova_numerica_central":"7.020/7.020 (100%) com |Δ| < 1e-5 na identidade do EIMe — |Δ| MEDIANO 2,17e-09, p99 7,0e-08, máximo 5,81e-06; pelo critério relativo fecham 6.889/7.020 (98,13%) e, restringindo às gerações em que a frente ND foi reconstruída EXATAMENTE (n=6.557), o relativo fecha 6.556/6.557 (99,98%). A prova irmã da propriedade §IV-D do paper: 0 de 1.299.400 linhas de pool coincidem com um ponto avaliado ANTERIORMENTE, `min_dist_infill` com mínimo global 1,508e-03 (0 gerações abaixo de 1e-8) e `dup_infill = 0` em 25/25.","achados_classe3":[],"caveats_d97":["CAVEAT DE LEITURA 1 — em BBOB_F1 o critério SATURA em 0 EXATO em 189 das 200 gerações (94,5% da célula, a partir da 8ª): σ NÃO é zero (está entre 4,9e-05 e 1,9e-03), mas |λ| ≥ 184,7 e Φ(−184)/φ(−184) underflowam para 0 em IEEE-754 double. Aquela célula não mede EIM, mede DoE + busca quase-aleatória dentro da caixa — e ainda assim termina com IGD+ 0,0276, rank 4/17 e ganho 60×, porque a esfera é fácil. É comportamento FIEL (a fórmula do Apêndice A tem exatamente essa propriedade), mas toda análise de 'o critério guiou?' precisa filtrar esse regime.","CAVEAT DE LEITURA 2 — c238 é o ÚNICO config main com máquinas MISTAS (7 células Mac: MMF16_20, DTLZ7, WFG1, WFG2, WFG4, WFG5, WFG9; 18 na vm3), rompendo a O-16 internamente: qualquer leitura bit-a-bit entre suas células é inválida, só indicadores comparam e só acima do piso O-18 (HV ≤1,55%, IGD+ ≤58,98%).","A propriedade CENTRAL do paper (§IV-D, amostragem densa) foi confirmada empiricamente PELA PRIMEIRA VEZ em escala, e o remédio anti-clustering que o repo NÃO implementa (B10.7) simplesmente não fez falta: 0 de 1.299.400 linhas de pool coincidem com ponto antigo e nenhuma geração chega sequer a 1e-6 de distância.","A única divergência MATERIAL é o otimizador interno (D30/DEF-B10.2): GA nativo do repo (pop=10D × 200 gerações = 2.000·D avaliações por infill, total 259.880.000) no lugar do DE/rand/1/bin do paper (10.000 fixas) — i.e. 2× a 6× MAIS busca interna em D≥10, e menos só nas três células D=2. O efeito colateral genuíno é o colapso de diversidade em D=2 (até 70,1% do pool final é o vencedor replicado no MMF1), sem custo em FE.","DI-18 PRECISA SER CORRIGIDO no dossiê: o ruído/salto do `eim_best` vem do REMODELO do GP (80,7% das 3.292 subidas), não da re-escala por iteração (4,0%) nem da troca de frente (15,3%); no ZDT1, 9 dos 10 maiores saltos ocorrem sem qualquer mudança de escala ou de tamanho da frente. Vale para toda a família BO com MLE re-otimizada.","DI-10 é QUASE REDUNDANTE neste config: `eim_mediana_pool/eim_best` = 1,000000 na 1ª geração de 25/25 células e ≥0,9946 na última em 24/25, porque o GA interno converge — o sinal útil passa a ser o contraste ENTRE células (decai em 17/25, SOBE em 8/25, e as que sobem são as multimodais/enganosas).","O erro de fantasia do infill é otimista em 73,8% global (cobertura 2σ 76,3%), mas isso mede o VIÉS DE SELEÇÃO do EI e não um σ mal calibrado — a sonda, que amostra sem seleção, dá cobertura 0,916.","A ③ do c238 mistura DOIS espaços no mesmo arquivo: linhas online em `espaco_modelo='transformado'` com `transf_params={min,range}` da iteração, e linhas de sonda já em `'cru'` com `transf_params=NULL` — des-transformar a sonda (ou não des-transformar o online) corrompe qualquer WAPE/cobertura.","`f_best` e `n_front1` são campos PÓS-infill (`f_best(g) ≡ norm_min(g+1)` e `n_front1(g) ≡ n_front(g+1)`, ambos em 6.995/6.995): lê-los como pré-infill erra por uma iteração inteira — é a armadilha estrutural gêmea do off-by-one da ② no b1.","ZDT1 é a célula mais cara de TODO o cluster MATLAB (14.284,6 s = 3 h 58), e a decomposição de tempo é fit 70% / busca 29% / sonda 0,6% / avaliação real 0,003% — a premissa 'expensive' do paper está invertida no nosso benchmark analítico: o surrogate custa 35.000× mais que a função."],"itens_torre_central":[{"item":"Corrigir no dossiê a atribuição causal do DI-18: o ruído/salto do `eim_best` vem do REMODELO do GP (80,7% das subidas), não da re-escala por iteração (4,0%) — e a correção vale para toda a família BO com MLE re-otimizada por iteração","onde":"dossiê DI-18 / SPEC + gen_bundles.py (nunca editar bundle à mão)","severidade":"media","custo":"edição de doc, 0 h-core, sem re-run"},{"item":"Registrar no protocolo que a identidade do critério do c238 exige tolerância ABSOLUTA (|Δ|<1e-5) e NUNCA a relativa, em células cujo `eim_best` desce a 1e-05–1e-28 (ZDT6, ZDT3, ZDT1, DTLZ7): o piso absoluto do export float32 é ~2e-09 e é constante, o relativo é artefato do denominador","onde":"PROTOCOLO_ANALISE_FIDELIDADE.md §5 / queries de identidade da família BO","severidade":"media","custo":"edição de protocolo, 0 h-core"},{"item":"Fixar a máquina do c238 para as 30 sementes e documentar o split O-19 (7 Mac / 18 vm3) na MATRIZ-MESTRA: é o único config main com a O-16 rompida internamente, e isso limita qualquer leitura fina inter-células","onde":"roster de execução M8/M9 + f5/tempo_f52d.csv (coluna `maquina`, já corrigida nas recuperações O-19)","severidade":"media","custo":"planejamento de disparo; 0 h-core agora"},{"item":"Filtrar o regime de saturação do EIM (`eim_best == 0` por underflow de cauda, |λ|≥184,7) antes de qualquer análise agregada de 'o critério guiou a busca' — hoje isso são 189 gerações de BBOB_F1, 94,5% da célula","onde":"queries da F5.5/D97 sobre papel da incerteza (família BO)","severidade":"media","custo":"1 filtro por query, 0 h-core"},{"item":"Reavaliar a utilidade do `eim_mediana_pool` (DI-10) neste config: a série é numericamente a mesma do melhor (razão 1,000000 na 1ª geração de 25/25), então o indicador não separa 'o melhor decaiu' de 'o pool inteiro decaiu' como foi desenhado","onde":"DI-10 / instrumentação c238","severidade":"baixa","custo":"revisão de doc; nenhuma mudança de código necessária"},{"item":"Manter DELIBERADAMENTE não consertado o crash latente stock (`Infill_EIM.m:25` → `Optimizer_GA.m:17`, frente ND de 1 ponto colapsa a escalar), por decisão de fidelidade do autor (⟦v5.2.1⟧, D81): não foi exercitado (`n_front` tem mínimo global 2 e `n_front==1` ocorreu 0 vezes em 7.020), mas segue como risco conhecido para as 30 sementes","onde":"Infill_EIM.m:25 / Optimizer_GA.m:17 (repo oficial do c238)","severidade":"baixa","custo":"0 (decisão de NÃO agir, com sentinela já instalada)"}],"celulas_para_rerun":[],"teto_T":["T1 — não-perturbação da sonda (invariante 🔴 do CONTRATO §3.1): exige run pareado com e sem sonda; nenhum rastro nos dados a decide.","T2 — operadores internos do GA (SBX dis_c=10 por-variável com p=0,5, PM dis_m=20 com pm=1/D, torneio k=2, seleção (μ+λ)) e as ~200 populações intermediárias: a ③ guarda APENAS a população final.","As 259.880.000 avaliações da aquisição: só o pool final sobrevive, o resto é irrecuperável.","O miolo do `fmincon` sqp (trajetória, nº efetivo de avaliações do MaxFunEvals=20D, critério de parada): só θ final e lnL são logados — embora os BOUNDS sejam diretos (0 violações em 15.740 componentes-geração, 6.260 saturações no piso 1e-3 e 1.133 no teto 1e3).","A prova de que a frente FLOAT64 que o MATLAB realmente usou é a que reconstruímos nas 3 células saturantes (DTLZ4, ZDT6, MMF11_L): a ① é float32 (D53) e a frente não é bit-recuperável — o que É recuperável (o valor do critério) fecha, com bracket de largura numérica zero.","Contabilidade de RNG (`lhsdesign` do DoE e do init do GA, `randi`/`rand` dos operadores)."],"capitulo_sintese":"O c238 executa o EIM de Zhan, Cheng & Liu (TEVC 2017) — EGO multiobjetivo SEQUENCIAL q=1 — sobre um embrulho `classdef EIM < ALGORITHM`: DoE LHS de 11n−1 e, por iteração, um Kriging INDEPENDENTE por objetivo sobre os objetivos re-escalados min-max a [0,1], a frente ND do arquivo, a montagem da matriz EI k×m para cada candidato (eq. 22) e a agregação Euclidiana `y = min_j ‖EI^j‖₂` (eq. 23), maximizada sobre a caixa por um otimizador interno até render exatamente 1 FE real. As 25 células somam 7.020 iterações EGO, 10.856 FEs reais, 1.299.400 linhas de pool, 7.070.000 linhas de sonda em 3.535 blocos, 259.880.000 avaliações da aquisição e 15.740 componentes-geração de θ, a 16,75 h-core — com ZDT1 (3 h 58) sendo a célula mais cara de todo o cluster MATLAB. A query-joia fecha 7.020/7.020 com |Δ| < 1e-5 e mediana 2,17e-09: a fórmula do Apêndice A do artigo é, comprovadamente, a que decidiu cada um dos 7.020 infills. E a incerteza é LOAD-BEARING por dois contrafactuais independentes: com σ→0⁺ o argmax muda em 5.316/7.020 iterações (75,7%, chegando a 97,3% em WFG4) e a monotonicidade N2 do §IV-C vale para todos os pontos do pool em 7.020/7.020 gerações. Mais forte ainda, a propriedade TEÓRICA central do paper (§IV-D, amostragem densa) foi confirmada empiricamente pela primeira vez em escala: 0 de 1.299.400 linhas de pool coincidem com um ponto antigo, `min_dist_infill` tem mínimo global 1,508e-03 e `dup_infill = 0` em 25/25 — o remédio anti-clustering que o repo oficial NÃO implementa (B10.7) não fez falta. Os desvios têm todos decisão citável e assinatura medida: kriging PRÓPRIO estilo Forrester no lugar do DACE, MLE por `fmincon` sqp single-start (0 violações de bound em 15.740 componentes, 6.260 saturações no piso e 1.133 no teto, lnL subindo em 25/25 mas ficando abaixo do máximo da própria trajetória em 25/25 — assinatura clara de ótimos locais), e a ÚNICA divergência material, o GA interno (pop=10D × 200 gerações) no lugar do DE do paper, que na verdade entrega 2× a 6× MAIS orçamento de aquisição em D≥10. Zero classe (3), 25/25 verificáveis fechando com a maior fração classe (1) do grupo (40,0%), contrato 100% limpo em ambas as fases, calibração boa (ΔWAPE −2,7%, cobertura 0,911→0,916, zero σ-NaN), trajetórias sem violação em 475 transições e 15/25 batendo o melhor piso com rank 6,88/17 e 3 primeiros lugares absolutos. Veredito 9,5 · ACEITAR, com dois caveats de LEITURA e nenhum de fidelidade: em BBOB_F1 o critério satura em 0 exato em 189 de 200 gerações por underflow de cauda (|λ| ≥ 184,7 com σ ainda positivo), de modo que aquela célula mede DoE mais busca quase-aleatória e não EIM; e o c238 é o único config main com máquinas MISTAS (7 Mac / 18 vm3, O-19), o que proíbe leitura bit-a-bit entre células."}]